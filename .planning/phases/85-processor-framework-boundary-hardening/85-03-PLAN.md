---
phase: 85-processor-framework-boundary-hardening
plan: 03
type: execute
wave: 3
depends_on: [85-01, 85-02]
files_modified:
  - src/BaseProcessor.Core/Processing/BaseProcessor.cs
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
  - src/Processor.Sample/SampleProcessor.cs
  - tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs
  - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs
  - tests/BaseApi.Tests/Processor/PrePipelineFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
autonomous: true
requirements: [PB-03]

must_haves:
  truths:
    - "Entry/source deletion is framework-owned on BOTH completion paths — the Mode-1 inline tail AND the Mode-2 null-return path — with the keeper DELETE escalation preserved"
    - "The Guid.Empty source net-effect is unchanged: the null-path delete is guarded by !SourceStep.IsSource, so a Guid.Empty seed is skipped (today's DeleteEntry no-op == tomorrow's skip; both leave nothing)"
    - "The concrete DeleteEntry API, SeamState.EntryId, SeamState.EscalateDelete, and the SetSeamState entryId/escalateDelete params are removed; the SampleProcessor no longer calls DeleteEntry"
    - "The author's only job on the Mode-2 path is spawn N + return null — no ack/nack/delete/escalation awareness"
  artifacts:
    - path: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs"
      provides: "Framework-owned delete-then-escalate on the null-return path (reusing the Mode-1 tail logic)"
      contains: "SourceStep.IsSource"
    - path: "src/BaseProcessor.Core/Processing/BaseProcessor.cs"
      provides: "DeleteEntry / SeamState.EntryId / SeamState.EscalateDelete removed"
    - path: "src/Processor.Sample/SampleProcessor.cs"
      provides: "Mode-2 entry path returns null after spawns with NO author DeleteEntry call"
  key_links:
    - from: "ProcessorPipeline null-return branch (dr is null)"
      to: "L2 KeyDelete → KeeperDelete escalation"
      via: "delete-then-escalate under !SourceStep.IsSource, AFTER succeeded spawns"
      pattern: "SourceStep\\.IsSource"
---

<objective>
PB-03: move entry/source deletion ownership entirely into the framework. Reuse the Mode-1 tail's
delete-then-escalate (`ProcessorPipeline.cs:222-227`) on the Mode-2 null-return branch (`:206`) — AFTER
the succeeded spawns — preserving the `!SourceStep.IsSource(d.EntryId)` skip so the net delete effect for
the `Guid.Empty` seed is identical to today (D-04). Remove the concrete `DeleteEntry` API
(`BaseProcessor.cs:142-148`), `SeamState.EntryId` (:54), `SeamState.EscalateDelete` (:65), the `SetSeamState`
`entryId`/`escalateDelete` params (:182-204), the `SampleProcessor.DeleteEntry()` call (`SampleProcessor.cs:72`),
and the pipeline's `escalateDelete:` closure (`ProcessorPipeline.cs:286`). Update every dependent test in
lockstep (this is one atomic compile unit — the `SetSeamState` signature change breaks all callers at once).

Purpose: shrink the concrete-processor surface to exactly "interpret input → business logic → return
DataResult (or spawn N + return null)". Deletion capability is NOT lost — it moves inline to the pipeline
null path. Ordering is enforced by D-01: a spawn exhaust throws (Plan 01) → never reaches `return null` →
nack (Plan 02) → no delete.
Output: the removals, the framework-owned null-path delete, and the flipped/updated/removed hermetic facts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-processor-framework-boundary-hardening/85-CONTEXT.md
@.planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md
@.planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md

<interfaces>
<!-- The Mode-1 delete-then-escalate block to reuse (ProcessorPipeline.cs:220-227): -->
- guard `if (!SourceStep.IsSource(d.EntryId)) { var del = await RetryLoop.ExecuteAsync(() => db.KeyDeleteAsync(L2ProjectionKeys.ExecutionData(d.EntryId)), limit, ct); if (!del.Succeeded) await SendKeeper(BuildDelete(d), limit, ct); }`
<!-- db / limit / SendKeeper / BuildDelete(d) are ALL already in RunAsync scope — no new plumbing. -->

<!-- The null-return branch to graft onto (ProcessorPipeline.cs:206): -->
- `if (dr is null) { LogHopExecuted(d, messageId, nameof(StepOutcome.Completed)); return; }`
<!-- D-04 target: keep LogHopExecuted, then run the delete-then-escalate block BEFORE return. -->
<!-- Discretion (Open-Q 1): extract a private DeleteEntryTail(d, db, limit, ct) called from BOTH the Mode-1 tail and the null path (RESEARCH recommends), OR inline. -->

<!-- SourceStep.IsSource(Guid) => entryId == Guid.Empty (Messaging.Contracts/SourceStep.cs:8). -->

<!-- REMOVALS RIPPLE MAP (85-PATTERNS.md §): -->
<!-- BaseProcessor.DeleteEntry() :139-148 → callers SampleProcessor.cs:72, DispatchTestKit.cs:92 -->
<!-- SeamState.EntryId :54 (read only by DeleteEntry) ; SeamState.EscalateDelete :65 (read only by DeleteEntry) -->
<!-- SetSeamState params entryId/escalateDelete :182-185 + assignments :195/:202 -->
<!-- Callers of SetSeamState: ProcessorPipeline.cs:269-286 (entryId: :271, escalateDelete: :286 closure — REMOVE both; KEEP onSpawnDropped :277-285) ; BaseProcessorSeamFacts.WireSeam :50-59 + inline :163-172 ; SampleProcessorFacts.WireSeam :37-46 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the DeleteEntry surface from BaseProcessor (method, SeamState fields, SetSeamState params)</name>
  <files>src/BaseProcessor.Core/Processing/BaseProcessor.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Processing/BaseProcessor.cs (DeleteEntry :139-148; SeamState :49-66 — EntryId :54, EscalateDelete :65; SetSeamState :182-204; class doc :19-28; NewResult :159-177 — confirm it does NOT read EntryId)
    - .planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md (§ BaseProcessor.cs PB-03 removals table; § Removals Ripple Map)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ SEAM 4; CR-01 isolation NOT affected)
  </read_first>
  <action>
    Remove the `DeleteEntry()` method (:139-148) entirely. Remove `SeamState.EntryId` (:54) and
    `SeamState.EscalateDelete` (:65) — both are read ONLY by `DeleteEntry`. Remove the `entryId` and
    `escalateDelete` parameters from `SetSeamState` (signature :182-185) and their `SeamState` assignments
    (`EntryId = entryId` :195, `EscalateDelete = escalateDelete` :202). KEEP everything else on `SeamState`
    (`Db`/`SendProvider`/`RetryLimit`/`ProcessorId`/`MessageId`/`WorkflowId`/`StepId`/`CorrelationId`/
    `OnSpawnDropped`) and the `AsyncLocal<SeamState?>` model, `SetSeamState` fresh-object publish, and
    `ClearSeamState` — CR-01 isolation is NOT affected (`NewResult` does not read `EntryId`). Update the class
    doc-comment lines describing author-owned `DeleteEntry` (:22-28) to state deletion is framework-owned.
    This alone will break the compile of `ProcessorPipeline`, `SampleProcessor`, and the test kit — those are
    fixed in Tasks 2-3 of this same plan (atomic unit).
  </action>
  <acceptance_criteria>
    - `grep` finds NO `DeleteEntry`, NO `EntryId` field on `SeamState`, NO `EscalateDelete` in `BaseProcessor.cs`.
    - `SetSeamState` signature no longer declares `entryId` or `escalateDelete` params.
    - `SeamState` still declares `MessageId`/`WorkflowId`/`StepId`/`CorrelationId`/`ProcessorId`/`OnSpawnDropped` and the `AsyncLocal` model is intact.
    - (Solution-wide build is NOT expected to pass until Tasks 2-3 land — this is one atomic compile unit.)
  </acceptance_criteria>
  <verify>
    <automated>grep -c -E "DeleteEntry|EscalateDelete|public Guid EntryId" src/BaseProcessor.Core/Processing/BaseProcessor.cs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Move deletion into the pipeline null path + remove the SampleProcessor DeleteEntry call</name>
  <files>src/BaseProcessor.Core/Processing/ProcessorPipeline.cs, src/Processor.Sample/SampleProcessor.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (null branch :206; Mode-1 delete tail :220-227; SetSeamState wiring :267-287 — entryId: :271, escalateDelete: :286, onSpawnDropped :277-285; SendKeeper :293-305; BuildDelete :319-321)
    - src/Processor.Sample/SampleProcessor.cs (Mode-2 entry path :57-74 — SpawnToPost loop :70, DeleteEntry call :72, return null :73; class doc :8-30)
    - .planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md (§ ProcessorPipeline.cs Analog 3 + D-04 target; § SampleProcessor.cs; § Delete-then-escalate tail)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ SEAM 3; Pitfall 4 — Guid.Empty net-effect; Open Question 1)
    - .planning/phases/85-processor-framework-boundary-hardening/85-VALIDATION.md (Manual-Only A1 — scheduler seeds Guid.Empty)
  </read_first>
  <action>
    In `ProcessorPipeline.cs`: (a) drop the `entryId: d.EntryId` arg (:271) and the entire `escalateDelete:`
    closure (:286) from the `SetSeamState` wiring — KEEP the `onSpawnDropped` closure (:277-285) verbatim (it
    is the preserved PB-01 telemetry). (b) On the null-return branch (:206), keep `LogHopExecuted`, then run the
    delete-then-escalate block from :222-227 (`!SourceStep.IsSource(d.EntryId)` → `RetryLoop` `KeyDeleteAsync(
    L2ProjectionKeys.ExecutionData(d.EntryId))` → on exhaust `SendKeeper(BuildDelete(d), limit, ct)`) BEFORE
    `return`. Recommended (Open-Q 1 / D-04 discretion): extract a private
    `DeleteEntryTail(EntryStepDispatch d, IDatabase db, int limit, CancellationToken ct)` from :222-227 and call
    it from BOTH the Mode-1 tail and the null path (DRY, reads as framework-owned deletion at both sites);
    inlining is also acceptable. `db`/`limit`/`SendKeeper`/`BuildDelete(d)` are already in `RunAsync` scope — no
    new plumbing. Do NOT delete on a `Guid.Empty` source (the `!IsSource` guard skips it — net-effect identical
    to today's no-op `DeleteEntry`, Pitfall 4).

    In `SampleProcessor.cs`: remove line 72 (`await this.DeleteEntry();`) ONLY. Leave the `SpawnToPost` loop
    (:70) and `return null;` (:73) unchanged (the author writes NO catch — the spawn-exhaust throw propagates
    transparently, D-01). Update the doc-comments (:16-19, :27-28) that describe author-owned `DeleteEntry`/
    "swallow" to "framework owns the entry delete; SpawnToPost fails loud on transient exhaust".
  </action>
  <acceptance_criteria>
    - `ProcessorPipeline.cs` SetSeamState wiring no longer passes `entryId:` or an `escalateDelete:` closure; the `onSpawnDropped:` closure is unchanged.
    - The null-return branch performs the `!SourceStep.IsSource(d.EntryId)`-guarded delete-then-escalate (inline or via a shared `DeleteEntryTail` helper called from both sites) AFTER `LogHopExecuted`, before `return`.
    - `SampleProcessor.cs` no longer contains `DeleteEntry`; the Mode-2 path still spawns then `return null`.
    - `dotnet build` of `BaseProcessor.Core` + `Processor.Sample` succeeds 0-warning (Release + Debug).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "DeleteEntry" src/Processor.Sample/SampleProcessor.cs; grep -c "SourceStep.IsSource" src/BaseProcessor.Core/Processing/ProcessorPipeline.cs; dotnet build src/Processor.Sample/Processor.Sample.csproj -c Release --nologo 2>&1 | grep -E "Build succeeded|error|warning"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update the hermetic tests in lockstep — flip, re-express, add, and remove the DeleteEntry surface</name>
  <files>tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs, tests/BaseApi.Tests/Processor/PrePipelineFacts.cs, tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs, tests/BaseApi.Tests/Processor/DispatchTestKit.cs</files>
  <behavior>
    - SampleProcessor entry: still spawns two distinct-execId completed results + returns null, but at the seam level (author-only, no pipeline) NO delete fires — `db.DidNotReceive().KeyDeleteAsync(...)`.
    - Pipeline null-return on a `Guid.Empty` (source) dispatch: NO entry delete (IsSource skip) — net-effect unchanged.
    - Pipeline null-return on a NON-source (non-empty entryId) dispatch: framework DOES delete `L2[entryId]`; a delete-exhaust escalates to exactly one `KeeperDelete`.
    - Deterministic-fault negative controls (`MalformedPayload_DeserFailure_*`, `SeamThrows_Unexpected_Failed`) still ack as `StepFailed`.
  </behavior>
  <read_first>
    - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs (Entry fact :48-80 — entryId :54, Received(1) :79; WireSeam :34-46)
    - tests/BaseApi.Tests/Processor/PrePipelineFacts.cs (SeamReturnsNull :126-144 — entryId :130, DidNotReceive :143; Completed...DeletesEntry :148-170; Completed_DeleteExhaust :172-187 — structures to mirror for the new non-source fact)
    - tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs (WireSeam :46-61; DeleteEntry_Success :114-132; ConcurrentConsumes :142-199; DeleteEntry_DeleteExhaust :201-221)
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (FakeProcessor.DeleteEntryAsync :92; Mode-2 delegate ctor :72-79; ReadWriteDeleteOkL2 :197-206; ReadOkDeleteFaultL2 :286-297)
    - .planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md (§ SampleProcessorFacts.cs; § PrePipelineFacts.cs UPDATE + NEW; § BaseProcessorSeamFacts.cs REMOVE/RE-EXPRESS; § DispatchTestKit.cs)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ Existing tests that WILL break — items 2-6; § Validation Architecture PB-03 rows)
  </read_first>
  <action>
    Drop the `entryId`/`escalateDelete` args from every `SetSeamState` call and every `WireSeam` helper now
    that `BaseProcessor.SetSeamState` lost those params: `SampleProcessorFacts.WireSeam` (:34-46),
    `BaseProcessorSeamFacts.WireSeam` (:46-61), and the inline `SetSeamState` in `ConcurrentConsumes`
    (:163-172). Remove `DispatchTestKit.FakeProcessor.DeleteEntryAsync` (:92) and update the Mode-2 delegate
    ctor doc (:72-79) mentioning `DeleteEntry` (keep `SpawnToPostAsync`/`NewResultPublic`; keep the
    `ReadWriteDeleteOkL2`/`ReadOkDeleteFaultL2` fakes — the pipeline null-path delete now exercises them).

    In `SampleProcessorFacts.cs`: flip the delete assertion at :79 from `Received(1)` to `DidNotReceive()` and
    rename the fact (drop `DeletesEntry`). Keep all other assertions (two spawns, distinct execIds, `{100,200}`,
    returns null).

    In `PrePipelineFacts.cs`: UPDATE `SeamReturnsNull_NoWrite_NoSend_NoDelete` (:126-144) to use
    `entryId = Guid.Empty` (source) — the `DidNotReceive().KeyDeleteAsync` assertion still holds under the
    `IsSource` skip and matches real Mode-2 semantics. ADD a new non-source null-path delete fact: a Mode-2
    `FakeProcessor((DataResult?)null)` + a NON-empty `entryId` + `ReadWriteDeleteOkL2` → assert
    `db.Received(1).KeyDeleteAsync((RedisKey)L2ProjectionKeys.ExecutionData(entryId), Arg.Any<CommandFlags>())`
    (mirror the happy-path assertion at :167-168). ADD a delete-exhaust variant: same but with
    `ReadOkDeleteFaultL2` → assert exactly one `KeeperDelete` (mirror :172-187) — proving the escalation moved
    onto the null path.

    In `BaseProcessorSeamFacts.cs`: REMOVE `DeleteEntry_Success_NoEscalation` (:114-132) and
    `DeleteEntry_DeleteExhaust_Escalates_ToDeleteHook` (:201-221) — delete coverage now lives in the pipeline
    null-path facts. RE-EXPRESS `ConcurrentConsumes_DoNotBleed_SeamStateIsPerDispatch` (:142-199): keep the
    CR-01 regression via the `NewResult` messageId-stamp assertions (:189-193), but drop the entry-delete half
    (the `deletedKeys`/`KeyDeleteAsync` plumbing :146-152, the `DeleteEntryAsync()` call :178, and the
    `Assert.Contains(...deletedKeys)` :195-198).
  </action>
  <acceptance_criteria>
    - `grep` finds NO `DeleteEntryAsync`, NO `escalateDelete`, NO `DeleteEntry_Success`/`DeleteEntry_DeleteExhaust` across the four test files.
    - `SampleProcessorFacts` entry fact asserts `db.DidNotReceive().KeyDeleteAsync(...)`.
    - `PrePipelineFacts.SeamReturnsNull_*` uses `Guid.Empty`; a new non-source null-path fact asserts `db.Received(1).KeyDeleteAsync(ExecutionData(entryId), ...)` and a variant asserts one `KeeperDelete`.
    - `ConcurrentConsumes` still asserts per-consume messageId isolation (no delete half).
    - Full solution builds 0-warning (Release + Debug) and `BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0 (zero new failures vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build SK_P.sln -c Release --nologo 2>&1 | grep -E "Build succeeded|error|warning" ; dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug --nologo && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → L2 (Redis) / keeper | The framework now issues the entry delete + DELETE escalation on the null path (previously author-issued) — same L2 key, same keeper, only the issuer moved |
| (no new external boundary) | Pure internal ownership move; no new input, auth, or network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04 | Tampering / (data loss) | Guid.Empty source net-effect drift | mitigate | The null-path delete is guarded by `!SourceStep.IsSource(d.EntryId)`; the `Guid.Empty` seed is SKIPPED (today's `DeleteEntry` no-op == tomorrow's skip; both leave nothing). Verified by the `SeamReturnsNull_* Guid.Empty` fact + the SC-4 sweep (Plan 04). Manual A1: confirm the scheduler seeds `Guid.Empty` for the Mode-2 entry |
| T-85-05 | Repudiation | Lost DELETE escalation after removing the `escalateDelete` closure | mitigate | The keeper DELETE escalation is NOT lost — it moves inline to the null-path `SendKeeper(BuildDelete(d))` (already in scope). Proven by the new delete-exhaust null-path fact asserting one `KeeperDelete` |
| T-85-01 | Denial of Service | Poison-loop (carried from Plan 02) | mitigate | Ordering enforced by D-01: a spawn-exhaust throws → never reaches `return null` → nack → no delete; the narrow filter (Plan 02) keeps deterministic faults on the ack path |
| T-85-SC | Tampering | package installs | n/a | No packages installed this phase |
</threat_model>

<verification>
- Full solution builds 0-warning Release + Debug; hermetic suite green (zero new failures).
- `grep` confirms the DeleteEntry surface is gone from source AND tests.
- The null-path delete fires for a non-source entryId and skips a `Guid.Empty` source; delete-exhaust escalates to one `KeeperDelete`.
</verification>

<success_criteria>
- Entry deletion is framework-owned on both completion paths; keeper DELETE escalation preserved (PB-03).
- `DeleteEntry`/`EntryId`/`EscalateDelete` removed; `SampleProcessor` no longer deletes.
- Guid.Empty source net-effect unchanged; hermetic suite green; solution 0-warning.
</success_criteria>

<output>
Create `.planning/phases/85-processor-framework-boundary-hardening/85-03-SUMMARY.md` when done.
</output>
