---
phase: 85-processor-framework-boundary-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseProcessor.Core/Processing/SpawnSendExhaustedException.cs
  - src/BaseProcessor.Core/Processing/BaseProcessor.cs
  - tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
autonomous: true
requirements: [PB-01]

must_haves:
  truths:
    - "SpawnToPost throws SpawnSendExhaustedException on a TRANSIENT send-exhaustion instead of swallowing it"
    - "The OnSpawnDropped telemetry hook still fires, and fires BEFORE the throw (SC-1 ordering)"
    - "A DETERMINISTIC send fault still throws the RAW exception (not the dedicated type) — poison-safety preserved"
    - "The dedicated exception carries the spawn ExecutionId only (ids-only, never Payload/Data)"
  artifacts:
    - path: "src/BaseProcessor.Core/Processing/SpawnSendExhaustedException.cs"
      provides: "The dedicated fail-loud spawn-exhaust exception type"
      contains: "public sealed class SpawnSendExhaustedException"
    - path: "src/BaseProcessor.Core/Processing/BaseProcessor.cs"
      provides: "SpawnToPost fail-loud transient-exhaust throw (telemetry-then-throw)"
      contains: "throw new SpawnSendExhaustedException"
    - path: "tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs"
      provides: "Inverted PB-01 seam fact (throws) + deterministic-negative fact"
  key_links:
    - from: "BaseProcessor.SpawnToPost"
      to: "SpawnSendExhaustedException"
      via: "throw after OnSpawnDropped on transient exhaust"
      pattern: "OnSpawnDropped\\?\\.Invoke.*\\n.*throw new SpawnSendExhaustedException"
---

<objective>
PB-01: make `SpawnToPost` fail-loud. On a TRANSIENT send-exhaustion it fires the `OnSpawnDropped`
telemetry hook and THEN throws a dedicated `SpawnSendExhaustedException` (propagating exception, D-01)
instead of swallowing the exhaust as silent success. The deterministic-fault raw throw at
`BaseProcessor.cs:115` and `IsTransientSendFault` (`:123-137`) are untouched (D-03 preserved).

Purpose: This is the prerequisite signal that PB-02 (nack) and PB-03 (delete-only-on-success ordering)
build on. It establishes the general fail-loud hand-off primitive a future KafkaImporter (KIMP-03) reuses.
Output: `SpawnSendExhaustedException.cs` (new), the flipped `SpawnToPost` tail, the inverted hermetic
seam fact + a new deterministic-negative fact, and `SendFaultProvider` lifted into the shared test kit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-processor-framework-boundary-hardening/85-CONTEXT.md
@.planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md
@.planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md

<interfaces>
<!-- Extracted from the current tree. Executor uses these directly — no codebase exploration needed. -->

BaseProcessor.cs:108-116 (the ONLY lines PB-01 changes — current shape):
- `if (sent.Succeeded) return;`
- `if (!IsTransientSendFault(sent.Error)) throw sent.Error!;`   // :115 deterministic → RAW throw (STAYS)
- `s.OnSpawnDropped?.Invoke(spawn.ExecutionId);`                // :116 SWALLOW ← the line PB-01 replaces

IsTransientSendFault (BaseProcessor.cs:123-137) — UNCHANGED. Classifies IOException, TimeoutException,
OperationCanceledException, SocketException, RedisConnectionException/RedisTimeoutException/
BrokerUnreachableException/etc. by type name.

Precedent for a plain-Exception framework sentinel (base type to mirror):
- src/BaseProcessor.Core/Resilience/KeyAbsentException.cs:7 — `internal sealed class KeyAbsentException() : Exception(...)`.
Precedent for shape/namespace (but NOT base type):
- src/BaseProcessor.Core/Processing/ProcessStatusException.cs:8 — `public abstract class ProcessStatusException(...) : Exception(...)`.

Reusable test double to LIFT (currently BaseProcessorSeamFacts.cs:28-44):
- `SendFaultProvider : ISendEndpointProvider` — throws RedisConnectionException (∈ transient) on both Send overloads.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SpawnSendExhaustedException + flip SpawnToPost transient-exhaust to fail-loud</name>
  <files>src/BaseProcessor.Core/Processing/SpawnSendExhaustedException.cs (new), src/BaseProcessor.Core/Processing/BaseProcessor.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Processing/BaseProcessor.cs (SpawnToPost :92-117, IsTransientSendFault :123-137, class doc :19-28)
    - src/BaseProcessor.Core/Processing/ProcessStatusException.cs (namespace + public-sealed + primary-ctor shape)
    - src/BaseProcessor.Core/Resilience/KeyAbsentException.cs (the plain-Exception base type to mirror)
    - .planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md (§ SpawnSendExhaustedException.cs copy-ready shape; § BaseProcessor.cs PB-01 target shape)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ Dedicated Exception; Pitfall 1; Pitfall 3)
  </read_first>
  <action>
    Create `src/BaseProcessor.Core/Processing/SpawnSendExhaustedException.cs` in namespace
    `BaseProcessor.Core.Processing`: a `public sealed class SpawnSendExhaustedException(Guid executionId,
    Exception inner) : Exception(...)` with a `public Guid ExecutionId { get; }` property. CRITICAL (D-01 /
    RESEARCH Pitfall 1): derive from `System.Exception` DIRECTLY — do NOT derive from `ProcessStatusException`
    (that family is caught at `ProcessorPipeline.cs:158` → OutputTail+ack, the exact opposite of the intended
    nack). Mirror `KeyAbsentException`'s base type and `ProcessStatusException`'s namespace/`public sealed`/
    primary-ctor shape. The message carries the ExecutionId only (e.g. `$"...execution {executionId:D}."`) —
    IDS ONLY (FW-03/T-70-10): never a `DataResult`/`Data`/`Payload`. Pass the inner transient transport
    exception as `innerException`.

    In `BaseProcessor.cs` `SpawnToPost` (:108-116): keep `if (sent.Succeeded) return;` and the deterministic
    raw-throw `if (!IsTransientSendFault(sent.Error)) throw sent.Error!;` (:115) UNTOUCHED. Keep the
    `s.OnSpawnDropped?.Invoke(spawn.ExecutionId);` telemetry line (:116) and add — immediately AFTER it — a
    `throw new SpawnSendExhaustedException(spawn.ExecutionId, sent.Error!);`. The telemetry MUST fire BEFORE
    the throw (SC-1 ordering landmine — do NOT reorder or drop the Invoke). Do NOT touch `IsTransientSendFault`.
    Update the class doc-comment wording (:19-28) that describes `SpawnToPost` as "SWALLOW on send-exhaust"
    to describe the fail-loud telemetry-then-throw (leave the `DeleteEntry` doc lines for Plan 03).
  </action>
  <acceptance_criteria>
    - `SpawnSendExhaustedException.cs` exists; `grep` finds `public sealed class SpawnSendExhaustedException` and its base type resolves to `Exception` (NOT `ProcessStatusException`).
    - `BaseProcessor.cs` SpawnToPost contains `s.OnSpawnDropped?.Invoke(spawn.ExecutionId);` on the line immediately preceding `throw new SpawnSendExhaustedException(spawn.ExecutionId, sent.Error!);`.
    - The deterministic-throw line `if (!IsTransientSendFault(sent.Error)) throw sent.Error!;` is unchanged; `IsTransientSendFault` body is byte-identical.
    - `dotnet build src/BaseProcessor.Core` succeeds 0-warning (Release + Debug).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj -c Release --nologo 2>&1 | grep -E "Build succeeded|error|warning" ; grep -c "throw new SpawnSendExhaustedException" src/BaseProcessor.Core/Processing/BaseProcessor.cs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invert the PB-01 seam facts (throw) + add deterministic-negative fact; lift SendFaultProvider to the shared kit</name>
  <files>tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs, tests/BaseApi.Tests/Processor/DispatchTestKit.cs</files>
  <behavior>
    - Transient send-exhaust: `SpawnToPostAsync` THROWS `SpawnSendExhaustedException`; `OnSpawnDropped` still fired first (single dropped execId == the minted spawn execId); no keeper escalation; `ex.ExecutionId == spawnExec`.
    - Deterministic send fault (non-transient `boom`): `SpawnToPostAsync` throws the RAW exception type (e.g. `ArgumentException`), NOT `SpawnSendExhaustedException`.
  </behavior>
  <read_first>
    - tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs (SendFaultProvider :28-44, WireSeam :46-61, the swallow fact :63-85)
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (where the lifted provider lands — near CapturingSendProvider :344)
    - .planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md (§ BaseProcessorSeamFacts.cs — Fact 1 INVERT, Fact 2 NEW; § Transient-fault send double reuse)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ Validation Architecture — PB-01 rows; break-list item 1)
  </read_first>
  <action>
    Lift `SendFaultProvider` (`BaseProcessorSeamFacts.cs:28-44`) into `DispatchTestKit.cs` as a
    `public sealed class SendFaultProvider : ISendEndpointProvider` that accepts an optional
    `Exception? boom` ctor arg (default = the existing transient `RedisConnectionException`), throwing that
    `boom` on BOTH Send overloads (plain + `IPipe<SendContext>`). Remove the private copy from
    `BaseProcessorSeamFacts.cs` and reference `DispatchTestKit.SendFaultProvider`.

    INVERT `SpawnToPost_SendExhaust_Swallows_FiresDropHook_NoThrow_NoKeeper` (`:63-85`): rename (drop
    `Swallows`/`NoThrow`, e.g. `SpawnToPost_SendExhaust_FiresDropHook_ThenThrows_NoKeeper`); replace the
    non-throwing call with `var ex = await Assert.ThrowsAsync<SpawnSendExhaustedException>(() =>
    processor.SpawnToPostAsync(...))`; keep the assertions that the drop hook fired first
    (`Assert.Single(droppedExecIds)` + `Assert.Equal(spawnExec, dropped)` — proves SC-1 telemetry-before-throw);
    keep `Assert.False(escalated)`; add `Assert.Equal(spawnExec, ex.ExecutionId)`.

    ADD a deterministic-negative fact: construct `new DispatchTestKit.SendFaultProvider(new ArgumentException(...))`
    (a NON-transient boom ∉ `IsTransientSendFault`) and assert `await Assert.ThrowsAsync<ArgumentException>(...)`
    — i.e. the raw deterministic fault surfaces, NOT `SpawnSendExhaustedException`. Leave `DeleteEntry_*` /
    `ConcurrentConsumes` facts and the `WireSeam` signature UNCHANGED (they still compile against the
    unremoved `DeleteEntry`/`SetSeamState` — those removals are Plan 03).
  </action>
  <acceptance_criteria>
    - `DispatchTestKit.SendFaultProvider` exists (public) with an optional `boom` ctor param; `BaseProcessorSeamFacts.cs` no longer declares its own `SendFaultProvider`.
    - The inverted fact asserts `Assert.ThrowsAsync<SpawnSendExhaustedException>` AND still asserts the drop hook fired (`Assert.Single(droppedExecIds)`).
    - A new deterministic-negative fact asserts `Assert.ThrowsAsync<ArgumentException>` (raw, not the dedicated type).
    - `BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*BaseProcessorSeamFacts*"` exits 0 (all green).
    - Full hermetic suite `BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0 (zero new failures vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug --nologo && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SpawnToPost → exception/log path | The new throw + preserved telemetry cross into the log/metrics sink; must carry ids only |
| (no new external boundary) | Pure internal control-flow; no auth, network endpoint, or new input parsing added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02 | Information Disclosure | `SpawnSendExhaustedException` message + `OnSpawnDropped` warn | mitigate | Exception carries `ExecutionId` (Guid) + inner transport exception ONLY (FW-03/T-70-10) — never `Data`/`Payload`; mirrors the existing `ProcessorPipeline.cs:280` ExecutionId-only warn |
| T-85-01a | Denial of Service | Fail-loud throw on deterministic fault | mitigate | Deterministic raw-throw at `:115` UNCHANGED — only the transient branch becomes the dedicated type; the pipeline's narrow filter (Plan 02) keeps deterministic faults on the ack path so they cannot nack-loop |
| T-85-SC | Tampering | npm/pip/cargo/NuGet installs | n/a | This phase installs NO packages (RESEARCH § Package Legitimacy Audit: N/A) — no supply-chain surface |
</threat_model>

<verification>
- `BaseApi.Tests.exe --filter-not-trait Category=RealStack` green (full hermetic suite, zero new failures).
- Solution builds 0-warning Release + Debug.
- No `Data`/`Payload` appears in the new exception type or any new log/throw path (ids-only).
</verification>

<success_criteria>
- SpawnToPost fires `OnSpawnDropped` then throws `SpawnSendExhaustedException` on transient exhaust (PB-01).
- Deterministic send faults still throw raw (D-03 preserved); `IsTransientSendFault` unchanged.
- Hermetic suite green; solution 0-warning.
</success_criteria>

<output>
Create `.planning/phases/85-processor-framework-boundary-hardening/85-01-SUMMARY.md` when done.
</output>
