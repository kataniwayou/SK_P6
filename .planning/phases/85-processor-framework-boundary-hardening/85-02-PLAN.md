---
phase: 85-processor-framework-boundary-hardening
plan: 02
type: execute
wave: 2
depends_on: [85-01]
files_modified:
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
  - tests/BaseApi.Tests/Processor/PrePipelineFacts.cs
autonomous: true
requirements: [PB-02]

must_haves:
  truths:
    - "A defeated spawn (transient send-exhaust) makes ProcessorPipeline.RunAsync PROPAGATE SpawnSendExhaustedException (= nack), instead of returning after a StepFailed (= ack)"
    - "A deterministic seam fault (JsonException/InvalidOperationException) STILL acks as StepFailed — the narrow filter does not let it escape (D-03 poison-safety)"
    - "The narrow catch sits between the ProcessStatusException catch (:158) and the generic catch (:185)"
  artifacts:
    - path: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs"
      provides: "Narrow nack-escape for the dedicated spawn-exhaust exception"
      contains: "catch (SpawnSendExhaustedException)"
    - path: "tests/BaseApi.Tests/Processor/PrePipelineFacts.cs"
      provides: "D-05 nack fact + explicit D-03 negative-control fact"
  key_links:
    - from: "ProcessorPipeline.RunAsync try/catch fulcrum"
      to: "MassTransit RabbitMQ nack-requeue"
      via: "catch (SpawnSendExhaustedException) { throw; }"
      pattern: "catch \\(SpawnSendExhaustedException\\)"
---

<objective>
PB-02: the pipeline acks a Mode-2 entry dispatch ONLY when every spawn succeeded. Insert a NARROW
`catch (SpawnSendExhaustedException) { throw; }` between the existing `ProcessStatusException` catch
(`ProcessorPipeline.cs:158`) and the generic `catch (Exception ex)` (`:185`) so ONLY the dedicated
transient-exhaust exception escapes the consumer → MassTransit default RabbitMQ nack-requeue (the exact
`SendKeeper` `:304` rethrow mechanism, D-02 — no new redelivery/backoff config).

Purpose: A defeated spawn re-fires the whole seed (broker redelivery, at-least-once) instead of being
silently swallowed as an acked `StepFailed`. D-03 is the crux — the filter MUST stay narrow so deterministic
faults keep acking (or they poison-loop forever on redelivery).
Output: the narrow catch + the D-05 nack hermetic fact + an explicit D-03 negative-control fact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-processor-framework-boundary-hardening/85-CONTEXT.md
@.planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md
@.planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md

<interfaces>
<!-- Current ProcessorPipeline.RunAsync ack/nack fulcrum (:154-200) — the two existing catches to insert between: -->
- `catch (ProcessStatusException e) { ... _ = await outputTail.RunAsync(statusDr, d.EntryId, ct); ... return; }`   // :158 STAYS (routes to OutputTail + ack)
- `catch (Exception ex) { ... _ = await outputTail.RunAsync(unexpectedDr, ...); ... return; }`                     // :185 STAYS (deterministic faults → StepFailed + ack)

<!-- The proven nack mechanism to reuse (SendKeeper :298-305): -->
- `if (!sent.Succeeded) throw sent.Error!;`   // :304 propagate → throw → RabbitMQ nack-requeue (class doc :41-46)

<!-- The dedicated exception (from Plan 01): BaseProcessor.Core.Processing.SpawnSendExhaustedException(Guid, Exception), public sealed : Exception. -->

<!-- Plain-construct harness (PrePipelineFacts.Build :27-35) — ProcessorPipeline public ctor
     (redis, context, processor, sendProvider, retryOptions, outputTail, metrics, logger).
     Reuse DispatchTestKit.SendFaultProvider (lifted in Plan 01) for the faulting send. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the narrow nack-escape catch in ProcessorPipeline.RunAsync</name>
  <files>src/BaseProcessor.Core/Processing/ProcessorPipeline.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (the try/catch fulcrum :154-200; SendKeeper rethrow :293-305; class doc :41-46)
    - .planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md (§ ProcessorPipeline.cs — Analog 1 target shape; § Narrow exception recognition between two catches)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ SEAM 2; Pitfall 1; Pitfall 2 — D-03 highest risk)
    - .planning/phases/85-processor-framework-boundary-hardening/85-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    In `ProcessorPipeline.RunAsync`, insert a NEW `catch (SpawnSendExhaustedException) { throw; }` immediately
    AFTER the `catch (ProcessStatusException e)` block (ends ~:184) and BEFORE the generic `catch (Exception ex)`
    (:185). The bare `throw;` re-propagates the dedicated exception out of the consumer → MassTransit default
    RabbitMQ nack-requeue (same escape as `SendKeeper` :304). Add `using BaseProcessor.Core.Processing;` only if
    not already in scope (the type is same-namespace — likely no using needed). D-03 CRUX (do NOT widen): leave
    the generic `:185` catch handling `JsonException`/`ArgumentException`/`NotSupportedException`/
    `InvalidOperationException` → `StepFailed`+ack unchanged. (Equivalent per D-03 discretion: a
    `when (ex is not SpawnSendExhaustedException)` guard on the `:185` catch — but the explicit rethrow-catch
    is preferred, matching the `ProcessStatusException` catch ordering above it.) Do NOT add any redelivery or
    backoff config (D-02). Do NOT touch the null-return branch (:206) or the delete tails — those are Plan 03.
  </action>
  <acceptance_criteria>
    - `ProcessorPipeline.cs` contains `catch (SpawnSendExhaustedException)` positioned after the `catch (ProcessStatusException` block and before the `catch (Exception ex)` block.
    - The generic `catch (Exception ex)` block is unchanged (still routes to `outputTail.RunAsync(unexpectedDr, ...)` + `return`).
    - No new endpoint/redelivery/backoff configuration is introduced anywhere.
    - `dotnet build src/BaseProcessor.Core` succeeds 0-warning (Release + Debug).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "catch (SpawnSendExhaustedException)" src/BaseProcessor.Core/Processing/ProcessorPipeline.cs && dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj -c Release --nologo 2>&1 | grep -E "Build succeeded|error|warning"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the D-05 nack fact + the explicit D-03 negative-control fact</name>
  <files>tests/BaseApi.Tests/Processor/PrePipelineFacts.cs</files>
  <behavior>
    - D-05 (nack): a Mode-2 `Guid.Empty` source dispatch driven through a pipeline whose `ISendEndpointProvider` faults transiently ⇒ `RunAsync` PROPAGATES `SpawnSendExhaustedException`; `send.Sent` (Step*) is EMPTY (no `StepFailed` ⇒ NOT the ack path).
    - D-03 (negative control): a deterministic seam fault (`InvalidOperationException`) ⇒ `RunAsync` does NOT throw; exactly one `StepFailed` is sent (ack path) — the narrow filter did not let it escape.
  </behavior>
  <read_first>
    - tests/BaseApi.Tests/Processor/PrePipelineFacts.cs (Build harness :27-35; existing D-03 controls MalformedPayload :317-340 + SeamThrows_Unexpected :278-303 to mirror/keep green)
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (SendFaultProvider lifted in Plan 01; Dispatch helper :326; FakeProcessor Mode-2 delegate ctor :72-79)
    - src/Processor.Sample/SampleProcessor.cs (real Mode-2 entry path — the true fan-out driven by the D-05 fact)
    - .planning/phases/85-processor-framework-boundary-hardening/85-PATTERNS.md (§ PrePipelineFacts.cs — NEW PB-02/D-05 nack fact; D-03 negative controls to keep green)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ Validation Architecture — PB-02/D-05 + D-03 rows; Open Question 2)
  </read_first>
  <action>
    Add the D-05 nack fact: build a `ProcessorPipeline` with `DispatchTestKit.SendFaultProvider` (transient) as
    the `sendProvider` — either overload `Build` to accept a plain `ISendEndpointProvider` or construct the
    pipeline inline via the public ctor (RESEARCH-confirmed). Drive it with a `Guid.Empty` source dispatch
    (`DispatchTestKit.Dispatch(entryId: Guid.Empty, correlationId: Guid.NewGuid())`) and the real
    `SampleProcessor` (Open-Q 2 recommendation — exercises the true Mode-2 path; a Mode-2 `FakeProcessor`
    delegate that calls `SpawnToPost` is an acceptable tighter alternative). Assert
    `await Assert.ThrowsAsync<SpawnSendExhaustedException>(() => pipeline.RunAsync(...))` AND that no Step*
    was sent (`Assert.Empty(send.Sent)` if the send capture is available on the non-faulting path — for the
    faulting provider, assert the throw is the terminal signal, i.e. no `StepFailed` was produced).

    Add the explicit D-03 negative-control fact: drive the pipeline (with a normal capturing send + a
    `ReadWriteDeleteOkL2` redis) using a `FakeProcessor(new InvalidOperationException("boom"))`; assert
    `RunAsync` does NOT throw and that exactly one `StepFailed` was sent (the deterministic fault stayed on the
    ack path — the narrow filter did not let it escape). Keep `MalformedPayload_DeserFailure_*` and
    `SeamThrows_Unexpected_Failed` UNCHANGED and green (they are the standing D-03 proof). Do NOT modify
    `SeamReturnsNull_*` or add delete facts here — those belong to Plan 03.
  </action>
  <acceptance_criteria>
    - New D-05 fact: `Assert.ThrowsAsync<SpawnSendExhaustedException>` on `RunAsync` with a faulting send provider + `Guid.Empty` dispatch; asserts no `StepFailed` was emitted.
    - New explicit D-03 fact: a deterministic `InvalidOperationException` seam fault ⇒ `RunAsync` returns (no throw) with exactly one `StepFailed`.
    - `MalformedPayload_DeserFailure_*` and `SeamThrows_Unexpected_Failed` still pass unchanged.
    - Full hermetic suite `BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0 (zero new failures vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug --nologo && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → RabbitMQ broker | The ack/nack decision crosses here; an escaped throw = nack-requeue (redelivery) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Denial of Service | The narrow catch filter at the ack/nack fulcrum | mitigate | Filter to `SpawnSendExhaustedException` ONLY — deterministic faults (`JsonException`/`ArgumentException`/`InvalidOperationException`) stay in the generic `:185` catch → `StepFailed`+ack, so they cannot nack-loop forever on redelivery. Guarded by the explicit D-03 negative-control fact + the standing `MalformedPayload`/`SeamThrows_Unexpected` facts. **Blocks if the filter is widened.** |
| T-85-03 | Tampering | Redelivery amplification (whole-seed re-fire duplicates succeeded spawns) | accept | At-least-once is the locked model (REQUIREMENTS "Out of Scope"; D-02); exactly-once dedup is explicitly rejected. Bare nack-requeue matches every other send-exhaust path — no new hot-loop config (D-02 rejected a redelivery bound) |
| T-85-SC | Tampering | package installs | n/a | No packages installed this phase |
</threat_model>

<verification>
- `grep` confirms `catch (SpawnSendExhaustedException)` sits between the `ProcessStatusException` and generic catches.
- D-05 nack fact + D-03 negative-control fact green; `MalformedPayload`/`SeamThrows_Unexpected` still green.
- Full hermetic suite green; solution 0-warning; no redelivery/backoff config added.
</verification>

<success_criteria>
- A defeated spawn ⇒ `RunAsync` propagates `SpawnSendExhaustedException` (nack), not `StepFailed`+ack (PB-02).
- Deterministic faults still ack as `StepFailed` (D-03 poison-safety intact).
- Hermetic suite green; solution 0-warning.
</success_criteria>

<output>
Create `.planning/phases/85-processor-framework-boundary-hardening/85-02-SUMMARY.md` when done.
</output>
