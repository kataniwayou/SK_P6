---
phase: 85-processor-framework-boundary-hardening
plan: 04
type: execute
wave: 4
depends_on: [85-01, 85-02, 85-03]
files_modified: []
autonomous: false
requirements: [PB-01, PB-02, PB-03]

must_haves:
  truths:
    - "The full hermetic suite is green (all PB-01/02/03 facts + the two new facts + the standing negative controls)"
    - "The 7-scenario fault-recovery sweep (TEST-01..TEST-07) reproduces its all-PASS baseline — every Missing==0, every Duplicates==0 — driving Processor.Sample"
    - "No happy-path regression leaked from the boundary hardening (the Guid.Empty source net-effect held live)"
  artifacts:
    - path: "scripts/phase-68-sweep.ps1"
      provides: "The SC-4 terminal fault-recovery sweep gate (run-only, not edited)"
  key_links:
    - from: "phase-68-sweep.ps1 harness exit code"
      to: "SC-4 all-PASS verdict"
      via: "detached run, trust the exit code (not a possibly-stale report read)"
      pattern: "Missing==0.*Duplicates==0"
---

<objective>
SC-4 terminal gate: prove the boundary hardening (PB-01/02/03) introduced no happy-path regression by
reproducing the existing 7-scenario fault-recovery sweep's all-PASS baseline (`Missing==0`, `Duplicates==0`
across TEST-01..TEST-07), driving `Processor.Sample`. This is the phase's final verification gate, run
ALONGSIDE the already-green PB-02 nack hermetic fact.

Purpose: the sweep is the SOLE integration proof that the framework-owned delete + fail-loud spawn + nack
filter left the observable happy path byte-for-byte unchanged (the `Guid.Empty` source net-effect holds live).
Output: a green full hermetic suite + an all-PASS detached sweep run + an operator-confirmed verdict.

Operational memories (MUST honor): run the sweep DETACHED (Start-Process + a separate short watcher) — a
~hours sweep gets reaped if run as a tracked bg task [[long-sweep-detached-process]]; clear stale analyzer
reports first and TRUST the harness exit code, not a possibly-stale report read [[phase-68-sweep-stale-report-read]];
after a rebuild the new SourceHash has no processor row — graph-delete → seed → THEN start
[[rebuild-sourcehash-reseed-order]]; run `BaseApi.Tests.exe` directly, not `dotnet test` [[hermetic-test-command]].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-processor-framework-boundary-hardening/85-CONTEXT.md
@.planning/phases/85-processor-framework-boundary-hardening/85-VALIDATION.md
@scripts/phase-68-sweep.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green the full hermetic suite, rebuild, and reseed L2 for the sweep</name>
  <files>(none — build + operational)</files>
  <read_first>
    - .planning/phases/85-processor-framework-boundary-hardening/85-VALIDATION.md (Sampling Rate; Test Infrastructure — run the EXE directly)
    - scripts/phase-68-sweep.ps1 (bring-up, seed, and reseed steps the harness performs; SkipBringUp flags)
    - .planning/phases/85-processor-framework-boundary-hardening/85-RESEARCH.md (§ Environment Availability — reseed/detached/stale-report notes)
  </read_first>
  <action>
    Build the solution (`SK_P.sln`, Release) 0-warning. Run the full hermetic suite directly:
    `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` — it MUST
    exit 0 (all PB-01/02/03 facts + the two new facts + the standing `MalformedPayload`/`SeamThrows_Unexpected`
    negative controls green). Then prepare the RealStack sweep environment: clear stale analyzer reports
    (`analyzer-reports/`) so a stale report cannot be misread; follow the rebuild→reseed order — because the
    rebuild mints a new SourceHash with no processor row, graph-delete the old processor row → seed the new
    SourceHash → only THEN start the pipeline (per [[rebuild-sourcehash-reseed-order]]). Do NOT start the sweep
    in this task — this task only reaches a green hermetic suite + a seeded, ready stack.
  </action>
  <acceptance_criteria>
    - `SK_P.sln` builds 0-warning (Release).
    - `BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0.
    - Stale analyzer reports cleared; the processor SourceHash row for the rebuilt image is seeded (reset heal-wait can proceed).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build SK_P.sln -c Release --nologo 2>&1 | grep -E "Build succeeded|error|warning" ; tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Launch the 7-scenario fault-recovery sweep DETACHED with a watcher</name>
  <files>(none — operational)</files>
  <read_first>
    - scripts/phase-68-sweep.ps1 (invocation + exit-code contract; per-scenario Missing/Duplicates roll-up)
    - .planning/phases/85-processor-framework-boundary-hardening/85-VALIDATION.md (SC-4 gate row)
  </read_first>
  <action>
    Launch `scripts/phase-68-sweep.ps1` DETACHED via `Start-Process` (redirecting stdout/stderr to a phase-85
    sweep .out/.err/.pid, mirroring the tracked phase-84 sweep artifacts) so the ~hours run is NOT reaped as a
    tracked background task [[long-sweep-detached-process]]. Then run a SEPARATE short watcher loop (poll the
    .pid / tail the .out) to observe completion without blocking on a single long foreground call. Do NOT read
    an analyzer report mid-run as the verdict — the harness exit code is the source of truth
    [[phase-68-sweep-stale-report-read]]. Record the launch (pid + out path) so Task 3 can confirm the terminal
    verdict.
  </action>
  <acceptance_criteria>
    - The sweep is running detached (a live pid + a growing .out exist); it is NOT a tracked bg task subject to reaping.
    - A watcher can report the process's running/exited state and the tail of its output.
  </acceptance_criteria>
  <verify>
    <automated>test -f phase-85-sweep.pid && echo "sweep launched pid=$(cat phase-85-sweep.pid)"</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms the sweep reproduced the all-PASS baseline</name>
  <action>Wait for the detached sweep to finish, then confirm the harness exit code is 0 and every scenario reports Missing==0 and Duplicates==0. Trust the exit code over any report read. This is the SC-4 terminal verdict gating /gsd:verify-work.</action>
  <what-built>
    The boundary hardening (PB-01 fail-loud SpawnToPost, PB-02 nack-on-spawn-exhaust, PB-03 framework-owned
    delete) is complete and the full hermetic suite is green. The 7-scenario fault-recovery sweep
    (`scripts/phase-68-sweep.ps1`, TEST-01..TEST-07) was launched detached to prove no happy-path regression.
  </what-built>
  <how-to-verify>
    1. Wait for the detached sweep to finish (watch `phase-85-sweep.pid` / tail `phase-85-sweep.out`).
    2. Confirm the harness EXIT CODE is 0 (trust the exit code, NOT a possibly-stale analyzer report read
       [[phase-68-sweep-stale-report-read]]).
    3. Confirm every scenario reports `Missing==0` and `Duplicates==0` (the all-PASS baseline) in the harness
       roll-up.
    4. If any scenario shows a non-zero Missing/Duplicates that reproduces reliably (not a known cold-ES
       TEST-01 flake), STOP — the boundary change leaked an observable regression; capture the failing
       scenario and hand back for gap closure rather than approving.
  </how-to-verify>
  <acceptance_criteria>
    - Harness exit code 0 AND every scenario Missing==0 / Duplicates==0 (all-PASS baseline reproduced).
    - Any reliably-reproducing non-zero Missing/Duplicates (not a cold-ES TEST-01 flake) is handed back for gap closure, NOT approved.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the sweep reproduces the all-PASS baseline (all Missing==0, all Duplicates==0, exit 0), or describe the failing scenario(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live k8s/RealStack sweep | The terminal gate runs the real fan-out pipeline under 7 sustained fault scenarios; the verdict gates `/gsd:verify-work` |
| (no new code boundary) | This plan edits no source; it is a run-only verification gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-06 | (verification integrity) | Stale analyzer report misread as a PASS/FAIL | mitigate | Clear stale reports first (Task 1) and trust the harness EXIT CODE, not a report read [[phase-68-sweep-stale-report-read]]; the human-verify checkpoint confirms the exit-code verdict |
| T-85-07 | (verification integrity) | Long sweep reaped mid-run → false-incomplete | mitigate | Run detached via `Start-Process` + a separate watcher [[long-sweep-detached-process]] |
| T-85-04 | Tampering / data loss | Guid.Empty source net-effect drift (live) | mitigate | The sweep is the live proof the framework-owned delete left the observable happy path unchanged (Missing==0/Duplicates==0) |
| T-85-SC | Tampering | package installs | n/a | No packages installed this phase |
</threat_model>

<verification>
- Full hermetic suite green (`BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0).
- The detached `phase-68-sweep.ps1` run reproduces the all-PASS baseline: every scenario `Missing==0`, `Duplicates==0`, harness exit 0.
- Operator-confirmed verdict (blocking checkpoint).
</verification>

<success_criteria>
- SC-4: the 7-scenario sweep reproduces its all-PASS baseline (no happy-path regression from PB-01/02/03).
- The full hermetic suite (incl. the PB-02 nack fact + the D-03 negative controls) is green.
</success_criteria>

<output>
Create `.planning/phases/85-processor-framework-boundary-hardening/85-04-SUMMARY.md` when done.
</output>
