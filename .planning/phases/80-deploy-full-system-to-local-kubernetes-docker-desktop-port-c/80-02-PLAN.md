---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/10-postgres.yaml
  - k8s/11-redis.yaml
autonomous: true
requirements: [GOAL-80-HEALTHY]

must_haves:
  truths:
    - "postgres runs as a StatefulSet with a ~1Gi PVC and reaches Ready via pg_isready"
    - "redis runs as a StatefulSet with NO PVC and the --save '' --appendonly no command args"
    - "both are reachable in-cluster at their bare service names postgres:5432 and redis:6379"
  artifacts:
    - path: "k8s/10-postgres.yaml"
      provides: "postgres StatefulSet + PVC + headless Service"
      contains: "kind: StatefulSet"
    - path: "k8s/11-redis.yaml"
      provides: "redis StatefulSet (no PVC) + headless Service"
      contains: "appendonly"
  key_links:
    - from: "postgres StatefulSet"
      to: "skp-dev-secrets"
      via: "secretKeyRef for POSTGRES_DB/USER/PASSWORD"
      pattern: "secretKeyRef"
    - from: "postgres/redis StatefulSet"
      to: "headless Service"
      via: "serviceName + clusterIP: None"
      pattern: "clusterIP: None"
---

<objective>
Port the compose `postgres` (L6-22) and `redis` (L135-151) blocks to Kubernetes StatefulSets, each with a headless Service for stable DNS. postgres gets a ~1Gi PVC (D-09); redis is ephemeral with NO PVC and the persistence-off command args (D-12).

Purpose: These are two of the four backing services the whole stack depends on. Their bare service names (`postgres`, `redis`) are what every app's connection string resolves to via k8s Service DNS — byte-for-byte the compose strings.
Output: `k8s/10-postgres.yaml`, `k8s/11-redis.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: postgres StatefulSet + PVC + headless Service</name>
  <read_first>
    - `compose.yaml` lines 6-22 (the postgres block — image, env, healthcheck cadence, pgdata volume)
    - `80-RESEARCH.md` Pattern 1 (StatefulSet + headless Service, full skeleton L167-214) and the probe arithmetic table row for postgres
    - `80-PATTERNS.md` `k8s/10-postgres.yaml` section
  </read_first>
  <action>
    Create `k8s/10-postgres.yaml` with a headless Service then a StatefulSet (namespace `skp`, label `app: postgres`):
    - Service: `clusterIP: None`, `selector: {app: postgres}`, port `5432` targetPort `5432`, `metadata.name: postgres`.
    - StatefulSet: `metadata.name: postgres`, `serviceName: postgres`, `replicas: 1`, `selector.matchLabels {app: postgres}`, pod label `app: postgres`.
    - Container `postgres`, `image: postgres:17-alpine` (verbatim; public image so no imagePullPolicy override needed).
    - env via `secretKeyRef` into `skp-dev-secrets`: `POSTGRES_DB` (key POSTGRES_DB), `POSTGRES_USER` (key POSTGRES_USER), `POSTGRES_PASSWORD` (key POSTGRES_PASSWORD).
    - `ports: [{containerPort: 5432}]`.
    - `volumeMounts: [{name: data, mountPath: /var/lib/postgresql/data}]`.
    - readinessProbe: `exec.command: ["pg_isready","-U","postgres","-d","stepsdb"]`, `initialDelaySeconds: 5`, `periodSeconds: 5`, `timeoutSeconds: 5`, `failureThreshold: 10` (translated from compose interval 5s/timeout 5s/retries 10/start_period 5s).
    - `resources: {requests: {memory: "128Mi"}, limits: {memory: "256Mi"}}` (Pitfall 6 single-node scheduling).
    - `volumeClaimTemplates`: one entry `metadata.name: data`, `accessModes: ["ReadWriteOnce"]`, `resources.requests.storage: 1Gi` (D-09). Omit `storageClassName` so Docker Desktop's default `hostpath` provisioner binds.
    Drop the compose `5433:5432` host publish (re-emerges only at port-forward, plan 80-09).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/10-postgres.yaml --dry-run=client 2>&1 | grep -E "statefulset.apps/postgres|service/postgres"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/10-postgres.yaml` contains `kind: StatefulSet`, `kind: Service`, `clusterIP: None`, `serviceName: postgres`, `image: postgres:17-alpine`
    - contains `secretKeyRef` for all three POSTGRES_* vars and `pg_isready`
    - contains `volumeClaimTemplates` with `storage: 1Gi` and mountPath `/var/lib/postgresql/data`
    - `kubectl apply -f k8s/10-postgres.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>postgres StatefulSet + PVC + headless Service validate client-side with secret-backed creds and a pg_isready readiness probe.</done>
</task>

<task type="auto">
  <name>Task 2: redis StatefulSet (no PVC) + headless Service</name>
  <read_first>
    - `compose.yaml` lines 135-151 (redis block — image, the `--save "" --appendonly no` command, redis-cli ping healthcheck, the D-03 persistence-off rationale comment)
    - `80-RESEARCH.md` "Redis StatefulSet, no PVC, command args" code example (L509-540)
    - `80-PATTERNS.md` `k8s/11-redis.yaml` section
  </read_first>
  <action>
    Create `k8s/11-redis.yaml` with a headless Service then a StatefulSet (namespace `skp`, label `app: redis`):
    - Service: `clusterIP: None`, `selector {app: redis}`, port `6379` targetPort `6379`, `metadata.name: redis`.
    - StatefulSet: `metadata.name: redis`, `serviceName: redis`, `replicas: 1`, matchLabels/pod label `app: redis`.
    - Container `redis`, `image: redis:7.4.9-alpine` (verbatim).
    - `command: ["redis-server","--save","","--appendonly","no"]` — carried BYTE-FOR-BYTE (D-12, ephemeral by design; do NOT re-enable persistence).
    - `ports: [{containerPort: 6379}]`.
    - readinessProbe: `exec.command: ["redis-cli","ping"]`, `initialDelaySeconds: 5`, `periodSeconds: 5`, `timeoutSeconds: 3`, `failureThreshold: 10` (from compose 5s/3s/10/5s).
    - `resources: {requests: {memory: "64Mi"}, limits: {memory: "192Mi"}}`.
    - NO `volumeClaimTemplates`, NO volumeMounts — redis is ephemeral (D-12).
    Drop the compose `6380:6379` host publish (re-emerges only at port-forward).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/11-redis.yaml --dry-run=client 2>&1 | grep -E "statefulset.apps/redis|service/redis"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/11-redis.yaml` contains `kind: StatefulSet`, `clusterIP: None`, `serviceName: redis`, `image: redis:7.4.9-alpine`
    - contains the command array with `--save`, `""` (empty string), `--appendonly`, `no`
    - contains `redis-cli` in a readiness `exec` probe
    - does NOT contain `volumeClaimTemplates`
    - `kubectl apply -f k8s/11-redis.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>redis StatefulSet validates client-side with the verbatim persistence-off command args and NO PVC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-cluster app pods → backing services | Apps reach postgres/redis by bare Service DNS inside the `skp` namespace only; no ingress, ClusterIP/headless only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-03 | Information Disclosure | postgres/redis exposed to LAN | accept | Headless Services are ClusterIP-class (no NodePort/ingress); only reachable in-cluster or via explicit 127.0.0.1 port-forward (plan 80-09). Single-node local Docker Desktop, no external surface. |
| T-80-04 | Spoofing | postgres creds | mitigate | Creds sourced only from `skp-dev-secrets` via `secretKeyRef` (never baked into the image); same dev-only posture as compose. |
</threat_model>

<verification>
- `kubectl apply -f k8s/10-postgres.yaml -f k8s/11-redis.yaml --dry-run=client` exits 0.
- redis carries `--save "" --appendonly no` and no PVC; postgres carries a 1Gi PVC.
</verification>

<success_criteria>
postgres (StatefulSet + 1Gi PVC + headless Service, secret-backed creds, pg_isready probe) and redis (StatefulSet no-PVC + headless Service, persistence-off command, redis-cli ping probe) both validate client-side.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-02-SUMMARY.md`.
</output>
