---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/00-namespace.yaml
  - k8s/01-secret.yaml
  - k8s/02-configmaps.yaml
autonomous: true
requirements: [GOAL-80-HEALTHY]   # success-bar: full stack Healthy on Docker Desktop k8s (ROADMAP §80 / D-16)

must_haves:
  truths:
    - "Namespace skp exists and all resources land in it"
    - "A dev-only Secret skp-dev-secrets carries the postgres + rabbitmq creds"
    - "otel-collector + prometheus configs exist as ConfigMaps mountable at the same in-container paths"
  artifacts:
    - path: "k8s/00-namespace.yaml"
      provides: "Namespace skp"
      contains: "kind: Namespace"
    - path: "k8s/01-secret.yaml"
      provides: "dev-only creds Secret"
      contains: "name: skp-dev-secrets"
    - path: "k8s/02-configmaps.yaml"
      provides: "otel + prometheus ConfigMaps"
      contains: "kind: ConfigMap"
  key_links:
    - from: "app Deployments + backing StatefulSets"
      to: "skp-dev-secrets"
      via: "secretKeyRef"
      pattern: "secretKeyRef"
    - from: "otel-collector / prometheus Deployments"
      to: "ConfigMaps"
      via: "configMap volume subPath mount"
      pattern: "configMap"
---

<objective>
Author the foundation config layer for the k8s port: the `skp` namespace (D-02), the committed dev-only credentials Secret (D-07), and the two ConfigMaps that replace compose's bind-mounted config files (D-06).

Purpose: Every downstream manifest (StatefulSets + Deployments) references these three objects — the namespace they live in, the Secret their creds come from, and the ConfigMaps the collector/prometheus mount. This is the shared substrate the parallel manifest plans build on.
Output: `k8s/00-namespace.yaml`, `k8s/01-secret.yaml`, `k8s/02-configmaps.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-CONTEXT.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Namespace + dev-only Secret</name>
  <read_first>
    - `.env` (repo root) — the exact dev creds (`POSTGRES_DB=stepsdb`, `POSTGRES_USER=postgres`, `POSTGRES_PASSWORD=postgres`)
    - `compose.yaml` lines 168-170 — `RABBITMQ_DEFAULT_USER: guest` / `RABBITMQ_DEFAULT_PASS: guest`
    - `80-RESEARCH.md` Pattern 8 (dev-only Secret, stringData) and Security Domain section
    - `80-PATTERNS.md` `k8s/01-secret.yaml` block
  </read_first>
  <action>
    Create `k8s/00-namespace.yaml`: `apiVersion: v1`, `kind: Namespace`, `metadata.name: skp`, with labels `app.kubernetes.io/part-of: skp`.

    Create `k8s/01-secret.yaml`: `apiVersion: v1`, `kind: Secret`, `metadata.name: skp-dev-secrets`, `metadata.namespace: skp`, `type: Opaque`. Add a bold leading comment `# DEV ONLY — mirrors the committed .env dev creds; NEVER a real cluster` and label `sensitivity: dev-only` plus `app.kubernetes.io/part-of: skp`. Use `stringData` (NOT `data` — keeps values readable/diff-able against .env; k8s base64-encodes on write) with exactly these five keys:
      POSTGRES_DB: stepsdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
  </action>
  <verify>
    <automated>kubectl apply -f k8s/00-namespace.yaml -f k8s/01-secret.yaml --dry-run=client 2>&1 | grep -E "namespace/skp|secret/skp-dev-secrets"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/00-namespace.yaml` contains `kind: Namespace` and `name: skp`
    - `k8s/01-secret.yaml` contains `name: skp-dev-secrets`, `stringData:`, all five keys (POSTGRES_DB/USER/PASSWORD, RABBITMQ_DEFAULT_USER/PASS), the `# DEV ONLY` comment, and the `sensitivity: dev-only` label
    - `kubectl apply -f k8s/00-namespace.yaml -f k8s/01-secret.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>Namespace + dev-only Secret manifests validate client-side; secret uses stringData with the five verbatim keys and is labeled dev-only.</done>
</task>

<task type="auto">
  <name>Task 2: ConfigMaps for otel-collector-config + prometheus.yml</name>
  <read_first>
    - `compose/otel-collector-config.yaml` (full — carried verbatim into a ConfigMap)
    - `prometheus.yml` (repo root, full — carried verbatim into a ConfigMap)
    - `compose.yaml` lines 61-63 (otel `--config=/etc/otel-collector-config.yaml`, bind-mount path) and lines 90-96 (prometheus `--config.file=/etc/prometheus/prometheus.yml`, bind-mount path)
    - `80-RESEARCH.md` Pattern 7 (ConfigMap file-mount via subPath)
  </read_first>
  <action>
    Create `k8s/02-configmaps.yaml` with TWO ConfigMaps in namespace `skp`:
      1. `metadata.name: otel-collector-config` — `data:` key `otel-collector-config.yaml` whose value is the FULL verbatim contents of `compose/otel-collector-config.yaml` (block scalar `|`). This is the file the collector mounts at `/etc/otel-collector-config.yaml`.
      2. `metadata.name: prometheus-config` — `data:` key `prometheus.yml` whose value is the FULL verbatim contents of the repo-root `prometheus.yml` (block scalar `|`). Mounted at `/etc/prometheus/prometheus.yml`.
    Carry the file contents byte-for-byte (D-06) — do NOT edit scrape targets, exporters, or `mapping.mode`. Label both `app.kubernetes.io/part-of: skp`.
    NOTE for the kustomization plan (80-07): these hand-authored ConfigMaps are the canonical drop-in. If plan 80-07 instead uses a kustomize `configMapGenerator`, it MUST set `disableNameSuffixHash: true` AND use these SAME names (`otel-collector-config`, `prometheus-config`) so the Deployment volume references (plan 80-04) do not break — and this file would then be dropped from the resource list. Author it inline here as the guaranteed-working fallback (RESEARCH Open-Q4).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/02-configmaps.yaml --dry-run=client 2>&1 | grep -E "configmap/otel-collector-config|configmap/prometheus-config"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/02-configmaps.yaml` contains `kind: ConfigMap` twice, `name: otel-collector-config`, `name: prometheus-config`
    - the otel ConfigMap `data` key is `otel-collector-config.yaml`; the prometheus ConfigMap `data` key is `prometheus.yml`
    - the ConfigMap bodies match the repo files (spot-check: otel body contains an `exporters:` / `elasticsearch` reference; prometheus body contains `otel-collector:8889`)
    - `kubectl apply -f k8s/02-configmaps.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>Both ConfigMaps validate client-side and carry the verbatim repo config contents under the correct data keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed repo → k8s cluster | The dev Secret is committed in plaintext (stringData) by explicit decision (D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Information Disclosure | `k8s/01-secret.yaml` committed dev Secret | accept | Values are the SAME public dev creds already committed in `.env`/`compose.yaml` (guest/guest, postgres/postgres) — no new exposure. Mitigate mis-use via bold `# DEV ONLY` header + `sensitivity: dev-only` label; scoped to the `skp` namespace on local Docker Desktop only. Never a real cluster. |
| T-80-02 | Tampering | ConfigMap drift vs repo config files | mitigate | ConfigMap bodies carried verbatim from `compose/otel-collector-config.yaml` + `prometheus.yml`; acceptance spot-checks the exporter/scrape targets survive the copy. |
</threat_model>

<verification>
- `kubectl apply -f k8s/00-namespace.yaml -f k8s/01-secret.yaml -f k8s/02-configmaps.yaml --dry-run=client` exits 0.
- No test-only env seams and no production creds beyond the committed dev set appear in the Secret.
</verification>

<success_criteria>
Three manifests exist and validate client-side; the Secret is dev-only-labeled with the five verbatim keys; the two ConfigMaps carry the verbatim otel + prometheus config bodies at the correct data keys.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-01-SUMMARY.md`.
</output>
