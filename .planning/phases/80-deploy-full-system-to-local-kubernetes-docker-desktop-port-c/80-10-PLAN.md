---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 10
type: execute
wave: 4
depends_on: ["07", "08", "09"]
files_modified:
  - scripts/phase-80-harness.ps1
autonomous: false
requirements: [GOAL-80-HEALTHY, GOAL-80-ROUNDTRIP]

must_haves:
  truths:
    - "A k8s happy-path harness chains build -> up -> reset -> clean-orchestrator -> seed -> wf-id -> POST 204 -> hold -> analyze, reusing the seeder + analyzer + POST unchanged"
    - "STEP B1 (clean orchestrator) uses kubectl rollout restart; STEP D (wf-id) uses kubectl exec psql — the second half of the Pitfall-1 re-target"
    - "The full stack reports Healthy on Docker Desktop k8s AND one workflow round-trips end-to-end (204 + analyzer PASS) from the SAME Prometheus + ES signals as the compose sweep"
  artifacts:
    - path: "scripts/phase-80-harness.ps1"
      provides: "k8s TEST-01 happy-path harness (D-16 proof runner)"
      contains: "kubectl -n skp rollout restart deployment/orchestrator"
  key_links:
    - from: "harness STEP B1"
      to: "clean orchestrator (no ghost Quartz crons)"
      via: "kubectl rollout restart + rollout status deployment/orchestrator"
      pattern: "rollout restart deployment/orchestrator"
    - from: "harness STEP D"
      to: "v8-fanout-proof workflow id"
      via: "kubectl exec statefulset/postgres -- psql ... SELECT id FROM workflows"
      pattern: "SELECT id FROM workflows"
    - from: "harness STEP E"
      to: "activation gate"
      via: "POST http://localhost:8080/api/v1/orchestration/start == 204"
      pattern: "orchestration/start"
---

<objective>
Author `scripts/phase-80-harness.ps1` — the k8s happy-path proof runner (D-16, TEST-01 baseline only) — and run the full end-to-end proof on Docker Desktop k8s. This completes the SECOND half of the RESEARCH Pitfall-1 re-target: `phase-67-harness.ps1` STEP B1 (`docker compose restart orchestrator` → `kubectl rollout restart`) and STEP D (`docker compose exec postgres psql` wf-id → `kubectl exec`). The seeder (STEP C), POST /start (STEP E) and analyzer (STEP H) are reused BYTE-FOR-BYTE — they use localhost ports covered by the plan-80-09 port-forwards.

Note on reuse: `phase-68-sweep.ps1` drives `phase-67-harness.ps1`, which is entirely docker-coupled (STEP A0/A/B/B1/D/Z all use the docker CLI). Literal reuse on k8s is impossible; this k8s sibling harness runs the happy-path chain with the docker touch-points swapped for kubectl, and its analyzer exit IS the verdict (exactly like phase-67-harness).

Purpose: This is the phase gate — proof that the ported stack is Healthy and round-trips end-to-end.
Output: `scripts/phase-80-harness.ps1` + a green live run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-CONTEXT.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-VALIDATION.md
<interfaces>
<!-- REUSED VERBATIM (host, port-forward-covered — do NOT re-target): -->
<!--   STEP C seed:  dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release -- --filter-method "*FanOutSeeder_SeedsAndSelfVerifies*" -->
<!--   STEP E start: POST http://localhost:8080/api/v1/orchestration/start  body = ConvertTo-Json @($wfId)  require 204 -->
<!--   STEP H analyze: dotnet test ... -- --filter-method "*Analyze_Window_Yields_Pass*"  with env SCENARIO_ID / WINDOW_START_UTC / WINDOW_END_UTC / RECOVERY_UTC -->
<!-- RE-TARGETED (Pitfall 1, second half): -->
<!--   STEP B1: kubectl -n skp rollout restart deployment/orchestrator ; kubectl -n skp rollout status deployment/orchestrator --timeout=120s -->
<!--   STEP D : kubectl -n skp exec statefulset/postgres -- psql -U postgres -d stepsdb -tA -c "SELECT id FROM workflows WHERE name = 'v8-fanout-proof'" -->
<!-- TEST-01 is faultType='none' => NO fault seams, NO crash sequencer, just hold the 300s window then analyze. -->
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase-80-harness.ps1 — k8s TEST-01 happy-path harness</name>
  <read_first>
    - `scripts/phase-67-harness.ps1` (FULL — the STEP A0..H flow, the exit-code table, the clean-orchestrator rationale STEP B1 L213-248, the wf-id STEP D L258-267, the window/drain STEP F.1/F.5/F.6, the analyze STEP H L583-643. Port the TEST-01 (faultType='none') path ONLY — skip the fault sequencer STEP F.2/F.3/F.4 and every FALSIFY/TEST-02+ branch)
    - `scripts/phase-68-sweep.ps1` (to confirm the exit-code contract the analyzer verdict must satisfy; TEST-01 => harness exit 0 == PASS)
    - `80-PATTERNS.md` "k8s reset re-target" swap table (STEP B1 + STEP D rows)
  </read_first>
  <action>
    Create `scripts/phase-80-harness.ps1` (no scenario param needed — hardwired TEST-01 baseline; Push-Location repo root; `$ErrorActionPreference='Stop'`; `Write-Phase` prefix `[phase-80-harness]`; dot-source `scripts/lib/exit-code-resolution.ps1` for the verdict resolution, as phase-67 does). Flow:
      - STEP A0: `pwsh -File scripts/phase-80-build.ps1` (build 5 images :local); fail-loud exit 10.
      - STEP A: `pwsh -File scripts/phase-80-up.ps1` (apply + rollout + rollout-restart + port-forwards + baseapi readiness); fail-loud exit 10.
      - STEP B: `pwsh -File scripts/phase-80-reset.ps1` (kubectl-exec reset); fail-loud exit 20.
      - STEP B1 (clean orchestrator — drop ghost Quartz crons): `kubectl -n skp rollout restart deployment/orchestrator` then `kubectl -n skp rollout status deployment/orchestrator --timeout=120s`; fail-loud exit 25. Preserve the WHY comment (HydrationBackgroundService re-runs on the fresh pod against an empty parent index → only the freshly seeded workflow fires; also avoids the POST /start 422).
      - STEP C (seed): the verbatim `dotnet test ... -- --filter-method "*FanOutSeeder_SeedsAndSelfVerifies*"`; fail-loud exit 30.
      - STEP D (wf-id): `$wfId = (kubectl -n skp exec statefulset/postgres -- psql -U postgres -d stepsdb -tA -c "SELECT id FROM workflows WHERE name = 'v8-fanout-proof'").Trim()`; abort exit 40 if empty. Static-literal SQL only.
      - STEP E (activation): POST `http://localhost:8080/api/v1/orchestration/start`, body `ConvertTo-Json @($wfId)`, require 204; fail-loud exit 50.
      - STEP F (window): record `$windowStart` (UTC), hold the 300s observation window (poll ~5s; the no-fault baseline path only — no inject), then STEP F.6 drain + pin `$windowEnd` (reuse phase-67 STEP F.5/F.6 shape; `$recoveryUtc = $null` for the no-fault baseline).
      - STEP H (analyze): set env `SCENARIO_ID=TEST-01`, `WINDOW_START_UTC`, `WINDOW_END_UTC`, `RECOVERY_UTC=''`; run `dotnet test ... -- --filter-method "*Analyze_Window_Yields_Pass*"`; resolve the verdict via `Resolve-AnalyzerExitCode` from the report JSON; clear the env in a `finally`. The analyzer reads Prometheus (localhost:9090) + ES (localhost:9200) via the forwards.
      - STEP Z (teardown): stop the port-forward background processes (Stop-Process on the PIDs recorded by phase-80-up.ps1 / the `.k8s-portforward-pids` file). Do NOT `kubectl delete` by default (keep the stack + PVCs for inspection); optionally accept a `-TearDownCluster` switch that runs `kubectl delete -k k8s/`. A teardown failure is NON-FATAL (log loud, still surface the verdict).
      - Final `exit $analyzerExit` (0 == PASS).
    Preserve the phase-67 exit-code table meanings (10 bring-up, 20 reset, 25 clean-orch, 30 seed, 40 wf-id, 50 activation, 0/1/2 verdict).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Get-Command -Syntax scripts/phase-80-harness.ps1" >/dev/null 2>&1; grep -c "kubectl -n skp rollout restart deployment/orchestrator" scripts/phase-80-harness.ps1; grep -c "SELECT id FROM workflows WHERE name = 'v8-fanout-proof'" scripts/phase-80-harness.ps1; grep -c "FanOutSeeder_SeedsAndSelfVerifies" scripts/phase-80-harness.ps1; ! grep -qE "docker exec|docker compose (exec|restart|ps|up|down|build)" scripts/phase-80-harness.ps1 && echo "no-docker-infra-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-80-harness.ps1` contains `kubectl -n skp rollout restart deployment/orchestrator` (STEP B1) + a `rollout status` wait
    - contains the wf-id `kubectl -n skp exec statefulset/postgres -- psql ... SELECT id FROM workflows WHERE name = 'v8-fanout-proof'` (STEP D)
    - reuses the verbatim seeder (`FanOutSeeder_SeedsAndSelfVerifies`), the POST `orchestration/start` 204 gate, and the analyzer (`Analyze_Window_Yields_Pass`) with SCENARIO_ID=TEST-01
    - contains NO docker infra CLI calls (`docker exec`/`docker compose exec|restart|ps|up|down|build`)
    - the script parses under pwsh
  </acceptance_criteria>
  <done>phase-80-harness.ps1 chains the k8s happy-path (build→up→reset→clean-orch→seed→wf-id→204→hold→analyze) with the Pitfall-1 STEP B1/D re-targets and verbatim seeder/POST/analyzer reuse; parses cleanly and has no docker infra coupling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete k8s port: 13 manifests + kustomization, phase-80-build.ps1, phase-80-up.ps1, phase-80-reset.ps1, and phase-80-harness.ps1. This checkpoint runs the harness end-to-end on Docker Desktop Kubernetes and confirms the D-16 success bar.
  </what-built>
  <how-to-verify>
    Preconditions (Manual-Only per 80-VALIDATION.md):
      1. `kubectl config current-context` == `docker-desktop`; `kubectl get storageclass` shows the default `hostpath`.
      2. The compose stack is DOWN (`docker compose ps` empty) so it does not hold the forward ports.
    Run the proof:
      3. `pwsh -File scripts/phase-80-harness.ps1`
    Confirm ALL of:
      4. `kubectl -n skp get pods` shows every tier Running/Ready — including keeper (2/2) and processor-sample (2/2), orchestrator (1/1), baseapi-service (1/1), otel-collector, prometheus, and the 4 StatefulSet pods (postgres-0, redis-0, rabbitmq-0, elasticsearch-0).
      5. The harness printed `activation accepted (204)` (POST /orchestration/start == 204 — proves SourceHash currency: no 422).
      6. The harness final exit code is 0 (analyzer verdict PASS).
      7. Prometheus signal present: `curl "http://localhost:9090/api/v1/query?query=orchestrator_messages_sent_total"` returns a non-empty result (the workflow fired) — the SAME counter the compose sweep uses.
      8. ES signal present: the analyzer verdict PASS was computed from in-window ES log docs (localhost:9200) — confirm the analyzer-report JSON shows `Verdict: Pass`, `Missing: 0`.
    If any tier is stuck (CrashLoopBackOff / ImagePullBackOff / Pending), consult RESEARCH Pitfalls 2/3/4/6 (image store toggle, probe decoupling, memory) and report the failing pod's `kubectl -n skp describe pod` events.
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 tiers are Ready, POST returned 204, and the analyzer verdict is PASS from the Prometheus + ES signals — or describe the failing tier/step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host harness → k8s (loopback + kubectl exec) | The harness drives the cluster via 127.0.0.1 forwards (HTTP/metrics) and `kubectl exec`/`rollout` (infra); all local, single-node, no ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-20 | Injection | STEP D wf-id `kubectl exec -- psql` | mitigate | Static-literal SQL (`SELECT id FROM workflows WHERE name = 'v8-fanout-proof'`) — no interpolated input (threat T-67-01 posture preserved). |
| T-80-21 | Tampering | stale image → 422 masks a real failure | mitigate | Build-before-seed + rollout-restart (plans 80-07/09) guarantee SourceHash currency; the 204 gate (STEP E) is the explicit check. |
| T-80-22 | Information Disclosure | proof surface exposed beyond loopback | accept | All host access is 127.0.0.1 forwards; happy-path only (D-16), no fault seams, local single-node. No external attack surface. |
</threat_model>

<verification>
- `scripts/phase-80-harness.ps1` parses under pwsh, uses kubectl (not docker) for infra, and reuses the seeder/POST/analyzer verbatim.
- Live gate: `pwsh -File scripts/phase-80-harness.ps1` exits 0 with all 10 tiers Ready, POST 204, analyzer PASS from Prometheus + ES.
</verification>

<success_criteria>
The full stack reports Healthy on Docker Desktop k8s (all 10 tiers Ready, keeper×2 + processor×2) AND one workflow round-trips end-to-end (seed → 204 → analyzer PASS from the same Prometheus + ES signals as the compose sweep) — the D-16 bar, driven by phase-80-harness.ps1 with exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-10-SUMMARY.md`.
</output>
