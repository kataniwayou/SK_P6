---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/30-baseapi-service.yaml
  - k8s/31-orchestrator.yaml
autonomous: true
requirements: [GOAL-80-HEALTHY, GOAL-80-ROUNDTRIP]

must_haves:
  truths:
    - "baseapi-service runs as a Deployment x1 with a ClusterIP Service on 8080 and composes its Postgres connection string from the Secret"
    - "orchestrator runs as a Deployment x1 (documented SPOF) with NO Service"
    - "both carry the production TTL literal 900 (no test seams) and decoupled readiness(/health/ready) + liveness(/health/live) probes"
  artifacts:
    - path: "k8s/30-baseapi-service.yaml"
      provides: "baseapi-service Deployment + ClusterIP Service 8080"
      contains: "baseapi-service:local"
    - path: "k8s/31-orchestrator.yaml"
      provides: "orchestrator Deployment x1, no Service"
      contains: "orchestrator:local"
  key_links:
    - from: "baseapi-service"
      to: "postgres via $(VAR) composed connection string"
      via: "3 POSTGRES_* secretKeyRef listed BEFORE ConnectionStrings__Postgres"
      pattern: "\\$\\(POSTGRES_PASSWORD\\)"
    - from: "orchestrator"
      to: "Orchestrator__OutputDataTtlSeconds literal 900"
      via: "explicit env (test seam dropped)"
      pattern: "Orchestrator__OutputDataTtlSeconds"
---

<objective>
Port the compose `baseapi-service` (L359-398) and `orchestrator` (L185-218) blocks to Deployments. baseapi-service is the ×1 HTTP control plane with a ClusterIP Service on 8080; orchestrator is the ×1 documented SPOF with NO Service (pure consumer). Both use `:local` images + `IfNotPresent` (D-04), the production TTL literal (D-08), and decoupled readiness/liveness probes (Pitfall 3).

Purpose: baseapi is the only tier the harness talks HTTP to (POST /start → 204). orchestrator drives the Quartz cron that fires the workflow. Both must come up Ready with SourceHash-current images.
Output: `k8s/30-baseapi-service.yaml`, `k8s/31-orchestrator.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-CONTEXT.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
<interfaces>
<!-- Confirmed by planner (BaseApi.Core + BaseConsole.Core): every app tier exposes THREE embedded health endpoints: -->
<!--   /health/live   = always-Healthy "self" check ONLY (never Redis/RMQ) — binds as soon as Kestrel starts -->
<!--   /health/ready  = bus readiness (HARD-on-broker for orchestrator; SOFT/Degraded-tolerant for baseapi) -->
<!--   /health/startup = startup gate -->
<!-- Ports: baseapi 8080, orchestrator 8081. Use /health/ready for readiness (traffic gate), /health/live for liveness. -->
<!-- Secret keys (from skp-dev-secrets, plan 80-01): POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD, RABBITMQ_DEFAULT_USER, RABBITMQ_DEFAULT_PASS -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: baseapi-service Deployment + ClusterIP Service (8080)</name>
  <read_first>
    - `compose.yaml` lines 359-398 (baseapi-service — build, ASPNETCORE_ENVIRONMENT, the composed Postgres connection string, Redis/OTEL/RMQ env, /health/ready healthcheck)
    - `80-RESEARCH.md` Pattern 3 ($(VAR) dependent env) and the probe table baseapi row
    - `80-PATTERNS.md` `k8s/30-baseapi-service.yaml` section
  </read_first>
  <action>
    Create `k8s/30-baseapi-service.yaml` — Deployment then ClusterIP Service (namespace `skp`, label `app: baseapi-service`):
    - Deployment: `metadata.name: baseapi-service`, `replicas: 1`.
    - Container `baseapi-service`, `image: baseapi-service:local`, `imagePullPolicy: IfNotPresent` (D-04).
    - env, IN THIS ORDER (the 3 POSTGRES_* refs MUST precede the composed string — Pattern 3):
        ASPNETCORE_ENVIRONMENT: Production
        POSTGRES_DB       ← secretKeyRef skp-dev-secrets/POSTGRES_DB
        POSTGRES_USER     ← secretKeyRef skp-dev-secrets/POSTGRES_USER
        POSTGRES_PASSWORD ← secretKeyRef skp-dev-secrets/POSTGRES_PASSWORD
        ConnectionStrings__Postgres: "Host=postgres;Port=5432;Database=$(POSTGRES_DB);Username=$(POSTGRES_USER);Password=$(POSTGRES_PASSWORD)"
        ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectTimeout=5000"   (verbatim)
        OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4317"                        (verbatim)
        RabbitMq__Host: rabbitmq                                                          (verbatim)
        RabbitMq__Username ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_USER
        RabbitMq__Password ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_PASS
    - `ports: [{containerPort: 8080}]`.
    - readinessProbe: `httpGet: {path: /health/ready, port: 8080}`, `initialDelaySeconds: 30`, `periodSeconds: 10`, `timeoutSeconds: 3`, `failureThreshold: 5`.
    - livenessProbe: `httpGet: {path: /health/live, port: 8080}`, `initialDelaySeconds: 30`, `periodSeconds: 15`, `failureThreshold: 6` (/health/live is the always-Healthy self check → decoupled from broker readiness, Pitfall 3).
    - `resources: {requests: {memory: "192Mi"}, limits: {memory: "512Mi"}}`.
    - Service (ClusterIP): `metadata.name: baseapi-service`, `selector {app: baseapi-service}`, port `{port: 8080, targetPort: 8080}`.
    Drop the compose `8080:8080` host publish and ALL `depends_on` (phased bring-up handles ordering).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/30-baseapi-service.yaml --dry-run=client 2>&1 | grep -E "deployment.apps/baseapi-service|service/baseapi-service"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/30-baseapi-service.yaml` contains `image: baseapi-service:local` and `imagePullPolicy: IfNotPresent`
    - the 3 `POSTGRES_*` `secretKeyRef` entries appear BEFORE the `ConnectionStrings__Postgres` line, which contains `$(POSTGRES_DB)`, `$(POSTGRES_USER)`, `$(POSTGRES_PASSWORD)`
    - contains readiness `httpGet /health/ready` and liveness `httpGet /health/live` both on port 8080
    - a ClusterIP Service exposes 8080
    - `kubectl apply -f k8s/30-baseapi-service.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>baseapi-service Deployment + Service validate with the $(VAR)-composed Postgres string, secret-backed creds, decoupled probes, and a local image.</done>
</task>

<task type="auto">
  <name>Task 2: orchestrator Deployment x1 (SPOF, no Service)</name>
  <read_first>
    - `compose.yaml` lines 185-218 (orchestrator — build, RMQ/Redis env, `Orchestrator__InstanceId`, the `${ORCH_OUTPUT_TTL:-900}` seam, OTEL, /health/ready :8081 healthcheck)
    - `80-PATTERNS.md` `k8s/31-orchestrator.yaml` section (D-08 CRITICAL note)
  </read_first>
  <action>
    Create `k8s/31-orchestrator.yaml` — Deployment ONLY, NO Service (namespace `skp`, label `app: orchestrator`):
    - Deployment: `metadata.name: orchestrator`, `replicas: 1` (documented SPOF — keep at 1, D locked).
    - Container `orchestrator`, `image: orchestrator:local`, `imagePullPolicy: IfNotPresent`.
    - env:
        RabbitMq__Host: rabbitmq
        RabbitMq__Username ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_USER
        RabbitMq__Password ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_PASS
        ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectTimeout=5000"
        Orchestrator__InstanceId: orchestrator-1
        Orchestrator__OutputDataTtlSeconds: "900"   ← LITERAL (drop the ${ORCH_OUTPUT_TTL:-900} test seam, D-08)
        OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4317"
      Do NOT add any test-only seam (no ORCH_OUTPUT_TTL, no K_EXECUTIONS).
    - `ports: [{containerPort: 8081}]` (probed pod-local by the kubelet; no Service).
    - readinessProbe: `httpGet: {path: /health/ready, port: 8081}`, `initialDelaySeconds: 30`, `periodSeconds: 10`, `timeoutSeconds: 3`, `failureThreshold: 5` (HARD-on-broker — gates Ready only after the bus is up).
    - livenessProbe: `httpGet: {path: /health/live, port: 8081}`, `initialDelaySeconds: 45`, `periodSeconds: 15`, `failureThreshold: 6` (self check → a slow 40s rabbitmq never CrashLoops the orchestrator, Pitfall 3).
    - `resources: {requests: {memory: "192Mi"}, limits: {memory: "512Mi"}}`.
    Drop ALL `depends_on`. NO Service block (orchestrator is a pure consumer).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/31-orchestrator.yaml --dry-run=client 2>&1 | grep -E "deployment.apps/orchestrator"; ! grep -q "kind: Service" k8s/31-orchestrator.yaml && echo "no-service-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/31-orchestrator.yaml` contains `image: orchestrator:local`, `imagePullPolicy: IfNotPresent`, `replicas: 1`
    - contains `Orchestrator__InstanceId: orchestrator-1` and `Orchestrator__OutputDataTtlSeconds: "900"` (literal, no `${` interpolation)
    - contains NO `${` env interpolation and NO test-only seam name (ORCH_OUTPUT_TTL / K_EXECUTIONS absent)
    - contains readiness `httpGet /health/ready` and liveness `httpGet /health/live` on port 8081
    - contains NO `kind: Service`
    - `kubectl apply -f k8s/31-orchestrator.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>orchestrator Deployment (×1, no Service) validates with the literal 900 TTL, no test seams, decoupled :8081 probes, and a local image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host harness → baseapi (via 127.0.0.1 port-forward) | The only HTTP entrypoint (POST /start). All other app comms are in-cluster. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-09 | Tampering | stale `:local` image runs unexpected code | mitigate | SourceHash currency: build-before-seed (plan 80-07) + `kubectl rollout restart` after build (plan 80-09) forces pods onto fresh bits; a stale hash correctly 422s POST /start. |
| T-80-10 | Elevation of Privilege | test-only fault seams shipped enabled | mitigate | Orchestrator ships with NO test seam env; only the production literal `Orchestrator__OutputDataTtlSeconds: "900"` (D-08). Acceptance greps for absence of `${`/seam names. |
</threat_model>

<verification>
- `kubectl apply -f k8s/30-baseapi-service.yaml -f k8s/31-orchestrator.yaml --dry-run=client` exits 0.
- baseapi composes Postgres via $(VAR); orchestrator carries literal 900 and no Service.
</verification>

<success_criteria>
baseapi-service (Deployment + ClusterIP Service 8080, $(VAR)-composed Postgres string, decoupled probes) and orchestrator (Deployment ×1, no Service, literal 900 TTL, no test seams) both validate client-side.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-05-SUMMARY.md`.
</output>
