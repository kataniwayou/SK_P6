---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/20-otel-collector.yaml
  - k8s/21-prometheus.yaml
autonomous: true
requirements: [GOAL-80-HEALTHY]

must_haves:
  truths:
    - "otel-collector runs as a Deployment, mounts its config, exposes 4317/4318/8889/13133, and reports Ready via httpGet :13133"
    - "prometheus runs as a Deployment, mounts prometheus.yml, and reports Ready via /-/healthy :9090"
    - "prometheus can scrape otel-collector:8889 in-cluster"
  artifacts:
    - path: "k8s/20-otel-collector.yaml"
      provides: "otel-collector Deployment + ClusterIP Service + ConfigMap subPath mount"
      contains: "otel-collector-config.yaml"
    - path: "k8s/21-prometheus.yaml"
      provides: "prometheus Deployment + ClusterIP Service + ConfigMap subPath mount"
      contains: "/-/healthy"
  key_links:
    - from: "otel-collector Deployment"
      to: "otel-collector-config ConfigMap"
      via: "configMap volume subPath mount at /etc/otel-collector-config.yaml"
      pattern: "subPath"
    - from: "prometheus Deployment"
      to: "prometheus-config ConfigMap"
      via: "configMap volume subPath mount at /etc/prometheus/prometheus.yml"
      pattern: "subPath"
---

<objective>
Port the compose `otel-collector` (L51-79) and `prometheus` (L86-114) blocks to Deployments with ClusterIP Services, each mounting its config from the ConfigMaps (plan 80-01) via `subPath`. Both are ephemeral (D-13, no PVC).

Purpose: This is the telemetry backbone — every app exports OTLP to `otel-collector:4317`, the collector exports logs to ES and exposes `:8889` for prometheus to scrape. The proof reads these signals (Prometheus + ES) through port-forwards.
Output: `k8s/20-otel-collector.yaml`, `k8s/21-prometheus.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
<interfaces>
<!-- ConfigMaps authored in plan 80-01 (k8s/02-configmaps.yaml). Reference by these exact names: -->
- ConfigMap `otel-collector-config`, data key `otel-collector-config.yaml` → mount at /etc/otel-collector-config.yaml
- ConfigMap `prometheus-config`, data key `prometheus.yml` → mount at /etc/prometheus/prometheus.yml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: otel-collector Deployment + ClusterIP Service + config mount</name>
  <read_first>
    - `compose.yaml` lines 51-79 (otel-collector — image, `--config` command, bind-mount path, the 4 ports, the distroless no-healthcheck note)
    - `80-RESEARCH.md` Pattern 6 (otel-collector distroless httpGet :13133) and Pattern 7 (subPath mount)
    - `80-PATTERNS.md` `k8s/20-otel-collector.yaml` section
  </read_first>
  <action>
    Create `k8s/20-otel-collector.yaml` — Deployment then ClusterIP Service (namespace `skp`, label `app: otel-collector`):
    - Deployment: `metadata.name: otel-collector`, `replicas: 1`, matchLabels/pod label `app: otel-collector`.
    - Container `otel-collector`, `image: otel/opentelemetry-collector-contrib:0.152.0` (verbatim; public image, default pull policy).
    - `args: ["--config=/etc/otel-collector-config.yaml"]` (compose `command`, verbatim).
    - `ports: [{containerPort: 4317}, {containerPort: 4318}, {containerPort: 8889}, {containerPort: 13133}]`.
    - `volumeMounts: [{name: otel-config, mountPath: /etc/otel-collector-config.yaml, subPath: otel-collector-config.yaml}]`.
    - `volumes: [{name: otel-config, configMap: {name: otel-collector-config}}]`.
    - readinessProbe: `httpGet: {path: /, port: 13133}`, `initialDelaySeconds: 5`, `periodSeconds: 10`, `failureThreshold: 5` (httpGet is kubelet-side → works on the distroless image; strictly better than the compose no-healthcheck workaround). No liveness needed (restartPolicy Always covers a hard crash; a bad config exits non-zero on start).
    - `resources: {requests: {memory: "128Mi"}, limits: {memory: "256Mi"}}`.
    - Service (ClusterIP, default): `metadata.name: otel-collector`, `selector {app: otel-collector}`, ports named `otlp-grpc 4317`, `otlp-http 4318`, `prom 8889`, `health 13133` (each port=targetPort).
    Drop the compose host publishes (host reach comes via port-forward in plan 80-09).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/20-otel-collector.yaml --dry-run=client 2>&1 | grep -E "deployment.apps/otel-collector|service/otel-collector"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/20-otel-collector.yaml` contains `kind: Deployment`, `kind: Service`, `image: otel/opentelemetry-collector-contrib:0.152.0`
    - contains `--config=/etc/otel-collector-config.yaml` and a `subPath: otel-collector-config.yaml` mount referencing `configMap` `otel-collector-config`
    - Service exposes 4317, 4318, 8889, 13133
    - contains a readiness `httpGet` on port `13133`
    - `kubectl apply -f k8s/20-otel-collector.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>otel-collector Deployment validates with the config subPath mount, all four ports on the ClusterIP Service, and an httpGet :13133 readiness probe.</done>
</task>

<task type="auto">
  <name>Task 2: prometheus Deployment + ClusterIP Service + config mount</name>
  <read_first>
    - `compose.yaml` lines 86-114 (prometheus — image, both command flags, bind-mount path, /-/healthy wget healthcheck, the dropped depends_on)
    - `80-PATTERNS.md` `k8s/21-prometheus.yaml` section
  </read_first>
  <action>
    Create `k8s/21-prometheus.yaml` — Deployment then ClusterIP Service (namespace `skp`, label `app: prometheus`):
    - Deployment: `metadata.name: prometheus`, `replicas: 1`.
    - Container `prometheus`, `image: prom/prometheus:v3.11.3` (verbatim).
    - `args: ["--config.file=/etc/prometheus/prometheus.yml", "--web.enable-lifecycle"]` (both compose flags, verbatim).
    - `ports: [{containerPort: 9090}]`.
    - `volumeMounts: [{name: prom-config, mountPath: /etc/prometheus/prometheus.yml, subPath: prometheus.yml}]`.
    - `volumes: [{name: prom-config, configMap: {name: prometheus-config}}]`.
    - readinessProbe: `httpGet: {path: /-/healthy, port: 9090}`, `initialDelaySeconds: 10`, `periodSeconds: 10`, `timeoutSeconds: 5`, `failureThreshold: 3` (from compose 10s/5s/3/10s).
    - `resources: {requests: {memory: "128Mi"}, limits: {memory: "256Mi"}}`.
    - Service (ClusterIP): `metadata.name: prometheus`, `selector {app: prometheus}`, port `{port: 9090, targetPort: 9090}`.
    Drop the compose `9090:9090` host publish and the `depends_on: otel-collector` (k8s has no depends_on; ordering is handled by the bring-up script, plan 80-09).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/21-prometheus.yaml --dry-run=client 2>&1 | grep -E "deployment.apps/prometheus|service/prometheus"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/21-prometheus.yaml` contains `kind: Deployment`, `kind: Service`, `image: prom/prometheus:v3.11.3`
    - contains both `--config.file=/etc/prometheus/prometheus.yml` and `--web.enable-lifecycle`
    - contains a `subPath: prometheus.yml` mount referencing `configMap` `prometheus-config`
    - contains a readiness `httpGet` on `/-/healthy` port `9090`
    - `kubectl apply -f k8s/21-prometheus.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>prometheus Deployment validates with both command flags, the prometheus.yml subPath mount, and a /-/healthy readiness probe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app pods → otel-collector; prometheus → otel-collector | OTLP ingest + scrape are in-cluster only; ClusterIP Services, no ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-07 | Tampering | ConfigMap subPath hides image dirs | mitigate | Mount is a single-file `subPath` (not a whole-dir mount over `/etc/prometheus`), so image console libs are preserved (RESEARCH Pattern 7). |
| T-80-08 | Information Disclosure | collector/prometheus endpoints | accept | ClusterIP only; host reach is 127.0.0.1 port-forward (plan 80-09). No new external surface. |
</threat_model>

<verification>
- `kubectl apply -f k8s/20-otel-collector.yaml -f k8s/21-prometheus.yaml --dry-run=client` exits 0.
- Both mount config via subPath referencing the plan-80-01 ConfigMap names; readiness probes are kubelet-side httpGet.
</verification>

<success_criteria>
otel-collector (Deployment + 4-port ClusterIP Service + config subPath + httpGet :13133 readiness) and prometheus (Deployment + ClusterIP Service + config subPath + /-/healthy readiness) both validate client-side.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-04-SUMMARY.md`.
</output>
