---
phase: 83-orchestrator-ha-failover-proof
plan: 04
type: execute
wave: 4
depends_on: [83-03]
files_modified:
  - analyzer-reports/phase-83-ha.json
autonomous: false
requirements: [HA-07]
user_setup: []

must_haves:
  truths:
    - "One live failover run against Docker-Desktop k8s (orchestrator replicas:3) force-kills the elected leader phase-aligned and the harness exits 0 (all five HA-07 claims green)"
    - "The written phase-83-ha.json report shows Verdict=Pass with ZeroDuplicate, GapObserved, NotBackfilled, RoleFlipVisible, and BoundedRecovery all true"
    - "An INCONCLUSIVE (exit 2, e.g. gap tick never landed) is operator-re-runnable; a FAIL (exit 1) is a real HA-07 finding"
  artifacts:
    - path: "analyzer-reports/phase-83-ha.json"
      provides: "The captured live verdict artifact from the passing failover run"
      contains: "Pass"
  key_links:
    - from: "scripts/phase-83-ha-failover.ps1"
      to: "analyzer-reports/phase-83-ha.json"
      via: "the live run writes the report; harness exit == Resolve-AnalyzerExitCode(report)"
      pattern: "Pass"
---

<objective>
Run the assembled proof end-to-end against a live Docker-Desktop k8s cluster and confirm HA-07: a single phase-aligned leader force-kill, one continuous observation window spanning pre-kill / election-gap / post-recovery, and an automated verdict of PASS (exit 0). This is the milestone-closing evidence that horizontal orchestrator scaling produces zero duplicate workflow triggers with bounded single-leader recovery.

Purpose: Plans 01-03 built and hermetically verified every piece (pure scorer, live verdict fact, kill sequencer) without a cluster. This plan is the one irreducibly-live step — it needs a running 3-replica orchestrator to actually kill a leader and observe the failover. Per VALIDATION.md this is the sole manual-only verification.

Output: a captured `analyzer-reports/phase-83-ha.json` with Verdict=Pass, committed as the HA-07 proof artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/83-orchestrator-ha-failover-proof/83-RESEARCH.md
@.planning/phases/83-orchestrator-ha-failover-proof/83-VALIDATION.md
@scripts/phase-83-ha-failover.ps1
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live HA-07 failover proof — run the harness, confirm exit 0 (PASS)</name>
  <files>analyzer-reports/phase-83-ha.json</files>
  <read_first>
    - .planning/phases/83-orchestrator-ha-failover-proof/83-VALIDATION.md "Manual-Only Verifications" + "Nyquist / Sampling Adequacy".
    - .planning/phases/83-orchestrator-ha-failover-proof/83-RESEARCH.md Assumptions Log (A1 recovery bracket — record the actual delta), Open Question 1 (kill offset vs kubectl latency), Pitfall 1 (gap-coverage -> INCONCLUSIVE, re-run).
    - [[long-sweep-detached-process]] — if the run is long, drive it detached via Start-Process + a short watcher loop; clear any stale analyzer-reports/phase-83-ha.json first.
  </read_first>
  <action>
    Run the assembled proof against a live Docker-Desktop k8s cluster: clear any stale `analyzer-reports/phase-83-ha.json` (and the copy under `tests/BaseApi.Tests/bin/**/analyzer-reports/`), then execute `pwsh -File scripts/phase-83-ha-failover.ps1` (add `-SkipBringUp` only if the 3-replica stack is already warm). Confirm the harness exits 0 (PASS) — re-run on an INCONCLUSIVE exit 2 (coverage miss, not a failure), triage an exit 1 (real HA-07 finding). Confirm the written report shows Verdict=Pass with all five claim flags true, record the recovery delta (<=30s) and the single empty gap bucket, and copy the passing report to repo-root `analyzer-reports/phase-83-ha.json`. The checkpoint fields below give the step-by-step.
  </action>
  <what-built>
    Plans 01-03 delivered the full proof pipeline: the pure `HaFireBucketScorer` + hermetic facts (green), the RealStack verdict fact `HaFailoverAnalyzerE2ETests.Ha_Failover_Window_Yields_Pass`, and the `scripts/phase-83-ha-failover.ps1` sequencer (bring-up chain + phase-aligned leader-kill + verdict driver). Nothing has been run against a live cluster yet.
  </what-built>
  <how-to-verify>
    1. Confirm Docker-Desktop k8s is running and the `skp` namespace is up (or let the harness STEP A0/A bring it up). Confirm the orchestrator Deployment is at `replicas: 3` (`kubectl get deploy orchestrator -n skp`).
    2. Clear any stale report so a fresh one is unambiguous: `rm -f analyzer-reports/phase-83-ha.json` and remove any copy under `tests/BaseApi.Tests/bin/**/analyzer-reports/phase-83-ha.json`.
    3. Run the proof end-to-end (single failover, multi-minute window incl. bring-up + ~120s hold + 60s drain):
       `pwsh -File scripts/phase-83-ha-failover.ps1`
       (add `-SkipBringUp` only if the stack is already up and warm.)
    4. Read the harness exit code:
       - `exit 0` (PASS) -> all five HA-07 claims green. SUCCESS.
       - `exit 2` (INCONCLUSIVE) -> observability-blind or the gap tick never landed (coverage miss). This is NOT a failure — re-run the harness (the phase-aligned kill is deterministic but kubectl force-delete latency can occasionally miss; Open Q1). Do NOT accept an INCONCLUSIVE as the proof.
       - `exit 1` (FAIL) -> a real HA-07 finding (bucket >=2 = duplicate trigger/split-brain, backfilled gap tick, recovery >30s, or no role flip). Investigate — this blocks HA-07.
    5. Open `tests/BaseApi.Tests/bin/**/analyzer-reports/phase-83-ha.json` (or the copy at repo-root `analyzer-reports/`) and confirm `"Verdict": "Pass"` with `ZeroDuplicate`, `GapObserved`, `NotBackfilled`, `RoleFlipVisible`, `BoundedRecovery` all `true`. Record the actual recovery delta (RESEARCH A1 — expected ~11-17s, must be <=30s) and confirm exactly one contiguous empty gap bucket between the last pre-kill fire and the first post-recovery fire.
    6. Copy the passing report to the repo-root `analyzer-reports/phase-83-ha.json` as the committed proof artifact.
  </how-to-verify>
  <acceptance_criteria>
    - `pwsh -File scripts/phase-83-ha-failover.ps1` exits 0 (an INCONCLUSIVE exit 2 is re-run, not accepted; a FAIL exit 1 blocks).
    - `analyzer-reports/phase-83-ha.json` exists with `"Verdict": "Pass"` and all five claim flags true.
    - The SUMMARY records the observed recovery delta (<=30s) and confirms exactly one empty gap bucket overlapping [KILL_UTC, roleFlipUtc].
  </acceptance_criteria>
  <verify>Harness `pwsh -File scripts/phase-83-ha-failover.ps1` exits 0; `analyzer-reports/phase-83-ha.json` shows `"Verdict": "Pass"` with all five claim flags true.</verify>
  <resume-signal>Type "approved" once the harness exits 0 and phase-83-ha.json shows Verdict=Pass (all five flags true); or describe the FAIL/INCONCLUSIVE observed (with the report's HumanSummary + recovery delta) so it can be triaged.</resume-signal>
  <done>The harness exited 0 (PASS), `analyzer-reports/phase-83-ha.json` shows Verdict=Pass with all five claim flags true, and the recovery delta (<=30s) + single empty gap bucket are recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live cluster | The operator runs the harness with their kubeconfig; it force-kills a pod in the local `skp` namespace and reads ES read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-04d | Repudiation | accepting a vacuous PASS | mitigate | The acceptance criteria explicitly reject INCONCLUSIVE (exit 2) as the proof and require the report's five claim flags all true + a recorded recovery delta — a coverage-miss run cannot be signed off as HA-07 closed. |
| T-83-03b | DoS (self-inflicted) | force-kill on the local cluster | accept | Namespace-scoped kill of one orchestrator replica on the operator's own Docker-Desktop k8s; replicas:3 leaves survivors; the killed pod self-heals via ReplicaSet. Expected, bounded, dev-only. |
</threat_model>

<verification>
- Harness exit 0; `analyzer-reports/phase-83-ha.json` Verdict=Pass with all five claim flags true.
- Recovery delta recorded and <=30s (2x LeaseDuration); exactly one empty gap bucket overlapping [KILL_UTC, roleFlipUtc].
- INCONCLUSIVE re-run to a clean PASS before sign-off; any FAIL triaged as a real HA-07 finding.
</verification>

<success_criteria>
- HA-07 proven live: one phase-aligned leader force-kill, automated verdict PASS (exit 0), zero duplicate triggers + gap-skip + bounded single-leader recovery all confirmed from the two ES evidence streams.
- The passing report is captured at `analyzer-reports/phase-83-ha.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/83-orchestrator-ha-failover-proof/83-04-SUMMARY.md` (record: harness exit code, the report's five claim flags, the observed recovery delta, and the number/placement of empty gap buckets).
</output>
