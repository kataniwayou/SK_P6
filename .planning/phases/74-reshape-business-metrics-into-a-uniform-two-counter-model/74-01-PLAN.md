---
phase: 74-reshape-business-metrics-into-a-uniform-two-counter-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Orchestrator/Observability/OrchestratorMetrics.cs
  - src/Orchestrator/Dispatch/StepDispatcher.cs
  - src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs
  - src/Orchestrator/Dispatch/RelocateTail.cs
  - src/Orchestrator/Consumers/TypedResultConsumer.cs
autonomous: true
requirements: [1, 6]
must_haves:
  truths:
    - "OrchestratorMetrics exposes MessagesConsumed (orchestrator_messages_consumed) and MessagesSent (orchestrator_messages_sent); ResultDeduped/DispatchSent/ResultConsumed are gone"
    - "Every successful orchestrator outbound Send increments orchestrator_messages_sent (forward dispatch, fan-out per match, keeper REINJECT/INJECT/DELETE escalations)"
    - "Every consumed result increments orchestrator_messages_consumed once at the consume entry"
    - "Both counters carry camelCase workflowId + processorId labels; messageId is NOT a label"
  artifacts:
    - path: "src/Orchestrator/Observability/OrchestratorMetrics.cs"
      provides: "MessagesConsumed + MessagesSent counters; StepUnresolved kept"
      contains: "orchestrator_messages_consumed"
    - path: "src/Orchestrator/Consumers/TypedResultConsumer.cs"
      provides: "consumed increment with workflowId+processorId"
      contains: "MessagesConsumed.Add"
  key_links:
    - from: "src/Orchestrator/Dispatch/StepDispatcher.cs"
      to: "OrchestratorMetrics.MessagesSent"
      via: "Add after endpoint.Send succeeds"
      pattern: "MessagesSent\\.Add"
    - from: "src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs"
      to: "OrchestratorMetrics.MessagesSent"
      via: "Add once per successful post.Send in fan-out loop + in SendKeeper"
      pattern: "MessagesSent\\.Add"
---

<objective>
Rewrite `OrchestratorMetrics` to the uniform two-counter model and rebind every orchestrator
increment site to the new counters with camelCase `workflowId`+`processorId` labels (REQ-1, REQ-6).

Purpose: Collapse `orchestrator_dispatch_sent` + `orchestrator_result_consumed` into
`orchestrator_messages_consumed` / `orchestrator_messages_sent`, drop the dormant
`orchestrator_result_deduped`, and ensure `*_sent` fires on EVERY successful outbound broker Send
(forward dispatch + fan-out + all keeper escalations), not just primary-path sends.

Output: `OrchestratorMetrics` with `MessagesConsumed`/`MessagesSent` (+ kept `StepUnresolved`);
increment sites in `StepDispatcher`, `OrchestratorPrePipeline`, `RelocateTail`, `TypedResultConsumer`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-SPEC.md
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-CONTEXT.md
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-PATTERNS.md

<interfaces>
<!-- Label sources — both ids in-hand at every site, no new plumbing (D-07). -->
From src/Messaging.Contracts/IExecutionCorrelated.cs (implemented by IStepResult + EntryStepDispatch):
  Guid WorkflowId { get; }
  Guid ProcessorId { get; }

Current PascalCase + single-label shape being converted (PATTERNS §Two-label increment):
  metrics.DispatchSent.Add(1, new KeyValuePair<string,object?>("ProcessorId", processorId.ToString("D")));

The NEW camelCase + 2-label shape ALREADY in-repo to copy (OrchestratorPrePipeline.cs StepUnresolved):
  metrics.StepUnresolved.Add(1, new KeyValuePair<string,object?>("workflowId", m.WorkflowId.ToString("D")));
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite OrchestratorMetrics to the uniform pair</name>
  <files>src/Orchestrator/Observability/OrchestratorMetrics.cs, tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs</files>
  <read_first>
    - src/Orchestrator/Observability/OrchestratorMetrics.cs (the file being rewritten)
    - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs (sibling analog, exact match per PATTERNS)
    - 74-SPEC.md REQ-1 + REQ-6 acceptance (no ResultDeduped member; snake_case, no _total in code)
    - 74-PATTERNS.md §OrchestratorMetrics.cs (current lines 28/31/42/49/54-57)
  </read_first>
  <behavior>
    - OrchestratorMetrics constructs MessagesConsumed via meter.CreateCounter&lt;long&gt;("orchestrator_messages_consumed")
    - OrchestratorMetrics constructs MessagesSent via meter.CreateCounter&lt;long&gt;("orchestrator_messages_sent")
    - StepUnresolved ("orchestrator_step_unresolved") still constructs unchanged
    - No DispatchSent / ResultConsumed / ResultDeduped members remain
    - MeterName const stays "Orchestrator"
  </behavior>
  <action>
    In src/Orchestrator/Observability/OrchestratorMetrics.cs:
    - DELETE the `DispatchSent` property (line ~28), `ResultConsumed` (line ~31), and `ResultDeduped` (line ~42).
    - ADD two public `Counter&lt;long&gt;` properties: `MessagesConsumed` and `MessagesSent`.
    - KEEP `StepUnresolved` (line ~49) and `MeterName = "Orchestrator"` (line ~25) untouched.
    - In the ctor (line ~53), replace lines 54-56 with:
        MessagesConsumed = meter.CreateCounter&lt;long&gt;("orchestrator_messages_consumed");
        MessagesSent     = meter.CreateCounter&lt;long&gt;("orchestrator_messages_sent");
      Leave line 57 (`StepUnresolved = meter.CreateCounter&lt;long&gt;("orchestrator_step_unresolved");`) intact.
    - Use IMeterFactory (`var meter = meterFactory.Create(MeterName);`) — NEVER a static Meter.
    In tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs:
    - Rename `metrics.DispatchSent` (line ~33) → `metrics.MessagesSent`, `metrics.ResultConsumed` (line ~34) → `metrics.MessagesConsumed`. Keep the `StepUnresolved` (line ~35) assertion. If the test asserts the old instrument name strings, update them to `orchestrator_messages_sent` / `orchestrator_messages_consumed`.
  </action>
  <verify>
    <automated>grep -nE 'orchestrator_messages_(consumed|sent)' src/Orchestrator/Observability/OrchestratorMetrics.cs &amp;&amp; ! grep -nE 'ResultDeduped|orchestrator_result_deduped|DispatchSent|ResultConsumed|orchestrator_dispatch_sent|orchestrator_result_consumed' src/Orchestrator/Observability/OrchestratorMetrics.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "orchestrator_messages_consumed" src/Orchestrator/Observability/OrchestratorMetrics.cs` matches
    - `grep -n "orchestrator_messages_sent" src/Orchestrator/Observability/OrchestratorMetrics.cs` matches
    - `grep -nE "ResultDeduped|DispatchSent|ResultConsumed" src/Orchestrator/Observability/OrchestratorMetrics.cs` returns NOTHING
    - `grep -n 'MeterName = "Orchestrator"' src/Orchestrator/Observability/OrchestratorMetrics.cs` still matches
    - `grep -n "StepUnresolved" src/Orchestrator/Observability/OrchestratorMetrics.cs` still matches (kept)
  </acceptance_criteria>
  <done>OrchestratorMetrics exposes MessagesConsumed + MessagesSent + StepUnresolved only; no removed members; meter name unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rebind consume + dispatch increment sites (camelCase 2-label)</name>
  <files>src/Orchestrator/Consumers/TypedResultConsumer.cs, src/Orchestrator/Dispatch/StepDispatcher.cs</files>
  <read_first>
    - src/Orchestrator/Consumers/TypedResultConsumer.cs (current increment at line ~53)
    - src/Orchestrator/Dispatch/StepDispatcher.cs (current increment at line 41, after endpoint.Send)
    - 74-PATTERNS.md §TypedResultConsumer.cs + §StepDispatcher.cs (corrected lines; D-05 recipient = target queue processor)
    - 74-CONTEXT.md D-02/D-03/D-04/D-05/D-07 (consume-entry count; sent-after-success; messageId NOT a label)
  </read_first>
  <action>
    In src/Orchestrator/Consumers/TypedResultConsumer.cs (increment now at line ~53, NOT 62 — file shrank in Phase 71):
    - Replace `metrics.ResultConsumed.Add(1, new KeyValuePair&lt;string,object?&gt;("ProcessorId", m.ProcessorId.ToString("D")))` with:
        metrics.MessagesConsumed.Add(1,
            new KeyValuePair&lt;string, object?&gt;("workflowId", m.WorkflowId.ToString("D")),
            new KeyValuePair&lt;string, object?&gt;("processorId", m.ProcessorId.ToString("D")));
      `m` is the consumed IStepResult (IExecutionCorrelated) → both ids in-hand. Keep it at the consume TOP, once per consumed message (D-03).
    In src/Orchestrator/Dispatch/StepDispatcher.cs (increment at line 41, AFTER `await endpoint.Send(msg, ct)`):
    - Replace `metrics.DispatchSent.Add(1, new KeyValuePair&lt;string,object?&gt;("ProcessorId", processorId.ToString("D")))` with:
        metrics.MessagesSent.Add(1,
            new KeyValuePair&lt;string, object?&gt;("workflowId", workflowId.ToString("D")),
            new KeyValuePair&lt;string, object?&gt;("processorId", processorId.ToString("D")));
      D-05: processorId here = the dispatch recipient = the target queue's `processorId` (already a method param). `workflowId` is also a method param in DispatchAsync. The increment stays AFTER the successful Send (a failed send must not count — D-02). This single site covers BOTH the orchestrator's direct forward dispatch AND RelocateTail's post-process dispatch (both call DispatchAsync); do NOT add a second dispatch increment in RelocateTail.
  </action>
  <verify>
    <automated>grep -nE 'MessagesConsumed\.Add' src/Orchestrator/Consumers/TypedResultConsumer.cs &amp;&amp; grep -nE 'MessagesSent\.Add' src/Orchestrator/Dispatch/StepDispatcher.cs &amp;&amp; grep -nE '"workflowId".*"processorId"|"workflowId"' src/Orchestrator/Dispatch/StepDispatcher.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'MessagesConsumed.Add' src/Orchestrator/Consumers/TypedResultConsumer.cs` matches with both `"workflowId"` and `"processorId"` keys present
    - `grep -n 'MessagesSent.Add' src/Orchestrator/Dispatch/StepDispatcher.cs` matches with `"workflowId"` and `"processorId"` keys
    - `grep -nE 'DispatchSent|ResultConsumed|"ProcessorId"' src/Orchestrator/Consumers/TypedResultConsumer.cs src/Orchestrator/Dispatch/StepDispatcher.cs` returns NOTHING (no PascalCase label key, no old member)
    - No `"messageId"` label key appears at either site
  </acceptance_criteria>
  <done>Consume increments orchestrator_messages_consumed once at top; dispatch increments orchestrator_messages_sent after success; both carry camelCase workflowId+processorId.</done>
</task>

<task type="auto">
  <name>Task 3: Count every fan-out + keeper-escalation send (Pre/Relocate pipelines)</name>
  <files>src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs, src/Orchestrator/Dispatch/RelocateTail.cs</files>
  <read_first>
    - src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs (fan-out loop ~126-137; SendKeeper ~164-173; OrchestratorMetrics already ctor-injected line ~66)
    - src/Orchestrator/Dispatch/RelocateTail.cs (SendKeeper ~89-98; dispatch via dispatcher.DispatchAsync line ~68 — already counted in StepDispatcher; ctor ~28-34 does NOT inject OrchestratorMetrics)
    - 74-PATTERNS.md §OrchestratorPrePipeline.cs + §RelocateTail.cs (D-02 fan-out = one increment per successful post.Send; D-06 non-processor recipient uses producing processor id)
    - 74-SPEC.md REQ-1 acceptance ("every successful outbound broker Send"); AC: keeper escalation sends count too
  </read_first>
  <action>
    In src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs:
    - In the fan-out `foreach (var (stepId, step) in selection.Matches)` loop (~126-137), AFTER `if (!sent.Succeeded) throw sent.Error!;`, add ONE increment per successful `post.Send`:
        metrics.MessagesSent.Add(1,
            new KeyValuePair&lt;string, object?&gt;("workflowId", m.WorkflowId.ToString("D")),
            new KeyValuePair&lt;string, object?&gt;("processorId", m.ProcessorId.ToString("D")));
      D-02: N matches ⇒ N increments. D-06: the `orchestrator-result-post` queue recipient is not a processor → use the PRODUCING processor's id from the inbound `m` (IStepResult). `metrics` is already ctor-injected (line ~66) — no ctor change.
    - In `SendKeeper` (~164-173), AFTER its `if (!sent.Succeeded) throw` guard, add:
        metrics.MessagesSent.Add(1,
            new KeyValuePair&lt;string, object?&gt;("workflowId", msg.WorkflowId.ToString("D")),
            new KeyValuePair&lt;string, object?&gt;("processorId", msg.ProcessorId.ToString("D")));
      `msg` is the `IKeeperRecoverable` param → exposes WorkflowId/ProcessorId (D-06: keeper escalation recipient is not a processor → producing processor id). This counts BOTH the REINJECT escalation (called ~line 112) and the DELETE escalation (called ~line 159) since both go through SendKeeper.
    In src/Orchestrator/Dispatch/RelocateTail.cs:
    - The next-step EntryStepDispatch goes out via `dispatcher.DispatchAsync(...)` (~line 68) → ALREADY counted inside StepDispatcher. Do NOT add an increment there (no double count).
    - Add `OrchestratorMetrics metrics` to the primary ctor (analog: OrchestratorPrePipeline ctor line ~66). In `SendKeeper` (~89-98), AFTER the `if (!sent.Succeeded) throw` guard, add:
        metrics.MessagesSent.Add(1,
            new KeyValuePair&lt;string, object?&gt;("workflowId", h.WorkflowId.ToString("D")),
            new KeyValuePair&lt;string, object?&gt;("processorId", h.ProcessorId.ToString("D")));
      (`h` = the BuildInject source carrying WorkflowId/ProcessorId; this is the INJECT write-exhaust keeper escalation called ~line 58.) OrchestratorMetrics is a registered singleton — adding the ctor param needs no Program.cs change beyond DI auto-resolution; verify the keeper/orchestrator Program.cs already registers OrchestratorMetrics (it does — used by TypedResultConsumer + PrePipeline).
  </action>
  <verify>
    <automated>test $(grep -cE 'MessagesSent\.Add' src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs) -ge 2 &amp;&amp; grep -nE 'MessagesSent\.Add' src/Orchestrator/Dispatch/RelocateTail.cs &amp;&amp; grep -nE 'OrchestratorMetrics' src/Orchestrator/Dispatch/RelocateTail.cs</automated>
  </verify>
  <acceptance_criteria>
    - `OrchestratorPrePipeline.cs` has at least 2 `MessagesSent.Add` calls (fan-out loop + SendKeeper), each with `"workflowId"` and `"processorId"` keys
    - The fan-out `MessagesSent.Add` is positioned AFTER `if (!sent.Succeeded) throw` (count-after-success — confirm by reading surrounding lines)
    - `RelocateTail.cs` ctor now lists `OrchestratorMetrics` and `SendKeeper` calls `MessagesSent.Add` after its success guard
    - `grep -n 'dispatcher.DispatchAsync' src/Orchestrator/Dispatch/RelocateTail.cs` has NO adjacent MessagesSent.Add (no double-count of the dispatch path)
    - 0-warning build: `dotnet build src/Orchestrator -c Debug -warnaserror` succeeds
  </acceptance_criteria>
  <done>Fan-out sends count once per match; both keeper-escalation SendKeeper sites count; the dispatch path is counted only in StepDispatcher (no double-count); RelocateTail injects OrchestratorMetrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → Prometheus collector | counter series + labels exported (already-existing scrape surface; no new endpoint) |

This is an internal observability refactor: no new external surface, no auth, no user input, no new network endpoint. The only attack-adjacent concern is metric-label cardinality.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | D (DoS via cardinality blow-up) | orchestrator_messages_* labels | accept | Labels are ONLY `workflowId`+`processorId` (bounded: few workflows × few processors per SPEC constraint). `messageId` is the counting unit, deliberately NOT a label (D-04) — prevents unbounded series. No cardinality guard needed (SPEC). |
| T-74-02 | I (Information disclosure) | metric label values | accept | Label values are GUIDs (`workflowId`/`processorId`), not PII; messageId is excluded so no per-message correlation leaks into the scrape surface. |

No high-severity threats. No mitigations require code beyond the bounded-label design already in the SPEC.
</threat_model>

<verification>
- `dotnet build src/Orchestrator -c Debug -warnaserror` and `-c Release -warnaserror` both 0-warning.
- New instrument names present: `orchestrator_messages_consumed`, `orchestrator_messages_sent` (snake_case, no `_total` in code).
- Old members/names absent: `DispatchSent`, `ResultConsumed`, `ResultDeduped`, and the PascalCase `"ProcessorId"` label key at the rebound sites.
- Hermetic OrchestratorMetricsFacts compiles and asserts the renamed members (final full-suite gate runs in Plan 04).
</verification>

<success_criteria>
- OrchestratorMetrics exposes exactly MessagesConsumed + MessagesSent + StepUnresolved (REQ-1, REQ-6).
- orchestrator_messages_sent increments on every successful outbound Send: forward dispatch (StepDispatcher), fan-out per match (PrePipeline), REINJECT+DELETE escalations (PrePipeline SendKeeper), INJECT escalation (RelocateTail SendKeeper).
- orchestrator_messages_consumed increments once per consumed result.
- Every increment carries camelCase workflowId+processorId; messageId is never a label.
</success_criteria>

<output>
After completion, create `.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-01-SUMMARY.md`
</output>
