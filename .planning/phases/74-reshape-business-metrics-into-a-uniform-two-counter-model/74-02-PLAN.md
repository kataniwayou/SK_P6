---
phase: 74-reshape-business-metrics-into-a-uniform-two-counter-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs
  - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
  - src/BaseProcessor.Core/Processing/OutputTail.cs
autonomous: true
requirements: [2, 6]
must_haves:
  truths:
    - "ProcessorMetrics exposes MessagesConsumed (processor_messages_consumed) and MessagesSent (processor_messages_sent); DispatchConsumed/ResultSent/DispatchDeduped are gone; SpawnDropped kept"
    - "processor_messages_consumed increments once at EntryStepDispatchConsumer consume entry"
    - "processor_messages_sent increments after the successful result send in OutputTail; the outcome label is gone and ResultOutcome helper is deleted"
    - "Both counters carry camelCase workflowId+processorId labels and NO outcome label"
  artifacts:
    - path: "src/BaseProcessor.Core/Observability/ProcessorMetrics.cs"
      provides: "MessagesConsumed + MessagesSent; SpawnDropped kept; no DispatchDeduped"
      contains: "processor_messages_consumed"
    - path: "src/BaseProcessor.Core/Processing/OutputTail.cs"
      provides: "sent increment without outcome label; ResultOutcome helper removed"
      contains: "MessagesSent.Add"
  key_links:
    - from: "src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs"
      to: "ProcessorMetrics.MessagesConsumed"
      via: "Add at consume entry with workflowId+processorId"
      pattern: "MessagesConsumed\\.Add"
    - from: "src/BaseProcessor.Core/Processing/OutputTail.cs"
      to: "ProcessorMetrics.MessagesSent"
      via: "Add after send-success guard, no outcome tag"
      pattern: "MessagesSent\\.Add"
---

<objective>
Rewrite `ProcessorMetrics` to the uniform two-counter model, drop the `outcome` label, remove the
dedup counter, and rebind the processor consume + send increment sites (REQ-2, REQ-6).

Purpose: Collapse `processor_dispatch_consumed` + `processor_result_sent{outcome}` into
`processor_messages_consumed` / `processor_messages_sent` (no `outcome` label), drop the dormant
`processor_dispatch_deduped`, and delete the now-unused `ResultOutcome` helper (so the 0-warning
build holds — an unused private method warns).

Output: `ProcessorMetrics` with `MessagesConsumed`/`MessagesSent` (+ kept `SpawnDropped`); increment
sites rebound in `EntryStepDispatchConsumer` (consumed) and `OutputTail` (sent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-SPEC.md
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-CONTEXT.md
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-PATTERNS.md

<interfaces>
<!-- Label sources — both ids in-hand, no new plumbing (D-07). -->
IStepResult : IExecutionCorrelated → result.WorkflowId, result.ProcessorId
EntryStepDispatch : IExecutionCorrelated → ctx.Message.WorkflowId (consume site); the consuming
processor's own id is `context.Id!.Value` (already used today as the ProcessorId tag value).

Current shape to convert (OutputTail.cs:129-131) — the real processor_result_sent site
(moved here from ProcessorPipeline in Phase 72):
  metrics.ResultSent.Add(1,
    new KeyValuePair<string,object?>("ProcessorId", context.Id!.Value.ToString("D")),
    new KeyValuePair<string,object?>("outcome", ResultOutcome(result)));   // ← drop outcome
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ProcessorMetrics (drop ResultSent/DispatchConsumed/DispatchDeduped)</name>
  <files>src/BaseProcessor.Core/Observability/ProcessorMetrics.cs, tests/BaseApi.Tests/Processor/ProcessorMetricsFacts.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs (current members lines 31/34/40/46; ctor 48-55)
    - src/Orchestrator/Observability/OrchestratorMetrics.cs (sibling analog — exact match)
    - 74-SPEC.md REQ-2 + REQ-6 acceptance ("no DispatchDeduped member"; no outcome label; snake_case no _total)
    - 74-PATTERNS.md §ProcessorMetrics.cs (SpawnDropped = processor_spawn_dropped is KEPT — NOT in removal list)
  </read_first>
  <behavior>
    - ProcessorMetrics constructs MessagesConsumed via meter.CreateCounter&lt;long&gt;("processor_messages_consumed")
    - ProcessorMetrics constructs MessagesSent via meter.CreateCounter&lt;long&gt;("processor_messages_sent")
    - SpawnDropped ("processor_spawn_dropped") still constructs unchanged
    - No DispatchConsumed / ResultSent / DispatchDeduped members remain
    - MeterName const stays "BaseProcessor"
  </behavior>
  <action>
    In src/BaseProcessor.Core/Observability/ProcessorMetrics.cs:
    - DELETE the `DispatchConsumed` property (line ~31), `ResultSent` (line ~34), and `DispatchDeduped` (line ~40).
    - ADD two public `Counter&lt;long&gt;` properties: `MessagesConsumed` and `MessagesSent`.
    - KEEP `SpawnDropped` (line ~46) and `MeterName = "BaseProcessor"` untouched — `SpawnDropped` is OUT of the removal list (IN-03).
    - In the ctor (lines ~48-55), replace the DispatchConsumed/ResultSent/DispatchDeduped creates with:
        MessagesConsumed = meter.CreateCounter&lt;long&gt;("processor_messages_consumed");
        MessagesSent     = meter.CreateCounter&lt;long&gt;("processor_messages_sent");
      Leave the `SpawnDropped = meter.CreateCounter&lt;long&gt;("processor_spawn_dropped");` line intact.
    - Build via IMeterFactory (`meterFactory.Create(MeterName)`) — never a static Meter.
    In tests/BaseApi.Tests/Processor/ProcessorMetricsFacts.cs:
    - Rename `metrics.DispatchConsumed` (line ~33) → `metrics.MessagesConsumed`, `metrics.ResultSent` (line ~34) → `metrics.MessagesSent`. DELETE the `metrics.DispatchDeduped` assertion (line ~35). KEEP the `SpawnDropped` assertion (line ~36). Update any old instrument-name string literals to `processor_messages_consumed` / `processor_messages_sent`.
  </action>
  <verify>
    <automated>grep -nE 'processor_messages_(consumed|sent)' src/BaseProcessor.Core/Observability/ProcessorMetrics.cs &amp;&amp; ! grep -nE 'DispatchDeduped|processor_dispatch_deduped|DispatchConsumed|ResultSent|processor_dispatch_consumed|processor_result_sent' src/BaseProcessor.Core/Observability/ProcessorMetrics.cs &amp;&amp; grep -n 'processor_spawn_dropped' src/BaseProcessor.Core/Observability/ProcessorMetrics.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "processor_messages_(consumed|sent)" src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` matches both
    - `grep -nE "DispatchConsumed|ResultSent|DispatchDeduped" src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` returns NOTHING
    - `grep -n "processor_spawn_dropped" src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` still matches (SpawnDropped kept)
    - `grep -n 'MeterName = "BaseProcessor"' src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` still matches
  </acceptance_criteria>
  <done>ProcessorMetrics exposes MessagesConsumed + MessagesSent + SpawnDropped only; no removed members; meter name unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rebind consume + send sites, delete outcome label and ResultOutcome helper</name>
  <files>src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs, src/BaseProcessor.Core/Processing/OutputTail.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs (consume increment lines 37-38)
    - src/BaseProcessor.Core/Processing/OutputTail.cs (SendResult send + increment 116-131; ResultOutcome helper 134-143)
    - 74-PATTERNS.md §EntryStepDispatchConsumer.cs + §OutputTail.cs (corrected lines; D-05/D-06 processorId = this processor's own id)
    - 74-CONTEXT.md D-02/D-03/D-04/D-07; 74-SPEC.md REQ-2 acceptance (no outcome label)
  </read_first>
  <action>
    In src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs (consume increment lines ~37-38):
    - Replace `metrics.DispatchConsumed.Add(1, new KeyValuePair&lt;string,object?&gt;("ProcessorId", context.Id!.Value.ToString("D")))` with:
        metrics.MessagesConsumed.Add(1,
            new KeyValuePair&lt;string, object?&gt;("workflowId", ctx.Message.WorkflowId.ToString("D")),
            new KeyValuePair&lt;string, object?&gt;("processorId", context.Id!.Value.ToString("D")));
      `workflowId` comes from the consumed `EntryStepDispatch` (`ctx.Message.WorkflowId` — IExecutionCorrelated); `processorId` is the consuming processor's own `context.Id!.Value` (the value already used today). Confirm the consume-context variable name in scope (the file uses it for `context.Id`) and use the same instance to reach `.Message.WorkflowId`.
    In src/BaseProcessor.Core/Processing/OutputTail.cs (SendResult, increment lines ~129-131):
    - Replace the `metrics.ResultSent.Add(...)` call (with its `"ProcessorId"` tag and `"outcome"`, ResultOutcome(result) tag) with:
        metrics.MessagesSent.Add(1,
            new KeyValuePair&lt;string, object?&gt;("workflowId", result.WorkflowId.ToString("D")),
            new KeyValuePair&lt;string, object?&gt;("processorId", context.Id!.Value.ToString("D")));
      Keep it AFTER `if (!sent.Succeeded) throw sent.Error!;` (count-after-success — D-02). DROP the `outcome` tag entirely (REQ-2). D-05/D-06: `processorId` = this producing processor's own id; `workflowId` from `result.WorkflowId` (IStepResult : IExecutionCorrelated).
    - DELETE the entire `private static string ResultOutcome(IStepResult result) => result switch { … };` helper (lines ~134-143). It is now unreferenced; leaving it triggers a 0-warning-build failure (unused private member). Confirm via grep that no other reference to `ResultOutcome` remains in the file.
  </action>
  <verify>
    <automated>grep -nE 'MessagesConsumed\.Add' src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs &amp;&amp; grep -nE 'MessagesSent\.Add' src/BaseProcessor.Core/Processing/OutputTail.cs &amp;&amp; ! grep -nE 'ResultOutcome|"outcome"' src/BaseProcessor.Core/Processing/OutputTail.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'MessagesConsumed.Add' src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs` matches with `"workflowId"` + `"processorId"` keys
    - `grep -n 'MessagesSent.Add' src/BaseProcessor.Core/Processing/OutputTail.cs` matches with `"workflowId"` + `"processorId"` keys
    - `grep -nE 'ResultOutcome|"outcome"' src/BaseProcessor.Core/Processing/OutputTail.cs` returns NOTHING (helper + label gone)
    - `grep -nE 'DispatchConsumed|ResultSent|"ProcessorId"' src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs src/BaseProcessor.Core/Processing/OutputTail.cs` returns NOTHING
    - 0-warning build: `dotnet build src/BaseProcessor.Core -c Debug -warnaserror` and `-c Release -warnaserror` both succeed
  </acceptance_criteria>
  <done>Consume increments processor_messages_consumed; send increments processor_messages_sent after success with no outcome tag; ResultOutcome helper deleted; build is 0-warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor → Prometheus collector | counter series + labels exported (existing scrape surface; no new endpoint) |

Internal observability refactor: no new external surface, no auth, no user input. Dropping the
`outcome` label REDUCES cardinality and removes a free-text dimension.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03 | D (cardinality) | processor_messages_* labels | accept | Labels reduced to `workflowId`+`processorId` (bounded). The previously-unbounded-risk `outcome` dimension is REMOVED this phase. messageId stays the counting unit, not a label (D-04). |
| T-74-04 | I (disclosure) | label values | accept | Values are GUIDs, not PII; no message body or messageId in labels. |

No high-severity threats; the change strictly narrows the metric surface.
</threat_model>

<verification>
- `dotnet build src/BaseProcessor.Core -c Debug -warnaserror` and `-c Release -warnaserror` both 0-warning.
- New names present: `processor_messages_consumed`, `processor_messages_sent`; SpawnDropped survives.
- Absent: `DispatchConsumed`/`ResultSent`/`DispatchDeduped` members, `processor_dispatch_consumed`/`processor_result_sent`/`processor_dispatch_deduped` names, the `outcome` label, the `ResultOutcome` helper.
</verification>

<success_criteria>
- ProcessorMetrics exposes exactly MessagesConsumed + MessagesSent + SpawnDropped (REQ-2, REQ-6).
- processor_messages_consumed increments once per consumed dispatch; processor_messages_sent once per successful result send.
- No outcome label anywhere; ResultOutcome helper removed; 0-warning Debug + Release.
</success_criteria>

<output>
After completion, create `.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-02-SUMMARY.md`
</output>
