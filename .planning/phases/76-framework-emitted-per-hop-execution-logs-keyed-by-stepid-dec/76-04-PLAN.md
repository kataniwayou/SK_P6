---
phase: 76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec
plan: 04
type: execute
wave: 2
depends_on: [76-03]
files_modified:
  - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs
  - scripts/lib/exit-code-resolution.ps1
  - scripts/phase-67-harness.ps1
  - scripts/phase-68-sweep.ps1
autonomous: true
requirements: [ANL-04, ANL-05]

must_haves:
  truths:
    - "Verdict has a third class Inconclusive; the verdict splits on evidence sufficiency, not severity"
    - "Total trace darkness (startedRuns==0) with self-consistent conservation (orch_consumed==proc_sent) yields INCONCLUSIVE regardless of metricGateOk (TEST-01 cold-ES shape)"
    - "Partial evidence with a real conservation gap yields FAIL; complete evidence yields PASS"
    - "Frozen/absent counters + absent traces yield INCONCLUSIVE (collector-blind); live counters with a genuine orch_consumed!=proc_sent gap yield FAIL"
    - "A non-zero TelemetryGap stays NON-binding on PASS (D-01, preserves 6e90216)"
    - "The harness maps analyzer Verdict==Inconclusive → exit code 2; the sweep roll-up adds 2→INCONCLUSIVE (sweep-fatal, no auto-retry, distinct message)"
    - "The exit-code resolution is a dot-sourceable function proven hermetically: a synthetic Verdict==Inconclusive JSON resolves to exit 2, and code 2 maps to the INCONCLUSIVE class with the distinct no-auto-retry message — BEFORE the live gate"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs"
      provides: "enum Verdict gains Inconclusive (serializes via JsonStringEnumConverter)"
      contains: "Inconclusive"
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      provides: "three-class verdict gate + inverted metric-gate blind branch"
      contains: "Inconclusive"
    - path: "scripts/lib/exit-code-resolution.ps1"
      provides: "dot-sourceable Resolve-AnalyzerExitCode (report→0/1/2) + Resolve-SweepClass (code→class+message) — testable without running the full scripts"
      contains: "Resolve-AnalyzerExitCode"
    - path: "scripts/phase-67-harness.ps1"
      provides: "STEP H dot-sources the lib and maps Verdict==Inconclusive (from JSON artifact) → exit 2; exit-code table adds 2"
      contains: "Inconclusive"
    - path: "scripts/phase-68-sweep.ps1"
      provides: "roll-up dot-sources the lib; class switch adds 2→INCONCLUSIVE + distinct instrument-failure message"
      contains: "INCONCLUSIVE"
  key_links:
    - from: "PassFailEngine verdict gate (:368)"
      to: "Verdict.Inconclusive when trace-dark + conservation self-consistent"
      via: "evidence-sufficiency branch before Pass/Fail"
      pattern: "Inconclusive"
    - from: "phase-67-harness.ps1 STEP H"
      to: "exit 2 on report.Verdict == Inconclusive"
      via: "Resolve-AnalyzerExitCode over the JSON artifact before the assert mirror"
      pattern: "Resolve-AnalyzerExitCode"
    - from: "phase-68-sweep.ps1 roll-up switch (:80)"
      to: "2 -> INCONCLUSIVE class + distinct message"
      via: "Resolve-SweepClass, sweep-fatal, no auto-retry"
      pattern: "INCONCLUSIVE"
---

<objective>
Add the third verdict class (INCONCLUSIVE) so an observability-tier failure is never reported as data loss. Re-thread the `PassFailEngine` verdict gate to split on evidence sufficiency, invert the metric gate for the collector-blind case (ANL-05), and plumb exit-code 2 through the harness + sweep roll-up (D-02/03/04). FAIL requires positive evidence of loss; absence of evidence is INCONCLUSIVE. The exit-code resolution is factored into a dot-sourceable lib so the 2→INCONCLUSIVE mapping is proven hermetically before the live gate.

Purpose: closes the two Phase-75 distortions' verdict half — TEST-01 cold-ES (trace-dark, conservation intact) becomes INCONCLUSIVE not a false FAIL, and a genuine conservation gap stays FAIL.
Output: `Verdict.Inconclusive`, the re-threaded engine gate, the ANL-04/05 engine facts, the `scripts/lib/exit-code-resolution.ps1` resolution lib, and the harness/sweep exit-2 plumbing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-SPEC.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-CONTEXT.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-RESEARCH.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-PATTERNS.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-03-SUMMARY.md

<interfaces>
<!-- Confirmed live 2026-07-16. Depends on plan 03's stepId-keyed completeness. -->

enum Verdict (AnalyzerReport.cs:24-39): currently `Pass`, `Fail`. Serialized via `[JsonConverter(typeof(JsonStringEnumConverter))]` (:90) so the sweep reads `"Inconclusive"` from JSON for free (RESEARCH A4). ADD `Inconclusive` member — no new report fields needed (existing CorroborationDetail/HumanSummary carry the blind-case reason).

PassFailEngine verdict gate (PassFailEngine.cs:363-369):
```csharp
var conservationContributes = !mg1Binding || conservationOk;   // :351
var metricGateOk = conservationContributes && keeperRecoveryOk && probeLiveOk;   // :360
...
var pass = missing == 0 && !dupFail && valueChainOk && metricGateOk;   // :368
var verdict = pass ? Verdict.Pass : Verdict.Fail;   // :369
```
`startedRuns` (:128 = runs.Count), `conservationOk` and `prom.OrchestratorMessagesConsumedAtEnd` / `prom.ProcessorMessagesSentAtEnd` (conservation), and `mg1Binding` seam are all in scope at the gate. The blind case = frozen/absent counters + `startedRuns == 0`.

harness STEP H (phase-67-harness.ps1:496-531): runs `dotnet test … --filter-method "*Analyze_Window_Yields_Pass*"`; `$analyzerExit = $LASTEXITCODE` (the fixture asserts Verdict.Pass, so any non-Pass → exit 1). The report JSON is located at `$report` (analyzer-reports/$ScenarioId.json, written BEFORE the assert). Exit-code table doc at :30-34 (0 PASS, 1 FAIL, ≥10 infra, 64 bad-arg; 2 UNUSED). Final harness exit mirrors $analyzerExit (D-04, STEP Z note :536).

sweep roll-up (phase-68-sweep.ps1:80-85):
```powershell
$class = switch ($code) {
    0       { 'PASS' }
    1       { 'VERDICT_FAIL' }
    64      { 'BAD_ARG' }
    default { 'INFRA_ABORT' }
}
```
Final sweep exit (:128): `exit ($(if ($passCount -eq $total) { 0 } else { 1 }))` — 0 IFF all PASS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verdict.Inconclusive + three-class engine gate + metric-gate inversion (ANL-04, ANL-05)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs (:24-39 enum Verdict, :90 JsonStringEnumConverter)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (:340-369 metric gate + verdict gate, :351 mg1Binding seam, :128 startedRuns)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs (:48 CleanSnapshot to invert for the blind case)
    - 76-RESEARCH.md § §6 three-class threading + § Validation Architecture (per-class floor)
    - 76-PATTERNS.md § PassFailEngine.cs verdict gate re-thread + § Blind-case snapshot pattern
    - 76-CONTEXT.md D-01 (three classes split on evidence sufficiency; INCONCLUSIVE narrow; TelemetryGap non-binding on PASS)
  </read_first>
  <behavior>
    - ANL-04a: total trace darkness (startedRuns == 0) + self-consistent conservation (orch_consumed == proc_sent, gap 0) → Verdict.Inconclusive, regardless of metricGateOk.
    - ANL-04b: partial evidence + a real conservation gap → Verdict.Fail.
    - ANL-04c: complete framework-record cohort → Verdict.Pass.
    - ANL-05 blind: frozen/absent counters + absent traces → Verdict.Inconclusive.
    - ANL-05 live: live counters with a genuine orch_consumed != proc_sent gap → Verdict.Fail.
    - D-01: a reconciled run with a non-zero TelemetryGap stays PASS (not FAIL).
  </behavior>
  <action>
    Add `Inconclusive` to `enum Verdict` (AnalyzerReport.cs) with an XML-doc mirroring the existing members ("evidence insufficient to judge — an observability-degraded run, explicitly not a flow failure and explicitly not a green"). Re-thread the verdict gate at PassFailEngine.cs:363-369: FIRST test evidence-sufficiency — if `startedRuns == 0` (total trace darkness) AND conservation is self-consistent (`conservationOk`, i.e. orch_consumed == proc_sent within ConservationTol) → `Verdict.Inconclusive` regardless of `metricGateOk` (the TEST-01 cold-ES shape). Only when evidence is sufficient apply the existing `pass = missing == 0 && !dupFail && valueChainOk && metricGateOk` → Pass/Fail. For ANL-05, invert the metric gate for the blind case: a metric gate failing BECAUSE the metrics tier is absent/frozen (counters frozen/zero AND traces absent — both axes blind since Prometheus scrapes the collector) → INCONCLUSIVE, NOT FAIL; a metric gate failing on a LIVE genuine conservation gap (counters present, orch_consumed != proc_sent) → FAIL. Preserve D-01: keep the non-zero-TelemetryGap-stays-PASS behavior from the 6e90216 path (do not let a reconciled telemetry gap flip PASS→FAIL). Populate CorroborationDetail/HumanSummary with the blind-case reason for INCONCLUSIVE. Add facts to PassFailEngineFacts.cs (invert CleanSnapshot for the blind cases): `*Verdict*Inconclusive*` (ANL-04a), `*Verdict*Fail*` (ANL-04b), `*Verdict*Pass*` (ANL-04c), `*MetricGate*Blind*` (ANL-05 INCONCLUSIVE), `*MetricGate*` live-gap (ANL-05 FAIL). Meet the per-class floor: PASS×1, FAIL≥2 here (ANL-04b + ANL-05 live; ANL-02 in plan 03 is the third), INCONCLUSIVE×2, plus D-01 non-binding-gap-stays-PASS.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*Verdict*"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Inconclusive" tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs` shows the new enum member.
    - `BaseApi.Tests.exe -- --filter-method "*Verdict*"` and `-- --filter-method "*MetricGate*"` each exit 0.
    - The ANL-04a fact asserts `Verdict.Inconclusive` for startedRuns==0 + conservation intact, and that it is Inconclusive even when metricGateOk is false.
    - The ANL-05 live fact asserts `Verdict.Fail` for a genuine orch_consumed != proc_sent gap with live counters.
    - A D-01 fact asserts a non-zero TelemetryGap still yields `Verdict.Pass`.
  </acceptance_criteria>
  <done>Verdict.Inconclusive added; the gate splits on evidence sufficiency; the metric gate inverts for the blind case; all ANL-04/05 facts green.</done>
</task>

<task type="auto">
  <name>Task 2: Plumb exit-code 2 through a dot-sourceable resolution lib + harness + sweep roll-up (D-02, D-03, D-04)</name>
  <read_first>
    - scripts/phase-67-harness.ps1 (:30-34 exit-code table doc, :496-531 STEP H analyzer invoke + report locate + $analyzerExit mirror, :536 final-exit note)
    - scripts/phase-68-sweep.ps1 (:19-32 exit-code doc, :80-85 class switch, :99 verdict read from JSON, :128 final exit)
    - 76-PATTERNS.md § Exit-code table discipline
    - 76-CONTEXT.md D-02 (2 = INCONCLUSIVE), D-03 (sweep-fatal), D-04 (no auto-retry, distinct message)
  </read_first>
  <action>
    Factor the exit-code decision into a NEW dot-sourceable lib `scripts/lib/exit-code-resolution.ps1` containing two pure functions with NO top-level side effects (so dot-sourcing does not execute a script body):
      - `function Resolve-AnalyzerExitCode([object]$Report)` — takes the parsed report object; returns 2 when `$Report.Verdict -eq 'Inconclusive'`, 1 when `-eq 'Fail'`, 0 when `-eq 'Pass'`.
      - `function Resolve-SweepClass([int]$Code)` — returns an object `@{ Class = …; Message = … }` mapping 0→PASS, 1→VERDICT_FAIL, 2→INCONCLUSIVE (with a DISTINCT message naming the instrument-failure cause and advising a deliberate warm-ES re-run with NO auto-retry, e.g. `INCONCLUSIVE — observability tier blind (StartedRuns=0, conservation intact); re-run deliberately against warm ES (no auto-retry)`), 64→BAD_ARG, default→INFRA_ABORT.
    In phase-67-harness.ps1: dot-source the lib near the top; extend the exit-code table doc (:30-34) to add `2   analyzer INCONCLUSIVE verdict (observability-degraded — deliberate re-run, D-02)`. In STEP H, after `$report` is located and BEFORE the harness exits, parse the report JSON `$report` and set `$analyzerExit = Resolve-AnalyzerExitCode $reportObj` (overrides the mirrored dotnet-test exit of 1 — the fixture's `assert Verdict==Pass` fails on Inconclusive so `$LASTEXITCODE` is 1, but the JSON artifact written before the assert carries the true class). Ensure the harness's final exit surfaces `$analyzerExit` (2 for Inconclusive) — do NOT remap 2 to an infra code. In phase-68-sweep.ps1: dot-source the lib and replace the `:80-85` inline switch with `Resolve-SweepClass $code` (so 2→INCONCLUSIVE with the distinct message); keep the final exit "0 IFF all PASS" (:128) so an INCONCLUSIVE run is sweep-fatal (D-03). Update the sweep's exit-code doc block (:19-32) to include 2.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command ". ./scripts/lib/exit-code-resolution.ps1; $j = Join-Path ([System.IO.Path]::GetTempPath()) 'inconclusive.json'; '{\"Verdict\":\"Inconclusive\"}' | Set-Content $j; $code = Resolve-AnalyzerExitCode (Get-Content $j -Raw | ConvertFrom-Json); $cls = Resolve-SweepClass $code; if ($code -eq 2 -and $cls.Class -eq 'INCONCLUSIVE' -and $cls.Message -match 'no auto-retry') { exit 0 } else { Write-Host \"code=$code class=$($cls.Class) msg=$($cls.Message)\"; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `pwsh -NoProfile -Command ". ./scripts/lib/exit-code-resolution.ps1; $j = Join-Path ([System.IO.Path]::GetTempPath()) 'inconclusive.json'; '{\"Verdict\":\"Inconclusive\"}' | Set-Content $j; $code = Resolve-AnalyzerExitCode (Get-Content $j -Raw | ConvertFrom-Json); $cls = Resolve-SweepClass $code; if ($code -eq 2 -and $cls.Class -eq 'INCONCLUSIVE' -and $cls.Message -match 'no auto-retry') { exit 0 } else { exit 1 }"` exits 0 — a synthetic Verdict==Inconclusive JSON resolves to exit 2 AND code 2 maps to the INCONCLUSIVE class with the distinct no-auto-retry message (hermetic, before the live gate).
    - A sibling hermetic assertion confirms `Resolve-AnalyzerExitCode` returns 1 for `{"Verdict":"Fail"}` and 0 for `{"Verdict":"Pass"}` (FAIL/PASS not mislabelled as INCONCLUSIVE — T-76-11/T-76-12).
    - `pwsh -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content scripts/phase-67-harness.ps1 -Raw)); $null = [ScriptBlock]::Create((Get-Content scripts/phase-68-sweep.ps1 -Raw))"` parses both scripts with no syntax error (exit 0).
    - `rg "Resolve-SweepClass" scripts/phase-68-sweep.ps1` and `rg "Resolve-AnalyzerExitCode" scripts/phase-67-harness.ps1` each match (both scripts consume the shared lib).
    - The INCONCLUSIVE roll-up message is distinct from VERDICT_FAIL and names the deliberate-re-run guidance (D-04); the final sweep exit remains non-zero on any INCONCLUSIVE (D-03).
  </acceptance_criteria>
  <done>Exit-code resolution factored into a dot-sourceable lib and proven hermetically (Inconclusive→2→INCONCLUSIVE class with the no-auto-retry message, Fail→1, Pass→0); harness + sweep consume the lib; both scripts parse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lossy telemetry → verdict class | the verdict must classify blindness as INCONCLUSIVE, never silently green (D-03) nor false-FAIL |
| analyzer JSON artifact → harness exit code | the harness trusts the report JSON to derive exit 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-10 | Repudiation | a persistent observability defect silently greened by an auto-retry | mitigate | D-03/D-04: INCONCLUSIVE is sweep-fatal (final exit non-zero) with NO auto-retry and a distinct instrument-failure message advising a deliberate re-run — the harness never hides a blind run |
| T-76-11 | Tampering | harness misreads the report and mislabels a FAIL as INCONCLUSIVE (exit 2) | mitigate | `Resolve-AnalyzerExitCode` sets 2 ONLY when report.Verdict == 'Inconclusive'; a real FAIL keeps exit 1 (hermetically asserted); the artifact is written before the assert so the class is authoritative |
| T-76-12 | Elevation | INCONCLUSIVE misused to mask a genuine conservation gap | mitigate | ANL-05: a live-counter genuine gap yields FAIL, not INCONCLUSIVE — INCONCLUSIVE fires only on frozen/absent counters + absent traces |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` and `-c Release` 0-warning.
- `BaseApi.Tests.exe --filter-not-trait Category=RealStack` green (Verdict + MetricGate facts included).
- The hermetic exit-code-resolution test exits 0 (Inconclusive→2→INCONCLUSIVE class, no-auto-retry message); both PowerShell scripts parse without syntax error and dot-source the shared lib.
</verification>

<success_criteria>
- Three-class verdict on evidence sufficiency; trace-dark + conservation intact → INCONCLUSIVE; partial + real gap → FAIL; complete → PASS (ANL-04).
- Metric gate inverted for the blind case; live gap → FAIL (ANL-05).
- Exit-2 plumbing proven hermetically before the live gate: `Resolve-AnalyzerExitCode` maps Inconclusive → 2; `Resolve-SweepClass` classes 2→INCONCLUSIVE, sweep-fatal, no auto-retry, distinct message (D-02/03/04).
</success_criteria>

<output>
After completion, create `.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-04-SUMMARY.md`
</output>
