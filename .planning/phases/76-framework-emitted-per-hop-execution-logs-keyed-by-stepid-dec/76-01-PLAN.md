---
phase: 76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseProcessor.Core/Processing/OutputTail.cs
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
  - src/BaseProcessor.Core/Processing/PostProcessConsumer.cs
  - tests/BaseApi.Tests/Processor/PerHopLogFacts.cs
  - tests/BaseApi.Tests/Observability/FrameworkNoPayloadFacts.cs
  - tests/BaseApi.Tests/Processor/NonBlockingLogFacts.cs
autonomous: true
requirements: [FW-01, FW-03, FW-04]

must_haves:
  truths:
    - "ProcessorPipeline.RunAsync emits exactly one Information-level record per executed hop, for Completed/Failed/Cancelled, and NOT on clean-absent / reinject / Processing branches"
    - "A processor whose ProcessAsync writes no log of its own still produces the framework record"
    - "The entry step (executionId == Guid.Empty) is logged as an explicit all-zeros marker"
    - "The normal-path record carries the OutputTail-resolved outcome (Completed downgraded to Failed on output-schema failure is honored)"
    - "No framework log template or argument references validatedData / dr.Data / d.Payload; a sentinel fed through a blob appears in no framework record"
    - "A throwing/blocking ILogger neither fails nor delays the hop; the pipeline outcome is unchanged"
  artifacts:
    - path: "src/BaseProcessor.Core/Processing/OutputTail.cs"
      provides: "RunAsync returns the resolved StepOutcome (D-18) so the pipeline logs the true terminal outcome"
      contains: "StepOutcome"
    - path: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs"
      provides: "Guarded per-hop LogInformation at the 5 executed-hop terminal branches, six explicit placeholder ids + outcome"
      contains: "LogInformation"
    - path: "tests/BaseApi.Tests/Processor/PerHopLogFacts.cs"
      provides: "CapturingLogger hermetic facts for FW-01 six-field / no-author-log / entry-marker / D-18-downgrade"
    - path: "tests/BaseApi.Tests/Observability/FrameworkNoPayloadFacts.cs"
      provides: "FW-03 grep-clean + sentinel-through-blob fact"
    - path: "tests/BaseApi.Tests/Processor/NonBlockingLogFacts.cs"
      provides: "FW-04 throwing-ILogger + export-shape structural fact"
  key_links:
    - from: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs:163"
      to: "src/BaseProcessor.Core/Processing/OutputTail.cs RunAsync resolved outcome"
      via: "tuple return consumed for the normal-path per-hop outcome"
      pattern: "outputTail\\.RunAsync"
    - from: "ProcessorPipeline per-hop LogInformation"
      to: "attributes.StepId / attributes.ExecutionId / attributes.Outcome in ES"
      via: "explicit {Placeholder} args on the MEL→OTLP bridge"
      pattern: "\\{StepId\\}.*\\{Outcome\\}"
---

<objective>
Emit the framework's own per-hop execution record from `ProcessorPipeline` — one structured `Information` record per executed hop, for every terminal outcome, carrying ids + outcome ONLY (never payload), on a non-blocking guarded path. Also change `OutputTail.RunAsync` to return the resolved outcome (D-18) so the record logs the TRUE terminal outcome.

Purpose: "Did this step execute" becomes platform-level operability for ANY processor, with zero author-written logs. This is the evidence base the re-keyed analyzer (plans 03/04) binds to.
Output: modified `OutputTail`/`ProcessorPipeline`/`PostProcessConsumer` + three hermetic fact classes (FW-01, FW-03, FW-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-SPEC.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-CONTEXT.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-RESEARCH.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-PATTERNS.md

<interfaces>
<!-- Confirmed live 2026-07-16. Executor works from these — no exploration needed. -->

ProcessorPipeline.RunAsync signature (ProcessorPipeline.cs:58):
```csharp
public async Task RunAsync(EntryStepDispatch d, Guid messageId, CancellationToken ct)
```
Six FW-01 ids in hand: d.StepId, d.ExecutionId (== Guid.Empty for the Mode-2 entry step), d.CorrelationId, d.EntryId, messageId (param), + resolved outcome. Constructor already injects `ILogger<ProcessorPipeline> logger` (:56) — no DI change.

The 5 EMIT branches (skip clean-absent :82, reinject :81/:92, Processing):
- :105  input-schema fail → outputTail.RunAsync(failDr,…)      → Failed
- :138  seam ProcessStatusException → outputTail.RunAsync       → Failed / Cancelled (skip Processing)
- :153  unexpected exception → outputTail.RunAsync              → Failed
- :157  `if (dr is null) return;` Mode-2 spawn handled          → Completed (D-09 entry marker; d.ExecutionId == Guid.Empty passed EXPLICITLY)
- :163  normal completion `var proceed = await outputTail.RunAsync(carried, d.EntryId, ct);` → outcome from D-18 tuple

OutputTail.RunAsync CURRENT signature (OutputTail.cs:49):
```csharp
public async Task<bool> RunAsync(DataResult dr, Guid deleteEntryId, CancellationToken ct)
```
Outcome downgrade the pipeline cannot see (OutputTail.cs:56-59):
```csharp
var result = dr.Result;
if (result == StepOutcome.Completed
    && !ProcessorJsonSchemaValidator.TryValidate(context.OutputDefinition, dr.Data, out _))
    result = StepOutcome.Failed;
```
Two return sites: :70 (INJECT-escalation → false) and :75 (success → true). Both must yield `result`.

Both callers of OutputTail.RunAsync (grep-confirmed):
- ProcessorPipeline.cs :105, :138, :153, :163
- PostProcessConsumer.cs :51  `await outputTail.RunAsync(ctx.Message, Guid.Empty, ctx.CancellationToken);`  (does NOT log a per-hop record — discard the new tuple field)

CapturingLogger<T> analog (ReinjectConsumerFacts.cs:24-48) — records ONLY explicit placeholder args, NOT scope (Pitfall 1):
```csharp
internal sealed class CapturingLogger<T> : ILogger<T> {
    internal sealed record Entry(LogLevel Level, IReadOnlyList<KeyValuePair<string, object?>> State);
    public IReadOnlyList<Entry> Entries => _entries;
    public IDisposable BeginScope<TState>(TState state) where TState : notnull => NullScope.Instance;
    public bool IsEnabled(LogLevel logLevel) => true;
    public void Log<TState>(LogLevel lvl, EventId id, TState state, Exception? ex, Func<TState,Exception?,string> fmt) {
        var pairs = state is IReadOnlyList<KeyValuePair<string, object?>> kvps ? kvps.ToList() : new();
        _entries.Add(new Entry(lvl, pairs));
    }
}
```
Placeholder-only template precedent (SampleProcessor.cs:89): `logger.LogInformation("{StepLabel} received {Received} produced {Produced}", label, ...)` — NEVER `$"..."`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OutputTail.RunAsync returns the resolved outcome (D-18) + update both callers</name>
  <read_first>
    - src/BaseProcessor.Core/Processing/OutputTail.cs (:49 signature, :56-59 downgrade, :70/:75 returns)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (:105, :138, :153, :163 callers)
    - src/BaseProcessor.Core/Processing/PostProcessConsumer.cs (:51 caller)
    - 76-PATTERNS.md § OutputTail.cs (D-18 return resolved outcome)
    - 76-CONTEXT.md D-18 (RESOLVED — return the resolved outcome, tuple preferred over out in async)
  </read_first>
  <behavior>
    - After the change, the value `result` computed at OutputTail.cs:56-59 (post output-schema downgrade) is observable by the caller.
    - Existing OutputTail behavior facts (write/inject/send-by-result) still pass — proceed semantics unchanged.
    - ProcessorPipeline.cs:163 normal-path can read the resolved StepOutcome to log the true terminal outcome.
  </behavior>
  <action>
    Change `OutputTail.RunAsync` from `Task<bool>` to `Task<(bool proceed, StepOutcome resolved)>` (tuple — cannot `out` in async, D-18). Return `(false, result)` at the INJECT-escalation site (:70) and `(true, result)` at the success site (:75), where `result` is the post-downgrade local at :56-59. Update the 4 ProcessorPipeline callers: at :105/:138/:153 the outcome is already known from the DataResult the pipeline built, so they may `await outputTail.RunAsync(...);` and ignore/deconstruct the tuple (do not change control flow there). At :163 replace `var proceed = await outputTail.RunAsync(carried, d.EntryId, ct);` with `var (proceed, resolvedOutcome) = await outputTail.RunAsync(carried, d.EntryId, ct);` and keep the `if (!proceed) return;` guard; `resolvedOutcome` is consumed by Task 2's per-hop emission. Update PostProcessConsumer.cs:51 to deconstruct-and-discard: `_ = await outputTail.RunAsync(ctx.Message, Guid.Empty, ctx.CancellationToken);` (it logs no per-hop record).
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Debug` compiles 0-warning (both callers updated; no CS on the tuple return).
    - grep of OutputTail.cs shows `Task<(bool proceed, StepOutcome resolved)>` on RunAsync.
    - The existing hermetic OutputTail/pipeline fact suite passes: `BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0.
  </acceptance_criteria>
  <done>OutputTail.RunAsync returns the resolved outcome; ProcessorPipeline:163 and PostProcessConsumer:51 updated; hermetic suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit the guarded per-hop record at the 5 executed-hop branches (FW-01, D-09, D-10, D-18)</name>
  <read_first>
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (:58 RunAsync, the 5 emit branches :105/:138/:153/:157/:163, branch map in interfaces above)
    - 76-RESEARCH.md § §1 branch map + Code Examples (processor per-hop shape)
    - 76-PATTERNS.md § ProcessorPipeline.cs (guarded helper shape, D-09 explicit-empty note)
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs:24-48 (CapturingLogger<T> to copy)
    - 76-CONTEXT.md D-05 (one record at completion), D-06 (Information level), D-08 (site is ProcessorPipeline not OutputTail), D-09 (entry marker explicit Guid.Empty), D-10 (six fields, placeholder-only)
  </read_first>
  <behavior>
    - PerHopLogFacts: a Completed hop yields exactly ONE captured Entry at LogLevel.Information whose State contains all six keys StepId, ExecutionId, CorrelationId, EntryId, MessageId, Outcome.
    - A Failed hop (input-schema fail path) yields exactly one record with Outcome == "Failed"; a Cancelled seam yields Outcome == "Cancelled".
    - A processor whose ProcessAsync writes NO log still yields exactly one framework Entry.
    - The Mode-2 entry step (d.ExecutionId == Guid.Empty) yields one Entry whose ExecutionId value is the all-zeros Guid (explicit, not omitted).
    - Clean-absent (:82) / reinject / Processing branches yield ZERO per-hop Entries.
    - D-18: a normal-completion hop whose output blob fails the output schema logs Outcome == "Failed" (matches the StepFailed wire), NOT "Completed".
  </behavior>
  <action>
    Add a private guarded helper to ProcessorPipeline: `private void LogHopExecuted(EntryStepDispatch d, Guid messageId, string outcome) { try { logger.LogInformation("hop executed {StepId} {ExecutionId} {CorrelationId} {EntryId} {MessageId} {Outcome}", d.StepId, d.ExecutionId, d.CorrelationId, d.EntryId, messageId, outcome); } catch { /* FW-04: observability must never fail or delay the hop */ } }` — placeholder-only, NO `$"..."`, NO payload arg (FW-03). Call it at exactly the 5 emit branches: before `return;` at :105 with `nameof(StepOutcome.Failed)` (or the string "Failed"); at :138 pass the resolved status ("Failed"/"Cancelled") from the `statusDr.Result` switch and DO NOT emit when the result is `Processing`; at :153 with "Failed"; at :157 (Mode-2 `if (dr is null) return;`) with "Completed" — d.ExecutionId is already Guid.Empty for the entry step and MUST be passed explicitly (D-09; ambient scope omits empty Guids, CapturingLogger sees only explicit args); at :163 pass `resolvedOutcome.ToString()` from Task 1's tuple (D-18) AFTER the `if (!proceed) return;` guard (so an INJECT-escalated hop that returned false does NOT double-log — it already emitted nothing and ended the round trip). Do NOT call it on the clean-absent (:82), reinject (:81/:92), or Processing branches. Create tests/BaseApi.Tests/Processor/PerHopLogFacts.cs copying CapturingLogger<T> from ReinjectConsumerFacts.cs:24-48, building a ProcessorPipeline with the capturing logger + a no-op author processor, and covering each behavior bullet. Name the fact methods so `--filter-method "*PerHop*"` selects them.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*PerHop*"</automated>
  </verify>
  <acceptance_criteria>
    - `BaseApi.Tests.exe -- --filter-method "*PerHop*"` exits 0.
    - A grep of ProcessorPipeline.cs shows `LogInformation("hop executed {StepId} {ExecutionId} {CorrelationId} {EntryId} {MessageId} {Outcome}"` wrapped in `try { … } catch`.
    - The Mode-2 entry-marker fact asserts the captured ExecutionId equals `Guid.Empty`.
    - The D-18 fact asserts a output-schema-forced-Failed normal hop logs Outcome "Failed".
    - No per-hop Entry is captured on the clean-absent branch fact.
  </acceptance_criteria>
  <done>Exactly-one per-hop record on the 5 executed branches with all six fields; entry-marker + D-18-downgrade + no-author-log facts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FW-03 no-payload (grep + sentinel) and FW-04 non-blocking (throwing ILogger + export shape)</name>
  <read_first>
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (the new LogHopExecuted template from Task 2)
    - src/BaseConsole.Core/DependencyInjection/BaseConsoleObservabilityExtensions.cs (:51-60 AddOtlpExporter / ParseStateValues — FW-04 export-shape structural)
    - 76-RESEARCH.md § Pitfall 4 (throwing ILogger guard) + § Security Domain (FW-03)
    - 76-PATTERNS.md § Placeholder-only / no-payload discipline
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs:24-48 (CapturingLogger to sub-class into a ThrowingLogger)
  </read_first>
  <behavior>
    - FrameworkNoPayloadFacts: a repo scan of the new framework log templates/args contains none of the literals `validatedData`, `dr.Data`, `d.Payload`.
    - FrameworkNoPayloadFacts: feeding a unique sentinel string through a processor's input/output blob leaves that sentinel absent from every captured framework Entry's State values.
    - NonBlockingLogFacts: a ThrowingLogger<ProcessorPipeline> whose Log throws does NOT propagate out of RunAsync and the pipeline reaches the same terminal outcome as with a normal logger.
    - NonBlockingLogFacts: the OTLP logs export is configured batch/drop (structural assertion on the observability wiring), not Simple/blocking.
  </behavior>
  <action>
    Create tests/BaseApi.Tests/Observability/FrameworkNoPayloadFacts.cs: (a) a grep/assert fact that reads the ProcessorPipeline.cs and OrchestratorPrePipeline.cs source (or asserts against the known template strings) and fails if any framework log template or argument list contains `validatedData`, `dr.Data`, or `d.Payload`; (b) a CapturingLogger-based fact that runs a hop with a distinctive sentinel (e.g. "SENTINEL-FW03-<guid>") in the blob and asserts no captured Entry's KeyValuePair values `.ToString()` contains the sentinel. Create tests/BaseApi.Tests/Processor/NonBlockingLogFacts.cs: define `ThrowingLogger<T> : ILogger<T>` whose `Log` throws `InvalidOperationException`, build a ProcessorPipeline with it, run a Completed hop, and assert (1) no exception surfaces from RunAsync, (2) the pipeline outcome (blob written / result sent) matches the normal-logger run. Add the export-shape structural assertion: assert `AddOtlpExporter` for logs uses `ExportProcessorType.Batch` (per RESEARCH A1; if the default cannot be asserted structurally, assert the wiring in BaseConsoleObservabilityExtensions declares Batch explicitly — if it does not, add the explicit `ExportProcessorType.Batch` there and note it in the SUMMARY). Name facts so `*Framework*NoPayload*` and `*NonBlocking*` / `*ExportShape*` select them.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*NoPayload*"</automated>
  </verify>
  <acceptance_criteria>
    - `BaseApi.Tests.exe -- --filter-method "*NoPayload*"` and `-- --filter-method "*NonBlocking*"` each exit 0.
    - Repo grep `rg "validatedData|dr\.Data|d\.Payload" src/BaseProcessor.Core/Processing/ProcessorPipeline.cs src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs` returns no match inside a `logger.Log*` template or its args (the FW-03 no-payload template rule).
    - The throwing-logger fact asserts RunAsync completes with the unchanged terminal outcome and no surfaced exception.
    - The export-shape assertion confirms batch/drop.
  </acceptance_criteria>
  <done>FW-03 grep-clean + sentinel-absent facts green; FW-04 throwing-logger + batch/drop facts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor consume → telemetry (OTLP→ES) | per-hop records leave the trusted consume path and land in production log storage at real volume |
| exporter → consume path | a stalled/throwing exporter could apply backpressure back into the hop (FW-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Information disclosure | ProcessorPipeline per-hop log | mitigate | FW-03: ids-only placeholder template; NEVER validatedData/dr.Data/d.Payload. Enforced by the Task 3 grep-clean assertion + the sentinel-through-blob capturing fact |
| T-76-02 | Denial of service | throwing/blocking exporter as a consume participant | mitigate | FW-04: the new LogInformation is wrapped in try/catch swallow; export stays batch/drop. Task 3 ThrowingLogger fact proves the hop is unaffected |
| T-76-03 | Tampering | log-template injection via ids | accept | ids are server-minted Guids placed as structured `{Placeholder}` values, never interpolated — same discipline as InboundExecutionScopeConsumeFilter |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` and `-c Release` build 0-warning.
- `BaseApi.Tests.exe --filter-not-trait Category=RealStack` green (PerHop + NoPayload + NonBlocking classes included).
- FW-03 repo grep clean on the new templates.
</verification>

<success_criteria>
- One Information per-hop record per executed hop across Completed/Failed/Cancelled; none on non-execution branches (FW-01).
- No-author-log processor still yields the record; entry-marker carries explicit Guid.Empty; normal-path honors the D-18 resolved outcome.
- No payload literal or sentinel in any framework record (FW-03).
- Throwing/blocking ILogger neither fails nor delays the hop; export batch/drop (FW-04).
</success_criteria>

<output>
After completion, create `.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-01-SUMMARY.md`
</output>
