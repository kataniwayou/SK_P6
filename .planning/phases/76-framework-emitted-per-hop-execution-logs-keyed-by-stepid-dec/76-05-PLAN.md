---
phase: 76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec
plan: 05
type: execute
wave: 3
depends_on: [76-01, 76-02, 76-03, 76-04]
files_modified: []
autonomous: false
requirements: [FW-01, FW-02, FW-03, FW-04, ANL-01, ANL-02, ANL-03, ANL-04, ANL-05, SMP-01]

must_haves:
  truths:
    - "After editing ProcessorPipeline.cs (BaseProcessor.Core), a clean-rebuild of BOTH configs + FK-safe graph DELETE + reseed makes POST /api/v1/orchestration/start return 204 (the 204 proves SourceHash currency)"
    - "phase-68-sweep.ps1 runs all 7 scenarios on a real Docker stack after the reseed"
    - "Every non-PASS verdict is examined and classified as one of: genuine data loss / telemetry gap / observability-degraded (INCONCLUSIVE) / blind-spot closure now correctly failing"
  artifacts:
    - path: ".planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-05-SUMMARY.md"
      provides: "the sweep roll-up + per-scenario classification of every non-PASS verdict"
  key_links:
    - from: "SourceHash reseed (rebuild BOTH configs → graph DELETE → seed → restart)"
      to: "POST /start 204"
      via: "container hash == seeder host-build hash"
      pattern: "204"
    - from: "phase-68-sweep.ps1"
      to: "7 analyzer verdicts + roll-up classes"
      via: "live 7-scenario sweep"
      pattern: "phase-68-sweep"
---

<objective>
The live acceptance gate. Editing `ProcessorPipeline.cs` (BaseProcessor.Core) changes `Processor.Sample`'s embedded SourceHash (`SourceHash.targets:82` folds BaseProcessor.Core's own sources), so a MANDATORY reseed is required before the sweep — else `POST /start` returns 422. After the reseed proves currency with a 204, run the full 7-scenario resilience sweep on a real Docker stack and classify every non-PASS verdict.

Purpose: prove the framework-emitted evidence + three-class verdict work end-to-end on real telemetry, not just hermetic synthetic records.
Output: `76-05-SUMMARY.md` capturing the sweep roll-up and the classification of every non-PASS scenario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-SPEC.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-RESEARCH.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-01-SUMMARY.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-02-SUMMARY.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-03-SUMMARY.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-04-SUMMARY.md

<interfaces>
<!-- SourceHash reseed order (SPEC constraint, RESEARCH §8 — all scripts confirmed present). -->
- SourceHash.targets:82 `@(ImplFiles)` folds `$(MSBuildThisFileDirectory)**\*.cs` (BaseProcessor.Core) + `$(MSBuildProjectDirectory)\**\*.cs` (the concrete) → editing ProcessorPipeline.cs changes Processor.Sample's embedded hash.
- Reseed scripts: scripts/phase-68-sweep.ps1 (drives all 7), scripts/phase-67-harness.ps1 (per-scenario; STEP B calls phase-65-reset.ps1), scripts/phase-65-reset.ps1 (FLUSHALL + heal-wait + FK-safe graph DELETE).
- Seeder fact: `BaseApi.Tests.exe --filter-method "*FanOutSeeder_SeedsAndSelfVerifies*"` — SeedProcessorAsync(hostHash) creates the processor row for the new hash.
- Liveness keys pattern: `skp:proc:*:*` (~10s to appear).
- Activation gate: `POST http://localhost:8080/api/v1/orchestration/start` must return 204 (422 = hash still diverged). Port 8080 is the baseapi-service host mapping (compose.yaml:357 `8080:8080`); the phase-67 harness STEP E hits this exact URL and requires 204.
- Pitfall 5 (RESEARCH): `-c Debug` alone leaves a stale Release dll with the OLD hash → seeder binds the wrong processor row → 422 even when heal-wait passes. Clean-rebuild BOTH configs.
- Pitfall 7 (RESEARCH / user memory): a cold ES `StartedRuns=0` with intact conservation is EXACTLY the D-01 INCONCLUSIVE shape (TEST-01 can flake on cold ES) — the three-class verdict now correctly labels it INCONCLUSIVE; a warm-ES deliberate re-run is the correct response, not auto-retry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mandatory SourceHash reseed to a POST /start 204</name>
  <read_first>
    - 76-SPEC.md § Constraints (the 5-step reseed order) + § Acceptance Criteria (live gate)
    - 76-RESEARCH.md § §8 SourceHash reseed reality + § Pitfall 5
    - src/BaseProcessor.Core/SourceHash.targets (:82 fold)
  </read_first>
  <action>
    Execute the reseed in EXACT order (Pitfall 5 — do NOT skip the Release clean-rebuild):
    1. Clean-rebuild BOTH configs: `rm -rf src/Processor.Sample/obj src/Processor.Sample/bin/Release && dotnet build SK_P.sln -c Release --no-incremental` (then a Debug build if the test host is Debug: `dotnet build SK_P.sln -c Debug --no-incremental`). Both must build 0-warning.
    2. FK-safe graph DELETE of the 6 workflow tables (the idempotent FanOutSeeder GET-matches by name and would not rebind the stale workflow otherwise) — use the project's reset path (scripts/phase-65-reset.ps1 performs the FK-safe graph DELETE) or the documented delete order.
    3. Seed: run `tests/BaseApi.Tests/bin/.../BaseApi.Tests.exe --filter-method "*FanOutSeeder_SeedsAndSelfVerifies*"` — creates the processor row for the new hash.
    4. Verify liveness keys appear (`skp:proc:*:*`, ~10s) → `docker compose restart orchestrator` → `POST http://localhost:8080/api/v1/orchestration/start`.
    The 204 IS the proof of SourceHash currency. If POST returns 422, STOP — the hash still diverged (re-check step 1's Release clean-rebuild); do not proceed to the sweep.
  </action>
  <verify>
    <automated>test "$(curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8080/api/v1/orchestration/start)" = "204"</automated>
    <manual>Steps 1-4 (clean-rebuild BOTH configs, FK-safe graph DELETE, seed, `docker compose restart orchestrator`) require the live Docker stack up and must be applied before the 204 check. A 422 from the curl means the SourceHash still diverged — resolve step 1's Release clean-rebuild (Pitfall 5) and re-run. Record the returned status code in the SUMMARY.</manual>
  </verify>
  <acceptance_criteria>
    - Both `-c Release` and `-c Debug` builds complete 0-warning with `--no-incremental`.
    - Liveness keys `skp:proc:*:*` are present after seeding.
    - `curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8080/api/v1/orchestration/start` prints `204` (a 422 blocks progression and must be resolved first).
  </acceptance_criteria>
  <done>Reseed complete; POST /start returns 204 proving the container hash matches the seeder host-build hash.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the live 7-scenario sweep and classify every non-PASS verdict</name>
  <action>Run `pwsh -File scripts/phase-68-sweep.ps1` on the reseeded live stack, then examine each scenario analyzer JSON artifact and the roll-up class and classify every non-PASS verdict per the four-way scheme in how-to-verify. This is an interpretive act (VALIDATION.md Manual-Only) — pause for the operator to confirm the classification.</action>
  <what-built>The framework per-hop + orchestrator fan-out/terminal records (plans 01/02), the stepId-keyed analyzer + three-class verdict (plans 03/04), and the exit-2 sweep plumbing are all landed. Task 1 reseeded to a 204. This checkpoint runs the live 7-scenario sweep and classifies the results.</what-built>
  <how-to-verify>
    1. From the repo root, run: `pwsh -File scripts/phase-68-sweep.ps1`
    2. Wait for all 7 scenarios to complete (each does reset + run + analyze). The capstone line reports `N/7 PASS`.
    3. For EACH non-PASS scenario, open its analyzer JSON artifact (`tests/BaseApi.Tests/bin/**/analyzer-reports/<ScenarioId>.json`) and read `Verdict` + `CorroborationDetail`/`HumanSummary` + the roll-up class printed by the sweep.
    4. Classify every non-PASS verdict as exactly one of:
       - genuine data loss (positive evidence of loss — a real conservation gap or a recoverable-but-lost run)
       - telemetry gap (a dropped record reconciled non-binding via framework redundancy — stays PASS)
       - observability-degraded / INCONCLUSIVE (trace-dark + intact conservation, e.g. a cold-ES first pass — Pitfall 7; deliberate warm-ES re-run is the correct response, NOT auto-retry)
       - blind-spot closure now correctly failing (a TEST-08-shape dispatched-but-never-executed step that previously false-PASSed and now correctly FAILs)
    5. If any INCONCLUSIVE is a cold-ES flake (Pitfall 7), deliberately re-run that scenario against warm ES and record both passes.
    6. Record the roll-up (N/7 PASS) and the per-scenario classification in 76-05-SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "approved" with the classification recorded, or describe any unexplained non-PASS verdict.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited framework source → live processor identity | a stale Release dll (old SourceHash) silently binds the wrong processor row → 422 masquerading as infra |
| live sweep verdict → operator classification | an INCONCLUSIVE must be recognized as a witness failure, never auto-greened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-13 | Spoofing | stale Release dll binds the wrong processor row (SourceHash divergence) | mitigate | Mandatory clean-rebuild of BOTH configs (Pitfall 5); the 204 on POST /start is the proof of currency; a 422 blocks the sweep |
| T-76-14 | Repudiation | a cold-ES INCONCLUSIVE mistaken for a green or a FAIL | mitigate | Three-class verdict labels it INCONCLUSIVE (Pitfall 7); the operator deliberately re-runs against warm ES and records both — no auto-retry |
| T-76-15 | Information disclosure | container logs dumped during the sweep leak payload | accept | Sweep logs carry framework ids-only records (FW-03, verified hermetically in plans 01/02); sample values are synthetic (D-03) |
</threat_model>

<verification>
- `curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8080/api/v1/orchestration/start` prints 204 after the reseed.
- `pwsh -File scripts/phase-68-sweep.ps1` completes all 7 scenarios; every non-PASS verdict is classified in the SUMMARY.
- Any INCONCLUSIVE from a cold-ES flake is resolved by a deliberate warm-ES re-run (Pitfall 7), both recorded.
</verification>

<success_criteria>
- The SourceHash reseed proves currency with a 204 (not 422).
- The live 7-scenario sweep runs on a real Docker stack after the reseed.
- Every non-PASS verdict is explained and classified (genuine loss / telemetry gap / observability-degraded INCONCLUSIVE / blind-spot closure). The classification is recorded in 76-05-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-05-SUMMARY.md`
</output>
