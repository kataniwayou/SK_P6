---
phase: 79-falsification-harness-for-the-resilience-sweep-negative-cont
plan: 05
type: execute
wave: 4
depends_on: ["01", "03", "04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Running scripts/phase-79-falsify.ps1 live exits 0 — the gate correctly flipped FALSIFY-01 to VERDICT_FAIL for the recoverable-but-lost binding-miss reason (True Positive fires; no silent-green False Negative)"
    - "The defeated reinject produces exactly ONE lost execution (Missing==1) with keeper 'reinject' logged — byte-identical to a genuine recoverable-but-lost strand"
    - "With KEEPER_DEFEAT_REINJECT unset (present-but-off in compose), a clean phase-68-sweep.ps1 (TEST-01..07) still reproduces 7/7 PASS (capstone exit 0) — the seam is provably inert (D-06)"
  artifacts:
    - path: "analyzer-reports/FALSIFY-01.json"
      provides: "the analyzer report proving the binding miss for the negative control"
      contains: "binding miss"
  key_links:
    - from: "keeper suppress-send seam (armed for FALSIFY-01)"
      to: "VERDICT_FAIL"
      via: "phase-79-falsify.ps1 exit 0"
      pattern: "exit 0"
    - from: "keeper seam present-but-unset"
      to: "7/7 capstone PASS"
      via: "phase-68-sweep.ps1 capstone exit 0"
      pattern: "CAPSTONE: 7/7 PASS"
---

<objective>
Prove the negative control end-to-end on the live stack, and prove the seam is inert when unset. Two live gates:
1. **Gate-teeth (True Positive):** run `scripts/phase-79-falsify.ps1` — it must exit 0, meaning the sweep flipped FALSIFY-01 to VERDICT_FAIL for the recoverable-but-lost binding-miss reason (harness exit exactly 1, Missing >= 1, MissingDetail names recoverable-but-lost → binding miss, sweep wrapper non-zero).
2. **Inertness (No False Positive on the capstone):** run the default `scripts/phase-68-sweep.ps1` (TEST-01..07) with the seam present-but-unset — it must still reproduce 7/7 PASS (capstone exit 0), proving KEEPER_DEFEAT_REINJECT default-off does not perturb the green capstone (D-06).

Purpose: This is the phase's whole reason for existing — show the gate goes RED for the right reason AND stays GREEN when the seam is off. Because a keeper PRODUCT source changed, the live run must honor the rebuild → reseed order and watch the documented live-gate flakes (cold-ES INCONCLUSIVE ≠ FAIL; stale Debug report — trust the harness exit code).
Output: A passing live falsification run + a re-proven 7/7 inertness sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-falsification-harness-for-the-resilience-sweep-negative-cont/79-CONTEXT.md
@.planning/phases/79-falsification-harness-for-the-resilience-sweep-negative-cont/79-04-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live gate-teeth run — phase-79-falsify.ps1 must exit 0 (True Positive fires)</name>
  <read_first>
    - .planning/phases/79-falsification-harness-for-the-resilience-sweep-negative-cont/79-04-PLAN.md (the driver's exit-code contract)
    - .planning/phases/79-falsification-harness-for-the-resilience-sweep-negative-cont/79-CONTEXT.md (code_context Integration Points — live reseed order; specifics — the live-gate traps)
    - MEMORY notes [[rebuild-sourcehash-reseed-order]] and [[phase-68-sweep-stale-report-read]] (reseed order + trust-the-harness-exit)
  </read_first>
  <what-built>
    The keeper suppress-send seam (Plan 01), its compose plumbing, the FALSIFY-01 harness scenario with keeper scaled to 1 replica + K_EXECUTIONS=1 (Plan 03), and the inverted-assertion driver (Plan 04) are all in place. This checkpoint runs the whole negative control on the live Docker stack.
  </what-built>
  <action>
    Run the live gate-teeth negative control and confirm exit 0. A keeper PRODUCT source changed, so honor the rebuild → reseed order (the harness STEP A0 rebuilds images; if the between-runs reset heal-wait aborts on a missing processor row, graph-DELETE → seed → 204-gate → then run, per [[rebuild-sourcehash-reseed-order]]).
    1. Ensure Docker Desktop is up.
    2. From the repo root: `pwsh -File scripts/phase-79-falsify.ps1` (it invokes phase-68-sweep.ps1 -ScenarioIds FALSIFY-01 → brings up / reseeds / observes / analyzes).
    3. On a cold-ES INCONCLUSIVE (harness exit 2, driver exit 2), re-run deliberately against warm ES — INCONCLUSIVE is neither PASS nor FAIL and is NOT a passing control.
    4. Present the evidence for operator confirmation: the driver exit code, the `analyzer-reports/phase-68-summary.json` FALSIFY-01 row (verdict/class/harnessExit), the freshest `**/analyzer-reports/FALSIFY-01.json` (Missing + MissingDetail), and the harness "keeper scaled to 1 replica (1 running)" line.
    5. If the driver exits 1 (gate-teeth regression / silent-green), capture the summary + report and STOP — the negative control did not fire; this is a real finding to resolve (do NOT rubber-stamp).
  </action>
  <how-to-verify>
    - Expected: driver prints `NEGATIVE CONTROL PASSED — the gate correctly FAILED FALSIFY-01 ... Gate has teeth.` and exits 0.
    - FALSIFY-01 harness exit == 1 (VERDICT_FAIL); NOT 2 (INCONCLUSIVE), NOT infra abort (10-70/64).
    - phase-68-summary.json FALSIFY-01 row: verdict == "Fail", class == "VERDICT_FAIL", harnessExit == 1.
    - Freshest FALSIFY-01.json: Missing >= 1 and a MissingDetail containing `recoverable-but-lost` and `binding miss`.
    - `docker compose ps keeper` showed exactly ONE keeper instance during the run.
  </how-to-verify>
  <verify>
    <automated>pwsh -File scripts/phase-79-falsify.ps1; echo "driver exit=$LASTEXITCODE"</automated>
  </verify>
  <done>phase-79-falsify.ps1 exits 0; the FALSIFY-01 summary row shows verdict=="Fail" + harnessExit==1; the report shows Missing>=1 with a MissingDetail naming recoverable-but-lost + binding miss; exactly one keeper replica ran. Operator approved.</done>
  <acceptance_criteria>
    - `pwsh -File scripts/phase-79-falsify.ps1` exits 0 (`$LASTEXITCODE -eq 0`)
    - `analyzer-reports/phase-68-summary.json` FALSIFY-01 row has `verdict == "Fail"` AND `harnessExit == 1`
    - The freshest `FALSIFY-01.json` report has `Missing >= 1` AND a `MissingDetail` entry containing both `recoverable-but-lost` and `binding miss`
    - Exactly one keeper replica ran during the scenario (harness log line "keeper scaled to 1 replica (1 running)")
  </acceptance_criteria>
  <resume-signal>Type "approved" once phase-79-falsify.ps1 exits 0 with the FALSIFY-01 evidence above, or describe the failure (e.g. exit 1 silent-green, or exit 2 cold-ES flake to re-run).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Inertness regression gate — default TEST-01..07 sweep still 7/7 PASS with the seam present-but-unset</name>
  <read_first>
    - .planning/phases/79-falsification-harness-for-the-resilience-sweep-negative-cont/79-CONTEXT.md (D-06 inertness guarantee — non-negotiable)
    - .planning/phases/79-falsification-harness-for-the-resilience-sweep-negative-cont/79-01-PLAN.md (why the seam is inert when unset — short-circuit + ${KEEPER_DEFEAT_REINJECT:-0})
  </read_first>
  <what-built>
    The keeper seam and its compose plumbing are present in product source, defaulting off (`${KEEPER_DEFEAT_REINJECT:-0}`). This checkpoint proves their mere presence does not perturb the green capstone.
  </what-built>
  <action>
    Prove the seam is inert when unset by re-running the default capstone.
    1. Clear the seam from the shell: `Remove-Item Env:KEEPER_DEFEAT_REINJECT -ErrorAction SilentlyContinue` (the `${KEEPER_DEFEAT_REINJECT:-0}` default then interpolates to 0 → seam inert).
    2. Run the default capstone (byte-unchanged TEST-01..07, FALSIFY-01 NOT included): `pwsh -File scripts/phase-68-sweep.ps1`.
    3. On a single-scenario cold-ES INCONCLUSIVE (typically TEST-01), re-run that scenario deliberately against warm ES: `pwsh -File scripts/phase-67-harness.ps1 -ScenarioId TEST-01`. INCONCLUSIVE ≠ FAIL; the inertness claim requires 7/7 PASS, not a FAIL on any row.
    4. Confirm the hermetic suite stays green (seam is Docker-gated live-only): run `BaseApi.Tests.exe --filter-not-trait Category=RealStack` directly (net8.0; `dotnet test` hangs on Windows MTP) — exit 0.
    5. Present the capstone roll-up (7/7 PASS, exit 0) + the hermetic exit code for operator confirmation.
  </action>
  <how-to-verify>
    - `pwsh -File scripts/phase-68-sweep.ps1` prints `CAPSTONE: 7/7 PASS` and exits 0; each TEST-0x row harnessExit == 0.
    - The default `$ScenarioIds` in phase-68-sweep.ps1 is byte-unchanged (seven TEST-01..07 ids; FALSIFY-01 absent).
    - Hermetic suite green (`BaseApi.Tests.exe --filter-not-trait Category=RealStack` exit 0).
  </how-to-verify>
  <verify>
    <automated>pwsh -File scripts/phase-68-sweep.ps1; echo "sweep exit=$LASTEXITCODE"</automated>
  </verify>
  <done>The default TEST-01..07 sweep reaches 7/7 PASS (exit 0) with KEEPER_DEFEAT_REINJECT unset; the default id list is unchanged; the hermetic suite is green. Operator approved — D-06 inertness confirmed.</done>
  <acceptance_criteria>
    - `pwsh -File scripts/phase-68-sweep.ps1` prints `CAPSTONE: 7/7 PASS` and exits 0
    - `analyzer-reports/phase-68-summary.json` shows all seven TEST-0x rows with `harnessExit == 0`
    - The default `$ScenarioIds` in phase-68-sweep.ps1 is byte-unchanged (`grep` shows the seven TEST-01..07 ids; FALSIFY-01 absent)
    - Hermetic suite green: `BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0
  </acceptance_criteria>
  <resume-signal>Type "approved" once the default sweep reaches 7/7 PASS (exit 0) with the seam present-but-unset and the hermetic suite is green, or describe any non-INCONCLUSIVE failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live stack | Operator runs the live gate + inertness sweep; the seam is armed ONLY for the FALSIFY-01 run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-12 | Tampering | seam accidentally active during the capstone | mitigate | Task 2 IS the mitigation — it proves the default TEST-01..07 sweep reaches 7/7 PASS with KEEPER_DEFEAT_REINJECT unset, empirically confirming the default-off short-circuit. This is the primary residual-LOW evidence for T-79-01. Residual: LOW. |
| T-79-13 | Repudiation | cold-ES INCONCLUSIVE misread as a passing/failing verdict | mitigate | Both checkpoints require the harness exit be classified exactly (FALSIFY-01 must be 1, not 2; a TEST-0x INCONCLUSIVE is re-run, never counted as PASS or FAIL). Residual: LOW. |
</threat_model>

<verification>
- phase-79-falsify.ps1 exits 0 with the FALSIFY-01 VERDICT_FAIL evidence (True Positive).
- Default phase-68-sweep.ps1 reaches 7/7 PASS with the seam unset (inertness / D-06).
- Hermetic suite green throughout.
</verification>

<success_criteria>
- The resilience-sweep gate is proven to have teeth: it fails a genuinely-lost execution for the right reason, and it stays green when the seam is off. The milestone's 7/7 true-negative sweeps become admissible evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/79-falsification-harness-for-the-resilience-sweep-negative-cont/79-05-SUMMARY.md`
</output>
