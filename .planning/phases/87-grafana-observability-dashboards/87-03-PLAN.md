---
phase: 87-grafana-observability-dashboards
plan: 03
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - k8s/dashboards/runtime.json
autonomous: true
requirements: [RTD-01, RTD-02, RTD-03, VAR-03, DASH-04]

must_haves:
  truths:
    - "The runtime dashboard covers all four service classes from the process_runtime_dotnet_* family only"
    - "GC, memory, thread-pool, exception and JIT axes each have their own panel"
    - "Every timeseries panel separates one replica from its peers by grouping on service_instance_id"
    - "No panel is titled CPU, Uptime or Working Set — the substitutes are titled for what they measure"
    - "Every panel honors both the source and pod dropdown selections"
  artifacts:
    - path: "k8s/dashboards/runtime.json"
      provides: "17 runtime panels across GC / memory / thread pool / exceptions / JIT / restarts"
      contains: "process_runtime_dotnet_jit_methods_compiled_count_total"
  key_links:
    - from: "every runtime panel target"
      to: "the source and pod template variables"
      via: "selector filter"
      pattern: "source=~\"\\$source\", service_instance_id=~\"\\$pod\""
    - from: "every timeseries panel target"
      to: "per-replica separability"
      via: "by-clause grouping"
      pattern: "by \\(source, service_instance_id"
---

<objective>
Author the complete panel set of the runtime instrumentation dashboard.

Purpose: RTD-01/02/03. The four service classes share exactly one instrumentation — `OpenTelemetry.Instrumentation.Runtime` — and this dashboard is the single place a stack-wide runtime regression becomes visible, per pod.

Output: `k8s/dashboards/runtime.json` grown from the one contract panel that plan 87-01 authored to the full 17-panel set, all against the live-verified `process_runtime_dotnet_*` inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md
@.planning/phases/87-grafana-observability-dashboards/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the GC and memory panel family</name>
  <files>k8s/dashboards/runtime.json</files>
  <read_first>
    - k8s/dashboards/runtime.json (the file being modified — plan 87-01 authored its top-level shape, the three template variables, and panel id 1 "GC collections / sec by generation", which is the exact structural template every panel below copies)
    - scripts/phase-87-dashboard-lint.ps1 (the rules this file must satisfy — in particular S7 both-filters, S8 no `\bdotnet_`, S10 runtime-allowlist-only, S11 timeseries by-clause, S12 forbidden titles)
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md sections "Complete live runtime inventory", "FINDING F-2", "FINDING F-3", "Runtime dashboard (RTD-01/02/03)" (the panel/PromQL/unit table), "Row organisation"
  </read_first>
  <action>
Append six panels to `panels[]`, ids 2 through 7, each copying panel 1's structure exactly: type timeseries, object-form `${datasource}` reference at both panel and target level, `gridPos` h 8 w 12 laying out two per row (x alternating 0 and 12, y advancing by 8), `fieldConfig.defaults.unit`, `options.legend` with displayMode table / placement bottom / calcs mean and max, `options.tooltip` mode multi, one target with refId A, editorMode code, range true, and a `legendFormat`.

The common selector on every expression is `{source=~"$source", service_instance_id=~"$pod"}` and the rate window is always `$__rate_interval` — never a hardcoded `[5m]`.

Panel 2 — title `GC heap size by generation`, unit bytes, expr `sum by (source, service_instance_id, generation) (process_runtime_dotnet_gc_heap_size_bytes{...})`, legendFormat `{{source}} / {{service_instance_id}} / {{generation}}`.

Panel 3 — title `GC heap fragmentation by generation`, unit bytes, expr `sum by (source, service_instance_id, generation) (process_runtime_dotnet_gc_heap_fragmentation_size_bytes{...})`, same legendFormat.

Panel 4 — title `Allocation rate`, unit Bps, expr `sum by (source, service_instance_id) (rate(process_runtime_dotnet_gc_allocations_size_bytes_total{...}[$__rate_interval]))`, legendFormat `{{source}} / {{service_instance_id}}`.

Panel 5 — title `GC committed memory`, unit bytes, expr `sum by (source, service_instance_id) (process_runtime_dotnet_gc_committed_memory_size_bytes{...})`. Add a panel `description` stating this is GC-committed bytes, NOT the OS working set: no `process_memory_working_set` metric exists in this stack, and obtaining one would need `OpenTelemetry.Instrumentation.Process` (a src/ change) or a Prometheus scrape-config change — both out of scope (research F-2, RTD-02 amendment). Note in the same description that this gauge is absent for `source="webapi"` until that pod performs its first GC (research F-3).

Panel 6 — title `Live object bytes`, unit bytes, expr `sum by (source, service_instance_id) (process_runtime_dotnet_gc_objects_size_bytes{...})`.

Panel 7 — title `GC time per second`, unit percentunit, expr `sum by (source, service_instance_id) (rate(process_runtime_dotnet_gc_duration_nanoseconds_total{...}[$__rate_interval])) / 1e9`. Add a description stating this is seconds of GC per second — a GC-pressure proxy standing in for process CPU, which this instrumentation does not emit at all (research F-2). Do not title it CPU; an empty or mislabelled CPU panel is a defect under the RTD-02 amendment.

Do not use `repeat` rows on `$source`. A single panel per axis with a table legend showing per-series mean and max makes the outlier replica obvious in one place, where a repeat would multiply the panel count by four and inflate the JSON.
  </action>
  <verify>
    <automated>pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime -ShapeOnly</automated>
  </verify>
  <acceptance_criteria>
    - `(Get-Content k8s/dashboards/runtime.json -Raw | ConvertFrom-Json).panels.Count` equals 7
    - Panel titles include exactly `GC heap size by generation`, `GC heap fragmentation by generation`, `Allocation rate`, `GC committed memory`, `Live object bytes`, `GC time per second`
    - No panel title matches the case-insensitive regex `\b(cpu|uptime|working set)\b`
    - Every one of the 7 panels' targets' `expr` contains both `source=~"$source"` and `service_instance_id=~"$pod"`
    - Every `expr` contains `by (source, service_instance_id` (RTD-03 per-replica separability)
    - No `expr` matches the regex `\bdotnet_`
    - Every metric token in the file is a member of the lint's 17-name RUNTIME allowlist
    - `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime -ShapeOnly` exits 0
  </acceptance_criteria>
  <done>The GC and memory family is complete: 6 new panels, all live-verified metric names, both substitution panels carrying descriptions that name the missing real metric and why it cannot be obtained in this milestone.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thread-pool, exception, contention and JIT panel family</name>
  <files>k8s/dashboards/runtime.json</files>
  <read_first>
    - k8s/dashboards/runtime.json (as extended by Task 1)
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md section "Runtime dashboard (RTD-01/02/03)" panel table rows for thread pool, exceptions, lock contention, assemblies, timers and JIT
    - scripts/phase-87-dashboard-lint.ps1
  </read_first>
  <action>
Append eight panels, ids 8 through 15, in the same structural shape and with the same `{source=~"$source", service_instance_id=~"$pod"}` selector on every expression.

Panel 8 — title `Thread-pool threads`, unit short, expr `max by (source, service_instance_id) (process_runtime_dotnet_thread_pool_threads_count{...})`.
Panel 9 — title `Thread-pool queue length`, unit short, expr `max by (source, service_instance_id) (process_runtime_dotnet_thread_pool_queue_length{...})`.
Panel 10 — title `Thread-pool completion rate`, unit ops, expr `sum by (source, service_instance_id) (rate(process_runtime_dotnet_thread_pool_completed_items_count_total{...}[$__rate_interval]))`.
Panel 11 — title `Exception rate`, unit ops, expr `sum by (source, service_instance_id) (rate(process_runtime_dotnet_exceptions_count_total{...}[$__rate_interval]))`.
Panel 12 — title `Lock contention rate`, unit ops, expr `sum by (source, service_instance_id) (rate(process_runtime_dotnet_monitor_lock_contention_count_total{...}[$__rate_interval]))`.
Panel 13 — title `Loaded assemblies`, unit short, expr `max by (source, service_instance_id) (process_runtime_dotnet_assemblies_count{...})`.
Panel 14 — title `Active timers`, unit short, expr `max by (source, service_instance_id) (process_runtime_dotnet_timer_count{...})`.
Panel 15 — title `JIT methods compiled / sec`, unit ops, expr `sum by (source, service_instance_id) (rate(process_runtime_dotnet_jit_methods_compiled_count_total{...}[$__rate_interval]))`.

Every legendFormat is `{{source}} / {{service_instance_id}}`. Continue the two-column gridPos progression.
  </action>
  <verify>
    <automated>pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime -ShapeOnly</automated>
  </verify>
  <acceptance_criteria>
    - `(Get-Content k8s/dashboards/runtime.json -Raw | ConvertFrom-Json).panels.Count` equals 15
    - The file references `process_runtime_dotnet_thread_pool_threads_count`, `process_runtime_dotnet_thread_pool_queue_length`, `process_runtime_dotnet_thread_pool_completed_items_count_total`, `process_runtime_dotnet_exceptions_count_total`, `process_runtime_dotnet_monitor_lock_contention_count_total`, `process_runtime_dotnet_assemblies_count`, `process_runtime_dotnet_timer_count` and `process_runtime_dotnet_jit_methods_compiled_count_total`
    - All 15 panels are type timeseries and every `expr` contains `by (source, service_instance_id`
    - Every `expr` contains both `source=~"$source"` and `service_instance_id=~"$pod"`
    - No two panels share a `gridPos` origin (x, y) pair
    - `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime -ShapeOnly` exits 0
  </acceptance_criteria>
  <done>The thread-pool, exception, contention and JIT axes each have a dedicated per-pod panel, all drawn from the live-verified runtime inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two stat panels that stand in for uptime, and close the runtime coverage gate</name>
  <files>k8s/dashboards/runtime.json</files>
  <read_first>
    - k8s/dashboards/runtime.json (as extended by Task 2)
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md rows "Pods reporting" and "Process restarts in range" in the runtime panel table, plus assumption A4 in the Assumptions Log (the `resets()` reliability caveat) and FINDING F-2
    - scripts/phase-87-dashboard-lint.ps1 (coverage rule C6 requires the four substitution titles to exist)
  </read_first>
  <action>
Append two `stat` panels, ids 16 and 17. Both keep the mandatory selector; per lint rule S11, stat panels are exempt from the by-clause grouping requirement but not from the selector requirement.

Panel 16 — type stat, title `Pods reporting`, unit short, expr `count by (source) (process_runtime_dotnet_gc_collections_count_total{source=~"$source", service_instance_id=~"$pod", generation="gen0"})`, legendFormat `{{source}}`. Description: the honest substitute for a per-class availability readout — this counts pods currently exporting runtime series, per selected service class.

Panel 17 — type stat, title `Process restarts in range`, unit short, expr `sum by (source, service_instance_id) (resets(process_runtime_dotnet_jit_methods_compiled_count_total{source=~"$source", service_instance_id=~"$pod"}[$__range]))`, legendFormat `{{source}} / {{service_instance_id}}`. Set field thresholds green at 0 and red above 0. Description must state: this is a counter-reset proxy standing in for process uptime, which this stack does not emit — there is no `process_start_time_seconds` anywhere in the inventory (research F-2). It must also carry the A4 caveat: a counter reset can also be produced by the collector's `metric_expiration` (5 min default), so a nonzero value is a restart *signal*, not a certified restart count. Record whether the live run in plan 87-05 shows spurious nonzero values on a stable stack.

Finally, run the full runtime lint (not `-ShapeOnly`) so coverage rule C6 is exercised: the titles `GC committed memory`, `GC time per second`, `Process restarts in range` and `Pods reporting` must all be present. If C6 fails, fix the titles, never the rule.
  </action>
  <verify>
    <automated>pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime</automated>
  </verify>
  <acceptance_criteria>
    - `(Get-Content k8s/dashboards/runtime.json -Raw | ConvertFrom-Json).panels.Count` equals 17
    - Exactly two panels have type `stat`, titled `Pods reporting` and `Process restarts in range`
    - Panel 17's `expr` contains `resets(` and `[$__range]`, and its description contains both `metric_expiration` and `uptime`
    - No panel title matches the case-insensitive regex `\b(cpu|uptime|working set)\b` (the word `uptime` appears only inside descriptions, never a title)
    - `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime` exits 0 with the coverage section enabled
    - `kubectl kustomize k8s/` exits 0
    - `git status --porcelain src/` is empty
  </acceptance_criteria>
  <done>All 17 runtime panels exist, the full runtime lint including coverage rule C6 is green, and both uptime/CPU/working-set substitutions carry descriptions naming the metric that does not exist and why it cannot be added in this milestone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo JSON -> Grafana provisioning | Panel expressions authored here are executed server-side by Grafana against the in-cluster Prometheus after the ConfigMap refresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-08 | Tampering | `k8s/dashboards/runtime.json` reaching the cluster malformed | mitigate | Every task's verify runs `scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime`, which parses the file, shape-asserts it and runs `kubectl kustomize k8s/` before any apply |
| T-87-10 | Information Disclosure | hardcoded datasource UID leaking into a checked-in dashboard | mitigate | Lint rules S3/S4 forbid the literal `skp-prometheus` and require `${datasource}` on every panel and target |
| T-87-14 | Tampering | a typo'd metric name shipping as a silently empty panel | mitigate | Lint rule S10 restricts runtime.json to the 17-name live-verified allowlist; rule S8 hard-fails on any `\bdotnet_` reference (research F-1) |
| T-87-15 | Repudiation | a proxy panel implying a measurement it does not make (an empty panel titled "CPU") | mitigate | Lint rule S12 forbids the titles CPU / Uptime / Working Set, and rule C6 requires the four honest substitute titles; each substitution panel carries a description naming the absent metric |
| T-87-13 | Denial of Service | an unbounded expression sweeping 15 days of dead-pod series | mitigate | Every expression is filtered by both template variables, and the variables' `allValue` stays null so "All" expands to an explicit four-way alternation rather than `.*` (research F-8) |
| T-87-SC | Tampering | supply chain | accept | This plan installs no packages and pulls no images; it edits one JSON file |
</threat_model>

<verification>
- `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard runtime` exits 0 (full rules, coverage included)
- `kubectl kustomize k8s/` exits 0 and the generated `grafana-dashboards` ConfigMap carries the updated `runtime.json`
- `git status --porcelain src/ k8s/23-grafana.yaml k8s/kustomization.yaml k8s/dashboards/business.json` is empty (this plan owns runtime.json only — 87-02 and 87-04 own the others in the same wave)
</verification>

<success_criteria>
- 17 panels covering GC (collections, heap, fragmentation, allocations, live objects, GC time), committed memory, thread pool (threads, queue, completions), exceptions, lock contention, assemblies, timers, JIT, pods reporting and restarts
- Every panel query is filtered by both dropdowns, and every timeseries panel groups on `service_instance_id` so one bad replica is separable from its peers
- Zero references to the `dotnet_*` family; zero panels titled CPU, Uptime or Working Set
</success_criteria>

<output>
Create `.planning/phases/87-grafana-observability-dashboards/87-03-SUMMARY.md` when done. Record the final panel count, the exact titles, and the A4 `resets()` caveat as an item for the phase findings document.
</output>
