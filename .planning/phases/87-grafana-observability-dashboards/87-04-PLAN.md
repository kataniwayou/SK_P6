---
phase: 87-grafana-observability-dashboards
plan: 04
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - k8s/dashboards/business.json
autonomous: true
requirements: [BPD-01, BPD-02, BPD-03, VAR-03, VAR-04, DASH-04]

must_haves:
  truths:
    - "All nine pipeline domain counters appear on the business dashboard"
    - "Consumed versus sent is overlaid on one axis per service, so conservation reads at a glance"
    - "Each fault indicator has its own dedicated panel, not a series buried in a combined graph"
    - "Fault panels render 0 rather than No data on a healthy stack"
    - "The keeper conservation panel is guarded too, because its counters fire only during recovery — it renders a flat 0 on a happy path instead of No data"
    - "No Class A panel is guarded, so a dead keeper or a stalled pipeline can never pass by rendering 0"
    - "The WebApi is represented by ASP.NET Core request metrics only"
    - "Processor panels discriminate per image by identityName, never by service_name"
  artifacts:
    - path: "k8s/dashboards/business.json"
      provides: "Conservation, fault-indicator and WebApi request panels; every Class-B expr guarded and every Class-A expr unguarded"
      contains: "or vector(0)"
  key_links:
    - from: "fault-indicator panels and the keeper conservation panel"
      to: "a rendered zero instead of No data"
      via: "guarded PromQL on every Class-B expression (lint C3)"
      pattern: "or vector\\(0\\)"
    - from: "the keeper L2 probe heartbeat panel"
      to: "an unguarded Class A assertion in plan 87-05"
      via: "deliberate absence of the guard (lint C7)"
      pattern: "keeper_l2_probe_total"
    - from: "processor panels"
      to: "per-image discrimination"
      via: "group-by on the identityName datapoint tag"
      pattern: "by \\(identityName"
    - from: "every business panel target"
      to: "the source and pod template variables"
      via: "selector filter"
      pattern: "source=~\"\\$source\", service_instance_id=~\"\\$pod\""
---

<objective>
Author the complete panel set of the business / pipeline dashboard.

Purpose: BPD-01/02/03 plus the VAR-04 correction. This is the dashboard on which the conservation relationship the resilience sweep scores on becomes readable by eye, and on which a fault counter that has never fired renders an honest `0` instead of "No data".

Output: `k8s/dashboards/business.json` grown from the one contract panel that plan 87-01 authored to the full 14-panel set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md
@.planning/phases/87-grafana-observability-dashboards/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the keeper and processor conservation panels plus the cross-tier gap</name>
  <files>k8s/dashboards/business.json</files>
  <read_first>
    - k8s/dashboards/business.json (the file being modified — plan 87-01 authored its top-level shape, the three template variables, and panel id 1 "Orchestrator consumed vs sent (conservation)", the structural template every panel below copies)
    - scripts/phase-87-dashboard-lint.ps1 (the rules this file must satisfy — S7 both-filters, S9 allowlist, S13 no pinned webapi source, C1 nine counters, C2 dedicated fault panels, C3 guards, C5 identityName)
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md sections "The nine domain counters", "Domain-counter label sets", "FINDING F-6", "Business / pipeline dashboard (BPD-01/02/03)"
    - .planning/REQUIREMENTS.md BPD-01, BPD-02, VAR-04
  </read_first>
  <action>
Append four panels, ids 2 through 5, copying panel 1's structure: type timeseries, object-form `${datasource}` at panel and target level, gridPos h 8 w 12 two-per-row, `options.legend` displayMode table / placement bottom / calcs mean and max, `options.tooltip` mode multi, targets with refId, editorMode code, range true and a legendFormat.

Every expression carries `{source=~"$source", service_instance_id=~"$pod"}` and uses `$__rate_interval`. The WebApi is never pinned with a literal `source="webapi"` — it is selected through the shared variable like every other class, which is also what lint rule S13 enforces.

Panel 2 — title `Keeper consumed vs sent (conservation)`, unit ops, two targets, and BOTH of them guarded: refId A `sum by (source) (rate(keeper_messages_consumed_total{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval])) or vector(0)` legendFormat `consumed {{source}}`, refId B the same over `keeper_messages_sent_total` legendFormat `sent {{source}}`.

The `or vector(0)` on a timeseries is not decoration, and omitting it breaks two gates at once. These two counters increment only inside `RecoveryConsumerBase.Consume` / `CountSent`, so they fire only during a recovery event: research F-4 measured zero live series and Pitfall 6 records they are absent from the entire 15-day `__name__` index. That puts them in REQUIREMENTS.md VER-01 Class B — "any counter with no live series on a healthy stack" — so this is a properly guarded Class-B panel, not a Class-A panel with a hole in it. Concretely: lint rule C3 requires the guard on EVERY expr referencing these two counters anywhere in `business.json`, not only on the dedicated gap stat, so an unguarded target here fails Task 3's full-coverage lint run; and plan 87-05 STEP F classifies an expression Class A or Class B purely by the presence of this substring, so an unguarded target here would be asserted as Class A and would fail the live proof, because 87-05's traffic drive (STEP E) is a happy-path `FanOutSeeder` run with no fault scenario and will not increment either counter.

Add a panel description recording all of that, and stating plainly what a flat `0` means: "no recovery traffic in this window", NOT "keeper down" — the keeper liveness signal is the `keeper_l2_probe_total` heartbeat panel added in Task 2.

Do NOT propagate the guard beyond the C3 set. Panel 3's processor targets and Task 2's `keeper_l2_probe_total` heartbeat are Class A and must return real samples; lint rule C7 fails the file if either carries `or vector(0)`.

Panel 3 — title `Processor consumed vs sent by image (conservation)`, unit ops, two targets grouping on the `identityName` datapoint tag: refId A `sum by (identityName) (rate(processor_messages_consumed_total{...}[$__rate_interval]))` legendFormat `consumed {{identityName}}`, refId B the same over `processor_messages_sent_total` legendFormat `sent {{identityName}}`. This is the VAR-04 correction in force: every `source="processor"` series carries the `unresolved_0.0.0` MLBL-03 sentinel on `service_name`, so `service_name` cannot discriminate processor images — `identityName` (carrying `{db.Name}_{db.Version}`) is the discriminator, and it exists only on these two business counters (research F-6). Do not group any processor panel by `service_name`.

Panel 4 — title `Cross-tier conservation gap (orchestrator sent - processor consumed)`, type stat, unit short, thresholds green at 0 and red above 0, one target with expr `sum(increase(orchestrator_messages_sent_total{source=~"$source", service_instance_id=~"$pod"}[$__range])) - sum(increase(processor_messages_consumed_total{source=~"$source", service_instance_id=~"$pod"}[$__range]))`. Description: this is the axis the 7-scenario resilience sweep scores on (MG-1 conservation), evaluated over the dashboard window.

Panel 5 — title `Per-processor dispatch gap`, unit short, one target with expr `sum by (processorId) (rate(orchestrator_messages_sent_total{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval])) - sum by (processorId) (rate(processor_messages_consumed_total{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval]))`, legendFormat `{{processorId}}`. This is the analyzer's historical bottleneck view expressed in the current counter names.
  </action>
  <verify>
    <automated>pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard business -ShapeOnly</automated>
  </verify>
  <acceptance_criteria>
    - `(Get-Content k8s/dashboards/business.json -Raw | ConvertFrom-Json).panels.Count` equals 5
    - The file references all six of `orchestrator_messages_consumed_total`, `orchestrator_messages_sent_total`, `keeper_messages_consumed_total`, `keeper_messages_sent_total`, `processor_messages_consumed_total`, `processor_messages_sent_total`
    - At least one `expr` contains `by (identityName)` (VAR-04); no `expr` in the file contains `by (service_name` or `service_name=~`
    - Both of Panel 2's target expressions contain `or vector(0)` (lint rule C3, VER-01 Class B)
    - No `expr` referencing `orchestrator_messages_*`, `processor_messages_*` or `keeper_l2_probe_total` contains `or vector(0)` (lint rule C7, VER-01 Class A)
    - Every `expr` contains both `source=~"$source"` and `service_instance_id=~"$pod"`
    - No `expr` contains the literal `source="webapi"` or `source=~"webapi"`
    - No `expr` matches the regex `\bdotnet_`
    - `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard business -ShapeOnly` exits 0
  </acceptance_criteria>
  <done>All three pipeline services have a consumed-versus-sent overlay on one axis, the cross-tier and per-processor gaps are their own panels, and processor discrimination goes through `identityName`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated, guarded fault-indicator panels</name>
  <files>k8s/dashboards/business.json</files>
  <read_first>
    - k8s/dashboards/business.json (as extended by Task 1)
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md sections "FINDING F-4", "FINDING F-5", "Fault indicators — each on its own panel", "Pitfall 5", "Pitfall 6"
    - .planning/REQUIREMENTS.md BPD-02 (the amended keeper wording) and VER-01 (the Class A / Class B split)
    - scripts/phase-87-dashboard-lint.ps1 (coverage rules C2 and C3)
  </read_first>
  <action>
Append four panels, ids 6 through 9. The first three are `stat` panels with field thresholds green at 0 and red above 0; every one of their expressions ends with `or vector(0)` so a counter that has never been incremented renders `0` instead of "No data". An OTel counter is not exported to Prometheus until its first increment, and a healthy stack should have zero unresolved steps and zero dropped spawns — the guard is what makes VER-01 Class B pass honestly rather than by weakening the assertion.

Panel 6 — type stat, title `Orchestrator unresolved steps in range`, unit short, one target, expr `sum(increase(orchestrator_step_unresolved_total{source=~"$source", service_instance_id=~"$pod"}[$__range])) or vector(0)`. This panel's targets must reference no other metric — lint rule C2 asserts the dedicated-panel property BPD-02 requires.

Panel 7 — type stat, title `Processor dropped spawns in range`, unit short, one target, expr `sum(increase(processor_spawn_dropped_total{source=~"$source", service_instance_id=~"$pod"}[$__range])) or vector(0)`. Again a single metric only.

Panel 8 — type stat, title `Keeper consumed - sent gap in range`, unit short, one target, expr `sum(increase(keeper_messages_consumed_total{source=~"$source", service_instance_id=~"$pod"}[$__range])) - sum(increase(keeper_messages_sent_total{source=~"$source", service_instance_id=~"$pod"}[$__range])) or vector(0)`. The title states what the panel shows — the gap — and must not imply a drop-outcome breakdown. Description: no keeper reinject/drop-outcome counter exists; Phase 74 deliberately deleted the legacy label-less reinject-drop counter in favour of the uniform consumed/sent pair, leaving `keeper_messages_consumed`, `keeper_messages_sent` and `keeper_l2_probe` as the keeper's only three instruments. Reinject outcome now lives solely in Elasticsearch as `attributes.ReinjectOutcome`, and ES panels are out of scope for this milestone (research F-5, BPD-02 amendment).

Panel 9 — type timeseries, title `Keeper L2 probe heartbeat`, unit ops, one target, expr `sum by (service_instance_id) (rate(keeper_l2_probe_total{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval]))`, legendFormat `{{service_instance_id}}`, and NO `or vector(0)`. Description: this is the "keeper is alive and probing" heartbeat that accompanies the gap panel; a healthy keeper holds this above zero. It is a Class A signal — unlike the three guarded panels above and unlike Task 1's guarded keeper conservation overlay, this one has live series on a healthy stack, which is exactly why it is the panel that distinguishes "keeper idle" from "keeper dead". Guarding it would demote it to Class B in 87-05's mechanical classifier and let a dead keeper pass the live proof, so lint rule C7 forbids the guard here.
  </action>
  <verify>
    <automated>pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard business -ShapeOnly</automated>
  </verify>
  <acceptance_criteria>
    - `(Get-Content k8s/dashboards/business.json -Raw | ConvertFrom-Json).panels.Count` equals 9
    - A panel exists whose targets reference `orchestrator_step_unresolved_total` and no other metric; likewise one for `processor_spawn_dropped_total`; and a panel of type `stat` (Panel 8, not Task 1's timeseries Panel 2) references only `keeper_messages_consumed_total` and `keeper_messages_sent_total` (BPD-02, lint rule C2)
    - Every `expr` referencing `orchestrator_step_unresolved_total`, `processor_spawn_dropped_total`, `keeper_messages_consumed_total` or `keeper_messages_sent_total` contains `or vector(0)` — this now covers Task 1's Panel 2 as well as Panels 6/7/8 (lint rule C3)
    - Panel 9's `keeper_l2_probe_total` expression does NOT contain `or vector(0)` (lint rule C7)
    - Panels 6, 7 and 8 are type `stat` with a threshold step at 0 green and above-0 red
    - No fault panel title contains the words `reinject` or `drop outcome`
    - All nine domain counter names now appear at least once in the file (BPD-01, lint rule C1)
    - `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard business -ShapeOnly` exits 0
  </acceptance_criteria>
  <done>Each fault indicator has its own guarded stat panel that renders 0 on a healthy stack, the keeper panel is titled for the gap it actually shows, and the L2 probe heartbeat accompanies it.</done>
</task>

<task type="auto">
  <name>Task 3: Add the WebApi ASP.NET Core request row and close the business coverage gate</name>
  <files>k8s/dashboards/business.json</files>
  <read_first>
    - k8s/dashboards/business.json (as extended by Task 2)
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md section "WebApi row (BPD-03)" including the live-verified label list and the `/health/` route-drop caveat
    - .planning/REQUIREMENTS.md BPD-03 and the Out of Scope entry forbidding WebApi business counters
    - scripts/phase-87-dashboard-lint.ps1 (coverage rules C1 and C4)
  </read_first>
  <action>
Append five panels, ids 10 through 14, representing the WebApi through ASP.NET Core request metrics only. Adding domain counters to the WebApi is explicitly out of scope; it registers neither a domain meter nor the MassTransit meter, so these request metrics are its entire representation on this dashboard.

Every expression keeps the shared `{source=~"$source", service_instance_id=~"$pod"}` selector — the metric family itself only exists for the webapi, so it self-restricts without pinning a literal source value.

Panel 10 — timeseries, title `WebApi request rate by route`, unit reqps, expr `sum by (http_route) (rate(http_server_request_duration_seconds_count{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval]))`, legendFormat `{{http_route}}`.

Panel 11 — timeseries, title `WebApi p95 request duration by route`, unit s, expr `histogram_quantile(0.95, sum by (le, http_route) (rate(http_server_request_duration_seconds_bucket{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval])))`, legendFormat `{{http_route}}`. Note the ASP.NET Core histogram is in seconds.

Panel 12 — timeseries, title `WebApi status-code mix`, unit reqps, expr `sum by (http_response_status_code) (rate(http_server_request_duration_seconds_count{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval]))`, legendFormat `{{http_response_status_code}}`.

Panel 13 — stat, title `WebApi 5xx ratio`, unit percentunit, thresholds green at 0 and red above 0, expr `sum(rate(http_server_request_duration_seconds_count{source=~"$source", service_instance_id=~"$pod", http_response_status_code=~"5.."}[$__rate_interval])) / sum(rate(http_server_request_duration_seconds_count{source=~"$source", service_instance_id=~"$pod"}[$__rate_interval])) or vector(0)`.

Panel 14 — timeseries, title `WebApi in-flight requests and Kestrel connections`, unit short, three targets: refId A `sum by (service_instance_id) (http_server_active_requests{...})` legendFormat `in-flight {{service_instance_id}}`, refId B over `kestrel_active_connections` legendFormat `kestrel active {{service_instance_id}}`, refId C over `kestrel_queued_connections` legendFormat `kestrel queued {{service_instance_id}}`.

Add a dashboard-level or panel-level description recording the BPD-03 caveat: the collector drops every `http.server.request.duration` datapoint whose `http.route` starts with `/health/`, so on an idle stack only the orchestration endpoints appear and these panels stay near-empty until traffic is driven — which is exactly what plan 87-05 does before asserting VER-01.

Finally, run the full business lint (not `-ShapeOnly`) so the business coverage rules C1 through C5 plus C7 are exercised (C6 is runtime-only). C3 and C7 are the pair that keeps this dashboard consistent with plan 87-05's Class-A/Class-B classifier: C3 fails if any of the four Class-B counters is unguarded (including Task 1's Panel 2), C7 fails if any Class-A counter is guarded. If a rule fails, fix the dashboard, never the rule.
  </action>
  <verify>
    <automated>pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard business</automated>
  </verify>
  <acceptance_criteria>
    - `(Get-Content k8s/dashboards/business.json -Raw | ConvertFrom-Json).panels.Count` equals 14
    - The file references `http_server_request_duration_seconds_count`, `http_server_request_duration_seconds_bucket`, `http_server_active_requests`, `kestrel_active_connections` and `kestrel_queued_connections`
    - No domain counter (`*_messages_*`, `orchestrator_step_unresolved_total`, `processor_spawn_dropped_total`, `keeper_l2_probe_total`) appears in any WebApi panel's targets (BPD-03)
    - Every `expr` in the file contains both `source=~"$source"` and `service_instance_id=~"$pod"`
    - Every metric token in the file is a member of the lint's DOMAIN or WEBAPI allowlists
    - `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard business` exits 0 with the coverage section enabled (C1–C5 and C7 all green)
    - `kubectl kustomize k8s/` exits 0
    - `git status --porcelain src/` is empty
  </acceptance_criteria>
  <done>The WebApi is represented by five ASP.NET Core request panels and no domain counters, and the full business lint including all five coverage rules is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo JSON -> Grafana provisioning | Panel expressions authored here are executed server-side by Grafana against the in-cluster Prometheus after the ConfigMap refresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-08 | Tampering | `k8s/dashboards/business.json` reaching the cluster malformed | mitigate | Every task's verify runs `scripts/phase-87-dashboard-lint.ps1 -Dashboard business`, which parses, shape-asserts and runs `kubectl kustomize k8s/` before any apply |
| T-87-10 | Information Disclosure | hardcoded datasource UID leaking into a checked-in dashboard | mitigate | Lint rules S3/S4 forbid the literal `skp-prometheus` and require `${datasource}` |
| T-87-14 | Tampering | a typo'd counter name shipping as a silently empty panel | mitigate | Lint rule S9 restricts business.json to the 9 DOMAIN plus 5 WEBAPI allowlisted names, all live-verified |
| T-87-16 | Repudiation | a fault panel rendering "No data" being read as "no faults" | mitigate | Lint rule C3 requires an `or vector(0)` guard on every Class-B expression — the three fault stats AND the keeper conservation overlay — so the panel renders an explicit 0; plan 87-05 asserts each guarded expression returns exactly one sample |
| T-87-20 | Repudiation | a guard added to a Class A expression, so a dead keeper or a stalled pipeline renders `0` and passes the live proof | mitigate | Lint rule C7 forbids `or vector(0)` on any expr referencing `keeper_l2_probe_total` or the orchestrator/processor consumed-sent counters; C3 and C7 are exact inverses over the same substring 87-05 classifies on, so the two-class split cannot drift |
| T-87-17 | Repudiation | a keeper panel title implying a reinject/drop-outcome breakdown that no counter can supply | mitigate | Panel 8 is titled for the gap it computes, carries a description citing the Phase-74 counter deletion, and the acceptance criteria forbid the words reinject and drop outcome in its title |
| T-87-13 | Denial of Service | an unbounded expression sweeping historical series | mitigate | Every expression is filtered by both template variables with `allValue` null (research F-8) |
| T-87-SC | Tampering | supply chain | accept | This plan installs no packages and pulls no images; it edits one JSON file |
</threat_model>

<verification>
- `pwsh -File scripts/phase-87-dashboard-lint.ps1 -Dashboard business` exits 0 (full rules, coverage included)
- `kubectl kustomize k8s/` exits 0 and the generated `grafana-dashboards` ConfigMap carries the updated `business.json`
- `git status --porcelain src/ k8s/23-grafana.yaml k8s/kustomization.yaml k8s/dashboards/runtime.json` is empty (this plan owns business.json only — 87-02 and 87-03 own the others in the same wave)
</verification>

<success_criteria>
- 14 panels: three per-service conservation overlays, two gap panels, three guarded fault stats, the keeper heartbeat, and five WebApi request panels
- All nine domain counters referenced; every Class-B expression (the three fault stats plus both keeper conservation targets) guarded with `or vector(0)`, and no Class-A expression guarded
- Processor discrimination through `identityName`; zero `service_name` grouping on processor panels; zero domain counters on WebApi panels
</success_criteria>

<output>
Create `.planning/phases/87-grafana-observability-dashboards/87-04-SUMMARY.md` when done. Record the final panel count, which panels are Class A versus Class B for VER-01, and the exact guarded expressions plan 87-05 must assert return exactly one sample.
</output>
