---
phase: 87-grafana-observability-dashboards
plan: 06
type: execute
wave: 4
depends_on: [87-05]
files_modified:
  - .planning/phases/87-grafana-observability-dashboards/87-FINDINGS.md
autonomous: false
requirements: [RTD-02, BPD-02, VAR-04, VER-01, VER-02]

must_haves:
  truths:
    - "The instrumentation gaps this phase could not close are recorded as accepted limitations, not silently omitted"
    - "A human has confirmed the conservation overlays and fault panels are legible in the browser"
    - "A human has confirmed selecting a source value narrows the pod dropdown in the UI"
  artifacts:
    - path: ".planning/phases/87-grafana-observability-dashboards/87-FINDINGS.md"
      provides: "Accepted-limitations record for RTD-02, BPD-02, VAR-04 and the VER-01 dropdown asymmetry"
      contains: "process_runtime_dotnet_"
  key_links:
    - from: "87-FINDINGS.md"
      to: "the research findings that produced each limitation"
      via: "explicit finding-id citation"
      pattern: "F-[1-8]"
---

<objective>
Close the phase by recording what the instrumentation could not supply, and by having a human confirm the two rendering properties no query assertion can capture.

Purpose: REQUIREMENTS.md RTD-02 requires the CPU / uptime / working-set gap to be "recorded as an accepted limitation in the phase VERIFICATION" rather than silently omitted; BPD-02 and VAR-04 carry equivalent substitutions; and VER-01's pod-dropdown superset rule is an accepted asymmetry, not a defect. `87-VALIDATION.md` separately names two manual-only verifications — panel legibility and dropdown interaction — that the API-level proof in plan 87-05 deliberately does not cover.

Output: `87-FINDINGS.md` plus a human sign-off on the two rendering properties.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md
@.planning/phases/87-grafana-observability-dashboards/87-VALIDATION.md
@.planning/phases/87-grafana-observability-dashboards/87-03-SUMMARY.md
@.planning/phases/87-grafana-observability-dashboards/87-04-SUMMARY.md
@.planning/phases/87-grafana-observability-dashboards/87-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the accepted-limitations record</name>
  <files>.planning/phases/87-grafana-observability-dashboards/87-FINDINGS.md</files>
  <read_first>
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md findings F-1 through F-8 and the Assumptions Log entry A4
    - .planning/REQUIREMENTS.md — the amendment note at the top plus RTD-01, RTD-02, BPD-02, VAR-04, VER-01
    - .planning/phases/87-grafana-observability-dashboards/87-05-SUMMARY.md (the live verdict, the Class A / Class B counts, and the live-versus-dropdown pod counts to quote as measured evidence)
    - analyzer-reports/phase-87-dashboards.json (the verdict artifact)
  </read_first>
  <action>
Write one markdown document with a short frontmatter block (phase 87, slug grafana-observability-dashboards, date, status accepted) and one numbered section per limitation. Each section states: the requirement id, what was asked for, what actually exists, what shipped instead, the research finding id, why the real thing cannot be obtained inside this milestone's fences, and the measured evidence from the live run.

Section 1 — RTD-02, process CPU / process uptime / working set do not exist (F-2). No `process_cpu_*`, no `process_start_time_seconds`, no `process_memory_working_set` anywhere in the live inventory. Shipped substitutes: panel `GC committed memory` for memory, `Process restarts in range` for uptime, `GC time per second` for CPU pressure. The two routes to the real metrics are `OpenTelemetry.Instrumentation.Process` (pre-GA 1.17.0-rc.1, and a `src/` change) or a Prometheus scrape-config change — both explicitly out of scope. State that RTD-02's own "where the instrumentation provides it" clause licenses the substitution and that every panel is titled for what it measures.

Section 2 — RTD-02 / assumption A4, the `resets()` restart-proxy caveat. A counter reset can also be produced by the collector's `metric_expiration` (5 min default), so a nonzero reading is a restart signal rather than a certified restart count. Record what the live run actually observed on a stable stack and whether any spurious nonzero value appeared.

Section 3 — RTD-01 / F-1, the runtime family is `process_runtime_dotnet_*` and not `dotnet_*`, because of net8.0 plus `OpenTelemetry.Instrumentation.Runtime` 1.15.0. Record the forward-looking hazard: a net8.0 to net9.0 upgrade silently renames the whole family and blanks the runtime dashboard. Record that `scripts/phase-87-dashboard-lint.ps1` hard-fails on any `\bdotnet_` reference, so the failure would surface hermetically in 5 seconds rather than as an empty dashboard. Also record that the `dotnet_*` series present in this Prometheus come from an unrelated `MCP.Terminal` host process leaking through the `localhost:4317` forward, not from this stack.

Section 4 — F-3, the webapi is missing three GC gauges (heap size, heap fragmentation, committed memory) until that pod performs its first GC. Record whether they populated during the live run's traffic window.

Section 5 — BPD-02 / F-5, no keeper reinject/drop-outcome counter exists; Phase 74 deliberately deleted the legacy label-less one in favour of the uniform consumed/sent pair. Shipped substitute: the `Keeper consumed - sent gap in range` stat plus the `Keeper L2 probe heartbeat` panel, both titled for what they show. Reinject outcome lives only in Elasticsearch as `attributes.ReinjectOutcome`, and ES panels are out of scope this milestone.

Section 6 — VER-01 / F-4, the Class A / Class B split. Name every expression the live run classified as Class B and confirm each returned exactly one guarded sample. Record that `keeper_messages_consumed_total` and `keeper_messages_sent_total` were absent from the entire 15-day `__name__` index because they increment only during a recovery event, so a happy-path run legitimately leaves them at the guarded zero, and that driving a fault scenario was explicitly not a precondition of this phase.

Section 7 — VER-01 / VAR-02 / F-7, the pod dropdown is asserted as a superset, not an exact live set. `label_values()` resolves through Prometheus's TSDB index over overlapping blocks rather than from samples, so it returns recently-dead pods even at a 60-second window (research measured 4 keeper pods listed while 2 were alive). Quote the live run's `LivePodCount` versus `DropdownPodCount`. State that this is index granularity with no Grafana setting that fixes it, and that VAR-02's `label_values()` mandate was kept as-is.

Section 8 — VAR-04 / F-6, `service_name` cannot discriminate processor images: every `source="processor"` series carries the `unresolved_0.0.0` MLBL-03 sentinel for the pod's whole life. `identityName` on the two processor business counters is the discriminator, and `service_instance_id` is the discriminator on the runtime series. Note also that only `processor-sample` is deployed to k8s, so "both processor images" is currently a single-image reality in this cluster.

Section 9 — deliberate non-changes. Record explicitly that no file under `src/` was touched, that `k8s/02-configmaps.yaml` and `k8s/21-prometheus.yaml` were not modified, that `scripts/phase-80-up.ps1` and `.k8s-portforward-pids` were left alone so the 7-scenario resilience sweep's bring-up is unperturbed, and that Grafana anonymous `Viewer` plus `admin`/`admin` is an accepted dev posture matching the stack's existing unauthenticated Prometheus and Elasticsearch, with auth hardening out of scope per REQUIREMENTS.md.

Close with a short "If a future milestone wants the real metrics" note listing the two blocked routes and what each would cost.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File scripts/phase-87-dashboard-lint.ps1 && pwsh -NoProfile -Command "$t = Get-Content .planning/phases/87-grafana-observability-dashboards/87-FINDINGS.md -Raw; @('RTD-02','BPD-02','VAR-04','VER-01','F-2','F-5','F-6','F-7','metric_expiration','process_runtime_dotnet_') | ForEach-Object { if ($t -notmatch [regex]::Escape($_)) { throw \"findings missing: $_\" } }; Write-Host 'findings ok'"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/87-grafana-observability-dashboards/87-FINDINGS.md` exists with nine numbered sections
    - The document contains all of the literal strings `RTD-02`, `RTD-01`, `BPD-02`, `VAR-04`, `VER-01`, `F-1`, `F-2`, `F-3`, `F-4`, `F-5`, `F-6`, `F-7`, `metric_expiration`, `OpenTelemetry.Instrumentation.Process`, `attributes.ReinjectOutcome`, `unresolved_0.0.0`
    - Section 7 quotes the live run's numeric `LivePodCount` and `DropdownPodCount` from `analyzer-reports/phase-87-dashboards.json`, not a placeholder
    - Section 6 lists the Class B expressions by name and states each returned exactly one sample
    - Section 9 asserts zero `src/` changes and names the four fenced-off files left untouched
    - No section proposes a `src/` change or a collector/Prometheus config change as an action item for this milestone
  </acceptance_criteria>
  <done>Every substitution and asymmetry this phase shipped is recorded with its requirement id, its research finding id, the reason the real thing was unobtainable inside the milestone's fences, and the measured evidence from the live run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirmation of panel legibility and dropdown interaction</name>
  <files>k8s/dashboards/runtime.json, k8s/dashboards/business.json</files>
  <read_first>
    - .planning/phases/87-grafana-observability-dashboards/87-VALIDATION.md section "Manual-Only Verifications" (the two behaviors and why each is manual)
    - .planning/phases/87-grafana-observability-dashboards/87-05-SUMMARY.md (what the API-level proof already established, so the human is not asked to re-verify it by eye)
  </read_first>
  <action>
Pause for a human reviewer. Present the two manual-only verifications from `87-VALIDATION.md` — BPD-02 panel legibility and VAR-03 dropdown interaction — with the exact URLs `http://127.0.0.1:3000/d/skp-business` and `http://127.0.0.1:3000/d/skp-runtime`, the panel titles to inspect, and the port-forward command. Do not re-verify anything plan 87-05 already asserted by API. Route any reported defect back to the owning dashboard JSON (`k8s/dashboards/runtime.json` for runtime panels, `k8s/dashboards/business.json` for business panels), then re-run `pwsh -File scripts/phase-87-dashboard-lint.ps1` and `pwsh -File scripts/phase-87-dashboards-verify.ps1` before asking for sign-off again.
  </action>
  <what-built>
    Grafana is deployed in the `skp` namespace with the in-cluster Prometheus as its default read-only datasource and both dashboards file-provisioned from `k8s/dashboards/*.json` with no PVC. `scripts/phase-87-dashboards-verify.ps1` has already proven, by assertion: every Class A panel expression returns real samples, every Class B fault expression returns exactly one guarded sample, the source dropdown enumerates exactly the four service classes, every live pod appears in the pod dropdown, the keeper-scoped chain returns only keeper pods, a Grafana pod delete reproduces both dashboards byte-identically from the repo, and a UI save is rejected with `Cannot save provisioned dashboard`.

    What remains is the rendering judgement no query assertion captures.
  </what-built>
  <how-to-verify>
    1. Ensure the stack is up (`pwsh -File scripts/phase-80-up.ps1` if the 8 shared forwards are not live) and that pipeline traffic is flowing.
    2. Start a Grafana forward: `kubectl -n skp port-forward svc/grafana 3000:3000 --address 127.0.0.1`. Do not add this to `scripts/phase-80-up.ps1`.
    3. Open `http://127.0.0.1:3000/d/skp-business` in a browser. Anonymous access is enabled with the `Viewer` role, so no login is needed.
    4. BPD-02 legibility check: on `Orchestrator consumed vs sent (conservation)`, `Keeper consumed vs sent (conservation)` and `Processor consumed vs sent by image (conservation)`, confirm the consumed and sent series read as a conservation pair on one axis — same scale, not one series flattened against the other — and that the three guarded fault stats (`Orchestrator unresolved steps in range`, `Processor dropped spawns in range`, `Keeper consumed - sent gap in range`) show a green `0` rather than "No data". Confirm no axis is misscaled and no legend overlaps its plot.
    5. Open `http://127.0.0.1:3000/d/skp-runtime` and confirm the same for the runtime panels: each panel has a readable table legend, and per-pod series are distinguishable.
    6. VAR-03 interaction check: on either dashboard, set the `Service class` dropdown to `keeper` alone and confirm the `Pod` dropdown re-populates to keeper pods only. Repeat for `orchestrator`, `processor` and `webapi`, then select `All` and confirm the pod list widens again.
    7. Confirm no panel is titled `CPU`, `Uptime` or `Working Set`, and that the substitution panels' descriptions (hover the info icon) explain what they actually measure.
    8. Stop the port-forward. Do not modify `.k8s-portforward-pids`.
  </how-to-verify>
  <acceptance_criteria>
    - The reviewer confirms conservation overlays are readable at a glance on all three per-service panels (BPD-02)
    - The reviewer confirms the three fault stats render `0` in green rather than "No data"
    - The reviewer confirms the pod dropdown narrows to the matching pods when a single service class is selected, on both dashboards (VAR-03)
    - The reviewer confirms no panel is titled CPU, Uptime or Working Set, and the substitution descriptions are present (RTD-02)
    - Any legibility defect reported is fixed in the owning dashboard JSON and re-verified with `pwsh -File scripts/phase-87-dashboard-lint.ps1` before sign-off
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File scripts/phase-87-dashboard-lint.ps1 &amp;&amp; pwsh -NoProfile -File scripts/phase-87-dashboards-verify.ps1</automated>
    <human-check>Reviewer confirms conservation legibility on all three per-service panels, the three fault stats rendering a green 0, and the pod dropdown narrowing per selected service class.</human-check>
  </verify>
  <done>The reviewer has signed off on panel legibility and dropdown interaction, any reported defect has been fixed in the owning dashboard JSON, and both the lint gate and the live proof are green afterwards.</done>
  <resume-signal>Type "approved" to sign off, or describe the specific panel and what reads wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human browser -> Grafana | Anonymous `Viewer` access over a loopback port-forward; the reviewer can read every dashboard without authenticating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information Disclosure | the reviewer's `svc/grafana 3000:3000` forward | mitigate | The verification instructions bind `--address 127.0.0.1` explicitly; a 0.0.0.0 bind would expose the cluster to the LAN (existing control T-80-17) |
| T-87-02 | Elevation of Privilege | anonymous browser access during review | accept | The anonymous role is `Viewer` and `GF_USERS_ALLOW_SIGN_UP` is false, so a reviewer cannot mutate anything; auth hardening is out of scope per REQUIREMENTS.md |
| T-87-03 | Tampering | a reviewer "fixing" a panel in the browser and saving it | mitigate | `allowUiUpdates: false` rejects the save outright; the checkpoint instructions route every defect back to the owning JSON file plus a lint re-run |
| T-87-15 | Repudiation | a shipped substitution being mistaken for the real measurement | mitigate | 87-FINDINGS.md records each substitution against its requirement id and finding id; step 7 of the checkpoint has the reviewer read the panel descriptions that state what is actually measured |
| T-87-11 | Tampering | the reviewer's forward disturbing the shared harness forwards | mitigate | The instructions start a standalone forward and explicitly forbid touching `.k8s-portforward-pids` |
| T-87-SC | Tampering | supply chain | accept | This plan installs no packages, pulls no images, and writes one markdown file |
</threat_model>

<verification>
- `.planning/phases/87-grafana-observability-dashboards/87-FINDINGS.md` exists and contains every required requirement id and finding id
- `pwsh -File scripts/phase-87-dashboard-lint.ps1` still exits 0 after any legibility fix
- `pwsh -File scripts/phase-87-dashboards-verify.ps1` still exits 0 after any legibility fix
- `git status --porcelain src/ .k8s-portforward-pids scripts/phase-80-up.ps1` is empty
</verification>

<success_criteria>
- Every gap between what the requirements asked for and what the instrumentation supplies is recorded as an explicit accepted limitation with evidence, and none of them is a silent omission
- A human has confirmed the two properties the API-level proof cannot assert: conservation legibility and dropdown interaction
- The phase ends with the lint gate and the live proof both green
</success_criteria>

<output>
Create `.planning/phases/87-grafana-observability-dashboards/87-06-SUMMARY.md` when done. Record the sign-off, any legibility defect found and how it was fixed, and the final state of both gates.
</output>
