---
phase: 87-grafana-observability-dashboards
plan: 05
type: execute
wave: 3
depends_on: [87-02, 87-03, 87-04]
files_modified:
  - scripts/phase-87-dashboards-verify.ps1
  - analyzer-reports/phase-87-dashboards.json
autonomous: true
requirements: [VER-01, VER-02, DASH-01, DASH-02, DASH-03, RTD-01, RTD-02, BPD-02, VAR-01, VAR-02, VAR-03]

must_haves:
  truths:
    - "Every Class A panel expression returns at least one real sample through Grafana's own datasource proxy"
    - "Every Class B fault expression returns exactly one guarded sample, where 0 passes"
    - "The keeper conservation panel passes as a guarded Class B panel on a happy-path drive, with no exception list entry"
    - "Every expression the classifier put in Class B is named in the report, so a silently-added guard is visible"
    - "The source dropdown enumerates exactly the four service classes"
    - "Every live pod from kubectl appears in the pod dropdown"
    - "Deleting the Grafana pod reproduces both dashboard specs byte-identically from the repo JSON"
    - "A POST to /api/dashboards/db is rejected with Cannot save provisioned dashboard"
  artifacts:
    - path: "scripts/phase-87-dashboards-verify.ps1"
      provides: "Single-purpose live proof of VER-01 and VER-02 with structured exit codes"
      contains: "Resolve-AnalyzerExitCode"
    - path: "analyzer-reports/phase-87-dashboards.json"
      provides: "Machine-readable verdict artifact"
      contains: "\"Verdict\""
  key_links:
    - from: "scripts/phase-87-dashboards-verify.ps1"
      to: "scripts/lib/exit-code-resolution.ps1"
      via: "dot-source, so the report Verdict maps to exit 0/1/2"
      pattern: "lib/exit-code-resolution.ps1"
    - from: "scripts/phase-87-dashboards-verify.ps1"
      to: "k8s/dashboards/*.json"
      via: "extract every targets[].expr and replay it through the Grafana datasource proxy"
      pattern: "datasources/proxy/uid/skp-prometheus"
---

<objective>
Prove, by assertion rather than by eye, that both dashboards render real data and that the repo is their only source of truth.

Purpose: VER-01 and VER-02. Every panel expression in the checked-in JSON is replayed through Grafana's own datasource proxy — which proves the datasource wiring and the PromQL in a single assertion — and the Grafana pod is deleted and recreated to prove the dashboards come from the repo and not from a database.

Output: `scripts/phase-87-dashboards-verify.ps1` and the `analyzer-reports/phase-87-dashboards.json` verdict artifact it writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md
@.planning/phases/87-grafana-observability-dashboards/87-PATTERNS.md
@.planning/phases/87-grafana-observability-dashboards/87-VALIDATION.md
@.planning/phases/87-grafana-observability-dashboards/87-02-SUMMARY.md
@.planning/phases/87-grafana-observability-dashboards/87-03-SUMMARY.md
@.planning/phases/87-grafana-observability-dashboards/87-04-SUMMARY.md

<interfaces>
<!-- Contracts the verify script consumes. -->

scripts/lib/exit-code-resolution.ps1 exports:
  Resolve-AnalyzerExitCode -Report <object>   # Verdict Pass->0, Fail->1, Inconclusive->2, anything else->1 (fail-closed)
  Resolve-SweepClass       -ExitCode <int>    # returns an object with a .Class human string

analyzer-reports/phase-83-ha.json is the single-proof report schema: one JSON object,
PascalCase keys, ScenarioId first, Verdict second, then one boolean per claim, then
measured scalars and UTC round-trip ("o") timestamps, then HumanSummary last.
(The camelCase arrays in phase-81-summary.json / phase-85-summary.json are the SWEEP
roll-up convention and are NOT the shape to copy here.)

Grafana endpoints, all live-verified in 87-RESEARCH.md:
  GET  /api/health                                              -> .database == "ok"
  GET  /api/datasources/uid/skp-prometheus                      -> .isDefault, .readOnly
  GET  /api/datasources/uid/skp-prometheus/health               -> .status == "OK"
  GET  /api/search?type=dash-db                                 -> [].uid
  GET  /api/dashboards/uid/<uid>                                -> .dashboard, .meta.provisioned, .meta.provisionedExternalId
  POST /api/datasources/proxy/uid/skp-prometheus/api/v1/query   -> .data.result
  GET  /api/datasources/proxy/uid/skp-prometheus/api/v1/label/service_instance_id/values?match[]=...
  POST /api/dashboards/db                                       -> HTTP 400 "Cannot save provisioned dashboard"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/phase-87-dashboards-verify.ps1</name>
  <files>scripts/phase-87-dashboards-verify.ps1</files>
  <read_first>
    - scripts/phase-83-ha-failover.ps1 (read in full — the exact skeleton: comment-based help with a STEP-flow table and an EXIT-CODE TABLE, `param()` with a `-SkipBringUp` switch, `Set-StrictMode -Version Latest`, `$ErrorActionPreference = 'Stop'`, `Push-Location $repoRoot` with a whole-body try/finally, a prefixed `Write-Phase` trace helper with the Cyan/Gray/Yellow/Red/Green colour convention, the dot-sourced exit-code lib, the pin-exit-code-BEFORE-trim idiom, the bounded-poll-with-deadline helper, the HTTP call plus status assertion, and the port-forward teardown with a recycled-PID guard)
    - scripts/lib/exit-code-resolution.ps1 (Resolve-AnalyzerExitCode and Resolve-SweepClass — reuse, do not reimplement as an inline switch)
    - scripts/phase-81-sweep.ps1 lines 110-150 (the report-AUTHORING pattern: build a pscustomobject, `ConvertTo-Json -Depth 5 | Set-Content -Encoding utf8`, then resolve the exit code from the same in-memory object — phase-83 only READS a report an analyzer wrote, and there is no analyzer here because this phase adds zero C#)
    - analyzer-reports/phase-83-ha.json (the single-proof report schema to mirror)
    - scripts/phase-67-harness.ps1 lines 320-332 (the repo's only basic-auth call — the base64 Authorization header form to copy for admin:admin)
    - scripts/phase-80-harness.ps1 lines 270-315 (STEP C seed, STEP D psql workflow-id lookup, STEP E POST /orchestration/start requiring 204 — the traffic-drive path to reuse)
    - .planning/phases/87-grafana-observability-dashboards/87-PATTERNS.md sections "scripts/phase-87-dashboards-verify.ps1", "Port-forward lifecycle", "jq -> PowerShell translation table", "Legacy comment text — do NOT copy"
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md section "Verification Mechanics (Q8)"
    - k8s/dashboards/runtime.json and k8s/dashboards/business.json (the expressions the script replays)
  </read_first>
  <action>
Write a single-purpose live proof following the phase-83 skeleton. `param([switch]$SkipTraffic, [switch]$SkipPodDelete)`. Comment-based help carries a STEP-flow table and an EXIT-CODE TABLE.

Exit codes: 0 verdict Pass, 1 verdict Fail, 2 verdict Inconclusive (all three resolved by `Resolve-AnalyzerExitCode`, never by an inline switch); 10 grafana rollout/apply failed; 15 the grafana port-forward never carried traffic; 20 `/api/health` never reported database ok; 30 datasource health non-OK; 40 a dashboard was not provisioned; 50 the traffic drive failed; 60 the VER-02 pod delete or recreate failed as infrastructure; 64 usage/precondition error. Every infra abort gets its own distinct code so it can never be mistaken for a verdict.

Precondition, checked first and failing with 64 and a remediation line: the shared stack forwards from `scripts/phase-80-up.ps1` must already be live, because the traffic drive uses `localhost:8080` (baseapi), `localhost:5433` (postgres) and the seeder's `5673`/`4317`. Probe `http://localhost:8080/health/ready` for a 200. Also guard the kube context is docker-desktop. There is no docker-compose in this stack; any compose reference copied from an analog's stale comment text is a bug.

STEP A — `kubectl -n skp rollout status deployment/grafana --timeout=180s` (code 10).

STEP B — start this script's OWN forward: `kubectl -n skp port-forward svc/grafana 3000:3000 -n skp --address 127.0.0.1` via `Start-Process -PassThru -WindowStyle Hidden`, keeping the PID in an in-memory variable ONLY. Never create, read or write `.k8s-portforward-pids` — that file is owned by `phase-80-up.ps1` and holds the 8 shared forwards; touching it would kill a concurrent resilience sweep's tunnels. Then poll `http://127.0.0.1:3000/api/health` on a bounded 90 s deadline until the parsed `database` equals `ok` (codes 15 then 20). Wrap the whole thing so STEP Z in the outer finally stops only that one PID, using the recycled-PID guard (kill only if the PID is still a live `kubectl` process).

STEP C — datasource (code 30): `GET /api/datasources/uid/skp-prometheus` with the admin basic-auth header, assert `isDefault` and `readOnly`; `GET .../health` assert `status` equals `OK`. Record the boolean `DatasourceDefaultReadOnly`.

STEP D — provisioning (code 40): `GET /api/search?type=dash-db`, assert the uid list contains both `skp-runtime` and `skp-business`. For each uid, `GET /api/dashboards/uid/<uid>` and assert `meta.provisioned` is true and `meta.provisionedExternalId` equals `runtime.json` / `business.json`. Record `DashboardsProvisioned`.

STEP E — drive traffic (code 50), skipped when `-SkipTraffic`: reuse the phase-80-harness path verbatim in shape — `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release -- --filter-method "*FanOutSeeder_SeedsAndSelfVerifies*"`, then resolve the workflow id with the static-literal `kubectl -n skp exec statefulset/postgres -- psql -U postgres -d stepsdb -tA -c "SELECT id FROM workflows WHERE name = 'v8-fanout-proof'"` (pin `$LASTEXITCODE` BEFORE the trim), then `POST http://localhost:8080/api/v1/orchestration/start` with `ConvertTo-Json @($wfId)` requiring HTTP 204. Hold a bounded observation window of about 120 s so the cron fires at least twice and the counters and webapi request series populate. Record `WindowStart` and `WindowEnd` in round-trip `o` format.

STEP F — the panel sweep, the heart of VER-01. Parse both dashboard JSONs, walk `panels[]` and any nested `panels[]`, and collect every `targets[].expr` with its file and panel title. Interpolate the dashboard variables the way Grafana would: `$source` becomes the literal alternation `(webapi|orchestrator|keeper|processor)`, `$pod` becomes `.+`, `$__rate_interval` becomes `5m`, `$__range` becomes `1h`. POST each interpolated expression to `/api/datasources/proxy/uid/skp-prometheus/api/v1/query` with `-ContentType 'application/x-www-form-urlencoded'` and a `@{ query = $expr }` body (POST matches the datasource's `httpMethod: POST` and avoids URL-length limits). Classify by the two-class rule REQUIREMENTS.md VER-01 defines: an expression containing `or vector(0)` is Class B and must return EXACTLY one sample (a value of `0` passes); every other expression is Class A and must return at least one sample.

The classifier is purely mechanical — substring presence, nothing else — and it deliberately carries NO hand-maintained exception list. That only holds because the authoring side is symmetric: lint rule C3 forces the guard onto every Class-B counter in `business.json` and lint rule C7 forbids it on every Class-A counter, so `k8s/dashboards/business.json` cannot present an expression that this classifier files on the wrong side. Two panels are expected Class B here BY DESIGN and their `0` is a pass, not a miss: `Keeper consumed - sent gap in range` and — less obviously — `Keeper consumed vs sent (conservation)`, whose two targets are guarded because `keeper_messages_consumed_total` / `keeper_messages_sent_total` increment only during a recovery event and STEP E's happy-path `FanOutSeeder` drive contains no fault scenario (research F-4, Pitfall 6). The `WebApi 5xx ratio` panel is likewise Class B by its own guard. Always `@()`-wrap `$r.data.result` before reading `.Count` — a single-element PromQL result is not an array in PowerShell, which is the bug already guarded at `scripts/phase-81-sweep.ps1`. Record `ClassAExprCount`, `ClassBExprCount`, `ClassAPanelsNonEmpty`, `ClassBPanelsGuarded`, an `EmptyClassAExprs` array naming file, panel title and expression for every Class A miss, and a `ClassBExprs` array naming file, panel title and expression for every expression the classifier put in Class B. The second array makes the classification auditable from the artifact alone — a panel that quietly acquired a guard, and so quietly stopped being asserted for real data, shows up as a new entry there instead of as silence.

STEP G — dropdowns. Assert `group by (source) (process_runtime_dotnet_gc_collections_count_total)` returns exactly the four values keeper, orchestrator, processor, webapi when sorted (`AllFourClassesPresent`). Then the SUPERSET assertion, not equality: for each class, read the live pod names from `kubectl -n skp get pods -l app=<label> -o jsonpath='{.items[*].metadata.name}'` (label mapping: webapi to `baseapi-service`, orchestrator to `orchestrator`, keeper to `keeper`, processor to `processor-sample`) and assert every one of them appears in the values returned by `GET /api/datasources/proxy/uid/skp-prometheus/api/v1/label/service_instance_id/values` with `match[]=process_runtime_dotnet_gc_collections_count_total{source=~"(<class>)"}` and `start`/`end` from `[DateTimeOffset]::UtcNow.ToUnixTimeSeconds()` minus 1800. The dropdown legitimately over-returns recently-dead pods — that is Prometheus TSDB index granularity, measured at a 60-second window, not a Grafana setting — so an exact-match assertion here would be wrong. Record `PodDropdownSuperset`, `LivePodCount`, `DropdownPodCount`. Also assert VAR-03 chaining actually filters: the keeper-scoped `match[]` returns only names beginning with `keeper-`.

STEP H — VER-02, skipped when `-SkipPodDelete` (code 60). Capture `GET /api/dashboards/uid/skp-runtime` and the same for `skp-business`, canonicalising `.dashboard` for comparison. `ConvertFrom-Json` does not preserve key order, so canonicalise by a recursive sorted re-serialise (or compare the raw response substrings) — do not naively `ConvertTo-Json` two independently parsed objects and expect equality. Then `kubectl -n skp delete pod -l app=grafana`, `kubectl -n skp rollout status deployment/grafana --timeout=180s`, restart the port-forward (it dies with the pod), re-poll `/api/health`, re-fetch both dashboards, and assert byte-equality of the canonicalised specs plus `meta.provisioned` still true. Record `ReproducedAfterPodDelete`. Additionally assert no PVC in the namespace has a name matching grafana, and that the Deployment's `grafana-storage` volume has an `emptyDir` key — record `NoPvc`.

STEP I — the tamper assertion. `POST /api/dashboards/db` with body `{"dashboard":{"uid":"skp-runtime","title":"tamper","schemaVersion":39,"panels":[]},"overwrite":true}`. Grafana answers HTTP 400 with a JSON body, so `Invoke-WebRequest` throws — catch it and read `$_.ErrorDetails.Message | ConvertFrom-Json`, asserting `.message` equals `Cannot save provisioned dashboard`. Record `UiSaveRejected`. A 200 here is a hard Fail, not an Inconclusive.

STEP J — build the report `[pscustomobject]` in the phase-83-ha.json shape: `ScenarioId` = `phase-87-dashboards`, `Verdict` = Pass only when every claim boolean is true (Fail when any is false; Inconclusive only when an assertion could not be evaluated at all, for example `-SkipTraffic` left Class A unevaluable), then the booleans `DashboardsProvisioned`, `DatasourceDefaultReadOnly`, `NoPvc`, `UiSaveRejected`, `AllFourClassesPresent`, `ClassAPanelsNonEmpty`, `ClassBPanelsGuarded`, `PodDropdownSuperset`, `ReproducedAfterPodDelete`, then the scalars and the `EmptyClassAExprs` and `ClassBExprs` arrays, then `HumanSummary` last as one self-contained sentence plus a bracketed key=Value dump of every claim. Write it with `ConvertTo-Json -Depth 5 | Set-Content -Path analyzer-reports/phase-87-dashboards.json -Encoding utf8` BEFORE resolving the exit code, so the on-disk Verdict is authoritative even if the process is interrupted. Then feed the same in-memory object to `Resolve-AnalyzerExitCode`, print the class from `Resolve-SweepClass`, and `exit` on it.

STEP Z — outer finally: stop only this script's forward PID with the recycled-PID guard, then `Pop-Location`. Leave the stack and the 8 shared forwards untouched.

Expect and ignore the benign Grafana log line `plugin route is not covered by RBAC and disabled default falling back to 403` on every datasource-proxy call — those calls still return HTTP 200 with correct data. Never treat that line as a failure signal. `jq` is not installed; use `ConvertFrom-Json` throughout.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content scripts/phase-87-dashboards-verify.ps1 -Raw)); 'parse ok'"</automated>
  </verify>
  <acceptance_criteria>
    - The script parses without error under `[ScriptBlock]::Create`
    - It dot-sources `lib/exit-code-resolution.ps1` and calls `Resolve-AnalyzerExitCode`; it contains no inline `switch` mapping Pass/Fail/Inconclusive to 0/1/2
    - `Select-String -Path scripts/phase-87-dashboards-verify.ps1 -SimpleMatch '.k8s-portforward-pids'` returns no match
    - `Select-String -Path scripts/phase-87-dashboards-verify.ps1 -SimpleMatch '--address'` matches, and `-SimpleMatch '0.0.0.0'` returns no match
    - `Select-String -Path scripts/phase-87-dashboards-verify.ps1 -SimpleMatch 'docker compose'` returns no match
    - `Select-String -Path scripts/phase-87-dashboards-verify.ps1 -SimpleMatch 'jq '` returns no match
    - The comment-based help contains an EXIT-CODE TABLE listing 0, 1, 2, 10, 15, 20, 30, 40, 50, 60 and 64
    - The script writes `analyzer-reports/phase-87-dashboards.json` before it calls `Resolve-AnalyzerExitCode`
    - The Class A / Class B classifier keys off the substring `or vector(0)` and `@()`-wraps every `.data.result` before reading `.Count`
    - The classifier contains no hardcoded panel-title or expression exception list — the only test is substring presence
    - The report object includes a `ClassBExprs` array alongside `EmptyClassAExprs`
  </acceptance_criteria>
  <done>The verification script exists, follows the phase-83 harness skeleton with distinct infra exit codes, owns its own loopback port-forward without touching the shared PID file, replays every checked-in panel expression through Grafana's datasource proxy under the two-class rule, and writes a phase-83-shaped verdict artifact before resolving its exit code.</done>
</task>

<task type="auto">
  <name>Task 2: Run the live proof to a Pass verdict</name>
  <files>analyzer-reports/phase-87-dashboards.json</files>
  <read_first>
    - scripts/phase-87-dashboards-verify.ps1 (as authored in Task 1)
    - k8s/dashboards/runtime.json and k8s/dashboards/business.json (the expressions being replayed — a Class A miss is diagnosed here)
    - .planning/phases/87-grafana-observability-dashboards/87-RESEARCH.md sections "FINDING F-3" (the webapi's three GC gauges are absent until it performs its first GC) and "FINDING F-7" (the label_values staleness asymmetry)
  </read_first>
  <action>
Bring the stack up with `pwsh -File scripts/phase-80-up.ps1` if the 8 shared forwards are not already live, then apply the current manifests with `kubectl apply -k k8s/` so Grafana picks up the final dashboard JSONs (the provider re-reads every 30 s, and the kubelet ConfigMap refresh takes up to ~60 s — wait it out rather than restarting the pod).

This is the authoritative apply for the phase. Plan 87-02 Task 3 also ran `kubectl apply -k k8s/`, but it did so mid-Wave-2 while plans 87-03 and 87-04 were still appending panels, so it proved contract-level provisioning only against whatever JSON was on disk at that moment. This apply is the one that carries the FINAL panel set into the `grafana-dashboards` ConfigMap, and it supersedes 87-02's. Do not skip it on the grounds that Grafana is already running.

Run `pwsh -File scripts/phase-87-dashboards-verify.ps1` and drive it to exit 0.

Diagnose, do not weaken. If a Class A expression returns nothing, the correct responses in order are: (a) confirm traffic actually flowed by checking the counter directly against Prometheus; (b) confirm the metric name against the live `__name__` index; (c) fix the expression in the dashboard JSON. Reclassifying a Class A panel as Class B, or deleting a panel, requires an explicit finding recorded for plan 87-06 — it is never a silent edit. Two known-legitimate exceptions the research already established: the webapi's three GC gauges are absent until that pod performs its first GC, which does not make any Class A expression empty because those expressions span all four classes; and the pod dropdown legitimately over-returns recently-dead pods, which is why STEP G asserts a superset.

There is deliberately NO third exception for the keeper conservation panel, even though its counters cannot fire on this happy-path drive. Plan 87-04 Task 1 guards both of its targets with `or vector(0)`, so the classifier files it Class B mechanically and a single `0` sample passes. If `Keeper consumed vs sent (conservation)` ever appears in `EmptyClassAExprs`, the defect is a missing guard in `k8s/dashboards/business.json` (which lint rule C3 should already have caught) — fix the JSON and rerun the lint, never add an exception here. An exception list is the thing this design is avoiding: it would have to be kept in sync by hand with a file two other plans own.

If the run reports Inconclusive, treat it as not-yet-proven and rerun with traffic — never accept exit 2 as the phase gate.

Confirm afterwards that `.k8s-portforward-pids` is unchanged and that the 8 shared forwards still carry traffic (`http://localhost:8080/health/ready` returns 200), proving the script's own forward lifecycle did not disturb the harness.
  </action>
  <verify>
    <automated>pwsh -File scripts/phase-87-dashboards-verify.ps1; echo "verify exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `pwsh -File scripts/phase-87-dashboards-verify.ps1` exits 0
    - `analyzer-reports/phase-87-dashboards.json` exists, parses, and has `ScenarioId` equal to `phase-87-dashboards` and `Verdict` equal to `Pass`
    - All nine claim booleans in the report are true: DashboardsProvisioned, DatasourceDefaultReadOnly, NoPvc, UiSaveRejected, AllFourClassesPresent, ClassAPanelsNonEmpty, ClassBPanelsGuarded, PodDropdownSuperset, ReproducedAfterPodDelete
    - `EmptyClassAExprs` is an empty array
    - `ClassBExprs` names exactly the expressions carrying `or vector(0)`: the three fault stats, both `Keeper consumed vs sent (conservation)` targets, and `WebApi 5xx ratio` — and nothing referencing `keeper_l2_probe_total` or the orchestrator/processor consumed-sent counters
    - `ClassAExprCount` plus `ClassBExprCount` equals the total number of `targets[]` entries across both dashboard JSONs
    - `HumanSummary` is present, is the last field, and names every claim in its bracketed dump
    - `git status --porcelain .k8s-portforward-pids src/` is empty
    - `Invoke-WebRequest http://localhost:8080/health/ready` still returns 200 after the run
  </acceptance_criteria>
  <done>The live proof is green: every Class A expression returned real samples, every Class B expression returned exactly one guarded sample, both dropdowns enumerated correctly, the Grafana pod delete reproduced both dashboards byte-identically from the repo, and a UI save was rejected. The verdict artifact records it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host script -> cluster | The script starts its own `kubectl port-forward` and issues `kubectl exec` and `kubectl delete pod` against the live namespace |
| script -> Grafana HTTP API | Admin basic auth over loopback; the script also exercises the negative path (an attempted dashboard save) |
| script -> Postgres | A workflow-id lookup runs through `kubectl exec ... psql` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information Disclosure | the script's `svc/grafana 3000:3000` forward | mitigate | Bound with `--address 127.0.0.1`, never 0.0.0.0, extending the existing T-80-17 control |
| T-87-11 | Tampering | clobbering `.k8s-portforward-pids` and killing the 8 shared harness forwards | mitigate | The forward PID lives in an in-memory variable only; the PID file is never read or written, and teardown kills exactly one PID behind a recycled-PID guard |
| T-87-12 | Spoofing / Tampering | SQL or argument injection through the psql workflow-id lookup | mitigate | A static string literal query with no interpolated input, copied verbatim from the existing T-80-20-guarded form in scripts/phase-80-harness.ps1 |
| T-87-18 | Repudiation | a false green — the script exiting 0 without having evaluated the assertions | mitigate | Every infra abort has its own distinct non-zero code (10/15/20/30/40/50/60/64) so it can never be read as a verdict; `Resolve-AnalyzerExitCode` fails closed on an unknown or absent Verdict; the report is written before the exit is resolved |
| T-87-03 | Tampering | Grafana UI becoming the source of truth | mitigate | STEP I actively attempts the tamper and requires the `Cannot save provisioned dashboard` rejection; a 200 is a hard Fail |
| T-87-19 | Denial of Service | the VER-02 pod delete disrupting a concurrent resilience sweep | accept | Only the grafana Deployment is deleted; it is not in the pipeline's data path and no other harness depends on it. The delete is skippable with `-SkipPodDelete` if a sweep is in flight |
| T-87-02 | Elevation of Privilege | admin credentials embedded in the script | accept | `admin`/`admin` is the deliberate dev posture set in the manifest and reachable only over loopback; auth hardening is out of scope per REQUIREMENTS.md, so parameterising it would imply a security property this deployment does not have |
| T-87-SC | Tampering | supply chain | accept | This plan installs no packages and pulls no images |
</threat_model>

<verification>
- `pwsh -File scripts/phase-87-dashboards-verify.ps1` exits 0 and writes a `Verdict: Pass` report
- `pwsh -File scripts/phase-87-dashboard-lint.ps1` (both dashboards, full rules) exits 0
- `kubectl apply -k k8s/ --dry-run=server` exits 0
- `git status --porcelain src/ .k8s-portforward-pids scripts/phase-80-up.ps1 k8s/02-configmaps.yaml k8s/21-prometheus.yaml` is empty
</verification>

<success_criteria>
- Every panel expression in both checked-in dashboards was replayed through Grafana's datasource proxy and satisfied its class (VER-01)
- The source dropdown returned exactly the four service classes; every live pod appeared in the pod dropdown; the keeper-scoped chain returned only keeper pods (VAR-01/02/03)
- A Grafana pod delete and recreate reproduced both dashboard specs byte-identically with `meta.provisioned` true, no PVC exists, and a UI save was rejected (VER-02, DASH-03)
- The verdict is recorded in `analyzer-reports/phase-87-dashboards.json` in the established single-proof schema
</success_criteria>

<output>
Create `.planning/phases/87-grafana-observability-dashboards/87-05-SUMMARY.md` when done. Record the verdict, every claim boolean, the Class A / Class B expression counts, the live-versus-dropdown pod counts, and any expression that needed a fix during the run.
</output>
