---
phase: 70-two-consumer-processor-design-pre-process-post-process-with
plan: 04
type: execute
wave: 3
depends_on: [70-02, 70-03]
files_modified:
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs
  - tests/BaseApi.Tests/Processor/PipelinePreFacts.cs
  - tests/BaseApi.Tests/Processor/PipelineInFacts.cs
  - tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs
  - tests/BaseApi.Tests/Processor/PipelinePostFacts.cs
  - tests/BaseApi.Tests/Processor/PipelineEndDeleteFacts.cs
  - tests/BaseApi.Tests/Processor/PostProcessConsumerFacts.cs
  - tests/BaseApi.Tests/Processor/OutputTailFacts.cs
  - tests/BaseApi.Tests/Processor/PrePipelineFacts.cs
  - tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs
  - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs
autonomous: true
requirements: [SPEC-req-1, SPEC-req-2, SPEC-req-3, SPEC-req-4, SPEC-req-5, SPEC-req-9, SPEC-req-10, SPEC-req-11, SPEC-req-12]
nyquist_compliant: true

must_haves:
  truths:
    - "DispatchTestKit returns DataResult? from FakeProcessor and captures the SendContext.MessageId set by the override lambda; an OutputData(messageId)-write-fault mux exists"
    - "Recovery/slot facts with no analog are deleted; the linear Pre-flow facts assert the new model (gate L2[entryId], no-delete-on-invalid, null early-exit, INJECT/DELETE escalations)"
    - "New PostProcessConsumerFacts and OutputTailFacts cover req 5 and the shared tail"
    - "SampleProcessorFacts proves the two-mode seam (spawn-2 / accumulate-1) and the log line"
    - "The whole hermetic suite passes (dotnet test) and the solution builds 0-warning Release + Debug"
  artifacts:
    - path: "tests/BaseApi.Tests/Processor/PrePipelineFacts.cs"
      provides: "Pre-flow facts: gate, read, no-delete-on-invalid, null early-exit, delete-exhaust→DELETE"
      contains: "DidNotReceive().KeyDeleteAsync"
    - path: "tests/BaseApi.Tests/Processor/PostProcessConsumerFacts.cs"
      provides: "req 5 — output-only tail, no entry touch"
      contains: "DidNotReceive"
    - path: "tests/BaseApi.Tests/Processor/OutputTailFacts.cs"
      provides: "write-gated-on-completed + INJECT escalation + TTL"
      contains: "OutputData"
  key_links:
    - from: "tests/BaseApi.Tests/Processor/DispatchTestKit.cs"
      to: "DataResult? seam"
      via: "FakeProcessor returns Task<DataResult?>"
      pattern: "DataResult"
    - from: "tests/BaseApi.Tests/Processor/PrePipelineFacts.cs"
      to: "C-2 no-delete-on-invalid"
      via: "DidNotReceive().KeyDeleteAsync assertion on the input-invalid path"
      pattern: "DidNotReceive\\(\\).KeyDeleteAsync"
---

<objective>
Migrate the Phase-50–68 processor test slice to the two-consumer model (D-17, req 12): extend the hermetic doubles, delete the no-analog recovery/slot facts, rewrite the Pre-flow facts, and add the new Post/OutputTail/Seam/Sample facts. Green `dotnet test` is the req-12 bar.

Purpose: the executor agents work from tests; this plan makes the new model verifiable and locks the four conflict resolutions (C-2 no-delete-on-invalid, C-3/A2 INJECT completed-gate, C-4/A3 REINJECT MessageId, A1 Guid.Empty placeholder) as facts.
Output: extended `DispatchTestKit` + deleted/rewritten/new fact files; whole suite green; solution 0-warning Release + Debug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-SPEC.md
@.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-CONTEXT.md
@.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-VALIDATION.md
@.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-PATTERNS.md

<interfaces>
<!-- Production from Plans 01/03 (landed). -->

DataResult: `record DataResult(Guid WorkflowId, Guid StepId, Guid ProcessorId){ Guid CorrelationId; Guid ExecutionId; Guid MessageId; StepOutcome Result; string Data; }`
ProcessorPipeline ctor now also takes `OutputTail outputTail` (after `processor` and the other collaborators).
OutputTail: `public sealed class OutputTail(IConnectionMultiplexer, IProcessorContext, ISendEndpointProvider, IOptions<RetryOptions>, IOptions<ProcessorLivenessOptions>, ProcessorMetrics)` → `Task<bool> RunAsync(DataResult dr, Guid deleteEntryId, CancellationToken ct)`.
PostProcessConsumer: `IConsumer<DataResult>` over `(OutputTail, IProcessorContext, ProcessorMetrics)`.
BaseProcessor: `protected Task SpawnToPost(DataResult, Guid executionId)`, `protected Task DeleteEntry()`, `protected DataResult NewResult(StepOutcome, string data)` (framework-stamped ids/messageId).
L2 keys: `OutputData(messageId)="skp:out:{messageId:D}"`, `ExecutionData(entryId)="skp:data:{entryId:D}"`.

From tests/BaseApi.Tests/Processor/DispatchTestKit.cs (CURRENT — to extend):
```csharp
public sealed class FakeProcessor : BaseProcessor<DummyConfig> {
  public FakeProcessor(List<ProcessItem> items); public FakeProcessor(Exception toThrow);
  protected override Task<List<ProcessItem>> ProcessAsync(...);  // ⇒ change to Task<DataResult?>
}
public static List<ProcessItem> Items(params string[] outputs);  // ⇒ remove/replace
// CapturingSendProvider (:568-590) captures Send(Arg.Any<object>(), Arg.Any<CancellationToken>()) only.
// PresentReadWriteFaultL2 / AbsentReadL2 / ReadOkDeleteFaultL2 fault muxes exist.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DispatchTestKit (DataResult? FakeProcessor + MessageId-capture + OutputData-write-fault mux) and delete no-analog facts</name>
  <files>tests/BaseApi.Tests/Processor/DispatchTestKit.cs, tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs, tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs, tests/BaseApi.Tests/Processor/PipelinePostFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (FakeProcessor :40-79; CapturingSendProvider :568-590; PresentReadWriteFaultL2 :87-119)
    - tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs (delete — recovery pass removed)
    - tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs, PipelinePostFacts.cs (slot-allocation facts — delete the slot specifics)
    - 70-VALIDATION.md § Wave 0 Requirements
    - 70-PATTERNS.md § "Test migration (D-17 / req 12)"
  </read_first>
  <action>
**Extend `DispatchTestKit`:**
- Change `FakeProcessor` to extend the `DataResult?` seam: the `_impl` delegate becomes `Func<string, DummyConfig?, Guid, CancellationToken, Task<DataResult?>>`; add ctors `FakeProcessor(DataResult? toReturn)` and keep `FakeProcessor(Exception toThrow)`. Add a `FakeProcessor(Func<...>)` ctor so a Mode-2 fake can call `this.SpawnToPost`/`this.DeleteEntry` and `return null`. Keep `Invoked`/`LastInputData`/`LastExecutionId`. Remove the `Items(...)` helpers and any `List<ProcessItem>` ctor; add `DataResult Result(StepOutcome outcome, string data, Guid? messageId = null)` factory that builds a `DataResult` with stable test ids.
- Extend `CapturingSendProvider` (:568-590): keep the existing `Send(Arg.Any<object>(), Arg.Any<CancellationToken>())` capture; ADD a capture of the override overload `Send(Arg.Any<object>(), Arg.Any<Action<SendContext>>(), Arg.Any<CancellationToken>())` (or the `IPipe<SendContext>` form, matching the production `SpawnToPost`/`OutputTail`/keeper send) that records the boxed message AND applies the captured `Action<SendContext>` to a `Substitute.For<SendContext>()` (or a minimal fake) to record the resulting `MessageId` into a new `List<Guid> SentMessageIds`. Provide the captured ids so a fact can assert the override.
- Add an `OutputWriteFaultL2(values, out IDatabase db)` mux modeled on `PresentReadWriteFaultL2` but whose `StringSetAsync` on the `OutputData` key throws `RedisConnectionException` (the INJECT-escalation case). Keep its `KeyDeleteAsync` a no-op success.

**Delete no-analog facts (D-17):**
- DELETE `PipelineRecoveryFacts.cs` entirely (recovery pass removed).
- In `PipelineForwardFacts.cs` and `PipelinePostFacts.cs`: remove the slot-allocation/`MessageIndex`/recovery-branch facts. If a file is left with no still-valid facts, delete it; otherwise keep only facts that map to the linear Pre flow (these get fully rewritten in Task 2 — it is acceptable to delete `PipelineForwardFacts.cs`/`PipelinePostFacts.cs` here and re-home their surviving assertions into `PrePipelineFacts`/`OutputTailFacts` in Task 2). Keep `RecoveryPartitionFacts.cs` (Keeper dir) untouched — the 4-tuple `PartitionGuid` is unchanged.
  </action>
  <verify>
    <automated>grep -q "Task<DataResult?>" "tests/BaseApi.Tests/Processor/DispatchTestKit.cs" && grep -q "SentMessageIds" "tests/BaseApi.Tests/Processor/DispatchTestKit.cs" && grep -q "OutputWriteFaultL2" "tests/BaseApi.Tests/Processor/DispatchTestKit.cs" && [ ! -f "tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `DispatchTestKit.cs` `FakeProcessor` overrides `Task<DataResult?> ProcessAsync` and contains no `List<ProcessItem>`
    - `DispatchTestKit.cs` contains `SentMessageIds` (the override-MessageId capture) and `OutputWriteFaultL2`
    - `PipelineRecoveryFacts.cs` does NOT exist on disk
    - No `ProcessItem`/`ProcessOutcome` reference remains in `DispatchTestKit.cs`
  </acceptance_criteria>
  <done>Test kit returns DataResult?, captures override MessageId, has an OutputData-write-fault mux; no-analog recovery/slot facts deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Pre-flow facts + add PostProcessConsumerFacts / OutputTailFacts / Sample / Seam facts; full-suite green</name>
  <files>tests/BaseApi.Tests/Processor/PipelinePreFacts.cs, tests/BaseApi.Tests/Processor/PipelineInFacts.cs, tests/BaseApi.Tests/Processor/PipelineEndDeleteFacts.cs, tests/BaseApi.Tests/Processor/PrePipelineFacts.cs, tests/BaseApi.Tests/Processor/PostProcessConsumerFacts.cs, tests/BaseApi.Tests/Processor/OutputTailFacts.cs, tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs, tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs, tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/PipelinePreFacts.cs, PipelineInFacts.cs, PipelineEndDeleteFacts.cs (the facts being rewritten)
    - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs, BaseProcessorSeamFacts.cs (rewrite targets)
    - tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs (req 11 endpoint posture — extend for the -post bind)
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (extended in Task 1 — FakeProcessor, CapturingSendProvider, fault muxes)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs + OutputTail.cs + PostProcessConsumer.cs + SampleProcessor.cs (the production under test)
    - 70-VALIDATION.md § Per-Task Verification Map (the req→behavior→test rows)
  </read_first>
  <action>
Construct the pipeline in facts via `new ProcessorPipeline(redis, context, fakeProcessor, send, retry, liveness, metrics, logger, outputTail)` (note the added `outputTail` arg — build a real `OutputTail` over the same fakes). Cover the VALIDATION map rows:

**`PrePipelineFacts.cs` (NEW, rewrite from PipelinePreFacts/PipelineEndDeleteFacts):**
- req 1: gate-fault (existence-check throws via a fault mux) → exactly one `KeeperReinject` in `send.SentKeeper`, `send.Sent` empty.
- req 1: clean-absent `entryId` (KeyExists → false) → no processing, `Invoked == false`, no send.
- req 2: read-fault → one `KeeperReinject`.
- req 2 (C-2): input-invalid (validatedData fails `InputDefinition`) → exactly one `StepFailed` AND `db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>(), Arg.Any<CommandFlags>())` (NO entry delete — assert with the exact key too: `db.DidNotReceive().KeyDeleteAsync((RedisKey)L2ProjectionKeys.ExecutionData(d.EntryId), Arg.Any<CommandFlags>())`).
- req 3: `FakeProcessor(toReturn: null)` → no write (`db.DidNotReceive().StringSetAsync(...)`), no send (`Assert.Empty(send.Sent)`), no delete.
- req 4 (completed): a `FakeProcessor` returning a `Completed` DataResult → one `StringSetAsync((RedisKey)OutputData(messageId), (RedisValue)data, Arg.Any<TimeSpan?>(), ...)`, one `StepCompleted`, one `KeyDeleteAsync((RedisKey)ExecutionData(d.EntryId), ...)`.
- req 4 (delete-exhaust): `ReadOkDeleteFaultL2` → one `KeeperDelete` in `SentKeeper`.
- req 4 (write-exhaust): `OutputWriteFaultL2` → one `KeeperInject` in `SentKeeper`, AND no `StepCompleted` send, AND no entry delete.

**`OutputTailFacts.cs` (NEW):** construct `OutputTail` directly.
- completed → `OutputData(messageId)` write once + send by result.
- non-completed (Failed) → NO write, still sends `StepFailed`.
- write-exhaust (`OutputWriteFaultL2`) → `KeeperInject` + `RunAsync` returns false.
- TTL: assert the `StringSetAsync` is called with a non-null `TimeSpan?` expiry (jittered) — `db.Received(1).StringSetAsync((RedisKey)OutputData(messageId), Arg.Any<RedisValue>(), Arg.Is<TimeSpan?>(t => t.HasValue), ...)` (req 11 jittered TTL).

**`PostProcessConsumerFacts.cs` (NEW, req 5):** drive `PostProcessConsumer.Consume(ctx)` with a `ConsumeContext<DataResult>` substitute.
- completed → `OutputData(messageId)` write + `StepCompleted` send; AND `db.DidNotReceive().StringGetAsync(Arg.Any<RedisKey>(), ...)` AND `db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>(), Arg.Any<CommandFlags>())` (NO entry read/delete).
- write-exhaust → `KeeperInject`.

**`SampleProcessorFacts.cs` (rewrite, req 10):** instantiate `SampleProcessor` and drive it through the pipeline (or directly invoke the seam via the framework wiring) with a `{label, number}` payload.
- empty `executionId` → exactly 2 messages captured to the `-post` queue (`send` capture filtered to DataResult to the post uri) with DISTINCT `executionId`s, the entry deleted (`DeleteEntry`), the seam returns null (Pre sends/writes nothing inline — assert `send.Sent` has no Step* for the inline path), and the log line matches `"... had the following numbers: ..."` (use a capturing `ILogger`/`FakeLogCollector`).
- non-empty `executionId` → exactly one completed `DataResult` flows through the inline tail (one `StepCompleted`), no spawn, no extra delete beyond the inline-tail entry delete.
- both paths emit the `"{label} had the following numbers: …"` log.

**`BaseProcessorSeamFacts.cs` (rewrite, req 9):** prove `SpawnToPost` swallows on exhaust (a send-fault mux → no throw, the spawn-drop hook fired) and `DeleteEntry` escalates `KeeperDelete` on delete-exhaust. Use a minimal concrete `BaseProcessor<DummyConfig>` subclass that calls `this.SpawnToPost`/`this.DeleteEntry` with framework state set the way the pipeline sets it.

**`PipelineInFacts.cs` (rewrite):** the `ProcessStatusException` family → exactly one `Step*` (Failed/Cancelled/Processing) and (C-2 parity) NO entry delete on the seam-throw path. Delete any forward/recovery branch assertions.

**`DispatchBindSequenceFacts.cs` (extend, req 11):** assert BOTH `queue:{id:D}` and `queue:{id:D}-post` are bound (two `ConnectReceiveEndpoint` calls) and both `.Ready` awaited BEFORE `MarkHealthy()`; the -post endpoint configures only `ConfigureConsumer` (no `UseMessageRetry`/`ConfigureError`).

Delete any now-empty leftover fact files. Then run the WHOLE suite (MTP `dotnet test` ignores `--filter`) and fix until green.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && dotnet test tests/BaseApi.Tests -c Release 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `PrePipelineFacts.cs` contains `DidNotReceive().KeyDeleteAsync` on the input-invalid path (C-2)
    - `PostProcessConsumerFacts.cs` asserts `DidNotReceive().StringGetAsync` and `DidNotReceive().KeyDeleteAsync` (req 5 — no entry touch)
    - `OutputTailFacts.cs` asserts the completed-only write, the Failed no-write, and a non-null TTL on the write
    - `SampleProcessorFacts.cs` asserts exactly 2 distinct-execId spawns on empty execId and 1 completed on non-empty, plus the `"had the following numbers"` log
    - `dotnet test tests/BaseApi.Tests -c Release` exits 0 (whole hermetic suite green) — the req-12 bar
    - `dotnet build SK_P.sln -c Release` and `-c Debug` both exit 0 with 0 warnings
  </acceptance_criteria>
  <done>All Pre/Post/OutputTail/Seam/Sample facts pass against the new model; whole suite green; solution 0-warning Release + Debug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test doubles ↔ production seam | Hermetic NSubstitute/FakeRedis doubles; no live stack, no external boundary (container build out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-11 | Tampering | false-green fault facts (SE.Redis overload ambiguity, Pitfall 1) | mitigate | Copy the existing `When/Do`-across-overloads idiom for the OutputData-write-fault mux; assert on the `Expiration/ValueCondition` overload where the consumer binds it; a fault fact that passes when it should fail is the warning sign. |
| T-70-12 | Repudiation | suite scoping silently runs the wrong tests (Pitfall 6) | accept | Use `dotnet test tests/BaseApi.Tests` (whole suite) for the gate — `--filter` is unreliable on MTP; the whole-suite run is the documented reliable scope. |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests -c Release` → exit 0 (whole hermetic suite green).
- `dotnet build SK_P.sln -c Release` → 0 warn / 0 err; `dotnet build SK_P.sln -c Debug` → 0 warn / 0 err.
- Scoped iteration during development: MTP exe `--filter-class "*PrePipelineFacts"` (NOT `dotnet test --filter`).
</verification>

<success_criteria>
- Whole hermetic suite green (req-12 bar).
- Solution 0-warning Release + Debug.
- C-2 / req-5 no-touch / OutputTail TTL / Sample two-mode all locked as passing facts.
</success_criteria>

<output>
After completion, create `.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-04-SUMMARY.md`
</output>
