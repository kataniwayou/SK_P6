---
phase: 84-byte-data-channel-widening
plan: 02
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - tests/BaseApi.Tests/Processor/PrePipelineFacts.cs
  - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
  - tests/BaseApi.Tests/Contracts/DataResultByteChannelFacts.cs
  - tests/BaseApi.Tests/Processor/SchemaByteDecodeFacts.cs
autonomous: true
requirements: [DATA-02, DATA-05]

must_haves:
  truths:
    - "The hermetic test project compiles against the byte[] contract"
    - "The full hermetic filtered run has ZERO new failures vs the ~272 pre-existing Docker-less infra baseline (0 logic facts among the baseline)"
    - "A new fact proves a DataResult with byte[] Data round-trips losslessly through STJ (base64 on the wire) — closes assumption A1 for DATA-05"
    - "A new fact proves invalid-UTF8 bytes under a schema return business Failed via ProcessorJsonSchemaValidator — closes assumption A2 for D-04/DATA-02"
  artifacts:
    - path: "tests/BaseApi.Tests/Contracts/DataResultByteChannelFacts.cs"
      provides: "A1 — DataResult STJ byte[] round-trip fact"
      contains: "SequenceEqual"
    - path: "tests/BaseApi.Tests/Processor/SchemaByteDecodeFacts.cs"
      provides: "A2 — invalid-UTF8-under-schema → Failed fact"
      contains: "TryValidate"
  key_links:
    - from: "tests/BaseApi.Tests/Processor/DispatchTestKit.cs"
      to: "byte[] validatedData seam"
      via: "FakeProcessor._impl Func<byte[],...> + LastInputData byte[]?"
      pattern: "byte\\[\\]\\? LastInputData"
    - from: "tests/BaseApi.Tests/Processor/SchemaByteDecodeFacts.cs"
      to: "ProcessorJsonSchemaValidator.TryValidate"
      via: "non-empty schema + invalid UTF-8 bytes → returns false"
      pattern: "TryValidate"
---

<objective>
Migrate the hermetic test compile-breaks that the byte[] contract flip surfaces, then add the two small hermetic facts RESEARCH recommends to convert assumptions A1 (recovery wire round-trip) and A2 (bad-UTF8 → Failed) from MEDIUM to VERIFIED. This restores the fast per-commit feedback loop; it is NOT the phase gate (the sweep in Plan 03 is).

Purpose: keep the existing hermetic facts green through the migration (D-05: they must still compile + pass via the D-02 helpers) and pin the two byte[] behaviors the sweep exercises live but does not unit-isolate.
Output: test project compiles; full hermetic filtered run has zero NEW failures; two new facts pass.

CRITICAL (Pitfall 2 — do NOT over-edit): `Assert.Equal(byte[], byte[])` is structural in xUnit, so almost every existing `.Data` assertion passes UNCHANGED. Fix ONLY what the compiler flags. Do NOT rewrite transparent assertions (SampleProcessorFacts, OrchestratorContractTests, FanInHermeticHarnessFacts, InjectConsumerFacts) to SequenceEqual/DataAsString — that is churn that risks the very equality bug you are avoiding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-byte-data-channel-widening/84-RESEARCH.md
@.planning/phases/84-byte-data-channel-widening/84-VALIDATION.md
@.planning/phases/84-byte-data-channel-widening/84-01-SUMMARY.md

<interfaces>
Confirmed compile-break sites (verified this session):

tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  :47   `private readonly Func<string, DummyConfig?, Guid, CancellationToken, Task<DataResult?>> _impl;`  → Func<byte[], ...>
  :54,:64,:76  `LastInputData = validatedData;`  (validatedData is now byte[])
  :83   `public string? LastInputData { get; private set; }`  → `public byte[]? LastInputData`
  :95   `public DataResult NewResultPublic(StepOutcome outcome, string data) => NewResult(outcome, data);`  → KEEP string (D-02 encode overload)
  :97-98 `protected override Task<DataResult?> ProcessAsync(string validatedData, DummyConfig? config, ...)`  → byte[] validatedData
  :104-112 `public static DataResult Result(StepOutcome outcome, string data, Guid? messageId = null)` sets `Data = data`  → encode: `Data = Encoding.UTF8.GetBytes(data)` (keep the string param)

tests/BaseApi.Tests/Processor/PrePipelineFacts.cs
  :312-313 `protected override Task<DataResult?> ProcessAsync(string validatedData, DeserConfig? config, ...)`  → byte[] validatedData

tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
  :204  `Data = "inject-payload",`  → `Data = Encoding.UTF8.GetBytes("inject-payload")`

Hermetic run command (VALIDATION.md / project memory — dotnet test HANGS on Windows MTP):
  tests/BaseApi.Tests/bin/{Debug,Release}/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack
  Expect ~272 pre-existing RealStack infra failures (RabbitMQ/Postgres/Redis) — 0 logic facts among them; the bar is ZERO NEW failures.

New-fact hooks:
  - ProcessorJsonSchemaValidator.TryValidate(string? definition, byte[] data, out IReadOnlyList<string> errors)  (byte[] after Plan 01)
  - DataResult is a sealed record with default STJ serialization; System.Text.Json serializes byte[] as base64.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the byte[] test compile-breaks (compiler-driven, no over-edit)</name>
  <files>tests/BaseApi.Tests/Processor/DispatchTestKit.cs, tests/BaseApi.Tests/Processor/PrePipelineFacts.cs, tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs:45-112 (FakeProcessor + Result helper)
    - tests/BaseApi.Tests/Processor/PrePipelineFacts.cs:305-320 (the fake ProcessAsync)
    - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs:198-210 (the DataResult literal)
    - 84-RESEARCH.md §Blast Radius "Tests — compile-breaks" table and §"Tests — TRANSPARENT (do NOT touch reflexively)" list
    - 84-01-SUMMARY.md (any consumer site the Plan 01 compiler surfaced beyond the inventory)
  </read_first>
  <action>
    In DispatchTestKit.cs: change the `_impl` field type (:47) first type-arg `string`→`byte[]`; change `LastInputData` property (:83) from `string?` to `byte[]?`; change the fake `ProcessAsync` override (:97-98) param to `byte[] validatedData`; in the `Result(...)` helper (:104-112) change `Data = data` to `Data = System.Text.Encoding.UTF8.GetBytes(data)` (keep the `string data` param). Leave `NewResultPublic(StepOutcome, string data)` (:95) as-is — it rides the D-02 encode overload. Add `using System.Text;` if needed.

    In PrePipelineFacts.cs:312-313 change the fake `ProcessAsync` param to `byte[] validatedData`.

    In SC2RecoveryPathsE2ETests.cs:204 change `Data = "inject-payload",` to `Data = System.Text.Encoding.UTF8.GetBytes("inject-payload"),`.

    Then BUILD the test project and fix ONLY the errors the compiler flags — the expected residue is any assertion that read `LastInputData` (or a `.Data`) as a string and now needs `DataAsString()` / `Encoding.UTF8.GetString(...)`. Do NOT touch any assertion the compiler does NOT flag (Pitfall 2): SampleProcessorFacts, OrchestratorContractTests, FanInHermeticHarnessFacts, InjectConsumerFacts, OrchestratorInjectConsumerFacts, StopConsumerLifecycleTests, TypedResultConsumerFacts, ResultAckTests, OrchestratorPrePipelineFacts, FrameworkNoPayloadFacts, BaseProcessorSeamFacts are all verified transparent unless the compiler says otherwise.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug -warnaserror 2>&1 | tail -5 && ./tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test project builds 0-warning Debug (and Release) with warnings-as-errors.
    - `grep -q "byte\[\]? LastInputData" tests/BaseApi.Tests/Processor/DispatchTestKit.cs`
    - The hermetic filtered run (`BaseApi.Tests.exe --filter-not-trait Category=RealStack`) shows ZERO new failures vs the ~272-failure Docker-less baseline (every failure is a RealStack infra connection failure, 0 logic facts).
    - No transparent assertion was rewritten — `git diff` touches only the three files above plus any single line the compiler explicitly flagged (record each in the SUMMARY).
  </acceptance_criteria>
  <done>The hermetic suite compiles and runs with no new failures; only compiler-flagged sites changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the A1 and A2 hermetic facts</name>
  <files>tests/BaseApi.Tests/Contracts/DataResultByteChannelFacts.cs, tests/BaseApi.Tests/Processor/SchemaByteDecodeFacts.cs</files>
  <read_first>
    - src/Messaging.Contracts/DataResult.cs (post-Plan-01 byte[] shape)
    - src/BaseProcessor.Core/Validation/ProcessorJsonSchemaValidator.cs (post-Plan-01 byte[] TryValidate + schema-absent guard)
    - an existing Contracts fact for the file/namespace/trait conventions, e.g. tests/BaseApi.Tests/Contracts/OrchestratorContractTests.cs
    - 84-RESEARCH.md §"Assumptions Log" (A1, A2) and §"Validation Architecture > Wave 0 Gaps"
  </read_first>
  <behavior>
    - A1: a DataResult whose Data is a non-UTF8-representable byte sequence (e.g. new byte[]{0x00,0xFF,0x10,0x7F}) serialized then deserialized with the default STJ options preserves Data exactly (SequenceEqual), and the serialized JSON contains the base64 of those bytes (proves the transient wire form)
    - A2: ProcessorJsonSchemaValidator.TryValidate with a NON-empty JSON schema definition and invalid-UTF8 bytes (e.g. new byte[]{0xFF,0xFE,0xFD}) returns false with an error mentioning JSON/UTF-8
    - A2 control: TryValidate with a NON-empty schema + valid UTF-8 JSON that SATISFIES the schema returns true; with EMPTY/whitespace definition returns true WITHOUT decoding (opaque bytes, DATA-02)
  </behavior>
  <action>
    Create tests/BaseApi.Tests/Contracts/DataResultByteChannelFacts.cs (namespace matching the Contracts test folder convention, `[Trait]` matching the hermetic — NOT RealStack — facts). Add A1: build a DataResult with `Data = new byte[]{0x00,0xFF,0x10,0x7F}`, serialize with `System.Text.Json.JsonSerializer.Serialize(dr)` and deserialize back, assert the round-tripped `Data.SequenceEqual(original)` and that the JSON string contains `Convert.ToBase64String(original)`. This closes A1 for DATA-05 (STJ base64 wire is lossless).

    Create tests/BaseApi.Tests/Processor/SchemaByteDecodeFacts.cs (hermetic trait). Add A2: call `ProcessorJsonSchemaValidator.TryValidate(schema, new byte[]{0xFF,0xFE,0xFD}, out var errs)` with a minimal valid JSON-schema definition string (e.g. `{"type":"object"}`); assert it returns false and `errs` mentions JSON/UTF-8. Add the two control asserts: (a) a schema + valid UTF-8 JSON payload conforming to the schema returns true; (b) `TryValidate(null, anyBytes, out _)` (or empty definition) returns true WITHOUT decoding — proving schema-absent bytes stay opaque (DATA-02 / Pitfall 3).

    Keep both files as ordinary existing-style hermetic facts — they are the fast feedback loop, NOT a new phase gate (D-05). Do not introduce a new fixture, base class, or framework.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release -warnaserror 2>&1 | tail -5 && ./tests/BaseApi.Tests/bin/Release/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both new fact files exist, build 0-warning, and their facts PASS in the hermetic filtered run.
    - A1 asserts `Data.SequenceEqual` after an STJ serialize→deserialize round-trip AND that the wire JSON carries the base64 of the bytes.
    - A2 asserts invalid-UTF8-under-schema → false; the schema-absent path returns true WITHOUT decoding (opaque-bytes control).
    - Full hermetic filtered run still shows ZERO new failures beyond the ~272 infra baseline.
  </acceptance_criteria>
  <done>A1 and A2 are green hermetic facts; assumptions A1/A2 are VERIFIED; DATA-02/DATA-05 have unit-level coverage complementing the sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Unchanged from Plan 01 — this plan only adds/migrates tests. No production trust boundary is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02 | Information disclosure | FrameworkNoPayloadFacts | mitigate | The existing token/reflection payload-scan fact must stay green through the migration — it guards FW-03 (byte[] Data never logged). Confirm it is in the zero-new-failures run. |
| T-84-01 | Tampering / DoS | SchemaByteDecodeFacts (A2) | mitigate | The A2 fact positively PROVES malformed bytes under a schema → business Failed (not a crash) — it hardens the D-04 mitigation into a regression guard. |
| T-84-SC | Tampering (supply chain) | package installs | accept | No package installs — new xUnit facts on the existing test stack only. |
</threat_model>

<verification>
- Test project builds 0-warning Debug + Release (warnings-as-errors).
- `BaseApi.Tests.exe --filter-not-trait Category=RealStack` — zero new failures vs the ~272-failure Docker-less baseline; A1 + A2 pass.
- `git diff --name-only` for tests touches only the five files in `files_modified` plus any single compiler-flagged line recorded in the SUMMARY.
</verification>

<success_criteria>
- Existing hermetic facts compile + pass against byte[] via the D-02 helpers (DATA-03 fast loop intact).
- A1 pins the recovery-wire byte[] round-trip (DATA-05); A2 pins schema-gated decode + opaque-bytes (DATA-02) and bad-UTF8 → Failed (D-04).
- No transparent assertion churned (Pitfall 2 respected).
</success_criteria>

<output>
Create `.planning/phases/84-byte-data-channel-widening/84-02-SUMMARY.md` when done — list every compiler-flagged site changed, the new-failure count vs baseline (must be 0), and confirm A1/A2 are green.
</output>
