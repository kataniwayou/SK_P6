---
phase: 84-byte-data-channel-widening
plan: 03
type: execute
wave: 3
depends_on: [84-02]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "The whole solution rebuilds 0-warning Debug + Release so the sweep runs the new byte[] code"
    - "The 7-scenario fault-recovery sweep (TEST-01..TEST-07) reproduces its all-PASS baseline — every Missing==0, every Duplicates==0"
    - "The all-PASS sweep is the SOLE phase verdict — byte[] altered nothing observable (DATA-04)"
    - "The recovery scenarios (TEST-02..07) prove byte[] Data survives KeeperInject over the RabbitMQ wire losslessly (DATA-05)"
  artifacts:
    - path: "analyzer-reports/phase-68-summary.json"
      provides: "the sweep's per-scenario Missing/Duplicates tabulation (oracle: trust the harness EXIT CODE over this if stale)"
      contains: "TEST-0"
  key_links:
    - from: "the rebuilt byte[] BaseProcessor.Core"
      to: "a fresh processor SourceHash"
      via: "reseed-FIRST (graph-delete → seed → start) before the sweep"
      pattern: "reseed"
    - from: "TEST-02..07 fault recovery"
      to: "byte[] Data through KeeperInject"
      via: "MassTransit STJ base64 wire round-trip"
      pattern: "Missing==0"
---

<objective>
Run the existing 7-scenario fault-recovery sweep against the rebuilt byte[] framework and confirm it reproduces its all-PASS baseline (every Missing==0, every Duplicates==0). Per locked D-05 this green sweep is the SOLE acceptance gate for the whole widening — the sweep reproducing its prior baseline IS the proof that byte[] changed nothing observable. No new test suite gates the phase.

Purpose: byte-for-byte equivalence (DATA-03) + recovery losslessness (DATA-05) proven live end-to-end; DATA-04 satisfied.
Output: an all-PASS sweep result, gated against the last-good all-PASS baseline commit.

ENVIRONMENT: this gate REQUIRES a Docker-capable host (Redis/RabbitMQ/Postgres/ES). It CANNOT run in the Docker-less sandbox (the ~272 RealStack infra failures there are not change-caused). This plan is autonomous:false — the executor performs the automated build + prep it can, then the sweep is launched and its verdict human-confirmed on the target host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-byte-data-channel-widening/84-VALIDATION.md
@.planning/phases/84-byte-data-channel-widening/84-01-SUMMARY.md
@.planning/phases/84-byte-data-channel-widening/84-02-SUMMARY.md
@scripts/phase-68-sweep.ps1

Runbook facts (VALIDATION.md §Sweep Runbook + project memory):
- Detached execution: the ~2h sweep is REAPED if run as a tracked bg task — launch via `Start-Process` + a SEPARATE short watcher loop; clear stale reports first ([[long-sweep-detached-process]]).
- Reseed-FIRST: after the BaseProcessor.Core rebuild the new SourceHash has NO processor row — graph-delete → seed → then start, or the first-scenario reset heal-wait traps ([[rebuild-sourcehash-reseed-order]]).
- Trust the harness EXIT CODE over a possibly-stale `analyzer-reports/phase-68-summary.json`; TEST-01 can flake on cold ES — re-run the single id, never auto-retry a verdict FAIL ([[phase-68-sweep-stale-report-read]]).
- Baseline oracle: gate the verdict against the last-good all-PASS baseline commit, not a possibly-clobbered HEAD report ([[phase-78-live-gate-duplicate-hops]]).
- Sweep entry point: `pwsh -File scripts/phase-68-sweep.ps1` (default `-ScenarioIds TEST-01..TEST-07`; a single id is re-runnable, e.g. `-ScenarioIds TEST-05`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the solution 0-warning so the sweep runs the byte[] code</name>
  <files>(no source edits — build artifacts only)</files>
  <read_first>
    - 84-01-SUMMARY.md + 84-02-SUMMARY.md (confirm production + tests migrated and green in-sandbox)
    - scripts/phase-68-sweep.ps1 (header — the scenarios it drives and the exit-code table)
  </read_first>
  <action>
    Rebuild the whole solution in BOTH Debug and Release with warnings-as-errors, so the deployed processor/keeper/orchestrator binaries carry the byte[] contract. Do NOT change any source. Confirm the BaseProcessor.Core assembly rebuilt (its change mints a new processor SourceHash — the reseed in Task 2 depends on this). This is the only step of this plan that runs cleanly in the sandbox; the sweep itself needs the Docker host.
  </action>
  <verify>
    <automated>dotnet build -c Release -warnaserror 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build -c Debug -warnaserror` and `-c Release -warnaserror` both succeed with zero warnings across the whole solution.
    - The BaseProcessor.Core DLL timestamp is newer than the last commit (rebuilt) — confirming a fresh SourceHash for the reseed.
  </acceptance_criteria>
  <done>The solution builds 0-warning in Debug + Release; deployable artifacts carry byte[].</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the 7-scenario fault-recovery sweep (SOLE phase gate)</name>
  <action>On the Docker host, clear stale reports, reseed-FIRST (graph-delete → seed → start), launch scripts/phase-68-sweep.ps1 DETACHED via Start-Process with a separate watcher, then confirm the harness exit code is 0 and every scenario reports Missing==0 and Duplicates==0, gated against the last-good all-PASS baseline commit.</action>
  <what-built>The byte[]-widened framework (Plans 01-02) rebuilt 0-warning. The 7-scenario fault-recovery sweep is ready to run on the Docker host as the SOLE phase gate (D-05 / DATA-04).</what-built>
  <how-to-verify>
    On a Docker-capable host (NOT the sandbox), reproduce the all-PASS sweep baseline:

    1. CLEAR stale reports first (a stale `analyzer-reports/phase-68-summary.json` can be misread) — remove/rotate the prior summary so only THIS run's tabulation is present.
    2. RESEED-FIRST (mandatory after the rebuild — the new SourceHash has no processor row): graph-delete the stale processor rows → seed → THEN start the stack. Skipping this traps the first scenario's reset heal-wait ([[rebuild-sourcehash-reseed-order]]).
    3. LAUNCH DETACHED (the sweep runs ~2h and is reaped if tracked): `Start-Process pwsh -ArgumentList '-File','scripts/phase-68-sweep.ps1'` (default all 7 scenarios TEST-01..TEST-07), then poll with a SEPARATE short watcher loop — do NOT run it as a tracked foreground/background task.
    4. When it finishes, TRUST THE HARNESS EXIT CODE (0 == all-PASS) over the JSON report. Confirm every scenario reports `Missing==0` AND `Duplicates==0`. If TEST-01 flakes on cold ES, re-run ONLY that single id (`-ScenarioIds TEST-01`) — never auto-retry a genuine verdict FAIL.
    5. GATE against the last-good all-PASS baseline COMMIT (not a possibly-clobbered HEAD report): the byte[] change must reproduce the SAME all-PASS shape. TEST-02..07 exercise the recovery round-trip — their PASS proves byte[] Data survived KeeperInject over the RabbitMQ wire losslessly (DATA-05).

    Report the harness exit code and the per-scenario Missing/Duplicates line for all 7.
  </how-to-verify>
  <acceptance_criteria>
    - Sweep harness exit code == 0 (all-PASS).
    - All 7 scenarios: Missing==0 AND Duplicates==0.
    - The result matches the last-good all-PASS baseline (byte[] altered nothing observable — DATA-04).
    - Any single re-run was an ES-flake retry of one id, NOT a masked verdict FAIL.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the exit code + the 7-scenario Missing/Duplicates summary, or describe the failing scenario(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Unchanged — this plan runs the existing sweep against the migrated framework; it introduces no new code and no new boundary. The sweep's fault scenarios ARE the adversarial exercise (injected read/reinject faults) already in the harness.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering / DoS | recovery round-trip under fault (TEST-02..07) | mitigate | The sweep's injected-fault scenarios drive byte[] Data through KeeperInject; an all-PASS (Missing==0/Duplicates==0) proves no loss/duplication was introduced by the type change. |
| T-84-02 | Information disclosure | live logs during the sweep | mitigate | FW-03 discipline preserved from Plans 01-02 (byte[] never logged); the sweep's analyzer reads ids/outcomes only, never payloads. |
| T-84-SC | Tampering (supply chain) | no installs | accept | No package installs — a verification-only run of an existing harness. |
</threat_model>

<verification>
- Whole-solution 0-warning rebuild (Task 1).
- Sweep harness exit code 0; all 7 scenarios Missing==0 and Duplicates==0 (Task 2 checkpoint), gated against the last-good all-PASS baseline commit.
</verification>

<success_criteria>
- DATA-04: the existing 7-scenario sweep reproduces its all-PASS baseline as the SOLE gate — no new suite gated the phase.
- DATA-03 + DATA-01: byte-for-byte equivalence proven live end-to-end (the sweep is byte-identical to its prior baseline).
- DATA-05: recovery scenarios prove byte[] Data survives the recovery backup over the RabbitMQ wire losslessly.
</success_criteria>

<output>
Create `.planning/phases/84-byte-data-channel-widening/84-03-SUMMARY.md` when done — record the harness exit code, the 7-scenario Missing/Duplicates table, the baseline commit gated against, and any single-id ES-flake re-run. This SUMMARY carries the phase verdict.
</output>
