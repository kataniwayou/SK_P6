---
phase: 72-processor-always-write-to-l2-and-uniform-orchestrator-pre-pi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/MeterCollectorSeam.cs
  - src/Orchestrator/Dispatch/StepAdvancement.cs
  - src/Orchestrator/Observability/OrchestratorMetrics.cs
  - tests/BaseApi.Tests/Orchestrator/StepAdvancementTests.cs
  - tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs
autonomous: true
requirements: [SPEC-4, SPEC-5, SPEC-6]

must_haves:
  truths:
    - "A net-new in-test metric-capture seam (BCL MeterListener) can assert orchestrator_step_unresolved increments + the workflowId tag without a live Prometheus collector"
    - "SelectNext returns a structured {Matches, UnresolvedIds} result: a dangling next-step id lands in UnresolvedIds, a condition-mismatch is collected nowhere, a match lands in Matches"
    - "SelectNext stays pure — no Redis, no metrics, no harness; the ?? Enumerable.Empty<Guid>() terminal guard is preserved"
    - "OrchestratorMetrics exposes a non-null orchestrator_step_unresolved counter built via IMeterFactory.Create(\"Orchestrator\") (snake_case, no _total in code, no static Meter)"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/MeterCollectorSeam.cs"
      provides: "MeterListener-based counter-capture helper over the Orchestrator meter"
      min_lines: 20
    - path: "src/Orchestrator/Dispatch/StepAdvancement.cs"
      provides: "SelectNext reshaped to a readonly record struct SelectNextResult(Matches, UnresolvedIds)"
      contains: "UnresolvedIds"
    - path: "src/Orchestrator/Observability/OrchestratorMetrics.cs"
      provides: "StepUnresolved counter named orchestrator_step_unresolved"
      contains: "orchestrator_step_unresolved"
  key_links:
    - from: "StepAdvancement.SelectNext"
      to: "SelectNextResult { Matches, UnresolvedIds }"
      via: "single pass over NextStepIds classifying each id three ways"
      pattern: "SelectNextResult"
    - from: "OrchestratorMetrics ctor"
      to: "orchestrator_step_unresolved counter"
      via: "meter.CreateCounter<long>"
      pattern: "CreateCounter<long>\\(\"orchestrator_step_unresolved\"\\)"
---

<objective>
Lay the orchestrator-side foundation that Plan 03 consumes: (1) the single net-new test seam — a BCL `MeterListener` helper to capture `orchestrator_step_unresolved` increments + tags; (2) reshape the pure `StepAdvancement.SelectNext` to return `{ Matches, UnresolvedIds }` so dangling next-step ids become observable; (3) add the `orchestrator_step_unresolved` counter to `OrchestratorMetrics`. The `OrchestratorPrePipeline` wiring (increments + uniform flow) lands in Plan 03.

Purpose: These three are the contracts Plan 03 builds against — the counter to increment, the classification result to fan out, and the seam to assert the metric. Keeping them in a separate Wave-1 plan with disjoint files lets Plan 03 (Wave 2) consume stable contracts.
Output: New `MeterCollectorSeam.cs`; modified `StepAdvancement.cs` + `OrchestratorMetrics.cs`; extended-in-place `StepAdvancementTests` + `OrchestratorMetricsFacts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-processor-always-write-to-l2-and-uniform-orchestrator-pre-pi/72-SPEC.md
@.planning/phases/72-processor-always-write-to-l2-and-uniform-orchestrator-pre-pi/72-CONTEXT.md
@.planning/phases/72-processor-always-write-to-l2-and-uniform-orchestrator-pre-pi/72-RESEARCH.md
@.planning/phases/72-processor-always-write-to-l2-and-uniform-orchestrator-pre-pi/72-PATTERNS.md

<interfaces>
<!-- Live-code contracts — VERIFIED this planning pass. No exploration needed. -->

StepAdvancement.SelectNext — current (src/Orchestrator/Dispatch/StepAdvancement.cs:36-43):
```csharp
private const int Always = 4; // Never = 5 falls out of the predicate
public IEnumerable<(Guid stepId, StepProjection step)> SelectNext(
    StepOutcome outcome, StepProjection completed, IReadOnlyDictionary<Guid, StepProjection> steps)
{
    foreach (var nextId in completed.NextStepIds ?? Enumerable.Empty<Guid>())
        if (steps.TryGetValue(nextId, out var next) &&
            (next.EntryCondition == (int)outcome || next.EntryCondition == Always))
            yield return (nextId, next);
}
```

OrchestratorMetrics ctor — current (src/Orchestrator/Observability/OrchestratorMetrics.cs:44-50):
```csharp
public const string MeterName = "Orchestrator";  // :25
public OrchestratorMetrics(IMeterFactory meterFactory)
{
    var meter = meterFactory.Create(MeterName);
    DispatchSent   = meter.CreateCounter<long>("orchestrator_dispatch_sent");
    ResultConsumed = meter.CreateCounter<long>("orchestrator_result_consumed");
    ResultDeduped  = meter.CreateCounter<long>("orchestrator_result_deduped");
}
```

OrchestratorMetricsFacts — current (tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs:24-35):
```csharp
[Fact] public void Constructs_With_NonNull_Counters_From_Real_MeterFactory()
{
    using var provider = new ServiceCollection().AddMetrics().BuildServiceProvider();
    var meterFactory = provider.GetRequiredService<IMeterFactory>();
    var metrics = new OrchestratorMetrics(meterFactory);
    Assert.NotNull(metrics.DispatchSent);
    Assert.NotNull(metrics.ResultConsumed);
}
```

StepAdvancementTests call sites that break on the reshape (tests/BaseApi.Tests/Orchestrator/StepAdvancementTests.cs): 6 facts chain `.Select(s => s.stepId)` at :62, :78, :93, :108(in DanglingNextStepId_IsSkipped_NoThrow), :126, :139; `TerminalStep_NullNextStepIds_*` (:118-129) uses `.ToList()`; `HelperPerformsNoIo_*` (:131-143) uses `.ToList()` + `selected[0].stepId`.

StepProjection shape (Messaging.Contracts.Projections): `record StepProjection(int EntryCondition, Guid ProcessorId, string Payload, Guid[] NextStepIds)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave-0 seam): MeterListener counter-capture helper</name>
  <files>tests/BaseApi.Tests/Orchestrator/MeterCollectorSeam.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs (:1-35 — the real IMeterFactory / ServiceCollection().AddMetrics() boilerplate to mirror)
    - 72-RESEARCH.md "Wave 0 gap" + Assumptions Log A1 (Option B = zero-dep BCL MeterListener — CHOSEN here to honor the "no new packages" constraint); 72-PATTERNS.md "Wave-0 Test Seam"
    - src/Orchestrator/Observability/OrchestratorMetrics.cs (:25 MeterName const = "Orchestrator")
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/MeterCollectorSeam.cs` — a zero-new-dependency BCL `System.Diagnostics.Metrics.MeterListener` helper (do NOT add `Microsoft.Extensions.Diagnostics.Testing`; A1 chooses Option B to preserve the "no new packages" constraint and REQ-6 hermetic isolation).

    Implement a small `IDisposable` class `MeterCollector` (namespace `BaseApi.Tests.Orchestrator`) that:
    - Takes a meter name (default `"Orchestrator"`) and an instrument name (e.g. `"orchestrator_step_unresolved"`) in its constructor.
    - Constructs a `MeterListener`, sets `InstrumentPublished` to call `listener.EnableMeasurementEvents(instrument)` only when `instrument.Meter.Name == meterName && instrument.Name == instrumentName`.
    - Registers `SetMeasurementEventCallback<long>` that accumulates each measurement into a thread-safe `List<(long Value, KeyValuePair<string, object?>[] Tags)>` (lock or `ConcurrentBag`), capturing `tags.ToArray()`.
    - Calls `listener.Start()` in the ctor.
    - Exposes `long Total` (sum of captured values), `int Count` (number of measurements), `IReadOnlyList<KeyValuePair<string, object?>[]> Tags` (per-measurement tag arrays), and a `Dispose()` that disposes the listener.

    This is the single net-new test infra (RESEARCH Wave-0). It is consumed by `OrchestratorPrePipelineFacts` in Plan 03 — design the surface so a fact can write: `using var mc = new MeterCollector("Orchestrator", "orchestrator_step_unresolved"); ... ; Assert.Equal(1, mc.Total); Assert.Contains(mc.Tags[0], t => t.Key == "workflowId");`
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests -c Debug --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Orchestrator/MeterCollectorSeam.cs` exists and contains `new MeterListener` (or `MeterListener(`)
    - It filters on `instrument.Meter.Name` and `instrument.Name` (grep both)
    - It exposes a `Total` and a `Tags` member usable from a fact
    - No new `<PackageReference>` for `Microsoft.Extensions.Diagnostics.Testing` was added to `tests/BaseApi.Tests/BaseApi.Tests.csproj`
    - `dotnet build tests/BaseApi.Tests -c Debug` exits 0
  </acceptance_criteria>
  <done>A zero-dep MeterListener-based MeterCollector helper exists, compiles, and exposes Total/Count/Tags for the new counter — the seam Plan 03's metric assertions depend on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reshape SelectNext to {Matches, UnresolvedIds} + migrate StepAdvancementTests</name>
  <files>src/Orchestrator/Dispatch/StepAdvancement.cs, tests/BaseApi.Tests/Orchestrator/StepAdvancementTests.cs</files>
  <read_first>
    - src/Orchestrator/Dispatch/StepAdvancement.cs (FULL — the yield loop :36-43, Always=4 const :23, the terminal guard ?? Enumerable.Empty<Guid>() :39, the no-I/O purity doc :12-19)
    - tests/BaseApi.Tests/Orchestrator/StepAdvancementTests.cs (FULL — all 6 `.Select(s => s.stepId)` chains; `DanglingNextStepId_IsSkipped_NoThrow` :103-115; `TerminalStep_NullNextStepIds_*` :117-129; `HelperPerformsNoIo_*` :131-143)
    - 72-RESEARCH.md Pattern 4 + Pitfall 3; 72-PATTERNS.md "StepAdvancement.cs" section (D-01/D-02 three-way classification)
  </read_first>
  <behavior>
    - A next-step id NOT in the step map → appears in `.UnresolvedIds` (the new stage-3 signal); NOT in `.Matches`.
    - A next-step id in the map whose EntryCondition matches outcome (or == Always 4) → appears in `.Matches`; NOT in `.UnresolvedIds`.
    - A next-step id in the map whose EntryCondition != outcome && != Always (incl. Never 5) → collected NOWHERE (neither Matches nor UnresolvedIds).
    - A null/empty NextStepIds → empty Matches, empty UnresolvedIds, no NRE (terminal guard preserved).
    - SelectNext takes the step map as an argument and returns synchronously — no Redis, no metrics (purity preserved).
  </behavior>
  <action>
    In `StepAdvancement.cs`: define a co-located `public readonly record struct SelectNextResult(IReadOnlyList<(Guid stepId, StepProjection step)> Matches, IReadOnlyList<Guid> UnresolvedIds);` (allocation-light, harness-free; D-02 keeps it pure — NO metrics, NO Redis).

    Change `SelectNext`'s return type from `IEnumerable<(Guid stepId, StepProjection step)>` to `SelectNextResult`. Replace the yield loop with a single pass over `completed.NextStepIds ?? Enumerable.Empty<Guid>()` building two local lists, classifying each `nextId`:
    - `!steps.TryGetValue(nextId, out var next)` → add `nextId` to `unresolvedIds` (case a — the new signal; previously dropped silently by the TryGetValue guard).
    - `else if (next.EntryCondition == (int)outcome || next.EntryCondition == Always)` → add `(nextId, next)` to `matches` (case c).
    - `else` → do nothing (case b — condition mismatch / Never; collected NOWHERE, D-03 — no metric).
    Return `new SelectNextResult(matches, unresolvedIds)`. Preserve the `?? Enumerable.Empty<Guid>()` terminal guard and the `Always = 4` const semantics. Update the SelectNext XML doc (:25-35) to describe the three-way classification and that the dangling id is now surfaced in `UnresolvedIds` (not silently dropped).

    In `StepAdvancementTests.cs`: migrate every `SelectNext(...).Select(s => s.stepId)` chain (the 6 sites) to `SelectNext(...).Matches.Select(s => s.stepId)`; migrate the `.ToList()` sites (`TerminalStep_NullNextStepIds_*`, `HelperPerformsNoIo_*`) to `.Matches.ToList()`. In `DanglingNextStepId_IsSkipped_NoThrow` (:103-115) additionally assert the dangling id now appears in `.UnresolvedIds` (e.g. `Assert.Contains(DanglingId, result.UnresolvedIds)`) — this is the new observability signal. Keep `TerminalStep_NullNextStepIds_*` asserting `.Matches` is empty AND assert `.UnresolvedIds` is empty (terminal ≠ unresolved). Extend in-place, preserve history (D-12).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~StepAdvancementTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "readonly record struct SelectNextResult" src/Orchestrator/Dispatch/StepAdvancement.cs` matches
    - `StepAdvancement.cs` SelectNext returns `SelectNextResult` (not `IEnumerable`); contains `UnresolvedIds` population for the `!steps.TryGetValue` case
    - `StepAdvancement.cs` contains no `metrics` or `redis`/`IDatabase` reference (purity preserved — D-02)
    - `grep -c "SelectNext(.*).Select(s => s.stepId)" tests/BaseApi.Tests/Orchestrator/StepAdvancementTests.cs` returns 0 (all migrated to `.Matches.Select`)
    - `StepAdvancementTests` `DanglingNextStepId_IsSkipped_NoThrow` asserts `.UnresolvedIds` contains the dangling id
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~StepAdvancementTests"` exits 0
  </acceptance_criteria>
  <done>SelectNext is a pure function returning {Matches, UnresolvedIds}; dangling ids surface in UnresolvedIds; condition-mismatch is collected nowhere; the terminal guard holds; all 6 migrated facts pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add orchestrator_step_unresolved counter to OrchestratorMetrics</name>
  <files>src/Orchestrator/Observability/OrchestratorMetrics.cs, tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs</files>
  <read_first>
    - src/Orchestrator/Observability/OrchestratorMetrics.cs (FULL — the ctor :44-50, the existing counter properties :28/:31/:42, MeterName const :25, the snake_case-no-_total doc :16-20)
    - tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs (:24-35 — the NonNull-counters fact to extend)
    - 72-PATTERNS.md "OrchestratorMetrics.cs" section (mirror the :47 CreateCounter line); 72-RESEARCH.md Pattern 6
  </read_first>
  <action>
    In `OrchestratorMetrics.cs`: add a property `public Counter<long> StepUnresolved { get; }` alongside `DispatchSent`/`ResultConsumed`/`ResultDeduped` (mirror the :28/:31 doc style — a one-line `<summary>` noting it increments at the two L1-resolution misses, label `workflowId`). In the ctor (:44-50), add `StepUnresolved = meter.CreateCounter<long>("orchestrator_step_unresolved");` (snake_case, NO `_total` in the name — the collector appends it; mirror the :47 line exactly). Do NOT add a static `Meter`; reuse the existing `meterFactory.Create(MeterName)`. Make NO change to `MeterName` or any `AddMeter` registration (REQ-6).

    In `OrchestratorMetricsFacts.cs`: add `Assert.NotNull(metrics.StepUnresolved);` to `Constructs_With_NonNull_Counters_From_Real_MeterFactory` (copy the :33-34 NotNull lines). Leave `MeterName_Is_Orchestrator` unchanged.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~OrchestratorMetricsFacts" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "orchestrator_step_unresolved" src/Orchestrator/Observability/OrchestratorMetrics.cs` matches and the string contains NO `_total`
    - `grep -n "public Counter<long> StepUnresolved" src/Orchestrator/Observability/OrchestratorMetrics.cs` matches
    - `OrchestratorMetrics.cs` still creates the counter via `meterFactory.Create(MeterName)` (no static `Meter` field added); `MeterName` const unchanged == "Orchestrator"
    - `OrchestratorMetricsFacts` asserts `Assert.NotNull(metrics.StepUnresolved)`
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~OrchestratorMetricsFacts"` exits 0
  </acceptance_criteria>
  <done>OrchestratorMetrics exposes a non-null orchestrator_step_unresolved counter built via IMeterFactory (snake_case, no _total, no static Meter); meter name + AddMeter unchanged; the facts prove non-null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound result `WorkflowId` → metric label | The `workflowId` tag value derives from the inbound message's WorkflowId (a Guid) — a potential cardinality source on the metric (the increment itself lands in Plan 03; the counter definition here is the surface) |
| in-process meter → MeterListener | The test seam subscribes to the in-process `Orchestrator` meter only (hermetic, no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05 | Denial of Service | metric-label cardinality on `orchestrator_step_unresolved` | accept | The single label is `workflowId` (a bounded set of authored workflow Guids), not a free-form/untrusted string. Cardinality is bounded by the number of deployed workflows; no `reason`/`stage`/free-text label is added (label-minimal by spec). The increment volume itself is bounded by L1-resolution-miss frequency (Plan 03). |
| T-72-06 | Spoofing/Tampering | counter built via static `Meter` leaking across hermetic tests | mitigate | REQ-6/D-11: the counter is built via the existing `IMeterFactory.Create("Orchestrator")` — never a static `Meter`. Enforced by acceptance criterion (no static `Meter` field) + the real-IMeterFactory `OrchestratorMetricsFacts`. The MeterListener seam is per-test `IDisposable` (no cross-test static state). |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~StepAdvancementTests|FullyQualifiedName~OrchestratorMetricsFacts"` GREEN
- `dotnet build tests/BaseApi.Tests -c Debug` exits 0 (the MeterCollector seam compiles)
- `dotnet build src/Orchestrator -c Debug -warnaserror` AND `-c Release -warnaserror` exit 0
- No new package reference added to `tests/BaseApi.Tests/BaseApi.Tests.csproj`
</verification>

<success_criteria>
- A zero-dep MeterListener-based `MeterCollector` seam exists for `orchestrator_step_unresolved` capture
- `SelectNext` is a pure function returning `{Matches, UnresolvedIds}`; dangling ids surface in `UnresolvedIds`
- `OrchestratorMetrics` exposes a non-null `orchestrator_step_unresolved` counter (snake_case, no `_total`, IMeterFactory, meter name `Orchestrator` unchanged)
- All 6 migrated `StepAdvancementTests` + `OrchestratorMetricsFacts` GREEN; 0-warning Debug + Release
</success_criteria>

<output>
After completion, create `.planning/phases/72-processor-always-write-to-l2-and-uniform-orchestrator-pre-pi/72-02-SUMMARY.md`
</output>
