---
phase: 78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - analyzer-reports/phase-68-summary.json
autonomous: false
requirements: [D-04]
security_enforcement: true

must_haves:
  truths:
    - "The SourceHash reseed runs FIRST (rebuild both host configs + docker compose build → graph-delete → seed → 204) before the sweep"
    - "All 7 scenarios (TEST-01..07) reproduce their HEAD baseline verdict (all PASS)"
    - "Each PASS reconstructs from framework ES logs ALONE — no StepLabel/Produced value evidence contributes"
  artifacts:
    - path: "analyzer-reports/phase-68-summary.json"
      provides: "The 7-scenario roll-up from the rerun (compared to the HEAD baseline, not the working-tree stale copy)"
  key_links:
    - from: "scripts/phase-68-sweep.ps1"
      to: "scripts/phase-67-harness.ps1"
      via: "per-scenario run-all no-fail-fast"
      pattern: "phase-67-harness"
---

<objective>
Terminal live gate (D-04): after the mandatory SourceHash reseed, run `scripts/phase-68-sweep.ps1` over all 7 scenarios and confirm every scenario reproduces its committed-HEAD baseline verdict (all 7 PASS) with the value-oracle axis fully dark — proving the verdict reconstructs from framework ES logs (structural stepId completeness + ANL-03 redundancy + the Prometheus metric gate) ALONE.

Purpose: this is the phase's outcome proof — the reworked analyzer must give the same verdicts live as the pre-77 baseline, now without any concrete value oracle. RESEARCH proved the baseline exercised ZERO tolerance paths, so deleting value-chain path #1 is verdict-neutral by construction; this gate proves it live.
Output: a fresh `analyzer-reports/phase-68-summary.json` roll-up whose 7 verdicts match `git show HEAD:analyzer-reports/phase-68-summary.json` (all PASS). A legitimate verdict SHIFT → STOP, treat as a finding (do NOT silently re-baseline).

Operator-gated: requires Docker + a live compose stack + the reseed. If Docker is unavailable in this environment, follow the deferred-automated fallback (record the exact runbook to `deferred-items.md`) — the precedent for phases 68/73/74/75.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-CONTEXT.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-RESEARCH.md
@scripts/phase-68-sweep.ps1
@scripts/phase-67-harness.ps1

<interfaces>
<!-- D-04 runbook (78-RESEARCH.md "Live-gate runbook") + memory refs. -->

BASELINE ORACLE: `git show HEAD:analyzer-reports/phase-68-summary.json` = all 7 PASS, startedRuns == completeRuns for every scenario. The working-tree `analyzer-reports/phase-68-summary.json` in `git status` is a STALE single-TEST-07 write — do NOT gate against it.

RESEED ORDER ([[rebuild-sourcehash-reseed-order]] — Phase 77 touched BaseProcessor.Core, so the new SourceHash has no processor row; a reset heal-wait fails until graph-delete → seed → start):
  1. Rebuild BOTH host configs (Debug + the config the compose images build from).
  2. `docker compose build` the images (so the running container is the just-built image; the harness does `docker compose up -d --force-recreate` at STEP A, phase-67-harness.ps1:154).
  3. graph-DELETE the old graph/processor rows.
  4. seed the fan-out workflow.
  5. confirm the 204 activation gate.
  6. only THEN run the sweep.

SWEEP: `pwsh -File scripts/phase-68-sweep.ps1` — run-all + collect, no fail-fast; exit 0 iff all 7 PASS. It reads only Verdict/StartedRuns/CompleteRuns/Missing/Duplicates from each `{id}.json`.

KNOWN TRAPS:
- Stale Debug analyzer report ([[phase-68-sweep-stale-report-read]]): the roll-up can read a stale `{id}.json` under bin/Debug vs bin/Release — TRUST the harness exit code over the tabulated row when they disagree.
- TEST-01 cold-ES INCONCLUSIVE flake: INCONCLUSIVE (harness exit 2) is NOT FAIL — re-run TEST-01 deliberately against warm ES (`pwsh -File scripts/phase-67-harness.ps1 -ScenarioId TEST-01`). No auto-retry (D-04).
- A legitimate verdict SHIFT (e.g. a scenario flips PASS→FAIL) → STOP, treat as a finding. A PASS with startedRuns > completeRuns (a genuinely OTLP-dropped hop caught by ANL-03) is a LEGITIMATE PASS, not a baseline mismatch — D-04 gates on verdict reproduction, not count reproduction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — hermetic green re-check, capture the HEAD baseline oracle, rebuild + docker compose build</name>
  <files>analyzer-reports/phase-68-summary.json (read HEAD baseline; no edit), docker images (rebuild)</files>
  <read_first>
    - 78-RESEARCH.md "Live-gate runbook" Steps 1-2
    - project memory [[rebuild-sourcehash-reseed-order]], [[hermetic-test-command]]
    - scripts/phase-67-harness.ps1 (STEP A force-recreate at :154)
  </read_first>
  <action>
    Automate everything that does not require a running live stack, BEFORE the operator checkpoint:
    - Re-confirm the Plan-03 hermetic gate is still green: `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` — zero new analyzer-scope failures.
    - Capture the baseline oracle: `git show HEAD:analyzer-reports/phase-68-summary.json` — record all 7 scenario verdicts (expected: all PASS, startedRuns == completeRuns) into the SUMMARY as the comparison table. Do NOT read the working-tree copy.
    - Rebuild BOTH host configs (Debug + the compose-image config) and `docker compose build` the images so the running container carries the Phase-77 SourceHash.
    - If Docker CLI is unavailable in this environment (precedent: phases 68/73/74/75 deferred-automated), record the exact reseed+sweep runbook to `deferred-items.md` under "DEFERRED: 78-04 live baseline reproduction" and mark the checkpoint DEFERRED-AUTOMATED. Do not fabricate a live result.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && git show HEAD:analyzer-reports/phase-68-summary.json | grep -c '"verdict": "Pass"'</automated>
  </verify>
  <acceptance_criteria>
    - Hermetic analyzer subset green (zero new failures) confirmed immediately before the live gate.
    - The HEAD baseline oracle is captured into the SUMMARY (7 scenarios, expected all PASS — `git show HEAD:analyzer-reports/phase-68-summary.json` shows 7 `"verdict": "Pass"`).
    - Both host configs rebuilt + `docker compose build` completed (OR, if Docker unavailable, the runbook recorded to `deferred-items.md` and the checkpoint marked DEFERRED-AUTOMATED).
  </acceptance_criteria>
  <done>Hermetic green re-confirmed; HEAD baseline captured as the oracle; images rebuilt (or deferred-automated recorded). Ready for the operator reseed + sweep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator live gate — reseed + 7-scenario sweep + HEAD-baseline reproduction</name>
  <files>analyzer-reports/phase-68-summary.json</files>
  <action>Operator-gated live verification — run the reseed-first runbook then the 7-scenario sweep and confirm HEAD-baseline reproduction, per the how-to-verify steps below. Not autonomous: requires Docker + a live compose stack (or DEFERRED-AUTOMATED if Docker is unavailable).</action>
  <what-built>
    The reworked LIVE analyzer (D-01 entry-marker self-exclusion, D-02 keeper `must_not`, D-03 value-oracle deleted) is hermetically green. The images are rebuilt to the Phase-77 SourceHash. This checkpoint runs the mandatory reseed + the 7-scenario live sweep and confirms every scenario reproduces its committed-HEAD baseline verdict with the value axis dark.
  </what-built>
  <how-to-verify>
    With Docker + the compose stack available, run the reseed-FIRST runbook ([[rebuild-sourcehash-reseed-order]]):
    1. graph-DELETE the old graph/processor rows.
    2. seed the fan-out workflow.
    3. confirm the 204 activation gate.
    4. Run the sweep: `pwsh -File scripts/phase-68-sweep.ps1` (run-all, no fail-fast).
    5. Compare each scenario's verdict to `git show HEAD:analyzer-reports/phase-68-summary.json` (all 7 PASS). TRUST the per-scenario harness exit code over any stale tabulated report row ([[phase-68-sweep-stale-report-read]]).
    6. If TEST-01 flakes to INCONCLUSIVE on cold ES, re-run it deliberately warm: `pwsh -File scripts/phase-67-harness.ps1 -ScenarioId TEST-01`. INCONCLUSIVE (exit 2) is NOT FAIL; no auto-retry.
    Expected outcome: all 7 scenarios reproduce their HEAD baseline PASS, reconstructed from framework ES logs alone (no StepLabel/Produced evidence). A PASS with startedRuns > completeRuns (OTLP-dropped hop caught by ANL-03) is still a PASS. A genuine verdict SHIFT → STOP and treat it as a finding (do NOT silently re-baseline).
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 scenarios reproduce their HEAD baseline verdict, or describe the scenario whose verdict shifted (treat as a finding). If Docker is unavailable, type "deferred" to record DEFERRED-AUTOMATED with the runbook in deferred-items.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The live gate exercises the isolated Docker test stack (compose-local ES/Prometheus/RabbitMQ/Redis/Postgres). The analyzer READS first-party ES/Prom; it writes only local `analyzer-reports/*.json`. No production surface, no untrusted input, no auth/network boundary added. The `scenarioId` path-traversal guard already exists via `ScenarioIdPattern`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-07 | Tampering (baseline integrity) | comparison against the wrong baseline | mitigate | Gate against `git show HEAD:analyzer-reports/phase-68-summary.json`, NOT the working-tree stale single-TEST-07 copy. The SUMMARY records the HEAD oracle explicitly to prevent a stale-baseline compare. |
| T-78-08 | Repudiation (verdict provenance) | a silent re-baseline hides a real shift | mitigate | D-04: a legitimate verdict shift STOPS the gate and is treated as a finding — never a silent re-baseline. The rerun report is compared, not overwritten-as-truth. |
| T-78-09 | DoS (test-env artifact) | cold-ES / stale-report flake mis-scored as FAIL | accept | INCONCLUSIVE (exit 2) ≠ FAIL; deliberate warm re-run per runbook. Test-env only; no production impact. |
</threat_model>

<verification>
- `pwsh -File scripts/phase-68-sweep.ps1` exits 0 (all 7 PASS) after the reseed; each scenario's verdict matches the HEAD baseline.
- Verdicts reconstruct from framework ES logs alone (structural stepId completeness + ANL-03 redundancy + metric gate) — no value-oracle evidence.
- OR: DEFERRED-AUTOMATED recorded with the exact runbook if Docker is unavailable (precedent phases 68/73/74/75).
</verification>

<success_criteria>
- All 7 scenarios reproduce their committed-HEAD baseline verdict (all PASS), value axis dark — OR deferred-automated with the runbook recorded.
- No silent re-baseline; any verdict shift surfaced as a finding.
</success_criteria>

<output>
After completion, create `.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-04-SUMMARY.md`.
</output>
