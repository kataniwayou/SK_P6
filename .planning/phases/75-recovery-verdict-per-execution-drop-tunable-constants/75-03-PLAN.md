---
phase: 75-recovery-verdict-per-execution-drop-tunable-constants
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs
  - src/Keeper/RecoveryOptions.cs
  - src/Orchestrator/Configuration/OrchestratorOutputOptions.cs
  - src/Processor.Sample/appsettings.json
  - src/Processor.BadConfig/appsettings.json
autonomous: false
requirements: [D75-6]
user_setup: []

must_haves:
  truths:
    - "All four TTL knobs (three Options-class defaults + the two appsettings values) move in lock-step to a value that outlasts full recovery (~300s+)."
    - "The jittered floor random[ttl, 2×ttl] comfortably exceeds the ~300s non-redis recovery window (dwell 45s + return-to-healthy + keeper reinject + drain 60s)."
    - "No distinct 5th index TTL exists — the slot-array index TTL was already unified to ExecutionDataTtl (quick 260615-dbf)."
  artifacts:
    - path: "src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs"
      provides: "Raised ExecutionDataTtlSeconds default"
      contains: "ExecutionDataTtlSeconds"
    - path: "src/Processor.Sample/appsettings.json"
      provides: "Raised ExecutionDataTtl test-env value"
      contains: "ExecutionDataTtl"
  key_links:
    - from: "ExecutionDataTtl config value"
      to: "L2ProjectionKeys.OutputDataTtl jitter random[ttl, 2×ttl]"
      via: "TTL floor supplied into the jitter policy"
      pattern: "ExecutionDataTtl"
---

<objective>
Neutralize the TTL confounder (D75-6): raise the test-env L2 TTLs so TTL-expiry cannot manufacture in-flight loss for the non-redis-crash scenarios (processor/orchestrator/keeper/rabbitmq — TEST-02/03/04/06). Raise all three Options-class defaults AND both appsettings values in lock-step from 300 to a value that outlasts full recovery (~300s+). Redis-crash scenarios (TEST-05/07) are the genuine in-flight-at-wipe durability boundary — TTL is moot there (redis is the wiped store).

Purpose: A benign TTL expiry must never be mistaken for a recovery failure; the raised TTL gives a stalled-then-reinjected execution's L2 blob time to survive until the keeper replays it.
Output: Four TTL knobs raised in lock-step, jitter floor verified to exceed the window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-RESEARCH.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-PATTERNS.md

<interfaces>
<!-- The four TTL knobs, extracted this session — all currently 300, cross-referencing each other's default in XML docs. -->

src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs:50-51
```csharp
[ConfigurationKeyName("ExecutionDataTtl")]
public int ExecutionDataTtlSeconds { get; set; } = 300;
```
src/Keeper/RecoveryOptions.cs:19
```csharp
public int ExecutionDataTtlSeconds { get; set; } = 300;   // doc: "matches ProcessorLivenessOptions"
```
src/Orchestrator/Configuration/OrchestratorOutputOptions.cs:11
```csharp
public int OutputDataTtlSeconds { get; set; } = 300;      // doc: "matching Processor/Recovery"
```
src/Processor.Sample/appsettings.json:33  →  "ExecutionDataTtl": 300
src/Processor.BadConfig/appsettings.json:33  →  "ExecutionDataTtl": 300

Jitter policy (do NOT change — just verify the floor) src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:70-71
```csharp
public static TimeSpan OutputDataTtl(int ttlSeconds)
    => TimeSpan.FromSeconds(Random.Shared.Next(ttlSeconds, 2 * ttlSeconds + 1));   // random[ttl, 2×ttl]
```

VERIFIED this session (assumption A2 confirmed): NO distinct 5th index TTL exists. The slot-array index EXPIRE was unified to the ExecutionDataTtl const and its jitter deleted in quick 260615-dbf. Grep for `KeyExpire`/`SlotTtl`/`SlotArrayOptions` in src returns only unrelated liveness/probe/projection TTLs, none on the execution-data slot-array index. These five knobs are the complete set.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise all three Options-class TTL defaults + both appsettings values to 900 in lock-step</name>
  <read_first>
    - src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs:44-52 (ExecutionDataTtlSeconds default 300, DISTINCT from the liveness TtlSeconds)
    - src/Keeper/RecoveryOptions.cs:10-20 (ExecutionDataTtlSeconds default 300)
    - src/Orchestrator/Configuration/OrchestratorOutputOptions.cs (OutputDataTtlSeconds default 300)
    - src/Processor.Sample/appsettings.json (the Redis section, ExecutionDataTtl: 300)
    - src/Processor.BadConfig/appsettings.json (ExecutionDataTtl: 300)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:70-71 (the random[ttl, 2×ttl] jitter — floor verification only, do not edit)
  </read_first>
  <files>src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs, src/Keeper/RecoveryOptions.cs, src/Orchestrator/Configuration/OrchestratorOutputOptions.cs, src/Processor.Sample/appsettings.json, src/Processor.BadConfig/appsettings.json</files>
  <action>
    Raise every TTL knob from 300 to 900 (3× the 300s window → jittered floor random[900, 1800] comfortably outlasts the ~300s non-redis recovery: dwell 45s + return-to-healthy + keeper reinject + drain 60s + poll-to-stable ≤60s). Edit ALL five in lock-step so no knob is left at 300 (Pitfall 3: appsettings binds OVER the Options default; a mismatch leaves an inconsistent lifetime):
    1. `ProcessorLivenessOptions.cs:51` — `public int ExecutionDataTtlSeconds { get; set; } = 900;` (leave the liveness `TtlSeconds` default untouched — it is a DISTINCT sliding-liveness floor, not the execution-data TTL).
    2. `RecoveryOptions.cs:19` — `public int ExecutionDataTtlSeconds { get; set; } = 900;`
    3. `OrchestratorOutputOptions.cs:11` — `public int OutputDataTtlSeconds { get; set; } = 900;`
    4. `Processor.Sample/appsettings.json:33` — `"ExecutionDataTtl": 900`
    5. `Processor.BadConfig/appsettings.json:33` — `"ExecutionDataTtl": 900`
    Update the XML doc-comments that say "default 300" to "default 900" on the three Options classes so the cross-references stay accurate (they all say "matches/matching" the others). Do NOT edit `L2ProjectionKeys.OutputDataTtl` — the jitter policy is unchanged; only the floor value flowing into it rises. Do NOT touch `ProcessorLivenessOptions.TtlSeconds` (:30, the liveness-key floor, default 30) or the L2ProbeRecovery scratch expiry (30s) — those are unrelated.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - `ProcessorLivenessOptions.cs` `ExecutionDataTtlSeconds` default is `= 900`; the file no longer contains `ExecutionDataTtlSeconds { get; set; } = 300`.
    - `RecoveryOptions.cs` `ExecutionDataTtlSeconds` default is `= 900`.
    - `OrchestratorOutputOptions.cs` `OutputDataTtlSeconds` default is `= 900`.
    - `Processor.Sample/appsettings.json` contains `"ExecutionDataTtl": 900` and no `"ExecutionDataTtl": 300`.
    - `Processor.BadConfig/appsettings.json` contains `"ExecutionDataTtl": 900` and no `"ExecutionDataTtl": 300`.
    - `ProcessorLivenessOptions.cs` still contains the DISTINCT liveness `TtlSeconds` default `= 30` (untouched).
    - Hermetic suite exits 0; 0-warning Debug + Release build (config-only change; no hermetic behavior change expected).
  </acceptance_criteria>
  <done>All five TTL knobs are 900 in lock-step; the liveness floor is untouched; hermetic suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Raised the four L2 TTL knobs (ExecutionDataTtl / OutputDataTtl, three Options defaults + two appsettings) from 300s to 900s so TTL-expiry cannot manufacture in-flight loss during the ~300s recovery of the non-redis scenarios (TEST-02/03/04/06).</what-built>
  <how-to-verify>
    This TTL neutralization is only OBSERVABLE on a live Docker RealStack (per prior phases 68/73/74, the live gate is deferred-automated when Docker is unavailable). When Docker IS available:
    1. Rebuild the containers so the new appsettings TTL is baked in: `pwsh -File scripts/phase-65-up.ps1` (or the standard compose-up), then confirm `ExecutionDataTtl=900` is picked up.
    2. Run the non-redis fault scenarios: `pwsh -File scripts/phase-68-sweep.ps1 -ScenarioIds TEST-02,TEST-03,TEST-04,TEST-06`.
    3. Expected: each scenario shows ZERO TTL-manufactured in-flight loss (the previously-observed TTL-expiry artifact — e.g. the Phase-68 TEST-06 self-expiry — is gone); the analyzer report's InFlightLoss for these scenarios is driven only by genuine recovery timing, not TTL.
    If Docker is unavailable: mark deferred-automated (precedent: phases 68/73/74 live close gates) and confirm the hermetic suite + 0-warning build as the automated gate.
  </how-to-verify>
  <resume-signal>Type "approved" (or "deferred-automated — Docker unavailable"), or describe any TTL-manufactured loss still observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → live service | TTL floor values are read from appsettings/Options into live services. This plan only raises magnitudes; no key shape, no new field, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-05 | Denial of Service | L2 (Redis) memory footprint under longer TTLs | accept | Raising TTL 300→900 lengthens key lifetime, but `RealStackNetZeroSweepFixture` actively DELETEs `skp:data:*`/`skp:msg:*` (it does not wait for TTL expiry — verified in research A4), so no residual-key accumulation gate is affected. Test-env only; no production impact. |
| T-75-06 | Tampering | TTL config values | accept | Compile-time / appsettings literals; no external input controls them. No new external surface. |
</threat_model>

<verification>
- `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0.
- 0-warning Debug + Release build.
- Live TTL neutralization: deferred-automated (Docker) per the checkpoint.
</verification>

<success_criteria>
- All five TTL knobs at 900 in lock-step; jitter floor random[900,1800] outlasts the ~300s window; hermetic green + 0-warning; live neutralization verified or deferred-automated.
</success_criteria>

<output>
After completion, create `.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-03-SUMMARY.md`
</output>
