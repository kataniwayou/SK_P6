---
phase: 75-recovery-verdict-per-execution-drop-tunable-constants
plan: 04
type: execute
wave: 2
depends_on: [75-01, 75-02]
files_modified:
  - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs
  - tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs
autonomous: false
requirements: [D75-4]
user_setup: []

must_haves:
  truths:
    - "The analyzer ES-queries the keeper REINJECT drop/success logs and builds a per-(corr,exec) keeper-outcome map."
    - "The keeper-outcome map is passed into PassFailEngine.Analyze via the new keeperOutcomeByExecution parameter."
    - "The join reads keeper outcomes from ES logs only — never a live Redis probe (the fixture is ES-read-only)."
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs"
      provides: "Keeper-outcome ES read + map + Analyze wiring"
      contains: "keeperOutcomeByExecution"
    - path: "tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs"
      provides: "ReinjectOutcome field-path const"
      contains: "ReinjectOutcomeFieldPath"
  key_links:
    - from: "AnalyzerE2ETests keeper-outcome ES read"
      to: "PassFailEngine.Analyze(keeperOutcomeByExecution:)"
      via: "cohort.KeeperOutcomeByExecution passed as the new named arg"
      pattern: "keeperOutcomeByExecution:"
---

<objective>
Complete the D75-4 live join: the analyzer ES-queries the keeper REINJECT drop/success logs (the structured `attributes.ReinjectOutcome`/`CorrelationId`/`ExecutionId` fields Plan 02 added), builds a per-`(corr,exec)` keeper-outcome map, and passes it into the Plan-01 classifier via the new `keeperOutcomeByExecution` parameter. This closes the loop so a lost execution the keeper clean-dropped is classified provably-unrecoverable (tolerated) and a recoverable-but-lost execution is a binding FAIL — from keeper evidence, on the live stack.

Purpose: Wire keeper recovery evidence into the verdict on the RealStack path.
Output: `AnalyzerE2ETests` reads keeper outcomes from ES and feeds them to `PassFailEngine`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-RESEARCH.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-PATTERNS.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-01-SUMMARY.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (new engine param) + Plan 02 (new keeper ES fields) + this file's own conventions. -->

Plan 01 added this trailing Analyze parameter (keyed "correlationId|executionId" → outcome string "drop"/"reinject"):
```csharp
IReadOnlyDictionary<string, string>? keeperOutcomeByExecution = null
```

Plan 02 made the keeper emit these ES attributes on both recovery paths:
`attributes.CorrelationId`, `attributes.ExecutionId`, `attributes.EntryId`, `attributes.MessageId`, `attributes.ReinjectOutcome` (values `"drop"` | `"reinject"`).

Existing Analyze call site to extend (AnalyzerE2ETests.cs:221-230) — add the new named arg alongside recoveryUtc/firstHop/lastHop:
```csharp
var report = new PassFailEngine().Analyze(
    traces, promSnapshot, scenarioId,
    tripDurationMsByExecution: cohort.TripDurationMsByExecution,
    tripDurationMsByCorrelation: cohort.TripDurationMsByCorrelation,
    seedsByExecution: cohort.SeedsByExecution,
    expectsKeeperActivity: ExpectsKeeperActivity.GetValueOrDefault(scenarioId, false),
    mg1Binding: Mg1Binding.GetValueOrDefault(scenarioId, true),
    recoveryUtc: recoveryUtc,
    firstHopUtcByExecution: cohort.FirstHopUtcByExecution,
    lastHopUtcByExecution: cohort.LastHopUtcByExecution);
```

The TraceCohort record to extend (AnalyzerE2ETests.cs:313-326) — add a KeeperOutcomeByExecution required member mirroring FirstHopUtcByExecution:
```csharp
public required IReadOnlyDictionary<string, DateTimeOffset> FirstHopUtcByExecution { get; init; } // keyed "corr|exec"
public required IReadOnlyDictionary<string, DateTimeOffset> LastHopUtcByExecution { get; init; }  // keyed "corr|exec"
```

ES query + map analogs to mirror:
- `BuildStepSearchBody` (AnalyzerE2ETests.cs:251-266) — static raw-string body over `EsIndexNames.*FieldPath` consts, window-bounded, sort asc. Mirror it for a keeper-outcome body filtering on `exists attributes.ReinjectOutcome` + the window range.
- `BuildRunTraces` (AnalyzerE2ETests.cs:339+) — group hits, defensively read attrs (`attrs.TryGetProperty(... ValueKind == String)`), skip odd-shaped hits (`continue`), key `$"{Corr}|{Exec}"`.

Field-path const convention (EsIndexNames.cs:71,87,107) — DIRECT path, NEVER `.keyword` (documented trap breaks the query):
```csharp
public const string CorrelationIdFieldPath = "attributes.CorrelationId";
public const string ExecutionIdFieldPath   = "attributes.ExecutionId";
```

Read-only constraint (AnalyzerE2ETests.cs:44-47): the fixture is ES-read-only. The keeper-outcome join MUST come from keeper logs in ES, NEVER a live Redis probe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ReinjectOutcome field-path const + keeper-outcome ES read + map into TraceCohort</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs:65-107 (the DIRECT-path const convention + the .keyword-trap doc — add the new const in the same style)
    - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs:243-457 (BuildStepSearchBody :251, PollHitsToStableAsync :282, TraceCohort :313-326, BuildRunTraces :339 — the group/defensive-read/skip idiom + the "corr|exec" keying at ~:409)
  </read_first>
  <files>tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs, tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs</files>
  <action>
    1. In `EsIndexNames.cs`, add `public const string ReinjectOutcomeFieldPath = "attributes.ReinjectOutcome";` alongside the existing field-path consts (:107 region), with a doc-comment mirroring the DIRECT-path / no-`.keyword`-trap rationale. (CorrelationIdFieldPath/ExecutionIdFieldPath already exist and are reused.)
    2. In `AnalyzerE2ETests.cs`, add a `BuildKeeperOutcomeSearchBody(windowStart, snapshot)` static method mirroring `BuildStepSearchBody` (:251-266): a static raw-string `_search` body, `"size": 2000`, filter = `{ "exists": { "field": "{{EsIndexNames.ReinjectOutcomeFieldPath}}" } }` + the window `range` on `EsIndexNames.WindowTimestampFieldPath` (`gte windowStart:o`, `lte snapshot:o`), sort asc on the window field. Query `attributes.ReinjectOutcome`/`CorrelationId`/`ExecutionId` DIRECTLY (never `.keyword`).
    3. Add a `BuildKeeperOutcomeMap(List<JsonElement> hits)` static method mirroring the `BuildRunTraces` defensive-read idiom (:339+): for each hit, read `_source.attributes` → defensively pull `CorrelationId`, `ExecutionId`, `ReinjectOutcome` via `attrs.TryGetProperty(path, out var v) && v.ValueKind == JsonValueKind.String` (skip odd-shaped hits with `continue` — never throw, per T-66-09); key the output dict `$"{corr}|{exec}"` (StringComparer.Ordinal) → the outcome string. If BOTH a `"drop"` and a `"reinject"` exist for the same key (keeper dropped then a later reinject, or vice versa), prefer `"reinject"` (a successful reinject means the data was recoverable — do NOT tolerate it as a clean drop). Return `IReadOnlyDictionary<string,string>`.
    4. Extend the `TraceCohort` record (:313-326) with `public required IReadOnlyDictionary<string, string> KeeperOutcomeByExecution { get; init; }` (keyed "corr|exec"), and populate it in the cohort-construction site — pull the keeper-outcome hits via `SearchAllHits(BuildKeeperOutcomeSearchBody(...))` over the SAME window (the keeper docs ride the same OTLP pipeline as the Step_* docs; the existing `DrainMs=60_000` + poll-to-stable already tolerates the ~60s export skew, so no new drain is needed) and run them through `BuildKeeperOutcomeMap`.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - `EsIndexNames.cs` contains `ReinjectOutcomeFieldPath = "attributes.ReinjectOutcome"` (no `.keyword`).
    - `AnalyzerE2ETests.cs` contains `BuildKeeperOutcomeSearchBody` and `BuildKeeperOutcomeMap`.
    - `AnalyzerE2ETests.cs` `TraceCohort` contains `KeeperOutcomeByExecution`.
    - The map-build reads `attributes.ReinjectOutcome`/`CorrelationId`/`ExecutionId` and never contains a Redis/`Db.StringLength`/`IDatabase` call (ES-read-only preserved — grep for `IDatabase`/`redis` in the new code returns nothing).
    - Hermetic suite exits 0 (the new methods compile; the RealStack fixture's hermetic-excluded so this is a build+compile gate for now); 0-warning Debug + Release.
  </acceptance_criteria>
  <done>The keeper-outcome ES read + defensive map + TraceCohort field exist; ES-read-only preserved; compiles + hermetic green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Wired the keeper-outcome map into the Analyze call so the classifier reads keeper evidence on the live stack: `keeperOutcomeByExecution: cohort.KeeperOutcomeByExecution` added to the `PassFailEngine().Analyze(...)` call at AnalyzerE2ETests.cs:221-230.</what-built>
  <how-to-verify>
    First, add the wiring: in `AnalyzerE2ETests.cs`, extend the `Analyze(...)` call (:221-230) with the new named arg `keeperOutcomeByExecution: cohort.KeeperOutcomeByExecution` after `lastHopUtcByExecution:`. Confirm `grep` shows `keeperOutcomeByExecution:` in the call site and the hermetic suite still exits 0 (this is a compile-safe additive named arg).

    The LIVE join surfacing is only OBSERVABLE on a Docker RealStack (per prior phases 68/73/74, deferred-automated when Docker is unavailable). When Docker IS available:
    1. Run the research Open-Question-1 probe first: inject a keeper clean-absent drop (a non-redis scenario where L2[entryId] is gone), then query ES for the keeper doc and confirm `attributes.ReinjectOutcome` + `attributes.CorrelationId`/`ExecutionId` actually land (verify keeper WARNING-level logs are not level-filtered before OTLP export — check the console log level vs LogLevelFilterTests).
    2. Run a full sweep: `pwsh -File scripts/phase-68-sweep.ps1`.
    3. Expected: a scenario with a recoverable-but-lost execution (keeper could have reinjected — L2 blob present, no drop log) → binding FAIL; a scenario where the keeper clean-dropped a gone execution → tolerated (reported + cause-labeled in `UnrecoverableLossDetail`), not a pass/fail lever. The verdict contains no absolute count / window-seconds / cron-rate term.
    If Docker is unavailable: mark deferred-automated (precedent: phases 68/73/74) and rely on the hermetic suite (Plan 01 facts already prove the classifier semantics with synthetic keeper maps) + 0-warning build as the automated gate.
  </how-to-verify>
  <resume-signal>Type "approved" (or "deferred-automated — Docker unavailable"), or describe any keeper attribute that failed to surface / any misclassification.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elasticsearch → analyzer | The fixture reads keeper log docs from ES. This plan adds one more read query + a defensive JSON parse. No write, no Redis, no untrusted external input (ES is the internal test-stack log store). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-07 | Tampering / malformed input | `BuildKeeperOutcomeMap` JSON parse | mitigate | Every attribute read is guarded (`TryGetProperty` + `ValueKind == String`, odd-shaped hits `continue`, never throw — the established T-66-09 defensive-read idiom). No injection surface; ES query body is a static raw-string with only validated window timestamps interpolated. |
| T-75-08 | Information Disclosure | keeper-outcome map | accept | Map carries only `correlationId|executionId` GUID keys + a `"drop"`/`"reinject"` label. No secrets/PII. No new external surface. |
</threat_model>

<verification>
- `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0.
- `grep -n "keeperOutcomeByExecution:" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` shows the wired named arg.
- No `IDatabase`/Redis read added to the fixture (ES-read-only preserved).
- Live join surfacing: deferred-automated (Docker) per the checkpoint.
- 0-warning Debug + Release build.
</verification>

<success_criteria>
- Keeper outcomes read from ES, mapped per-(corr,exec), fed into the classifier via `keeperOutcomeByExecution`; ES-read-only preserved; hermetic green + 0-warning; live join verified or deferred-automated.
</success_criteria>

<output>
After completion, create `.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-04-SUMMARY.md`
</output>
