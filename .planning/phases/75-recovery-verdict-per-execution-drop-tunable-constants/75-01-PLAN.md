---
phase: 75-recovery-verdict-per-execution-drop-tunable-constants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs
  - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs
autonomous: true
requirements: [D75-1, D75-2, D75-3, D75-5, D75-8]
user_setup: []

must_haves:
  truths:
    - "The verdict never references any absolute in-flight-loss count — MaxInFlightLoss is gone from the engine."
    - "A started-but-incomplete execution the keeper confirmed as a clean-absent DROP is tolerated (non-binding)."
    - "A started-but-incomplete execution with NO keeper drop evidence and not stalled-before-recovery is a binding FAIL."
    - "The per-(correlationId, executionId) started denominator is preserved (startedRuns = runs.Count)."
    - "The value-chain loop still skips the tolerated-keys set (commit 3028f43 stays green)."
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      provides: "Recoverability classifier + keeperOutcomeByExecution param; MaxInFlightLoss removed"
      contains: "keeperOutcomeByExecution"
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs"
      provides: "Hermetic facts proving D75-2/3/5 (no absolute bound; keeper-drop tolerated; recoverable-lost FAIL)"
      contains: "keeperOutcomeByExecution"
  key_links:
    - from: "PassFailEngine.Analyze B-criterion loop"
      to: "inFlightLossKeys skip-set in value-chain loop"
      via: "tolerated-keys HashSet populated in classifier, consumed at value-chain continue"
      pattern: "inFlightLossKeys\\.Add"
---

<objective>
Rework the resilience-sweep verdict in `PassFailEngine` so it is a pure function of per-`(correlationId, executionId)` recovery, decoupled from every tunable constant. Delete the absolute `MaxInFlightLoss=4` bound (D75-2, the cron-rate coupling), add a keeper-evidence recoverability classifier (D75-3: recoverable-but-lost → binding FAIL; keeper-confirmed clean-absent DROP → tolerated + cause-labeled), preserve the redis-wipe timestamp-heuristic tolerance path (D75-5), and preserve the value-chain in-flight-loss skip-set (D75-8, commit 3028f43). D75-1 (per-`(corr,exec)` denominator) is already satisfied — this plan verifies and locks it.

Purpose: A green sweep must reflect real recovery, never a benign firing rate or time slot.
Output: A reworked pure `PassFailEngine.Analyze` with a new `keeperOutcomeByExecution` contract parameter, an extended `AnalyzerReport`, and hermetic facts proving the new semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-RESEARCH.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase this session — the executor uses these directly, no exploration needed. -->

Current Analyze signature tail (PassFailEngine.cs:101-111) — the last param `int maxInFlightLoss = 4` is DELETED, a new trailing map is ADDED:
```csharp
public AnalyzerReport Analyze(IReadOnlyList<RunTrace> runs, PromCounterSnapshot prom,
                              string scenarioId,
                              IReadOnlyDictionary<string, double>? tripDurationMsByExecution = null,
                              IReadOnlyDictionary<string, double>? tripDurationMsByCorrelation = null,
                              IReadOnlyDictionary<string, int>? seedsByExecution = null,
                              bool expectsKeeperActivity = false,
                              bool mg1Binding = true,
                              DateTimeOffset? recoveryUtc = null,
                              IReadOnlyDictionary<string, DateTimeOffset>? firstHopUtcByExecution = null,
                              IReadOnlyDictionary<string, DateTimeOffset>? lastHopUtcByExecution = null,
                              int maxInFlightLoss = 4)   // ← DELETE this trailing param (D75-2)
```

The current B-criterion classifier loop (PassFailEngine.cs:147-174):
```csharp
foreach (var r in incomplete)
{
    var key = $"{r.CorrelationId}|{r.ExecutionId}";
    var startedAfterRecovery = recoveryUtc is { } rec
        && firstHop.TryGetValue(key, out var fh) && fh > rec;
    var stalledBeforeRecovery = recoveryUtc is { } rec2
        && lastHop.TryGetValue(key, out var lh) && lh < rec2;

    if (stalledBeforeRecovery && !startedAfterRecovery)
    {
        inFlightLoss++;
        inFlightLossKeys.Add(key);
        inFlightLossDetail.Add($"[{key}] in-flight loss: last hop ... (tolerated).");
    }
    else
    {
        bindingMissing++;
        missingDetail.Add($"[{key}] started-but-incomplete and NOT an in-flight-at-wipe loss → binding miss.");
    }
}
var missing = bindingMissing;
var inFlightOverBound = inFlightLoss > maxInFlightLoss;   // ← DELETE
if (inFlightOverBound) missingDetail.Add(...);            // ← DELETE
```

The verdict (PassFailEngine.cs:293):
```csharp
var pass = missing == 0 && !inFlightOverBound && !dupFail && valueChainOk && metricGateOk;
```

The D75-8 skip-set consumer (PassFailEngine.cs:216-219) — MUST keep working:
```csharp
if (inFlightLossKeys.Contains($"{run.CorrelationId}|{run.ExecutionId}"))
{
    continue;   // partial-by-design trace → not value-chain-checked (prevents TEST-03 false FAIL)
}
```

BuildSummary (PassFailEngine.cs:402-421) currently takes `bool inFlightOverBound` and appends `"in-flight loss over bound"` at :409 — remove that param + line.

The join key shape used everywhere: `$"{corr}|{exec}"` (StringComparer.Ordinal).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — repurpose the tunable-bound fact + add D75-3/D75-5 keeper-evidence facts (RED)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs (the three in-flight facts at :222-271 are the exact templates: InFlight_LossBeforeRecovery_IsTolerated_Yields_Pass :223, Incomplete_StartedAfterRecovery_Yields_Fail :241, InFlight_LossExceedsBound_Yields_Fail :256)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs:101-174 (signature + classifier being reworked)
  </read_first>
  <files>tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs</files>
  <behavior>
    - Test A (D75-2, replaces InFlight_LossExceedsBound_Yields_Fail): 6 started-but-incomplete runs, ALL keeper-confirmed clean-absent drops → Verdict.Pass. Proves "N in-flight losses (any N) do not by themselves fail" — no absolute bound.
    - Test B (D75-3 tolerated): one COMPLETE run + one incomplete run whose (corr,exec) maps to "drop" in keeperOutcomeByExecution → InFlightLoss==1, Missing==0, Verdict.Pass.
    - Test C (D75-3 binding): one incomplete run with NO keeper outcome entry and last-hop AFTER recovery (not stalled-before) → Missing==1, Verdict.Fail.
    - Test D (D75-5 redis-wipe path preserved): one incomplete run with NO keeper outcome entry but last-hop BEFORE recovery (stalled) → tolerated, InFlightLoss==1, Missing==0, Verdict.Pass (the timestamp heuristic still tolerates redis-crash TEST-05/07 which have no keeper drop log).
  </behavior>
  <action>
    In `PassFailEngineFacts.cs`:
    1. DELETE the fact `InFlight_LossExceedsBound_Yields_Fail` (:256-271) — it asserts `report.InFlightLoss > 4 ⇒ Fail`, the exact bound D75-2 removes.
    2. ADD `InFlightLoss_ManyCleanDrops_DoNotFail_Yields_Pass` (D75-2): build 6 incomplete runs via `RunTrace.FromLabels($"corr-{i}", $"exec-{i}", Missing4Hops)`; build `var keeperDrops = runs.ToDictionary(r => $"{r.CorrelationId}|{r.ExecutionId}", _ => "drop", StringComparer.Ordinal);`; call `new PassFailEngine().Analyze(runs, ConservingSnapshot(results: 9), "TEST-06", keeperOutcomeByExecution: keeperDrops)`; assert `report.Missing == 0` and `report.Verdict == Verdict.Pass` (any count of clean drops passes).
    3. ADD `KeeperDrop_MarksIncomplete_Tolerated_Yields_Pass` (D75-3): mirror `InFlight_LossBeforeRecovery_IsTolerated_Yields_Pass` (:223) but instead of the timestamp heuristic, drive tolerance via `keeperOutcomeByExecution = new Dictionary<string,string>{ ["corr-2|exec-2"] = "drop" }` (no recoveryUtc/firstHop/lastHop needed). Assert `report.InFlightLoss == 1`, `report.Missing == 0`, `report.Verdict == Verdict.Pass`.
    4. ADD `RecoverableButLost_NoKeeperDrop_AfterRecovery_Yields_Fail` (D75-3): mirror `Incomplete_StartedAfterRecovery_Yields_Fail` (:241) — one incomplete run, last-hop AFTER recovery, EMPTY keeperOutcomeByExecution. Assert `report.Missing == 1`, `report.Verdict == Verdict.Fail`.
    5. ADD `RedisWipe_StalledBeforeRecovery_NoKeeperDrop_Yields_Pass` (D75-5): mirror `InFlight_LossBeforeRecovery_IsTolerated_Yields_Pass` (:223) verbatim (last-hop BEFORE recovery via lastHop/firstHop/recoveryUtc), EMPTY keeperOutcomeByExecution. Assert `report.InFlightLoss == 1`, `report.Missing == 0`, `report.Verdict == Verdict.Pass` — proves the timestamp-heuristic redis-wipe path survives when there is no keeper drop evidence (Pitfall 5).
    All facts use the existing `ConservingSnapshot(results: 9)` helper and `RunTrace.FromLabels(...)` idiom (no ES/Prom/IO). The `keeperOutcomeByExecution` named arg does not exist yet — these MUST fail to compile / fail RED until Task 2 lands.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-class *PassFailEngineFacts</automated>
  </verify>
  <acceptance_criteria>
    - `PassFailEngineFacts.cs` no longer contains `InFlight_LossExceedsBound_Yields_Fail` (grep returns nothing).
    - `PassFailEngineFacts.cs` contains all four new fact names: `InFlightLoss_ManyCleanDrops_DoNotFail_Yields_Pass`, `KeeperDrop_MarksIncomplete_Tolerated_Yields_Pass`, `RecoverableButLost_NoKeeperDrop_AfterRecovery_Yields_Fail`, `RedisWipe_StalledBeforeRecovery_NoKeeperDrop_Yields_Pass`.
    - `PassFailEngineFacts.cs` no longer contains any assertion on `InFlightLoss > 4` or `maxInFlightLoss`.
    - Facts reference `keeperOutcomeByExecution` (the new contract) → RED (compile error is acceptable RED before Task 2).
  </acceptance_criteria>
  <done>The old tunable-bound fact is gone; four new facts encode D75-2/3/5 semantics and fail RED (keeperOutcomeByExecution not yet a parameter).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove MaxInFlightLoss + add keeper-evidence classifier + preserve 3028f43 skip-set (GREEN)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs:101-321 (signature, B-criterion classifier :147-174, value-chain skip :216-219, verdict :293, report build :297-320, BuildSummary :402-421)
    - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs:100-140 (InFlightLoss/InFlightLossDetail required members :118-123 — the pattern for adding a new required field)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs (the facts from Task 1 that must go GREEN)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs (the 3028f43 reproducing fact ToleratedInFlightLoss_PartialTerminalOnly_NotValueChainFailed_Yields_Pass at :261 must stay GREEN — D75-8)
  </read_first>
  <files>tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs, tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs, tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs</files>
  <behavior>
    - The four Task-1 facts turn GREEN.
    - The 3028f43 fact `ToleratedInFlightLoss_PartialTerminalOnly_NotValueChainFailed_Yields_Pass` stays GREEN (D75-8).
    - The two existing timestamp-heuristic facts `InFlight_LossBeforeRecovery_IsTolerated_Yields_Pass` + `Incomplete_StartedAfterRecovery_Yields_Fail` stay GREEN (backward compat: no keeper map ⇒ behaviour unchanged).
  </behavior>
  <action>
    In `PassFailEngine.cs`:
    1. D75-2 — DELETE the trailing parameter `int maxInFlightLoss = 4` (:111). ADD a new trailing parameter `IReadOnlyDictionary<string, string>? keeperOutcomeByExecution = null` keyed `"correlationId|executionId"` → outcome string (`"drop"` for a clean-absent DROP, `"reinject"` for a recovered reinject). Null-coalesce at the top of the classifier region exactly like firstHop/lastHop: `var keeperOutcome = keeperOutcomeByExecution ?? new Dictionary<string, string>(StringComparer.Ordinal);`. Default-null keeps all existing callers compiling (the file's stated convention).
    2. D75-2 — DELETE `var inFlightOverBound = inFlightLoss > maxInFlightLoss;` (:171) and the `if (inFlightOverBound) missingDetail.Add(...)` block (:172-174).
    3. D75-3 — in the B-criterion loop (:147-168), replace the tolerance CONDITION. An incomplete run `key` is TOLERATED iff `keeperOutcome.TryGetValue(key, out var oc) && oc == "drop"` (keeper-confirmed clean-absent DROP → provably-unrecoverable) OR the existing `(stalledBeforeRecovery && !startedAfterRecovery)` (D75-5 redis-wipe timestamp path, kept verbatim). Combine: `var tolerated = (keeperOutcome.TryGetValue(key, out var oc) && oc == "drop") || (stalledBeforeRecovery && !startedAfterRecovery);`. When tolerated: `inFlightLoss++; inFlightLossKeys.Add(key);` and append a detail line stating the cause (`"keeper clean-absent DROP (provably-unrecoverable, tolerated)"` vs the existing timestamp line). Else `bindingMissing++;` with the existing binding-miss detail. D75-8 — `inFlightLossKeys.Add(key)` MUST still run for EVERY tolerated key so the value-chain loop at :216-219 keeps skipping them.
    4. D75-2 — in the verdict (:293) drop the `&& !inFlightOverBound` term: `var pass = missing == 0 && !dupFail && valueChainOk && metricGateOk;`.
    5. D75-2 — in `BuildSummary` (:402-421): remove the `bool inFlightOverBound` parameter and the `if (inFlightOverBound) reasons.Add("in-flight loss over bound");` line (:409); update the call site (:316-318) to drop the `inFlightOverBound` argument.
    6. D75-1 — VERIFY (do not change): `startedRuns = runs.Count` (:117) and one RunTrace per `(corr,exec)` remains the denominator. Add a one-line comment confirming D75-1 is locked.
    In `AnalyzerReport.cs`:
    7. ADD a new `required IReadOnlyList<string> UnrecoverableLossDetail { get; init; }` member (mirror InFlightLossDetail :122-123) surfacing the keeper-classification detail lines; populate it in the report-build block (:297-320) from a list you accumulate in the classifier for the `"drop"`-tolerated keys.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-class *PassFailEngineFacts --filter-class *PassFailEngineValueChainFacts</automated>
  </verify>
  <acceptance_criteria>
    - `PassFailEngine.cs` no longer contains `maxInFlightLoss` or `MaxInFlightLoss` (grep both, case-sensitive, returns nothing).
    - `PassFailEngine.cs` no longer contains `inFlightOverBound` (grep returns nothing).
    - `PassFailEngine.cs` contains parameter `keeperOutcomeByExecution` and the classifier reads `oc == "drop"`.
    - `PassFailEngine.cs` still contains `inFlightLossKeys.Add` inside the tolerated branch AND the skip `if (inFlightLossKeys.Contains(...)) { continue; }` at the value-chain loop (D75-8 preserved).
    - `PassFailEngine.cs` still contains `var startedRuns = runs.Count;` (D75-1 denominator locked).
    - `AnalyzerReport.cs` contains `UnrecoverableLossDetail`.
    - The command exits 0: all four Task-1 facts GREEN, `ToleratedInFlightLoss_PartialTerminalOnly_NotValueChainFailed_Yields_Pass` GREEN, `InFlight_LossBeforeRecovery_IsTolerated_Yields_Pass` + `Incomplete_StartedAfterRecovery_Yields_Fail` still GREEN.
  </acceptance_criteria>
  <done>The absolute bound is gone; the classifier tolerates keeper-confirmed drops + the redis-wipe timestamp path; the 3028f43 skip-set is preserved; the per-(corr,exec) denominator is locked; all engine facts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a pure in-process test-harness arbiter (`PassFailEngine`) + a report record + hermetic facts. No network, no untrusted input, no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Information Disclosure | `AnalyzerReport.UnrecoverableLossDetail` detail strings | accept | Detail carries only `correlationId|executionId` GUIDs + an outcome label — no secrets, no PII, no payload (payload is never in the RunTrace model). |
| T-75-02 | Tampering | verdict logic | accept | Pure function over in-process synthetic/ES-derived facts; no external mutation surface. No new external attack surface introduced. |
</threat_model>

<verification>
- `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0 (full hermetic suite green after the rework).
- `grep -rn "maxInFlightLoss\|MaxInFlightLoss\|inFlightOverBound" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` returns nothing.
- 0-warning Debug + Release build.
</verification>

<success_criteria>
- D75-2 bound removed, D75-3 classifier reads keeper evidence, D75-5 redis-wipe path preserved, D75-8 skip-set preserved, D75-1 denominator locked.
- All `*PassFailEngineFacts` + `*PassFailEngineValueChainFacts` GREEN; full hermetic suite GREEN; 0-warning.
</success_criteria>

<output>
After completion, create `.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-01-SUMMARY.md`
</output>
