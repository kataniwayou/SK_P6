---
phase: 73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f
plan: 03
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs
  - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs
autonomous: true
requirements: [SPEC-R5, SPEC-R6]
must_haves:
  truths:
    - "PassFailEngine.AllLabels is the 10-label set including Step_G"
    - "The convergent terminal Step_G has expected multiplicity 2 per (corr, exec): exactly 2 is legitimate fan-in (NOT a duplicate-fail); 1 (missing arrival) or 3+ (same-entryId redelivery) still fails closed; any OTHER label appearing twice still fails closed"
    - "A value-chain assertion: each label's surfaced value equals seed + hop-count; both Step_G arrivals at the expected terminal value (seed+6); a wrong value fails the verdict"
    - "RunTrace surfaces per-step values (filled from synthetic values in facts, from ES attributes in the live fixture)"
    - "AnalyzerReport carries per-(corr,exec) + per-correlationId trip duration (ms) and value-chain evidence fields"
    - "A hermetic engine fixture yields Pass on a correct incrementing chain through Step_G x2 + terminal blob present, and Fail on wrong value / missing label / Step_G count != 2 / same-entryId redelivery"
    - "No metric-counter assertions are added; the Prom corroboration structure stays intact/non-binding"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      provides: "10-label set, Step_G x2 multiplicity, value-chain verdict folding"
      contains: "Step_G"
    - path: "tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs"
      provides: "convergent-label-aware duplicate handling + per-step value surfacing"
      contains: "FromLabels"
    - path: "tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs"
      provides: "trip-duration + value-chain report fields"
      contains: "TripDurationMs"
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs"
      provides: "synthetic-trace Pass/Fail facts proving the new logic once"
      contains: "Step_G"
  key_links:
    - from: "RunTrace.FromLabels (extended signature)"
      to: "PassFailEngine.Analyze + the hermetic facts + the live fixture (Plan 04)"
      via: "the per-step value map + convergent-multiplicity flag computed once in FromLabels"
      pattern: "FromLabels"
    - from: "PassFailEngine value-chain verdict"
      to: "AnalyzerReport value-chain + trip-duration fields"
      via: "the new AnalyzerReport initializer in Analyze"
      pattern: "ValueChain|TripDuration"
---

<objective>
Extend the pure auditor model — `RunTrace`, `PassFailEngine`, `AnalyzerReport` — to (a) recognize the 10-label completeness set including `Step_G`, (b) treat the convergent terminal `Step_G` as expected-multiplicity-2 (legitimate fan-in, NOT a duplicate-fail) while keeping every other duplicate fail-closed and failing a `Step_G` count != 2, (c) assert the deterministic value chain (`seed + hop-count`, both `Step_G` arrivals at `seed+6`, terminal blob present), and (d) carry per-exec + per-corr trip duration. Prove the new logic ONCE with synthetic-trace engine facts.

Purpose: This is the pure, IO-free heart of the auditor. Both the live RealStack fixture (Plan 04) and these hermetic facts build `RunTrace` via `FromLabels`, so the value/multiplicity/value-chain logic is proven once here and shared — a green RealStack run is then trustworthy, never vacuous.

Output: extended `RunTrace.cs` / `PassFailEngine.cs` / `AnalyzerReport.cs` + a new `PassFailEngineValueChainFacts.cs`, all GREEN at 0-warning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-SPEC.md
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-CONTEXT.md
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-PATTERNS.md
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-01-SUMMARY.md

<interfaces>
<!-- Current shapes to extend. Extracted from the three pure-model files. -->

PassFailEngine today (PassFailEngine.cs:50-54, :99, :119-120, :191):
- AllLabels = 9-label HashSet (no Step_G); LabelsPerRun = 9.
- COMPLETE: `var complete = runs.Where(r => r.DistinctLabels.SetEquals(AllLabels)).ToList();`
- DUPLICATE: `var duplicates = runs.Where(r => r.HasAnyDuplicateLabel).ToList(); var dupFail = duplicates.Count > 0;`
- VERDICT: `var pass = missing == 0 && !dupFail;`
- The `new AnalyzerReport { … }` initializer (:195-214) sets every required member.

RunTrace today (RunTrace.cs:38-51, :59-84):
- `Labels`, `DistinctLabels`, `HasAnyDuplicateLabel` (= `labels.Count != distinct.Count`), `DuplicateLabels`.
- `FromLabels(string correlationId, string executionId, IReadOnlyList<string> labels)` computes distinct + the dupes loop.

AnalyzerReport today: a `required`-member serializable record (AnalyzerReport.cs:63-151); enums get `[JsonConverter(typeof(JsonStringEnumConverter))]`; every required member set in `Analyze`.

The Plan-01 value contract: Mode-1 log fields are `Received` (inbound) and `Produced` (accumulated) — confirm the exact names in 73-01-SUMMARY.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RunTrace — convergent-multiplicity awareness + per-step value surfacing</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs (full file — the record shape :26-51 + FromLabels :59-84)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-CONTEXT.md (D-10, D-11)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-PATTERNS.md (the RunTrace.cs EXTEND-IN-PLACE section)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-01-SUMMARY.md (the Received/Produced field names)
  </read_first>
  <behavior>
    - Step_G appearing exactly twice in Labels (with all other labels once) → NOT flagged as an illegitimate duplicate (the convergent fan-in is legitimate)
    - Step_G appearing once → flagged (missing arrival)
    - Step_G appearing 3+ times → flagged (same-entryId redelivery)
    - Any NON-convergent label (e.g. Step_C) appearing twice → still flagged (fail-closed preserved)
    - FromLabels surfaces a per-label value map (label → value) carried onto the RunTrace
  </behavior>
  <action>
    Extend `RunTrace`:

    1. CONVERGENT-MULTIPLICITY AWARENESS. Add a static convergent-label spec mirroring the `AllLabels` style: the convergent label is `Step_G` with expected multiplicity `2`. Replace the blanket `HasAnyDuplicateLabel = labels.Count != distinct.Count` semantics for the BINDING signal with convergent-aware logic in the existing `seen`/`dupes` loop:
       - Count occurrences per label.
       - A label is an ILLEGITIMATE duplicate iff: (it is NOT the convergent label `Step_G` AND its count > 1) OR (it IS `Step_G` AND its count != 2).
       - Add a new required member `public required bool HasIllegitimateDuplicate { get; init; }` set from that rule. KEEP the existing `HasAnyDuplicateLabel` member for report-shape stability (it still means "any raw repeat"), but the engine (Task 2) will read `HasIllegitimateDuplicate` for the binding verdict.
       - Keep `DuplicateLabels` populated with the illegitimately-colliding labels (so a `Step_G` x2 is NOT listed, but a `Step_C` x2 or a `Step_G` x3 IS).

    2. PER-STEP VALUE SURFACING (D-11). Add `public required IReadOnlyDictionary<string, int> Values { get; init; }` (label → surfaced value). Extend `FromLabels`'s signature to accept the values alongside labels — `FromLabels(string correlationId, string executionId, IReadOnlyList<string> labels, IReadOnlyDictionary<string, int>? values = null)`. When `values` is null (legacy callers), default to an empty `Dictionary<string,int>` so existing call sites keep compiling. For `Step_G` (two arrivals, same value), the map holds the single shared terminal value.

    Preserve the required-member record shape, the `StringComparer.Ordinal` comparisons, and the deterministic `OrderBy(Ordinal)` sort of `DuplicateLabels`.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && grep -n "HasIllegitimateDuplicate" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs && grep -n "Step_G" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs && grep -n "IReadOnlyDictionary<string, int> Values" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "HasIllegitimateDuplicate" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs` present (the convergent-aware flag)
    - `grep -n "Step_G" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs` present (the convergent label spec, expected multiplicity 2)
    - `grep -n "IReadOnlyDictionary<string, int> Values" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs` present (per-step value map)
    - `grep -nE "FromLabels\(.*values" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs` shows the extended signature accepting values
    - `grep -n "HasAnyDuplicateLabel" tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs` still present (legacy member kept for report-shape stability)
  </acceptance_criteria>
  <done>`RunTrace` flags `Step_G` x2 as legitimate but `Step_G` x!=2 / any other duplicate as illegitimate via `HasIllegitimateDuplicate`, and surfaces a per-label `Values` map through the extended `FromLabels`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend PassFailEngine (10-label set, value-chain verdict) + AnalyzerReport fields</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (full file — AllLabels :50-54, complete :99, duplicate :119-120, pass :191, the AnalyzerReport initializer :195-214)
    - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs (full file — the required-member record idiom :63-151)
    - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs (Task 1 output — HasIllegitimateDuplicate + Values)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-CONTEXT.md (D-10, D-11, D-12)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-PATTERNS.md (the PassFailEngine + AnalyzerReport EXTEND-IN-PLACE sections)
  </read_first>
  <behavior>
    - A run with all 10 distinct labels incl. Step_G x2 + a correct value chain (seed+1 .. seed+6) → Verdict.Pass
    - A run missing Step_G → Missing > 0 → Verdict.Fail
    - A run whose Step_G appears once or 3+ times → HasIllegitimateDuplicate → Verdict.Fail
    - A run with a wrong mid-chain value (e.g. C != seed+2) → value-chain fail → Verdict.Fail
    - The Prom corroboration math stays non-binding (never flips a green ES verdict)
  </behavior>
  <action>
    In `PassFailEngine.cs`:

    1. AllLabels (:50-51) → add `"Step_G"`, making the 10-label set:
    `{ "Step_A", "Step_B", "Step_C", "Step_D1", "Step_E1", "Step_F1", "Step_D2", "Step_E2", "Step_F2", "Step_G" }`.
    COMPLETE (:99) stays `r.DistinctLabels.SetEquals(AllLabels)` — now requires all 10 distinct labels incl. Step_G.

    2. LabelsPerRun (:54): metrics are DEFERRED (SPEC out-of-scope; no metric-counter assertions). Because `Step_G` runs x2, dispatch math is no longer a clean x-labels multiple — but the value-chain/COMPLETE verdict must NOT depend on Prom. KEEP `LabelsPerRun = 9` as the dispatch-count basis for the inert Prom corroboration math (the corroboration block stays intact and non-binding). Add an XML-doc note that `Step_G`'s x2 fan-in is intentionally NOT folded into `LabelsPerRun` because the binding verdict is value-chain + completeness, not Prom.

    3. DUPLICATE verdict (:119-120): change `runs.Where(r => r.HasAnyDuplicateLabel)` to `runs.Where(r => r.HasIllegitimateDuplicate)` so a legitimate `Step_G` x2 does NOT trip the fail-closed duplicate rule, while every other duplicate and a `Step_G` count != 2 still fails.

    4. VALUE-CHAIN ASSERTION (NEW, D-11). Add a private method that, given a `RunTrace` and the seed for its execution, checks each label's `Values[label]` equals `seed + hop-count` per the chain (B=seed+1, C=seed+2, D=seed+3, E=seed+4, F=seed+5, G=seed+6) and that both `Step_G` arrivals are at `seed+6` (the shared terminal value). The seed per run is recoverable from the chain itself (`Values["Step_B"] - 1`) or passed in. Compute `valueChainOk` across all runs (true when EVERY started run's chain matches). Fold into the verdict: extend the :191 line to `var pass = missing == 0 && !dupFail && valueChainOk;`. Collect per-run expected-vs-actual evidence strings for the report.

    In `AnalyzerReport.cs` — ADD `required` members (copy the existing XML-doc + `public required <T> <Name> { get; init; }` idiom):
    - `public required bool ValueChainOk { get; init; }`
    - `public required IReadOnlyList<string> ValueChainDetail { get; init; }` (per-exec expected-vs-actual evidence)
    - `public required IReadOnlyDictionary<string, double> TripDurationMsByExecution { get; init; }` (keyed by `corr|exec`)
    - `public required IReadOnlyDictionary<string, double> TripDurationMsByCorrelation { get; init; }`

    Thread ALL new required members through the `new AnalyzerReport { … }` initializer in `Analyze` (:195-214) — every required member MUST be set. The engine does not itself read ES timestamps; add an `Analyze` overload or optional parameters accepting the trip-duration maps + per-execution seeds (default to empty maps so existing callers compile — the live fixture in Plan 04 passes the real maps; the engine facts here pass synthetic ones). Do NOT add any metric-counter assertion; keep the entire Prom corroboration block intact and non-binding.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && grep -n '"Step_G"' tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs && grep -n "HasIllegitimateDuplicate" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs && grep -niE "valueChainOk" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs && grep -n "TripDurationMs" tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs && dotnet build tests/BaseApi.Tests -c Debug -warnaserror</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"Step_G"' tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` present (10-label set)
    - `grep -n "HasIllegitimateDuplicate" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` present (reads the convergent-aware flag, not raw HasAnyDuplicateLabel)
    - `grep -niE "valueChainOk" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` present AND `grep -n "&& valueChainOk" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` shows it folded into the pass expression
    - `grep -n "TripDurationMsByExecution" tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs` and `grep -n "TripDurationMsByCorrelation" tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs` and `grep -n "ValueChainOk" tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs` all present
    - `grep -niE "Metric|Counter assertion|Assert.*Counter" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` adds no new metric-counter assertion (the Prom corroboration block is unchanged in structure)
    - `dotnet build tests/BaseApi.Tests -c Debug -warnaserror` exits 0
  </acceptance_criteria>
  <done>The engine requires all 10 labels incl. Step_G, treats Step_G x2 as legitimate via HasIllegitimateDuplicate, folds a value-chain check into the verdict, and AnalyzerReport carries value-chain + trip-duration fields wired through Analyze; test project builds 0-warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the new logic with synthetic-trace engine facts (Pass + every Fail mode)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (Task 2 output)
    - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs (Task 1 output — FromLabels with values)
    - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs (Task 2 output)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-SPEC.md (Requirement 5 Acceptance — Pass/Fail matrix)
    - any existing PassFailEngine fact file in tests/BaseApi.Tests/Observability/Analysis/ (for the synthetic PromCounterSnapshot construction idiom)
  </read_first>
  <behavior>
    - Pass: 2 executions (seeds 100, 200), each with all 10 labels, Step_G x2 at seed+6, correct chain → Verdict.Pass
    - Fail (missing label): drop Step_E1 from one run → Missing > 0 → Verdict.Fail
    - Fail (Step_G count 1): one G arrival → HasIllegitimateDuplicate → Verdict.Fail
    - Fail (Step_G count 3 = same-entryId redelivery): three G arrivals → Verdict.Fail
    - Fail (wrong value): set Values["Step_C"] = 999 → value-chain fail → Verdict.Fail
    - A non-convergent duplicate (Step_C x2) still → Verdict.Fail
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs` (namespace `BaseApi.Tests.Observability.Analysis`, xUnit `[Fact]`s). Build synthetic `RunTrace`s via the extended `FromLabels` (labels + a per-label `Values` map) and a synthetic `PromCounterSnapshot` (reuse the existing facts' construction idiom). For each behavior case above, build the input runs, call `PassFailEngine.Analyze(...)`, and assert the `AnalyzerReport.Verdict`:

    - GREEN case: two executions seeded 100 and 200. exec_a Values = {B:101, C:102, D1:103, E1:104, F1:105, D2:103, E2:104, F2:105, G:106}, labels include `Step_A`..`Step_F2` once + `Step_G` TWICE; exec_b the +100 variant (B:201 … G:206). Assert `Verdict.Pass`, `ValueChainOk == true`, and that the legitimate `Step_G` x2 did NOT set `HasIllegitimateDuplicate`.
    - Each FAIL case: mutate one run and assert `Verdict.Fail` with the specific driver (missing label / Step_G count != 2 / wrong value / non-convergent duplicate).

    Also assert the report carries the trip-duration maps (even if synthetic/empty) and `ValueChainDetail` is populated on a value-chain failure. Run GREEN and confirm 0-warning Debug + Release.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && dotnet build tests/BaseApi.Tests -c Release -warnaserror && dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~PassFailEngineValueChainFacts" -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -niE "Verdict.Pass" tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs` present (the green case) AND `grep -niE "Verdict.Fail" tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs` present (the fail cases)
    - `grep -nE "106|206" tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs` present (terminal value oracle)
    - `grep -niE "Step_G" tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs` shows both the x2 legitimate case and a x!=2 fail case
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~PassFailEngineValueChainFacts"` reports all facts passed (0 failed)
    - `dotnet build tests/BaseApi.Tests -c Release -warnaserror` exits 0
  </acceptance_criteria>
  <done>Synthetic engine facts prove Pass on a correct incrementing chain through Step_G x2 and Fail on each of: missing label, Step_G count != 2, wrong value, non-convergent duplicate — GREEN at 0-warning Debug + Release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ES log attributes → auditor verdict | the auditor consumes `attributes.*` (StepLabel, Received/Produced values) parsed from Elasticsearch; a malformed/forged value must fail closed, never silently pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-05 | Tampering | the value-chain verdict | mitigate | A mid-chain value mutation is exactly what the new value-chain assertion catches: `Values[label] != seed + hop-count` folds into `pass = … && valueChainOk`, failing the verdict. The synthetic Fail facts (Task 3) prove the detection. |
| T-73-06 | Spoofing / redelivery | the Step_G x2 multiplicity exception | mitigate | The convergent exception is narrow: ONLY `Step_G` count == 2 is legitimate. A same-`entryId` redelivery (Step_G count 3+) and any non-convergent duplicate still fail closed via `HasIllegitimateDuplicate` — proven by the Step_G-x3 and Step_C-x2 Fail facts. ASVS L1: fail-closed default preserved. |
</threat_model>

<verification>
- `PassFailEngine.AllLabels` is the 10-label set incl. `Step_G`; COMPLETE requires all 10
- The duplicate verdict reads `HasIllegitimateDuplicate` (Step_G x2 legitimate; x!=2 + other duplicates fail)
- A value-chain check folds into the pass expression
- `AnalyzerReport` carries `ValueChainOk` / `ValueChainDetail` + `TripDurationMsBy{Execution,Correlation}`
- Synthetic facts prove Pass + every Fail mode; GREEN at 0-warning Debug + Release
</verification>

<success_criteria>
- 10-label completeness set incl. `Step_G` with expected x2 convergence multiplicity
- Value-chain assertion (seed + hop-count, both `Step_G` at seed+6) folded into the binding verdict
- `RunTrace` surfaces per-step values; both fixture + facts build via the shared `FromLabels`
- `AnalyzerReport` carries trip-duration + value-chain evidence
- No metric-counter assertions; Prom corroboration stays non-binding
- Hermetic engine facts GREEN + 0-warning Debug + Release
</success_criteria>

<output>
After completion, create `.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-03-SUMMARY.md`. MUST record the final extended `RunTrace.FromLabels` signature and the new `PassFailEngine.Analyze` parameter shape so Plan 04's live fixture calls them correctly.
</output>
