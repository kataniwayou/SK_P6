---
phase: 73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f
plan: 04
type: execute
wave: 3
depends_on: [73-01, 73-03]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs
  - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs
  - scripts/phase-73-sweep.ps1
autonomous: true
requirements: [SPEC-R1, SPEC-R3, SPEC-R5, SPEC-R6, SPEC-R7]
must_haves:
  truths:
    - "The seeder builds the G-extended DAG: 10 steps, 10 edges (adds Step_F1->Step_G and Step_F2->Step_G), Step_G the lone zero-outgoing sink (F1/F2 no longer sinks), 10 assignments"
    - "Every assignment payload carries number = 1 (replaces the A=1..F2=9 map) so each Mode-1 hop increments by exactly +1"
    - "The seeder self-verify counts are updated to 10 steps / 10 edges / 10 assignments / Step_G sink"
    - "The live fixture reads the per-step value ES attribute (Received/Produced) mirroring the defensive TryReadSum, threads it into the extended RunTrace.FromLabels, and retains Step_G x2"
    - "The live fixture computes per-(corr,exec) + per-correlationId trip duration (ms) from the ES @timestamp min->max span, reusing the hits it already pulls (no new ES query)"
    - "A scripts/phase-73-sweep.ps1 exists and is syntactically runnable: seeds the G-extended DAG, drives the live round-trip, invokes the auditor; no fail-fast; rolls up a phase-73-summary.json"
    - "The live Docker run is deferred-automated (the sweep script is operator-runnable, NOT a phase gate)"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs"
      provides: "G-extended seeder data (10/10/10) + number=1 payloads"
      contains: "Step_G"
    - path: "tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs"
      provides: "ES value-attribute read + trip-duration computation + extended-engine feed"
      contains: "Received"
    - path: "scripts/phase-73-sweep.ps1"
      provides: "operator-runnable live sweep (seed -> round-trip -> auditor)"
      contains: "phase-73-sweep"
  key_links:
    - from: "AnalyzerE2ETests.BuildRunTraces"
      to: "RunTrace.FromLabels (extended, Plan 03) + PassFailEngine.Analyze (extended, Plan 03)"
      via: "reads attributes.Received/Produced, builds the per-label Values map, passes trip-duration maps"
      pattern: "FromLabels|Received"
    - from: "FanOutSeederE2ETests Step_G + number=1"
      to: "the live DAG the auditor scores"
      via: "REST seed of the G-extended graph"
      pattern: "Step_G"
---

<objective>
Land the live half: (a) extend the in-test seeder data so the seeded DAG includes the shared convergent terminal `Step_G` (10 steps / 10 edges / 10 assignments, `G` the lone sink) and every payload carries `number = 1` (the uniform `+1` per hop), (b) extend the RealStack auditor fixture to read the per-step value ES attribute, thread it into the Plan-03 extended `RunTrace.FromLabels`, retain `Step_G` x2, and compute per-exec + per-corr trip duration from the ES `@timestamp` span, and (c) add a `scripts/phase-73-sweep.ps1` operator-runnable live sweep. The live Docker run is deferred-automated.

Purpose: This wires the proven pure model (Plan 03) to real ES data and seeds the real graph that produces it. It is the integration layer; it depends on the Plan-01 field names and the Plan-03 extended signatures.

Output: extended `FanOutSeederE2ETests.cs` + `AnalyzerE2ETests.cs` + a new `scripts/phase-73-sweep.ps1`, building 0-warning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-SPEC.md
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-CONTEXT.md
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-PATTERNS.md
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-01-SUMMARY.md
@.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-03-SUMMARY.md

<interfaces>
<!-- Current seeder + fixture shapes to extend. -->

Seeder NodeNumbers today (FanOutSeederE2ETests.cs:64-75) — replace per-node numbers with uniform 1, add Step_G:
```csharp
private static readonly IReadOnlyDictionary<string, int> NodeNumbers = new Dictionary<string, int>
{ ["Step_A"]=1, ["Step_B"]=2, ["Step_C"]=3, ["Step_D1"]=4, ["Step_E1"]=5, ["Step_F1"]=6, ["Step_D2"]=7, ["Step_E2"]=8, ["Step_F2"]=9 };
```

Seeder ExpectedEdges today (FanOutSeederE2ETests.cs:78-88) — 8 edges, add the two G edges:
```
A->B, B->C, C->D1, C->D2, D1->E1, D2->E2, E1->F1, E2->F2
```

Seeder reverse-topo create (FanOutSeederE2ETests.cs:298-307) — sinks created first; G must be created FIRST, then F1/F2 point to it:
```csharp
node["Step_F1"] = await SeedStepWithNextAsync(client, procId, "F1", null, ct);  // <- becomes next = [Step_G]
node["Step_F2"] = await SeedStepWithNextAsync(client, procId, "F2", null, ct);  // <- becomes next = [Step_G]
```

Self-verify counts today (FanOutSeederE2ETests.cs:126-139): steps=9, edges(step_next_steps)=8, assignments=9, workflow_assignments=9; sink check is over {Step_F1, Step_F2}.

Fixture TryReadSum (AnalyzerE2ETests.cs:317-325) — the defensive numeric-attribute reader template to mirror for Received/Produced:
```csharp
private static bool TryReadSum(JsonElement attrs, out int sum) { /* TryGetProperty + Number/String parse, never throws */ }
```

Fixture BuildRunTraces (AnalyzerE2ETests.cs:275-310) — already retains duplicate labels per instance (so Step_G lands twice); reads CorrelationId/ExecutionId/StepLabel; calls `_ = TryReadSum(attrs, out _)` informationally at :296.

Fixture window hits (AnalyzerE2ETests.cs:213-228, sorted asc on WindowTimestampFieldPath) — already time-sorted; first-hit/last-hit gives the span.

Sweep analog: scripts/phase-68-sweep.ps1 (whole file) — header doc-comment, `param([string[]]$ScenarioIds=…)`, `$ErrorActionPreference='Stop'`, `Set-StrictMode`, `$repoRoot`, `Push-Location/finally Pop-Location`, `Write-Phase` helper, child `& pwsh -File …; $code=$LASTEXITCODE` + exit-class switch (NO fail-fast), analyzer-report roll-up + summary JSON.

Plan-01 field names: `attributes.Received` (inbound) + `attributes.Produced` (accumulated) — confirm in 73-01-SUMMARY.md.
Plan-03 extended signatures: `RunTrace.FromLabels(corr, exec, labels, values)` + `PassFailEngine.Analyze(... trip-duration maps + seeds)` — confirm in 73-03-SUMMARY.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the seeder — add Step_G (10/10/10), number=1 payloads, update self-verify</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs (full relevant ranges: NodeNumbers :64-75, ExpectedEdges :78-88, the self-verify counts :122-216, SeedFanOutAsync reverse-topo :277-330)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-CONTEXT.md (D-04, D-05)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-PATTERNS.md (the FanOutSeederE2ETests EXTEND-IN-PLACE section)
  </read_first>
  <action>
    Edit `tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` (in-test data only — NOT a src/ change):

    1. NodeNumbers (:64-75): set EVERY value to `1` (replaces A=1..F2=9) and ADD `["Step_G"] = 1`. Result: a 10-entry dictionary, every number `1`. With Mode-1 `accumulated = incoming + baseNumber` and `baseNumber = 1`, each hop is `+1` (D-02 chain).

    2. ExpectedEdges (:78-88): ADD the two convergence edges so the set is 10 edges:
    `A->B, B->C, C->D1, C->D2, D1->E1, D2->E2, E1->F1, E2->F2, Step_F1->Step_G, Step_F2->Step_G`.
    (Use the exact `"Step_F1->Step_G"` / `"Step_F2->Step_G"` strings matching the existing `{from}->{to}` format.)

    3. LabelRegex (:94-95): extend the alternation to include `G`: `^Step_(A|B|C|D1|E1|F1|D2|E2|F2|G)$`.

    4. SeedFanOutAsync reverse-topo (:298-307): create `Step_G` FIRST (sinks-first / reverse-topological, since `next_step` FKs are OnDelete(Restrict)), then point F1 and F2 at it:
    ```csharp
    node["Step_G"]  = await SeedStepWithNextAsync(client, procId, "G", null, ct);   // the shared terminal, no successor
    node["Step_F1"] = await SeedStepWithNextAsync(client, procId, "F1", new List<Guid> { node["Step_G"] }, ct);
    node["Step_F2"] = await SeedStepWithNextAsync(client, procId, "F2", new List<Guid> { node["Step_G"] }, ct);
    // E1/E2/D1/D2/C/B/A unchanged below
    ```
    The assignment loop `foreach (var (label, number) in NodeNumbers)` now creates 10 assignments (Step_G included) automatically.

    5. Self-verify counts (:122-216): update so a clean seed asserts:
    - steps == 10 (was 9)
    - step_next_steps edges == 10 (was 8)
    - assignments == 10 (was 9)
    - workflow_assignments == 10 (was 9)
    - `Assert.Equal(10, edges.Count);` and `Assert.Equal(ExpectedEdges, edgeNodePairs);` (10-edge set)
    - the sink zero-outgoing check: change the sink set from `{Step_F1, Step_F2}` to `{Step_G}` ONLY (F1/F2 now have 1 outgoing edge each → G; G is the lone zero-outgoing sink). Assert F1 and F2 each have exactly 1 outgoing edge (to G), and G has 0.
    - `Assert.Equal(10, seenLabels.Count);` and `Assert.Equal(AllNodeLabels, seenLabels);` (AllNodeLabels is derived from NodeNumbers.Keys, so it auto-includes Step_G).
    - The per-payload `Assert.Equal(NodeNumbers[label], number)` now asserts `number == 1` for every label (the fixed mapping is now uniform 1).
    - The 1/9/9/8 idempotency comment (:114) should read 1/10/10/10.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && grep -n "Step_F1->Step_G" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs && grep -n "Step_F2->Step_G" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs && grep -n '\["Step_G"\] = 1' tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs && grep -n "node\[\"Step_G\"\]  = await SeedStepWithNextAsync" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs && dotnet build tests/BaseApi.Tests -c Debug -warnaserror</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Step_F1->Step_G" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` and `grep -n "Step_F2->Step_G" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` both present (the 2 new edges)
    - `grep -cE '\] = 1,' tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` plus the final `["Step_G"] = 1` shows all 10 NodeNumbers values are `1` (no value 2..9 remains in NodeNumbers — `grep -n '\] = [2-9]' tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` returns 0 within the NodeNumbers block)
    - `grep -n "node\[\"Step_G\"\]" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` shows Step_G created with `null` next (the terminal) and F1/F2 created with `new List<Guid> { node["Step_G"] }`
    - `grep -nE "Assert.Equal\(10, " tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` shows the updated 10-counts (steps / edges / assignments / workflow_assignments / edges.Count / seenLabels)
    - `grep -n "Step_(A|B|C|D1|E1|F1|D2|E2|F2|G)" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` shows the regex includes G
    - the sink check targets `Step_G` (not F1/F2) — `grep -n "Step_G" tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` in the sink zero-outgoing block
    - `dotnet build tests/BaseApi.Tests -c Debug -warnaserror` exits 0
  </acceptance_criteria>
  <done>The seeder builds a 10-step / 10-edge / 10-assignment DAG with Step_G the lone sink reachable from both F1 and F2, every payload `number = 1`, and the self-verify counts assert 10/10/10 + G-sink; test project builds 0-warning.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend the RealStack fixture — read the value attribute, feed the extended engine, compute trip duration</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs (full relevant ranges: BuildStepSearchBody :213-228, PollHitsToStableAsync :244-266, BuildRunTraces :275-310, TryReadSum :317-325)
    - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs + PassFailEngine.cs (Plan-03 extended signatures)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-01-SUMMARY.md (the Received/Produced ES attribute names)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-03-SUMMARY.md (the FromLabels + Analyze parameter shapes)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-CONTEXT.md (D-11, D-12)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-PATTERNS.md (the AnalyzerE2ETests EXTEND-IN-PLACE section)
  </read_first>
  <action>
    Edit `tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` (RealStack fixture — extend in place, preserve the window-pin/drain/poll-to-stable structure):

    1. VALUE ATTRIBUTE READER. Add a sibling to `TryReadSum` that reads the Plan-01 per-step value attribute. The Mode-1 log emits `attributes.Produced` (the accumulated output) and `attributes.Received` (the inbound). Read `Produced` as the per-step surfaced value (it is the value AT that label):
    ```csharp
    private static bool TryReadProduced(JsonElement attrs, out int produced)
    {
        produced = 0;
        if (!attrs.TryGetProperty("Produced", out var el)) return false;
        if (el.ValueKind == JsonValueKind.Number && el.TryGetInt32(out produced)) return true;
        if (el.ValueKind == JsonValueKind.String && int.TryParse(el.GetString(), out produced)) return true;
        return false;
    }
    ```
    (Confirm the exact attribute name against 73-01-SUMMARY.md; if Mode-2's seed log uses a different field for Step_A's value, handle Step_A's value via its seed log or omit Step_A from the value-chain oracle since the chain is asserted B..G.)

    2. THREAD VALUES INTO RunTrace. In `BuildRunTraces` (:275-310): change the `_ = TryReadSum(attrs, out _)` informational call (:296) to capture the per-step value via `TryReadProduced`, accumulate a per-(corr,exec) `Dictionary<string,int>` label→value alongside the existing label list, and pass it to the extended `RunTrace.FromLabels(corr, exec, labels, values)`. Step_G already lands twice in the label list (the grouping retains duplicates) — leave that; the Plan-03 engine interprets the x2.

    3. TRIP DURATION (D-12). Reuse the already-pulled, time-sorted hits (no new ES query). For each `(corr, exec)` group, compute the `@timestamp` span = (last hit timestamp − first hit timestamp) in ms; build `Dictionary<string,double> tripByExecution` (key `corr|exec`). For each `corr`, compute the min→max span across all its hits → `tripByCorrelation`. Read the timestamp from the same `WindowTimestampFieldPath`/`@timestamp` field the search sorts on. Pass these maps into the extended `PassFailEngine.Analyze(...)` (per the Plan-03 signature) so they land in the report.

    4. Pass the per-execution seeds (100 for one exec, 200 for the other — recoverable as `Values["Step_B"] - 1`, or let the engine derive) into `Analyze` so the value-chain check has its oracle. The fixture's terminal assertion stays `Assert.True(report.Verdict == Verdict.Pass, report.HumanSummary)` (:202).

    Do NOT touch the Prom path; do NOT add any metric-counter assertion. Preserve drain/poll-to-stable/window-pin.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && grep -n "TryReadProduced" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs && grep -nE "FromLabels\(.*values|, values\)" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs && grep -niE "tripBy|TripDuration|@timestamp|span" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs && dotnet build tests/BaseApi.Tests -c Debug -warnaserror && dotnet build tests/BaseApi.Tests -c Release -warnaserror</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TryReadProduced" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` present (the value-attribute reader, mirroring TryReadSum)
    - `grep -nE "FromLabels\(" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` shows the call passing the per-label values map (4-arg form)
    - `grep -niE "tripBy|TripDuration" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` present (trip-duration maps computed) AND `grep -nE "@timestamp|WindowTimestampFieldPath" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` shows it reuses the existing timestamp field (no new ES query)
    - `grep -niE "Metric|Counter assertion|Assert.*Counter" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` adds no new metric-counter assertion
    - `dotnet build tests/BaseApi.Tests -c Debug -warnaserror` and `dotnet build tests/BaseApi.Tests -c Release -warnaserror` both exit 0
  </acceptance_criteria>
  <done>The RealStack fixture reads `attributes.Produced` into a per-label values map, feeds the extended `RunTrace.FromLabels` + `PassFailEngine.Analyze`, retains Step_G x2, and computes per-exec + per-corr trip duration from the existing ES `@timestamp` hits; test project builds 0-warning Debug + Release.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add the operator-runnable phase-73 live sweep script (deferred-automated)</name>
  <read_first>
    - scripts/phase-68-sweep.ps1 (whole file — the structural analog)
    - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs:20-24 (the RealStack analyzer fixture filter contract: Category=RealStack&FullyQualifiedName~Analyzer)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-CONTEXT.md (D-13)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-SPEC.md (Requirement 7 — deferred-automated, not a phase gate)
    - .planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-PATTERNS.md (the NEW scripts/phase-73-sweep.ps1 section)
  </read_first>
  <action>
    Create `scripts/phase-73-sweep.ps1` by copying the `phase-68-sweep.ps1` STRUCTURE verbatim and re-targeting the stages (D-13). Reuse:
    - The header doc-comment block + `param([string[]]$ScenarioIds = @('TEST-01'))` shape (a single happy-path scenario default for this phase — there is no 7-scenario fault matrix here; the goal is the value-chain + fan-in proof on the happy path).
    - `$ErrorActionPreference='Stop'`, `Set-StrictMode -Version Latest`, `$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot '..')).Path`, `Push-Location $repoRoot` / `finally { Pop-Location }`.
    - The `Write-Phase` helper re-prefixed `[phase-73-sweep]`.
    - The child-process invocation idiom `& pwsh -File … ; $code = $LASTEXITCODE` + the exit-class `switch` — NO fail-fast.
    - The analyzer-report discovery + roll-up table + a `phase-73-summary.json` artifact write (under `analyzer-reports/`).
    - The anti-pattern guards from the analog header: NEVER auto-retries, NEVER re-scores Missing/Duplicates/value-chain (only reads + tabulates the analyzer's already-computed values).

    Re-target the STAGES:
    1. Seed the G-extended DAG: `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~FanOutSeeder"`.
    2. Drive the live round-trip (the cron-fired DAG over the live stack) — document this stage as deferred-automated where Docker is unavailable.
    3. Invoke the auditor: `dotnet test tests/BaseApi.Tests --filter "Category=RealStack&FullyQualifiedName~Analyzer"`.

    Mark in the header NOTES that the live Docker run is DEFERRED-AUTOMATED (the sandbox has no Docker) — the script must be syntactically runnable (passes `pwsh -NoProfile -Command "Get-Command -Syntax"` / parses) but is NOT a phase gate. The phase closes on hermetic GREEN (Plans 02/03) + 0-warning, not on this script's live run.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && grep -n "phase-73-sweep" scripts/phase-73-sweep.ps1 && grep -n "FanOutSeeder" scripts/phase-73-sweep.ps1 && grep -nE "Category=RealStack&FullyQualifiedName~Analyzer" scripts/phase-73-sweep.ps1 && pwsh -NoProfile -Command "\$ErrorActionPreference='Stop'; \$null = [System.Management.Automation.Language.Parser]::ParseFile('scripts/phase-73-sweep.ps1', [ref]\$null, [ref]\$null); 'PARSE_OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "phase-73-sweep" scripts/phase-73-sweep.ps1` present (the re-prefixed Write-Phase helper)
    - `grep -n "FanOutSeeder" scripts/phase-73-sweep.ps1` present (the seed stage) AND `grep -n "Category=RealStack&FullyQualifiedName~Analyzer" scripts/phase-73-sweep.ps1` present (the auditor stage)
    - `grep -niE "deferred|no Docker|NOT a phase gate" scripts/phase-73-sweep.ps1` present (documents the deferred-automated posture)
    - `grep -niE "NEVER auto-retr|NEVER re-score" scripts/phase-73-sweep.ps1` present (anti-pattern guards copied)
    - the PowerShell parse check prints `PARSE_OK` (syntactically runnable)
  </acceptance_criteria>
  <done>`scripts/phase-73-sweep.ps1` exists, parses cleanly, seeds the G-extended DAG → drives the round-trip → invokes the RealStack auditor with no fail-fast and a roll-up JSON, and documents the live Docker run as deferred-automated (not a phase gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live ES/Prom store → RealStack auditor fixture | the fixture parses `attributes.*` and `@timestamp` from real Elasticsearch hits; malformed JSON must be dropped defensively, never thrown on |
| sweep script → child test processes | the script invokes `dotnet test` / `pwsh -File` children with hardcoded filters; no untrusted input is interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-07 | Denial of Service / robustness | the new `TryReadProduced` ES-attribute reader | mitigate | Mirror the existing defensive `TryReadSum` pattern (TryGetProperty + tolerant Number/String parse, never throws) so an odd-shaped or missing attribute is dropped, not fatal — preserving the fixture's T-66-09 defensive-parse posture. |
| T-73-08 | Information Disclosure | the sweep script + seeded payloads | accept | Payloads carry only the synthetic integer `1` and `Step_*` labels; the script interpolates no secrets and feeds only hardcoded test filters. No sensitive surface. ASVS L1: no credential/PII handling introduced. |
| T-73-09 | Tampering | the seeded DAG shape | mitigate | The seeder self-verify (10/10/10 + G-sink + uniform number=1) fails closed if the seeded graph drifts from the locked shape — the live auditor's value chain then has a trustworthy substrate. |
</threat_model>

<verification>
- Seeder builds 10 steps / 10 edges / 10 assignments; `Step_G` the lone sink reachable from F1 + F2; every payload `number = 1`; self-verify asserts 10/10/10
- RealStack fixture reads `attributes.Produced`, threads values into the extended `FromLabels`, retains Step_G x2, computes per-exec + per-corr trip duration from existing `@timestamp` hits
- `scripts/phase-73-sweep.ps1` exists, parses, seeds → round-trips → audits with no fail-fast + roll-up JSON; live Docker run deferred-automated
- No metric-counter assertions; 0-warning Debug + Release
</verification>

<success_criteria>
- The seeded DAG includes the shared convergent terminal `Step_G` (10/10/10, G-sink) with uniform `number = 1`
- The live auditor asserts the value chain through `Step_G` x2 + per-exec/per-corr trip duration from ES timestamps
- An operator-runnable `phase-73-sweep.ps1` exists; the live Docker run is deferred-automated (not a phase gate)
- 0-warning Debug + Release build
</success_criteria>

<output>
After completion, create `.planning/phases/73-verify-and-instrument-end-to-end-l2-data-delivery-across-a-f/73-04-SUMMARY.md`.
</output>
