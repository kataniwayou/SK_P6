---
phase: 86-console-webapi-health-liveness-refactor
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs
  - tests/BaseApi.Tests/Processor/IdentitySchemaReadyHealthCheckTests.cs
  - tests/BaseApi.Tests/Processor/StartupOrchestratorResilienceFacts.cs
  - tests/BaseApi.Tests/Features/Liveness/LivenessWatchdogHealthCheckTests.cs
  - tests/BaseApi.Tests/Keeper/Health/KeeperLivenessWatchdogTests.cs
autonomous: true
requirements: [HLTH-02, HLTH-04]

must_haves:
  truths:
    - "A processor identity+schema readiness skeleton exists reading IProcessorContext.IsHealthy."
    - "A RED test pins the identity+schema readiness mapping (IsHealthy false→Unhealthy, true→Healthy)."
    - "A test locks the HLTH-02 invariant: ProcessorStartupOrchestrator catches a broker-down fault, logs, retries, and never throws out."
    - "The two watchdog-only test files that would fail to compile after the Wave-2 type deletions are retired, and the solution still builds."
  artifacts:
    - path: "src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs"
      provides: "ProcessorIdentitySchemaReadyHealthCheck(IServiceProvider outer) : IHealthCheck skeleton"
      contains: "class ProcessorIdentitySchemaReadyHealthCheck"
    - path: "tests/BaseApi.Tests/Processor/StartupOrchestratorResilienceFacts.cs"
      provides: "HLTH-02 broker-down-never-throws regression lock"
      contains: "Trait(\"Phase\", \"86\")"
  key_links:
    - from: "src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs"
      to: "IProcessorContext.IsHealthy"
      via: "outer.GetRequiredService<IProcessorContext>() at check time"
      pattern: "IProcessorContext"
---

<objective>
Wave-0 RED scaffold for the processor-side readiness check and the HLTH-02 resilience lock, plus
retirement of the two watchdog-only test files that reference types being deleted in Wave 2.

Purpose: the processor identity+schema readiness check is BaseProcessor-specific (reads IProcessorContext),
so it needs its own skeleton + RED test. HLTH-02 requires a test that LOCKS the already-compliant broad-catch
in ProcessorStartupOrchestrator (no code change — behavior preservation). The two old watchdog test files
(LivenessWatchdogHealthCheckTests, KeeperLivenessWatchdogTests) reference LivenessWatchdogHealthCheck /
KeeperLivenessWatchdogHealthCheck / IKeeperLivenessState which are DELETED in Wave 2 — deleting the tests now
is safe (removing references never breaks compile) and keeps every wave green.
Output: 1 processor skeleton + 2 RED tests + 2 retired test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-RESEARCH.md
@.planning/phases/86-console-webapi-health-liveness-refactor/86-VALIDATION.md

<interfaces>
From src/BaseProcessor.Core/Identity/IProcessorContext.cs:
  IProcessorContext.IsHealthy => Volatile.Read(ref _isHealthy)==1  // never throws; == "identity + all required definitions resolved"
  (Do NOT read Id/InputDefinition/etc for readiness — WR-03 stale-null hazard. IsHealthy is the one synchronized signal.)

From src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs (L141-147, L195-201):
  broad `catch (Exception ex) when (ex is not OperationCanceledException)` → log + backoff, never rethrows.

Test analogs: tests/BaseApi.Tests/Processor/FakeProcessorContext.cs (stub IProcessorContext),
tests/BaseApi.Tests/Processor/LivenessResilienceFacts.cs (resilience-fact structure to mirror).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeleton ProcessorIdentitySchemaReadyHealthCheck + its RED test</name>
  <read_first>
    - src/BaseProcessor.Core/Identity/IProcessorContext.cs (IsHealthy semantics)
    - src/BaseConsole.Core/Health/BusReadyHealthCheck.cs (OUTER-resolution never-throw idiom)
    - tests/BaseApi.Tests/Processor/FakeProcessorContext.cs (stub to reuse)
    - 86-RESEARCH.md §Pattern 4
  </read_first>
  <files>
    src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs,
    tests/BaseApi.Tests/Processor/IdentitySchemaReadyHealthCheckTests.cs
  </files>
  <action>
    Declare `public sealed class ProcessorIdentitySchemaReadyHealthCheck : IHealthCheck` in namespace
    BaseProcessor.Core.Liveness with ctor `(IServiceProvider outer)`. Skeleton body returns
    `Task.FromResult(HealthCheckResult.Unhealthy("NOT IMPLEMENTED"))` (RED). XML-doc the locked contract:
    resolve `IProcessorContext` from `outer` at check time and map `IsHealthy` → Healthy "identity+schema resolved"
    / false → Unhealthy "identity+schema not yet resolved"; never read Id/definition props directly.
    Then write `tests/BaseApi.Tests/Processor/IdentitySchemaReadyHealthCheckTests.cs`, `[Trait("Phase","86")]`,
    stubbing IProcessorContext via a provider: IsHealthy=false → Unhealthy; IsHealthy=true → Healthy. RED now.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*IdentitySchemaReadyHealthCheckTests*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ProcessorIdentitySchemaReadyHealthCheck(IServiceProvider outer)" src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs` matches.
    - The new class runs and reports FAILING assertions (RED), not compile errors.
    - Test tagged `[Trait("Phase","86")]`, not RealStack.
  </acceptance_criteria>
  <done>Skeleton + RED identity/schema readiness test exist; project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the HLTH-02 broker-down-never-throws invariant on ProcessorStartupOrchestrator</name>
  <read_first>
    - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs (the broad catch at L141-147 / L195-201)
    - tests/BaseApi.Tests/Processor/LivenessResilienceFacts.cs (existing resilience-fact structure)
    - 86-RESEARCH.md §Component map (ProcessorStartupOrchestrator) + spec §7
  </read_first>
  <files>
    tests/BaseApi.Tests/Processor/StartupOrchestratorResilienceFacts.cs
  </files>
  <action>
    Write a Phase-86 hermetic fact that drives ProcessorStartupOrchestrator's identity/schema resolution against
    a dependency stub that throws a broker-unreachable-style exception (e.g. a MassTransit/Redis-style exception,
    NOT OperationCanceledException) and asserts: the orchestrator's ExecuteAsync/StartAsync does NOT propagate the
    exception (it is caught, logged, and the loop backs off/retries). Reuse the harness/stub shape from
    LivenessResilienceFacts. This test should PASS immediately (the code is already HLTH-02-compliant) — it is a
    regression LOCK, not a RED-then-GREEN scaffold. If wiring the orchestrator directly is impractical hermetically,
    assert at minimum that the exact broad-catch guard (`when (ex is not OperationCanceledException)`) is exercised
    for the broker-down exception type via the smallest faithful harness.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*StartupOrchestratorResilienceFacts*"</automated>
  </verify>
  <acceptance_criteria>
    - The fact PASSES (green) — locks the existing invariant.
    - The test injects a non-OperationCanceledException fault and asserts no throw escapes the orchestrator loop.
    - Tagged `[Trait("Phase","86")]`, not RealStack.
  </acceptance_criteria>
  <done>HLTH-02 invariant is locked by a green Phase-86 regression test.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the two watchdog-only test files being deleted in Wave 2</name>
  <read_first>
    - tests/BaseApi.Tests/Features/Liveness/LivenessWatchdogHealthCheckTests.cs
    - tests/BaseApi.Tests/Keeper/Health/KeeperLivenessWatchdogTests.cs
    - 86-RESEARCH.md §Wave 0 Gaps (retire/port list) + Pitfall 6 (keep IProcessorLivenessState — do NOT touch it)
  </read_first>
  <files>
    tests/BaseApi.Tests/Features/Liveness/LivenessWatchdogHealthCheckTests.cs,
    tests/BaseApi.Tests/Keeper/Health/KeeperLivenessWatchdogTests.cs
  </files>
  <action>
    DELETE both test files. They exclusively exercise the retired types (`LivenessWatchdogHealthCheck`,
    `KeeperLivenessWatchdogHealthCheck`, `IKeeperLivenessState`) whose deletion in Wave 2 would otherwise break
    compile. Their behavioral coverage is superseded by the new LoopLivenessHealthCheckTests (86-01). Do NOT touch
    ProcessorLivenessStateFacts, LivenessHeartbeatFacts, BitHealthLoopTests, or anything referencing
    IProcessorLivenessState (kept — it backs the separate L2 gate). Confirm the remaining suite still builds.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - Both files no longer exist (`test ! -f` for each).
    - Full hermetic suite builds and runs; only the new Phase-86 RED tests (86-01 + this plan's Task 1) fail; nothing else newly fails.
    - `grep -rl "LivenessWatchdogHealthCheck\|KeeperLivenessWatchdogHealthCheck\|IKeeperLivenessState" tests/` returns only files that will be updated in Wave 2 (BitHealthLoopTests) — no orphaned refs in retired areas.
  </acceptance_criteria>
  <done>The two watchdog-only test files are removed; suite still builds; IProcessorLivenessState untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| infra client (Redis/RMQ) → ProcessorStartupOrchestrator loop | untrusted failure must be caught, never crash |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-02 | Denial of Service (self-inflicted crash) | ProcessorStartupOrchestrator identity/schema loop | mitigate | StartupOrchestratorResilienceFacts locks broker-down → caught+logged+retried, never throws out |
| T-86-03 | Information Disclosure | identity/schema readiness message | mitigate | Static-literal messages ("identity+schema resolved" / "... not yet resolved"); no infra detail |
</threat_model>

<verification>
Full hermetic build+run: only the new RED tests fail; the resilience lock passes; retired files gone.
</verification>

<success_criteria>
Processor identity/schema readiness skeleton + RED test exist; HLTH-02 invariant locked green; the two
watchdog-only test files retired without breaking compile.
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-02-SUMMARY.md` when done.
</output>
