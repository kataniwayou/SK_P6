---
phase: 86-console-webapi-health-liveness-refactor
plan: 03
type: execute
wave: 1
depends_on: ["86-01"]
files_modified:
  - src/BaseConsole.Core/Health/LivenessHeartbeat.cs
  - src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs
  - src/BaseConsole.Core/DependencyInjection/ConsoleLivenessServiceCollectionExtensions.cs
autonomous: true
requirements: [HLTH-01, HLTH-03, HLTH-06]

must_haves:
  truths:
    - "ILivenessHeartbeat.Beat() stamps the current TimeProvider instant in-memory (zero I/O); Current reads it; never-beaten reads null."
    - "LoopLivenessHealthCheck reports Healthy while now < Current + k*interval (k=3), Unhealthy 'liveness loop stale' at/after that boundary, Unhealthy 'liveness loop not started' when Current is null — resolving ILivenessHeartbeat + TimeProvider from the OUTER provider at check time."
    - "AddConsoleLivenessWatchdog(intervalSeconds) is the opt-in seam: it registers the ILivenessHeartbeat singleton and a 'live'-tagged HealthCheckDescriptor for LoopLivenessHealthCheck; services that never call it keep self-only liveness."
  artifacts:
    - path: "src/BaseConsole.Core/Health/LivenessHeartbeat.cs"
      provides: "Interlocked long-ticks ILivenessHeartbeat impl over an injected TimeProvider"
      contains: "Interlocked"
    - path: "src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs"
      provides: "k=3 staleness check, timestamp-only, OUTER-resolved at check time"
      contains: "liveness loop stale"
    - path: "src/BaseConsole.Core/DependencyInjection/ConsoleLivenessServiceCollectionExtensions.cs"
      provides: "AddConsoleLivenessWatchdog opt-in registration seam"
      contains: "HealthCheckDescriptor"
  key_links:
    - from: "src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs"
      to: "ILivenessHeartbeat.Current + TimeProvider"
      via: "outer.GetRequiredService at check time"
      pattern: "GetRequiredService<(ILivenessHeartbeat|TimeProvider)>"
    - from: "ConsoleLivenessServiceCollectionExtensions.AddConsoleLivenessWatchdog"
      to: "EmbeddedHealthEndpointService live-tag fold-in"
      via: "HealthCheckDescriptor Name='liveness-watchdog' Tags=['live']"
      pattern: "new\\[\\] \\{ \"live\" \\}"
---

<objective>
GREEN the shared liveness-watchdog primitive: fill the LivenessHeartbeat + LoopLivenessHealthCheck
bodies (turning 86-01's RED tests green) and add the AddConsoleLivenessWatchdog opt-in registration
seam that keeper + processor call to surface the "live" watchdog on the embedded /health/live via the
existing generic HealthCheckDescriptor fold-in. Orchestrator never calls it, so it stays self-only.

Purpose: one shared primitive replaces the two divergent per-service watchdogs (HLTH-01/06); the
staleness margin widens to k=3 so one slow tick under the new unconditional-beat model never false-trips.
Output: two implemented primitive bodies + one registration extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-RESEARCH.md
@docs/superpowers/specs/2026-07-26-console-health-liveness-refactor-design.md

<interfaces>
From src/BaseConsole.Core/Health/HealthCheckDescriptor.cs:
  record HealthCheckDescriptor(string Name, string[] Tags, Func<IServiceProvider,IHealthCheck> Factory)

From src/BaseConsole.Core/Health/EmbeddedHealthEndpointService.cs (L84-88): folds every outer
  HealthCheckDescriptor into the inner listener via hc.AddCheck(d.Name, d.Factory(_outer), tags: d.Tags);
  /health/live Predicate = Tags.Contains("live"). No change needed here — a "live" descriptor is auto-picked-up.

From src/Keeper/Health/KeeperLivenessState.cs: the exact Interlocked long-ticks idiom (0 = never stamped).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LivenessHeartbeat (Interlocked long-ticks over injected TimeProvider)</name>
  <read_first>
    - src/BaseConsole.Core/Health/LivenessHeartbeat.cs (skeleton from 86-01)
    - src/Keeper/Health/KeeperLivenessState.cs (idiom to mirror)
    - tests/BaseApi.Tests/Console/LivenessHeartbeatTests.cs (the RED test to satisfy)
  </read_first>
  <files>src/BaseConsole.Core/Health/LivenessHeartbeat.cs</files>
  <action>
    Implement `Beat()` to stamp `_clock.GetUtcNow().UtcDateTime.Ticks` via `Interlocked.Exchange(ref _ticks, ...)`,
    and `Current` to `Interlocked.Read(ref _ticks)` mapped to `null` when 0 else `new DateTime(t, DateTimeKind.Utc)`.
    Store the ctor's `TimeProvider clock`. Pure in-memory, zero I/O, thread-safe across the loop-writer and the
    probe-reader threads. 0 remains the "never beaten" sentinel.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*LivenessHeartbeatTests*"</automated>
  </verify>
  <acceptance_criteria>
    - LivenessHeartbeatTests passes (never-beaten → null; Beat at T → Current==T; re-Beat updates).
    - `grep -n "Interlocked" src/BaseConsole.Core/Health/LivenessHeartbeat.cs` matches.
  </acceptance_criteria>
  <done>LivenessHeartbeat is implemented and its RED test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LoopLivenessHealthCheck k=3 staleness math</name>
  <read_first>
    - src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs (skeleton)
    - src/BaseProcessor.Core/Liveness/LivenessWatchdogHealthCheck.cs (L44-72 staleness template; generalize ×2 → ×k)
    - tests/BaseApi.Tests/Console/LoopLivenessHealthCheckTests.cs (RED test — boundary cases pin the math)
  </read_first>
  <files>src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs</files>
  <action>
    Resolve `ILivenessHeartbeat` and `TimeProvider` from `_outer` at check time (never captured at registration).
    Read `Current`; null → `HealthCheckResult.Unhealthy("liveness loop not started")`. Compute
    `now = clock.GetUtcNow().UtcDateTime`; if `now >= Current + TimeSpan.FromSeconds(intervalSeconds * k)` →
    `Unhealthy("liveness loop stale")` (strict `>=` so the exact boundary is stale, matching the retired check's
    boundary discipline). Otherwise `Healthy("live")`. Timestamp-only — no per-schema summary Data (the old
    processor summary is not part of the new contract). All messages STATIC literals (no secrets).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*LoopLivenessHealthCheckTests*"</automated>
  </verify>
  <acceptance_criteria>
    - LoopLivenessHealthCheckTests passes: fresh→Healthy "live"; stale→Unhealthy "liveness loop stale"; null→Unhealthy "liveness loop not started"; exact boundary (now == Current + 3*interval) → Unhealthy; one tick before → Healthy.
    - No message/data contains "Password=", "abortConnect", or "   at ".
  </acceptance_criteria>
  <done>LoopLivenessHealthCheck implements the k=3 math; all its RED boundary tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the AddConsoleLivenessWatchdog opt-in registration seam</name>
  <read_first>
    - src/BaseConsole.Core/DependencyInjection/ConsoleHealthServiceCollectionExtensions.cs (registration-home pattern)
    - src/BaseConsole.Core/Health/EmbeddedHealthEndpointService.cs (how a "live" descriptor is folded in)
    - src/Keeper/Program.cs L45-54 (the OLD keeper watchdog descriptor this opt-in replaces)
    - 86-RESEARCH.md §Pattern 1 (interval baked at registration)
  </read_first>
  <files>src/BaseConsole.Core/DependencyInjection/ConsoleLivenessServiceCollectionExtensions.cs</files>
  <action>
    Create a new static class `ConsoleLivenessServiceCollectionExtensions` in namespace
    BaseConsole.Core.DependencyInjection exposing
    `AddConsoleLivenessWatchdog(this IServiceCollection services, int intervalSeconds, int k = 3)`. It must:
    (1) `TryAddSingleton(TimeProvider.System)`; (2) register `ILivenessHeartbeat` as a singleton
    (`LivenessHeartbeat` resolving the TimeProvider); (3) `AddSingleton(new HealthCheckDescriptor("liveness-watchdog",
    new[] { "live" }, outer => new LoopLivenessHealthCheck(outer, intervalSeconds, k)))` so the embedded listener
    folds it onto /health/live automatically. Interval is baked at registration (keeper=5, processor=10). This is a
    NEW file — do NOT edit ConsoleHealthServiceCollectionExtensions (avoids a Wave-1 file conflict with 86-04).
    Document that a service which never calls this keeps self-only liveness (orchestrator).
  </action>
  <verify>
    <automated>dotnet build src/BaseConsole.Core/BaseConsole.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AddConsoleLivenessWatchdog" src/BaseConsole.Core/DependencyInjection/ConsoleLivenessServiceCollectionExtensions.cs` matches.
    - `grep -n "liveness-watchdog" src/BaseConsole.Core/DependencyInjection/ConsoleLivenessServiceCollectionExtensions.cs` matches and the descriptor Tags include "live".
    - BaseConsole.Core builds.
  </acceptance_criteria>
  <done>The opt-in seam exists; keeper/processor can register the shared watchdog with one call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| /health/live body → kubelet | a stale-loop signal triggers a real restart; must not react to infra |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Information Disclosure | LoopLivenessHealthCheck message | mitigate | Static-literal "live"/"liveness loop stale"/"liveness loop not started"; test asserts no secret tokens |
| T-86-05 | Tampering (false restart) | staleness boundary math | mitigate | k=3 margin + strict-boundary tests (exact boundary + one-tick-before) lock the trip point |
</threat_model>

<verification>
LivenessHeartbeatTests + LoopLivenessHealthCheckTests green; BaseConsole.Core builds; the opt-in seam
registers ILivenessHeartbeat + a "live" descriptor.
</verification>

<success_criteria>
The shared primitive is implemented and green; the opt-in seam exists; orchestrator stays self-only by
simply not calling it (HLTH-01/06).
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-03-SUMMARY.md` when done.
</output>
