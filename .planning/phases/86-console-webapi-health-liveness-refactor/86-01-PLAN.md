---
phase: 86-console-webapi-health-liveness-refactor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/BaseConsole.Core/Health/ILivenessHeartbeat.cs
  - src/BaseConsole.Core/Health/LivenessHeartbeat.cs
  - src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs
  - src/BaseConsole.Core/Health/RedisReadyHealthCheck.cs
  - src/BaseConsole.Core/Health/LatchedReadinessHealthCheck.cs
  - tests/BaseApi.Tests/Console/LoopLivenessHealthCheckTests.cs
  - tests/BaseApi.Tests/Console/LivenessHeartbeatTests.cs
  - tests/BaseApi.Tests/Console/RedisReadyHealthCheckTests.cs
  - tests/BaseApi.Tests/Console/LatchedReadinessHealthCheckTests.cs
autonomous: true
requirements: [HLTH-01, HLTH-04, HLTH-05, HLTH-06, HLTH-08]

must_haves:
  truths:
    - "The four new BaseConsole.Core health primitives exist as compiling types with the exact ctor/member signatures the implementation waves and tests bind against."
    - "Four new Phase-86 hermetic test classes exist, run, and FAIL (RED) against the not-yet-implemented skeletons."
    - "Pre-existing non-watchdog hermetic tests still compile and pass."
  artifacts:
    - path: "src/BaseConsole.Core/Health/ILivenessHeartbeat.cs"
      provides: "ILivenessHeartbeat contract: void Beat(); DateTime? Current { get; }"
      contains: "interface ILivenessHeartbeat"
    - path: "src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs"
      provides: "LoopLivenessHealthCheck(IServiceProvider outer, int intervalSeconds, int k = 3) : IHealthCheck skeleton"
      contains: "class LoopLivenessHealthCheck"
    - path: "src/BaseConsole.Core/Health/RedisReadyHealthCheck.cs"
      provides: "RedisReadyHealthCheck(IServiceProvider outer) : IHealthCheck skeleton"
      contains: "class RedisReadyHealthCheck"
    - path: "src/BaseConsole.Core/Health/LatchedReadinessHealthCheck.cs"
      provides: "LatchedReadinessHealthCheck(IHealthCheck inner, int failureThreshold) : IHealthCheck skeleton"
      contains: "class LatchedReadinessHealthCheck"
    - path: "tests/BaseApi.Tests/Console/LoopLivenessHealthCheckTests.cs"
      provides: "RED unit proof of the k=3 staleness math"
      contains: "Trait(\"Phase\", \"86\")"
  key_links:
    - from: "tests/BaseApi.Tests/Console/*Tests.cs"
      to: "src/BaseConsole.Core/Health/*.cs"
      via: "direct type reference (BaseApi.Tests references BaseConsole.Core)"
      pattern: "new (LoopLivenessHealthCheck|RedisReadyHealthCheck|LatchedReadinessHealthCheck|LivenessHeartbeat)"
---

<objective>
Wave-0 RED scaffold for the shared BaseConsole.Core health primitives. Create the four new
primitive types as COMPILING SKELETONS (correct signatures, deliberately-wrong / NotImplemented
bodies) and the four failing Phase-86 hermetic test classes that pin their locked behavior. This
plan establishes the RED half of the TDD cycle: the solution builds, the new tests run and FAIL,
and the implementation waves (86-03, 86-04) turn them GREEN by filling the bodies only.

Purpose: C# couples compilation across the whole test project, so the referenced production types
must exist before any test can run. Skeletons let the new tests fail on BEHAVIOR (not compile
errors), which is the honest RED state.
Output: 4 skeleton type files + 4 RED test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-RESEARCH.md
@.planning/phases/86-console-webapi-health-liveness-refactor/86-VALIDATION.md
@docs/superpowers/specs/2026-07-26-console-health-liveness-refactor-design.md

<interfaces>
Existing analog contracts the skeletons and tests mirror (already in the codebase):

From src/Keeper/Health/KeeperLivenessState.cs — the Interlocked long-ticks holder idiom (0 = never stamped):
  Update(DateTime utcNow) => Interlocked.Exchange(ref _ticks, utcNow.Ticks);
  DateTime? Current => Interlocked.Read == 0 ? null : new DateTime(t, Utc);

From src/BaseConsole.Core/Health/BusReadyHealthCheck.cs — the "resolve from OUTER provider at check time" idiom and the null-dep → Unhealthy discipline (never throws).

From src/BaseConsole.Core/Health/HealthCheckDescriptor.cs — record(string Name, string[] Tags, Func<IServiceProvider,IHealthCheck> Factory).

Test analog to copy structurally: tests/BaseApi.Tests/Features/Liveness/LivenessWatchdogHealthCheckTests.cs
(FakeTimeProvider + Substitute.For<IServiceProvider>() bridging; the AssertSummaryDataPresent no-secret guard block).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeleton the liveness primitive (ILivenessHeartbeat, LivenessHeartbeat, LoopLivenessHealthCheck)</name>
  <read_first>
    - src/Keeper/Health/KeeperLivenessState.cs (the long-ticks holder to mirror)
    - src/BaseProcessor.Core/Liveness/LivenessWatchdogHealthCheck.cs (the staleness-math template being generalized, ×2 → ×k)
    - src/BaseConsole.Core/Health/BusReadyHealthCheck.cs (the check-time OUTER-resolution idiom)
    - 86-RESEARCH.md §Pattern 1 (exact primitive shape + k=3 decision)
  </read_first>
  <files>
    src/BaseConsole.Core/Health/ILivenessHeartbeat.cs,
    src/BaseConsole.Core/Health/LivenessHeartbeat.cs,
    src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs
  </files>
  <action>
    In namespace BaseConsole.Core.Health, declare the three types with FINAL signatures but placeholder bodies:
    (1) `public interface ILivenessHeartbeat` with `void Beat();` and `DateTime? Current { get; }`.
    (2) `public sealed class LivenessHeartbeat : ILivenessHeartbeat` with a primary/explicit ctor taking a
    `TimeProvider clock`; skeleton bodies that compile but are unimplemented — `Beat()` throws
    `NotImplementedException`, `Current` throws `NotImplementedException`. Do NOT implement the Interlocked
    logic yet (that is 86-03).
    (3) `public sealed class LoopLivenessHealthCheck : IHealthCheck` with ctor
    `(IServiceProvider outer, int intervalSeconds, int k = 3)`, storing the args; `CheckHealthAsync` returns
    `Task.FromResult(HealthCheckResult.Unhealthy("NOT IMPLEMENTED"))` so every behavioral assertion in the test
    fails RED. Keep messages STATIC literals (no interpolated secrets) — the info-disclosure guard is locked.
    The check must (once implemented in 86-03) resolve ILivenessHeartbeat + TimeProvider from `outer` at check
    time; document that intent in an XML-doc comment so the implementer does not capture at registration.
  </action>
  <verify>
    <automated>dotnet build src/BaseConsole.Core/BaseConsole.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - BaseConsole.Core compiles with the three new types present.
    - `grep -n "interface ILivenessHeartbeat" src/BaseConsole.Core/Health/ILivenessHeartbeat.cs` matches.
    - `grep -n "LoopLivenessHealthCheck(IServiceProvider outer, int intervalSeconds, int k = 3)" src/BaseConsole.Core/Health/LoopLivenessHealthCheck.cs` matches (exact ctor signature).
    - No fenced-code / interpolated connection-string literal appears in any HealthCheckResult message.
  </acceptance_criteria>
  <done>The three types exist with locked signatures and RED bodies; BaseConsole.Core builds.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton the readiness checks (RedisReadyHealthCheck, LatchedReadinessHealthCheck)</name>
  <read_first>
    - src/BaseConsole.Core/Health/BusReadyHealthCheck.cs (never-throw, null-dep → Unhealthy pattern)
    - src/BaseConsole.Core/DependencyInjection/ConsoleRedisServiceCollectionExtensions.cs (IConnectionMultiplexer singleton the Redis check will resolve)
    - 86-RESEARCH.md §Pattern 3 (Redis bounded ping) + §Pattern 5 (latch decorator, Pitfall 4 sticky-after-recovery)
  </read_first>
  <files>
    src/BaseConsole.Core/Health/RedisReadyHealthCheck.cs,
    src/BaseConsole.Core/Health/LatchedReadinessHealthCheck.cs
  </files>
  <action>
    In namespace BaseConsole.Core.Health, declare:
    (1) `public sealed class RedisReadyHealthCheck : IHealthCheck` with ctor `(IServiceProvider outer)`;
    skeleton `CheckHealthAsync` returns `Task.FromResult(HealthCheckResult.Unhealthy("NOT IMPLEMENTED"))`.
    XML-doc the intended contract: resolve `IConnectionMultiplexer` from `outer` at check time, null → Unhealthy
    "Redis not started", bounded PING, any exception → Unhealthy "Redis unreachable", NEVER throw, and NEVER put
    the connection string / exception detail in the message (info-disclosure guard T-18-08).
    (2) `public sealed class LatchedReadinessHealthCheck : IHealthCheck` with ctor
    `(IHealthCheck inner, int failureThreshold)`; skeleton passes through to `inner.CheckHealthAsync` (no latch
    yet) so the sticky-after-recovery test fails RED. XML-doc the intended contract: count consecutive Unhealthy
    evaluations; at `failureThreshold` flip a per-process sticky latch that returns Unhealthy forever until
    restart; a single non-Unhealthy result resets the counter.
  </action>
  <verify>
    <automated>dotnet build src/BaseConsole.Core/BaseConsole.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RedisReadyHealthCheck(IServiceProvider outer)" src/BaseConsole.Core/Health/RedisReadyHealthCheck.cs` matches.
    - `grep -n "LatchedReadinessHealthCheck(IHealthCheck inner, int failureThreshold)" src/BaseConsole.Core/Health/LatchedReadinessHealthCheck.cs` matches.
    - BaseConsole.Core compiles.
  </acceptance_criteria>
  <done>Both readiness check types exist with locked signatures and RED bodies; BaseConsole.Core builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the four RED Phase-86 hermetic test classes</name>
  <read_first>
    - tests/BaseApi.Tests/Features/Liveness/LivenessWatchdogHealthCheckTests.cs (structure to copy: FakeTimeProvider, stub IServiceProvider, no-secret guard)
    - tests/BaseApi.Tests/Console/ConsoleBusReadyHealthCheckTests.cs (null-provider readiness path)
    - 86-VALIDATION.md Per-Requirement Verification Map (exact behaviors per class)
  </read_first>
  <files>
    tests/BaseApi.Tests/Console/LoopLivenessHealthCheckTests.cs,
    tests/BaseApi.Tests/Console/LivenessHeartbeatTests.cs,
    tests/BaseApi.Tests/Console/RedisReadyHealthCheckTests.cs,
    tests/BaseApi.Tests/Console/LatchedReadinessHealthCheckTests.cs
  </files>
  <behavior>
    - LoopLivenessHealthCheckTests (HLTH-01/06): Current==null → Unhealthy "liveness loop not started";
      fresh beat (now-1s) → Healthy "live"; stale (now - k*interval - 1s, k=3) → Unhealthy "liveness loop stale";
      EXACT boundary now == Current + 3*interval → Unhealthy stale; one tick before boundary → Healthy.
      Use intervalSeconds=10, k=3, FakeTimeProvider, stub IServiceProvider returning ILivenessHeartbeat + TimeProvider.
      Assert no message/data leaks "Password=", "abortConnect", "   at ".
    - LivenessHeartbeatTests (HLTH-01): a fresh LivenessHeartbeat has Current==null (never-beaten sentinel);
      after Beat() with FakeTimeProvider at T, Current == T; advancing the clock and Beat() again updates Current.
    - RedisReadyHealthCheckTests (HLTH-04/08): null IConnectionMultiplexer from provider → Unhealthy "Redis not started";
      a substituted multiplexer whose GetDatabase().PingAsync() completes → Healthy; a multiplexer that throws
      → Unhealthy "Redis unreachable" and the call does NOT throw out; result message/data contains no secret token.
    - LatchedReadinessHealthCheckTests (HLTH-05): with failureThreshold=3, three consecutive Unhealthy inner results
      → third+ returns Unhealthy and STAYS Unhealthy even after the inner then returns Healthy (sticky, Pitfall 4);
      a single Unhealthy followed by Healthy does NOT latch (counter resets).
  </behavior>
  <action>
    Create the four xUnit.v3 test classes in namespace BaseApi.Tests.Console, each tagged `[Trait("Phase", "86")]`
    and NOT `[Trait("Category","RealStack")]`. Build providers via `Substitute.For<IServiceProvider>()` stubbing
    `GetService(typeof(...))` (GetRequiredService routes through GetService), exactly as the watchdog test analog does.
    For the latch test, drive the inner check with an `IHealthCheck` stub whose queued results are scripted via
    NSubstitute. These MUST fail now (skeletons return NOT IMPLEMENTED / pass-through) — that is the intended RED.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*LoopLivenessHealthCheckTests*"</automated>
  </verify>
  <acceptance_criteria>
    - The test project BUILDS (skeletons satisfy every reference).
    - Running each new class via `--filter-class "*<Class>*"` REPORTS FAILING assertions (RED) — not compile errors, not passes.
    - `grep -rc "Trait(\"Phase\", \"86\")" tests/BaseApi.Tests/Console/LoopLivenessHealthCheckTests.cs tests/BaseApi.Tests/Console/LivenessHeartbeatTests.cs tests/BaseApi.Tests/Console/RedisReadyHealthCheckTests.cs tests/BaseApi.Tests/Console/LatchedReadinessHealthCheckTests.cs` each ≥ 1.
    - No new test carries `[Trait("Category","RealStack")]`.
  </acceptance_criteria>
  <done>Four RED test classes exist, compile, and fail on behavior against the skeletons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| health probe body → operator/kubelet | probe response is read externally; must not leak secrets |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Information Disclosure | new HealthCheckResult messages/Data | mitigate | Static-literal messages only; LoopLivenessHealthCheckTests + RedisReadyHealthCheckTests assert absence of "Password=", "abortConnect", "   at " |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — no new dependencies) |
</threat_model>

<verification>
Full hermetic build passes; the four new Phase-86 classes run and fail RED; all pre-existing
non-watchdog tests remain green. Command: `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack`.
</verification>

<success_criteria>
Solution compiles with the four skeleton primitives + four RED Phase-86 test classes; the RED tests
fail on behavior only; no regression in existing hermetic tests.
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-01-SUMMARY.md` when done.
</output>
