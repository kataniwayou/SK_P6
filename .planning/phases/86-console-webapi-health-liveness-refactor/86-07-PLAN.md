---
phase: 86-console-webapi-health-liveness-refactor
plan: 07
type: execute
wave: 2
depends_on: ["86-02", "86-03", "86-04"]
files_modified:
  - src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs
  - src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs
  - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs
  - src/BaseProcessor.Core/Liveness/LivenessWatchdogHealthCheck.cs
  - tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs
autonomous: true
requirements: [HLTH-03, HLTH-04, HLTH-06, HLTH-07, HLTH-08]

must_haves:
  truths:
    - "ProcessorLivenessHeartbeat beats the shared ILivenessHeartbeat unconditionally at the TOP of the loop, ABOVE the IsHealthy gate; the IsHealthy gate now guards ONLY the L2 WriteAsync."
    - "The processor marks the startup gate ready on its FIRST beat; a not-yet-Healthy processor booting against a down bus still beats → /health/live stays healthy → no restart."
    - "The retired BaseProcessor.Core LivenessWatchdogHealthCheck is deleted; processor /health/live is the shared LoopLivenessHealthCheck via AddConsoleLivenessWatchdog(interval=10)."
    - "Processor /health/ready includes identity+schema resolved (ProcessorIdentitySchemaReadyHealthCheck reading IProcessorContext.IsHealthy) plus Redis+latch inherited from BaseConsole; IProcessorLivenessState is untouched (still backs the L2 gate)."
  artifacts:
    - path: "src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs"
      provides: "unconditional top-of-loop beat + first-beat MarkReady + gate-only-on-L2-write"
      contains: "Beat()"
    - path: "src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs"
      provides: "identity+schema readiness via IProcessorContext.IsHealthy"
      contains: "IsHealthy"
    - path: "src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs"
      provides: "shared watchdog opt-in + identity/schema 'ready' descriptor; retired watchdog descriptor removed"
      contains: "AddConsoleLivenessWatchdog"
  key_links:
    - from: "src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs"
      to: "ILivenessHeartbeat.Beat() + IStartupGate.MarkReady()"
      via: "top-of-loop unconditional beat, first-beat gate mark"
      pattern: "Beat\\(\\)"
    - from: "BaseProcessorServiceCollectionExtensions"
      to: "ProcessorIdentitySchemaReadyHealthCheck 'ready' descriptor"
      via: "HealthCheckDescriptor Tags=['ready'] folded into /health/ready"
      pattern: "identity-schema-ready"
---

<objective>
Integrate the shared watchdog into the processor and fix the processor's original bug: move the liveness
beat ABOVE the `if (_context.IsHealthy)` gate so it beats unconditionally (keeping the gate only on the L2
write), mark the startup gate on the first beat, delete the retired BaseProcessor watchdog, register the
shared watchdog (interval=10 → stale at 30s under k=3) plus the identity+schema "ready" descriptor, and
implement the identity/schema readiness body. Processor inherits Redis+latch readiness from BaseConsole.

Purpose: HLTH-03/04/06/07 for processor; closes the "only-when-Healthy never beats under a down bus →
false restart" bug and puts the identity+schema "become healthy" work on readiness, not liveness.
Output: rewired heartbeat + DI + identity/schema check, one deleted file, one updated test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-RESEARCH.md
@docs/superpowers/specs/2026-07-26-console-health-liveness-refactor-design.md

<interfaces>
From src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs (current): loop wraps the whole beat in
  `if (_context.IsHealthy && _context.Id is {} id)` (L76); L2 write via _writer.WriteAsync inside try/catch-log.
From src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs:
  L152-155 old "liveness-watchdog" descriptor → LivenessWatchdogHealthCheck; L217-227 heartbeat + orchestrator
  registered via ActivatorUtilities.CreateInstance(..., instanceId); L234-239 StartupCompletionService already removed.
From src/BaseConsole.Core/DependencyInjection/ConsoleLivenessServiceCollectionExtensions.cs (86-03): AddConsoleLivenessWatchdog(interval,k=3).
From src/BaseProcessor.Core/Identity/IProcessorContext.cs: IsHealthy (Volatile.Read; never throws).
From src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs: IntervalSeconds (=10).
Keep IProcessorLivenessState + ProcessorLivenessWriter (they back the separate L2 gate — Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Beat unconditionally above the gate + first-beat MarkReady in ProcessorLivenessHeartbeat</name>
  <read_first>
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs (full — the ExecuteAsync loop)
    - 86-RESEARCH.md §Pattern 2 (Processor) + Pitfall 2
    - src/BaseConsole.Core/Health/ILivenessHeartbeat.cs + IStartupGate.cs
  </read_first>
  <files>src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs</files>
  <action>
    Add `ILivenessHeartbeat _heartbeat` and `IStartupGate _gate` to the ctor (they resolve from DI via the existing
    ActivatorUtilities registration). At the TOP of the while-loop, before the `if (_context.IsHealthy ...)` gate,
    call `_heartbeat.Beat()` unconditionally; on the first iteration also call `_gate.MarkReady()` once (a local
    firstBeat bool). Leave the `if (_context.IsHealthy && _context.Id is {} id)` gate wrapping ONLY the L2
    `_writer.WriteAsync` block (unchanged, including its log-and-continue catch). The Task.Delay tail is unchanged.
    Result: a not-yet-Healthy processor booting against a down bus still beats every tick → /health/live stays
    healthy → no restart; the L2 healthy write still only happens when actually Healthy.
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_heartbeat.Beat()" src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs` matches and appears BEFORE the `if (_context.IsHealthy` line (verify by line order).
    - The `if (_context.IsHealthy && _context.Id is { } id)` gate still wraps only the WriteAsync block.
    - `grep -n "MarkReady" src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs` matches (first-beat).
    - BaseProcessor.Core builds.
  </acceptance_criteria>
  <done>The heartbeat beats unconditionally above the gate and marks ready on first beat.</done>
</task>

<task type="auto">
  <name>Task 2: Implement identity/schema readiness body, swap DI to the shared watchdog, delete the retired watchdog</name>
  <read_first>
    - src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs (skeleton from 86-02)
    - tests/BaseApi.Tests/Processor/IdentitySchemaReadyHealthCheckTests.cs (RED test to satisfy)
    - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs (L145-162 descriptor + L217-227 heartbeat registration)
    - src/BaseProcessor.Core/Identity/IProcessorContext.cs (IsHealthy) + src/BaseConsole.Core/Health/HealthCheckDescriptor.cs
    - 86-RESEARCH.md §Pattern 4 + §Component map (BaseProcessorServiceCollectionExtensions row) + Pitfall 6
  </read_first>
  <files>
    src/BaseProcessor.Core/Liveness/ProcessorIdentitySchemaReadyHealthCheck.cs,
    src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs,
    src/BaseProcessor.Core/Liveness/LivenessWatchdogHealthCheck.cs
  </files>
  <action>
    (a) Implement ProcessorIdentitySchemaReadyHealthCheck: resolve `IProcessorContext` from `_outer`
    (GetRequiredService) at check time; `IsHealthy` → Healthy "identity+schema resolved", else Unhealthy
    "identity+schema not yet resolved". Never read Id/InputDefinition/etc (WR-03). Never throws; static-literal
    messages. (b) In BaseProcessorServiceCollectionExtensions: remove the old "liveness-watchdog" descriptor
    (L152-155) that pointed at LivenessWatchdogHealthCheck; call `services.AddConsoleLivenessWatchdog(intervalSeconds:
    10)` to register the shared ILivenessHeartbeat + "live" LoopLivenessHealthCheck descriptor; add a "ready"-tagged
    `HealthCheckDescriptor("identity-schema-ready", new[]{"ready"}, outer => new
    ProcessorIdentitySchemaReadyHealthCheck(outer))` (folds in UNLATCHED via the existing descriptor loop — it is a
    bootstrap gate, not an infra dep). Keep IProcessorLivenessState + ProcessorLivenessWriter (they back the L2 gate).
    (c) DELETE src/BaseProcessor.Core/Liveness/LivenessWatchdogHealthCheck.cs. Redis+latch readiness is inherited from
    the 86-04 EmbeddedHealthEndpointService change — no processor code needed for it.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*IdentitySchemaReadyHealthCheckTests*" && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*AddBaseProcessorFacts*"</automated>
  </verify>
  <acceptance_criteria>
    - IdentitySchemaReadyHealthCheckTests passes (IsHealthy false→Unhealthy, true→Healthy; no secret token).
    - `grep -n "AddConsoleLivenessWatchdog" src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs` matches with intervalSeconds 10.
    - `grep -n "identity-schema-ready" ...BaseProcessorServiceCollectionExtensions.cs` matches with Tags ["ready"].
    - LivenessWatchdogHealthCheck.cs no longer exists; `grep -rn "LivenessWatchdogHealthCheck" src/` returns nothing.
    - `grep -n "IProcessorLivenessState\|ProcessorLivenessWriter" ...BaseProcessorServiceCollectionExtensions.cs` still matches (kept).
  </acceptance_criteria>
  <done>Identity/schema readiness implemented + registered; shared watchdog registered; retired watchdog deleted; L2 state kept.</done>
</task>

<task type="auto">
  <name>Task 3: Port LivenessHeartbeatFacts to the new heartbeat ctor + assert unconditional beat</name>
  <read_first>
    - tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs (current ctor construction)
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs (new ctor)
  </read_first>
  <files>tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs</files>
  <action>
    Update the heartbeat construction to supply the new ILivenessHeartbeat + IStartupGate deps (fakes). Preserve the
    existing frozen-healthy L2-write assertions. Add a Phase-86 assertion that the loop calls ILivenessHeartbeat.Beat()
    even when IProcessorContext.IsHealthy is FALSE (unconditional beat), and that the first beat calls
    IStartupGate.MarkReady(). If any other processor test (e.g. ClashRefreshFacts) fails to compile due to the deleted
    LivenessWatchdogHealthCheck, fix the reference minimally (those tests target IProcessorLivenessState, which is kept).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*LivenessHeartbeatFacts*"</automated>
  </verify>
  <acceptance_criteria>
    - LivenessHeartbeatFacts compiles against the new ctor and passes, including the new not-Healthy-still-beats assertion.
    - Full hermetic build succeeds (no orphaned refs to the deleted watchdog).
  </acceptance_criteria>
  <done>LivenessHeartbeatFacts is ported and green; the suite builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| down bus during bootstrap → heartbeat loop | must keep beating so liveness stays healthy (readiness handles not-Healthy) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-14 | Denial of Service (false restart on cold boot) | ProcessorLivenessHeartbeat gate | mitigate | Beat moved above IsHealthy gate; LivenessHeartbeatFacts asserts beat when not-Healthy |
| T-86-15 | Information Disclosure | identity/schema readiness body | mitigate | Static-literal messages; no infra detail |
| T-86-16 | Tampering (retired-type residue / wrong deletion) | LivenessWatchdogHealthCheck delete vs IProcessorLivenessState keep | mitigate | grep gate: zero watchdog refs; IProcessorLivenessState registration retained |
</threat_model>

<verification>
Processor builds; IdentitySchemaReadyHealthCheckTests + LivenessHeartbeatFacts + AddBaseProcessorFacts green;
no residual LivenessWatchdogHealthCheck refs; full hermetic run shows no new failures.
</verification>

<success_criteria>
Processor beats unconditionally above the gate, marks ready on first beat, exposes identity+schema on readiness,
drops the retired watchdog while keeping the L2 state, and inherits Redis+latch (HLTH-03/04/06/07; HLTH-08 inherited).
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-07-SUMMARY.md` when done.
</output>
