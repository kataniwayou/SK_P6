---
phase: 86-console-webapi-health-liveness-refactor
plan: 08
type: execute
wave: 3
depends_on: ["86-05", "86-06", "86-07"]
files_modified:
  - k8s/30-baseapi-service.yaml
  - k8s/31-orchestrator.yaml
  - k8s/32-keeper.yaml
  - k8s/33-processor-sample.yaml
autonomous: true
requirements: [HLTH-07]

must_haves:
  truths:
    - "All four k8s deployments have a startupProbe → /health/startup on their existing health port, giving boot its own grace window."
    - "The startupProbe grace window comfortably covers a ~40s rabbitmq cold start so a slow boot never CrashLoops."
    - "readinessProbe and livenessProbe blocks are otherwise preserved (liveness stays conservative until the startupProbe is proven)."
  artifacts:
    - path: "k8s/32-keeper.yaml"
      provides: "startupProbe → /health/startup :8083"
      contains: "startupProbe"
  key_links:
    - from: "k8s/3{0,1,2,3}-*.yaml"
      to: "/health/startup"
      via: "startupProbe httpGet path /health/startup on the service's health port"
      pattern: "startupProbe"
---

<objective>
Add a startupProbe → /health/startup to all four k8s deployments (currently absent) so boot gets its own
grace window and steady-state liveness can be trusted. Startup-gate marking is already per-service in code
(orchestrator=hydration, keeper/processor=first beat via Waves 1-2, baseapi=migrations) — this plan wires
the kubelet probe that reads that gate.

Purpose: HLTH-07 k8s half. A startupProbe holds off liveness/readiness evaluation until boot completes,
covering the ~40s rabbitmq cold start (Pitfall 5).
Output: four manifest edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-RESEARCH.md
@docs/superpowers/specs/2026-07-26-console-health-liveness-refactor-design.md

<interfaces>
Health ports (from the current manifests / RESEARCH §Component map): baseapi :8080 (30), orchestrator :8081 (31),
keeper :8083 (32), processor-sample :8082 (33). Each already has readinessProbe (/health/ready) + livenessProbe
(/health/live) but NO startupProbe. All three probes serve on the same containerPort.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add startupProbe → /health/startup to all four deployments</name>
  <read_first>
    - k8s/30-baseapi-service.yaml (probe block + port 8080)
    - k8s/31-orchestrator.yaml (probe block + port 8081)
    - k8s/32-keeper.yaml (probe block + port 8083 — already read; readiness/liveness present, no startupProbe)
    - k8s/33-processor-sample.yaml (probe block + port 8082)
    - 86-RESEARCH.md §k8s manifests row + Pitfall 5 (ordering)
  </read_first>
  <files>
    k8s/30-baseapi-service.yaml,
    k8s/31-orchestrator.yaml,
    k8s/32-keeper.yaml,
    k8s/33-processor-sample.yaml
  </files>
  <action>
    In each of the four Deployment manifests, add a `startupProbe` to the container spec (alongside the existing
    readinessProbe/livenessProbe) with `httpGet: { path: /health/startup, port: <service health port> }` — port 8080
    for 30-baseapi, 8081 for 31-orchestrator, 8083 for 32-keeper, 8082 for 33-processor-sample. Use a grace window
    that covers a ~40s cold start: `initialDelaySeconds: 5`, `periodSeconds: 5`, `timeoutSeconds: 3`,
    `failureThreshold: 30` (≈150s). Leave the existing readinessProbe and livenessProbe blocks unchanged (keep
    liveness initialDelaySeconds conservative — the spec defers tightening to a later revisit). Preserve YAML
    structure/indentation and the surrounding comments.
  </action>
  <verify>
    <automated>grep -c startupProbe k8s/30-baseapi-service.yaml k8s/31-orchestrator.yaml k8s/32-keeper.yaml k8s/33-processor-sample.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c startupProbe` returns exactly 1 for each of the four manifests.
    - Each startupProbe httpGet path is `/health/startup` on the correct port (8080/8081/8083/8082 respectively).
    - readinessProbe and livenessProbe blocks are still present and unchanged in each file.
    - `grep -c "path: /health/startup" k8s/3{0,1,2,3}-*.yaml` each = 1.
  </acceptance_criteria>
  <done>All four manifests carry a startupProbe → /health/startup on the correct port.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| kubelet startup probe → boot window | slow boot must not be killed by liveness before startup completes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-17 | Denial of Service (CrashLoop on slow boot) | absent startupProbe | mitigate | startupProbe grace window (~150s) covers ~40s rabbitmq cold start before liveness engages |
</threat_model>

<verification>
`grep -c startupProbe k8s/3{0,1,2,3}-*.yaml` → each 1; paths /health/startup on correct ports.
</verification>

<success_criteria>
All four k8s deployments gain a startupProbe → /health/startup with a boot-covering grace window; existing
readiness/liveness preserved (HLTH-07).
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-08-SUMMARY.md` when done.
</output>
