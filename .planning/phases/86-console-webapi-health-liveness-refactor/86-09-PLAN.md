---
phase: 86-console-webapi-health-liveness-refactor
plan: 09
type: execute
wave: 4
depends_on: ["86-05", "86-06", "86-07", "86-08"]
files_modified: []
autonomous: false
requirements: [HLTH-01, HLTH-02, HLTH-03, HLTH-04, HLTH-05, HLTH-06, HLTH-07, HLTH-08]

must_haves:
  truths:
    - "The full hermetic suite is green (no RealStack), with all Phase-86 tests passing and no regressions."
    - "All four k8s manifests carry a startupProbe → /health/startup (grep gate)."
    - "Live proof: with the broker made unreachable, all four services stay running (RESTARTS 0), go NotReady, show caught+logged infra errors, and recover only on restart."
  artifacts: []
  key_links:
    - from: "phase gate"
      to: "RESTARTS 0 under broker outage"
      via: "kubectl set env RabbitMq__Host=rabbitmq-unreachable.invalid on all four → kubectl get pods RESTARTS"
      pattern: "RESTARTS"
---

<objective>
Terminal verification gate for Phase 86. Prove the headline outcome — an infrastructure outage never
crashes the process nor restarts the pod; only a genuinely stalled watchdog loop restarts — via the full
hermetic suite, the startupProbe grep, and the live broker-unreachable experiment (RESTARTS 0).

Purpose: this is the phase's success gate (ROADMAP criterion 7 / the live proof). No production code here.
Output: a recorded verdict (SUMMARY) with the hermetic result, grep result, and the live-proof pod table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-VALIDATION.md
@docs/superpowers/specs/2026-07-26-console-health-liveness-refactor-design.md

<interfaces>
Hermetic run: tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack
Live proof (k8s ONLY, never docker-compose): kubectl set env deployment/{baseapi-service,orchestrator,keeper,processor-sample}
  -n skp RabbitMq__Host=rabbitmq-unreachable.invalid  → observe; revert with RabbitMq__Host=rabbitmq.
k8s local-image-stale trap (project memory): after any rebuild, deploy under a UNIQUE tag (fix-<sha>) + kubectl set image,
  then run the harness -SkipBringUp with manual port-forwards. IfNotPresent + :local runs STALE bits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full hermetic green + startupProbe grep gate</name>
  <read_first>
    - 86-VALIDATION.md (Sampling Rate → phase gate)
    - .planning/phases/86-console-webapi-health-liveness-refactor/86-01..08 SUMMARYs
  </read_first>
  <files></files>
  <action>
    Rebuild the test project and run the FULL hermetic suite (no class filter, RealStack excluded). Confirm all
    Phase-86 tests pass and there are zero regressions vs the pre-phase baseline. Then run the startupProbe grep
    across all four manifests. Record both results in the SUMMARY.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack && grep -c startupProbe k8s/30-baseapi-service.yaml k8s/31-orchestrator.yaml k8s/32-keeper.yaml k8s/33-processor-sample.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Full hermetic suite passes (0 failed); all Phase-86 classes present and green.
    - `grep -c startupProbe` returns 1 for each of the four manifests.
    - No test carrying `[Trait("Category","RealStack")]` was required for this gate.
  </acceptance_criteria>
  <done>Hermetic suite green and startupProbe present on all four manifests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The refactor: shared BaseConsole.Core watchdog (opt-in; keeper+processor beat unconditionally, orchestrator
    self-only), unconditional beats with bounded infra calls, Redis added to readiness on all four, a hard
    readiness latch (no self-heal), processor identity+schema on readiness, and k8s startupProbes. The live proof
    re-runs the original broker-unreachable experiment on the Docker-Desktop k8s cluster.
  </what-built>
  <how-to-verify>
    1. Rebuild the four service images and deploy them under a UNIQUE tag (fix-<sha>) with `kubectl set image`
       (avoid the :local IfNotPresent stale-bits trap); confirm the pods are running the new bits.
    2. Make the broker unreachable on all four:
       `kubectl set env deployment/baseapi-service deployment/orchestrator deployment/keeper deployment/processor-sample -n skp RabbitMq__Host=rabbitmq-unreachable.invalid`
    3. Watch for ~3-4 minutes: `kubectl get pods -n skp -w`.
    4. Confirm ALL four show `RESTARTS 0` (no Exit 137 / SIGTERM restart), go NotReady (readiness 503 → latched
       after the failureThreshold window), and their logs show caught+logged infra errors (not crashes).
    5. Confirm `/health/live` stays healthy throughout (the watchdog loops keep beating).
    6. Revert: `... RabbitMq__Host=rabbitmq`; confirm services recover (or, per "no self-heal", require a pod
       restart to clear the latched readiness) and return to Ready.
    Record the `kubectl get pods` RESTARTS column and the observed readiness transitions in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" with the RESTARTS-0 pod table, or describe the failure (which service restarted / crashed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| broker outage → live pods | must not restart/crash; must go NotReady with logs; recover only on restart |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-18 | Denial of Service (crash-loop under infra outage) | all four services live | mitigate | Live proof asserts RESTARTS 0 under broker-unreachable; failure = a service restarted (gate fails) |
| T-86-19 | Tampering (stale k8s bits validate old code) | live proof image | mitigate | Deploy under unique tag + kubectl set image (project-memory trap), verify new bits before the proof |
</threat_model>

<verification>
Phase gate = full hermetic green + startupProbe grep (each 1) + the live broker-unreachable proof showing all
four at RESTARTS 0, NotReady, with logs, recovering only on restart.
</verification>

<success_criteria>
All eight HLTH requirements demonstrably satisfied: hermetic suite green, startupProbes present, and the live
broker-outage experiment leaves all four services at RESTARTS 0 (the headline outcome).
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-09-SUMMARY.md` when done, including the
hermetic result, the grep result, and the live-proof RESTARTS-0 pod table.
</output>
