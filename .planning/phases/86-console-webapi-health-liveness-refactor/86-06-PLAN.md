---
phase: 86-console-webapi-health-liveness-refactor
plan: 06
type: execute
wave: 2
depends_on: ["86-02", "86-03", "86-04"]
files_modified:
  - src/Keeper/Health/BitHealthLoop.cs
  - src/Keeper/Program.cs
  - src/Keeper/Health/KeeperLivenessWatchdogHealthCheck.cs
  - src/Keeper/Health/IKeeperLivenessState.cs
  - src/Keeper/Health/KeeperLivenessState.cs
  - tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs
autonomous: true
requirements: [HLTH-03, HLTH-04, HLTH-05, HLTH-06, HLTH-07, HLTH-08]

must_haves:
  truths:
    - "BitHealthLoop beats the shared ILivenessHeartbeat at the TOP of every tick, before ProbeOnceAsync and before any bus op, unconditionally."
    - "The edge bus-ops (ReceiveEndpoint.Start().Ready + bus.Publish PauseAll/ResumeAll + Stop) are bounded/off the beat path so a dead broker can no longer freeze the tick and starve the beat."
    - "Keeper marks the startup gate ready on its FIRST beat; the base StartupCompletionService is removed from keeper."
    - "The keeper-specific watchdog (KeeperLivenessWatchdogHealthCheck + IKeeperLivenessState + KeeperLivenessState) is deleted; keeper /health/live is now the shared LoopLivenessHealthCheck via AddConsoleLivenessWatchdog; keeper /health/ready gains Redis + latch inherited from BaseConsole."
  artifacts:
    - path: "src/Keeper/Health/BitHealthLoop.cs"
      provides: "unconditional top-of-tick beat + bounded edge bus-ops + first-beat MarkReady"
      contains: "ILivenessHeartbeat"
    - path: "src/Keeper/Program.cs"
      provides: "AddConsoleLivenessWatchdog(interval=5) opt-in; removed old watchdog + state + StartupCompletionService"
      contains: "AddConsoleLivenessWatchdog"
  key_links:
    - from: "src/Keeper/Health/BitHealthLoop.cs"
      to: "ILivenessHeartbeat.Beat() + IStartupGate.MarkReady()"
      via: "top-of-loop beat, first-beat gate mark"
      pattern: "Beat\\(\\)"
    - from: "src/Keeper/Program.cs"
      to: "BaseConsole.Core AddConsoleLivenessWatchdog"
      via: "opt-in registration replacing the retired keeper watchdog descriptor"
      pattern: "AddConsoleLivenessWatchdog"
---

<objective>
Integrate the shared watchdog into the keeper and fix the keeper's original bug: move the liveness beat
to the top of the BitHealthLoop tick (unconditional, before any bus op), bound the edge bus-ops so a dead
broker can no longer freeze the tick and starve the beat, mark the startup gate on the first beat, and
delete the retired keeper-specific watchdog + its L1 state. Keeper opts into the shared primitive with a
5s interval (stale at 15s under k=3) and inherits Redis + latch readiness from BaseConsole.

Purpose: HLTH-03/06/07 for keeper; closes the "BIT loop stale → false restart under broker outage" bug.
Output: rewired BitHealthLoop + Program.cs, three deleted files, one updated test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-RESEARCH.md
@docs/superpowers/specs/2026-07-26-console-health-liveness-refactor-design.md

<interfaces>
From src/BaseConsole.Core/DependencyInjection/ConsoleLivenessServiceCollectionExtensions.cs (86-03):
  AddConsoleLivenessWatchdog(this IServiceCollection services, int intervalSeconds, int k = 3)
  → registers ILivenessHeartbeat singleton + "live" HealthCheckDescriptor(LoopLivenessHealthCheck).

From src/BaseConsole.Core/Health/ILivenessHeartbeat.cs: void Beat(); DateTime? Current { get; }
From src/BaseConsole.Core/Health/IStartupGate.cs: bool IsReady { get; }; void MarkReady();

From src/Keeper/Health/BitHealthLoop.cs (current): ctor takes IKeeperLivenessState liveness; L57 liveness.Update
  every tick AFTER ProbeOnceAsync; edge block L59-96 awaits h.ReceiveEndpoint.Start(ct).Ready (L71) + bus.Publish
  (L72/L82) + Stop (L81) inside the tick with no timeout → hangs on a dead broker.
From src/Keeper/Program.cs L45-54: TimeProvider.System + IKeeperLivenessState + "keeper-liveness-watchdog" descriptor;
  L22/base AddBaseConsole keeps StartupCompletionService (readiness on bus-start today).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire BitHealthLoop — top-of-tick beat, bounded edge bus-ops, first-beat MarkReady</name>
  <read_first>
    - src/Keeper/Health/BitHealthLoop.cs (full — the tick + edge block)
    - 86-RESEARCH.md §Pattern 2 (Keeper) + Pitfall 1
    - src/BaseConsole.Core/Health/ILivenessHeartbeat.cs + IStartupGate.cs
  </read_first>
  <files>src/Keeper/Health/BitHealthLoop.cs</files>
  <action>
    Change the ctor: replace the `IKeeperLivenessState liveness` dependency with `ILivenessHeartbeat heartbeat`
    and `IStartupGate gate`. At the TOP of the while-loop body, before `probe.ProbeOnceAsync`, call
    `heartbeat.Beat()` unconditionally; on the first iteration also call `gate.MarkReady()` once (a local bool
    firstBeat). Delete the old `liveness.Update(...)` line. Keep the `metrics.L2Probe.Add(1)` heartbeat.
    Bound the edge bus-ops so a hung broker cannot freeze the tick and starve the beat: wrap
    `h.ReceiveEndpoint.Start(stoppingToken).Ready`, `bus.Publish(PauseAll/ResumeAll)`, and
    `h.ReceiveEndpoint.Stop(stoppingToken)` with a bounded wait (e.g. `.WaitAsync(TimeSpan)`) + catch-log, OR move
    them off the tick — per the locked "off the beat path" decision. Preserve the existing edge semantics: gates
    Open/Close stay, prevHealthy is only advanced after a successful broadcast (idempotent retry on failure),
    OperationCanceledException on shutdown still breaks cleanly. The beat being FIRST guarantees a fresh stamp even
    on a tick whose bounded bus-op times out.
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "heartbeat.Beat()" src/Keeper/Health/BitHealthLoop.cs` matches and appears BEFORE the `probe.ProbeOnceAsync` call (verify by line order).
    - `grep -n "IKeeperLivenessState\|liveness.Update" src/Keeper/Health/BitHealthLoop.cs` returns nothing.
    - `grep -n "MarkReady" src/Keeper/Health/BitHealthLoop.cs` matches (first-beat gate).
    - The edge bus-ops (Start().Ready / Publish / Stop) are each bounded (WaitAsync/timeout) or moved off-tick — no unbounded await inside the tick.
    - Keeper builds.
  </acceptance_criteria>
  <done>BitHealthLoop beats first/unconditionally, bounds its edge bus-ops, and marks the gate on first beat.</done>
</task>

<task type="auto">
  <name>Task 2: Swap keeper registration to the shared watchdog; delete the retired watchdog + state</name>
  <read_first>
    - src/Keeper/Program.cs (L22 base AddBaseConsole; L45-54 old watchdog registration)
    - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs L234-239 (the StartupCompletionService-removal idiom to copy)
    - 86-RESEARCH.md §Component map (Keeper/Program.cs + Pattern 6 first-beat) + Pitfall 6 (do NOT touch IProcessorLivenessState — that is processor-side)
  </read_first>
  <files>
    src/Keeper/Program.cs,
    src/Keeper/Health/KeeperLivenessWatchdogHealthCheck.cs,
    src/Keeper/Health/IKeeperLivenessState.cs,
    src/Keeper/Health/KeeperLivenessState.cs
  </files>
  <action>
    In Program.cs: remove the L45-54 block (the IKeeperLivenessState singleton + the "keeper-liveness-watchdog"
    HealthCheckDescriptor); keep `TryAddSingleton(TimeProvider.System)` (AddConsoleLivenessWatchdog also does
    TryAddSingleton, so either is fine). Call `builder.Services.AddConsoleLivenessWatchdog(intervalSeconds: 5)`
    (matches the Probe:DelaySeconds cadence → stale at 15s under k=3). Remove the base StartupCompletionService so
    keeper marks ready on first beat (copy the removal idiom from BaseProcessorServiceCollectionExtensions L234-239,
    adapted to builder.Services). DELETE the three files KeeperLivenessWatchdogHealthCheck.cs, IKeeperLivenessState.cs,
    KeeperLivenessState.cs. The BitHealthLoop ctor change (Task 1) resolves ILivenessHeartbeat + IStartupGate from DI
    (both now registered — ILivenessHeartbeat by AddConsoleLivenessWatchdog, IStartupGate by AddBaseConsoleHealth).
    Keeper /health/ready gains Redis + latch automatically via the 86-04 EmbeddedHealthEndpointService change — no
    keeper code needed for that.
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AddConsoleLivenessWatchdog" src/Keeper/Program.cs` matches with intervalSeconds 5.
    - The three files no longer exist (`test ! -f` each).
    - `grep -rn "IKeeperLivenessState\|KeeperLivenessWatchdogHealthCheck\|KeeperLivenessState" src/` returns nothing.
    - Program.cs removes StartupCompletionService (the removal foreach is present).
    - Keeper builds.
  </acceptance_criteria>
  <done>Keeper uses the shared watchdog + first-beat readiness; the retired keeper watchdog + state are gone.</done>
</task>

<task type="auto">
  <name>Task 3: Update BitHealthLoopTests for the new ctor + beat assertion</name>
  <read_first>
    - tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs (L29 class, L99 factory with IKeeperLivenessState param)
    - src/BaseConsole.Core/Health/ILivenessHeartbeat.cs (Beat/Current to fake)
  </read_first>
  <files>tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs</files>
  <action>
    Replace the `IKeeperLivenessState? liveness` construction parameter with an `ILivenessHeartbeat` fake (and an
    IStartupGate fake) so the test compiles against the new BitHealthLoop ctor. Preserve all existing edge/gate
    assertions (first-healthy-tick → 1 ResumeAll, etc.). Add a Phase-86 assertion that the loop calls
    `ILivenessHeartbeat.Beat()` on each tick and BEFORE the probe/bus interaction, and that a bounded/hanging edge
    bus-op still leaves a fresh beat (drive with the existing ScriptedRedis + FakeTimeProvider harness). Tag new
    assertions `[Trait("Phase","86")]` where added as new facts.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*BitHealthLoopTests*"</automated>
  </verify>
  <acceptance_criteria>
    - BitHealthLoopTests compiles against the new ctor and passes (existing edge assertions + new beat-first assertion).
    - `grep -n "IKeeperLivenessState" tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs` returns nothing.
  </acceptance_criteria>
  <done>BitHealthLoopTests is ported to the shared heartbeat and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| dead broker → BitHealthLoop tick | a hung bus op must not freeze the tick and trip false liveness |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-12 | Denial of Service (false restart) | BitHealthLoop edge bus-ops | mitigate | Beat first + bound Start().Ready/Publish/Stop (WaitAsync/off-tick); BitHealthLoopTests asserts beat survives a hung edge op |
| T-86-13 | Tampering (retired-type residue) | deleted keeper watchdog/state | mitigate | grep gate proves zero remaining refs in src/ |
</threat_model>

<verification>
Keeper builds; BitHealthLoopTests green; no residual refs to the deleted types; full hermetic run shows no new failures.
Command: `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack`.
</verification>

<success_criteria>
Keeper beats the shared watchdog first/unconditionally with bounded edge bus-ops, marks ready on first beat,
drops the retired watchdog+state, and inherits Redis+latch readiness (HLTH-03/06/07; HLTH-04/05/08 inherited).
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-06-SUMMARY.md` when done.
</output>
