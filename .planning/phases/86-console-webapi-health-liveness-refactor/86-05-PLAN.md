---
phase: 86-console-webapi-health-liveness-refactor
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseApi.Core/Health/ApiRedisReadyHealthCheck.cs
  - src/BaseApi.Core/Health/ApiLatchedReadinessHealthCheck.cs
  - src/BaseApi.Core/DependencyInjection/HealthServiceCollectionExtensions.cs
  - src/BaseApi.Core/DependencyInjection/RedisServiceCollectionExtensions.cs
  - tests/BaseApi.Tests/Api/ApiReadinessLatchTests.cs
autonomous: true
requirements: [HLTH-04, HLTH-05, HLTH-08]

must_haves:
  truths:
    - "baseapi /health/ready includes a bounded never-throw Redis readiness check (Redis was previously on no probe)."
    - "baseapi Postgres and Redis readiness are wrapped in a sticky latch (no self-heal → restart-only recovery); the baseapi bus check stays capped at Degraded and is NOT latched."
    - "The superseded D-06 'no Redis health check / Redis down ⇒ /health/ready 200' comment is corrected."
  artifacts:
    - path: "src/BaseApi.Core/Health/ApiRedisReadyHealthCheck.cs"
      provides: "bounded never-throw Redis readiness for the webapi"
      contains: "IConnectionMultiplexer"
    - path: "src/BaseApi.Core/Health/ApiLatchedReadinessHealthCheck.cs"
      provides: "sticky readiness latch for the webapi (Postgres+Redis)"
      contains: "failureThreshold"
    - path: "src/BaseApi.Core/DependencyInjection/HealthServiceCollectionExtensions.cs"
      provides: "Postgres+Redis latched on /health/ready; bus untouched"
      contains: "ready"
  key_links:
    - from: "HealthServiceCollectionExtensions.AddBaseApiHealth"
      to: "ApiLatchedReadinessHealthCheck over Postgres + Redis"
      via: "AddCheck(name, singleton-latch-instance, tags:['ready'])"
      pattern: "ApiLatchedReadinessHealthCheck"
---

<objective>
Mirror the Redis-readiness + hard-latch pattern into the WebApi base (BaseApi.Core), which keeps its OWN
health pattern and does NOT reference BaseConsole.Core. Add a bounded never-throw Redis readiness check,
wrap Postgres + Redis readiness in a sticky latch (no self-heal), leave the bus check capped-at-Degraded and
unlatched, and correct the stale D-06 comment. Self-contained TDD (RED test → mirror types → GREEN).

Purpose: baseapi is a separate library/pattern; its readiness must gain Redis + the latch to satisfy
HLTH-04/05/08 for the fourth service, while preserving the deliberate bus-soft-dep exception.
Output: two mirror types + readiness wiring + one comment fix + one latch test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-console-webapi-health-liveness-refactor/86-RESEARCH.md
@docs/superpowers/specs/2026-07-26-console-health-liveness-refactor-design.md

<interfaces>
From src/BaseApi.Core/DependencyInjection/HealthServiceCollectionExtensions.cs:
  services.AddHealthChecks()
    .AddCheck("self", ... tags:["live"])
    .AddCheck<StartupHealthCheck>("startup", tags:["startup","ready"])
    .AddNpgSql(cfg.RequireConnectionString("Postgres"), tags:["ready"]);   // Postgres readiness (to be latched)
  Single Kestrel container — IConnectionMultiplexer + config are directly DI-resolvable (unlike the console two-container model).

From src/BaseApi.Core/DependencyInjection/RedisServiceCollectionExtensions.cs: IConnectionMultiplexer singleton;
  the D-06 XML-doc comment ("does NOT register a Redis health check", "Redis down ⇒ /health/ready 200") is now stale.

From src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs: bus check MinimalFailureStatus=Degraded
  (publish-only/soft) — DO NOT touch; the baseapi bus is explicitly NOT latched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror the Redis + latch types into BaseApi.Core with a RED latch test</name>
  <read_first>
    - src/BaseConsole.Core/Health/RedisReadyHealthCheck.cs + LatchedReadinessHealthCheck.cs (the proven patterns to mirror — after 86-04 they are the reference; if this runs before 86-04, mirror 86-RESEARCH §Pattern 3 + §Pattern 5)
    - src/BaseApi.Core/Health/IStartupGate.cs (existing BaseApi.Core.Health namespace conventions; StartupGate lives here)
    - 86-RESEARCH.md §Pattern 3, §Pattern 5, Pitfall 4
  </read_first>
  <files>
    src/BaseApi.Core/Health/ApiRedisReadyHealthCheck.cs,
    src/BaseApi.Core/Health/ApiLatchedReadinessHealthCheck.cs,
    tests/BaseApi.Tests/Api/ApiReadinessLatchTests.cs
  </files>
  <behavior>
    - ApiRedisReadyHealthCheck: null IConnectionMultiplexer → Unhealthy "Redis not started"; reachable (PING ok) → Healthy;
      throwing/timeout → Unhealthy "Redis unreachable"; never throws out; no secret token in body.
    - ApiLatchedReadinessHealthCheck: failureThreshold consecutive Unhealthy → latched Unhealthy forever even after
      inner recovers; single blip + Healthy resets. Per-process singleton instance.
  </behavior>
  <action>
    Create `ApiRedisReadyHealthCheck : IHealthCheck` in namespace BaseApi.Core.Health resolving IConnectionMultiplexer
    (constructor-injected in this single-container webapi, or resolved at check time) and issuing a bounded PING with the
    never-throw + no-secret discipline. Create `ApiLatchedReadinessHealthCheck(IHealthCheck inner, int failureThreshold) :
    IHealthCheck` with the sticky-latch logic (Interlocked counter + volatile latched flag; reset on non-Unhealthy).
    Write `tests/BaseApi.Tests/Api/ApiReadinessLatchTests.cs` `[Trait("Phase","86")]` (NOT RealStack) proving the sticky
    latch and the Redis null/unreachable mapping. Tests are RED until the bodies are filled in this same task, then GREEN.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class "*ApiReadinessLatchTests*"</automated>
  </verify>
  <acceptance_criteria>
    - ApiReadinessLatchTests passes: sticky-after-recovery, blip-reset, Redis null→Unhealthy, unreachable→Unhealthy (no throw), no secret token.
    - `grep -n "failureThreshold" src/BaseApi.Core/Health/ApiLatchedReadinessHealthCheck.cs` matches.
  </acceptance_criteria>
  <done>Mirror types implemented and their latch/Redis test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Postgres+Redis latch into AddBaseApiHealth and fix the D-06 comment</name>
  <read_first>
    - src/BaseApi.Core/DependencyInjection/HealthServiceCollectionExtensions.cs (the AddHealthChecks chain)
    - src/BaseApi.Core/DependencyInjection/RedisServiceCollectionExtensions.cs (the stale D-06 comment)
    - 86-RESEARCH.md §Component map (BaseApi rows) + State of the Art (D-06 superseded)
  </read_first>
  <files>
    src/BaseApi.Core/DependencyInjection/HealthServiceCollectionExtensions.cs,
    src/BaseApi.Core/DependencyInjection/RedisServiceCollectionExtensions.cs
  </files>
  <action>
    In AddBaseApiHealth, add the Redis readiness check tagged "ready" and wrap BOTH the Postgres readiness and the
    Redis readiness in per-process singleton `ApiLatchedReadinessHealthCheck` instances (register via
    AddCheck(name, instance, tags:["ready"]) so each latch instance is constructed once and persists). Keep the "self"
    live check and the "startup" gate check unchanged. Do NOT latch or alter the bus check (it lives in
    MessagingServiceCollectionExtensions, MinimalFailureStatus=Degraded — soft dep, explicitly not latched). Source
    failureThreshold from config with a sensible default matching the k8s readiness failureThreshold. Then correct the
    RedisServiceCollectionExtensions D-06 XML-doc comment so it no longer claims "Redis down ⇒ /health/ready 200" —
    describe the new behavior (Redis is now a latched required readiness dep).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ApiLatchedReadinessHealthCheck" src/BaseApi.Core/DependencyInjection/HealthServiceCollectionExtensions.cs` matches (wrapping Postgres + Redis).
    - The bus check registration is untouched (`grep -n "MinimalFailureStatus" src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` still shows Degraded).
    - The RedisServiceCollectionExtensions comment no longer asserts "Redis down ⇒ /health/ready 200".
    - Full hermetic suite builds and shows no NEW failures vs the wave baseline.
  </acceptance_criteria>
  <done>baseapi /health/ready gains latched Postgres+Redis; bus stays soft/unlatched; D-06 comment corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Postgres/Redis client → webapi readiness | required-dep failure → NotReady (never restart), no self-heal |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-09 | Information Disclosure | ApiRedisReadyHealthCheck body | mitigate | Static-literal messages; test asserts no secret token |
| T-86-10 | Correctness (self-heal leaks a dead pod back to Ready) | ApiLatchedReadinessHealthCheck | mitigate | Sticky-after-recovery test; per-process singleton latch |
| T-86-11 | Correctness (over-latching a soft dep) | baseapi bus check | accept | Bus stays Degraded-capped + unlatched (locked); CRUD works publish-only |
</threat_model>

<verification>
ApiReadinessLatchTests green; full hermetic build clean; bus check unchanged; D-06 comment corrected.
</verification>

<success_criteria>
baseapi readiness gains a bounded Redis check + a sticky latch over Postgres+Redis; the bus stays soft
and unlatched; the stale D-06 comment is fixed (HLTH-04/05/08 for the fourth service).
</success_criteria>

<output>
Create `.planning/phases/86-console-webapi-health-liveness-refactor/86-05-SUMMARY.md` when done.
</output>
