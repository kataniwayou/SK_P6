---
phase: 88-pipeline-dashboard-maintenance-handoff-proof-fault-injection
plan: 04
type: execute
wave: 3
depends_on: ["88-03"]
files_modified:
  - scripts/phase-88-panel-discriminate.ps1
  - analyzer-reports/phase-88-BASE-01.json
autonomous: true
requirements: [DISC-01, DISC-03]

must_haves:
  truths:
    - "Every one of the fourteen business panels has a recorded healthy baseline band, captured from a settled stack over absolute pinned windows of identical width at a fixed viewport, read from the rendered panel."
    - "Each panel's band states which of the three statistical regimes it belongs to, because one rule cannot serve a jittering rate, a zero-floor event counter and a range-cumulative stat."
    - "The banding is falsifiable: the band, the samples it was computed from, the viewport, the window bounds and the derived rate interval are all in the artifact."
    - "Panel 4 is banded on its per-minute increase at the fixed sub-window, and its operator-visible monotone level is recorded separately as a misleading-by-default note rather than scored as movement."
    - "Every scenario the phase will run exists as a row in one static in-script table with a pre-declared, non-empty cross-talk panel list."
  artifacts:
    - path: "scripts/phase-88-panel-discriminate.ps1"
      provides: "the scenario driver: static scenario table, PRE gate, forward, -Mode Baseline, artifact writer"
      contains: "BASE-01"
    - path: "analyzer-reports/phase-88-BASE-01.json"
      provides: "the DISC-01 baseline band for all fourteen panels"
      contains: "BaselineBands"
  key_links:
    - from: "scripts/phase-88-panel-discriminate.ps1"
      to: "scripts/lib/phase-88-panel-read.ps1"
      via: "Invoke-PanelReadBatch over a Get-PinnedWindowSeries set"
      pattern: "Invoke-PanelReadBatch"
    - from: "the scenario table"
      to: "88-PROBE-DECISIONS.md"
      via: "each probe-gated lever is set from the recorded probe field, not assumed"
      pattern: "PQ-0[1-6]"
---

<objective>
Author the scenario driver's frame and its baseline mode, then capture the DISC-01 healthy baseline
band for all fourteen business panels from a settled stack.

Without a band, "it moved" is unfalsifiable — that is the roadmap's own wording for why criterion 1
exists. The band must be captured now, from the rendered panels, over absolute pinned windows,
because Prometheus retention is roughly twelve hours and a band measured from historical prose is
not a live measurement.

Purpose: establish the null hypothesis every later scenario is tested against, and get the driver's
frame, scenario table and artifact writer in place before any fault is driven.
Output: `scripts/phase-88-panel-discriminate.ps1` (frame + `-Mode Baseline`),
`analyzer-reports/phase-88-BASE-01.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PATTERNS.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-03-SUMMARY.md

<interfaces>
<!-- The static scenario table every later plan selects rows from. Authored here, extended by no one. -->

The fourteen panels of `k8s/dashboards/business.json`, with the regime and predicted direction each
scenario will assert. Panel id -> title -> type -> regime:

   1  Orchestrator consumed vs sent (conservation)                    timeseries  A
   2  Keeper consumed vs sent (conservation)                          timeseries  B (guarded)
   3  Processor consumed vs sent by image (conservation)              timeseries  A
   4  Orchestrator sends minus processor pickups (structural…)        stat        C
   5  Per-processor dispatch gap                                      timeseries  A
   6  Orchestrator unresolved steps in range                          stat        B (guarded)
   7  Processor dropped spawns in range                               stat        B (guarded)
   8  Keeper consumed - sent gap in range                             stat        B (guarded)
   9  Keeper L2 probe heartbeat                                       timeseries  A (unguarded by design)
  10  WebApi request rate by route                                    timeseries  A
  11  WebApi p95 request duration by route                            timeseries  A
  12  WebApi status-code mix                                          timeseries  A
  13  WebApi 5xx ratio                                                stat        B (guarded)
  14  WebApi in-flight requests and Kestrel connections               timeseries  A (gauges)

Scenario table row schema (a caller supplies only the ID; nothing is derived from the parameter):
  scenarioId, mode, lever, targetTier, seamTier, seamVar, dwellSeconds,
  panelIds[], predictedDirection{panelId->'up'|'down'|'nonzero'|'nodata'|'slope-up'},
  crossTalkPanels[] (MUST be non-empty), notes

Rows (levers marked [PQ-nn] are set from 88-PROBE-DECISIONS.md, not assumed):
  BASE-01    baseline   lever none   panels 1-14                       crossTalk n/a (baseline mode)
  WEB-01     scenario   lever http   panels 10,11,12,14  dwell 180     crossTalk 1,3,9
  WEB-02     scenario   lever http   panel 13            dwell 120     crossTalk 1,3,9,10   [PQ-03]
  SCALE-01   scenario   lever scale  tier processor-sample  panels 3,4,5  dwell 420
                                                                       crossTalk 9,10,12,14
  SCALE-02   scenario   lever scale  tier orchestrator      panel 1     dwell 240
                                                                       crossTalk 9,10,12
  SCALE-03   scenario   lever scale  tier keeper            panel 9     dwell 240
                                                                       crossTalk 1,3,10,12
  ZERO-01    scenario   lever data   panel 6                            crossTalk 9,10,12,13 [PQ-05]
  ZERO-02    scenario   lever scale|seam  panel 2          dwell 90     crossTalk 9,10,12     [PQ-01]
  ZERO-03    scenario   lever seam   seamTier keeper  seamVar KEEPER_DEFEAT_REINJECT
                                     panel 8            dwell 90        crossTalk 6,7,13,10,12 [PQ-06]
  LADDER-01  ladder     lever scale+http  panels 3,4,14                 crossTalk 9,12

Predicted directions:
  1 down · 2 nonzero · 3 down · 4 slope-up · 5 up-then-nodata · 6 nonzero · 7 n/a (accepted
  unproven, user-locked) · 8 nonzero · 9 nodata · 10 up · 11 up · 12 up · 13 nonzero · 14 up

Regime C handling (panel 4), resolved here so no later plan re-litigates it:
  The panel's expression is `increase(...[$__range])`, so its value depends on the VISIBLE window
  width. At this phase's fixed 60-second sub-window the reading is a per-minute increase and is
  banded exactly like a Regime-A value. SEPARATELY, one wide (1 hour) capture is taken at baseline
  and at after and recorded as `RangeCumulativeLevelBaseline` / `RangeCumulativeLevelAfter` — that
  is the number an operator actually sees on the shipped dashboard, it grows on a healthy stack
  too, and it is recorded for the HAND-02 misleading-by-default list. It is NEVER scored as
  movement; applying a Regime-A band to it would be a guaranteed false positive.

Artifact schema (single-proof, `analyzer-reports/phase-83-ha.json` shape: one object, PascalCase,
ScenarioId first, Verdict second, claims, then measured scalars and "o" timestamps, HumanSummary
last). Baseline-mode fields:
  ScenarioId, Verdict, AllPanelsRead, AllBandsComputed, StackCleanAtStart,
  BaselineWindowStart, BaselineWindowEnd, SubWindowSeconds, SubWindowCount,
  ViewportWidth, ViewportHeight, RateIntervalSeconds, DatasourceTimeInterval,
  OldestSampleUtc,
  BaselineBands[] one entry per panel: { PanelId, PanelTitle, Regime, SeriesName, Values[],
     States[], BandLow, BandHigh, BandMean, BandSigma, FloorApplied, PanelState },
  RangeCumulativeLevelBaseline, DiagnosticQueryValues[], DiagnosticAgreesWithPanel,
  ScreenshotPaths[], UnevaluablePanels[], HumanSummary

Exit codes: as plan 88-03's table, plus 66 = a panel could not be read at all in baseline mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver frame, static scenario table, and the unknown-id guard</name>
  <files>scripts/phase-88-panel-discriminate.ps1</files>
  <read_first>
    - scripts/phase-88-wave0-probe.ps1 (the frame and exit-code table just proven live — reuse its PRE gate, forward, teardown and proxy helpers rather than re-deriving them)
    - scripts/phase-87-dashboards-verify.ps1 lines 24-86 (comment-based help STEP flow + EXIT-CODE TABLE) and lines 126-200 (harness frame, forward helpers)
    - scripts/phase-80-harness.ps1 lines 110-175 (the static `[ordered]@{}` scenario table + unknown-id guard at :121-136, and `$TierKind` at :143-147 which is still correct; the `$TierReplicas` map at :148-150 is stale and must NOT be reintroduced)
    - scripts/lib/phase-88-cluster-ops.ps1 and scripts/lib/phase-88-panel-read.ps1 (the two libraries this driver composes)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md (the Locked/Dropped/Blocked status and the resolved lever for every probe-gated scenario)
    - k8s/dashboards/business.json (all fourteen panel ids, titles, types and `or vector(0)` guards — the source of the regime column)
  </read_first>
  <action>
    Create `scripts/phase-88-panel-discriminate.ps1` with parameters
    `-ScenarioId <string>`, `-Mode <Baseline|Scenario|DurationLadder>`, `-SkipTraffic`,
    and the standard repo frame: `$ErrorActionPreference = 'Stop'`,
    `Set-StrictMode -Version Latest`, `Push-Location $repoRoot`, whole-body try/finally, a prefixed
    `Write-Phase "[phase-88-panel-discriminate] ..."` helper, and a comment-based help block with a
    lettered STEP flow and the EXIT-CODE TABLE from `<interfaces>` (one row per code, including the
    new 66).

    Declare before the `try` so the outer `finally` can read them under StrictMode:
    `$gfForwardPid = 0`, `$seamArmed = $false`, `$seamTier = ''`, `$seamVar = ''`,
    `$scaledTier = ''`, `$replicasBefore = -1`, `$probeWorkflowId = ''`.
    Dot-source `scripts/lib/exit-code-resolution.ps1`, `scripts/lib/phase-88-panel-read.ps1`,
    `scripts/lib/phase-88-cluster-ops.ps1`.

    Declare `$Panels` as a static `[ordered]@{}` keyed by panel id carrying Title, Type, Regime and
    SeriesName for every one of the fourteen panels exactly as listed in `<interfaces>` — titles
    copied verbatim from `k8s/dashboards/business.json`, since a title mismatch would break the
    headerwalk locator silently.

    Declare `$Scenarios` as a static `[ordered]@{}` with one row per scenario id from
    `<interfaces>`, each carrying the full row schema. Set every `[PQ-nn]`-marked lever from the
    resolved value recorded in `88-PROBE-DECISIONS.md`, and mark any scenario the decisions file
    recorded as Dropped or Blocked with a `status` field of `Dropped` / `Blocked` plus the reason —
    a dropped scenario must be visible in the table, not deleted from it, so the roll-up can show
    it as a row rather than as an absence.

    Guard: `if (-not $Scenarios.Contains($ScenarioId)) { Write-Phase "unknown scenario ..." 'Red';
    exit 64 }`. The id only SELECTS a fixed row; no tier, deployment, env-var name or panel id is
    ever derived from the parameter. Additionally refuse to run a scenario whose `status` is
    `Dropped` or `Blocked` with exit 64 and the recorded reason.

    Assert at authoring time, in the source, that every scenario row of mode `scenario` has a
    non-empty `crossTalkPanels` list — a scenario with an empty cross-talk list is a defect by
    DISC-03, so implement it as a startup validation loop that exits 64 naming the offending row.

    Port the PRE gate, the loopback-only Grafana forward with the recycled-PID teardown, the
    concurrency/rest-state check and the diagnostic proxy helpers over from
    `scripts/phase-88-wave0-probe.ps1`. Never read or write `.k8s-portforward-pids`.
  </action>
  <acceptance_criteria>
    - `pwsh -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content scripts/phase-88-panel-discriminate.ps1 -Raw)); 'parse ok'"` prints `parse ok`
    - `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId NOPE-99` exits 64 and prints the known-scenario list
    - The source contains a row for each of BASE-01, WEB-01, WEB-02, SCALE-01, SCALE-02, SCALE-03, ZERO-01, ZERO-02, ZERO-03, LADDER-01
    - The source contains all fourteen panel titles verbatim as they appear in `k8s/dashboards/business.json`
    - Every scenario row of mode `scenario` declares a non-empty `crossTalkPanels` list; the startup validation loop exists in source
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'TierReplicas'` returns NO match
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'apply -k'` returns NO match
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch '.k8s-portforward-pids'` returns NO match
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch '0.0.0.0'` returns NO match
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch '--address'` MATCHES
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'Resolve-AnalyzerExitCode'` MATCHES
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$p='scripts/phase-88-panel-discriminate.ps1'; $null=[ScriptBlock]::Create((Get-Content $p -Raw)); pwsh -NoProfile -File $p -ScenarioId 'NOPE-99' *> $null; if($LASTEXITCODE -ne 64){ throw ('unknown-id guard returned '+$LASTEXITCODE+', expected 64') }; foreach($s in 'BASE-01','WEB-01','WEB-02','SCALE-01','SCALE-02','SCALE-03','ZERO-01','ZERO-02','ZERO-03','LADDER-01'){ if(-not (Select-String -Path $p -SimpleMatch $s -Quiet)){ throw \"scenario row missing: $s\" } }; $dash=Get-Content k8s/dashboards/business.json -Raw | ConvertFrom-Json; $src=Get-Content $p -Raw; foreach($pn in $dash.panels){ if($pn.type -eq 'row'){ continue }; if($src -notmatch [regex]::Escape($pn.title)){ throw ('panel title missing from driver: '+$pn.title) } }; foreach($s in @('TierReplicas','apply -k','.k8s-portforward-pids','0.0.0.0','docker compose')){ if(Select-String -Path $p -SimpleMatch $s -Quiet){ throw \"forbidden token: $s\" } }; foreach($s in @('--address','Resolve-AnalyzerExitCode','crossTalkPanels')){ if(-not (Select-String -Path $p -SimpleMatch $s -Quiet)){ throw \"missing: $s\" } }; 'driver frame ok'"</automated>
  </verify>
  <done>The driver parses, rejects an unknown scenario id with exit 64, carries all ten scenario rows and all fourteen verbatim panel titles, validates that every scenario declares a non-empty cross-talk list, and reintroduces none of the phase's forbidden tokens.</done>
</task>

<task type="auto">
  <name>Task 2: -Mode Baseline — pinned-window capture, per-regime banding, artifact writer</name>
  <files>scripts/phase-88-panel-discriminate.ps1</files>
  <read_first>
    - scripts/phase-88-panel-discriminate.ps1 (the frame and tables just authored)
    - scripts/lib/phase-88-panel-read.ps1 (Get-PinnedWindowSeries, Invoke-PanelReadBatch, Get-PanelSamples, Get-PanelStates, Get-PanelBand — the exact return shapes)
    - scripts/phase-87-dashboards-verify.ps1 lines 300-330 (the `$__rate_interval` derivation from the live datasource `timeInterval`, never hardcoded) and lines 820-922 (verdict precedence — Fail beats Inconclusive — and the write-artifact-BEFORE-resolving-the-exit-code order)
    - analyzer-reports/phase-83-ha.json (the single-proof artifact schema: one object, PascalCase, ScenarioId first, Verdict second, claims, measured scalars, "o" timestamps, HumanSummary last)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md § Baseline Banding (:242-283), Pitfall 1 (:451-455), Pitfall 6 (:479-482), Pitfall 9 (:494-496)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PATTERNS.md § artifact depth caution (:385-394)
  </read_first>
  <action>
    Implement `-Mode Baseline` in `scripts/phase-88-panel-discriminate.ps1`.

    STEP: verify the stack is at rest before measuring — all four tiers Ready at their declared
    replica counts, zero `DEFEAT` / `REINJECT_DELAY` env vars — and set `StackCleanAtStart`. A
    baseline taken while a seam is armed or a tier is degraded is worse than no baseline.

    STEP: drive steady-state traffic using the Phase-87 shape (seeder, STATIC string-literal
    `psql -c` lookup of `v8-fanout-proof`, `POST /api/v1/orchestration/start` requiring 204) and
    hold it for the whole capture, unless `-SkipTraffic` is given — in which case the Class-A
    panels cannot be banded honestly, so the verdict degrades to Inconclusive and the run exits 2.
    Also drive a light, steady host HTTP load against `http://localhost:8080` non-health routes for
    the duration, so panels 10-14 have a real idle-plus-traffic baseline rather than an empty one.

    STEP: derive `$__rate_interval` from the live datasource `timeInterval` with
    `max(4 * timeInterval, step + timeInterval)` and record `RateIntervalSeconds` and
    `DatasourceTimeInterval`. Do not hardcode 240. Record `OldestSampleUtc` from the diagnostic
    proxy so the artifact states its own evidentiary horizon.

    STEP: build the capture with `Get-PinnedWindowSeries -SubWindowSeconds 60 -Count 10` ending at
    a settled moment at least 150 seconds after traffic began (two export cadences), then call
    `Invoke-PanelReadBatch` ONCE for all fourteen panel ids across all ten sub-windows, with the
    LocatorMode recorded in `88-PROBE-DECISIONS.md`, a pinned 1920x1080 viewport and an absolute
    `-ScreenshotDir` under `analyzer-reports/phase-88-screenshots/BASE-01/`. Every sub-window must
    be the same width — the legend Mean is computed over the visible range, so a sliding or
    unequal-width window would make the baseline and every later after-capture incomparable, which
    is the phase's first pitfall. If the batch returns `Truncated` or `Unreadable`, do not band on
    a partial set: record the affected panels in `UnevaluablePanels[]` and degrade the verdict to
    Inconclusive.

    STEP: for each panel compute the band with `Get-PanelBand` over its ten samples and record
    `BaselineBands[]` per the schema, including `Values[]` and `States[]` so a reader can recompute
    the band. For a timeseries panel with multiple legend series, emit one `BaselineBands` entry
    per series with its `SeriesName`. Regime B panels are expected to band at exactly Low 0 /
    High 0 — that is the strongest form of discrimination available and no statistics are needed.
    Panel 9's band must be bound by the ±10 % floor rather than by 3σ; assert `FloorApplied` is
    true for it and record it, because its 3σ is roughly 0.0006 and any trivial perturbation would
    breach an unfloored band.

    STEP: take ONE additional wide capture for panel 4 — a single 1-hour absolute window at the
    same viewport — and record it as `RangeCumulativeLevelBaseline`. Annotate in the artifact that
    it is recorded for the HAND-02 misleading-by-default list and is never scored as movement.

    STEP: record a diagnostic proxy `query_range` value beside each panel's rendered value as
    `DiagnosticQueryValues[]` with a `DiagnosticAgreesWithPanel` flag. The proxy value is DIAGNOSIS
    only and never the verdict; where it disagrees with the rendered panel, that disagreement is a
    finding to record loudly, not an error to reconcile away.

    STEP: write `analyzer-reports/phase-88-BASE-01.json` with `ConvertTo-Json -Depth 10` and
    `Set-Content -Encoding utf8` BEFORE resolving the exit code, then resolve the exit code from
    the SAME in-memory object via the dot-sourced `Resolve-AnalyzerExitCode`. Verdict: Pass when
    every panel was read and banded; Fail when a claim was evaluated false; Inconclusive when a
    panel could not be read at all (that is an unevaluated assertion, not a defect) — Fail beats
    Inconclusive. After writing, assert the file contains no literal `System.Object[]`, which is
    the signature of an insufficient serialisation depth.
  </action>
  <acceptance_criteria>
    - `pwsh -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content scripts/phase-88-panel-discriminate.ps1 -Raw)); 'parse ok'"` prints `parse ok`
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'ConvertTo-Json -Depth 10'` MATCHES and `-SimpleMatch '-Depth 5'` returns NO match
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'System.Object[]'` MATCHES (the post-write depth assertion exists)
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'SubWindowSeconds 60'` MATCHES
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'RangeCumulativeLevelBaseline'` MATCHES
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'DiagnosticAgreesWithPanel'` MATCHES
    - `Select-String -Path scripts/phase-88-panel-discriminate.ps1 -SimpleMatch 'now-1h'` returns NO match
    - The artifact write occurs before the `Resolve-AnalyzerExitCode` call in source order
    - Every `psql -c` occurrence uses a double-quoted static literal containing no `$`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$p='scripts/phase-88-panel-discriminate.ps1'; $null=[ScriptBlock]::Create((Get-Content $p -Raw)); $t=Get-Content $p -Raw; foreach($s in @('ConvertTo-Json -Depth 10','System.Object[]','SubWindowSeconds 60','RangeCumulativeLevelBaseline','DiagnosticAgreesWithPanel','BaselineBands','FloorApplied')){ if(-not (Select-String -Path $p -SimpleMatch $s -Quiet)){ throw \"missing: $s\" } }; foreach($s in @('-Depth 5','now-1h','apply -k','TierReplicas')){ if(Select-String -Path $p -SimpleMatch $s -Quiet){ throw \"forbidden token: $s\" } }; $w=$t.IndexOf('Set-Content'); $r=$t.IndexOf('Resolve-AnalyzerExitCode ' + '$report'); if($w -lt 0){ throw 'no artifact write found' }; if($r -ge 0 -and $r -lt $w){ throw 'exit code resolved before the artifact was written' }; foreach($l in @(Select-String -Path $p -SimpleMatch 'psql')){ if($l.Line -match '-c ' -and $l.Line -notmatch '-c \"[^\"$]*\"'){ throw ('interpolated psql -c: '+$l.Line.Trim()) } }; 'baseline mode ok'"</automated>
  </verify>
  <done>Baseline mode is implemented with equal-width pinned sub-windows, per-regime banding, the separate wide-window panel-4 level, a diagnostic-only proxy cross-check, and a Depth-10 artifact written before the exit code is resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the live DISC-01 baseline for all fourteen panels</name>
  <files>analyzer-reports/phase-88-BASE-01.json</files>
  <read_first>
    - scripts/phase-88-panel-discriminate.ps1 (the complete baseline mode)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md (the LocatorMode and the RateIntervalPinned finding)
    - analyzer-reports/phase-88-wave0-probe.json (the probe's recorded viewport, rate interval and evidentiary horizon, for consistency)
  </read_first>
  <action>
    Confirm the stack is at rest (all four tiers Ready at 1/2/3/2, zero seam vars, images at
    `:tags-const-1544`, nothing else running against `skp`), then run
    `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId BASE-01 -Mode Baseline` and
    capture the exit code.

    The run must produce `analyzer-reports/phase-88-BASE-01.json` with a `BaselineBands` entry for
    every one of the fourteen panels (multiple entries where a panel has multiple legend series),
    or name the panel in `UnevaluablePanels[]`. Verify by inspection of the artifact that:
    the five Regime-B panels (2, 6, 7, 8, 13) banded at exactly Low 0 / High 0; panel 9's band
    carries `FloorApplied` true; panel 4 has both a per-minute band and a separate
    `RangeCumulativeLevelBaseline`; `SubWindowSeconds` is 60 and `SubWindowCount` is 10;
    `ViewportWidth`/`Height` are 1920/1080; and `RateIntervalSeconds` equals the value the probe
    derived.

    If a Class-A panel bands at exactly zero across all ten sub-windows, that is a finding, not a
    band: it means the panel has no live signal to move away from and its scenario cannot
    discriminate. Record such a panel in `UnevaluablePanels[]` with the reason and carry it forward
    to the HAND-04 register rather than proceeding as though a zero band were a valid null
    hypothesis.

    Do not re-run the baseline to "get a better number". If a run is discarded, say why in the
    summary and record both runs' artifacts — a silently re-rolled baseline is unfalsifiable.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-BASE-01.json` exists, parses, and its `Verdict` is `Pass` or `Inconclusive`
    - `SubWindowSeconds` is 60, `SubWindowCount` is 10, `ViewportWidth` is 1920, `ViewportHeight` is 1080
    - `BaselineWindowEnd` minus `BaselineWindowStart` is exactly 600 seconds
    - Every panel id 1-14 appears in `BaselineBands[]` or in `UnevaluablePanels[]`
    - Panels 2, 6, 7, 8 and 13 each have `BandLow` 0 and `BandHigh` 0, or are named in `UnevaluablePanels[]`
    - Panel 9's band entry has `FloorApplied` true
    - `RangeCumulativeLevelBaseline` is present and non-null
    - `StackCleanAtStart` is true
    - The written file contains no literal `System.Object[]`
    - After the run, all four tiers still read 1/2/3/2 replicas and carry zero `DEFEAT`/`REINJECT_DELAY` env vars
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$f='analyzer-reports/phase-88-BASE-01.json'; $raw=Get-Content $f -Raw; if($raw -match 'System.Object\[\]'){ throw 'serialisation depth too shallow' }; $j=$raw | ConvertFrom-Json; if($j.Verdict -eq 'Fail'){ throw ('baseline verdict Fail: '+$j.HumanSummary) }; if(-not $j.StackCleanAtStart){ throw 'stack was not clean at baseline start' }; if($j.SubWindowSeconds -ne 60 -or $j.SubWindowCount -ne 10){ throw 'sub-window contract violated' }; if($j.ViewportWidth -ne 1920 -or $j.ViewportHeight -ne 1080){ throw 'viewport not pinned' }; $span=([datetime]$j.BaselineWindowEnd - [datetime]$j.BaselineWindowStart).TotalSeconds; if([math]::Abs($span-600) -gt 1){ throw ('baseline window is '+$span+'s, expected 600') }; $banded=@($j.BaselineBands | ForEach-Object { $_.PanelId }); $unev=@($j.UnevaluablePanels | ForEach-Object { \"$_\" }); foreach($id in 1..14){ if(($banded -notcontains $id) -and -not ($unev -match \"$id\")){ throw \"panel $id neither banded nor declared unevaluable\" } }; foreach($id in 2,6,7,8,13){ $b=@($j.BaselineBands | Where-Object { $_.PanelId -eq $id }); if($b.Count -gt 0){ foreach($e in $b){ if($e.BandLow -ne 0 -or $e.BandHigh -ne 0){ throw \"regime-B panel $id did not band at zero\" } } } }; $p9=@($j.BaselineBands | Where-Object { $_.PanelId -eq 9 }); if($p9.Count -gt 0 -and -not $p9[0].FloorApplied){ throw 'panel 9 band was not floor-bound' }; if($null -eq $j.RangeCumulativeLevelBaseline){ throw 'panel 4 wide-window level missing' }; foreach($tier in 'keeper','processor-sample'){ $e=(kubectl -n skp get deploy $tier -o jsonpath='{.spec.template.spec.containers[0].env}'); if(\"$e\" -match 'DEFEAT|REINJECT_DELAY'){ throw \"seam left armed on $tier\" } }; $exp=@{'orchestrator'=3;'keeper'=2;'processor-sample'=2;'baseapi-service'=1}; foreach($k in $exp.Keys){ $r=[int]((\"\"+(kubectl -n skp get deploy $k -o jsonpath='{.spec.replicas}')).Trim()); if($r -ne $exp[$k]){ throw \"replicas drifted: $k=$r\" } }; 'DISC-01 baseline captured'"</automated>
  </verify>
  <done>A DISC-01 baseline band exists for all fourteen panels (or the panel is explicitly declared unevaluable with a reason), captured over ten equal 60-second pinned sub-windows at a fixed 1920x1080 viewport, with the Regime-B zero bands and panel 9's floored band both verified, and the stack unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `-ScenarioId` parameter -> the static scenario table | The only caller-supplied value; it selects a row and nothing more. |
| Driver -> `kubectl` -> `skp` | Read-only in baseline mode (rest-state assertions), mutating in later modes. |
| Driver -> Grafana loopback forward -> browser | The verdict channel. |
| Driver -> Grafana datasource proxy | The diagnosis channel only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-13 | Tampering (V5 input validation) | `-ScenarioId` | mitigate | Static `[ordered]@{}` table with an unknown-id guard exiting 64, asserted by an actual `-ScenarioId NOPE-99` invocation. No tier, deployment, env-var name or panel id is derived from the parameter. A Dropped/Blocked scenario is also refused with exit 64 and its recorded reason. |
| T-88-12 | Spoofing / Repudiation | verdict channel | mitigate | The rendered panel is the verdict; the proxy `query_range` value is recorded beside it as `DiagnosticQueryValues` with a `DiagnosticAgreesWithPanel` flag, and a disagreement is recorded as a finding. This is the direct control against the Phase-87 defect where 30/30 Class A reported green against three panels rendering "No data". |
| T-88-17 | Repudiation | unfalsifiable baseline | mitigate | The artifact carries `Values[]`, `States[]`, window bounds, sub-window width and count, viewport and derived rate interval, so the band can be recomputed by a reader. A discarded run must be recorded, not silently re-rolled. |
| T-88-18 | Tampering | shallow JSON serialisation | mitigate | `ConvertTo-Json -Depth 10` with a post-write assertion that no literal `System.Object[]` appears; the repo's usual `-Depth 5` cannot carry `BaselineBands[]`'s nested value arrays and would silently truncate the evidence. |
| T-88-06 | Injection | `psql -c` in the traffic drive | mitigate | Static double-quoted string literal with no `$` interpolation, asserted per line. |
| T-88-04 | Information Disclosure | Grafana port-forward | mitigate | `--address 127.0.0.1`; `0.0.0.0` asserted absent. |
| T-88-05 | DoS | shared port-forward PID file | mitigate | `.k8s-portforward-pids` asserted absent. |
| T-88-02 | Tampering / DoS | replica restore | mitigate | `TierReplicas` asserted absent from the driver; all restores go through `Invoke-TierScaleFault`'s live-read path. |
| T-88-10 | Tampering | stale `:local` images | mitigate | `apply -k` asserted absent; the post-run check re-reads all four tiers' replica counts. |
| T-88-SC | Tampering | npm / pip / cargo installs | accept | No package-manager install occurs. Legitimacy gate not applicable. |
</threat_model>

<verification>
- `[ScriptBlock]::Create` parse check passes on the driver.
- `-ScenarioId NOPE-99` exits 64.
- `analyzer-reports/phase-88-BASE-01.json` bands all fourteen panels (or declares them unevaluable) over a 600-second window of ten equal 60-second sub-windows at 1920x1080.
- No `System.Object[]` literal in the written artifact.
- `pwsh -File scripts/phase-87-dashboard-lint.ps1` still exits 0.
- All four tiers read 1/2/3/2 with zero seam vars after the run.
</verification>

<success_criteria>
- Roadmap Success Criterion 1 is satisfied: every business-dashboard panel has a recorded healthy baseline band captured from a settled stack.
- The band is stated as a numeric interval plus the regime it belongs to, and is recomputable from the artifact.
- The scenario table every later plan selects from exists, is static, and refuses unknown or dropped ids.
- Panel 4's misleading monotone level is recorded separately from its banded per-minute value.
</success_criteria>

<output>
Create `.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-04-SUMMARY.md` when done.
</output>
