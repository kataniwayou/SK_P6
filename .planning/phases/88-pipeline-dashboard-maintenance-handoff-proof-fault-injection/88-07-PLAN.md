---
phase: 88-pipeline-dashboard-maintenance-handoff-proof-fault-injection
plan: 07
type: execute
wave: 6
depends_on: ["88-06"]
files_modified:
  - analyzer-reports/phase-88-ZERO-01.json
  - analyzer-reports/phase-88-ZERO-02.json
  - analyzer-reports/phase-88-ZERO-03.json
autonomous: true
requirements: [DISC-02, DISC-03, DISC-04, DISC-05, DISC-06]

must_haves:
  truths:
    - "Panel 2 has been observed non-zero during a real recovery event, with the keeper's reinject intact — consumed and sent both move and the gap stays 0."
    - "Panel 8 has been observed non-zero with the keeper's reinject suppressed — consumed moves, sent does not, and the guarded green 0 turns red."
    - "Panel 6 has been observed non-zero, or is recorded in the accepted-unproven register with the enumerated blocked routes and their observed statuses."
    - "The runtime-only seam mechanism was used exactly once, from a finally-guarded arm/disarm, and the stack was asserted byte-identical afterwards."
    - "The guarded zeros that should NOT have fired during each scenario are asserted to have stayed at zero, so a guard firing for the wrong reason is caught."
  artifacts:
    - path: "analyzer-reports/phase-88-ZERO-02.json"
      provides: "the DISC-04 proof that panel 2 goes non-zero in a real recovery event"
      contains: "LegendNamesAfter"
    - path: "analyzer-reports/phase-88-ZERO-03.json"
      provides: "the DISC-04 + DISC-05 proof that panel 8 goes non-zero with the reinject suppressed, and the stack restored"
      contains: "SeamVarsAfter"
    - path: "analyzer-reports/phase-88-ZERO-01.json"
      provides: "the DISC-02 proof for panel 6, or its accepted-unproven record"
      contains: "PanelId"
  key_links:
    - from: "ZERO-03"
      to: "src/Keeper/Recovery/ReinjectConsumer.cs KEEPER_DEFEAT_REINJECT"
      via: "kubectl -n skp set env deployment/keeper KEEPER_DEFEAT_REINJECT=1 … then VAR-"
      pattern: "KEEPER_DEFEAT_REINJECT"
    - from: "each artifact"
      to: "the post-run live Deployment env"
      via: "Assert-StackRestored SeamVarsClean claim"
      pattern: "SeamVarsClean"
---

<objective>
Drive the three Regime-B guarded-zero panels off their floor. These are the panels the roadmap
singles out: they are `or vector(0)`-guarded, so they render a confident green `0` whether the
subsystem is idle or dead, and none has ever been observed otherwise. A panel that always reads the
same value is indistinguishable from a broken one.

This is also the only plan in the phase that arms a fault seam. Roadmap Success Criterion 5 names
the runtime-only arm/disarm mechanism as a deliverable, and this is where it is exercised against
a real recovery event and then proven to have left nothing behind.

Purpose: satisfy DISC-04 (panels 2 and 8 non-zero in a real recovery event), close DISC-02 for
panel 6, and demonstrate DISC-05 end to end.
Output: three scenario artifacts under `analyzer-reports/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PATTERNS.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-06-SUMMARY.md

<interfaces>
<!-- The three guarded-zero scenarios and the exact discrimination each must show. -->

ZERO-01  panel 6  Orchestrator unresolved steps in range   B  predicted: nonzero (0 -> >= 1, green -> red)
  Lever: whatever `88-PROBE-DECISIONS.md` recorded as `UnresolvedRouteChosen`. Do not re-decide it.
  Mechanism recap: `orchestrator_step_unresolved` increments once per entry in
  `selection.UnresolvedIds` — a next-step id present in a step's `NextStepIds` but ABSENT from the
  orchestrator's L1 step map (`StepAdvancement.SelectNext`). The increment site never throws and
  always acks, so this is the safest fault in the phase.
  crossTalk 9, 10, 12, 13

ZERO-02  panel 2  Keeper consumed vs sent (conservation)   B  predicted: nonzero
  Lever: whatever `88-PROBE-DECISIONS.md` recorded from PQ-01 — `scale` (processor-sample scale-0
  during traffic, forcing the orchestrator's RelocateTail to send to keeper-recovery) if keeper
  recovery traffic was observed, otherwise `seam` with `PROCESSOR_DEFEAT_READ` on processor-sample
  and `KEEPER_DEFEAT_REINJECT` LEFT UNSET so the keeper consumes AND reinjects.
  TWO independent signals, both asserted:
    (a) numeric — the `consumed` series Mean rises above the zero band;
    (b) legend name — while the counter does not exist the `or vector(0)` guard supplies a
        label-less series that `legendFormat: "consumed {{source}}"` renders as the literal
        `consumed ` with a trailing space; once the counter exists the legend reads
        `consumed keeper`. Assert the NAME change as well as the number.
  DISC-04 also requires that panel 8's gap stays 0 throughout this scenario — the keeper consumed
  AND sent, so there is nothing to gap. Panel 8 is therefore a cross-talk control here.
  crossTalk 8, 9, 10, 12

ZERO-03  panel 8  Keeper consumed - sent gap in range      B  predicted: nonzero (0 -> >= 1, green -> red)
  Lever: seam. `KEEPER_DEFEAT_REINJECT=1` on `deployment/keeper`, then the same recovery trigger
  ZERO-02 used. Scaling the keeper to 0 does NOT produce a gap — a keeper that is not running
  consumes nothing, so consumed and sent both stay 0 and the difference stays 0. The gap requires
  the keeper to CONSUME and NOT SEND, which is exactly what the seam does. This is why the seam
  mechanism is a roadmap deliverable rather than a convenience.
  crossTalk 6, 7, 13, 10, 12  — the other guarded zeros must stay at zero, which is what catches a
  guard firing for the wrong reason.
  `KEEPER_REINJECT_DELAY_MS` is NOT used: it is uncommitted working-tree-only, the deployed
  `keeper:tags-const-1544` image may not contain it, and this scenario does not need it.

Sequencing: ZERO-01, then ZERO-02, then ZERO-03, strictly sequential with the stack asserted at
rest between each. ZERO-03 must run last because it is the only scenario that arms a seam, and a
seam leak would be indistinguishable from a genuine conservation defect in anything that followed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ZERO-01 — drive panel 6 off its guarded zero, or record it accepted-unproven</name>
  <files>analyzer-reports/phase-88-ZERO-01.json</files>
  <read_first>
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md (the PQ-05 record: `UnresolvedRouteChosen`, `UnresolvedRouteApiDanglingEdgeStatus`, `UnresolvedRouteL2StepKeyViable`, and ZERO-01's Locked or Dropped status)
    - scripts/phase-88-panel-discriminate.ps1 (the ZERO-01 row and the Dropped-status guard)
    - src/Orchestrator/Dispatch/StepAdvancement.cs lines 25-63 (`SelectNext` files an unresolvable next-step id into `UnresolvedIds`)
    - src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs lines 175-190 (stage-3 increments `orchestrator_step_unresolved`; graceful, never throws, always acks; deliberately sits AFTER the clean-absent gate)
    - src/Orchestrator/Hydration/WorkflowLifecycle.cs lines 68-102 (the L1 hydration BFS and the business-skip for an absent L2 step key)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (the Step key shape `skp:{workflowId:D}:{stepId:D}`)
    - analyzer-reports/phase-88-BASE-01.json (panel 6's zero band and the cross-talk bands for 9, 10, 12, 13)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md § Panel 6 note (:151-152) — the ghost-cron hazard
  </read_first>
  <action>
    Branch on the recorded probe answer — do not re-decide it.

    If `UnresolvedRouteChosen` is `l2-step-key` or `api-dangling-edge`: assert the stack is at rest,
    then run `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId ZERO-01 -Mode Scenario`.
    The scenario must operate on a SEPARATE workflow it seeds itself and must never mutate
    `v8-fanout-proof`. It must STOP that workflow through the API BEFORE deleting any row or key,
    and it must delete the workflow it created when done — deleting workflow rows while the
    orchestrator is running leaves an orphaned Quartz cron firing forever and breaks later
    conservation checks with `Missing=0`. If any hand cleanup is unavoidable, flush Redis and
    restart the orchestrator, and record that in the artifact.
    Assert panel 6 leaves its zero band for at least two consecutive 60-second samples and turns
    from green to red. Record `ScenarioWorkflowId` and `ScenarioWorkflowCleanedUp` as artifact
    fields, and assert the cleanup from the outer `finally`.

    If `UnresolvedRouteChosen` is `none`: do NOT invent a fault and do NOT make a `src/` change.
    Write a degraded-row artifact at `analyzer-reports/phase-88-ZERO-01.json` in the same schema
    with `Verdict` `Inconclusive`, `Moved` false, panel 6's observed guarded zero in
    `PanelResults[]`, and an `AcceptedUnprovenReason` naming both enumerated routes with their
    observed evidence: the API dangling-edge route's actual HTTP status (the D-08 missing-step gate
    in `CycleDetector` rejects a child referenced via `NextStepIds` but absent from the graph), and
    why the L2-step-key route was not viable. Add the log-corroboration route maintenance should
    use instead: a non-zero panel 6 corresponds to the orchestrator's
    `Dangling next-step id {NextStepId} — skipping (business)` log line in the same window.

    In both branches, assert the cross-talk panels 9, 10, 12 and 13 stayed inside their bands. Panel
    13 is included deliberately: it is another guarded zero, and a guard that fires here would mean
    the guard is not measuring what its title claims.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-ZERO-01.json` exists and parses, in either branch
    - If the probe recorded a viable route: `Verdict` is `Pass`; panel 6's entry has `BandLow` 0, `BandHigh` 0, `Moved` true, `ConsecutiveSamplesOutside` at least 2, and at least one after value greater than or equal to 1; `ScenarioWorkflowCleanedUp` is true
    - If the probe recorded `none`: `Verdict` is `Inconclusive`, `Moved` is false, and `AcceptedUnprovenReason` names both routes, the observed API status, and the log line maintenance should search
    - In both branches `CrossTalkPanels[]` covers panel ids 9, 10, 12 and 13 with `StayedPut` true for each, and panel 13's `AfterValues[]` are all 0
    - In both branches `SeamVarsClean`, `ReplicasRestored` and `ImagesUnchanged` are true
    - `v8-fanout-proof` still exists and is still scheduled after the run (a `psql` count of workflows named `v8-fanout-proof` returns 1)
    - The written file contains no literal `System.Object[]`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$probe=Get-Content analyzer-reports/phase-88-wave0-probe.json -Raw | ConvertFrom-Json; $f='analyzer-reports/phase-88-ZERO-01.json'; $raw=Get-Content $f -Raw; if($raw -match 'System.Object\[\]'){ throw 'serialisation depth too shallow' }; $j=$raw | ConvertFrom-Json; foreach($c in 'SeamVarsClean','ReplicasRestored','ImagesUnchanged'){ if(-not $j.$c){ throw \"claim false: $c\" } }; if(\"$($probe.UnresolvedRouteChosen)\" -eq 'none'){ if($j.Verdict -ne 'Inconclusive'){ throw ('ZERO-01 dropped branch expected Inconclusive, got '+$j.Verdict) }; if($j.Moved){ throw 'dropped branch must not claim movement' }; if([string]::IsNullOrWhiteSpace(\"$($j.AcceptedUnprovenReason)\")){ throw 'AcceptedUnprovenReason empty' }; if(\"$($j.AcceptedUnprovenReason)\" -notmatch 'Dangling next-step id'){ throw 'reason does not name the log-corroboration route' } } else { if($j.Verdict -ne 'Pass'){ throw ('ZERO-01 expected Pass, got '+$j.Verdict) }; $r=@($j.PanelResults | Where-Object { $_.PanelId -eq 6 }); if($r.Count -eq 0){ throw 'panel 6 result missing' }; if($r[0].BandLow -ne 0 -or $r[0].BandHigh -ne 0){ throw 'panel 6 band was not the regime-B zero band' }; if(-not $r[0].Moved -or $r[0].ConsecutiveSamplesOutside -lt 2){ throw 'panel 6 did not move on two consecutive samples' }; if(@($r[0].AfterValues | Where-Object { $_ -ge 1 }).Count -lt 1){ throw 'panel 6 never reached 1' }; if(-not $j.ScenarioWorkflowCleanedUp){ throw 'scenario workflow was not cleaned up — ghost-cron hazard' } }; foreach($id in 9,10,12,13){ $ct=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq $id }); if($ct.Count -eq 0 -or -not $ct[0].StayedPut){ throw \"cross-talk failed or missing for panel $id\" } }; $ct13=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq 13 }); if(@($ct13[0].AfterValues | Where-Object { $_ -ne 0 }).Count -gt 0){ throw 'panel 13 left zero during ZERO-01 — a guard fired for the wrong reason' }; $wf=((kubectl -n skp exec statefulset/postgres -- psql -U postgres -d stepsdb -tA -c \"SELECT count(*) FROM workflows WHERE name = 'v8-fanout-proof'\") | Out-String).Trim(); if([int]$wf -ne 1){ throw \"v8-fanout-proof workflow count = $wf, expected 1\" }; 'ZERO-01 resolved'"</automated>
  </verify>
  <done>Panel 6 is either proven to leave its guarded zero from the rendered panel with the cross-talk guards holding, or recorded as a degraded row naming both blocked routes and the log-corroboration path — and `v8-fanout-proof` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run ZERO-02 — prove panel 2 goes non-zero in a real recovery event (DISC-04, first half)</name>
  <files>analyzer-reports/phase-88-ZERO-02.json</files>
  <read_first>
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md (the PQ-01 record fixing ZERO-02's lever as `scale` or `seam`)
    - scripts/phase-88-panel-discriminate.ps1 (the ZERO-02 row)
    - scripts/lib/phase-88-cluster-ops.ps1 (Invoke-TierScaleFault and, if the lever is `seam`, Set-Phase88Seam / Clear-Phase88Seam for `PROCESSOR_DEFEAT_READ` on processor-sample)
    - k8s/dashboards/business.json (panel 2's two guarded targets and their `legendFormat: "consumed {{source}}"` / `"sent {{source}}"` templates — the source of the legend-name signal)
    - analyzer-reports/phase-88-BASE-01.json (panel 2's zero band, its baseline legend names, and the cross-talk bands for 8, 9, 10, 12)
    - analyzer-reports/phase-88-wave0-probe.json (the `Panel2LegendNamesAfter` the probe already observed, for comparison)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md § Panel 2 note (:141-146)
  </read_first>
  <action>
    Assert the stack is at rest, then run
    `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId ZERO-02 -Mode Scenario` using
    the lever recorded in the decisions file.

    If the lever is `seam`, `PROCESSOR_DEFEAT_READ` is armed on `processor-sample` and
    `KEEPER_DEFEAT_REINJECT` is deliberately LEFT UNSET, so the keeper consumes and reinjects
    normally. Arming causes a rollout, so the sequence is arm → rollout status → settle at least
    150 s with traffic flowing → RE-CAPTURE the authoritative baseline → trigger → capture after →
    disarm from the outer `finally` → assert clean. `BaselineRecaptured` must be true and the
    pod-identity pair non-empty and disjoint.

    Assert BOTH panel-2 signals and record both:
    (a) numeric — the `consumed` series Mean leaves the zero band for at least two consecutive
        60-second samples;
    (b) legend name — `LegendNamesAfter[]` contains a name not present in
        `LegendNamesBaseline[]`, specifically the transition from the label-less guard series to a
        labelled `consumed keeper`. Record both arrays verbatim, including any trailing space, so a
        reader can see the transition rather than take it on trust.

    Cross-talk: panels 9, 10 and 12 must hold their bands, and panel 8 must stay at exactly 0. Panel
    8 staying at 0 is the DISC-04 half of this scenario: the keeper consumed AND sent, so there is
    no gap. If panel 8 moves here, either the reinject genuinely failed — which would be a real
    finding about the stack, not about the dashboard — or the panels are wired to the wrong
    metrics. Record it either way; do not reconcile it away.

    Record the diagnostic proxy value for `keeper_messages_consumed_total` beside the rendered
    value with `DiagnosticAgreesWithPanel`. A disagreement between the counter and the panel is a
    finding, and it is exactly the class of defect locked constraint 2 exists to expose.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-ZERO-02.json` exists, parses, and its `Verdict` is `Pass`
    - Panel 2's `PanelResults[]` entry has `BandLow` 0, `BandHigh` 0, `Moved` true, `ConsecutiveSamplesOutside` at least 2
    - `LegendNamesAfter[]` contains at least one entry absent from `LegendNamesBaseline[]`, and at least one after entry matches `consumed\s*\S+` (a labelled series, not the bare guard series)
    - `CrossTalkPanels[]` covers panel ids 8, 9, 10 and 12; `CrossTalkHeld` is true; panel 8's `AfterValues[]` are all 0
    - If the lever was `seam`: `BaselineRecaptured` is true, `RolloutOldInstanceIds[]` and `RolloutNewInstanceIds[]` are non-empty and disjoint, and `SeamVarsAfter[]` is empty
    - `SeamVarsClean`, `ReplicasRestored` and `ImagesUnchanged` are all true
    - `DiagnosticQueryValue` and `DiagnosticAgreesWithPanel` are both recorded
    - The written file contains no literal `System.Object[]`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$f='analyzer-reports/phase-88-ZERO-02.json'; $raw=Get-Content $f -Raw; if($raw -match 'System.Object\[\]'){ throw 'serialisation depth too shallow' }; $j=$raw | ConvertFrom-Json; if($j.Verdict -ne 'Pass'){ throw ('ZERO-02 verdict '+$j.Verdict+': '+$j.HumanSummary) }; foreach($c in 'SeamVarsClean','ReplicasRestored','ImagesUnchanged','CrossTalkHeld'){ if(-not $j.$c){ throw \"claim false: $c\" } }; if(@($j.SeamVarsAfter).Count -ne 0){ throw 'seam vars present after the scenario' }; $r=@($j.PanelResults | Where-Object { $_.PanelId -eq 2 }); if($r.Count -eq 0){ throw 'panel 2 result missing' }; $e=$r[0]; if($e.BandLow -ne 0 -or $e.BandHigh -ne 0){ throw 'panel 2 band was not the regime-B zero band' }; if(-not $e.Moved -or $e.ConsecutiveSamplesOutside -lt 2){ throw 'panel 2 did not move on two consecutive samples' }; $base=@($e.LegendNamesBaseline); $after=@($e.LegendNamesAfter); $new=@($after | Where-Object { $base -notcontains $_ }); if($new.Count -lt 1){ throw 'panel 2 legend names did not change — the guard signal was not captured' }; if(@($after | Where-Object { $_ -match 'consumed\s+\S' }).Count -lt 1){ throw 'panel 2 never gained a LABELLED consumed series' }; if(\"$($j.Lever)\" -eq 'seam'){ if(-not $j.BaselineRecaptured){ throw 'seam lever without a re-captured baseline' }; $old=@($j.RolloutOldInstanceIds); $nw=@($j.RolloutNewInstanceIds); if($old.Count -eq 0 -or $nw.Count -eq 0 -or @($nw | Where-Object { $old -contains $_ }).Count -gt 0){ throw 'rollout discontinuity not captured or overlapping' } }; foreach($id in 8,9,10,12){ $ct=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq $id }); if($ct.Count -eq 0 -or -not $ct[0].StayedPut){ throw \"cross-talk failed or missing for panel $id\" } }; $ct8=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq 8 }); if(@($ct8[0].AfterValues | Where-Object { $_ -ne 0 }).Count -gt 0){ throw 'panel 8 gap opened during ZERO-02 — the keeper did not reinject' }; if($null -eq $j.DiagnosticAgreesWithPanel){ throw 'diagnostic cross-check not recorded' }; 'ZERO-02 proven — panel 2 non-zero with the reinject intact'"</automated>
  </verify>
  <done>Panel 2 is proven non-zero during a real recovery event on both its numeric and its legend-name signal, panel 8's gap stayed at exactly 0 (the reinject worked), and any seam used was disarmed and asserted clean.</done>
</task>

<task type="auto">
  <name>Task 3: Run ZERO-03 — suppress the keeper reinject and prove panel 8's gap opens (DISC-04, second half; DISC-05 end to end)</name>
  <files>analyzer-reports/phase-88-ZERO-03.json</files>
  <read_first>
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md (the PQ-06 record: `SeamArmLanded`, `SeamDisarmClean`, and ZERO-03's Locked or Blocked status)
    - scripts/phase-88-panel-discriminate.ps1 (the ZERO-03 row)
    - scripts/lib/phase-88-cluster-ops.ps1 (Set-Phase88Seam / Clear-Phase88Seam / Assert-StackRestored and the finally-guarded disarm contract)
    - src/Keeper/Recovery/ReinjectConsumer.cs (line 104 — the `KEEPER_DEFEAT_REINJECT` suppress-send seam, committed in 7ec9169; line 55's `KEEPER_REINJECT_DELAY_MS` is uncommitted working-tree-only and must NOT be used)
    - k8s/dashboards/business.json (panel 8 `Keeper consumed - sent gap in range` and its `or vector(0)` guard)
    - analyzer-reports/phase-88-BASE-01.json (panel 8's zero band and the cross-talk bands for 6, 7, 13, 10, 12)
    - analyzer-reports/phase-88-ZERO-02.json (the recovery trigger that is known to work, and the legend-name evidence that the keeper consumed)
  </read_first>
  <action>
    This is the phase's only seam scenario and the last live scenario. Run it last, with the stack
    asserted at rest first: all four tiers Ready at 1/2/3/2, zero `DEFEAT`/`REINJECT_DELAY` env
    vars, images at `:tags-const-1544`.

    If `88-PROBE-DECISIONS.md` recorded ZERO-03 as Blocked, do not attempt it: write the degraded-row
    artifact with `Verdict` `Inconclusive` and an `AcceptedUnprovenReason` citing the probe's
    `SeamArmLanded` / `SeamDisarmClean` values, and escalate in the summary that roadmap Success
    Criterion 5 is at risk. Do not quietly defer it.

    Otherwise run
    `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId ZERO-03 -Mode Scenario`. The
    sequence is exactly: arm `KEEPER_DEFEAT_REINJECT=1` on `deployment/keeper` → `rollout status`
    → settle at least 150 s with traffic flowing → RE-CAPTURE the authoritative baseline → trigger
    the same recovery event ZERO-02 used → capture after → disarm with the trailing-hyphen form
    from the OUTER `finally` → `rollout status` → `Assert-StackRestored`.

    Assert panel 8 leaves its zero band upward for at least two consecutive 60-second samples and
    crosses its red threshold. Record the rendered value and the threshold colour state; the
    green-to-red transition is the operator-visible half of the claim and it is what the runbook
    row will cite.

    Cross-talk: panels 6, 7 and 13 must remain at exactly 0 and panels 10 and 12 must hold their
    bands. The three other guarded zeros staying at zero is what proves panel 8's guard fired for
    its own reason rather than because something global changed. Panel 7 in particular must stay at
    0 — it is the accepted-unproven panel and a movement here would contradict the user-locked
    decision not to drive it.

    The restore assertion is a first-class deliverable of this task, not a cleanup step: after the
    run, `SeamVarsAfter[]` must be empty, `SeamVarsClean`, `ReplicasRestored` and `ImagesUnchanged`
    must all be true, and an INDEPENDENT `kubectl` read must confirm zero `DEFEAT` and
    `REINJECT_DELAY` occurrences on both `keeper` and `processor-sample`. A left-armed seam would
    silently lose recovery messages in every later sweep and would present as an inexplicable
    conservation violation.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-ZERO-03.json` exists and parses
    - If ZERO-03 was Locked: `Verdict` is `Pass`; panel 8's entry has `BandLow` 0, `BandHigh` 0, `Moved` true, `ConsecutiveSamplesOutside` at least 2, and at least one after value greater than or equal to 1; `Lever` is `seam`
    - If ZERO-03 was Blocked: `Verdict` is `Inconclusive`, `Moved` is false, `AcceptedUnprovenReason` cites the probe's seam fields, and the summary escalates the SC-5 risk
    - `BaselineRecaptured` is true and `RolloutOldInstanceIds[]` / `RolloutNewInstanceIds[]` are non-empty and disjoint (arming caused a rollout)
    - `SeamVarsAfter[]` is empty; `SeamVarsClean`, `ReplicasRestored`, `ImagesUnchanged` are all true
    - `CrossTalkPanels[]` covers panel ids 6, 7, 13, 10 and 12; `CrossTalkHeld` is true; panels 6, 7 and 13 each have all `AfterValues[]` equal to 0
    - An independent `kubectl -n skp get deploy keeper` and `processor-sample` env read contains no `DEFEAT` and no `REINJECT_DELAY`
    - All four tiers read 1/2/3/2 replicas and their images are unchanged at `:tags-const-1544`
    - The written file contains no literal `System.Object[]`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$probe=Get-Content analyzer-reports/phase-88-wave0-probe.json -Raw | ConvertFrom-Json; $f='analyzer-reports/phase-88-ZERO-03.json'; $raw=Get-Content $f -Raw; if($raw -match 'System.Object\[\]'){ throw 'serialisation depth too shallow' }; $j=$raw | ConvertFrom-Json; foreach($c in 'SeamVarsClean','ReplicasRestored','ImagesUnchanged'){ if(-not $j.$c){ throw \"restore claim false: $c\" } }; if(@($j.SeamVarsAfter).Count -ne 0){ throw 'seam vars present after the scenario' }; $blocked = -not ($probe.SeamArmLanded -and $probe.SeamDisarmClean); if($blocked){ if($j.Verdict -ne 'Inconclusive'){ throw 'blocked branch must be Inconclusive' }; if([string]::IsNullOrWhiteSpace(\"$($j.AcceptedUnprovenReason)\")){ throw 'AcceptedUnprovenReason empty' } } else { if($j.Verdict -ne 'Pass'){ throw ('ZERO-03 verdict '+$j.Verdict+': '+$j.HumanSummary) }; if(\"$($j.Lever)\" -ne 'seam'){ throw ('ZERO-03 lever is '+$j.Lever+', expected seam') }; $r=@($j.PanelResults | Where-Object { $_.PanelId -eq 8 }); if($r.Count -eq 0){ throw 'panel 8 result missing' }; $e=$r[0]; if($e.BandLow -ne 0 -or $e.BandHigh -ne 0){ throw 'panel 8 band was not the regime-B zero band' }; if(-not $e.Moved -or $e.ConsecutiveSamplesOutside -lt 2){ throw 'panel 8 did not move on two consecutive samples' }; if(@($e.AfterValues | Where-Object { $_ -ge 1 }).Count -lt 1){ throw 'panel 8 gap never reached 1' }; if(-not $j.BaselineRecaptured){ throw 'seam scenario without a re-captured baseline' }; $old=@($j.RolloutOldInstanceIds); $nw=@($j.RolloutNewInstanceIds); if($old.Count -eq 0 -or $nw.Count -eq 0 -or @($nw | Where-Object { $old -contains $_ }).Count -gt 0){ throw 'rollout discontinuity not captured or overlapping' }; foreach($id in 6,7,13,10,12){ $ct=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq $id }); if($ct.Count -eq 0 -or -not $ct[0].StayedPut){ throw \"cross-talk failed or missing for panel $id\" } }; foreach($id in 6,7,13){ $ct=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq $id }); if(@($ct[0].AfterValues | Where-Object { $_ -ne 0 }).Count -gt 0){ throw \"guarded zero panel $id left zero during ZERO-03\" } } }; foreach($tier in 'keeper','processor-sample'){ $env=(kubectl -n skp get deploy $tier -o jsonpath='{.spec.template.spec.containers[0].env}'); if(\"$env\" -match 'DEFEAT|REINJECT_DELAY'){ throw \"SEAM LEFT ARMED on $tier — every later run is poisoned\" } }; $exp=@{'orchestrator'=3;'keeper'=2;'processor-sample'=2;'baseapi-service'=1}; foreach($k in $exp.Keys){ $rc=[int]((\"\"+(kubectl -n skp get deploy $k -o jsonpath='{.spec.replicas}')).Trim()); if($rc -ne $exp[$k]){ throw \"replicas drifted: $k=$rc\" }; $img=((\"\"+(kubectl -n skp get deploy $k -o jsonpath='{.spec.template.spec.containers[0].image}')).Trim()); if($img -notmatch 'tags-const-1544'){ throw \"image drifted: $k=$img\" } }; 'ZERO-03 resolved and stack byte-identical'"</automated>
  </verify>
  <done>Panel 8's guarded green zero is proven to turn red under a suppressed keeper reinject (or recorded as blocked with the SC-5 risk escalated), the other three guarded zeros stayed at zero, and the stack is independently verified byte-identical: zero seam vars, replicas 1/2/3/2, images at `:tags-const-1544`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Driver -> `kubectl set env` -> live keeper Deployment | The only fault-seam write in the phase; it changes the behaviour of a running production-shaped service. |
| Driver -> Postgres / Redis via `kubectl exec` | ZERO-01 seeds and removes a workflow and may remove one L2 projection key. |
| Restored cluster -> every later run and every later resilience sweep | A seam left armed is invisible until it presents as an inexplicable conservation violation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03 | Tampering | left-armed `KEEPER_DEFEAT_REINJECT` | mitigate | Disarm with the trailing-hyphen form from the OUTER `finally`, then `Assert-StackRestored`, then an INDEPENDENT `kubectl` env read in the task verify that fails on any `DEFEAT` or `REINJECT_DELAY` match. A false restore claim is Fail with exit 65, a code no verdict occupies. ZERO-03 is deliberately the last live scenario so a leak cannot confound anything that follows. |
| T-88-01 | Tampering | seam target selection | mitigate | `Set-Phase88Seam` validates the (tier, var) pair against the static `$Phase88Seams` allow-list; only `keeper`/`KEEPER_DEFEAT_REINJECT` and `processor-sample`/`PROCESSOR_DEFEAT_READ` are expressible. Nothing is derived from `-ScenarioId`. |
| T-88-15 | Tampering | uncommitted `KEEPER_REINJECT_DELAY_MS` | mitigate | Not used and not supported by the library. The deployed `keeper:tags-const-1544` image may not contain it, so depending on it would make the scenario's result unattributable. |
| T-88-11 | Tampering | ZERO-01 workflow lifecycle | mitigate | The scenario seeds its OWN workflow, never mutates `v8-fanout-proof`, stops through the API before any deletion, and asserts `ScenarioWorkflowCleanedUp` from the outer `finally`. The verify independently counts `v8-fanout-proof` rows. Deleting workflow rows with the orchestrator running leaves an orphaned Quartz cron firing forever. |
| T-88-20 | Repudiation | rollout mistaken for the fault | mitigate | Arming a seam causes a rollout, so `BaselineRecaptured` and a disjoint pod-identity pair are asserted for every seam scenario, and the scoring band is the post-settle re-capture. |
| T-88-21 | Repudiation | a guard firing for the wrong reason | mitigate | Each guarded-zero scenario asserts the OTHER guarded zeros stayed at exactly 0. Panel 7 in particular must stay at 0, since the user locked it as accepted-unproven and a movement there would contradict that decision. |
| T-88-12 | Spoofing / Repudiation | verdict channel | mitigate | The rendered panel decides; `DiagnosticQueryValue` and `DiagnosticAgreesWithPanel` are recorded beside it and a disagreement is a finding. Panel 2's legend-name change is asserted as an independent second signal so the numeric claim does not stand alone. |
| T-88-02 | Tampering / DoS | replica restore | mitigate | All scaling goes through the live-read path; `ReplicasRestored` is asserted and the verify re-reads all four tiers. |
| T-88-10 | Tampering | image drift | mitigate | The verify independently reads all four images and requires `:tags-const-1544`. `apply -k` is absent from every Phase-88 script. |
| T-88-18 | Tampering | shallow JSON serialisation | mitigate | Every artifact is checked for a literal `System.Object[]` after writing. |
| T-88-SC | Tampering | npm / pip / cargo installs | accept | No package-manager install occurs. Legitimacy gate not applicable. |
</threat_model>

<verification>
- Three artifacts exist; none has `Verdict` `Fail`.
- Panels 2 and 8 are each proven non-zero during a real recovery event (DISC-04), or panel 8 is recorded blocked with the SC-5 risk escalated.
- Panel 6 is proven or recorded with both enumerated routes and their observed evidence.
- Every scenario's cross-talk control held, and the guarded zeros not under test stayed at exactly 0.
- An independent `kubectl` read confirms zero `DEFEAT`/`REINJECT_DELAY` on `keeper` and `processor-sample`, replicas 1/2/3/2, and all four images at `:tags-const-1544`.
- `pwsh -File scripts/phase-87-dashboard-lint.ps1` still exits 0.
</verification>

<success_criteria>
- Roadmap Success Criterion 4 is satisfied: the keeper conservation panels have been seen non-zero during a real recovery event, panel 2 with the reinject intact and panel 8 with it suppressed.
- Roadmap Success Criterion 5 is demonstrated end to end: the runtime-only seam mechanism armed, drove a real fault, disarmed from a `finally`, and left the stack byte-identical — proven by an independent read, not by assumption.
- Panel 6 is proven or has an evidence-backed accepted-unproven record.
- No manifest was edited and no `src/` file was changed.
</success_criteria>

<output>
Create `.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-07-SUMMARY.md` when done.
</output>
