---
phase: 88-pipeline-dashboard-maintenance-handoff-proof-fault-injection
plan: 03
type: execute
wave: 2
depends_on: ["88-01", "88-02"]
files_modified:
  - scripts/phase-88-wave0-probe.ps1
  - analyzer-reports/phase-88-wave0-probe.json
  - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md
autonomous: true
requirements: [DISC-02, DISC-04, DISC-05, DISC-06, HAND-04]

must_haves:
  truths:
    - "Whether a plain processor-tier crash produces keeper recovery traffic in THIS cluster is a measured fact, not an assumption."
    - "The reader's primary DOM locator is chosen from evidence against the live Grafana 12.3.9 instance, for a stat panel and a table-legend timeseries panel independently."
    - "Panel 13's fate — proven by HTTP or entered into the accepted-unproven register — is decided from an enumerated, safe endpoint probe, not guessed."
    - "Panel 6's fault route is decided from evidence about which of the two candidate levers is actually reachable without a src/ change."
    - "The seam arm/disarm mechanism is proven to land on and leave a live pod BEFORE any scenario depends on it."
    - "The stack is provably clean when the probe finishes: zero seam vars, replicas restored to the pre-read counts, images unchanged."
    - "The artifact states its own evidentiary horizon — the oldest Prometheus sample available at run start."
  artifacts:
    - path: "scripts/phase-88-wave0-probe.ps1"
      provides: "the seven-question Wave-0 probe with one independently recorded answer per question"
      contains: "PQ-01"
    - path: "analyzer-reports/phase-88-wave0-probe.json"
      provides: "the probe answers the scenario plans branch on"
      contains: "KeeperRecoveryTrafficObserved"
    - path: ".planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md"
      provides: "the locked downstream branch decisions, each citing its probe field"
      contains: "PQ-01"
  key_links:
    - from: "scripts/phase-88-wave0-probe.ps1"
      to: "scripts/lib/phase-88-panel-read.ps1 and scripts/lib/phase-88-cluster-ops.ps1"
      via: "dot-source"
      pattern: "phase-88-(panel-read|cluster-ops)\\.ps1"
    - from: "88-PROBE-DECISIONS.md"
      to: "analyzer-reports/phase-88-wave0-probe.json"
      via: "each decision cites the probe field that determined it"
      pattern: "PQ-0[1-7]"
---

<objective>
Answer, against the live `skp` cluster, the seven questions whose answers change the structure of
every downstream scenario plan. This is the phase's genuinely blocking step: RESEARCH.md and
PATTERNS.md agree that three of these answers alter what the later plans must do, so the scenario
plans deliberately declare their levers as branches on this artifact rather than as assumptions.

Purpose: Phase 87's transferable lesson was that a proof which does not reproduce the caller's
shape proves only that the PromQL parses. The equivalent failure here would be locking eight
scenarios around assumptions A1-A4 and discovering mid-sweep that the keeper never receives
recovery traffic, or that `viewPanel` redirects with "Panel not found". Measuring first is cheaper
than replanning eight scenarios.
Output: `scripts/phase-88-wave0-probe.ps1`, `analyzer-reports/phase-88-wave0-probe.json`,
`88-PROBE-DECISIONS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PATTERNS.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-01-SUMMARY.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-02-SUMMARY.md

<interfaces>
<!-- The seven probe questions and the exact artifact field each must set. -->

  PQ-01  Does a plain `processor-sample` scale-0 during traffic produce
         `keeper_messages_consumed_total` at all in this cluster?
         -> KeeperRecoveryTrafficObserved (bool), KeeperConsumedSeriesCount (int),
            Panel2LegendNamesAfter (string[])
         Decides: panel 2's lever is `scale` (no seam) or `seam` (PROCESSOR_DEFEAT_READ).

  PQ-02  Does `?viewPanel=panel-<id>` work on this Grafana 12.3.9 instance, and does
         `getByTestId('data-testid panel content').innerText()` yield a parseable number for BOTH
         a stat panel (id 8) and a table-legend timeseries panel (id 9)?
         -> ViewPanelUrlWorks (bool), StatPanelParsed (bool), TimeseriesLegendParsed (bool),
            LocatorModeChosen ('viewpanel'|'headerwalk'), Panel8RawText, Panel9RawText
         Decides: the reader's primary locator for the whole phase.

  PQ-03  Is there any WebApi endpoint that returns 5xx on SAFE input?
         -> Safe5xxFound (bool), Safe5xxRoute (string|null), ProbedEndpoints[] each with
            {method, path, status}
         Decides: panel 13 is proven by HTTP, or joins the accepted-unproven register.

  PQ-04  Is `$__rate_interval` pinned at 240 s across a narrow and a wide window at a fixed
         1920x1080 viewport?
         -> DatasourceTimeIntervalSeconds (int), RateIntervalNarrowSeconds (int),
            RateIntervalWideSeconds (int), RateIntervalPinned (bool)
         Decides: whether one RateIntervalSeconds value can be stated for every artifact.

  PQ-05  Which route to an `orchestrator_step_unresolved` increment is reachable without a `src/`
         change?
         -> UnresolvedRouteApiDanglingEdgeStatus (int), UnresolvedRouteL2StepKeyViable (bool),
            UnresolvedRouteChosen ('api-dangling-edge'|'l2-step-key'|'none'),
            ProbeWorkflowId, ProbeWorkflowCleanedUp (bool)
         Decides: panel 6 is driven by data, or joins the accepted-unproven register.

  PQ-06  Does `kubectl set env` land `KEEPER_DEFEAT_REINJECT` on a live keeper pod, and does the
         disarm plus restore assertion leave the stack clean?
         -> SeamArmLanded (bool), SeamDisarmClean (bool), SeamRolloutOldPods[], SeamRolloutNewPods[]
         Decides: whether panel 8's scenario (the only seam scenario) can proceed at all.

  PQ-07  What is the oldest Prometheus sample available at run start?
         -> OldestSampleUtc (string, "o"), RetentionHoursObserved (double)
         Decides: nothing — it states the artifact's evidentiary horizon. Retention is ~12 h, so
         evidence must be captured during the run and cannot be re-queried the next day.

Probe artifact verdict semantics (deliberately NOT a verdict about the ANSWERS — a `false` answer
is a measurement, not a defect):
    Pass          all seven questions were ANSWERED and the stack was left clean
    Inconclusive  a question could not be evaluated at all (recorded in UnevaluableQuestions[])
    Fail          the stack was NOT left clean (Assert-StackRestored returned any false claim)

Exit codes (extending the Phase-87 table; the 0/1/2 classes stay owned by
scripts/lib/exit-code-resolution.ps1):
    0/1/2 verdict · 15 forward never carried traffic · 20 grafana db never ok · 50 traffic drive failed
    60 scale fault failed · 61 seam arm/disarm failed · 62 could not read live replicas/images
    63 panel reader unavailable (node / run.js / skill missing) · 64 usage or precondition error
    65 restore assertion failed (stack not left clean)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe frame, precondition gate, and the DOM/retention questions (PQ-02, PQ-04, PQ-07)</name>
  <files>scripts/phase-88-wave0-probe.ps1</files>
  <read_first>
    - scripts/phase-87-dashboards-verify.ps1 lines 1-200 (comment-based help with STEP flow + EXIT-CODE TABLE at :24-86, harness frame at :126-156, Start-GrafanaForward / Stop-GrafanaForward at :163-179, Wait-GrafanaHealthy at :184-197) and lines 200-330 (datasource-proxy helpers and the `$__rate_interval` arithmetic at :303-322 with ConvertFrom-GrafanaDuration)
    - scripts/phase-87-dashboards-verify.ps1 lines 364-400 (the PRE precondition gate: kube-context check with the pin-exit-before-trim idiom, shared-forward probe on http://localhost:8080/health/ready, exit 64 with a remediation line)
    - scripts/phase-80-collector-crash.ps1 lines 1-70 (the small fail-closed single-purpose live control shape, and the discipline of stating what a PASS does and does NOT prove)
    - scripts/lib/phase-88-panel-read.ps1 (the batch reader contract just authored: Invoke-PanelReadBatch, Get-PinnedWindowSeries, Get-PanelSamples, Get-PanelStates)
    - scripts/lib/exit-code-resolution.ps1 (Resolve-AnalyzerExitCode / Resolve-SweepClass — dot-source, never reimplement)
    - k8s/dashboards/business.json (panel 8 `Keeper consumed - sent gap in range`, a stat; panel 9 `Keeper L2 probe heartbeat`, a table-legend timeseries — the two PQ-02 subjects)
  </read_first>
  <action>
    Create `scripts/phase-88-wave0-probe.ps1` following the `phase-87-dashboards-verify.ps1` frame
    exactly: `$ErrorActionPreference = 'Stop'`, `Set-StrictMode -Version Latest`,
    `Push-Location $repoRoot` with a whole-body try/finally, a prefixed
    `Write-Phase "[phase-88-wave0-probe] ..."` trace helper, and a comment-based help block
    carrying a lettered STEP flow plus the EXIT-CODE TABLE from `<interfaces>` — one row per code,
    so an infra abort can never be read as a verdict.

    Declare BEFORE the `try`, so the outer `finally` can read them under StrictMode:
    `$gfForwardPid = 0`, `$seamArmed = $false`, `$probeWorkflowId = ''`.
    Dot-source `scripts/lib/exit-code-resolution.ps1`, `scripts/lib/phase-88-panel-read.ps1` and
    `scripts/lib/phase-88-cluster-ops.ps1`.

    PRE gate: assert `kubectl config current-context` equals `docker-desktop` (pin `$LASTEXITCODE`
    into a variable BEFORE the `.Trim()` — on a failed call stdout is empty, so a `.Trim()` on the
    raw capture must not be allowed to bypass the failure branch), assert
    `GET http://localhost:8080/health/ready` returns 200 with a remediation line naming
    `pwsh -File scripts/phase-80-up.ps1`, and add the concurrency check this phase needs: assert
    that `keeper`, `processor-sample`, `orchestrator` and `baseapi-service` each report
    `.status.readyReplicas` equal to `.spec.replicas` and carry zero `DEFEAT` / `REINJECT_DELAY`
    env vars at rest — a concurrent sweep or a seam left armed by an earlier run would confound
    every measurement. Any failure exits 64.

    STEP A: start this script's OWN grafana forward with `--address 127.0.0.1` and keep the PID in
    an in-memory variable only. Never create, read or write `.k8s-portforward-pids` — that file
    owns the eight shared tunnels and reading it for teardown would kill all eight and break a
    concurrent sweep. Gate on a Grafana health poll (exit 15 when the tunnel never carried traffic,
    exit 20 when it carried traffic but the database never reported ok). Tear down in the outer
    `finally` behind the recycled-PID guard.

    STEP B (PQ-07): read the oldest available sample through the Grafana datasource proxy at
    `/api/datasources/proxy/uid/skp-prometheus/api/v1/query_range` — the diagnosis channel the
    locked constraint permits — and record `OldestSampleUtc` and `RetentionHoursObserved`. State in
    the artifact that this is the run's evidentiary horizon.

    STEP C (PQ-04): read the provisioned datasource `timeInterval` and derive `$__rate_interval`
    with `max(4 * timeInterval, step + timeInterval)` for a narrow window (10 minutes) and a wide
    window (2 hours) at a fixed 1920x1080 viewport. Set `RateIntervalPinned` when both derive to
    the same value. Do not hardcode 240 — derive it, and record what was derived into
    `DatasourceTimeIntervalSeconds`, `RateIntervalNarrowSeconds`, `RateIntervalWideSeconds`.

    STEP D (PQ-02): build a 10-sub-window series with
    `Get-PinnedWindowSeries -SubWindowSeconds 60 -Count 10` ending at a settled moment, then call
    `Invoke-PanelReadBatch -PanelIds '8,9' -Windows <that series> -LocatorMode 'viewpanel'` with an
    absolute `-ScreenshotDir` under `analyzer-reports/phase-88-screenshots/` (run.js chdir's to the
    skill directory, so a relative path would land there). Set `StatPanelParsed` when panel 8
    yields a numeric `statValue` and `TimeseriesLegendParsed` when panel 9 yields at least one
    series row with a numeric mean; capture `Panel8RawText` / `Panel9RawText` verbatim. If the
    batch returns `State 'ReaderMissing'`, exit 63 — that is an infra abort, never a verdict. If
    the readings come back `PanelState 'Error'` or the raw text shows Grafana's "Panel not found",
    set `ViewPanelUrlWorks` false, retry the whole batch with `-LocatorMode 'headerwalk'` supplying
    the two panel titles, and set `LocatorModeChosen` from whichever mode produced parseable
    readings. Record `ScreenshotPaths[]`.
  </action>
  <acceptance_criteria>
    - `pwsh -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content scripts/phase-88-wave0-probe.ps1 -Raw)); 'parse ok'"` prints `parse ok`
    - The help block contains a row for each of exit codes 0 1 2 15 20 50 60 61 62 63 64 65
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch '--address'` MATCHES
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'Invoke-PanelReadBatch'` MATCHES
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch '0.0.0.0'` returns NO match
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch '.k8s-portforward-pids'` returns NO match
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'docker compose'` returns NO match
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'apply -k'` returns NO match
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'now-1h'` returns NO match
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'Resolve-AnalyzerExitCode'` MATCHES and no line matches `switch ($verdict)`
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -Pattern 'PQ-0[2478]'` MATCHES (the questions are named in the source)
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$p='scripts/phase-88-wave0-probe.ps1'; $null=[ScriptBlock]::Create((Get-Content $p -Raw)); $t=Get-Content $p -Raw; foreach($c in 0,1,2,15,20,50,60,61,62,63,64,65){ if($t -notmatch ('(?m)^\s*#?\s*'+$c+'\s+\S')){ throw \"exit-code table missing row: $c\" } }; foreach($s in @('--address','Resolve-AnalyzerExitCode','Invoke-PanelReadBatch')){ if(-not (Select-String -Path $p -SimpleMatch $s -Quiet)){ throw \"missing: $s\" } }; foreach($s in @('0.0.0.0','.k8s-portforward-pids','docker compose','apply -k','now-1h')){ if(Select-String -Path $p -SimpleMatch $s -Quiet){ throw \"forbidden token: $s\" } }; if(-not (Select-String -Path $p -Pattern 'PQ-0[2478]' -Quiet)){ throw 'probe question ids not named in source' }; 'probe frame ok'"</automated>
  </verify>
  <done>The probe frame parses, carries a complete exit-code table, owns a loopback-only forward, and answers PQ-02, PQ-04 and PQ-07 in source using the batch reader.</done>
</task>

<task type="auto">
  <name>Task 2: The cluster questions — keeper recovery, safe 5xx, unresolved-step route, seam smoke (PQ-01, PQ-03, PQ-05, PQ-06)</name>
  <files>scripts/phase-88-wave0-probe.ps1</files>
  <read_first>
    - scripts/phase-88-wave0-probe.ps1 (the frame just authored)
    - scripts/lib/phase-88-cluster-ops.ps1 (Invoke-TierScaleFault, Set-Phase88Seam, Clear-Phase88Seam, Assert-StackRestored — the exact return shapes and failure codes)
    - scripts/phase-87-dashboards-verify.ps1 lines 487-523 (the traffic drive: seeder -> STATIC-LITERAL psql wf-id lookup -> POST /api/v1/orchestration/start requiring 204 -> hold)
    - scripts/phase-80-harness.ps1 lines 110-175 (the static `[ordered]@{}` table + unknown-id guard shape at :121-136 — the T-81-01 input-validation control)
    - src/Orchestrator/Dispatch/StepAdvancement.cs lines 25-63 (`SelectNext`: a next-step id ABSENT from the L1 `steps` map lands in `UnresolvedIds`) and src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs lines 175-190 (stage-3 increments `orchestrator_step_unresolved`, graceful, never throws, deliberately sits AFTER the clean-absent gate)
    - src/Orchestrator/Hydration/WorkflowLifecycle.cs lines 68-102 (L1 hydration BFS reads `OrchestratorL2Keys.Step(workflowId, stepId)` from Redis; an absent step key is a business skip, leaving that step out of the L1 map)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (the key shapes: Step is `skp:{workflowId:D}:{stepId:D}`, hyphenated "D" GUIDs, prefix const `skp:`)
    - src/BaseApi.Service/Features/Orchestration/Validation/CycleDetector.cs lines 85-96 (the D-08 missing-step gate: a child referenced via NextStepIds but absent from the graph throws `OrchestrationValidationException.MissingStep` at activation — so the API dangling-edge route is EXPECTED to be blocked; recording the actual status is the point)
    - src/BaseApi.Service/Features/Workflow/WorkflowController.cs and src/BaseApi.Service/Features/Orchestration/OrchestrationController.cs (the real route templates behind the PQ-03 candidate list)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md § Panel-by-panel notes (:139-171) and § What NOT to drive (:172-178)
  </read_first>
  <action>
    Extend `scripts/phase-88-wave0-probe.ps1` with STEPs E-H and STEP J.

    STEP E (PQ-03) — safe 5xx enumeration. Declare a STATIC `[ordered]@{}` candidate table in the
    script; nothing is derived from a caller parameter and no route is discovered from a Swagger
    document (Swagger is dev-only in this stack and is not served under the k8s Production
    environment). Probe each candidate against `http://localhost:8080` and record
    `{method, path, status}` into `ProbedEndpoints[]`. The candidates are GET-only plus one inert
    POST: a well-formed but nonexistent workflow GUID on the workflow GET route; a malformed
    non-GUID on the same route; a nonexistent schema GUID; a nonexistent processor source-hash
    lookup; an unmatched path `/api/v1/__phase88_probe_unmatched` (expected 404 — this is also the
    404 driver panel 12 will need); and `POST /api/v1/orchestration/start` with a body of `[]`, an
    empty JSON array that starts nothing. Set `Safe5xxFound` and `Safe5xxRoute` from the first
    candidate observing a status in 500-599. This step MUST NOT scale redis or any other
    dependency: the Phase-86 hard readiness latch means a dependency outage costs a WebApi pod
    restart to recover, and this plan does not budget for it.

    STEP F (PQ-05) — unresolved-step route enumeration, non-destructively and against a SEPARATE
    workflow, never `v8-fanout-proof`.
    Route A: create a workflow whose step carries a `nextStepIds` entry pointing at a step id
    absent from the graph and attempt activation; record the HTTP status into
    `UnresolvedRouteApiDanglingEdgeStatus`.
    Route B: for a validly created and activated separate workflow, read (do not yet delete) the
    Redis L2 step key of a NON-ENTRY step at `skp:{workflowId}:{stepId}` via
    `kubectl -n skp exec statefulset/redis -- redis-cli GET <key>`, then determine whether that key
    survives a stop/start cycle through the API or is rewritten from the database — if a stop/start
    rewrites it, a deletion cannot survive long enough to produce an unresolved edge and the route
    is not viable. Set `UnresolvedRouteL2StepKeyViable` from that determination and
    `UnresolvedRouteChosen` to `api-dangling-edge`, `l2-step-key`, or `none`.
    Whatever this step creates it must STOP through the API BEFORE deleting anything, and it must
    delete the probe workflow it created: deleting workflow rows while the orchestrator runs leaves
    an orphaned Quartz cron firing forever and breaks later conservation checks with `Missing=0`.
    Record `ProbeWorkflowId` and `ProbeWorkflowCleanedUp`; the cleanup must also run from the outer
    `finally` when `$probeWorkflowId` is non-empty.

    STEP G (PQ-01) — keeper recovery traffic. Drive traffic using the Phase-87 STEP E shape
    (seeder, then a STATIC string-literal `psql -c` to resolve the `v8-fanout-proof` workflow id,
    then `POST /api/v1/orchestration/start` requiring 204), let it run, then call
    `Invoke-TierScaleFault -Tier 'processor-sample' -DwellSeconds 90`. After restore and settle,
    determine `KeeperRecoveryTrafficObserved` two independent ways and record BOTH: a diagnostic
    proxy query for the existence and value of `keeper_messages_consumed_total`
    (`KeeperConsumedSeriesCount`), and a panel-2 read through `Invoke-PanelReadBatch` capturing
    `Panel2LegendNamesAfter`. The legend name is itself a signal: while the counter does not exist
    the `or vector(0)` guard supplies a label-less series that `legendFormat: "consumed {{source}}"`
    renders as the literal `consumed ` with a trailing space; the moment the counter exists the
    legend reads `consumed keeper`. Record the raw names verbatim — do not trim them.

    STEP H (PQ-06) — seam smoke. `Set-Phase88Seam -Tier 'keeper' -Name 'KEEPER_DEFEAT_REINJECT'`,
    set `$seamArmed = $true` immediately on success, assert the variable is present on the live
    Deployment spec, capture `SeamRolloutOldPods` / `SeamRolloutNewPods` from the returned pod-name
    pair, then `Clear-Phase88Seam` and set `SeamArmLanded` / `SeamDisarmClean`. The disarm MUST
    also be issued unconditionally from the outer `finally` when `$seamArmed` is true, so an
    interrupted run still leaves the keeper clean. This step deliberately proves only that the
    mechanism lands and clears — it does NOT prove the deployed `keeper:tags-const-1544` image
    HONOURS the seam, which only a real recovery event can show and which plan 88-07 owns. Say so
    in the script's help text.

    STEP J — artifact and verdict. Build the report as one PascalCase object with `ScenarioId`
    first and `Verdict` second, one field per probe answer as listed in `<interfaces>`, plus
    `Assert-StackRestored`'s `SeamVarsClean` / `ReplicasRestored` / `ImagesUnchanged` claims and
    their mismatch arrays, `UnevaluableQuestions[]`, `ViewportWidth` / `ViewportHeight` /
    `RateIntervalSeconds`, `ScreenshotPaths[]`, and `HumanSummary` last. Verdict semantics are the
    ones in `<interfaces>`: Pass when all seven questions were answered and the stack is clean,
    Inconclusive when a question could not be evaluated, Fail when any restore claim is false —
    Fail beats Inconclusive, because a claim evaluated and returned false is positive evidence of a
    defect. Write the artifact with `ConvertTo-Json -Depth 10` (the repo's usual `-Depth 5` cannot
    survive this phase's nested arrays and would serialise them as `System.Object[]`) BEFORE
    resolving the exit code, then resolve the exit code from the SAME in-memory object via the
    dot-sourced `Resolve-AnalyzerExitCode`.
  </action>
  <acceptance_criteria>
    - `pwsh -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content scripts/phase-88-wave0-probe.ps1 -Raw)); 'parse ok'"` prints `parse ok`
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -Pattern 'PQ-0[1-7]'` matches for all seven ids
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'ConvertTo-Json -Depth 10'` MATCHES and `-SimpleMatch '-Depth 5'` returns NO match
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'scale statefulset'` returns NO match (no dependency outage is driven)
    - Every `psql -c` occurrence uses a double-quoted static literal containing no `$`
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'Clear-Phase88Seam'` matches at least twice (the happy path and the outer finally)
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'Assert-StackRestored'` MATCHES
    - `Select-String -Path scripts/phase-88-wave0-probe.ps1 -SimpleMatch 'v8-fanout-proof'` appears only on the traffic-drive lookup line, never on a DELETE or UPDATE line
    - A grep for each of KeeperRecoveryTrafficObserved, ViewPanelUrlWorks, Safe5xxFound, RateIntervalPinned, UnresolvedRouteChosen, SeamArmLanded, OldestSampleUtc, ProbeWorkflowCleanedUp matches
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$p='scripts/phase-88-wave0-probe.ps1'; $null=[ScriptBlock]::Create((Get-Content $p -Raw)); foreach($q in 1..7){ $id=('PQ-0'+$q); if(-not (Select-String -Path $p -SimpleMatch $id -Quiet)){ throw \"missing question: $id\" } }; foreach($s in @('ConvertTo-Json -Depth 10','Assert-StackRestored','KeeperRecoveryTrafficObserved','ViewPanelUrlWorks','Safe5xxFound','RateIntervalPinned','UnresolvedRouteChosen','SeamArmLanded','OldestSampleUtc','ProbeWorkflowCleanedUp')){ if(-not (Select-String -Path $p -SimpleMatch $s -Quiet)){ throw \"missing: $s\" } }; foreach($s in @('-Depth 5','scale statefulset','apply -k','docker compose','0.0.0.0','.k8s-portforward-pids')){ if(Select-String -Path $p -SimpleMatch $s -Quiet){ throw \"forbidden token: $s\" } }; if(@(Select-String -Path $p -SimpleMatch 'Clear-Phase88Seam').Count -lt 2){ throw 'disarm not present on both the happy path and the finally' }; foreach($l in @(Select-String -Path $p -SimpleMatch 'psql')){ if($l.Line -match '-c ' -and $l.Line -notmatch '-c \"[^\"$]*\"'){ throw ('interpolated psql -c: '+$l.Line.Trim()) } }; 'probe cluster questions ok'"</automated>
  </verify>
  <done>All seven questions are implemented and named in source; the seam disarm and the probe-workflow cleanup exist on both the happy path and the outer finally; no dependency outage or interpolated psql is present; the artifact writer uses Depth 10 and writes before resolving the exit code.</done>
</task>

<task type="auto">
  <name>Task 3: Run the probe live and lock the downstream branches in 88-PROBE-DECISIONS.md</name>
  <files>analyzer-reports/phase-88-wave0-probe.json, .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md</files>
  <read_first>
    - scripts/phase-88-wave0-probe.ps1 (the complete probe)
    - .planning/phases/87-grafana-observability-dashboards/87-FINDINGS.md lines 1-75 (the frontmatter + numbered-record + measured-evidence discipline this decisions record borrows)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md § Per-Panel Fault Mapping (:116-137) — the lever column each decision either confirms or overrides
    - .planning/ROADMAP.md lines 89-99 (the nine success criteria a blocked question would put at risk)
    - k8s/dashboards/business.json (the fourteen panel titles, needed verbatim if PQ-02 selects headerwalk)
  </read_first>
  <action>
    Run `pwsh -File scripts/phase-88-wave0-probe.ps1` against the live `skp` cluster and capture its
    exit code. Then author
    `.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md`
    applying this branch table MECHANICALLY — every decision cites the probe field that determined
    it, and no decision may be made on judgement where a field exists:

      PQ-01 KeeperRecoveryTrafficObserved true  -> scenario ZERO-02 (panel 2) lever = `scale`
            (processor-sample scale-0 during traffic), NO seam. This is the roadmap's stated
            preference for driving a panel by system state alone.
      PQ-01 false -> ZERO-02 lever = `seam`: `PROCESSOR_DEFEAT_READ` on processor-sample with
            `KEEPER_DEFEAT_REINJECT` left UNSET, so the keeper consumes AND reinjects. Record that
            ZERO-02 therefore gains an arm rollout plus a re-baseline (DISC-06).
      PQ-02 ViewPanelUrlWorks true and both parse flags true -> LocatorMode `viewpanel` for the
            whole phase.
      PQ-02 ViewPanelUrlWorks false -> LocatorMode `headerwalk`, and every later capture must pass
            PANEL_TITLES; record the fourteen panel titles verbatim in this file so no later plan
            has to rediscover them.
      PQ-02 TimeseriesLegendParsed false -> record a reader amendment for plan 88-04: per-series
            reads must additionally use `data-testid VizLegend series <name>` with positional row
            indexing, because the series NAME itself changes when a guard stops firing and cannot
            be the only locator.
      PQ-03 Safe5xxFound true -> scenario WEB-02 proves panel 13 via that route, named here with
            its method and path.
      PQ-03 false -> WEB-02 is DROPPED and panel 13 enters the HAND-04 accepted-unproven register
            with the reason "no endpoint returns 5xx on safe input; the dependency-outage route
            costs a WebApi pod restart under the Phase-86 hard readiness latch and is out of
            budget". Do NOT substitute the redis-outage route.
      PQ-04 RateIntervalPinned true -> one RateIntervalSeconds value is stated in every artifact.
            false -> each artifact records its own measured value and the equal-window-width rule
            is restated as load-bearing.
      PQ-05 UnresolvedRouteChosen -> scenario ZERO-01's lever, verbatim, with the concrete key or
            request shape it will use. `none` -> ZERO-01 is DROPPED and panel 6 enters the HAND-04
            register with the enumerated blocked routes and their observed statuses.
      PQ-06 SeamArmLanded and SeamDisarmClean both true -> scenario ZERO-03 (panel 8) proceeds.
            Either false -> ZERO-03 is BLOCKED, panel 8 enters the HAND-04 register, AND this is
            escalated in the file as a roadmap SC-5 risk rather than quietly deferred, because SC 5
            requires the mechanism to exist and to leave the stack byte-identical.

    The file carries YAML frontmatter in the `87-FINDINGS.md` style (`phase: 88`, `slug`, `date`,
    `status`, `requirements`, `evidence: analyzer-reports/phase-88-wave0-probe.json`), then one
    numbered record per question with the headings **Question** / **Measured answer** / **Decision**
    / **Consequence for the scenario plans**, then a closing table mapping each of the scenario ids
    BASE-01, WEB-01, WEB-02, SCALE-01, SCALE-02, SCALE-03, ZERO-01, ZERO-02, ZERO-03, LADDER-01 to
    `Locked` / `Dropped` / `Blocked` with the probe field that decided it.

    If the probe exits 65 (stack not left clean), STOP: do not author the decisions file as though
    the run were valid. Re-run the restore assertion, restore by hand, and re-run the probe. A
    dirty stack invalidates every subsequent measurement in the phase.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-wave0-probe.json` exists, parses with `ConvertFrom-Json`, and its `Verdict` is `Pass` or `Inconclusive` (never `Fail`)
    - Its `SeamVarsClean`, `ReplicasRestored` and `ImagesUnchanged` are all `true`
    - `kubectl -n skp get deploy keeper -o jsonpath='{.spec.template.spec.containers[0].env}'` contains no `DEFEAT` and no `REINJECT_DELAY`; the same holds for `processor-sample`
    - `kubectl -n skp get deploy orchestrator -o jsonpath='{.spec.replicas}'` reads 3, `keeper` reads 2, `processor-sample` reads 2, `baseapi-service` reads 1
    - Each of the seven answer fields named in `<interfaces>` is present and non-null in the artifact, or is listed in `UnevaluableQuestions[]`
    - `ProbeWorkflowCleanedUp` is true, or `ProbeWorkflowId` is empty (no probe workflow was created)
    - `88-PROBE-DECISIONS.md` exists, its frontmatter `evidence` field names the probe artifact, and it contains one numbered record per PQ id and a scenario status row for each of the ten scenario ids, each citing a `PQ-0n` field
    - `analyzer-reports/phase-88-screenshots/` contains at least the two PQ-02 screenshots
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$j=Get-Content analyzer-reports/phase-88-wave0-probe.json -Raw | ConvertFrom-Json; if($j.Verdict -eq 'Fail'){ throw ('probe verdict Fail: '+$j.HumanSummary) }; foreach($c in 'SeamVarsClean','ReplicasRestored','ImagesUnchanged'){ if(-not $j.$c){ throw \"restore claim false: $c\" } }; $un=@($j.UnevaluableQuestions); foreach($f in 'KeeperRecoveryTrafficObserved','ViewPanelUrlWorks','Safe5xxFound','RateIntervalPinned','UnresolvedRouteChosen','SeamArmLanded','OldestSampleUtc'){ if($null -eq $j.$f -and -not ($un -match $f)){ throw \"probe answer missing and not declared unevaluable: $f\" } }; $d='.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PROBE-DECISIONS.md'; if(-not (Test-Path $d)){ throw 'decisions file missing' }; $t=Get-Content $d -Raw; foreach($q in 1..7){ if($t -notmatch ('PQ-0'+$q)){ throw ('decisions file missing PQ-0'+$q) } }; foreach($s in 'BASE-01','WEB-01','WEB-02','SCALE-01','SCALE-02','SCALE-03','ZERO-01','ZERO-02','ZERO-03','LADDER-01'){ if($t -notmatch [regex]::Escape($s)){ throw \"decisions file missing scenario status: $s\" } }; foreach($tier in 'keeper','processor-sample'){ $e=(kubectl -n skp get deploy $tier -o jsonpath='{.spec.template.spec.containers[0].env}'); if(\"$e\" -match 'DEFEAT|REINJECT_DELAY'){ throw \"seam left armed on $tier\" } }; $exp=@{'orchestrator'=3;'keeper'=2;'processor-sample'=2;'baseapi-service'=1}; foreach($k in $exp.Keys){ $r=[int]((\"\"+(kubectl -n skp get deploy $k -o jsonpath='{.spec.replicas}')).Trim()); if($r -ne $exp[$k]){ throw (\"replicas not restored: $k=$r\") } }; 'wave-0 probe complete and stack clean'"</automated>
  </verify>
  <done>The probe ran, all seven questions are answered or explicitly declared unevaluable, the stack is provably clean (zero seam vars, replicas 1/2/3/2, images unchanged), and every downstream scenario has a Locked/Dropped/Blocked status citing the probe field that decided it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host -> `kubectl` -> `skp` namespace | The first live cluster mutation of the phase (scale, set env, redis exec, psql exec). |
| Host HTTP -> WebApi `localhost:8080` | Deliberately malformed-but-safe inputs cross here during the 5xx enumeration. |
| Grafana loopback forward | A newly bound host port on 127.0.0.1:3000. |
| Probe -> Postgres / Redis via `kubectl exec` | Read-mostly; one probe workflow may be created and must be removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-13 | Tampering (V5 input validation) | PQ-03 endpoint list, PQ-05 routes | mitigate | The candidate endpoint list is a static in-script `[ordered]@{}` table; no route is discovered from a Swagger document or derived from a parameter. Only GET requests plus one inert `POST /api/v1/orchestration/start` with an empty JSON array body are issued. |
| T-88-06 | Injection | `psql -c` in the traffic drive and PQ-05 | mitigate | Every `psql -c` argument is a static double-quoted string literal with no `$` interpolation, asserted per line in the task verify. |
| T-88-11 | Tampering | PQ-05 probe workflow lifecycle | mitigate | The probe workflow is stopped through the API BEFORE any row or key is removed; `ProbeWorkflowCleanedUp` is an artifact field and cleanup also runs from the outer `finally`. Deleting workflow rows while the orchestrator runs leaves an orphaned Quartz cron firing forever and breaks later conservation checks with `Missing=0`. |
| T-88-03 | Tampering | seam left armed by an interrupted probe | mitigate | `$seamArmed` is declared before the `try`; `Clear-Phase88Seam` is issued from the outer `finally`; `Assert-StackRestored` is an explicit claim set and a false claim produces exit 65, a code no verdict can occupy. |
| T-88-02 | Tampering / DoS | PQ-01 processor scale | mitigate | `Invoke-TierScaleFault` reads `spec.replicas` live before scaling and restores to that value, asserting equality afterwards. The task verify independently re-reads all four tiers. |
| T-88-16 | DoS | dependency outage as a 5xx route | mitigate | Explicitly forbidden here: `scale statefulset` is asserted absent. The Phase-86 hard readiness latch means a dependency outage requires a pod restart to recover and would perturb every later baseline. |
| T-88-04 | Information Disclosure | Grafana port-forward | mitigate | `--address 127.0.0.1` explicit; `0.0.0.0` asserted absent. |
| T-88-05 | DoS | shared port-forward PID file | mitigate | `.k8s-portforward-pids` asserted absent; the probe's own forward PID lives in one in-memory variable behind a recycled-PID guard. |
| T-88-07 | Repudiation | infra abort read as a verdict | mitigate | One distinct exit code per abort (15/20/50/60/61/62/63/64/65); the verdict comes only from the dot-sourced fail-closed `Resolve-AnalyzerExitCode`, asserted present with no inline `switch ($verdict)`. |
| T-88-10 | Tampering | stale `:local` images | mitigate | `apply -k` asserted absent; `ImagesUnchanged` asserted true. A stale-image cluster emits no `source` resource attribute and would make every correct dashboard look broken. |
| T-88-SC | Tampering | npm / pip / cargo installs | accept | No package-manager install occurs. Legitimacy gate not applicable. |
</threat_model>

<verification>
- `[ScriptBlock]::Create` parse check passes on `scripts/phase-88-wave0-probe.ps1`.
- The probe exits 0 or 2; it must NOT exit 65 (a dirty stack invalidates the whole phase) and must not exit 1.
- `analyzer-reports/phase-88-wave0-probe.json` contains an answer for every PQ-01..PQ-07 field, or names it in `UnevaluableQuestions[]`.
- All four tiers read back their pre-probe replica counts and images; `keeper` and `processor-sample` carry zero `DEFEAT`/`REINJECT_DELAY` env vars.
- `pwsh -File scripts/phase-87-dashboard-lint.ps1` still exits 0 (no dashboard JSON was touched).
- `88-PROBE-DECISIONS.md` assigns Locked/Dropped/Blocked to all ten scenario ids, each citing a probe field.
</verification>

<success_criteria>
- The three plan-structure-changing assumptions (keeper recovery traffic, the reader's locator, panel 13's fault route) are replaced by measurements.
- Panel 6's route and the seam mechanism's viability are likewise measured rather than assumed.
- Every downstream scenario has a locked lever or a documented reason it cannot exist.
- The stack is left byte-identical: seam vars gone, replicas 1/2/3/2, images at `:tags-const-1544`.
</success_criteria>

<output>
Create `.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-03-SUMMARY.md` when done.
</output>
