---
phase: 88-pipeline-dashboard-maintenance-handoff-proof-fault-injection
plan: 06
type: execute
wave: 5
depends_on: ["88-05"]
files_modified:
  - analyzer-reports/phase-88-SCALE-01.json
  - analyzer-reports/phase-88-SCALE-02.json
  - analyzer-reports/phase-88-SCALE-03.json
autonomous: true
requirements: [DISC-02, DISC-03, DISC-05, DISC-06]

must_haves:
  truths:
    - "Panels 1, 3, 4, 5 and 9 have each been observed changing under the tier outage they exist to reveal, asserted from the rendered Grafana panel."
    - "Panel 5's real behaviour under a processor outage — an initial spike while stale series persist, then emptiness once they expire — is measured, not predicted."
    - "Panel 9 reading 'No data' under a dead keeper is scored as the correct discrimination signal, because that panel is deliberately unguarded."
    - "Every one of the three scenarios re-captured its baseline after its rollout settled, and recorded the pod-identity discontinuity as an expected artifact."
    - "Each scenario's cross-talk panels stayed inside their DISC-01 bands, so a panel wired to the wrong metric would have been caught."
    - "After all three scenarios the orchestrator is still at 3 replicas — the HA the previous milestone shipped is intact."
  artifacts:
    - path: "analyzer-reports/phase-88-SCALE-01.json"
      provides: "the DISC-02/03/06 proof for panels 3, 4 and 5 under a processor-tier outage"
      contains: "RolloutNewInstanceIds"
    - path: "analyzer-reports/phase-88-SCALE-02.json"
      provides: "the DISC-02/03/06 proof for panel 1 under an orchestrator-tier outage"
      contains: "ReplicasRestored"
    - path: "analyzer-reports/phase-88-SCALE-03.json"
      provides: "the DISC-02/03/06 proof for panel 9 under a keeper-tier outage"
      contains: "PanelStateAfter"
  key_links:
    - from: "each scenario artifact"
      to: "the live deployment's pre-fault spec.replicas"
      via: "ReplicasBefore read live, ReplicasAfter re-read, ReplicasRestored asserted"
      pattern: "ReplicasRestored"
---

<objective>
Drive the three tier-outage scenarios and prove the five panels they exist to reveal actually
discriminate. These are the panels the roadmap describes as having been seen in only one state:
panel 9 has shown nothing but a flat line, and panels 1, 3, 4 and 5 have only ever been observed on
a healthy stack.

The lever is `kubectl scale` alone — no seam. That is the roadmap's stated preference: a fault
drivable by system state alone should never use a seam.

Purpose: satisfy DISC-02, DISC-03 and DISC-06 for the pipeline conservation and heartbeat panels,
and produce the measured evidence for two of the phase's known misleading-by-default findings
(panel 5 empties instead of spiking; panel 9's "No data" is correct behaviour, not a read failure).
Output: three scenario artifacts under `analyzer-reports/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-PATTERNS.md
@.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-05-SUMMARY.md

<interfaces>
<!-- The three scenarios, their predicted movements, and their pre-declared cross-talk controls. -->

SCALE-01  lever scale · tier processor-sample · dwell 420 s
  panel 3  Processor consumed vs sent by image        A  predicted: down (toward 0 / NoData)
  panel 4  Orchestrator sends minus processor pickups C  predicted: slope-up (per-minute increase
                                                          rises — the orchestrator keeps sending
                                                          while pickups stop)
  panel 5  Per-processor dispatch gap                 A  predicted: up-then-nodata (see below)
  crossTalk 9, 10, 12, 14
  Dwell is 420 s deliberately: panel 5's expression is
  `sum by (processorId)(rate(orch_sent)) - sum by (processorId)(rate(proc_consumed))`, and PromQL's
  binary `-` requires matching label sets on both sides. For roughly the first five minutes after
  the pods die the stale processor series still exist and their `rate()` reads 0, so the gap SPIKES
  to the orchestrator's full send rate. Once the collector's ~5-minute `metric_expiration` removes
  them, no label set matches and the panel goes EMPTY — not high. Both phases must be captured:
  the spike is the useful signal, the emptiness is the trap, and the trap is a HAND-02 row.

SCALE-02  lever scale · tier orchestrator · dwell 240 s
  panel 1  Orchestrator consumed vs sent               A  predicted: down (both legend Means fall
                                                          toward 0; line gaps appear)
  crossTalk 9, 10, 12
  The orchestrator runs at 3 replicas (Phase 83 HA-06, leader-elected via a k8s Lease). The restore
  MUST return it to the count read live before the fault. A blanket restore to 1 would silently
  dismantle that HA — quietly, because a single orchestrator still works.

SCALE-03  lever scale · tier keeper · dwell 240 s
  panel 9  Keeper L2 probe heartbeat                   A  predicted: nodata
  crossTalk 1, 3, 10, 12
  Panel 9 is deliberately UNGUARDED so a dead keeper renders "No data" rather than a comfortable 0.
  `NoData` is therefore the CORRECT discrimination signal here and counts as moved. Scoring it as
  "could not read the panel" would be the phase's sixth pitfall.
  The keeper handles recovery only, so a keeper outage must not disturb the happy-path conservation
  panels — which is exactly what the cross-talk control on 1 and 3 asserts.

Order matters. Run SCALE-01, then SCALE-02, then SCALE-03, sequentially, with the stack asserted
back at rest between each. They all mutate the same cluster and cannot overlap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run SCALE-01 — processor-tier outage; prove panels 3, 4 and 5</name>
  <files>analyzer-reports/phase-88-SCALE-01.json</files>
  <read_first>
    - scripts/phase-88-panel-discriminate.ps1 (the SCALE-01 row and the scenario engine)
    - scripts/lib/phase-88-cluster-ops.ps1 (Invoke-TierScaleFault: the live-read replica capture, terminate-wait, dwell, restore, readiness gate, and the ReplicasRestored claim)
    - analyzer-reports/phase-88-BASE-01.json (the DISC-01 bands for panels 3, 4, 5 and the cross-talk bands for 9, 10, 12, 14)
    - k8s/dashboards/business.json (panel 3's `identityName` grouping, panel 4's `increase(...[$__range])` stat and its "read the trend, not the value" description, panel 5's `sum by (processorId)` subtraction)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md § Panel 5 note (:148-149) and Pitfall 7 (:484-487)
  </read_first>
  <action>
    Assert the stack is at rest (all four tiers Ready at 1/2/3/2, zero seam vars, images at
    `:tags-const-1544`), then run
    `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId SCALE-01 -Mode Scenario`.

    The engine must, in order: capture the pre-arm baseline for the record, scale
    `deployment/processor-sample` to 0 through `Invoke-TierScaleFault` (which reads
    `spec.replicas` live first — the restore target is that read value, never a hardcoded map),
    terminate-wait until zero running pods, dwell 420 s while capturing after-window sub-windows,
    restore to the read count, gate on readiness, and assert restoration.

    Because the scale causes a rollout, `BaselineRecaptured` applies: the authoritative band for
    scoring is the post-restore re-capture taken at least 150 s after the tier settled, and the
    pre-fault DISC-01 band is recorded alongside it. `RolloutOldInstanceIds` and
    `RolloutNewInstanceIds` must be present and distinct, with `RolloutUtc`, labelled an expected
    artifact.

    Capture panel 5 in TWO segments and record both: an early after-segment covering the first
    240 s of the outage (where the stale processor series still exist and the gap should spike to
    the orchestrator's full send rate) and a late after-segment covering the period after roughly
    300 s (where the series have expired from the collector, no label set matches, and the panel
    should go empty). Record the early segment's values and the late segment's `PanelStateAfter`.
    Panel 5 counts as moved if EITHER segment shows the predicted behaviour; record which. If the
    panel empties without ever spiking, that is a measurement, not a failure — record it verbatim
    and carry it to the HAND-02 misleading-by-default list.

    Panel 4 is Regime C: score the per-minute increase read at the fixed 60-second sub-window
    against its band, and record the wide-window `RangeCumulativeLevelAfter` separately without
    scoring it. Applying a Regime-A band to the wide-window level would be a guaranteed false
    positive, because that value grows on a healthy stack too.

    Cross-talk: panels 9, 10, 12 and 14 must stay inside their DISC-01 bands for the whole fault
    window. Panel 1 is deliberately NOT a cross-talk control here — an orchestrator whose
    downstream processors are gone will see its own consumed rate fall, so it has a real causal
    path to the fault and asserting it unchanged would be wrong.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-SCALE-01.json` exists, parses, and its `Verdict` is `Pass` or `Inconclusive` (never `Fail`)
    - `PanelResults[]` contains entries for panel ids 3, 4 and 5, each with `Moved` true and `ConsecutiveSamplesOutside` at least 2, or named in `UnevaluablePanels[]` with a reason
    - `RolloutOldInstanceIds[]` and `RolloutNewInstanceIds[]` are both non-empty and share no element; `RolloutUtc` is present
    - `BaselineRecaptured` is true
    - `ReplicasBefore` equals 2, `ReplicasAfter` equals 2, `ReplicasRestored` is true
    - Panel 5's entry records both an early and a late after-segment, and states which behaviour was observed
    - `RangeCumulativeLevelAfter` is present for panel 4 and is NOT used in its `Moved` computation
    - `CrossTalkPanels[]` covers panel ids 9, 10, 12 and 14 and `CrossTalkHeld` is true
    - `SeamVarsClean`, `ReplicasRestored` and `ImagesUnchanged` are all true
    - The written file contains no literal `System.Object[]`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$f='analyzer-reports/phase-88-SCALE-01.json'; $raw=Get-Content $f -Raw; if($raw -match 'System.Object\[\]'){ throw 'serialisation depth too shallow' }; $j=$raw | ConvertFrom-Json; if($j.Verdict -eq 'Fail'){ throw ('SCALE-01 verdict Fail: '+$j.HumanSummary) }; foreach($c in 'SeamVarsClean','ReplicasRestored','ImagesUnchanged','CrossTalkHeld','BaselineRecaptured'){ if(-not $j.$c){ throw \"claim false: $c\" } }; $old=@($j.RolloutOldInstanceIds); $new=@($j.RolloutNewInstanceIds); if($old.Count -eq 0 -or $new.Count -eq 0){ throw 'rollout discontinuity not captured' }; if(@($new | Where-Object { $old -contains $_ }).Count -gt 0){ throw 'rollout pod identities overlap — the rollout was not captured' }; if($j.ReplicasBefore -ne 2 -or $j.ReplicasAfter -ne 2){ throw ('processor replicas: before='+$j.ReplicasBefore+' after='+$j.ReplicasAfter) }; $unev=@($j.UnevaluablePanels | ForEach-Object { \"$_\" }); foreach($id in 3,4,5){ $r=@($j.PanelResults | Where-Object { $_.PanelId -eq $id }); if($r.Count -eq 0){ if(-not ($unev -match \"$id\")){ throw \"panel $id neither asserted nor declared unevaluable\" }; continue }; foreach($e in $r){ if(-not $e.Moved){ throw \"panel $id did not move\" }; if($e.ConsecutiveSamplesOutside -lt 2){ throw \"panel $id moved on fewer than 2 consecutive samples\" } } }; if($null -eq $j.RangeCumulativeLevelAfter){ throw 'panel 4 wide-window level not recorded' }; foreach($id in 9,10,12,14){ $ct=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq $id }); if($ct.Count -eq 0){ throw \"cross-talk control missing for panel $id\" }; if(-not $ct[0].StayedPut){ throw \"cross-talk panel $id drifted outside its band\" } }; 'SCALE-01 proven'"</automated>
  </verify>
  <done>Panels 3, 4 and 5 are proven to change under a processor-tier outage from the rendered panel; panel 5's spike-then-empty transition is measured across two after-segments; the rollout discontinuity is recorded; processor-sample is back at 2 replicas.</done>
</task>

<task type="auto">
  <name>Task 2: Run SCALE-02 — orchestrator-tier outage; prove panel 1 and preserve the HA replica count</name>
  <files>analyzer-reports/phase-88-SCALE-02.json</files>
  <read_first>
    - scripts/phase-88-panel-discriminate.ps1 (the SCALE-02 row)
    - scripts/lib/phase-88-cluster-ops.ps1 (the live-read replica path — the control against the stale `phase-80-harness.ps1:148-150` map whose `orchestrator = 1` contradicts the live 3)
    - k8s/31-orchestrator.yaml (line 41: `replicas: 3`, "HA — leader-elected via k8s Lease (HA-06)")
    - analyzer-reports/phase-88-BASE-01.json (panel 1's band and the cross-talk bands for 9, 10, 12)
    - analyzer-reports/phase-88-SCALE-01.json (confirm the previous scenario restored cleanly before starting this one)
  </read_first>
  <action>
    Assert the stack is back at rest after SCALE-01 — specifically that `processor-sample` reads 2
    and `orchestrator` reads 3 — then run
    `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId SCALE-02 -Mode Scenario`.

    Scale `deployment/orchestrator` to 0 for 240 s and assert panel 1's two legend Means both fall
    below their DISC-01 band for at least two consecutive 60-second samples, or that the panel
    reaches `NoData` — either is the predicted `down` direction and both must be recorded verbatim.

    The single most important assertion of this task is the restore: `ReplicasBefore` must read 3
    from the live deployment before the scale, `ReplicasAfter` must read 3 after, and
    `ReplicasRestored` must be true. This is the scenario the stale replica map would have
    damaged, silently, because a single orchestrator still works. Verify it independently with a
    direct `kubectl get deploy orchestrator -o jsonpath='{.spec.replicas}'` after the run, not only
    from the artifact.

    Cross-talk: panels 9, 10 and 12 must hold their bands. Panel 3 is deliberately NOT a control
    here — with the orchestrator down the processors receive no dispatches, so panel 3 has a real
    causal path to this fault.

    A stopped orchestrator also stops the Quartz cron that drives `v8-fanout-proof`. After restore,
    confirm traffic resumed before the post-fault re-capture: if the workflow did not resume, the
    "after" window measures an idle stack rather than a recovered one. Record the resumption check
    in the artifact.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-SCALE-02.json` exists, parses, and its `Verdict` is `Pass` or `Inconclusive` (never `Fail`)
    - `ReplicasBefore` is 3, `ReplicasAfter` is 3, `ReplicasRestored` is true
    - A direct `kubectl -n skp get deploy orchestrator -o jsonpath='{.spec.replicas}'` reads 3 after the run
    - Panel 1's `PanelResults[]` entry has `Moved` true with `ConsecutiveSamplesOutside` at least 2, or the panel is named in `UnevaluablePanels[]` with a reason
    - `RolloutOldInstanceIds[]` and `RolloutNewInstanceIds[]` are non-empty and disjoint; `BaselineRecaptured` is true
    - `CrossTalkPanels[]` covers panel ids 9, 10 and 12 and `CrossTalkHeld` is true
    - `SeamVarsClean` and `ImagesUnchanged` are true
    - The artifact records that pipeline traffic resumed after restore, before the post-fault re-capture
    - The written file contains no literal `System.Object[]`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$f='analyzer-reports/phase-88-SCALE-02.json'; $raw=Get-Content $f -Raw; if($raw -match 'System.Object\[\]'){ throw 'serialisation depth too shallow' }; $j=$raw | ConvertFrom-Json; if($j.Verdict -eq 'Fail'){ throw ('SCALE-02 verdict Fail: '+$j.HumanSummary) }; if($j.ReplicasBefore -ne 3 -or $j.ReplicasAfter -ne 3 -or -not $j.ReplicasRestored){ throw ('orchestrator HA replica count not preserved: before='+$j.ReplicasBefore+' after='+$j.ReplicasAfter) }; $live=[int]((\"\"+(kubectl -n skp get deploy orchestrator -o jsonpath='{.spec.replicas}')).Trim()); if($live -ne 3){ throw (\"live orchestrator replicas = $live, expected 3 — the Phase-83 HA was dismantled\") }; foreach($c in 'SeamVarsClean','ImagesUnchanged','CrossTalkHeld','BaselineRecaptured'){ if(-not $j.$c){ throw \"claim false: $c\" } }; $old=@($j.RolloutOldInstanceIds); $new=@($j.RolloutNewInstanceIds); if($old.Count -eq 0 -or $new.Count -eq 0 -or @($new | Where-Object { $old -contains $_ }).Count -gt 0){ throw 'rollout discontinuity not captured or overlapping' }; $unev=@($j.UnevaluablePanels | ForEach-Object { \"$_\" }); $r=@($j.PanelResults | Where-Object { $_.PanelId -eq 1 }); if($r.Count -eq 0){ if(-not ($unev -match '1')){ throw 'panel 1 neither asserted nor declared unevaluable' } } else { foreach($e in $r){ if(-not $e.Moved -or $e.ConsecutiveSamplesOutside -lt 2){ throw 'panel 1 did not move on two consecutive samples' } } }; foreach($id in 9,10,12){ $ct=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq $id }); if($ct.Count -eq 0 -or -not $ct[0].StayedPut){ throw \"cross-talk failed or missing for panel $id\" } }; 'SCALE-02 proven and HA intact'"</automated>
  </verify>
  <done>Panel 1 is proven to fall under an orchestrator-tier outage from the rendered panel, the orchestrator is verifiably back at 3 replicas from both the artifact and a direct live read, and pipeline traffic is confirmed resumed.</done>
</task>

<task type="auto">
  <name>Task 3: Run SCALE-03 — keeper-tier outage; prove panel 9's "No data" is the signal</name>
  <files>analyzer-reports/phase-88-SCALE-03.json</files>
  <read_first>
    - scripts/phase-88-panel-discriminate.ps1 (the SCALE-03 row)
    - scripts/lib/phase-88-panel-read.ps1 (Test-PanelMoved's `nodata` direction and Get-PanelStates)
    - k8s/dashboards/business.json (panel 9 `Keeper L2 probe heartbeat` — deliberately carries NO `or vector(0)` guard, which is why a dead keeper renders "No data" rather than a comfortable 0)
    - analyzer-reports/phase-88-BASE-01.json (panel 9's floor-bound band and the cross-talk bands for 1, 3, 10, 12)
    - analyzer-reports/phase-88-SCALE-02.json (confirm the previous scenario restored cleanly, including orchestrator at 3)
    - .planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-RESEARCH.md Pitfall 6 (:479-482)
  </read_first>
  <action>
    Assert the stack is back at rest after SCALE-02, then run
    `pwsh -File scripts/phase-88-panel-discriminate.ps1 -ScenarioId SCALE-03 -Mode Scenario`.

    Scale `deployment/keeper` to 0 for 240 s. Panel 9's predicted direction is `nodata`: at least
    two consecutive after sub-windows must report `PanelState` of `NoData`. This counts as MOVED.
    Do not treat it as a read failure and do not degrade the verdict to Inconclusive on account of
    it — the panel is unguarded precisely so that a dead keeper is visible rather than comfortable,
    and modelling `NoData` as a first-class state is what makes that design legible.

    Record `AfterStates[]` verbatim so the transition from `Rendered` to `NoData` and back is
    visible in the artifact, and capture a screenshot of the emptied panel as evidence. The
    screenshot is evidence, never the verdict.

    Cross-talk: panels 1, 3, 10 and 12 must hold their bands. The keeper serves the recovery path
    only, so a keeper outage should not disturb the happy-path conservation counters. If panel 1 or
    3 drifts, that is a genuine finding — it would mean the keeper is on a path the architecture
    says it is not — and it must be recorded rather than explained away.

    Restore the keeper to its live-read count (2) and assert. Note in the artifact that scaling the
    keeper to 0 does NOT produce a keeper consumed-minus-sent gap: a keeper that is not running
    consumes nothing, so panels 2 and 8 stay at 0 throughout. That is why panel 8 needs the seam
    scenario in plan 88-07, and stating it here prevents a later reader concluding this scenario
    should have moved them.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-88-SCALE-03.json` exists, parses, and its `Verdict` is `Pass` or `Inconclusive` (never `Fail`)
    - Panel 9's `PanelResults[]` entry has `PredictedDirection` `nodata`, `Moved` true, and `AfterStates[]` containing at least two consecutive `NoData` entries
    - `ReplicasBefore` is 2, `ReplicasAfter` is 2, `ReplicasRestored` is true
    - `RolloutOldInstanceIds[]` and `RolloutNewInstanceIds[]` are non-empty and disjoint; `BaselineRecaptured` is true
    - `CrossTalkPanels[]` covers panel ids 1, 3, 10 and 12 and `CrossTalkHeld` is true
    - `ScreenshotPaths[]` contains at least one path under `analyzer-reports/phase-88-screenshots/SCALE-03/` and the file exists on disk
    - The `HumanSummary` states that panels 2 and 8 remained at 0 during this outage and why
    - `SeamVarsClean` and `ImagesUnchanged` are true
    - After the run, all four tiers read 1/2/3/2 replicas with zero `DEFEAT`/`REINJECT_DELAY` env vars
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$f='analyzer-reports/phase-88-SCALE-03.json'; $raw=Get-Content $f -Raw; if($raw -match 'System.Object\[\]'){ throw 'serialisation depth too shallow' }; $j=$raw | ConvertFrom-Json; if($j.Verdict -eq 'Fail'){ throw ('SCALE-03 verdict Fail: '+$j.HumanSummary) }; foreach($c in 'SeamVarsClean','ImagesUnchanged','CrossTalkHeld','BaselineRecaptured','ReplicasRestored'){ if(-not $j.$c){ throw \"claim false: $c\" } }; if($j.ReplicasBefore -ne 2 -or $j.ReplicasAfter -ne 2){ throw ('keeper replicas: before='+$j.ReplicasBefore+' after='+$j.ReplicasAfter) }; $r=@($j.PanelResults | Where-Object { $_.PanelId -eq 9 }); if($r.Count -eq 0){ throw 'panel 9 result missing' }; if($r[0].PredictedDirection -ne 'nodata'){ throw ('panel 9 predicted direction is '+$r[0].PredictedDirection+', expected nodata') }; if(-not $r[0].Moved){ throw 'panel 9 NoData was not scored as moved — pitfall 6' }; $st=@($r[0].AfterStates); $run=0; $ok=$false; foreach($s in $st){ if(\"$s\" -eq 'NoData'){ $run++; if($run -ge 2){ $ok=$true } } else { $run=0 } }; if(-not $ok){ throw 'panel 9 never reported two consecutive NoData samples' }; $old=@($j.RolloutOldInstanceIds); $new=@($j.RolloutNewInstanceIds); if($old.Count -eq 0 -or $new.Count -eq 0 -or @($new | Where-Object { $old -contains $_ }).Count -gt 0){ throw 'rollout discontinuity not captured or overlapping' }; foreach($id in 1,3,10,12){ $ct=@($j.CrossTalkPanels | Where-Object { $_.PanelId -eq $id }); if($ct.Count -eq 0 -or -not $ct[0].StayedPut){ throw \"cross-talk failed or missing for panel $id\" } }; $shots=@($j.ScreenshotPaths | Where-Object { Test-Path $_ }); if($shots.Count -lt 1){ throw 'no screenshot evidence on disk' }; if(\"$($j.HumanSummary)\" -notmatch 'panel[s]? 2 and 8|panels 2 and 8'){ throw 'HumanSummary does not record that panels 2 and 8 stayed at 0' }; $exp=@{'orchestrator'=3;'keeper'=2;'processor-sample'=2;'baseapi-service'=1}; foreach($k in $exp.Keys){ $rc=[int]((\"\"+(kubectl -n skp get deploy $k -o jsonpath='{.spec.replicas}')).Trim()); if($rc -ne $exp[$k]){ throw \"replicas drifted: $k=$rc\" } }; foreach($tier in 'keeper','processor-sample'){ $e=(kubectl -n skp get deploy $tier -o jsonpath='{.spec.template.spec.containers[0].env}'); if(\"$e\" -match 'DEFEAT|REINJECT_DELAY'){ throw \"seam left armed on $tier\" } }; 'SCALE-03 proven'"</automated>
  </verify>
  <done>Panel 9 is proven to render "No data" under a dead keeper and that state is scored as the correct discrimination signal; the four cross-talk panels held; the keeper is back at 2 replicas and the whole stack reads 1/2/3/2 with zero seam vars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Driver -> `kubectl scale` -> `skp` | Three whole-tier outages against a live namespace. |
| Driver -> Grafana loopback forward -> browser | The verdict channel. |
| Restored cluster -> every later scenario | A tier left at the wrong replica count silently poisons every subsequent measurement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-02 | Tampering / DoS | orchestrator replica restore | mitigate | `Invoke-TierScaleFault` reads `spec.replicas` live before scaling and restores to that value; SCALE-02's verify additionally issues a direct `kubectl get deploy orchestrator -o jsonpath='{.spec.replicas}'` and fails unless it reads 3. This is the exact scenario the stale `phase-80-harness.ps1:148-150` map would have damaged silently. |
| T-88-01 | Tampering | scaled workload selection | mitigate | The tier comes from the static scenario row, validated against `$Phase88Tiers`; nothing is derived from `-ScenarioId`. Only `processor-sample`, `orchestrator` and `keeper` are scaled — never `otel-collector`, `prometheus`, `grafana`, `redis`, `rabbitmq` or `postgres`. |
| T-88-22 | DoS | scaling the telemetry path | mitigate | Killing the collector, Prometheus or Grafana would make every panel move, prove nothing, and destroy the baselines. None appears in the allow-list, so the scenario table cannot express it. |
| T-88-20 | Repudiation | rollout mistaken for the fault | mitigate | All three scenarios cause a rollout, so `BaselineRecaptured` must be true and the pod-identity pair must be non-empty and disjoint — asserted per scenario. The band used for scoring is the post-settle re-capture. |
| T-88-21 | Repudiation | cross-talk suppressed | mitigate | Each scenario's control list is pre-declared and deliberately excludes panels with a real causal path to its fault (panel 1 for SCALE-01, panel 3 for SCALE-02), so the control asserts something falsifiable rather than something trivially true. |
| T-88-23 | Repudiation | NoData misread as a read failure | mitigate | Panel 9's predicted direction is `nodata` and `AfterStates[]` is asserted to contain two consecutive `NoData` entries. Scoring it as unread would be the phase's sixth pitfall and would understate the dashboard's only unguarded fault signal. |
| T-88-03 | Tampering | left-armed seam | mitigate | No seam is armed in this plan, and `SeamVarsClean` is still asserted after each scenario so a leak from an earlier plan is caught here rather than silently confounding these measurements. |
| T-88-10 | Tampering | stale `:local` images | mitigate | `ImagesUnchanged` asserted per scenario; `apply -k` is absent from every Phase-88 script. |
| T-88-18 | Tampering | shallow JSON serialisation | mitigate | Every artifact is checked for a literal `System.Object[]` after writing. |
| T-88-SC | Tampering | npm / pip / cargo installs | accept | No package-manager install occurs. Legitimacy gate not applicable. |
</threat_model>

<verification>
- Three artifacts exist, each with `Verdict` `Pass` or `Inconclusive`, never `Fail`.
- Panels 3, 4, 5, 1 and 9 are each proven moved (or explicitly declared unevaluable with a reason).
- Every scenario recorded a distinct rollout pod-identity pair and re-captured its baseline afterwards.
- Every scenario's cross-talk control held.
- `kubectl -n skp get deploy` reads `baseapi-service` 1, `keeper` 2, `orchestrator` 3, `processor-sample` 2 after all three runs, with zero `DEFEAT`/`REINJECT_DELAY` env vars.
- `pwsh -File scripts/phase-87-dashboard-lint.ps1` still exits 0.
</verification>

<success_criteria>
- Five of the fourteen panels move from "seen in one state" to "proven to discriminate", asserted from the rendered Grafana panel.
- Panel 5's spike-then-empty behaviour and panel 9's NoData-as-signal behaviour are measured and recorded for the HAND-02 misleading-by-default list.
- The rollout discontinuity is an expected, recorded artifact in all three scenarios — never scored as movement.
- The Phase-83 orchestrator HA replica count survives the phase.
</success_criteria>

<output>
Create `.planning/phases/88-pipeline-dashboard-maintenance-handoff-proof-fault-injection/88-06-SUMMARY.md` when done.
</output>
