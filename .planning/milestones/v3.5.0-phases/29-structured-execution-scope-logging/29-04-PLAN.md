---
phase: 29-structured-execution-scope-logging
plan: 04
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src/Orchestrator/Scheduling/WorkflowFireJob.cs
  - tests/BaseApi.Tests/Orchestrator/WorkflowFireJobScopeTests.cs
autonomous: true
requirements: [LOG-05, LOG-01]
tags: [logging, otel, scopes, orchestrator, quartz]

must_haves:
  truths:
    - "WorkflowFireJob.Execute log lines emitted after the correlationId mint carry CorrelationId and WorkflowId scope values"
    - "The scope wraps only the post-mint body (after both ids are known); the early unparseable/absent returns are unaffected"
    - "CorrelationId is scoped via CorrelationKeys.LogScope and WorkflowId via ExecutionLogScope.WorkflowId"
  artifacts:
    - path: "src/Orchestrator/Scheduling/WorkflowFireJob.cs"
      provides: "Explicit BeginScope(CorrelationId + WorkflowId) wrapping the post-mint Execute body"
      contains: "ExecutionLogScope.WorkflowId"
    - path: "tests/BaseApi.Tests/Orchestrator/WorkflowFireJobScopeTests.cs"
      provides: "Hermetic test that fire log lines carry CorrelationId + WorkflowId scope values"
      contains: "WorkflowFireJob"
  key_links:
    - from: "src/Orchestrator/Scheduling/WorkflowFireJob.cs"
      to: "src/Messaging.Contracts/ExecutionLogScope.cs and CorrelationKeys.cs"
      via: "BeginScope dictionary keyed by CorrelationKeys.LogScope + ExecutionLogScope.WorkflowId"
      pattern: "CorrelationKeys\\.LogScope|ExecutionLogScope\\.WorkflowId"
---

<objective>
Wrap the post-mint body of `WorkflowFireJob.Execute` in an explicit `BeginScope` carrying the per-fire `CorrelationId` (via `CorrelationKeys.LogScope`) and the `WorkflowId` (via `ExecutionLogScope.WorkflowId`), and prove it with a hermetic test.

Purpose: `WorkflowFireJob` is a Quartz job that runs OUTSIDE the consume pipeline, so neither the correlation filter nor the new execution filter ever sees it — its fire logs would carry neither id ambiently. The explicit scope (D-06) makes its fire logs correlate with the round-trip it triggers, surfacing `attributes.CorrelationId` and `attributes.WorkflowId` via the unchanged OTel bridge (LOG-01). This is the ONE place the job owns CorrelationId (it is not in the consume pipeline), so it uses `CorrelationKeys.LogScope` directly.

Output: the scope edit to `WorkflowFireJob.Execute` + `WorkflowFireJobScopeTests`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structured-execution-scope-logging/29-SPEC.md
@.planning/phases/29-structured-execution-scope-logging/29-CONTEXT.md
@.planning/phases/29-structured-execution-scope-logging/29-PATTERNS.md

<interfaces>
The VERIFIED anchors (this session — src/Orchestrator/Scheduling/WorkflowFireJob.cs:37-87). The ctor injects `ILogger<WorkflowFireJob> logger`. `workflowId` is parsed at :40; the early returns at :42-44 (unparseable) and :46-51 (workflow absent) happen BEFORE the correlationId mint; `correlationId = NewId.NextGuid()` at :54; the dispatch loop is :56-72; the liveness refresh + reschedule are :74-86:

```csharp
var raw = context.MergedJobDataMap.GetString("workflowId");
if (!Guid.TryParse(raw, out var workflowId)) { logger.LogWarning(...); return; }   // :40-44 early return
if (!store.TryGet(workflowId, out var wf))   { logger.LogInformation(...); return; } // :46-51 early return

var correlationId = NewId.NextGuid();   // :54  <-- both ids known after this line

foreach (var entryStepId in wf.EntryStepIds) { ... await dispatcher.DispatchAsync(...); }  // :56-72
// liveness refresh (:74-80) + self-reschedule (:82-86)
```

The keys: `CorrelationKeys.LogScope == "CorrelationId"` (src/Messaging.Contracts/CorrelationKeys.cs) and `ExecutionLogScope.WorkflowId == "WorkflowId"` (from 29-01). The `BeginScope` shape to mirror (src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs:39): `new Dictionary<string,object>{ [key] = value.ToString() }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Explicit BeginScope in WorkflowFireJob.Execute + hermetic test</name>
  <read_first>
    - src/Orchestrator/Scheduling/WorkflowFireJob.cs (the full Execute method :37-87 — the two early returns before :54, the correlationId mint at :54, the dispatch loop + liveness/reschedule to wrap; the ctor's `ILogger<WorkflowFireJob> logger`)
    - src/Messaging.Contracts/CorrelationKeys.cs (CorrelationKeys.LogScope == "CorrelationId")
    - src/Messaging.Contracts/ExecutionLogScope.cs (ExecutionLogScope.WorkflowId == "WorkflowId" — from 29-01)
    - src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs (:39 — the BeginScope Dictionary shape)
    - tests/BaseApi.Tests/Orchestrator/ (existing WorkflowFireJob / scheduling tests — mirror their Quartz IJobExecutionContext construction / stubs; reuse the capturing-logger double from 29-02 Task 2 if present)
  </read_first>
  <behavior>
    - Log lines emitted after the correlationId mint (the dispatch loop + liveness refresh + reschedule) carry CorrelationId == the per-fire correlationId and WorkflowId == the parsed workflowId
    - The early-return logs (unparseable workflowId, workflow absent) are NOT wrapped — they emit before both ids are known
  </behavior>
  <action>
    Edit `src/Orchestrator/Scheduling/WorkflowFireJob.cs`: immediately after `var correlationId = NewId.NextGuid();` (:54 — the point where BOTH ids are known), wrap the remaining body (the `foreach` dispatch loop AND the liveness refresh + self-reschedule) in an explicit `using` `BeginScope` carrying both ids. Use `CorrelationKeys.LogScope` for CorrelationId (this is the ONE place the job owns it — it is outside the consume pipeline, D-06) and `ExecutionLogScope.WorkflowId` for WorkflowId; `.ToString()` string values, `Dictionary<string,object>` shape:

```csharp
var correlationId = NewId.NextGuid();   // :54
using (logger.BeginScope(new Dictionary<string, object>
{
    [CorrelationKeys.LogScope]     = correlationId.ToString(),
    [ExecutionLogScope.WorkflowId] = workflowId.ToString(),
}))
{
    foreach (var entryStepId in wf.EntryStepIds)
    {
        // ... existing dispatch-loop body unchanged ...
    }

    // ... existing liveness refresh + self-reschedule unchanged ...
}
```

    Add `using Messaging.Contracts;` if not already present. Do NOT move or wrap the two early returns (:40-44 unparseable, :46-51 absent) — they fire BEFORE the correlationId mint (Pattern 6 / RESEARCH). Do NOT interpolate either id into any log-message template (T-18-04) — they go ONLY into the scope dictionary; existing templates stay unchanged (additive only). Keep all existing logic inside the loop/refresh byte-identical except for the indentation change from the wrapping block.

    Create `tests/BaseApi.Tests/Orchestrator/WorkflowFireJobScopeTests.cs` (reuse the capturing-logger double from 29-02 Task 2). Drive `WorkflowFireJob.Execute` with a stub `IJobExecutionContext` whose `MergedJobDataMap` carries a known `workflowId`, a stubbed `IWorkflowL1Store` returning a workflow with at least one entry step, and stubbed `IStepDispatcher`/`WorkflowScheduler`/`TimeProvider` (mirror the existing scheduling tests' stubs). Assert: a log line emitted inside the dispatch loop / liveness path carries a captured scope with `CorrelationId` present (any non-empty Guid string — it is minted per fire) AND `WorkflowId` == the known workflowId. (CorrelationId is minted inside Execute, so assert its presence/shape, not a fixed value; WorkflowId is the deterministic input value.)
  </action>
  <verify>
    <automated>dotnet build src/Orchestrator -c Debug</automated>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release -- --filter-class *WorkflowFireJobScopeTests</automated>
  </verify>
  <acceptance_criteria>
    - `WorkflowFireJob.cs` contains `logger.BeginScope(new Dictionary<string, object>` with both `CorrelationKeys.LogScope` and `ExecutionLogScope.WorkflowId` keys
    - The `BeginScope` appears AFTER `var correlationId = NewId.NextGuid();` and BEFORE the `foreach (var entryStepId in wf.EntryStepIds)` loop; the dispatch loop + liveness refresh + reschedule are all INSIDE the `using` block
    - The two early returns (unparseable workflowId LogWarning; workflow-absent LogInformation) are NOT inside the new scope (they precede the correlationId mint)
    - No id is interpolated into a log template string (grep: no new `{CorrelationId}`/`{WorkflowId}` placeholder added; existing templates unchanged — additive only)
    - `WorkflowFireJobScopeTests` asserts a fire-loop log line's captured scope carries `CorrelationId` (non-empty) and `WorkflowId` == the input workflowId
    - `dotnet test ... -- --filter-class *WorkflowFireJobScopeTests` exits 0
    - `dotnet build src/Orchestrator -c Debug` exits 0 with 0 warnings / 0 errors
  </acceptance_criteria>
  <done>WorkflowFireJob.Execute wraps its post-mint body in an explicit CorrelationId+WorkflowId scope; early returns untouched; no template interpolation; the hermetic test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quartz job body → log telemetry | The per-fire `correlationId` (server-minted) and the parsed `workflowId` (from job-data, validated by `Guid.TryParse`) cross into the explicit log scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering (log injection via job-data workflowId) | WorkflowFireJob scope WorkflowId value | accept | `workflowId` is `Guid.TryParse`-validated BEFORE the scope (the early `:42-44` return rejects unparseable input); only a valid `Guid` reaches the scope, placed as a VALUE under a fixed key (T-18-04), never interpolated. Injection surface nil. |
| T-29-09 | Information Disclosure | CorrelationId / WorkflowId attributes | accept | Both are GUIDs — no PII/secret. No sensitive data newly placed into attributes. |

No `high` (block-worthy) threats: the WorkflowId is Guid-validated before scoping and the CorrelationId is server-minted; both are placed as structured values, not interpolated. No new auth/network/storage surface. Reasoning: the only external input (job-data workflowId) is type-validated upstream of the scope, so it cannot carry an injection payload.
</threat_model>

<verification>
- `WorkflowFireJobScopeTests` GREEN.
- `dotnet build src/Orchestrator -c Debug` 0/0.
- After the wave merges, the hermetic full suite (exclude Category=RealStack) is GREEN with no regression (existing WorkflowFireJob/scheduling tests still pass — the scope wrap is additive, logic unchanged).
</verification>

<success_criteria>
- WorkflowFireJob.Execute opens an explicit BeginScope(CorrelationId + WorkflowId) wrapping the post-mint body; the early returns are unaffected.
- CorrelationId via CorrelationKeys.LogScope, WorkflowId via ExecutionLogScope.WorkflowId; no template interpolation.
- The hermetic test is GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/29-structured-execution-scope-logging/29-04-SUMMARY.md`
</output>
