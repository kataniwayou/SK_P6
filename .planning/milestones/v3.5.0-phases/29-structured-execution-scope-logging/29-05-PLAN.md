---
phase: 29-structured-execution-scope-logging
plan: 05
type: execute
wave: 3
depends_on: [29-02, 29-03, 29-04]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs
  - scripts/phase-29-close.ps1
autonomous: false
requirements: [LOG-06, LOG-01]
tags: [logging, otel, scopes, e2e, real-stack, close-gate]

must_haves:
  truths:
    - "The extended real-stack E2E asserts at least one execution id reaches Elasticsearch from a SCOPE on a processor-side log (not the template-sourced orchestrator hit)"
    - "The new E2E assertion FAILS if the new scope work (filter/enricher) is reverted"
    - "Existing log templates and assertions are unchanged (additive only — no log-shape regression)"
    - "The phase close-gate (3x full-suite GREEN + triple-SHA BEFORE==AFTER) holds"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs"
      provides: "A second PollEsForLog asserting a scope-sourced execution id on a processor-sample log"
      contains: "processor-sample"
    - path: "scripts/phase-29-close.ps1"
      provides: "3x full-suite GREEN + triple-SHA close gate for phase 29"
      contains: "processor-sample"
  key_links:
    - from: "tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs"
      to: "Elasticsearch (processor-sample scope-sourced WorkflowId)"
      via: "second PollEsForLog with a term on resource.attributes.service.name == processor-sample"
      pattern: "resource\\.attributes\\.service\\.name.*processor-sample"
---

<objective>
Close the phase: (1) extend the existing real-stack `SampleRoundTripE2ETests` with a SECOND `PollEsForLog` that proves at least one execution id round-trips to Elasticsearch FROM A SCOPE on a PROCESSOR-side log; and (2) author `scripts/phase-29-close.ps1` (mirroring `phase-28-close.ps1`) for the 3x-full-suite-GREEN + triple-SHA close gate.

Purpose: This is the LOG-06 proof bar — no log-shape regression PLUS one real-stack assertion that can ONLY pass if the new scope work is present. CRITICAL (Landmine L1 / Pitfall 1): the EXISTING `attributes.WorkflowId` assertion (scoped to `service.name == "orchestrator"`) is TEMPLATE-sourced from `StartOrchestrationConsumer`'s `"Start reload for WorkflowId={WorkflowId}"` placeholder — `StartOrchestration` is NOT `IExecutionCorrelated`, so the new filter never touches it; that assertion would PASS even with the entire scope feature reverted. The NEW assertion must tie `attributes.WorkflowId` to a PROCESSOR-side log (the processor consumes `EntryStepDispatch`, which IS `IExecutionCorrelated`, so its consume logs get `WorkflowId` ONLY via the new filter), scoped to `resource.attributes.service.name == "processor-sample"` (VERIFIED from `src/Processor.Sample/appsettings.json` → `"Name": "processor-sample"`).

Output: the E2E extension (ADD ONLY an ES read — no new seeded state) + `phase-29-close.ps1`. This plan is NON-autonomous: the close-gate run (Task 2) is an operator-authorized checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structured-execution-scope-logging/29-SPEC.md
@.planning/phases/29-structured-execution-scope-logging/29-RESEARCH.md
@.planning/phases/29-structured-execution-scope-logging/29-PATTERNS.md

<interfaces>
The EXISTING template-sourced block to mirror in shape but NOT replace (VERIFIED this session — tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs:150-167). `StartReloadMessage = "Start reload for WorkflowId="` (:74), `EsPollTimeoutMs = 120_000` (:85), `wfId` is the seeded workflow id, `ct` is `TestContext.Current.CancellationToken`:

```csharp
using var es = new ElasticsearchTestClient();
var advanceQuery = $$"""
  { "size": 5, "sort": [ { "@timestamp": { "order": "desc" } } ],
    "query": { "bool": { "must": [
      { "term": { "attributes.WorkflowId": "{{wfId}}" } },
      { "term": { "resource.attributes.service.name": "orchestrator" } }
    ] } } }
  """;
var advance = await es.PollEsForLog(advanceQuery, timeoutMs: EsPollTimeoutMs, ct: ct);
Assert.NotNull(advance);
Assert.Contains(StartReloadMessage, advance!.Value.GetRawText());   // <-- TEMPLATE-sourced (L1) — keep, do not rely on for scope proof
```

The close-gate analog (VERIFIED — scripts/phase-28-close.ps1): `$services = @('postgres','redis','rabbitmq','otel-collector','elasticsearch','prometheus','orchestrator','processor-sample','baseapi-service')` (:136); 3x full-suite `dotnet test` runs collecting `Passed`/`Failed`; triple-SHA (`psql \l` + `redis-cli --scan` + `rabbitmqctl list_queues`) BEFORE==AFTER; the steady-state stable-Processor-row pre-flight; Release+Debug zero-warning build. processor-sample is REQUIRED healthy (NOT a health exception).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SampleRoundTripE2ETests with a scope-sourced processor-side ES assertion (L1)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the FULL file — the existing advanceQuery block :150-167, the teardown discipline :127-142, the constants `StartReloadMessage`/`EsPollTimeoutMs`, the `wfId`/`ct` in scope)
    - .planning/phases/29-structured-execution-scope-logging/29-RESEARCH.md (Landmine L1 / Pitfall 1 — why the existing WorkflowId hit is template-sourced and proves nothing about scopes; Pitfall 4 / L7 — ADD ONLY an ES read, no new seeded state)
    - src/Processor.Sample/appsettings.json (`"Name": "processor-sample"` — the resource.attributes.service.name term to pin)
    - tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs (the attributes.<Key> field-path convention — query the path DIRECTLY, never a .keyword sub-field) — and ElasticsearchTestClient.PollEsForLog signature
  </read_first>
  <action>
    Edit `tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs`: KEEP the existing `advanceQuery` block (the orchestrator/template hit — it still proves the orchestrator advanced; do NOT remove or change it). ADD a SECOND `PollEsForLog` immediately after it, asserting a SCOPE-sourced execution id on the PROCESSOR side. The processor consumes `EntryStepDispatch` (= `IExecutionCorrelated`), so its consume logs carry `attributes.WorkflowId` ONLY via the new execution-scope filter — scoping the query to `resource.attributes.service.name == "processor-sample"` makes the assertion fail if the new scope work is reverted (closing the L1 trap):

```csharp
// LOG-06 / L1: prove WorkflowId reached ES FROM A SCOPE, not from a template. The processor-sample
// service consumes EntryStepDispatch (IExecutionCorrelated), so its logs get attributes.WorkflowId
// ONLY via the new InboundExecutionScopeConsumeFilter — this assertion fails if the scope work is
// reverted. (The orchestrator hit above is template-sourced — StartOrchestration is NOT
// IExecutionCorrelated — and would pass even without scopes.)
var scopeProofQuery = $$"""
  {
    "size": 5,
    "sort": [ { "@timestamp": { "order": "desc" } } ],
    "query": {
      "bool": {
        "must": [
          { "term": { "attributes.WorkflowId": "{{wfId}}" } },
          { "term": { "resource.attributes.service.name": "processor-sample" } }
        ]
      }
    }
  }
  """;
var scopeProof = await es.PollEsForLog(scopeProofQuery, timeoutMs: EsPollTimeoutMs, ct: ct);
Assert.NotNull(scopeProof);   // WorkflowId round-tripped to ES from the new scope on a processor log
```

    Reuse the EXISTING `es` (`ElasticsearchTestClient`), `wfId`, `EsPollTimeoutMs`, and `ct` already in scope. ADD ONLY this ES read — do NOT seed any new Redis/RMQ/PG state and do NOT change the existing net-zero teardown (`L2KeysToCleanup`, `ParentIndexMembersToSrem` — :127-142), so the close-gate triple-SHA invariant holds (L7 / Pitfall 4; ES logs are append-only telemetry, not part of the triple-SHA). Use the `$$"""..."""` raw-string template (NOT manual concat) and query `attributes.WorkflowId` DIRECTLY (string → keyword; never a `.keyword` sub-field).

    NOTE: this test is `[Trait("Category","RealStack")]` — it requires the full compose stack (incl. a live `processor-sample` container running CURRENT code). Confirm `docker compose ps` shows all healthy before running it; rebuild stale containers (`docker compose up -d --build processor-sample orchestrator baseapi-service`) if they predate this phase's code, as prior phases required.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug</automated>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release -- --filter-class *SampleRoundTripE2ETests</automated>
  </verify>
  <acceptance_criteria>
    - `SampleRoundTripE2ETests.cs` contains a SECOND `PollEsForLog` whose query has a `term` on `resource.attributes.service.name` == `processor-sample` AND a `term` on `attributes.WorkflowId` == the seeded `wfId`
    - The new assertion is `Assert.NotNull(scopeProof)` (or equivalent) — it proves a processor-side scope-sourced WorkflowId reached ES
    - The EXISTING `advanceQuery` block (orchestrator/`StartReloadMessage`) is unchanged (still present — additive, no log-shape regression)
    - No new seeded Redis/RMQ/PG state added; the existing teardown lists (`L2KeysToCleanup`/`ParentIndexMembersToSrem`) are unchanged (`git diff` shows only the added ES-read block, not teardown changes)
    - The query targets `attributes.WorkflowId` DIRECTLY (no `.keyword` sub-field)
    - `dotnet test ... -- --filter-class *SampleRoundTripE2ETests` exits 0 against the full live stack (with a current-code processor-sample container)
    - L1 sanity: the new assertion would FAIL if the execution-scope filter (29-02) were reverted — note this reasoning in the SUMMARY (the processor-side service.name term cannot be satisfied by the orchestrator's template hit)
  </acceptance_criteria>
  <done>The real-stack E2E now has a processor-side, scope-sourced WorkflowId assertion that fails without the new scope work; the existing template hit is preserved; no new seeded state; the test is GREEN against the live stack.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/phase-29-close.ps1 (mirror phase-28-close.ps1)</name>
  <read_first>
    - scripts/phase-28-close.ps1 (the FULL script to copy — `$services` list at :136 already includes processor-sample; the 3x full-suite run loop :186+; the triple-SHA capture; the stable-Processor-row steady-state pre-flight; Release+Debug zero-warning build)
    - .planning/STATE.md (the Phase 28 close-gate evidence section — the proven steady-state procId / triple-SHA discipline)
    - .planning/phases/29-structured-execution-scope-logging/29-RESEARCH.md (Validation Architecture — phase gate = full suite incl. RealStack GREEN x3 + triple-SHA; L7 close-gate hygiene)
  </read_first>
  <action>
    Copy `scripts/phase-28-close.ps1` → `scripts/phase-29-close.ps1`. Keep the structure byte-faithful (the close-gate discipline is unchanged since Phase 3 D-15/D-18): the `$services` pre-flight health list INCLUDING `processor-sample` as REQUIRED healthy (NOT a health exception — a dead processor must fail the gate); the stable-Processor-row GET-or-create-on-source-hash steady-state pre-flight (so the live container's `skp:{procId:D}` liveness key + `{procId:D}` dispatch queue are in BOTH snapshots); 3 consecutive full-suite `dotnet test` runs (no Category filter — both real-stack E2Es incl. the extended `SampleRoundTripE2ETests` run live each run) each Exit=0 with identical Passed counts; triple-SHA (`psql \l` + `redis-cli --scan` + `rabbitmqctl list_queues`) captured BEFORE and AFTER, asserting BEFORE==AFTER; Release+Debug zero-warning build (0 Warning / 0 Error). Update ONLY the phase-identifying strings (any "phase 28"/"Phase 28"/"28" header/log labels → "29"); do NOT change the gate logic, the service list (already correct for v3.5.0), or the SHA commands. NO `FLUSHDB`. Keep it BOM-free and parse-clean (`pwsh -NoProfile -Command "& { . ./scripts/phase-29-close.ps1 }"` must at least parse; or run with `-WhatIf`-style guard if the script supports it — at minimum confirm it parses).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path ./scripts/phase-29-close.ps1), [ref]$null, [ref]$err); if ($err) { $err; exit 1 } else { 'PARSE OK' }"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-29-close.ps1` exists and parses clean (the ParseFile check prints PARSE OK, exit 0)
    - `$services` includes `processor-sample` (and the full v3.5.0 stack: postgres, redis, rabbitmq, otel-collector, elasticsearch, orchestrator, baseapi-service) — grep confirms
    - The script captures all three SHAs (`psql \l`, `redis-cli --scan`, `rabbitmqctl list_queues`) and asserts BEFORE==AFTER (grep the three commands)
    - The script runs the full suite 3x (no `Category` exclusion) and requires 3 identical Passed counts with Exit=0 each
    - The script contains NO `FLUSHDB` (grep returns no match)
    - The file is BOM-free (first bytes are not EF BB BF)
    - Phase-label strings reference 29 (not 28) where they identify the phase
  </acceptance_criteria>
  <done>scripts/phase-29-close.ps1 exists, parses clean, mirrors the proven phase-28 triple-SHA / 3x-GREEN discipline with processor-sample required-healthy, no FLUSHDB, BOM-free.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-authorized phase-29 close-gate run</name>
  <what-built>The phase-29 close gate (`scripts/phase-29-close.ps1`) is authored and parses clean; the real-stack E2E now carries the scope-sourced processor-side WorkflowId assertion. All hermetic tests (29-01..29-04) are GREEN. The gate run itself is operator-authorized (it brings up / requires the full live compose stack and runs the full suite 3x — minutes-long, environment-dependent).</what-built>
  <how-to-verify>
    1. Confirm the live stack: `docker compose ps` shows all of postgres, redis, rabbitmq, otel-collector, elasticsearch, orchestrator, processor-sample, baseapi-service healthy. If any container predates this phase's code, rebuild: `docker compose up -d --build processor-sample orchestrator baseapi-service`.
    2. Run the gate: `pwsh -NoProfile -File ./scripts/phase-29-close.ps1`.
    3. Expected: exit 0, with — 3 consecutive full-suite GREEN runs (identical Passed counts, Failed 0 each; both real-stack E2Es incl. the extended SampleRoundTripE2ETests ran live each run); triple-SHA (`psql \l` + `redis-cli --scan` + `rabbitmqctl list_queues`) BEFORE==AFTER all HELD; Release+Debug builds 0 Warning / 0 Error.
    4. If the gate surfaces a stale/flaky PRIOR-phase test (per the known close-gate pattern — first full live run in a while), triage: stale ES assertion / flaky race / dirty-before redis SHA are usually prior-phase, not this phase's scope.
  </how-to-verify>
  <resume-signal>Type "approved" (paste the gate evidence: 3x Passed counts, the three SHAs BEFORE==AFTER, exit code) or describe the failure to triage.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → Elasticsearch (read) | The E2E adds one ES query (read-only) for the scope-sourced WorkflowId. No new write boundary. |
| close-gate → live infra (read snapshots) | The gate reads `psql \l` / `redis-cli --scan` / `rabbitmqctl list_queues` snapshots; it does not mutate steady state (no FLUSHDB). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Information Disclosure | ES scope-proof query | accept | The query reads back a GUID `WorkflowId` it seeded itself — no PII/secret. ES logs are internal telemetry. No sensitive data exposure. |
| T-29-11 | Denial of Service / state leak (close-gate triple-SHA) | E2E added ES read | mitigate | The extension ADDS ONLY a read; no new seeded Redis/RMQ/PG state. ES docs are append-only telemetry, NOT part of the triple-SHA. The existing net-zero teardown is unchanged, so BEFORE==AFTER holds — verified by the gate's own SHA assertion. |

No `high` (block-worthy) threats: the E2E change is a read-only ES assertion against self-seeded GUIDs, and the close gate only reads infra snapshots without mutating steady state. Reasoning: no new write/seed crosses any boundary, so the close-gate invariant and data-exposure surface are both unchanged.
</threat_model>

<verification>
- `SampleRoundTripE2ETests` GREEN against the live stack with the processor-side scope-proof assertion present.
- `scripts/phase-29-close.ps1` parses clean, includes processor-sample as required-healthy, captures triple-SHA, no FLUSHDB, BOM-free.
- Close gate (operator-authorized): 3x full-suite GREEN + triple-SHA BEFORE==AFTER HELD + Release/Debug 0/0.
- Existing log templates/assertions unchanged (additive only — the existing advanceQuery block preserved).
</verification>

<success_criteria>
- The real-stack E2E proves ≥1 execution id round-trips to ES from a SCOPE on a processor-side log (L1 trap closed — fails if scope work reverted), while keeping the existing template hit.
- No log-shape regression (existing templates/assertions unchanged).
- phase-29-close.ps1 authored (mirrors phase-28), and the close gate (3x GREEN + triple-SHA BEFORE==AFTER) holds on the operator-authorized run.
</success_criteria>

<output>
After completion, create `.planning/phases/29-structured-execution-scope-logging/29-05-SUMMARY.md`
</output>
