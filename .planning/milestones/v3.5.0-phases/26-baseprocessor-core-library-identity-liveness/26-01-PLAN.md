---
phase: 26-baseprocessor-core-library-identity-liveness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseProcessor.Core/BaseProcessor.Core.csproj
  - src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs
  - src/BaseProcessor.Core/Identity/ISourceHashProvider.cs
  - src/BaseProcessor.Core/Identity/AssemblyMetadataSourceHashProvider.cs
  - src/BaseProcessor.Core/Identity/IProcessorContext.cs
  - src/BaseProcessor.Core/Identity/ProcessorContext.cs
  - src/BaseProcessor.Core/Processing/BaseProcessor.cs
  - src/BaseProcessor.Core/Processing/ProcessResult.cs
  - SK_P.sln
  - tests/BaseApi.Tests/BaseApi.Tests.csproj
  - tests/BaseApi.Tests/Processor/ProcessorTestHarness.cs
  - tests/BaseApi.Tests/Processor/SourceHashProviderFacts.cs
  - tests/BaseApi.Tests/Processor/ProcessorOptionsBindingFacts.cs
  - tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs
  - tests/BaseApi.Tests/Processor/RequestClientSchemeFacts.cs
autonomous: true
requirements: [BPC-01, BPC-02, IDENT-03, CONFIG-01]

must_haves:
  truths:
    - "A new src/BaseProcessor.Core project exists, references BaseConsole.Core + Messaging.Contracts (NOT BaseApi.Service), and builds clean on MassTransit 8.5.5"
    - "AssemblyMetadataSourceHashProvider reads [assembly: AssemblyMetadata(\"SourceHash\", ...)] via reflection and throws InvalidOperationException when the attribute is absent"
    - "IProcessorContext holds Id + three schema Ids + input/output definitions + a volatile IsHealthy flag and a Task WhenHealthy latch; SetIdentity/SetDefinition/MarkHealthy mutate it thread-safely"
    - "BaseProcessor abstract class declares exactly one abstract ProcessAsync seam (declared, not invoked this phase); a test double can override it and DI-resolve"
    - "ProcessorLivenessOptions binds Interval/Ttl/RequestTimeout/BackoffCap seconds independently from the Processor config section"
    - "The test project references BaseProcessor.Core, the Processor harness fixture sequences NotFound->NotFound->Found responder replies, and a harness round-trip confirms the exchange: request-client scheme + GetResponse<TFound,TNotFound>() overload resolve against a real in-memory responder"
  artifacts:
    - path: "src/BaseProcessor.Core/BaseProcessor.Core.csproj"
      provides: "New library project, CPM no-Version refs"
      contains: "BaseConsole.Core.csproj"
    - path: "src/BaseProcessor.Core/Identity/AssemblyMetadataSourceHashProvider.cs"
      provides: "Reflection SourceHash read (IDENT-03)"
      contains: "AssemblyMetadataAttribute"
    - path: "src/BaseProcessor.Core/Identity/IProcessorContext.cs"
      provides: "Shared identity/Healthy holder contract (D-06)"
      contains: "WhenHealthy"
    - path: "src/BaseProcessor.Core/Processing/BaseProcessor.cs"
      provides: "Abstract ProcessAsync seam (D-12 / BPC-02)"
      contains: "abstract"
    - path: "src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs"
      provides: "Interval/Ttl/RequestTimeout/BackoffCap knobs (CONFIG-01)"
      contains: "IntervalSeconds"
    - path: "tests/BaseApi.Tests/Processor/ProcessorTestHarness.cs"
      provides: "Sequenceable NotFound->NotFound->Found responder fixture"
      contains: "AddMassTransitTestHarness"
  key_links:
    - from: "tests/BaseApi.Tests/BaseApi.Tests.csproj"
      to: "src/BaseProcessor.Core/BaseProcessor.Core.csproj"
      via: "ProjectReference"
      pattern: "BaseProcessor\\.Core\\.csproj"
    - from: "tests/BaseApi.Tests/Processor/RequestClientSchemeFacts.cs"
      to: "exchange: scheme request client"
      via: "AddRequestClient + GetResponse<TFound,TNotFound>"
      pattern: "exchange:"
---

<objective>
Stand up the `BaseProcessor.Core` library skeleton + all dependency-free contracts (the SourceHash seam, the shared `IProcessorContext` holder, the abstract `ProcessAsync` seam, the liveness options POCO) AND the Wave 0 test scaffolding the rest of the phase samples against — including a harness round-trip that CONFIRMS the MEDIUM-confidence `exchange:` request-client scheme + the `GetResponse<TFound,TNotFound>()` overload (RESEARCH A2/A5) before Plan 02 relies on them.

Purpose: This is the interface-first wave. Plans 02 (composition root + startup orchestrator) and 03 (liveness heartbeat) build against the contracts created here, so they receive the exact type shapes rather than exploring the codebase. The IRequestClient-scheme confirmation surfaces a wrong research assumption EARLY (VALIDATION.md Wave 0).

Output: A new compiling `src/BaseProcessor.Core/` project wired into the solution and the test project, plus five Processor test fact files (SourceHash, options binding, abstract seam, the harness fixture, and the request-client-scheme confirmation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-baseprocessor-core-library-identity-liveness/26-CONTEXT.md
@.planning/phases/26-baseprocessor-core-library-identity-liveness/26-RESEARCH.md
@.planning/phases/26-baseprocessor-core-library-identity-liveness/26-PATTERNS.md
@.planning/phases/26-baseprocessor-core-library-identity-liveness/26-VALIDATION.md

<interfaces>
<!-- Frozen contracts the executor consumes verbatim. Do NOT redefine. -->

From src/Messaging.Contracts/ProcessorQueries.cs (consume unchanged):
```csharp
public sealed record GetProcessorBySourceHash(string SourceHash);
public sealed record ProcessorIdentityFound(Guid Id, Guid? InputSchemaId, Guid? OutputSchemaId, Guid? ConfigSchemaId);
public sealed record ProcessorIdentityNotFound(string SourceHash);
public sealed record GetSchemaDefinition(Guid SchemaId);
public sealed record SchemaDefinitionFound(string Definition);
public sealed record SchemaDefinitionNotFound(Guid SchemaId);
```

From src/Messaging.Contracts/ProcessorQueues.cs (endpoint name constants):
```csharp
public const string IdentityQuery = "processor-identity-query";
public const string SchemaQuery   = "schema-definition-query";
```

From src/BaseConsole.Core/Health/IStartupGate.cs (the latch idiom to mirror for ProcessorContext):
```csharp
public sealed class StartupGate : IStartupGate
{
    private int _isReady; // 0 = false, 1 = true (Interlocked has no bool overload in .NET 8)
    public bool IsReady => Volatile.Read(ref _isReady) == 1;
    public void MarkReady() => Interlocked.Exchange(ref _isReady, 1);
}
```

ProcessAsync signature locked by PROJECT.md:32:
```csharp
Task<IReadOnlyList<ProcessResult>> ProcessAsync(string inputData, string config, CancellationToken ct)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseProcessor.Core project + wire into solution and test project</name>
  <files>src/BaseProcessor.Core/BaseProcessor.Core.csproj, SK_P.sln, tests/BaseApi.Tests/BaseApi.Tests.csproj</files>
  <read_first>
    - tests/BaseApi.Tests/BaseApi.Tests.csproj:114-128 (the ProjectReference block + how Phase 18/19 added BaseConsole.Core / Orchestrator — the analog for adding BaseProcessor.Core)
    - .planning/phases/26-baseprocessor-core-library-identity-liveness/26-RESEARCH.md "Installation" block (lines 111-125 — the exact csproj ItemGroups + CPM no-Version convention + the firewall comment)
    - src/BaseConsole.Core/BaseConsole.Core.csproj (a sibling library csproj for TargetFramework/Nullable/ImplicitUsings property conventions)
  </read_first>
  <action>
    Create `src/BaseProcessor.Core/BaseProcessor.Core.csproj` as a .NET 8 library mirroring BaseConsole.Core's property block (TargetFramework net8.0, Nullable enable, ImplicitUsings enable). Use CPM — NO `Version=` attributes on any PackageReference (RESEARCH Pitfall 6: stay on the CPM-pinned MassTransit 8.5.5, never bump to 9.x).
    Add exactly:
    ```xml
    <ItemGroup>
      <PackageReference Include="MassTransit" />
      <PackageReference Include="StackExchange.Redis" />
    </ItemGroup>
    <ItemGroup>
      <ProjectReference Include="..\BaseConsole.Core\BaseConsole.Core.csproj" />
      <ProjectReference Include="..\Messaging.Contracts\Messaging.Contracts.csproj" />
      <!-- NOT BaseApi.Service — firewall: the processor talks to the WebApi only over the bus. -->
    </ItemGroup>
    ```
    Add the new project to `SK_P.sln` (use `dotnet sln SK_P.sln add src/BaseProcessor.Core/BaseProcessor.Core.csproj`).
    Add `<ProjectReference Include="..\..\src\BaseProcessor.Core\BaseProcessor.Core.csproj" />` to the existing ProjectReference ItemGroup in `tests/BaseApi.Tests/BaseApi.Tests.csproj` (the block ending at line 128, after the Orchestrator reference at line 127), with a one-line comment noting it is the Phase 26 standalone-validation reference (mirrors the Phase 18/19 comments). MassTransit.Testing, FakeTimeProvider, StackExchange.Redis, and NSubstitute are already referenced by the test project — add NO new packages.
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj` exits 0.
    - grep `BaseProcessor.Core.csproj` in `tests/BaseApi.Tests/BaseApi.Tests.csproj` returns the new ProjectReference line.
    - grep `Version=` in `src/BaseProcessor.Core/BaseProcessor.Core.csproj` returns NO matches (CPM convention).
    - grep `BaseApi.Service` in `src/BaseProcessor.Core/BaseProcessor.Core.csproj` returns NO matches (firewall).
  </acceptance_criteria>
  <done>The new library project compiles, is in the solution, and is referenced by tests/BaseApi.Tests with no BaseApi.Service reference and no Version= attributes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identity contracts — ISourceHashProvider, IProcessorContext, abstract BaseProcessor, ProcessResult, ProcessorLivenessOptions</name>
  <files>src/BaseProcessor.Core/Identity/ISourceHashProvider.cs, src/BaseProcessor.Core/Identity/AssemblyMetadataSourceHashProvider.cs, src/BaseProcessor.Core/Identity/IProcessorContext.cs, src/BaseProcessor.Core/Identity/ProcessorContext.cs, src/BaseProcessor.Core/Processing/BaseProcessor.cs, src/BaseProcessor.Core/Processing/ProcessResult.cs, src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs, tests/BaseApi.Tests/Processor/SourceHashProviderFacts.cs, tests/BaseApi.Tests/Processor/ProcessorOptionsBindingFacts.cs, tests/BaseApi.Tests/Processor/BaseProcessorSeamFacts.cs</files>
  <read_first>
    - src/BaseConsole.Core/Health/IStartupGate.cs:13-45 (the interface + `public sealed` latch impl with Volatile.Read/Interlocked.Exchange — mirror this for ProcessorContext.IsHealthy; the doc-comment at 31-34 explains why `public sealed` is required for cross-assembly AddSingleton resolution)
    - src/BaseApi.Core/Configuration/RedisProjectionOptions.cs:14-31 (sealed POCO, seconds/days int with defaults + per-field doc-comment — the analog for ProcessorLivenessOptions)
    - src/Messaging.Contracts/Projections/LivenessProjection.cs:11-14 (sealed positional record shape — the analog for ProcessResult)
    - src/Messaging.Contracts/ProcessorQueries.cs (the ProcessorIdentityFound fields SetIdentity destructures: Id, InputSchemaId?, OutputSchemaId?, ConfigSchemaId?)
    - .planning/phases/26-baseprocessor-core-library-identity-liveness/26-RESEARCH.md §4 (Code Examples — the AssemblyMetadataSourceHashProvider body) + Open Q1 (the volatile bool + Task WhenHealthy recommendation) + Open Q2 (throw-when-absent)
  </read_first>
  <behavior>
    - SourceHashProviderFacts: AssemblyMetadataSourceHashProvider stubbed via a test assembly carrying `[assembly: AssemblyMetadata("SourceHash","<64-hex>")]` (or assert the absent-path) -> throws InvalidOperationException whose message names "SourceHash" when the attribute is absent on the executing assembly. (Use the default impl against an assembly with no SourceHash attribute to exercise the throw; the happy path is exercised by an NSubstitute stub of ISourceHashProvider in later plans.)
    - ProcessorOptionsBindingFacts: binding an in-memory config dictionary { "Processor:Interval"="10", "Processor:Ttl"="45", "Processor:RequestTimeout"="7", "Processor:BackoffCap"="30" } to ProcessorLivenessOptions yields IntervalSeconds=10, TtlSeconds=45, RequestTimeoutSeconds=7, BackoffCapSeconds=30 — Interval and Ttl bind to two independent properties (CONFIG-01). Binding an EMPTY config yields the baked defaults.
    - BaseProcessorSeamFacts: a test-double subclass of BaseProcessor overriding ProcessAsync compiles and DI-resolves; calling the override returns the expected IReadOnlyList<ProcessResult>. (Seam is declared, NOT invoked by framework this phase.)
  </behavior>
  <action>
    Create under `src/BaseProcessor.Core/`:

    `Identity/ISourceHashProvider.cs` — `public interface ISourceHashProvider { string Get(); }`.

    `Identity/AssemblyMetadataSourceHashProvider.cs` — `public sealed class AssemblyMetadataSourceHashProvider : ISourceHashProvider`. `Get()` reads `(Assembly.GetEntryAssembly() ?? Assembly.GetExecutingAssembly()).GetCustomAttributes<AssemblyMetadataAttribute>().FirstOrDefault(a => a.Key == "SourceHash")?.Value` and, when null, throws `new InvalidOperationException("Assembly metadata 'SourceHash' is missing. The MSBuild embed target (IDENT-01/02, Phase 28) must emit [assembly: AssemblyMetadata(\"SourceHash\", \"<64-hex>\")].")` (fail-fast per RESEARCH Open Q2 — a null/empty hash would otherwise drive an infinite silent not-found retry). Names the KEY, never a value (V7 / Info-Disclosure mitigation).

    `Identity/IProcessorContext.cs` — interface exposing: `Guid? Id { get; }`, `Guid? InputSchemaId { get; }`, `Guid? OutputSchemaId { get; }`, `Guid? ConfigSchemaId { get; }`, `string? InputDefinition { get; }`, `string? OutputDefinition { get; }`, `bool IsHealthy { get; }`, `Task WhenHealthy { get; }`, `void SetIdentity(ProcessorIdentityFound identity)`, `void SetDefinition(Guid schemaId, string definition)`, `void MarkHealthy()`. (Per RESEARCH Open Q1: expose BOTH a cheap `volatile bool IsHealthy` for the per-beat heartbeat read AND a `Task WhenHealthy` for Phase 27's queue-bind-after-Healthy await — costs nothing now, forward-fits EXEC-01.)

    `Identity/ProcessorContext.cs` — `public sealed class ProcessorContext : IProcessorContext` (`public sealed` so `AddSingleton<IProcessorContext, ProcessorContext>()` resolves cross-assembly without InternalsVisibleTo — same reason as StartupGate). Back `IsHealthy` with the StartupGate `Volatile.Read`/`Interlocked.Exchange(ref _isHealthy, 1)` int-latch idiom. Back `WhenHealthy` with a `TaskCompletionSource` (`TaskCreationOptions.RunContinuationsAsynchronously`) completed in `MarkHealthy()`. `SetIdentity(ProcessorIdentityFound identity)` stores Id + the three schema Ids. `SetDefinition(Guid schemaId, string definition)` stores the definition into InputDefinition if schemaId == InputSchemaId, OutputDefinition if schemaId == OutputSchemaId. `MarkHealthy()` flips the latch and completes WhenHealthy (idempotent — guard the TCS so a second call is a no-op).

    `Processing/ProcessResult.cs` — `public sealed record ProcessResult();` (minimal positional record — concrete fields are firmed up in Phase 27; declared now). Mirror the LivenessProjection sealed-record shape.

    `Processing/BaseProcessor.cs` — `public abstract class BaseProcessor` declaring exactly the locked seam: `protected abstract Task<IReadOnlyList<ProcessResult>> ProcessAsync(string inputData, string config, CancellationToken ct);` (D-12 / BPC-02 — declared now, INVOKED in Phase 27; no infra/id/L2/bus code).

    `Configuration/ProcessorLivenessOptions.cs` — `public sealed class ProcessorLivenessOptions` with four independent seconds-int auto-properties + baked defaults + per-field doc-comments (mirror RedisProjectionOptions): `public int IntervalSeconds { get; set; } = 10;` (heartbeat delay, CONFIG-01), `public int TtlSeconds { get; set; } = 30;` (sliding key expiry, CONFIG-01 — independent of Interval), `public int RequestTimeoutSeconds { get; set; } = 8;` (per-IRequestClient timeout, D-04 ~5-10s), `public int BackoffCapSeconds { get; set; } = 30;` (retry backoff cap, D-04). NOTE the config keys are `Interval`/`Ttl`/`RequestTimeout`/`BackoffCap` under section `Processor` — the property names carry the `Seconds` suffix but the bound config keys do not (set `[ConfigurationKeyName]` if the binder needs it, OR name the config keys to match; pick whichever binds cleanly — confirm in ProcessorOptionsBindingFacts).

    Then create the three test files under `tests/BaseApi.Tests/Processor/` per the <behavior> block (xunit.v3 facts; ProcessorOptionsBindingFacts uses `new ConfigurationBuilder().AddInMemoryCollection(...)` + `.GetSection("Processor").Get<ProcessorLivenessOptions>()` or `Bind`).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class "*SourceHashProviderFacts*" --filter-class "*ProcessorOptionsBindingFacts*" --filter-class "*BaseProcessorSeamFacts*"</automated>
  </verify>
  <acceptance_criteria>
    - The three filter-class slices run and pass (`Passed`, `Failed: 0`).
    - grep `Task WhenHealthy` in `src/BaseProcessor.Core/Identity/IProcessorContext.cs` returns a match.
    - grep `abstract Task<IReadOnlyList<ProcessResult>> ProcessAsync` in `src/BaseProcessor.Core/Processing/BaseProcessor.cs` returns a match.
    - grep `AssemblyMetadataAttribute` in `src/BaseProcessor.Core/Identity/AssemblyMetadataSourceHashProvider.cs` returns a match.
    - grep `IntervalSeconds` AND `TtlSeconds` in `src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs` both return matches (two independent values, CONFIG-01).
    - ProcessorContext is declared `public sealed`.
  </acceptance_criteria>
  <done>All five contract files exist; SourceHash throws-when-absent, the context holder exposes IsHealthy + WhenHealthy + SetIdentity/SetDefinition/MarkHealthy, the abstract seam declares the locked ProcessAsync signature, and the options POCO binds Interval/Ttl/RequestTimeout/BackoffCap independently — proven by three passing fact slices.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 harness fixture + confirm the exchange: request-client scheme and dual-response overload</name>
  <files>tests/BaseApi.Tests/Processor/ProcessorTestHarness.cs, tests/BaseApi.Tests/Processor/RequestClientSchemeFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs:51-101 (the AddMassTransitTestHarness + UsingInMemory + ConfigureEndpoints + Start/Stop + Consumed.Any pattern — the harness analog)
    - src/BaseApi.Service/Composition/ResponderMessaging.cs:38-41 (how the WebApi binds the named ReceiveEndpoint(ProcessorQueues.IdentityQuery/SchemaQuery) the request client targets — confirms exchange:{name} is the routing target)
    - src/Messaging.Contracts/ProcessorQueries.cs + ProcessorQueues.cs (the dual-response contracts + endpoint-name constants)
    - .planning/phases/26-baseprocessor-core-library-identity-liveness/26-RESEARCH.md §2/§3 + Assumptions A2/A4/A5 (the MEDIUM-confidence claims this task confirms: exchange: scheme, RequestTimeout.After overload, response.Is(out Response<T>))
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Processor/ProcessorTestHarness.cs` — a reusable in-memory MassTransit harness helper for the Processor test slice (mirror ConsoleCorrelationFilterTests' AddMassTransitTestHarness/UsingInMemory shape). It must:
    - Register two consumers acting as the WebApi responders, bound on NAMED receive endpoints `ProcessorQueues.IdentityQuery` and `ProcessorQueues.SchemaQuery` (use `x.AddConsumer<...>()` + endpoint config / `cfg.ReceiveEndpoint(ProcessorQueues.IdentityQuery, e => e.ConfigureConsumer<...>(ctx))` so the request client's `exchange:{name}` target resolves).
    - The identity responder must be SEQUENCEABLE: it responds `ProcessorIdentityNotFound(request.SourceHash)` for the first N calls then `ProcessorIdentityFound(...)` thereafter (back the sequence with an injected counter/queue the test controls — e.g. a shared `Queue<bool>` or an int threshold). The schema responder is similarly sequenceable: `SchemaDefinitionNotFound(request.SchemaId)` then `SchemaDefinitionFound("<definition>")`. This NotFound->NotFound->Found responder is the Wave 0 fixture Plans 02 reuse to prove retry-then-resolve.
    - Register the two request clients exactly as the production composition will: `x.AddRequestClient<GetProcessorBySourceHash>(new Uri("exchange:" + ProcessorQueues.IdentityQuery));` and `x.AddRequestClient<GetSchemaDefinition>(new Uri("exchange:" + ProcessorQueues.SchemaQuery));`.

    Create `tests/BaseApi.Tests/Processor/RequestClientSchemeFacts.cs` — a single confirmation fact (Wave 0 gate, RESEARCH A2/A5):
    - Build the harness with the responders configured to respond Found immediately.
    - Resolve `IRequestClient<GetProcessorBySourceHash>`, call `await client.GetResponse<ProcessorIdentityFound, ProcessorIdentityNotFound>(new GetProcessorBySourceHash("deadbeef"), ct, RequestTimeout.After(s: 5))`, and assert `response.Is(out Response<ProcessorIdentityFound> found)` is true and `found.Message.Id` is the responder's value. This CONFIRMS the `exchange:` URI scheme routes to the named endpoint AND the `GetResponse<TFound,TNotFound>()` overload + `response.Is(out ...)` API exist as researched.
    - If the build reveals the overload arg-order or `.Is` API differs from RESEARCH §3, FIX the harness/test to the actual 8.5.5 API and record the correction in the SUMMARY (this is the explicit purpose of the Wave 0 confirmation — surface a wrong assumption now, not in Plan 02).
    - Use the ConsoleCorrelationFilterTests Start/try/finally-Stop discipline with a CancellationTokenSource timeout so a routing failure fails the test fast rather than hanging.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class "*RequestClientSchemeFacts*"</automated>
  </verify>
  <acceptance_criteria>
    - `*RequestClientSchemeFacts*` slice runs and passes (`Passed`, `Failed: 0`) — confirming exchange:{name} routing + GetResponse<TFound,TNotFound>() + response.Is.
    - grep `exchange:` in `tests/BaseApi.Tests/Processor/ProcessorTestHarness.cs` returns a match.
    - grep `GetResponse<ProcessorIdentityFound, ProcessorIdentityNotFound>` in `tests/BaseApi.Tests/Processor/RequestClientSchemeFacts.cs` returns a match.
    - The harness exposes a sequenceable responder (grep `NotFound` in ProcessorTestHarness.cs returns a match).
  </acceptance_criteria>
  <done>The Processor harness fixture exists with a sequenceable NotFound->Found responder bound on the named endpoints, and a passing confirmation fact proves the exchange: request-client scheme + the dual-response overload resolve against a real in-memory responder (Wave 0 A2/A5 confirmed, or the SUMMARY records the corrected API).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config -> ProcessorLivenessOptions | appsettings values cross into runtime knobs (Interval/Ttl/RequestTimeout/BackoffCap) |
| assembly metadata -> SourceHash read | a string attribute value crosses into the identity-query input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | ProcessorLivenessOptions / config read | mitigate | Follow `cfg.Require` posture (RequiredConfig.cs:20-23): any required config error names the KEY only, never the value. Options POCO has no secret fields. |
| T-26-02 | Denial of Service | SourceHash absent -> infinite silent retry | mitigate | AssemblyMetadataSourceHashProvider.Get() THROWS InvalidOperationException (fail-fast, RESEARCH Open Q2) when the attribute is absent, rather than returning null/empty that would drive an unbounded not-found retry on an empty hash. |
| T-26-03 | Tampering | malformed L2 value (downstream) | accept | Not produced this plan (heartbeat is Plan 03); the reuse-the-typed-record mitigation (D-09) lands there. No writer surface here. |
</threat_model>

<verification>
- `dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj` exits 0.
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class "*Processor*"` runs the new slice green (SourceHash, options, seam, request-client-scheme facts).
- The full suite still builds: `dotnet build SK_P.sln -c Debug` exits 0 (no regression from the new project/csproj edits).
</verification>

<success_criteria>
- New `src/BaseProcessor.Core/` library compiles, is in the solution, references only BaseConsole.Core + Messaging.Contracts, and is referenced by tests/BaseApi.Tests.
- ISourceHashProvider + default reflection impl (throws-when-absent), IProcessorContext (Id + 3 schema Ids + defs + IsHealthy + WhenHealthy), abstract BaseProcessor.ProcessAsync seam, ProcessResult, and ProcessorLivenessOptions (4 independent seconds knobs) all exist and DI-resolve.
- The Wave 0 harness fixture (sequenceable NotFound->Found responder on named endpoints) exists and the exchange: request-client scheme + GetResponse<TFound,TNotFound>() overload are CONFIRMED by a passing fact.
</success_criteria>

<output>
After completion, create `.planning/phases/26-baseprocessor-core-library-identity-liveness/26-01-SUMMARY.md`. If the Wave 0 confirmation revealed the actual MassTransit 8.5.5 request-client API differs from RESEARCH §3 (overload arg-order or response.Is), RECORD the corrected API verbatim in the SUMMARY so Plan 02 uses the confirmed shape.
</output>
