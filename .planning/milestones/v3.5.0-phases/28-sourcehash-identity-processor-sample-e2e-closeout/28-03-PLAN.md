---
phase: 28-sourcehash-identity-processor-sample-e2e-closeout
plan: 03
type: execute
wave: 3
depends_on: [28-01, 28-02]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "A live processor-sample container resolves identity by its genuine embedded hash, binds queue:{id:D}, and writes a fresh skp:{id:D} Healthy heartbeat"
    - "The E2E registers the Processor DB row with the GENUINE reflected embedded hash (not a synthetic/random one) and null schema Ids"
    - "The E2E does NOT seed the liveness key — it polls host Redis for the real container's skp:{id:D} before POSTing Start (truthful liveness gate, SC#4)"
    - "POST /api/v1/orchestration/start returns 204 NoContent (Start admitted because the live processor is Healthy)"
    - "The round-trip completes: a dispatch is consumed, output is written to L2 (skp:data:*), and the orchestrator advances on the returned ExecutionResult"
    - "The test cleans up the skp:data:* keys it minted (net-zero teardown); the steady-state skp:{id:D} liveness key is left (in both gate snapshots)"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs"
      provides: "Real-stack E2E proving live round-trip + truthful liveness-gated Start"
      contains: "SampleRoundTripE2ETests"
  key_links:
    - from: "SampleRoundTripE2ETests"
      to: "Processor.Sample.dll embedded SourceHash"
      via: "reflect AssemblyMetadataAttribute, register as DB row"
      pattern: "GetCustomAttributes<.*AssemblyMetadataAttribute>"
    - from: "SampleRoundTripE2ETests"
      to: "real processor-sample container skp:{id:D}"
      via: "poll host Redis (NOT seed)"
      pattern: "L2ProjectionKeys.Processor"
    - from: "SampleRoundTripE2ETests"
      to: "POST /api/v1/orchestration/start"
      via: "HttpClient PostAsJsonAsync"
      pattern: "orchestration/start"
---

<objective>
Author the real-stack E2E proving the live orchestrator -> Processor.Sample -> orchestrator round-trip and the truthful liveness-gated Start, mirroring CorrelationPropagationE2ETests but OMITTING the synthetic liveness seed.

Purpose: TEST-01 / SC#4. This is the only proof that the whole milestone works end-to-end against real containers: a genuine heartbeating Processor.Sample makes the Start liveness gate pass truthfully (D-07), and the genuine embedded SourceHash (Plan 01/02) closes the identity loop (D-08). Depends on Plan 02's verified cross-OS reproducibility — the E2E reflects the host-built hash but the live container runs the Docker-built hash; they must be equal (already gated by Plan 02 Task 2).
Output: `tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-CONTEXT.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-RESEARCH.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-PATTERNS.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-02-SUMMARY.md

<interfaces>
<!-- The harness to mirror MINUS the synthetic seed -->
tests/BaseApi.Tests/Orchestrator/CorrelationPropagationE2ETests.cs:
  - Class attrs (lines 55-58): [Trait("Category","E2E")] [Trait("Category","RealStack")] [Collection("Observability")]
  - RealStackWebAppFactory (lines 296-406): env-var-in-ctor host overrides (RMQ localhost:5673, Redis localhost:6380, Postgres localhost:5433, otel localhost:4317); L2KeysToCleanup / ParentIndexMembersToSrem net-zero teardown drained in DisposeAsync (lines 379-405).
  - SeedProcessorAsync/SeedStepAsync/SeedWorkflowAsync (lines 234-278): CRUD seed helpers. SeedProcessorAsync currently uses HashHelpers.RandomSha256Hex() (line 241) -> REPLACE with the genuine embedded hash. InputSchemaId/OutputSchemaId/ConfigSchemaId already null (lines 242-243).
  - Start drive (lines 93-95): client.PostAsJsonAsync("/api/v1/orchestration/start", new List<Guid>{ wfId }) -> Assert 204 NoContent.
  - SeedHostProcessorLiveAsync (analog line 89) -> DO NOT CALL (Pitfall 3 — the real container heartbeats).
  - ElasticsearchTestClient.PollEsForLog (lines 125-176) -> reuse for the "orchestrator advances" assertion (term-on-attributes query).

L2 key shapes (src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:31-39):
  Processor(procId) = "skp:{procId:D}"  (the liveness key to POLL, never seed)
  ExecutionData(entryId) = "skp:data:{entryId:D}"  (the round-trip output key to clean up; server-minted entryId is unknown -> SCAN skp:data:*)
  ParentIndex() = "skp:"  (SREM members, never KeyDelete)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SampleRoundTripE2ETests — genuine-hash registration, NO liveness seed, poll real heartbeat, Start, assert round-trip, net-zero teardown</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/CorrelationPropagationE2ETests.cs (the FULL harness to mirror — class attrs, RealStackWebAppFactory env-var ctor + DisposeAsync teardown, Seed* helpers, the Start drive, the ES poll; AND the SeedHostProcessorLiveAsync call to OMIT)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (Processor/ExecutionData/ParentIndex key builders — exact shapes)
    - tests/BaseApi.Tests/Processor/SourceHashEmbedFacts.cs (the reflection idiom for reading the genuine embedded hash — reuse it here, do NOT recompute)
    - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs (the "Processor row not yet registered for hash {Hash}" boot-before-register loop, so the test knows the container resolves AFTER the row is seeded) + src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs (writes skp:data:{newEntryId} on output, the round-trip output to assert)
    - .planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-RESEARCH.md §"Code Examples §6" + Pitfall 3 + Pitfall 4 + §PATTERNS SampleRoundTripE2ETests
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` (namespace matching CorrelationPropagationE2ETests; `[Trait("Category","E2E")] [Trait("Category","RealStack")] [Collection("Observability")]`). Mirror CorrelationPropagationE2ETests with these EXACT changes:

    1. **Reuse RealStackWebAppFactory WHOLESALE** — the env-var-in-ctor host overrides (RMQ localhost:5673 / Redis localhost:6380 / Postgres localhost:5433 / otel localhost:4317) and the L2KeysToCleanup / ParentIndexMembersToSrem net-zero teardown. Do NOT fork it.

    2. **Read the GENUINE embedded hash (D-08)** — reflect the built Processor.Sample.dll exactly as the runtime reader / SourceHashEmbedFacts does:
       ```csharp
       var hash = typeof(Processor.Sample.SampleProcessor).Assembly
           .GetCustomAttributes<System.Reflection.AssemblyMetadataAttribute>()
           .First(a => a.Key == "SourceHash").Value!;
       ```
       Do NOT use HashHelpers.RandomSha256Hex(). Do NOT recompute the hash (anti-pattern).

    3. **Register the Processor DB row via CRUD with THAT hash + null schemas (D-05)** — adapt SeedProcessorAsync to take the genuine `hash` and keep InputSchemaId/OutputSchemaId/ConfigSchemaId null. The id the WebApi assigns at row create is `procId`; the live container will resolve THIS id by querying GetProcessorBySourceHash(hash) (the identity loop closes because the row carries the same hash). Then SeedStepAsync(procId) -> stepId; SeedWorkflowAsync([stepId]) -> wfId.

    4. **DO NOT seed the liveness key (Pitfall 3 / D-07)** — OMIT `factory.SeedHostProcessorLiveAsync(...)`. Instead, after seeding the row, POLL host Redis for `L2ProjectionKeys.Processor(procId)` (`skp:{procId:D}`) presence + freshness, with a generous timeout (the container boots, loops "row not yet registered", then resolves + binds + MarkHealthy + writes the key — allow up to ~60s with the compose start_period 30s + identity-resolve latency). Only proceed once the key exists (the REAL container went Healthy). Fail with a clear message if it never appears (that would mean identity divergence — Plan 02's gate should have caught it).

    5. **Drive Start** — `var startResp = await client.PostAsJsonAsync("/api/v1/orchestration/start", new List<Guid> { wfId }, ct); Assert.Equal(HttpStatusCode.NoContent, startResp.StatusCode);` Start passes ONLY because the live container's heartbeat is fresh (truthful SC#4 gate).

    6. **Assert the round-trip (two clauses, SC#4)** — (a) "output written to L2": SCAN host Redis for `skp:data:*` keys that appeared after Start (the entryId is server-minted/unknown — verified EntryStepDispatchConsumer mints NewId.NextGuid()); assert at least one new `skp:data:*` key exists. (b) "orchestrator advances": reuse `ElasticsearchTestClient.PollEsForLog` (the otel->ES precedent in CorrelationPropagationE2ETests) to poll for the orchestrator step-advancement / result-consumed seam log (confirm the exact log message text by reading ResultConsumer / StepAdvancement). Recommend asserting BOTH clauses (RESEARCH §6 note).

    7. **Net-zero teardown (Pitfall 4)** — register the run's new `skp:data:*` keys into the factory's `L2KeysToCleanup` (drained in DisposeAsync), SREM any parent-index members the run added, and delete the L2 root/step keys the Start created. Do NOT delete `skp:{procId:D}` (steady-state — the live container keeps refreshing it; it is in BOTH gate snapshots, leave it — Plan 04). Do NOT FLUSHDB. The Processor DB row may be left or cleaned (psql -lqt is db-list level, not row level — does not break the gate; clean for hygiene if trivial).
  </action>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` exists with `[Trait("Category", "RealStack")]`.
    - `grep "GetCustomAttributes<.*AssemblyMetadataAttribute>" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` matches AND there is NO `RandomSha256Hex` and NO `ComputeHash`/`SHA256` in the file (genuine reflected hash, not synthetic, not recomputed — D-08).
    - `grep "SeedHostProcessorLive" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` returns NOTHING (the synthetic seed is OMITTED — Pitfall 3).
    - `grep "L2ProjectionKeys.Processor" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` matches (polls the real liveness key) AND `grep "skp:data" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` matches (SCAN for output) AND `grep "orchestration/start" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` matches.
    - `grep "NoContent" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` matches (Start assertion).
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug` = exit 0, 0 Warning / 0 Error (the test compiles).
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~SampleRoundTrip"` = exit 0, the E2E PASSES against the live stack (requires the full compose stack incl. processor-sample up healthy).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~SampleRoundTrip"</automated>
  </verify>
  <done>SampleRoundTripE2ETests registers the genuine embedded hash, omits the synthetic liveness seed, polls the real container's skp:{id:D}, drives Start to 204, asserts the round-trip (skp:data output + orchestrator-advance ES log), and cleans up net-zero; the E2E passes live.</done>
</task>

<task type="auto">
  <name>Task 2: Full hermetic suite + Release build regression gate</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the file just added — confirm it carries the RealStack trait so the hermetic filter excludes it)
    - .planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-VALIDATION.md §"Sampling Rate" (per-wave merge: full hermetic suite + Release zero-warning build)
  </read_first>
  <action>
    No new code. Run the per-wave merge sampling gate (28-VALIDATION.md): the full hermetic suite (`--filter "Category!=RealStack"`) to confirm zero regression from the E2E addition, and the zero-warning Release build of the whole solution (TreatWarningsAsErrors is on — the E2E test file must not introduce a warning). If either fails, fix the offending code (most likely an unused-using or nullable warning in the new test file) and re-run. This task exists to satisfy the Nyquist sampling continuity rule (no automated-verify gap) and to catch a TreatWarningsAsErrors break before the close gate.
  </action>
  <acceptance_criteria>
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "Category!=RealStack"` = exit 0, 0 failed (no regression in the hermetic tier).
    - `dotnet build SK_P.sln -c Release` = exit 0, 0 Warning(s) / 0 Error(s).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <done>Full hermetic suite green (0 failed) and Release build zero-warning — the E2E addition introduced no regression and no TreatWarningsAsErrors break.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process WebApi -> host compose stack | The E2E driver crosses into real containers (RMQ/Redis/Postgres) via host ports; untrusted only in the sense of test isolation. |
| seeded DB row -> live container identity | The registered SourceHash must be the genuine embedded value or identity never resolves (correctness, not security). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Spoofing | Synthetic liveness seed masking the real gate | mitigate | OMIT SeedHostProcessorLiveAsync (Pitfall 3) — the test asserts the REAL container's heartbeat, so a false-green (test passing with the container stopped) is impossible; grep acceptance criterion enforces the omission. |
| T-28-09 | Spoofing | Forged/random SourceHash registered as the row | mitigate | Register the GENUINE reflected embedded hash (D-08); grep guards against RandomSha256Hex / recompute; the DB ^[a-f0-9]{64}$ validator also gates it at CRUD. |
| T-28-10 | Tampering | Leaked skp:data:* keys breaking the gate SHA | mitigate | Net-zero teardown drains the run's skp:data:* keys via L2KeysToCleanup + short container ExecutionDataTtl (Plan 02); steady-state liveness key left in both snapshots. |

No high-severity threats; test-harness surface over a dev stack.
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug` = 0/0.
- `dotnet test ... --filter "FullyQualifiedName~SampleRoundTrip"` = E2E passes live (full stack incl. processor-sample healthy).
- `dotnet test ... --filter "Category!=RealStack"` = 0 failed (no regression).
- `dotnet build SK_P.sln -c Release` = 0 Warning / 0 Error.
</verification>

<success_criteria>
- The E2E proves the live round-trip: dispatch consumed, output written to L2 (skp:data:*), orchestrator advances on the ExecutionResult.
- The E2E proves the truthful liveness-gated Start: a real heartbeating processor-sample (not a synthetic seed) lets Start return 204.
- Identity closes against the GENUINE embedded hash (D-08); no synthetic/random hash, no recompute.
- Net-zero teardown (skp:data:* cleaned, liveness key left); hermetic suite + Release build green.
</success_criteria>

<output>
After completion, create `.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-03-SUMMARY.md`
</output>
