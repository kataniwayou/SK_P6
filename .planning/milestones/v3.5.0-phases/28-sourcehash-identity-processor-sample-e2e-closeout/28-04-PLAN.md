---
phase: 28-sourcehash-identity-processor-sample-e2e-closeout
plan: 04
type: execute
wave: 4
depends_on: [28-01, 28-02, 28-03]
files_modified:
  - scripts/phase-28-close.ps1
autonomous: false
requirements: [TEST-02]
must_haves:
  truths:
    - "scripts/phase-28-close.ps1 mirrors phase-22-close.ps1 with processor-sample added to the $services pre-flight list"
    - "The gate pre-flight tolerates processor-sample being healthy once a stable Processor DB row exists (steady-state), so its skp:{id:D} liveness key + queue:{id:D} are in BOTH the BEFORE and AFTER snapshots"
    - "The gate runs 3 consecutive GREEN full-suite runs (RealStack live) with zero-warning Release + Debug builds"
    - "Triple-SHA (psql -lqt / redis-cli --scan / rabbitmqctl list_queues) BEFORE == AFTER for all three"
  artifacts:
    - path: "scripts/phase-28-close.ps1"
      provides: "Phase 28 triple-SHA / 3-GREEN close gate"
      contains: "processor-sample"
  key_links:
    - from: "scripts/phase-28-close.ps1"
      to: "compose processor-sample service"
      via: "$services pre-flight health list"
      pattern: "processor-sample"
    - from: "gate BEFORE/AFTER snapshots"
      to: "steady-state skp:{id:D} + queue:{id:D}"
      via: "stable Processor row -> stable id -> key/queue in both snapshots"
      pattern: "redis-cli --scan|list_queues"
---

<objective>
Author the Phase 28 close gate (copy phase-22-close.ps1, add processor-sample to $services), resolve the two gate-integration decisions (steady-state processor-id stability + pre-flight health tolerance), and run the 3-GREEN / triple-SHA gate to closeout.

Purpose: TEST-02 / SC#5. The close-gate discipline is unchanged (locked since Phase 3 D-15/D-18): 3-consecutive-GREEN + triple-SHA BEFORE=AFTER. Phase 28 adds the live processor-sample container to the steady-state stack, so the gate must (a) include it in the pre-flight health list and (b) ensure its liveness key + dispatch queue are stable across the run (steady-state, not test-churned) so the redis/rabbitmq SHAs hold. The skp:data:* execution-data leak is handled by Plan 02 (short container TTL) + Plan 03 (net-zero teardown), NOT the gate script.
Output: `scripts/phase-28-close.ps1` + gate evidence (3× full-suite GREEN + triple-SHA held).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-CONTEXT.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-RESEARCH.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-PATTERNS.md
@.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-03-SUMMARY.md

<interfaces>
<!-- scripts/phase-22-close.ps1 — the gate to copy VERBATIM (186 lines), changing ONLY label + $services -->
Header (lines 1-2): "Phase 22 close gate — v3.4.0 (triple-SHA)" -> "Phase 28 close gate — v3.5.0 (triple-SHA)".
Pre-flight (lines 33-46): $services = @('postgres','redis','rabbitmq','otel-collector','elasticsearch','prometheus','orchestrator','baseapi-service'); foreach loop aborts (exit 2) if Health != 'healthy' AND svc != 'otel-collector'.
BEFORE snapshots (lines ~48-66): psql -U postgres -lqt | SHA-256; docker exec sk-redis redis-cli --scan | Sort-Object -CaseSensitive | SHA-256; docker exec sk-rabbitmq rabbitmqctl -q list_queues name | Sort-Object -CaseSensitive | SHA-256.
Zero-warning build gate (lines ~68-81): dotnet clean; Release + Debug both 0 Warning/0 Error.
3-GREEN cadence (lines ~83-116): full suite no Category filter; parse "Passed:"; distinct-count guard.
AFTER snapshots + invariant assertions (lines ~138-173): BEFORE == AFTER for all three SHAs.
Exit codes: 0 = all green + SHAs held; 1 = invariant violation / RED / unparseable; 2 = stack not healthy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/phase-28-close.ps1 (copy phase-22, add processor-sample to $services, resolve pre-flight tolerance)</name>
  <read_first>
    - scripts/phase-22-close.ps1 (the FULL 186-line gate to copy verbatim — header, pre-flight $services loop, triple-SHA BEFORE/AFTER, zero-warning Release+Debug, 3-GREEN cadence, invariant assertions)
    - .planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-RESEARCH.md §"Code Examples §7" + §"Runtime State Inventory" (psql -lqt is db-list level; redis/rabbitmq steady-state requires stable processor id) + Open Questions 1 & 2 (steady-state id + chicken-and-egg pre-flight)
    - .planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-PATTERNS.md §"scripts/phase-28-close.ps1" (the exact copy + $services edit instructions)
    - compose.yaml processor-sample block (created Plan 02 — to confirm the service name `processor-sample` and that it carries a healthcheck)
  </read_first>
  <action>
    Copy `scripts/phase-22-close.ps1` to `scripts/phase-28-close.ps1` VERBATIM, then change exactly these (RESEARCH §7 + Open Q1/Q2 resolution):

    1. **Header label** (lines 1-2 + any "Phase 22"/"v3.4.0" string in the Write-Host banners and final summary): `Phase 22` -> `Phase 28`, `v3.4.0` -> `v3.5.0`. Update the comment block's invariant descriptions to mention the new processor-liveness (`skp:{id:D}`) + execution-data (`skp:data:*`) key families covered by the test teardown + short container TTL.

    2. **$services pre-flight list** — add `'processor-sample'` (RESEARCH §7):
       ```powershell
       $services = @('postgres', 'redis', 'rabbitmq', 'otel-collector', 'elasticsearch', 'prometheus',
                     'orchestrator', 'processor-sample', 'baseapi-service')
       ```

    3. **RESOLVE Open Q2 (chicken-and-egg pre-flight health, gate-integration decision — bake the choice into the script):** The DECISION is **steady-state stable processor row** (NOT adding processor-sample to the health-exception list). Rationale: a health exception would let the gate pass with a dead processor (defeats the point); instead, the gate REQUIRES processor-sample healthy at pre-flight, which REQUIRES a stable Processor DB row carrying its embedded hash to exist BEFORE the gate runs. Implement this as a pre-flight SEED step in the script (BEFORE the BEFORE-snapshot): register (idempotently — skip if a row with that hash already exists, since `uq_processor_source_hash` is unique) a Processor row via the WebApi CRUD over host port (the same `POST /api/v1/processors` the E2E uses) with the genuine embedded hash reflected off the built `Processor.Sample.dll` and null schema Ids. Then wait for processor-sample `/health/ready` to flip healthy (identity resolved -> bind -> MarkHealthy -> heartbeat) before snapshotting. This makes the container's `skp:{id:D}` liveness key + `queue:{id:D}` dispatch queue STEADY-STATE — present in BOTH the BEFORE and AFTER snapshots, so the redis `--scan` and rabbitmq `list_queues` SHAs hold (Open Q1 / A5). The E2E (Plan 03) then reuses this same stable row (its SeedProcessor is idempotent against the unique hash) so the id does not churn across the 3 runs. If implementing the seed in PowerShell is impractical, alternatively keep the pre-flight requiring healthy and document that the operator runs the E2E once (which seeds the row) before the gate; but the script-embedded idempotent seed is the preferred deterministic form — implement it.
       > Keep `psql -lqt` unchanged: it lists DATABASES not rows, so the seeded Processor row does NOT change the postgres SHA (verified RESEARCH §Runtime State Inventory / A6). No teardown of the row is required for the gate (clean for hygiene only).

    4. Everything ELSE unchanged: triple-SHA BEFORE/AFTER capture, zero-warning Release+Debug build gate, 3-consecutive-GREEN cadence with the `Passed:` parse + distinct-count guard, the BEFORE==AFTER invariant assertions, exit codes (0/1/2). Do NOT add FLUSHDB. The skp:data:* teardown lives in the TEST + the container's short ExecutionDataTtl (Plan 02/03), NOT here.

    Use Write/Edit (NOT Set-Content — MEMORY: BOM/mojibake risk). Confirm the script is ASCII (no BOM) after writing.
  </action>
  <acceptance_criteria>
    - File `scripts/phase-28-close.ps1` exists; `grep "processor-sample" scripts/phase-28-close.ps1` matches inside the `$services` array.
    - `grep "Phase 28" scripts/phase-28-close.ps1` matches AND `grep -c "Phase 22" scripts/phase-28-close.ps1` == 0 (no stale label).
    - The script still contains all three SHA captures: `grep "psql -U postgres -lqt" scripts/phase-28-close.ps1`, `grep "redis-cli --scan" scripts/phase-28-close.ps1`, `grep "rabbitmqctl -q list_queues" scripts/phase-28-close.ps1` all match.
    - `grep -c "FLUSHDB" scripts/phase-28-close.ps1` == 0.
    - The pre-flight resolves the chicken-and-egg: either `grep "processors" scripts/phase-28-close.ps1` matches (the idempotent seed call) OR a documented operator-seed note is present; processor-sample is NOT silently added to the health-exception (`-ne 'otel-collector'`) clause without a seed.
    - scripts/phase-28-close.ps1 has no UTF-8 BOM (first 3 bytes not 239 187 191).
    - `pwsh -NoProfile -Command "& { . scripts/phase-28-close.ps1 }" -WhatIf` is not required, but `pwsh -NoProfile -File scripts/phase-28-close.ps1 -?`-style syntax check: `pwsh -NoProfile -Command "[scriptblock]::Create((Get-Content scripts/phase-28-close.ps1 -Raw)) | Out-Null"` exits 0 (parses without syntax error).
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "[scriptblock]::Create((Get-Content scripts/phase-28-close.ps1 -Raw)) | Out-Null; if ($?) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>phase-28-close.ps1 mirrors phase-22 with processor-sample in $services, Phase 28/v3.5.0 labels, all three SHA captures intact, no FLUSHDB, the steady-state idempotent-seed pre-flight resolving the chicken-and-egg, BOM-free, parses clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the close gate — 3-consecutive-GREEN + triple-SHA BEFORE=AFTER (operator-authorized)</name>
  <read_first>
    - scripts/phase-28-close.ps1 (the gate just authored)
    - .planning/STATE.md §"Phase 22 Plan 05 — Close Gate Evidence" + §"Phase 21 Plan 01 — Close Gate Evidence" (the evidence format to reproduce: 3× Passed:N/Failed:0, triple-SHA BEFORE==AFTER hex, zero-warning Release+Debug)
    - MEMORY: project_phase20_close_gate_failure (read GATE_*_EXIT from the file, not the bg-task wrapper's exit; no runaway poll loops — use Monitor, Start-Sleep is blocked)
  </read_first>
  <what-built>
    The Phase 28 close gate (scripts/phase-28-close.ps1). To run it the full v3.5.0 stack must be up healthy INCLUDING the new processor-sample container, and a stable Processor DB row with the genuine embedded hash must exist (the script's pre-flight seed handles this). The gate then: captures triple-SHA BEFORE, runs zero-warning Release+Debug builds, runs the full test suite 3 consecutive times (RealStack E2E — incl. SampleRoundTripE2ETests — live each run), captures triple-SHA AFTER, asserts BEFORE==AFTER for psql -lqt / redis-cli --scan / rabbitmqctl list_queues.
  </what-built>
  <how-to-verify>
    1. Bring up the full stack: `docker compose up -d --build` (builds processor-sample). Wait for all services healthy: `docker compose ps` (processor-sample goes healthy only after its DB row exists — the gate pre-flight seeds it; if running manually, run SampleRoundTripE2ETests once or invoke the seed first).
    2. Run the gate (operator-authorized): `pwsh -File scripts/phase-28-close.ps1` (run in BACKGROUND per MEMORY no-runaway-poll-loops; read GATE exit from the script's own output/exit code, not the wrapper).
    3. Confirm the script prints:
       - `psql \l SHA-256 BEFORE = <hex>` and `... AFTER = <hex>` — MATCH.
       - `redis-cli --scan SHA-256 BEFORE = <hex>` and `... AFTER = <hex>` — MATCH (the steady-state skp:{id:D} liveness key is in both; no leaked skp:data:* — short TTL + test teardown).
       - `rabbitmqctl list_queues SHA-256 BEFORE = <hex>` and `... AFTER = <hex>` — MATCH (the steady-state queue:{id:D} is in both).
       - 3× `Passed: N, Failed: 0` (full suite, RealStack live).
       - Release = 0 Warning(s) / 0 Error(s); Debug = 0 Warning(s) / 0 Error(s).
       - Final exit code 0.
    4. If exit 1 (SHA mismatch): inspect which SHA diverged. A redis mismatch with a leaked `skp:data:*` -> the container ExecutionDataTtl (Plan 02) or the E2E teardown (Plan 03) needs adjustment. A rabbitmq mismatch -> the processor id churned (the row was re-created with a different id) -> make the seed/E2E idempotent against the unique hash. A psql mismatch is unexpected (db-list level) -> investigate.
    Human verifies all three SHAs held + 3 GREEN + zero-warning before closeout.
  </how-to-verify>
  <acceptance_criteria>
    - `pwsh -File scripts/phase-28-close.ps1` exits 0.
    - Output shows all three triple-SHA pairs BEFORE == AFTER (psql, redis, rabbitmq).
    - Output shows 3 consecutive `Failed: 0` full-suite runs (RealStack live).
    - Output shows Release AND Debug both 0 Warning(s) / 0 Error(s).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the gate exits 0 with all three SHAs held + 3× GREEN + zero-warning builds, or paste the divergence/failure output to remediate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script -> live containers | The gate execs into postgres/redis/rabbitmq containers to snapshot state; read-only enumeration. |
| pre-flight seed -> WebApi CRUD | The seed registers a Processor row gated by ^[a-f0-9]{64}$ + the unique hash index. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-11 | Spoofing | Stale/duplicate Processor row from a non-idempotent seed | mitigate | Seed is idempotent against `uq_processor_source_hash` (unique index) + the ^[a-f0-9]{64}$ validator; a duplicate hash is rejected, so the id stays stable across the 3-run gate (keeps redis/rabbitmq SHAs holding). |
| T-28-12 | Tampering | Leaked execution-data keys masking under the redis SHA | mitigate | Short container ExecutionDataTtl (Plan 02) + net-zero E2E teardown (Plan 03); the gate's BEFORE/AFTER captures around steady-state catch any leak as a SHA mismatch (fail-closed, exit 1). |
| T-28-13 | Repudiation | Gate run without recorded evidence | accept | The gate prints all SHAs + run counts; the SUMMARY records them (Phase 21/22 evidence precedent) — operator-authorized, auditable. |

No high-severity threats; operator-run close gate over the dev stack.
</threat_model>

<verification>
- `pwsh ... [scriptblock]::Create(...)` = exit 0 (script parses).
- `grep` acceptance criteria: processor-sample in $services, Phase 28 label, three SHA captures, no FLUSHDB, BOM-free.
- `pwsh -File scripts/phase-28-close.ps1` = exit 0: 3× Failed:0 + triple-SHA BEFORE==AFTER + zero-warning Release+Debug.
</verification>

<success_criteria>
- scripts/phase-28-close.ps1 mirrors the proven phase-22 gate with processor-sample added and the steady-state pre-flight seed resolving the chicken-and-egg.
- The gate holds: 3-consecutive-GREEN (full suite, RealStack live) + triple-SHA (psql -lqt / redis-cli --scan / rabbitmqctl list_queues) BEFORE == AFTER + zero-warning Release+Debug.
- The new skp:{id:D} liveness key + queue:{id:D} are steady-state (both snapshots); no leaked skp:data:* under the SHA.
</success_criteria>

<output>
After completion, create `.planning/phases/28-sourcehash-identity-processor-sample-e2e-closeout/28-04-SUMMARY.md`
</output>
