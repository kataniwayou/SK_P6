---
phase: 30-runtime-business-metrics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Orchestrator/Observability/OrchestratorMetrics.cs
  - src/Orchestrator/Program.cs
  - src/Orchestrator/Dispatch/StepDispatcher.cs
  - src/Orchestrator/Consumers/ResultConsumer.cs
  - tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs
autonomous: true
requirements: [METRIC-04]
nyquist_compliant: true

must_haves:
  truths:
    - "The Orchestrator owns a DI-singleton OrchestratorMetrics built via IMeterFactory.Create(\"Orchestrator\") exposing two Counter<long> instruments named (in code, no _total) orchestrator_dispatch_sent and orchestrator_result_consumed."
    - "The \"Orchestrator\" meter is registered on the MeterProvider via ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(OrchestratorMetrics.MeterName)) in Program.cs."
    - "orchestrator_dispatch_sent increments AFTER endpoint.Send in StepDispatcher tagged ProcessorId; orchestrator_result_consumed increments at the TOP of ResultConsumer.Consume tagged ProcessorId; NO workflowId tag."
  artifacts:
    - path: "src/Orchestrator/Observability/OrchestratorMetrics.cs"
      provides: "IMeterFactory holder with MeterName const + two counters"
      contains: "meterFactory.Create"
    - path: "src/Orchestrator/Program.cs"
      provides: "AddSingleton<OrchestratorMetrics> + ConfigureOpenTelemetryMeterProvider AddMeter"
      contains: "ConfigureOpenTelemetryMeterProvider"
    - path: "src/Orchestrator/Dispatch/StepDispatcher.cs"
      provides: "DispatchSent increment after Send"
      contains: "DispatchSent.Add"
    - path: "src/Orchestrator/Consumers/ResultConsumer.cs"
      provides: "ResultConsumed increment at top of Consume"
      contains: "ResultConsumed.Add"
  key_links:
    - from: "StepDispatcher.cs"
      to: "OrchestratorMetrics.DispatchSent"
      via: "ctor-injected metrics, .Add(1, ProcessorId) after endpoint.Send"
      pattern: "DispatchSent\\.Add"
    - from: "Program.cs"
      to: "Orchestrator MeterProvider"
      via: "ConfigureOpenTelemetryMeterProvider AddMeter(OrchestratorMetrics.MeterName)"
      pattern: "AddMeter\\(OrchestratorMetrics"
---

<objective>
Define the Orchestrator's code-owned `Meter` ("Orchestrator") with two monotonic counters and increment them at the confirmed send-and-consume sites, each tagged `ProcessorId` (+ ambient `service_instance_id` from Plan 01) and NO `workflowId`. This is the orchestrator half of the per-processor dispatch-bottleneck PromQL.

Purpose: METRIC-04 — `orchestrator_dispatch_sent_total` (the collector appends `_total`) + `orchestrator_result_consumed_total`, keyed by `ProcessorId`.
Output: 1 new metrics holder + 3 modified orchestrator files + 1 hermetic holder test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-runtime-business-metrics/30-SPEC.md
@.planning/phases/30-runtime-business-metrics/30-RESEARCH.md
@.planning/phases/30-runtime-business-metrics/30-PATTERNS.md

<interfaces>
<!-- The exact contracts the executor needs — no codebase exploration required. -->

NEW holder contract (D-01/D-02/D-03 — build it exactly so the meter name matches AddMeter):
  namespace Orchestrator.Observability;
  public sealed class OrchestratorMetrics {
    public const string MeterName = "Orchestrator";   // D-02 — MUST equal AddMeter("Orchestrator")
    public Counter<long> DispatchSent { get; }
    public Counter<long> ResultConsumed { get; }
    public OrchestratorMetrics(IMeterFactory meterFactory) {
      var meter = meterFactory.Create(MeterName);
      DispatchSent   = meter.CreateCounter<long>("orchestrator_dispatch_sent");     // D-03 — no _total
      ResultConsumed = meter.CreateCounter<long>("orchestrator_result_consumed");   // D-03 — no _total
    }
  }

StepDispatcher.cs (CURRENT — single dispatch-send owner; processorId is a method arg):
  public sealed class StepDispatcher(ISendEndpointProvider sendProvider) : IStepDispatcher
  // inside DispatchAsync(Guid workflowId, Guid stepId, Guid processorId, string payload, ...):
  var endpoint = await sendProvider.GetSendEndpoint(new Uri($"queue:{processorId:D}"));
  await endpoint.Send(msg, ct);

ResultConsumer.cs (CURRENT — ctor + top of Consume; m.ProcessorId is non-nullable Guid):
  public sealed class ResultConsumer(IWorkflowL1Store store, StepAdvancement advancement,
      IStepDispatcher dispatcher, ILogger<ResultConsumer> logger) : IConsumer<ExecutionResult> {
    public async Task Consume(ConsumeContext<ExecutionResult> context) {
      var m = context.Message;
      if (!store.TryGet(m.WorkflowId, out var wf) || ...) { ... return; }   // L1 read

Program.cs (CURRENT — generic host; AddBaseConsoleObservability at line 20; singleton block lines 47-52).
  ExecutionResult.ProcessorId is a non-nullable Guid (src/Messaging.Contracts/ExecutionResult.cs:10).

ProcessorIdLogEnricher.cs (the sealed DI-singleton *.Observability analog to mirror the holder shape).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OrchestratorMetrics holder + register the "Orchestrator" meter</name>
  <read_first>
    - src/BaseProcessor.Core/Observability/ProcessorIdLogEnricher.cs (the sealed DI-singleton *.Observability holder shape to mirror)
    - src/Orchestrator/Program.cs (the composition root — singleton block lines 47-52; AddBaseConsoleObservability at line 20)
    - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs lines 101-103 (the Phase-29 ConfigureOpenTelemetryLoggerProvider seam — the meter variant ConfigureOpenTelemetryMeterProvider is its exact analog)
    - 30-PATTERNS.md § "OrchestratorMetrics.cs" + § "Program.cs (MODIFY — meter registration)"
    - 30-RESEARCH.md § "Pattern 1" + § "Pattern 2" (TWO different seams — Open Question 1 recommends ConfigureOpenTelemetryMeterProvider in Program.cs to preserve D-02 const symmetry)
  </read_first>
  <behavior>
    - OrchestratorMetrics constructs from a real IMeterFactory and exposes non-null DispatchSent + ResultConsumed counters.
    - OrchestratorMetrics.MeterName == "Orchestrator" (the const the AddMeter call references).
    - The counters are named orchestrator_dispatch_sent and orchestrator_result_consumed (snake_case, NO _total — the collector appends _total).
  </behavior>
  <action>
    Create `src/Orchestrator/Observability/OrchestratorMetrics.cs` EXACTLY per the holder contract in `<interfaces>` (D-01/D-02/D-03): `sealed`, namespace `Orchestrator.Observability`, `using System.Diagnostics.Metrics;`, `public const string MeterName = "Orchestrator";`, ctor takes `IMeterFactory meterFactory`, `var meter = meterFactory.Create(MeterName);`, then `DispatchSent = meter.CreateCounter<long>("orchestrator_dispatch_sent");` and `ResultConsumed = meter.CreateCounter<long>("orchestrator_result_consumed");`. Counter names are snake_case with NO `_total` (D-03 — the collector's `add_metric_suffixes` default appends `_total`; adding it here would yield `_total_total`). NEVER use a `static Meter` field (cross-test static leak in the shared hermetic process — use `IMeterFactory`, the .NET 8 blessed pattern auto-registered by the generic host).

    In `src/Orchestrator/Program.cs`, add (mirroring the singleton style at lines 47-52, e.g. next to `AddSingleton<IStepDispatcher, StepDispatcher>`):
    ```csharp
    using Orchestrator.Observability;
    using OpenTelemetry.Metrics;   // ConfigureOpenTelemetryMeterProvider (via OpenTelemetry.Extensions.Hosting)
    ...
    builder.Services.AddSingleton<OrchestratorMetrics>();
    builder.Services.ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(OrchestratorMetrics.MeterName));
    ```
    Use `ConfigureOpenTelemetryMeterProvider` (NOT a literal `AddMeter("Orchestrator")` string inside the base-lib `WithMetrics`) — this preserves D-02's "a const referenced by both `AddMeter` and `Create`" and mirrors the Phase-29 `ConfigureOpenTelemetryLoggerProvider` seam (RESEARCH Open Q1, HIGH confidence). `ConfigureOpenTelemetryMeterProvider` ships in `OpenTelemetry.Extensions.Hosting` (already referenced — it provides the sibling logger-provider method). It attaches additively to the shared MeterProvider that `AddBaseConsoleObservability` (line 20) already built.

    Add `tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs` — a hermetic `[Fact]` that constructs `OrchestratorMetrics` from a real `IMeterFactory` (resolve one from a minimal `new ServiceCollection().AddMetrics().BuildServiceProvider().GetRequiredService<IMeterFactory>()`, or the host's `IMeterFactory`) and asserts `DispatchSent` and `ResultConsumed` are non-null AND `OrchestratorMetrics.MeterName == "Orchestrator"`. This catches the D-02 meter-name/const-mismatch bug without the full stack.
  </action>
  <verify>
    <automated>dotnet build src/Orchestrator -c Debug && dotnet test tests/BaseApi.Tests --filter "Category!=RealStack" -- --filter-class "*OrchestratorMetricsFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/Orchestrator/Observability/OrchestratorMetrics.cs` exists; `grep` finds `meterFactory.Create(`, `const string MeterName = "Orchestrator"`, `orchestrator_dispatch_sent`, `orchestrator_result_consumed`.
    - `grep` of OrchestratorMetrics.cs does NOT find `_total`.
    - `grep` of `src/Orchestrator/Program.cs` finds `AddSingleton<OrchestratorMetrics>` and `ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(OrchestratorMetrics.MeterName))`.
    - `dotnet build src/Orchestrator -c Debug` exits 0.
    - `dotnet test ... --filter-class "*OrchestratorMetricsFacts"` exits 0.
  </acceptance_criteria>
  <done>OrchestratorMetrics holder built via IMeterFactory with the two correctly-named counters; the "Orchestrator" meter registered via ConfigureOpenTelemetryMeterProvider; holder fact GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Increment dispatch_sent (after Send) and result_consumed (at consume entry)</name>
  <read_first>
    - src/Orchestrator/Dispatch/StepDispatcher.cs (the file being edited — single build-and-Send owner; increment AFTER endpoint.Send, D-04/D-05)
    - src/Orchestrator/Consumers/ResultConsumer.cs (the file being edited — increment at TOP of Consume before the L1 read, D-06)
    - 30-PATTERNS.md § "StepDispatcher.cs" + § "ResultConsumer.cs" (the exact ctor-param + increment edits, with the ProcessorId "D" format)
  </read_first>
  <action>
    `StepDispatcher.cs` (D-05): add `OrchestratorMetrics metrics` to the primary ctor (`public sealed class StepDispatcher(ISendEndpointProvider sendProvider, OrchestratorMetrics metrics) : IStepDispatcher`) and increment AFTER `endpoint.Send` returns (D-04: SENT = count-after-Send; an infra throw on Send correctly skips the increment) — `processorId` is already a method arg:
    ```csharp
    await endpoint.Send(msg, ct);
    metrics.DispatchSent.Add(1, new KeyValuePair<string, object?>("ProcessorId", processorId.ToString("D")));
    ```
    This single owner covers BOTH the `WorkflowFireJob` entry fire and the `ResultConsumer` continuation (D-05). Tag KEY is the literal PascalCase `"ProcessorId"` (the collector exporter PRESERVES tag-key case, so `sum by (ProcessorId)` works verbatim); value is `.ToString("D")` matching the `queue:{processorId:D}` naming. NO `workflowId` tag (cardinality — D-03/SPEC).

    `ResultConsumer.cs` (D-06): add `OrchestratorMetrics metrics` to the primary ctor (between `IStepDispatcher dispatcher` and `ILogger<ResultConsumer> logger`) and increment at the TOP of `Consume`, BEFORE the L1 `store.TryGet` read, so the graceful L1-miss ack is ALSO counted (D-06 counts EVERY consumed result):
    ```csharp
    var m = context.Message;
    metrics.ResultConsumed.Add(1, new KeyValuePair<string, object?>("ProcessorId", m.ProcessorId.ToString("D")));
    // existing L1-only read follows...
    ```
    `ExecutionResult.ProcessorId` is a non-nullable `Guid` (no null-guard needed here — contrast the processor-side `Guid?` in Plan 03). Add `using Orchestrator.Observability;` where needed.
  </action>
  <verify>
    <automated>dotnet build src/Orchestrator -c Debug && dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` of `StepDispatcher.cs` finds `OrchestratorMetrics metrics` in the ctor AND `metrics.DispatchSent.Add(1` with `"ProcessorId"` and `processorId.ToString("D")`, positioned AFTER `endpoint.Send`.
    - `grep` of `ResultConsumer.cs` finds `OrchestratorMetrics metrics` in the ctor AND `metrics.ResultConsumed.Add(1` with `"ProcessorId"` and `m.ProcessorId.ToString("D")`, positioned BEFORE `store.TryGet`.
    - `grep` of both files does NOT find `workflowId` or `WorkflowId` in any `.Add(` metric tag.
    - `dotnet build src/Orchestrator -c Debug` exits 0; `dotnet build SK_P.sln -c Release` 0 warnings.
    - `dotnet test ... --filter "Category!=RealStack"` exits 0 (orchestrator slice + full hermetic GREEN — the DI now resolves OrchestratorMetrics into both consumers).
  </acceptance_criteria>
  <done>Both orchestrator counters increment at the correct sites (sent after Send, consumed at entry) tagged ProcessorId with no workflowId; hermetic suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator code → metric instruments | Counter tags are constructed from `ProcessorId` (a bounded Guid), never per-execution ids. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Denial of Service | orchestrator counter label cardinality | mitigate | Only `ProcessorId` (bounded) + ambient `service_instance_id` (bounded by replicas) tagged; NO `workflowId`/per-execution id — locked by SPEC/D-03 and grep-verified. |
</threat_model>

<verification>
- `dotnet build src/Orchestrator -c Debug` exits 0.
- `dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"` exits 0 (includes OrchestratorMetricsFacts).
- `dotnet build SK_P.sln -c Release` = 0 Warning / 0 Error.
- `grep` confirms no `workflowId` tag on either counter.
</verification>

<success_criteria>
- `OrchestratorMetrics` holder (IMeterFactory, MeterName "Orchestrator", two snake_case no-`_total` counters) exists; the meter is registered via `ConfigureOpenTelemetryMeterProvider`.
- `orchestrator_dispatch_sent` increments after `endpoint.Send` in `StepDispatcher`, tagged `ProcessorId`.
- `orchestrator_result_consumed` increments at the top of `ResultConsumer.Consume`, tagged `ProcessorId`.
- No `workflowId` tag on either; hermetic suite GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/30-runtime-business-metrics/30-02-SUMMARY.md`.
</output>
