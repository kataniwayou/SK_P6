---
phase: 30-runtime-business-metrics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs
  - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs
  - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
  - tests/BaseApi.Tests/Processor/ProcessorMetricsFacts.cs
autonomous: true
requirements: [METRIC-05]
nyquist_compliant: true

must_haves:
  truths:
    - "BaseProcessor.Core owns a DI-singleton ProcessorMetrics built via IMeterFactory.Create(\"BaseProcessor\") exposing two Counter<long> instruments named (in code, no _total) processor_dispatch_consumed and processor_result_sent — inherited by every Processor.*."
    - "The \"BaseProcessor\" meter is registered via services.ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(ProcessorMetrics.MeterName)) INSIDE AddBaseProcessor (NOT in BaseConsoleObservabilityExtensions — compile firewall)."
    - "processor_dispatch_consumed increments at the TOP of EntryStepDispatchConsumer.Consume tagged ProcessorId; processor_result_sent increments AFTER Send via a single SendResult(ExecutionResult) routing BOTH send paths, tagged ProcessorId + outcome ∈ {completed, failed, cancelled}; NO workflowId; no processing outcome."
  artifacts:
    - path: "src/BaseProcessor.Core/Observability/ProcessorMetrics.cs"
      provides: "IMeterFactory holder with MeterName const + two counters"
      contains: "meterFactory.Create"
    - path: "src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs"
      provides: "AddSingleton<ProcessorMetrics> + ConfigureOpenTelemetryMeterProvider AddMeter"
      contains: "ConfigureOpenTelemetryMeterProvider"
    - path: "src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs"
      provides: "DispatchConsumed at consume top; SendResult single owner with outcome tag"
      contains: "SendResult"
  key_links:
    - from: "EntryStepDispatchConsumer.cs"
      to: "ProcessorMetrics.ResultSent"
      via: "SendResult increments after Send with outcome + ProcessorId"
      pattern: "ResultSent\\.Add"
    - from: "BaseProcessorServiceCollectionExtensions.cs"
      to: "Processor MeterProvider"
      via: "ConfigureOpenTelemetryMeterProvider AddMeter(ProcessorMetrics.MeterName)"
      pattern: "AddMeter\\(ProcessorMetrics"
---

<objective>
Define `BaseProcessor.Core`'s code-owned `Meter` ("BaseProcessor") with two counters and increment them in the framework consumer, inherited by every `Processor.*`. Register the meter via the SAME additive seam Phase 29 used for the log enricher — NOT in `BaseConsoleObservabilityExtensions` (that would not compile: the one-way `BaseProcessor.Core → BaseConsole.Core` firewall). This is the processor half of the bottleneck PromQL.

Purpose: METRIC-05 — `processor_dispatch_consumed_total` + `processor_result_sent_total{outcome}` (the collector appends `_total`), keyed by `ProcessorId`.
Output: 1 new metrics holder + 2 modified processor files + 1 hermetic holder test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-runtime-business-metrics/30-SPEC.md
@.planning/phases/30-runtime-business-metrics/30-RESEARCH.md
@.planning/phases/30-runtime-business-metrics/30-PATTERNS.md

<interfaces>
<!-- Exact contracts the executor needs — no codebase exploration required. -->

NEW holder contract (D-01/D-02/D-03):
  namespace BaseProcessor.Core.Observability;
  public sealed class ProcessorMetrics {
    public const string MeterName = "BaseProcessor";   // D-02 — MUST equal AddMeter("BaseProcessor")
    public Counter<long> DispatchConsumed { get; }
    public Counter<long> ResultSent { get; }
    public ProcessorMetrics(IMeterFactory meterFactory) {
      var meter = meterFactory.Create(MeterName);
      DispatchConsumed = meter.CreateCounter<long>("processor_dispatch_consumed");   // D-03 — no _total
      ResultSent       = meter.CreateCounter<long>("processor_result_sent");         // D-03 — no _total
    }
  }

The Phase-29 SEAM to mirror — BaseProcessorServiceCollectionExtensions.cs:101-103 (log enricher):
  services.AddSingleton<ProcessorIdLogEnricher>();
  services.ConfigureOpenTelemetryLoggerProvider((sp, lp) =>
      lp.AddProcessor(sp.GetRequiredService<ProcessorIdLogEnricher>()));
  // `using OpenTelemetry.Metrics;` may be needed for ConfigureOpenTelemetryMeterProvider/AddMeter;
  // the file already imports `OpenTelemetry` + `OpenTelemetry.Logs` + `BaseProcessor.Core.Observability`.

IProcessorContext.Id is Guid? (null until identity resolves; src/BaseProcessor.Core/Identity/IProcessorContext.cs:36).
  Consume runs ONLY post-MarkHealthy → identity IS resolved → tag with context.Id!.Value.ToString("D")
  (the bang is justified; document it so reviewers don't flag a NRE — Landmine 2).

EntryStepDispatchConsumer.cs (CURRENT — the two send paths to unify):
  ctor: (IConnectionMultiplexer redis, IProcessorContext context, BaseProcessor processor,
         IOptions<ProcessorLivenessOptions> options, ISendEndpointProvider sendProvider,
         ILogger<EntryStepDispatchConsumer> logger)
  Consume(ctx): var dispatch = ctx.Message; ...   // top — increment here (D-07)
  Early path (×4): await SendOne(BuildFailed/BuildCancelled(...));   // SendOne at lines 199-203
  Final loop (lines 183-190): endpoint = GetSendEndpoint(queue:{OrchestratorQueues.Result});
                              foreach (var er in built) await endpoint.Send(er, CancellationToken.None);

StepOutcome (src/Messaging.Contracts/StepOutcome.cs): Processing=0, Completed=1, Failed=2, Cancelled=3.
  Build paths (BuildCompleted/BuildFailed/BuildCancelled) NEVER produce Processing → outcome ∈ {completed,failed,cancelled}.
  ExecutionResult.Outcome is StepOutcome (src/Messaging.Contracts/ExecutionResult.cs:10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProcessorMetrics holder + register the "BaseProcessor" meter (firewall-correct seam)</name>
  <read_first>
    - src/BaseProcessor.Core/Observability/ProcessorIdLogEnricher.cs (the sealed DI-singleton *.Observability holder shape to mirror — drop ProcessorMetrics.cs beside it)
    - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs (the file being edited — lines 101-103 carry the EXACT seam to copy as a meter variant)
    - 30-PATTERNS.md § "ProcessorMetrics.cs" + § "BaseProcessorServiceCollectionExtensions.cs" (the compile-firewall fix, Landmine 1)
    - 30-RESEARCH.md § "Pitfall 1 (LANDMINE)" (why AddMeter("BaseProcessor") CANNOT go in BaseConsoleObservabilityExtensions)
  </read_first>
  <behavior>
    - ProcessorMetrics constructs from a real IMeterFactory and exposes non-null DispatchConsumed + ResultSent counters.
    - ProcessorMetrics.MeterName == "BaseProcessor".
    - The counters are named processor_dispatch_consumed and processor_result_sent (snake_case, NO _total).
  </behavior>
  <action>
    Create `src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` EXACTLY per the holder contract in `<interfaces>` (D-01/D-02/D-03): `sealed`, namespace `BaseProcessor.Core.Observability`, `using System.Diagnostics.Metrics;`, `public const string MeterName = "BaseProcessor";`, ctor takes `IMeterFactory meterFactory`, `var meter = meterFactory.Create(MeterName);`, then `DispatchConsumed = meter.CreateCounter<long>("processor_dispatch_consumed");` and `ResultSent = meter.CreateCounter<long>("processor_result_sent");`. Snake_case, NO `_total` (D-03 — collector appends it). NEVER a `static Meter` field.

    In `src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs`, add — ALONGSIDE the log-enricher block at step 6b (lines 101-103) — the meter variant of the SAME Phase-29 seam (this is the compile-firewall fix, Landmine 1):
    ```csharp
    services.AddSingleton<ProcessorMetrics>();
    services.ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(ProcessorMetrics.MeterName));
    ```
    Register it HERE inside `AddBaseProcessor` (in `BaseProcessor.Core`) — NOT in `BaseConsoleObservabilityExtensions`. `BaseConsole.Core` has NO project reference to `BaseProcessor.Core` (the dependency runs the other way; the only ref is `Messaging.Contracts`), so it cannot see `ProcessorMetrics.MeterName` — putting `AddMeter("BaseProcessor")` there is a compile error or a forbidden project reference. `ConfigureOpenTelemetryMeterProvider` is the exact meter-provider analog of the `ConfigureOpenTelemetryLoggerProvider` already at lines 101-103, from the same `OpenTelemetry.Extensions.Hosting` package. Add `using OpenTelemetry.Metrics;` if the analyzer needs it for `AddMeter`/`ConfigureOpenTelemetryMeterProvider` (the file already has `using OpenTelemetry;`, `using OpenTelemetry.Logs;`, `using BaseProcessor.Core.Observability;`). Every `Processor.*` inherits this via `AddBaseProcessor`.

    Add `tests/BaseApi.Tests/Processor/ProcessorMetricsFacts.cs` — a hermetic `[Fact]` constructing `ProcessorMetrics` from a real `IMeterFactory` and asserting `DispatchConsumed`/`ResultSent` non-null AND `ProcessorMetrics.MeterName == "BaseProcessor"` (catches the D-02 mismatch without the full stack).
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core -c Debug && dotnet test tests/BaseApi.Tests --filter "Category!=RealStack" -- --filter-class "*ProcessorMetricsFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` exists; `grep` finds `meterFactory.Create(`, `const string MeterName = "BaseProcessor"`, `processor_dispatch_consumed`, `processor_result_sent`.
    - `grep` of ProcessorMetrics.cs does NOT find `_total`.
    - `grep` of `BaseProcessorServiceCollectionExtensions.cs` finds `AddSingleton<ProcessorMetrics>` and `ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(ProcessorMetrics.MeterName))`.
    - `grep` of `src/BaseConsole.Core/DependencyInjection/BaseConsoleObservabilityExtensions.cs` does NOT find `BaseProcessor` (firewall held — Landmine 1).
    - `dotnet build src/BaseProcessor.Core -c Debug` exits 0; `dotnet test ... --filter-class "*ProcessorMetricsFacts"` exits 0.
  </acceptance_criteria>
  <done>ProcessorMetrics holder built via IMeterFactory with the two correctly-named counters; the "BaseProcessor" meter registered via the firewall-correct ConfigureOpenTelemetryMeterProvider seam inside AddBaseProcessor; holder fact GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Increment dispatch_consumed (at consume top) + route both send paths through SendResult{outcome}</name>
  <read_first>
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs (the file being edited — top of Consume at line 50; SendOne at 199-203; the final loop at 183-190; the four early `await SendOne(...)` callers)
    - src/BaseProcessor.Core/Identity/IProcessorContext.cs (Id is Guid?; safe post-Healthy — Landmine 2)
    - src/Messaging.Contracts/StepOutcome.cs (Processing=0..Cancelled=3) + ExecutionResult.cs (Outcome is StepOutcome)
    - 30-PATTERNS.md § "EntryStepDispatchConsumer.cs (MODIFY — D-07 + D-08)" (the exact ctor param + increment + SendResult refactor)
    - 30-RESEARCH.md § "Pitfall 2" (the Guid? bang) + § "Pitfall 3" (no processing outcome by construction)
  </read_first>
  <action>
    Inject the holder: add `ProcessorMetrics metrics` to the primary ctor (e.g. between `ISendEndpointProvider sendProvider` and `ILogger<EntryStepDispatchConsumer> logger`). Add `using BaseProcessor.Core.Observability;` if needed.

    D-07 — at the TOP of `Consume` (right after `var dispatch = ctx.Message;`), increment dispatch_consumed:
    ```csharp
    var dispatch = ctx.Message;
    // D-07: count at entry. Immediate(3) retry re-runs consume → re-increments — accepted rate noise (D-07).
    // context.Id is Guid? but Consume only runs post-MarkHealthy (the runtime binds queue:{id:D} after Healthy),
    // so identity IS resolved — the bang is justified (Landmine 2; not a NRE).
    metrics.DispatchConsumed.Add(1, new KeyValuePair<string, object?>("ProcessorId", context.Id!.Value.ToString("D")));
    ```
    Tag KEY is literal PascalCase `"ProcessorId"` (collector preserves case → `sum by (ProcessorId)` works); value `.ToString("D")` (matches `queue:{id:D}`). NO `workflowId`.

    D-08 — introduce ONE private send owner `SendResult(ExecutionResult result)` that increments AFTER `Send` (D-04: SENT = count-after-Send), tagged `ProcessorId` + `outcome`:
    ```csharp
    // D-08: the single Send owner — every result (early Failed/Cancelled AND the per-result loop) flows
    // through here so no send path is uncounted. Increment AFTER Send (confirmed delivery, D-04).
    private async Task SendResult(ExecutionResult result)
    {
        var endpoint = await sendProvider.GetSendEndpoint(new Uri($"queue:{OrchestratorQueues.Result}"));
        await endpoint.Send(result, CancellationToken.None);
        metrics.ResultSent.Add(1,
            new KeyValuePair<string, object?>("ProcessorId", context.Id!.Value.ToString("D")),
            // outcome ∈ {completed, failed, cancelled} — the build paths never emit Processing (Pitfall 3); no filter needed.
            new KeyValuePair<string, object?>("outcome", result.Outcome.ToString().ToLowerInvariant()));
    }
    ```
    Route BOTH existing send paths through it:
    - Replace the existing private `SendOne` (lines 199-203) — either make its body call `SendResult` (thin alias) OR rename all four early `await SendOne(BuildFailed/BuildCancelled(...))` callers to `await SendResult(...)`. ALL early Failed/Cancelled sends MUST go through `SendResult`.
    - Change the final one-by-one loop (lines 183-190) from `await endpoint.Send(er, CancellationToken.None);` to `await SendResult(er);` (the loop no longer pre-resolves `endpoint` — `SendResult` resolves it; this is acceptable, OR hoist the endpoint and pass it — your discretion, but every send MUST increment). Preserve `CancellationToken.None` (the business signal must reach the orchestrator even if the inbound token tripped) and the infra-Send-fault-propagates contract (D-15).
    Add a code comment noting the three terminal outcomes are the only ones the send paths produce (Pitfall 3).
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core -c Debug && dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` of `EntryStepDispatchConsumer.cs` finds `ProcessorMetrics metrics` in the ctor.
    - `grep` finds `metrics.DispatchConsumed.Add(1` with `"ProcessorId"` and `context.Id!.Value.ToString("D")`, positioned at the top of Consume (before input resolution).
    - `grep` finds a `private async Task SendResult(` method that contains `metrics.ResultSent.Add(1`, `"ProcessorId"`, `"outcome"`, and `result.Outcome.ToString().ToLowerInvariant()`.
    - `grep` confirms NO remaining `await endpoint.Send(er, CancellationToken.None)` raw loop send that bypasses SendResult (every send routes through SendResult, including the four early Failed/Cancelled paths).
    - `grep` of the file does NOT find `workflowId`/`WorkflowId` in any metric `.Add(` tag, and does NOT find a `"processing"` literal.
    - `dotnet build src/BaseProcessor.Core -c Debug` exits 0; `dotnet build SK_P.sln -c Release` 0 warnings (no nullable-ref warning on the bang — document the post-Healthy justification).
    - `dotnet test ... --filter "Category!=RealStack"` exits 0 (the processor slice DI now resolves ProcessorMetrics into the consumer; existing EntryStepDispatch tests still GREEN).
  </acceptance_criteria>
  <done>dispatch_consumed increments at consume entry; processor_result_sent increments after Send via a single SendResult covering BOTH send paths, tagged ProcessorId + outcome; no workflowId, no processing; hermetic suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor code → metric instruments | Counter tags are `ProcessorId` (bounded Guid) + `outcome` (3 bounded values), never per-execution ids. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Denial of Service | processor counter label cardinality | mitigate | Only `ProcessorId` (bounded) + `outcome` ∈ {completed,failed,cancelled} (3 values) + ambient `service_instance_id`; NO `workflowId`/per-execution id — locked by SPEC/D-03, grep-verified. |
| T-30-05 | Tampering | outcome tag value | accept | `outcome` is derived from the framework-owned `StepOutcome` enum (`.ToString().ToLowerInvariant()`), never user input; build paths never emit `processing`. |
</threat_model>

<verification>
- `dotnet build src/BaseProcessor.Core -c Debug` exits 0.
- `dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"` exits 0 (includes ProcessorMetricsFacts).
- `dotnet build SK_P.sln -c Release` = 0 Warning / 0 Error.
- `grep` confirms the firewall held (no `BaseProcessor` reference in BaseConsoleObservabilityExtensions) and no `workflowId` tag.
</verification>

<success_criteria>
- `ProcessorMetrics` holder (IMeterFactory, MeterName "BaseProcessor", two snake_case no-`_total` counters) exists; meter registered via the firewall-correct seam inside `AddBaseProcessor`.
- `processor_dispatch_consumed` increments at consume entry tagged `ProcessorId`.
- `processor_result_sent` increments after Send via a single `SendResult` covering both paths, tagged `ProcessorId` + `outcome`.
- No `workflowId`, no `processing`; hermetic suite GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/30-runtime-business-metrics/30-03-SUMMARY.md`.
</output>
