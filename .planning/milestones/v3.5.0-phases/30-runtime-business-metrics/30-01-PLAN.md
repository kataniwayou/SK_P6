---
phase: 30-runtime-business-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseApi.Core/DependencyInjection/ObservabilityServiceCollectionExtensions.cs
  - src/BaseConsole.Core/DependencyInjection/BaseConsoleObservabilityExtensions.cs
  - tests/BaseApi.Tests/Observability/Helpers/PrometheusTestClient.cs
  - tests/BaseApi.Tests/Observability/ResolveInstanceIdFacts.cs
autonomous: true
requirements: [METRIC-01, METRIC-02, METRIC-03, METRIC-07]
nyquist_compliant: true

must_haves:
  truths:
    - "Both base libs resolve a per-replica instance id ONCE per process (POD_NAME → HOSTNAME → MachineName → GUID) and apply it as a service.instance.id resource attribute to BOTH the logs and metrics OTel resources."
    - "Every metric (runtime, HTTP, custom) and every log carries a non-empty service_instance_id with no collector config change."
    - "A PrometheusTestClient (mirror of ElasticsearchTestClient) can poll localhost:9090/api/v1/query with exponential backoff."
  artifacts:
    - path: "src/BaseApi.Core/DependencyInjection/ObservabilityServiceCollectionExtensions.cs"
      provides: "Shared resource + service.instance.id on logs + metrics; ResolveInstanceId helper (duplicated)"
      contains: "service.instance.id"
    - path: "src/BaseConsole.Core/DependencyInjection/BaseConsoleObservabilityExtensions.cs"
      provides: "Shared resource + service.instance.id on logs + metrics; ResolveInstanceId helper (duplicated)"
      contains: "service.instance.id"
    - path: "tests/BaseApi.Tests/Observability/Helpers/PrometheusTestClient.cs"
      provides: "PollPromForQuery against localhost:9090"
      contains: "localhost:9090"
    - path: "tests/BaseApi.Tests/Observability/ResolveInstanceIdFacts.cs"
      provides: "Hermetic test of the env-precedence resolver"
      contains: "POD_NAME"
  key_links:
    - from: "ObservabilityServiceCollectionExtensions.cs"
      to: "OTel logs + metrics resources"
      via: "AddAttributes([new(service.instance.id, id)]) on both SetResourceBuilder and ConfigureResource"
      pattern: "AddAttributes"
    - from: "PrometheusTestClient.cs"
      to: "Prometheus server"
      via: "HttpClient BaseAddress http://localhost:9090/"
      pattern: "localhost:9090"
---

<objective>
Set a code-defined `service.instance.id` resource attribute in BOTH base libs so every metric AND every log carries a uniform per-replica `service_instance_id` Prometheus label — a free consequence for the existing runtime + HTTP instrumentation — with NO collector metrics-pipeline change. Also lay the Wave 0 test scaffolding (`PrometheusTestClient` + the `ResolveInstanceId` precedence unit test) consumed by the Wave 2 E2E.

Purpose: METRIC-01/02/03 (instance label on all signals, all 3 process types, HTTP) + METRIC-07 (collector untouched). Unblocks the E2E's Prometheus queries.
Output: 2 modified base-lib observability extensions + 1 new test helper + 1 new hermetic unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-runtime-business-metrics/30-SPEC.md
@.planning/phases/30-runtime-business-metrics/30-RESEARCH.md
@.planning/phases/30-runtime-business-metrics/30-PATTERNS.md

<interfaces>
<!-- Existing OTel wiring shape both base libs share TODAY (two separate AddService calls). -->
<!-- Both files currently call .AddService(serviceName, serviceVersion) twice: once on the logs -->
<!-- SetResourceBuilder, once on the metrics ConfigureResource. This plan unifies the id source. -->

BaseApi.Core/DependencyInjection/ObservabilityServiceCollectionExtensions.cs (lines 48-73):
  builder.Logging.AddOpenTelemetry(o => {
      o.IncludeFormattedMessage = true; o.IncludeScopes = true; o.ParseStateValues = true;
      o.SetResourceBuilder(ResourceBuilder.CreateDefault().AddService(serviceName, serviceVersion));
      o.AddOtlpExporter();
  });
  builder.Services.AddOpenTelemetry()
      .ConfigureResource(r => r.AddService(serviceName, serviceVersion))
      .WithMetrics(m => m
          .AddAspNetCoreInstrumentation()      // METRIC-03 — keep; /health filtered at collector
          .AddHttpClientInstrumentation()
          .AddRuntimeInstrumentation()         // METRIC-02
          .AddOtlpExporter());

BaseConsole.Core/DependencyInjection/BaseConsoleObservabilityExtensions.cs (lines 43-64):
  builder.Logging.AddOpenTelemetry(o => { /* same option block */ SetResourceBuilder(... AddService ...); AddOtlpExporter(); });
  builder.Services.AddOpenTelemetry()
      .ConfigureResource(r => r.AddService(serviceName, serviceVersion))
      .WithMetrics(m => m
          .AddMeter(InstrumentationOptions.MeterName)   // "MassTransit" — LEAVE AS-IS
          .AddRuntimeInstrumentation()                  // METRIC-02
          .AddOtlpExporter());

ElasticsearchTestClient (the poll analog to copy) — tests/BaseApi.Tests/Observability/Helpers/ElasticsearchTestClient.cs:
  sealed class : IDisposable; const InitialDelayMs = 200; const MaxDelayMs = 3_200;
  HttpClient { BaseAddress = new Uri("http://localhost:9200/") };
  PollEsForLog(queryBody, timeoutMs, indexPath?, ct): Stopwatch loop, exponential backoff,
    catch(HttpRequestException) swallow, JsonDocument.Parse(...).Clone() detach-on-return, null on timeout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the PrometheusTestClient + ResolveInstanceId precedence unit test (Wave 0)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Helpers/ElasticsearchTestClient.cs (the poll-with-backoff analog to copy WHOLESALE — class skeleton, const InitialDelayMs=200 / MaxDelayMs=3_200, long-lived HttpClient, Stopwatch loop, HttpRequestException swallow, JsonDocument.Parse(...).Clone() detach)
    - 30-RESEARCH.md § "Code Examples" (the PrometheusTestClient.PollPromForQuery body + VectorNonEmpty / HasNumericValue predicate helpers — copy verbatim)
    - 30-PATTERNS.md § "tests/.../PrometheusTestClient.cs (NEW — test helper)" (key deltas from ElasticsearchTestClient)
  </read_first>
  <behavior>
    - PrometheusTestClient construct: BaseAddress is exactly http://localhost:9090/ (the Prometheus SERVER — D-13, NOT the collector :8889).
    - ResolveInstanceIdFacts: when POD_NAME is set, the resolver returns POD_NAME (highest precedence).
    - ResolveInstanceIdFacts: when POD_NAME is unset but HOSTNAME is set, returns HOSTNAME.
    - ResolveInstanceIdFacts: when both POD_NAME and HOSTNAME are unset, returns a non-empty string (MachineName fallback) — never null/empty.
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Observability/Helpers/PrometheusTestClient.cs` by copying the `ElasticsearchTestClient` skeleton (sealed : IDisposable; `const InitialDelayMs = 200; const MaxDelayMs = 3_200;` long-lived HttpClient; Stopwatch timeout loop; exponential backoff `delay = Math.Min(delay * 2, MaxDelayMs)`; `catch (HttpRequestException) { }` swallow; `JsonDocument.Parse(...).Clone()` detach-on-return; `null` on timeout). DELTAS:
    - `private readonly HttpClient _prom = new() { BaseAddress = new Uri("http://localhost:9090/") };` — the Prometheus SERVER (D-13; host-published at compose.yaml:98). NOT `:8889` (that exporter has NO PromQL engine — Pitfall 5).
    - One method `public async Task<JsonElement?> PollPromForQuery(string promQL, Func<JsonElement, bool> predicate, int timeoutMs, CancellationToken ct = default)` (D-12).
    - Build the URL `$"api/v1/query?query={Uri.EscapeDataString(promQL)}"` (V5 / Tampering mitigation — escape the interpolated query). `GET` it.
    - On `resp.IsSuccessStatusCode`: parse, check `root.GetProperty("status").GetString() == "success"` AND `root.TryGetProperty("data", out var data)` AND `predicate(data)` → return `JsonDocument.Parse(data.GetRawText()).RootElement.Clone()`.
    Copy the exact body from 30-RESEARCH.md § Code Examples. Also add the two static predicate helpers `VectorNonEmpty(JsonElement)` and `HasNumericValue(JsonElement)` from that section AS public/internal statics on the class (they will be reused by the Wave 2 E2E) — remember Prometheus sample values are QUOTED JSON strings (`"1"`), so parse via `.GetString()` + `double.TryParse`.

    Create `tests/BaseApi.Tests/Observability/ResolveInstanceIdFacts.cs` — a hermetic `[Fact]` test of the env-precedence rule (D-10) that the base libs implement. Because `ResolveInstanceId()` is a `private static` duplicated in each base lib (D-09, not test-visible), assert the precedence against a small local copy of the SAME expression OR (preferred) make the test drive the precedence logic directly: set/clear `POD_NAME` and `HOSTNAME` via `Environment.SetEnvironmentVariable` and assert a local `Resolve()` lambda matching `Environment.GetEnvironmentVariable("POD_NAME") ?? Environment.GetEnvironmentVariable("HOSTNAME") ?? Environment.MachineName ?? Guid.NewGuid().ToString("N")` returns POD_NAME > HOSTNAME > non-empty. Restore env vars in a finally. Tag the class `[Collection("Observability")]` if it shares env mutation with other observability tests (check ElasticsearchTestClient siblings). This catches the D-10 precedence bug cheaply without the full stack.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "Category!=RealStack" -- --filter-class "*ResolveInstanceIdFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Observability/Helpers/PrometheusTestClient.cs` exists; `grep` finds `localhost:9090` and `PollPromForQuery` and `Uri.EscapeDataString`.
    - `grep` of PrometheusTestClient.cs does NOT find `8889`.
    - `tests/BaseApi.Tests/Observability/ResolveInstanceIdFacts.cs` exists; `grep` finds `POD_NAME` and `HOSTNAME`.
    - `dotnet test ... --filter-class "*ResolveInstanceIdFacts"` exits 0 (the precedence facts pass).
    - `dotnet build tests/BaseApi.Tests -c Debug` exits 0 (the new helper compiles, predicate helpers included).
  </acceptance_criteria>
  <done>PrometheusTestClient (pointed at :9090, with the two predicate helpers) and the ResolveInstanceId precedence test exist and the hermetic test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Unify the shared OTel resource + set service.instance.id in BOTH base libs</name>
  <read_first>
    - src/BaseApi.Core/DependencyInjection/ObservabilityServiceCollectionExtensions.cs (the file being edited — lines 48-73 carry the two AddService calls)
    - src/BaseConsole.Core/DependencyInjection/BaseConsoleObservabilityExtensions.cs (the sibling being edited — lines 43-64; KEEP its AddMeter(InstrumentationOptions.MeterName) "MassTransit" untouched)
    - 30-PATTERNS.md § "ObservabilityServiceCollectionExtensions.cs" and § "BaseConsoleObservabilityExtensions.cs" (the resolve-once + AddAttributes edit shape + the ResolveInstanceId helper to DUPLICATE)
    - 30-RESEARCH.md § "Pattern 3" + § "ResolveInstanceId() helper" + Anti-Patterns (resolve ONCE; instance.id is a RESOURCE attribute, never a per-instrument tag)
  </read_first>
  <action>
    In EACH base lib (`ObservabilityServiceCollectionExtensions.cs` AND `BaseConsoleObservabilityExtensions.cs`), after reading `serviceName`/`serviceVersion`, resolve the per-replica id ONCE into a single local (D-10 correctness — calling the resolver twice risks the GUID fallback differing between the logs and metrics resources):
    ```csharp
    var instanceId = ResolveInstanceId();   // resolve ONCE
    var instanceAttrs = new[] { new KeyValuePair<string, object>("service.instance.id", instanceId) };
    ```
    Then apply `.AddAttributes(instanceAttrs)` to BOTH resources:
    - On the logs resource: `o.SetResourceBuilder(ResourceBuilder.CreateDefault().AddService(serviceName: serviceName, serviceVersion: serviceVersion).AddAttributes(instanceAttrs));`
    - On the metrics resource: `.ConfigureResource(r => r.AddService(serviceName: serviceName, serviceVersion: serviceVersion).AddAttributes(instanceAttrs))`.
    Leave EVERYTHING else exactly as-is — the API lib keeps `AddAspNetCoreInstrumentation()` (METRIC-03 — `/health/*` is filtered at the collector, NOT here) + `AddHttpClientInstrumentation()` + `AddRuntimeInstrumentation()`; the console lib keeps `.AddMeter(InstrumentationOptions.MeterName)` ("MassTransit") + `AddRuntimeInstrumentation()`. Do NOT touch `compose/otel-collector-config.yaml` — `resource_to_telemetry_conversion: true` already promotes `service.instance.id` → the `service_instance_id` label (METRIC-07).

    Add a `private static string ResolveInstanceId()` helper to EACH class — DUPLICATE it independently per D-09 (`Messaging.Contracts` is the wrong home; `BaseConsole.Core` is hard-forbidden from referencing `BaseApi.Core`; ~6 lines is not worth a shared lib):
    ```csharp
    private static string ResolveInstanceId() =>
        Environment.GetEnvironmentVariable("POD_NAME")
        ?? Environment.GetEnvironmentVariable("HOSTNAME")
        ?? Environment.MachineName
        ?? Guid.NewGuid().ToString("N");   // MachineName is effectively non-null; GUID is the documented final fallback (D-10)
    ```
    DO NOT add `AddMeter("BaseProcessor")` or `AddMeter("Orchestrator")` in EITHER of these files — those meters are registered in Plans 02/03 (the console lib CANNOT reference `BaseProcessor.Core` — compile firewall, Landmine 1). This plan's ONLY metrics change to these files is the shared-resource/instance-id unification.
  </action>
  <verify>
    <automated>dotnet build src/BaseApi.Core -c Debug && dotnet build src/BaseConsole.Core -c Debug && dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` of `ObservabilityServiceCollectionExtensions.cs` finds `service.instance.id`, `AddAttributes`, AND `ResolveInstanceId`; finds `POD_NAME`, `HOSTNAME`, `Environment.MachineName`.
    - `grep` of `BaseConsoleObservabilityExtensions.cs` finds `service.instance.id`, `AddAttributes`, `ResolveInstanceId`; STILL finds `InstrumentationOptions.MeterName` (MassTransit meter preserved).
    - `grep` of BOTH files does NOT find `AddMeter("BaseProcessor")` or `AddMeter("Orchestrator")` (Landmine 1 — those land in Plans 02/03).
    - `git diff --quiet compose/otel-collector-config.yaml` exits 0 (collector unchanged — METRIC-07).
    - `dotnet build src/BaseApi.Core -c Debug` and `dotnet build src/BaseConsole.Core -c Debug` both exit 0; `dotnet build SK_P.sln -c Release` 0 warnings.
    - `dotnet test ... --filter "Category!=RealStack"` exits 0 (no hermetic regression).
  </acceptance_criteria>
  <done>Both base libs resolve the instance id once and apply service.instance.id to both the logs and metrics resources; collector config untouched; hermetic suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Prometheus server (:9090) | The PrometheusTestClient constructs a PromQL query string; the query param is the only interpolated value (Wave 2 interpolates a GUID). |
| env → OTel resource | `POD_NAME`/`HOSTNAME` are read into a bounded resource attribute (one value per replica). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | PromQL query construction in PrometheusTestClient | mitigate | `Uri.EscapeDataString(promQL)` on the query param; query bodies are static C# templates (no untrusted input) — RESEARCH §Security. |
| T-30-02 | Denial of Service | metric label cardinality (service_instance_id) | accept | `service_instance_id` is bounded by replica count (not per-execution); NO `workflowId`. Locked by SPEC/D-03. |
</threat_model>

<verification>
- `dotnet build src/BaseApi.Core -c Debug` and `dotnet build src/BaseConsole.Core -c Debug` exit 0.
- `dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"` exits 0 (includes ResolveInstanceIdFacts).
- `git diff --quiet compose/otel-collector-config.yaml` exits 0.
- `dotnet build SK_P.sln -c Release` = 0 Warning / 0 Error.
</verification>

<success_criteria>
- Both base libs set `service.instance.id` on the shared OTel resource from `POD_NAME → HOSTNAME → MachineName → GUID`, applied to BOTH logs and metrics resources, resolved once.
- `PrometheusTestClient` (BaseAddress `localhost:9090`, `PollPromForQuery`, `VectorNonEmpty`/`HasNumericValue` predicate helpers) exists and compiles.
- `ResolveInstanceIdFacts` proves the env precedence and is GREEN.
- The collector metrics pipeline is unchanged (METRIC-07).
</success_criteria>

<output>
After completion, create `.planning/phases/30-runtime-business-metrics/30-01-SUMMARY.md`.
</output>
