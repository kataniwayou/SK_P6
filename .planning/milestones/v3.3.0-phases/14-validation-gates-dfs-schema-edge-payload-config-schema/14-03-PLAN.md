---
phase: 14-validation-gates-dfs-schema-edge-payload-config-schema
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/BaseApi.Service/Features/Orchestration/Validation/SchemaEdgeValidator.cs
  - tests/BaseApi.Tests/Features/Orchestration/SchemaEdgeFacts.cs
autonomous: true
requirements:
  - L1-VALIDATE-05
user_setup: []

must_haves:
  truths:
    - "A schema-edge mismatch (parent.OutputSchemaId != child.InputSchemaId) returns 422 with (parentStepId, childStepId)"
    - "null on either side (source/sink/unconfigured processor) passes the edge"
    - "every entry in parent.NextStepIds is checked, not just the first"
  artifacts:
    - path: "src/BaseApi.Service/Features/Orchestration/Validation/SchemaEdgeValidator.cs"
      provides: "Independent edge walk, strict Guid equality, null-passes"
      contains: "OrchestrationValidationException.SchemaEdge"
    - path: "tests/BaseApi.Tests/Features/Orchestration/SchemaEdgeFacts.cs"
      provides: "mismatch → 422; null-side → pass"
      contains: "SchemaEdgeFacts"
  key_links:
    - from: "src/BaseApi.Service/Features/Orchestration/Validation/SchemaEdgeValidator.cs"
      to: "snapshot.Processors"
      via: "resolve parent/child ProcessorId → Output/InputSchemaId"
      pattern: "snapshot\\.Processors"
    - from: "SchemaEdgeValidator"
      to: "OrchestrationValidationException"
      via: "throw on mismatch"
      pattern: "OrchestrationValidationException\\.SchemaEdge"
---

<objective>
Fill the `SchemaEdgeValidator.Validate` seam body (D-09). An INDEPENDENT edge walk (NOT sharing the
cycle DFS — Phase 13 D-03 / D-07: no shared traversal abstraction) over every `(parent → child)`
edge across EVERY entry in `parent.NextStepIds`, requiring strict `Guid` equality between
`parent.Processor.OutputSchemaId` and `child.Processor.InputSchemaId`; null on either side passes.

Purpose: SC#3 (schema-edge mismatch → 422 with (parentStepId, childStepId); null-side passes).
Output: filled SchemaEdgeValidator + SchemaEdgeFacts integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-CONTEXT.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-RESEARCH.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-PATTERNS.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-01-SUMMARY.md

<interfaces>
Seam to fill (src/BaseApi.Service/Features/Orchestration/Validation/SchemaEdgeValidator.cs — current):
```csharp
internal sealed class SchemaEdgeValidator
{
    public void Validate(WorkflowGraphSnapshot snapshot) { /* no-op in P13 */ }
}
```

Input contract:
- snapshot.Steps : Dictionary<Guid, StepReadDto>; StepReadDto.Id (Guid), .ProcessorId (Guid), .NextStepIds (List<Guid>?).
- snapshot.Processors : Dictionary<Guid, ProcessorReadDto>; ProcessorReadDto.InputSchemaId, .OutputSchemaId, .ConfigSchemaId are all `Guid?` (ProcessorDtos.cs lines 48-50).

Exception factory (from Plan 14-01):
```csharp
public static OrchestrationValidationException SchemaEdge(Guid parentStepId, Guid childStepId);
```
offending body `{ parentStepId, childStepId }`, gate "schemaEdge".

Test fixture + seeding: Phase8WebAppFactory; seed Schemas via POST /api/v1/schemas (SchemaCreateDto:
Name, Version, Description, Definition), then Processors with InputSchemaId/OutputSchemaId set to
schema ids, then Steps with NextStepIds wiring the edge, then a Workflow.
HashHelpers.RandomSha256Hex() for Processor.SourceHash.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill SchemaEdgeValidator with independent edge walk</name>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/Validation/SchemaEdgeValidator.cs (seam to fill)
    - src/BaseApi.Service/Features/Processor/ProcessorDtos.cs (InputSchemaId/OutputSchemaId Guid?, lines 42-54)
    - src/BaseApi.Service/Features/Step/StepDtos.cs (StepReadDto.ProcessorId, .NextStepIds, .Id)
    - src/BaseApi.Service/Features/Orchestration/Validation/CycleDetector.cs (the cycle gate — confirm SchemaEdge does NOT reuse it; D-07 independent walk)
    - 14-RESEARCH.md §"Schema-Edge Validator" (lines 299-307)
  </read_first>
  <behavior>
    - parent.Processor.OutputSchemaId == child.Processor.InputSchemaId (both non-null, equal Guid) → passes.
    - mismatch (both non-null, different Guid) → throws SchemaEdge(parent.Id, child.Id).
    - parent.OutputSchemaId null OR child.InputSchemaId null → passes (source/sink/unconfigured).
    - a step with multiple NextStepIds checks EVERY edge; a mismatch on the 2nd child still throws.
  </behavior>
  <action>
    Replace the no-op body of `SchemaEdgeValidator.Validate(WorkflowGraphSnapshot snapshot)`. This is
    an INDEPENDENT walk — do NOT call CycleDetector and do NOT build a shared traversal abstraction
    (D-07 / Phase 13 D-03).

    For each `parent` in `snapshot.Steps.Values`:
    - `foreach (var childId in parent.NextStepIds ?? Enumerable.Empty<Guid>())`:
      - `if (!snapshot.Steps.TryGetValue(childId, out var child)) continue;` (a dangling child is the CycleDetector/missing-step gate's concern, which runs FIRST in the locked order — defensively skip here rather than throwing a different gate's error).
      - Resolve `var parentOut = snapshot.Processors.TryGetValue(parent.ProcessorId, out var pproc) ? pproc.OutputSchemaId : (Guid?)null;`
      - Resolve `var childIn = snapshot.Processors.TryGetValue(child.ProcessorId, out var cproc) ? cproc.InputSchemaId : (Guid?)null;`
      - `if (parentOut is null || childIn is null) continue;` (null on either side passes — Phase 10 source/sink/unconfigured processor).
      - `if (parentOut.Value != childIn.Value) throw OrchestrationValidationException.SchemaEdge(parent.Id, child.Id);` (strict Guid equality).

    Keep `internal sealed class SchemaEdgeValidator`, signature `public void Validate(WorkflowGraphSnapshot snapshot)` UNCHANGED. Add `using System.Linq;` if needed for Enumerable.Empty. Add an XML doc summary: independent edge walk over EVERY NextStepIds entry (not just the first), strict Guid equality, null-on-either-side passes; does NOT share the cycle DFS (D-07/D-09 — L1-VALIDATE-05).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~SchemaEdgeFacts"</automated>
  </verify>
  <acceptance_criteria>
    - grep `OrchestrationValidationException.SchemaEdge` present in SchemaEdgeValidator.cs.
    - grep `snapshot.Processors` present; grep `OutputSchemaId` and `InputSchemaId` both present.
    - grep `parentOut is null || childIn is null` (or equivalent null-pass guard) present.
    - grep for any reference to `CycleDetector` in SchemaEdgeValidator.cs returns ZERO hits (independent walk — D-07).
    - `dotnet build` zero warnings; SchemaEdgeFacts (Task 2) passes.
  </acceptance_criteria>
  <done>SchemaEdgeValidator walks every edge, requires strict Guid equality of OutputSchemaId==InputSchemaId, passes null-on-either-side, throws SchemaEdge(parent,child) on mismatch — independent of the cycle DFS.</done>
</task>

<task type="auto">
  <name>Task 2: SchemaEdgeFacts integration test (mismatch → 422; null-side → pass)</name>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/StartOrchestrationFacts.cs (scaffold + Processor/Step/Workflow seeding + 422/problem+json idiom)
    - src/BaseApi.Service/Features/Schema/SchemaDtos.cs (SchemaCreateDto: Name, Version, Description, Definition)
    - src/BaseApi.Service/Features/Processor/ProcessorDtos.cs (ProcessorCreateDto with InputSchemaId/OutputSchemaId)
    - tests/BaseApi.Tests/TestHelpers/HashHelpers.cs (RandomSha256Hex)
  </read_first>
  <action>
    Create tests/BaseApi.Tests/Features/Orchestration/SchemaEdgeFacts.cs (namespace
    `BaseApi.Tests.Features.Orchestration`, `[Trait("Phase", "14")]`,
    `IClassFixture<Phase8WebAppFactory>`). Mirror StartOrchestrationFacts scaffold.

    Seeding helpers (extend the analog): POST a Schema (Definition = a minimal valid draft-2020-12
    schema string, e.g. `"{\"type\":\"object\"}"`), POST Processors with chosen InputSchemaId /
    OutputSchemaId, POST Steps (parent.NextStepIds=[childId]; wire parent's ProcessorId to the
    output-schema processor and child's ProcessorId to the input-schema processor), POST a Workflow
    EntryStepIds=[parentStepId].

    Fact 1 `SchemaEdgeMismatch_Returns422_WithParentAndChild`: seed schemaX and schemaY (distinct).
    parentProcessor.OutputSchemaId = schemaX; childProcessor.InputSchemaId = schemaY (mismatch).
    parent.NextStepIds=[child]. POST start. Assert:
    - 422 + application/problem+json.
    - body `errors.gate` == "schemaEdge"; `errors.offending.parentStepId` == parent step id;
      `errors.offending.childStepId` == child step id.

    Fact 2 `SchemaEdgeNullSide_Passes`: parentProcessor.OutputSchemaId = null (source processor),
    childProcessor.InputSchemaId = schemaY. parent.NextStepIds=[child]. POST start. Assert 204 (null
    on either side passes). To keep the cycle + payload gates from interfering, use distinct steps
    with no back-edge and ConfigSchemaId=null on all processors.

    Use `var ct = TestContext.Current.CancellationToken;` and the async PostAsJsonAsync idiom.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~SchemaEdgeFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; grep `[Trait("Phase", "14")]`, `SchemaEdgeMismatch_Returns422_WithParentAndChild`, `SchemaEdgeNullSide_Passes` present.
    - grep `HttpStatusCode.UnprocessableEntity` and `HttpStatusCode.NoContent` both present.
    - grep `"schemaEdge"`, `parentStepId`, `childStepId` present.
    - Both facts pass.
  </acceptance_criteria>
  <done>Schema-edge mismatch → 422 with errors.gate="schemaEdge" + (parentStepId, childStepId); null-on-either-side → 204. Both facts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted workflow graph → SchemaEdgeValidator | Processor schema-id references resolved via snapshot dictionaries (no EF nav, no I/O) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering (incompatible-pipeline) | SchemaEdgeValidator | mitigate | Strict Guid equality on every edge over EVERY NextStepIds entry; mismatched pipelines rejected with 422 before any L2 projection (Phase 15). |
| T-14-08 | Information Disclosure | schemaEdge 422 offending | accept | offending carries only stepId Guids — no schema bodies, no internals; low risk. |
</threat_model>

<verification>
- `dotnet build` Debug + Release zero warnings.
- `dotnet test --filter "FullyQualifiedName~SchemaEdgeFacts"` GREEN.
- `dotnet test --filter "Phase=9"` GREEN (happy path + existence unchanged).
</verification>

<success_criteria>
- SC#3: schema-edge mismatch → 422 with (parentStepId, childStepId); null-on-either-side passes;
  every NextStepIds entry checked. L1-VALIDATE-05 covered. Independent walk (no shared abstraction).
</success_criteria>

<output>
After completion, create `.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-03-SUMMARY.md`
</output>
