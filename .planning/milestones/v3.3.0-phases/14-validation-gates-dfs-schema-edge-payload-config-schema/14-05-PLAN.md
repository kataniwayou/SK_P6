---
phase: 14-validation-gates-dfs-schema-edge-payload-config-schema
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - tests/BaseApi.Tests/Features/Orchestration/ValidationOrderFacts.cs
  - .planning/STATE.md
autonomous: true
requirements:
  - L1-VALIDATE-01
  - L1-VALIDATE-02
  - L1-VALIDATE-09
  - L1-VALIDATE-10
  - ORCH-START-03
user_setup: []

must_haves:
  truths:
    - "A multi-failure workflow returns the FIRST gate's status in the locked order existence(404) → cycle → schema-edge → payload"
    - "L1 cleanup (snapshot Dispose / IsDisposed) still runs on the validation-failure path"
    - "Assignment PUT/POST remain valid-JSON-only (VALID-21 closes only at orchestration-start)"
    - "Existence gate stays 404 (D-13) — the only structural gates that 422 are the three new ones"
    - "Full suite (v3.2.0 baseline + v3.3.0 additions) is GREEN; SSRF <500ms non-regression holds"
  artifacts:
    - path: "tests/BaseApi.Tests/Features/Orchestration/ValidationOrderFacts.cs"
      provides: "Gate-order short-circuit proof + L1-cleanup-on-validation-failure"
      contains: "ValidationOrderFacts"
  key_links:
    - from: "ValidationOrderFacts"
      to: "POST /api/v1/orchestration/start"
      via: "multi-failure workflow asserts FIRST gate"
      pattern: "orchestration/start"
    - from: "ValidationOrderFacts cleanup fact"
      to: "WorkflowGraphSnapshot.IsDisposed"
      via: "recording loader captures disposal on validation-failure path"
      pattern: "IsDisposed"
---

<objective>
Prove the locked gate order and the L1-cleanup-on-failure contract end-to-end, confirm the v3.2.0
invariants did not regress, and record the L1-VALIDATE-09 / L1-VALIDATE-10 traceability notes.

This is the integration/order plan (runs last — needs all three gate bodies from 14-02/03/04 active
to prove short-circuit). It supplies workflows that fail MULTIPLE gates at once and asserts the FIRST
gate fires in the order existence(404 — D-13) → cycle → schema-edge → payload, and that
`snapshot.Dispose()` runs on the validation-failure path (the `using var snapshot` in StartAsync).

Purpose: SC#5 (gate order verified by multi-failure workflows; L1 cleanup on every failure path).
Output: ValidationOrderFacts + STATE.md decision/traceability entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-CONTEXT.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-RESEARCH.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-02-SUMMARY.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-03-SUMMARY.md
@.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-04-SUMMARY.md

<interfaces>
Locked pipeline order (src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs StartAsync):
```
1. await ExistenceCheckAsync(workflowIds, ct);        // NotFoundException → 404 (D-13)
2. using var snapshot = await _loader.LoadL1Async(...);  // Dispose on success AND throw
3. _cycleDetector.Validate(snapshot);                 // cycle / missingStep → 422
4. _schemaEdgeValidator.Validate(snapshot);           // schemaEdge → 422
5. _payloadConfigSchemaValidator.Validate(snapshot);  // payloadConfigSchema → 422
6. await _redisProjectionWriter.UpsertAsync(...);     // no-op until Phase 15
```

L1-cleanup canary pattern (tests/BaseApi.Tests/Features/Orchestration/StartCleanupFacts.cs):
- RecordingWorkflowGraphLoader wraps the real IWorkflowGraphLoader, stashes the returned snapshot.
- Registered via `_factory.WithWebHostBuilder(b => b.ConfigureTestServices(services => { ... }))`.
- After the request, asserts `recorder.Captured.IsDisposed == true` and all 5 dicts Count==0.

Existence stays 404 (D-13): StartOrchestrationFacts.Start_Returns404_WhenAnyWorkflowIdMissing asserts
404 + resourceType="WorkflowEntity". Do NOT change it. The "422 for existence" wording in
L1-VALIDATE-01/02/ORCH-START-03 applies to the THREE structural gates only.

Seeding helpers: reuse the multi-step/cycle/schema-edge/payload seeding built in 14-02/03/04 (copy
the needed helper into ValidationOrderFacts or factor a small shared seeding helper file — keep it
local to the test project).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ValidationOrderFacts — gate-order short-circuit + L1 cleanup on validation-failure</name>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/StartOrchestrationFacts.cs (scaffold + seeding + 404/422/problem+json idioms)
    - tests/BaseApi.Tests/Features/Orchestration/StartCleanupFacts.cs (RecordingWorkflowGraphLoader + ConfigureTestServices disposal-capture pattern)
    - tests/BaseApi.Tests/Features/Orchestration/CycleDetectionFacts.cs (14-02 — cycle + multi-step seeding helper)
    - tests/BaseApi.Tests/Features/Orchestration/SchemaEdgeFacts.cs (14-03 — schema/processor seeding helper)
    - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs (the locked pipeline order; using var snapshot line 78)
  </read_first>
  <action>
    Create tests/BaseApi.Tests/Features/Orchestration/ValidationOrderFacts.cs (namespace
    `BaseApi.Tests.Features.Orchestration`, `[Trait("Phase", "14")]`,
    `IClassFixture<Phase8WebAppFactory>`). Mirror StartOrchestrationFacts + StartCleanupFacts.

    Fact 1 `ExistenceBeforeCycle_MissingWorkflowId_Returns404_NotCycle`: POST start with a
    Guid.NewGuid() that does not exist. Assert 404 (existence wins — D-13; existence runs BEFORE the
    snapshot is even loaded, so a cycle that "would" exist is never evaluated). Confirms existence
    short-circuits ahead of the 422 gates.

    Fact 2 `CycleBeforeSchemaEdge_WorkflowFailingBoth_Returns422Cycle`: seed a workflow that fails
    BOTH the cycle gate AND the schema-edge gate (a back-edge cycle A→B→A where A's processor
    OutputSchemaId != B's processor InputSchemaId). POST start. Assert 422 + body `errors.gate` ==
    "cycle" (cycle runs at step 3, schema-edge at step 4 — cycle wins). This proves cycle short-circuits
    ahead of schema-edge.

    Fact 3 `SchemaEdgeBeforePayload_WorkflowFailingBoth_Returns422SchemaEdge`: seed an ACYCLIC
    workflow A→B where A.OutputSchemaId != B.InputSchemaId (schema-edge fail) AND B has an Assignment
    whose Payload also violates B's ConfigSchema (payload fail). POST start. Assert 422 + body
    `errors.gate` == "schemaEdge" (schema-edge at step 4 wins over payload at step 5). Proves
    schema-edge short-circuits ahead of payload.

    Fact 4 `L1Cleanup_RunsOnValidationFailurePath`: install a RecordingWorkflowGraphLoader (copy the
    inner-wrapping double from StartCleanupFacts) via ConfigureTestServices. Seed a workflow that fails
    the cycle gate (a real back-edge cycle). POST start → expect 422. Then assert:
    - `recorder.Captured` is not null AND `recorder.Captured.IsDisposed == true`,
    - all 5 dictionaries Count == 0 (snapshot.Dispose ran on the validation-failure path via the
      `using var snapshot` declaration — L1-VALIDATE-01 / ORCH-START "L1 cleanup still runs on failure").
    Unlike StartCleanupFacts (which forces a 500 via a throwing writer), this fact forces a 422 via a
    real cycle and proves cleanup runs on the DOMAIN-VALIDATION failure path too.

    Use `var ct = TestContext.Current.CancellationToken;`, async PostAsJsonAsync/PutAsJsonAsync, and
    JsonDocument body parsing for the `errors.gate` assertions. Copy/import seeding helpers from the
    14-02/14-03/14-04 fact files (duplicating a small private static helper is acceptable; do not add
    production code).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~ValidationOrderFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; grep `[Trait("Phase", "14")]`, `ExistenceBeforeCycle_MissingWorkflowId_Returns404_NotCycle`, `CycleBeforeSchemaEdge_WorkflowFailingBoth_Returns422Cycle`, `SchemaEdgeBeforePayload_WorkflowFailingBoth_Returns422SchemaEdge`, `L1Cleanup_RunsOnValidationFailurePath` all present.
    - grep `HttpStatusCode.NotFound`, `HttpStatusCode.UnprocessableEntity` present.
    - grep `"cycle"`, `"schemaEdge"` present as gate assertions; grep `IsDisposed` present.
    - grep `RecordingWorkflowGraphLoader` (or equivalent inner-wrapping loader double) present + `ConfigureTestServices`.
    - All four facts pass.
  </acceptance_criteria>
  <done>Multi-failure workflows assert the FIRST gate in the locked order (existence 404 → cycle → schema-edge → payload); L1 cleanup (IsDisposed + empty dicts) confirmed on the 422 validation-failure path. All facts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression sweep + STATE.md traceability (L1-VALIDATE-02/09/10, D-13)</name>
  <read_first>
    - .planning/STATE.md (Accumulated Context > Decisions — append format used by prior plans)
    - tests/BaseApi.Tests/Features/Orchestration/StartOrchestrationFacts.cs (existence 404 facts — D-13 / L1-VALIDATE-02; must stay GREEN)
    - tests/BaseApi.Tests/Integration/ErrorMappingFacts.cs (SSRF <500ms — D-06; must stay GREEN)
    - Any AssignmentsIntegrationTests (Assignment PUT/POST valid-JSON-only — L1-VALIDATE-09; must stay GREEN — locate via grep "Assignments" under tests/)
  </read_first>
  <action>
    This task adds NO production code. It is the regression-and-traceability gate.

    1. Run the FULL suite (requires Postgres + Redis compose stack up): `dotnet test`. Confirm GREEN.
       The v3.2.0 invariants that MUST stay GREEN:
       - Existence 404 (StartOrchestrationFacts.Start_Returns404_*) — D-13 / L1-VALIDATE-02. The three
         new structural gates 422; existence intentionally stays 404 by user decision.
       - SSRF <500ms + draft-2020-12 (ErrorMappingFacts) — D-06 / L1-VALIDATE-07.
       - Assignment PUT/POST valid-JSON-only (AssignmentsIntegrationTests) — L1-VALIDATE-09 (VALID-21
         closes ONLY at orchestration-start; HTTP-write side untouched).
       - RFC 7807 + X-Correlation-Id; Mapperly RMG codes; zero-warning build.
       If any of these fail, STOP and report — a regression must be fixed before closing the phase.

    2. Append to .planning/STATE.md Accumulated Context > Decisions (mirror the existing bullet style):
       - "Plan 14-05: L1-VALIDATE-02 satisfied by re-using the existence path (D-13) — existence stays
         404 (NotFoundException), NOT 422; StartOrchestrationFacts.Start_Returns404_* unchanged. Only
         the three NEW structural gates (cycle/missingStep, schemaEdge, payloadConfigSchema) throw
         OrchestrationValidationException → 422. The '422 for existence' wording in
         L1-VALIDATE-01/02/ORCH-START-03 applies to the three structural gates only."
       - "Plan 14-05: L1-VALIDATE-09 — VALID-21 closes ONLY at orchestration-start; Assignment PUT/POST
         remain valid-JSON-only (AssignmentsIntegrationTests unchanged + GREEN). HTTP-write Payload↔Schema
         conformance is FUTURE-VALID-21-HTTP-WRITE (out of v3.3.0)."
       - "Plan 14-05: L1-VALIDATE-10 — TEST-03 (Testcontainers.PostgreSql) + TEST-04 (Respawn) remain
         DEFERRED; v3.3.0 does NOT close them. Recorded for traceability."
       - "Plan 14-05: Gate order proven by ValidationOrderFacts multi-failure workflows (existence 404 →
         cycle → schema-edge → payload short-circuit) + L1 cleanup (snapshot.IsDisposed) on the 422
         validation-failure path. D-04 split-Fallback (AddBaseApiFallbackHandler last in Program.cs) keeps
         Fallback last-walked; OrchestrationValidationExceptionHandler reachable → 422."

    Do NOT modify any production source in this plan. Do NOT change the existence-gate status code.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` (full suite) exits 0 — all v3.2.0 baseline + v3.3.0 Phase 14 facts GREEN.
    - `dotnet test --filter "FullyQualifiedName~StartOrchestrationFacts"` GREEN (existence 404 intact — D-13).
    - `dotnet test --filter "FullyQualifiedName~ErrorMappingFacts"` GREEN (SSRF <500ms — D-06).
    - `dotnet test --filter "FullyQualifiedName~Assignments"` GREEN (valid-JSON-only — L1-VALIDATE-09).
    - STATE.md contains the four new Plan 14-05 bullets (grep "Plan 14-05" returns ≥4 hits).
    - No production .cs file changed in this plan (git diff shows only the test file from Task 1 + STATE.md).
  </acceptance_criteria>
  <done>Full suite GREEN with all v3.2.0 invariants intact (404 existence, SSRF <500ms, Assignment valid-JSON-only); STATE.md records L1-VALIDATE-02/09/10 + D-13 + gate-order traceability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multi-failure untrusted workflow → gate pipeline | The first failing gate must short-circuit deterministically; no gate may leak past the first failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Tampering (gate bypass) | gate ordering in StartAsync | mitigate | ValidationOrderFacts proves the locked order short-circuits at the FIRST failure; a reordering regression flips an asserted gate discriminator and fails the build. |
| T-14-13 | Denial of Service (resource leak) | L1 snapshot on failure path | mitigate | L1Cleanup_RunsOnValidationFailurePath asserts snapshot.Dispose() runs on the 422 path via `using var snapshot` — no leaked in-memory graph on validation failure. |
| T-14-14 | Information Disclosure | regression of error-body shape | accept | Existing ErrorMappingFacts + StartOrchestrationFacts lock the body shapes; this plan re-runs them as the guard. |
</threat_model>

<verification>
- `dotnet test` (FULL suite, Postgres + Redis up) exits 0.
- ValidationOrderFacts: existence 404 wins; cycle wins over schema-edge; schema-edge wins over payload; L1 cleanup on 422 path.
- v3.2.0 invariants GREEN: existence 404 (D-13), SSRF <500ms (D-06), Assignment valid-JSON-only (L1-VALIDATE-09).
- STATE.md updated with L1-VALIDATE-02/09/10 + D-13 traceability.
</verification>

<success_criteria>
- SC#5: validation runs existence → cycle → schema-edge → payload; multi-failure workflows assert the
  FIRST gate's status; L1 cleanup runs in the failure path on EVERY gate. L1-VALIDATE-01/02/09/10 +
  ORCH-START-03 covered. Full suite GREEN; no v3.2.0 regression.
</success_criteria>

<output>
After completion, create `.planning/phases/14-validation-gates-dfs-schema-edge-payload-config-schema/14-05-SUMMARY.md`
</output>
