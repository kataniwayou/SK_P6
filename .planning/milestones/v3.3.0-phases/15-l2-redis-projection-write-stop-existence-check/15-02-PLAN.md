---
phase: 15-l2-redis-projection-write-stop-existence-check
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/BaseApi.Service/Features/Orchestration/Projection/IRedisProjectionWriter.cs
  - src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs
  - tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs
autonomous: true
requirements:
  - L2-PROJECT-01
  - L2-PROJECT-03
  - L2-PROJECT-04
  - L2-PROJECT-05
  - L2-PROJECT-06
  - ORCH-START-06

must_haves:
  truths:
    - "UpsertAsync(snapshot, correlationId, ct) writes all 3 keyspaces (root, per-step, per-processor) for a workflow in one CreateBatch pipeline"
    - "Processor keys carry a TTL of ProcessorKeyTtlDays; root and per-step keys carry NO TTL"
    - "ProcessorKeyTtlDays <= 0 means no expiry on processor keys"
    - "liveness.timestamp comes from injected TimeProvider; status is 'Pending', interval is 0, jobId is a fresh Guid each call"
    - "A mid-batch failure rethrows the first faulting task's exception after one MEL warning naming the workflowId"
  artifacts:
    - path: "src/BaseApi.Service/Features/Orchestration/Projection/IRedisProjectionWriter.cs"
      provides: "UpsertAsync with explicit correlationId param (D-01)"
      contains: "string correlationId"
    - path: "src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs"
      provides: "Batch 3-keyspace write with per-processor TTL"
      contains: "CreateBatch"
  key_links:
    - from: "RedisProjectionWriter"
      to: "Redis"
      via: "IConnectionMultiplexer.GetDatabase().CreateBatch() + StringSetAsync"
      pattern: "StringSetAsync"
    - from: "RedisProjectionWriter"
      to: "RedisProjectionKeys"
      via: "Root/Step/Processor formatters"
      pattern: "RedisProjectionKeys\\.(Root|Step|Processor)"
---

<objective>
Fill the no-op `RedisProjectionWriter.UpsertAsync` so it projects a single-workflow `WorkflowGraphSnapshot` into the 3 Redis keyspaces using a `CreateBatch()` pipeline (L2-PROJECT-01), with the camelCase records from Plan 01, a per-processor TTL (D-08), `TimeProvider`-sourced liveness timestamps (D-05), and the D-03 partial-failure semantics (one MEL warning + rethrow). Change the interface signature to accept the explicit `correlationId` param (D-01).

Purpose: This is the L1→L2 write engine. The Start loop (Plan 04) calls it per workflow. The writer owns DTO assembly + serialization + batch SET; it stays HTTP-agnostic (correlationId arrives as a param).
Output: 1 modified interface, 1 filled writer, 1 new integration-test fact class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-RESEARCH.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-01-SUMMARY.md

<interfaces>
<!-- Verified from codebase this session. Executor uses these directly. -->

Current seam (src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs — VERBATIM):
```csharp
internal sealed class RedisProjectionWriter : IRedisProjectionWriter {
    public Task UpsertAsync(WorkflowGraphSnapshot snapshot, CancellationToken ct) => Task.CompletedTask;
}
```
Current interface (IRedisProjectionWriter.cs):
```csharp
internal interface IRedisProjectionWriter {
    Task UpsertAsync(WorkflowGraphSnapshot snapshot, CancellationToken ct);
}
```

WorkflowGraphSnapshot (src/BaseApi.Service/Features/Orchestration/WorkflowGraphSnapshot.cs — verified):
```csharp
internal sealed record WorkflowGraphSnapshot(ILogger<WorkflowGraphSnapshot> Logger) : IDisposable {
    public Dictionary<Guid, WorkflowReadDto>   Workflows   { get; init; } = new();
    public Dictionary<Guid, AssignmentReadDto> Assignments { get; init; } = new();
    public Dictionary<Guid, StepReadDto>       Steps       { get; init; } = new();
    public Dictionary<Guid, ProcessorReadDto>  Processors  { get; init; } = new();
    public Dictionary<Guid, SchemaReadDto>     Schemas     { get; init; } = new();
}
```
Note: a per-workflow snapshot (Plan 04 calls LoadL1Async([oneId])) has exactly one entry in Workflows. The writer projects THAT workflow's root + every step in Steps + every processor in Processors.

Field-source mapping (PATTERNS.md lines 72-83 — verified against the DTOs):
| Projection field | Source |
|---|---|
| root entryStepIds | WorkflowReadDto.EntryStepIds ?? [] |
| root cron | WorkflowReadDto.CronExpression |
| root jobId | Guid.NewGuid() per Start (D-05) |
| root correlationId | the correlationId param (D-01) |
| step entryCondition | StepReadDto.EntryCondition |
| step processorId | StepReadDto.ProcessorId |
| step payload | snapshot.Assignments.Values.First(a => a.StepId == stepId).Payload |
| step nextStepIds | StepReadDto.NextStepIds ?? [] |
| proc inputDefinition | InputSchemaId is { } sid ? snapshot.Schemas[sid].Definition : null |
| proc outputDefinition | OutputSchemaId is { } sid ? snapshot.Schemas[sid].Definition : null |

Available DI (verified):
- IConnectionMultiplexer — Singleton (RedisServiceCollectionExtensions.cs:57); call GetDatabase() per op, never Connect() (Pitfall 5).
- TimeProvider — Singleton (PersistenceServiceCollectionExtensions.cs:24, AuditInterceptor precedent); use _clock.GetUtcNow().UtcDateTime.
- IOptions<RedisProjectionOptions> — reads KeyPrefix + new ProcessorKeyTtlDays.

Batch pattern (RESEARCH Pattern 1, lines 265-281):
```csharp
var db = _multiplexer.GetDatabase();
var batch = db.CreateBatch();
var tasks = new List<Task>();
tasks.Add(batch.StringSetAsync(RedisProjectionKeys.Root(prefix, wf.Id), rootJson));        // no TTL
foreach (var step in steps)
    tasks.Add(batch.StringSetAsync(RedisProjectionKeys.Step(prefix, wf.Id, step.Id), stepJson)); // no TTL
TimeSpan? ttl = days <= 0 ? (TimeSpan?)null : TimeSpan.FromDays(days);
foreach (var proc in processors)
    tasks.Add(batch.StringSetAsync(RedisProjectionKeys.Processor(prefix, proc.Id), procJson, expiry: ttl));
batch.Execute();
await Task.WhenAll(tasks);   // first faulting task surfaces here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen IRedisProjectionWriter.UpsertAsync signature (add correlationId)</name>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/Projection/IRedisProjectionWriter.cs (self — current signature, line 7)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md (IRedisProjectionWriter section, lines 115-118)
  </read_first>
  <action>
    Edit `IRedisProjectionWriter.cs` — change the method signature (D-01) from:
    `Task UpsertAsync(WorkflowGraphSnapshot snapshot, CancellationToken ct);`
    to:
    `Task UpsertAsync(WorkflowGraphSnapshot snapshot, string correlationId, CancellationToken ct);`
    Add a one-line XML doc: the writer stays HTTP-agnostic; `correlationId` is resolved once by `OrchestrationService` and passed explicitly (D-01).
  </action>
  <verify>
    <automated>rg "string correlationId" src/BaseApi.Service/Features/Orchestration/Projection/IRedisProjectionWriter.cs</automated>
  </verify>
  <acceptance_criteria>
    - `IRedisProjectionWriter.cs` declares `Task UpsertAsync(WorkflowGraphSnapshot snapshot, string correlationId, CancellationToken ct);`.
  </acceptance_criteria>
  <done>Interface carries the explicit correlationId param; writer impl (Task 2) and callers (Plan 04) will match.</done>
</task>

<task type="auto">
  <name>Task 2: Fill RedisProjectionWriter — 3-keyspace batch write with TTL + liveness + partial-failure semantics</name>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs (self — no-op seam to fill)
    - src/BaseApi.Service/Features/Orchestration/Loading/WorkflowGraphLoader.cs (with { } enrichment + AsNoTracking assembly idiom, lines 104-123)
    - src/BaseApi.Core/Persistence/AuditInterceptor.cs (TimeProvider usage: _clock.GetUtcNow().UtcDateTime — analog for liveness timestamp)
    - src/BaseApi.Core/DependencyInjection/RedisServiceCollectionExtensions.cs (IConnectionMultiplexer Singleton + GetSection("Redis") bind, lines 57-63)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-RESEARCH.md (Pattern 1, lines 262-281; Pitfall 2, lines 413-417; Pitfall 5; D-03/D-08)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md (RedisProjectionWriter section, lines 87-111; field-source table lines 72-83)
  </read_first>
  <action>
    Replace the no-op body of `RedisProjectionWriter.cs`. Keep the class `internal sealed` and `IRedisProjectionWriter`.

    Add a ctor injecting (with `?? throw new ArgumentNullException(nameof(x))` guards — OrchestrationService.cs:59-65 idiom): `IConnectionMultiplexer multiplexer`, `IOptions<RedisProjectionOptions> options`, `TimeProvider clock`, `ILogger<RedisProjectionWriter> logger`. (Liveness timestamp lives in the WRITER per RESEARCH Open Q1; correlationId stays on the service.)

    Implement `UpsertAsync(WorkflowGraphSnapshot snapshot, string correlationId, CancellationToken ct)`:
    1. `var prefix = _options.Value.KeyPrefix;` and `var days = _options.Value.ProcessorKeyTtlDays;`.
    2. `var now = _clock.GetUtcNow().UtcDateTime;` Build the shared liveness once: `var liveness = new LivenessProjection(now, 0, "Pending");` (D-05: interval 0, status "Pending").
    3. The single workflow: `var wf = snapshot.Workflows.Values.Single();`.
    4. Assemble `WorkflowRootProjection` per the field-source table: `EntryStepIds = wf.EntryStepIds ?? new List<Guid>()`, `Cron = wf.CronExpression`, `JobId = Guid.NewGuid()` (D-05), `Liveness = liveness`, `CorrelationId = correlationId`. Serialize with `JsonSerializer.Serialize(root)` (default options — pins hold via Plan 01 [JsonPropertyName]). NO Mapperly (L2-PROJECT-06).
    5. `var db = _multiplexer.GetDatabase();` `var batch = db.CreateBatch();` `var tasks = new List<Task>();`
    6. `tasks.Add(batch.StringSetAsync(RedisProjectionKeys.Root(prefix, wf.Id), rootJson));` — NO expiry.
    7. For each `step` in `snapshot.Steps.Values`: assemble `StepProjection(step.EntryCondition, step.ProcessorId, payload, step.NextStepIds ?? new List<Guid>())` where `payload = snapshot.Assignments.Values.First(a => a.StepId == step.Id).Payload`. Serialize, `tasks.Add(batch.StringSetAsync(RedisProjectionKeys.Step(prefix, wf.Id, step.Id), stepJson));` — NO expiry.
    8. Compute `TimeSpan? ttl = days <= 0 ? (TimeSpan?)null : TimeSpan.FromDays(days);` (Pitfall 2). For each `proc` in `snapshot.Processors.Values`: assemble `ProcessorProjection(inputDefinition, outputDefinition, liveness)` where `inputDefinition = proc.InputSchemaId is { } isid ? snapshot.Schemas[isid].Definition : null` and same for output. Serialize, `tasks.Add(batch.StringSetAsync(RedisProjectionKeys.Processor(prefix, proc.Id), procJson, expiry: ttl));` — TTL ONLY here.
    9. `batch.Execute();` then `try { await Task.WhenAll(tasks); } catch (Exception) { _logger.LogWarning("L2 projection write partially failed for workflow {WorkflowId}; partial state may exist", wf.Id); throw; }` (D-03: one MEL warning naming workflowId, then rethrow so Phase 4 maps to 500).

    Wording/levels of the MEL message are Claude's discretion (CONTEXT) — keep `{WorkflowId}` structured.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RedisProjectionWriterFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `RedisProjectionWriter.cs` contains `CreateBatch`, `RedisProjectionKeys.Root`, `RedisProjectionKeys.Step`, `RedisProjectionKeys.Processor`, and `_clock.GetUtcNow()`.
    - The processor `StringSetAsync` is the ONLY one with an `expiry:` argument (grep: `expiry:` appears exactly once).
    - `rg "DateTime.UtcNow|Mapperly|JsonStringEnumConverter" src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs` returns 0 matches.
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RedisProjectionWriterFacts"` exits 0.
  </acceptance_criteria>
  <done>UpsertAsync writes all 3 keyspaces in one batch; processor keys TTL'd, root/step not; liveness via TimeProvider; partial-failure rethrows after a warning.</done>
</task>

<task type="auto">
  <name>Task 3: Integration facts — 3-keyspace write + TTL + camelCase shape against real Redis</name>
  <read_first>
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (RedisMultiplexer + RedisKeyPrefix surface, lines 112-128; ConfigureWebHost Redis prefix injection, lines 179-204)
    - tests/BaseApi.Tests/Composition/RedisFixture.cs (per-class prefix; SCAN+DEL teardown already sweeps processor keys, lines 52-94)
    - tests/BaseApi.Tests/Observability/SchemasLogsE2ETests.cs (xUnit.v3 + TestContext.Current.CancellationToken idiom, line 46)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-VALIDATION.md (Req→Test map, lines 555-559)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md (Tests Integration section, lines 240-241)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs` — xUnit.v3 integration class, `[Trait("Phase","15")]`, `IClassFixture<Phase8WebAppFactory>` (it already composes `RedisFixture` and exposes `RedisMultiplexer` + `RedisKeyPrefix`). Every async test passes `TestContext.Current.CancellationToken` (xUnit1051 under TreatWarningsAsErrors).

    Drive the writer through a real Start (POST /api/v1/orchestration/start) OR resolve the writer from the factory's service provider with a hand-built per-workflow `WorkflowGraphSnapshot` — pick whichever is the smaller seam (driving via HTTP Start exercises Plan 04 too, but Plan 04 is not yet merged in this wave; prefer resolving `IRedisProjectionWriter` from `_factory.Services.CreateScope()` and seeding Postgres rows via the existing entity POST endpoints, OR construct the snapshot directly since the test assembly has InternalsVisibleTo). Use the `RedisKeyPrefix` to read back keys via `RedisMultiplexer.GetDatabase()`.

    Facts to cover (map to L2-PROJECT-01/03/04/05):
    - `Upsert_Writes_Three_Keyspaces`: after Upsert, `GET {prefix}{wf}` and `GET {prefix}{wf}:{step}` and `GET {prefix}{proc}` all return non-empty values; deserialize each to its projection record and assert shapes (RootProjection_Shape: entryStepIds/cron/jobId/liveness/correlationId with correlationId == the passed value; StepProjection_Shape: entryCondition as int, nextStepIds full list / [] for terminal; ProcessorProjection: inputDefinition/outputDefinition field names).
    - `ProcessorProjection_Ttl`: `db.KeyTimeToLive({prefix}{proc})` returns a positive TimeSpan close to ProcessorKeyTtlDays; `db.KeyTimeToLive({prefix}{wf})` and `({prefix}{wf}:{step})` return null (no TTL on root/step — Pitfall 2).

    Do NOT add any teardown that FLUSHDBs; the per-class `RedisFixture` SCAN+DEL already sweeps the TTL'd processor keys (RESEARCH A3 / Pitfall 3 — VERIFIED, no fixture change).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RedisProjectionWriterFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `RedisProjectionWriterFacts.cs` exists with `IClassFixture<Phase8WebAppFactory>` and `[Trait("Phase","15")]`.
    - Contains a fact named `~Ttl` asserting `KeyTimeToLive` is positive for the processor key and null for the root key.
    - Every `async Task` test method passes `TestContext.Current.CancellationToken` to async calls (grep: `TestContext.Current.CancellationToken` present).
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RedisProjectionWriterFacts"` exits 0 (requires compose Redis up).
  </acceptance_criteria>
  <done>Real-Redis facts prove all 3 keyspaces, the locked camelCase shapes, and the processor-only TTL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → Redis | The writer crosses into Redis storage; payload/schema JSON it writes originates from already-validated entity rows. |
| correlationId param → root key value | correlationId echoed into stored root value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering / Injection | user-supplied payload/schema JSON written into step/processor values | mitigate | Values are serialized as JSON string members via STJ (escapes embedded quotes/control chars); payload originates from AssignmentEntity.Payload, already validated on create. No string concatenation into the key structure (RedisProjectionKeys uses Guid interpolation only). |
| T-15-05 | Tampering (CRLF/log injection) | correlationId echoed into root value | mitigate | correlationId is already CRLF-sanitized (ASCII-printable, ≤128) by CorrelationIdMiddleware.IsValid (Phase 4) before reaching HttpContext.Items; preserved unchanged here. |
| T-15-06 | DoS | connection storm from per-op multiplexers | mitigate | Inject Singleton IConnectionMultiplexer; call GetDatabase() per op, never Connect() (Pitfall 5). |
| T-15-07 | Information Disclosure | RedisConnectionException 500 body leaking connection string | mitigate (Plan 04/05) | This writer only rethrows; the Phase 4 FallbackExceptionHandler emits a generic 500 body (no connection string, no stack). Op-name attachment handled in Plan 04. |
</threat_model>

<verification>
- `dotnet build src/BaseApi.Service -c Release` exits 0 (zero warnings).
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RedisProjectionWriterFacts"` exits 0.
- `rg "IServer|\.Keys\(|\bKEYS\b" src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs` returns 0 matches (L2-PROJECT-07 — writer does not enumerate).
- `expiry:` appears exactly once in RedisProjectionWriter.cs (processor only).
</verification>

<success_criteria>
- UpsertAsync writes the 3 keyspaces in one CreateBatch pipeline (L2-PROJECT-01).
- Processor keys carry ProcessorKeyTtlDays TTL (refresh-on-write); `<=0` ⇒ no expiry (D-08).
- Root/step keys carry NO TTL.
- liveness via TimeProvider; jobId fresh per call; correlationId from param (D-01/D-05).
- Partial-failure rethrows after one MEL warning naming the workflowId (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-02-SUMMARY.md`
</output>
