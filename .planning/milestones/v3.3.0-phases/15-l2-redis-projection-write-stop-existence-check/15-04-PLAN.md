---
phase: 15-l2-redis-projection-write-stop-existence-check
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs
  - src/BaseApi.Service/Features/Orchestration/OrchestrationServiceCollectionExtensions.cs
  - src/BaseApi.Service/Features/Orchestration/OrchestrationController.cs
  - tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/StopGateFacts.cs
autonomous: true
requirements:
  - ORCH-START-01
  - ORCH-START-02
  - ORCH-START-03
  - ORCH-START-04
  - ORCH-START-05
  - ORCH-START-06
  - ORCH-START-07
  - ORCH-START-08
  - ORCH-STOP-01
  - ORCH-STOP-02
  - ORCH-STOP-03
  - ORCH-STOP-04
  - ORCH-STOP-05
  - ORCH-STOP-06
  - ORCH-STOP-07
  - OBSERV-REDIS-01
  - OBSERV-REDIS-03

must_haves:
  truths:
    - "StartAsync existence-checks all ids upfront (404 before any mutation), then per-workflow: tolerant cleanup → load L1([id]) → validate (cycle→schema-edge→payload) → UpsertAsync → dispose snapshot"
    - "Re-Start of a shrunk graph leaves no orphan per-step keys (delete-then-write)"
    - "StopAsync batch-EXISTS the root keys; any missing → 422 with full missing list and NO deletion; all exist → per-workflow cleanup → 204; repeated Stop → 422"
    - "Redis failure on Start or Stop surfaces as 500 + RFC 7807 + correlationId + offending op name"
    - "correlationId is resolved once from HttpContext.Items and passed explicitly to UpsertAsync"
  artifacts:
    - path: "src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs"
      provides: "Per-workflow Start loop + Redis-based Stop gate+cleanup (D-04/D-06/D-07)"
      contains: "foreach"
    - path: "src/BaseApi.Service/Features/Orchestration/OrchestrationController.cs"
      provides: "422/500 ProducesResponseType on Start+Stop (ORCH-START-08)"
      contains: "Status422UnprocessableEntity"
  key_links:
    - from: "OrchestrationService.StartAsync"
      to: "IRedisProjectionWriter.UpsertAsync"
      via: "per-workflow loop with explicit correlationId"
      pattern: "UpsertAsync\\(snapshot, correlationId"
    - from: "OrchestrationService.StopAsync"
      to: "Redis KeyExistsAsync + IRedisL2Cleanup"
      via: "batch EXISTS gate then per-workflow cleanup"
      pattern: "KeyExistsAsync"
---

<objective>
Restructure `OrchestrationService` into the D-07 per-workflow Start loop and the D-04/D-06 Redis-based Stop (gate + cleanup), wire the new dependencies (`IRedisL2Cleanup`, `IHttpContextAccessor`, `IConnectionMultiplexer`) through the internal-ctor DI factory, add the 422/500 `[ProducesResponseType]` attributes to the controller (ORCH-START-08), and ensure Redis-failure 500 bodies carry the offending op name (OBSERV-REDIS-03). This is the wave that connects the Plan-02 writer and Plan-03 cleanup into the live HTTP pipeline.

Purpose: Goal closure — after this wave a real Start populates the 3 keyspaces and a real Stop gates-then-cleans. The validation gate order (cycle→schema-edge→payload) and the upfront 404 existence check are preserved.
Output: 1 restructured service, 1 modified DI factory, 1 modified controller, 2 new integration-test fact classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-RESEARCH.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-02-SUMMARY.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-03-SUMMARY.md

<interfaces>
<!-- Verified from codebase this session. -->

Current OrchestrationService (src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs — verified):
- `public sealed class OrchestrationService`; INTERNAL ctor (CS0051 — internal seam types); fields
  _db, _idsValidator, _loader, _cycleDetector, _schemaEdgeValidator, _payloadConfigSchemaValidator,
  _redisProjectionWriter.
- StartAsync (lines 75-84): ExistenceCheckAsync → using snapshot = LoadL1Async(workflowIds) → 3 validators
  → UpsertAsync(snapshot, ct). REPLACE with per-workflow loop.
- StopAsync (lines 91-92): `=> ExistenceCheckAsync(...)`. REPLACE with Redis EXISTS gate + cleanup.
- ExistenceCheckAsync (lines 109-143): KEEP UNCHANGED — Postgres 404 gate; rule-validates ids then
  SELECT id WHERE id IN (...); throws NotFoundException(string.Join(", ", missing)).

Target StartAsync (D-07; PATTERNS.md lines 160-172):
```csharp
await ExistenceCheckAsync(workflowIds, ct);                                  // unchanged 404 fast-fail
var correlationId = _httpContextAccessor.HttpContext?.Items["CorrelationId"] as string ?? string.Empty;  // D-01, ONCE
foreach (var workflowId in workflowIds) {
    await _cleanup.StopCleanupAsync(workflowId, ct);                         // tolerant pre-clean
    using var snapshot = await _loader.LoadL1Async(new[] { workflowId }, ct); // PER-WORKFLOW
    _cycleDetector.Validate(snapshot);                                       // gate order LOCKED (Phase 14)
    _schemaEdgeValidator.Validate(snapshot);
    _payloadConfigSchemaValidator.Validate(snapshot);
    await _redisProjectionWriter.UpsertAsync(snapshot, correlationId, ct);
}   // snapshot disposed each iteration (using) — L1 cleanup on success AND throw
```

Target StopAsync (D-04/D-06; PATTERNS.md line 176):
- rule-validate ids (reuse _idsValidator.ValidateAndThrowAsync as ExistenceCheckAsync does at line 127; also keep the null-body guard from lines 118-124).
- var db = _multiplexer.GetDatabase(); batch KeyExistsAsync on each RedisProjectionKeys.Root(prefix, id);
  await all; collect ALL ids whose EXISTS == false (not fail-fast).
- if missing.Count > 0 → throw OrchestrationValidationException.MissingRoots(missing) (Plan 03) → 422, NO deletion.
- else foreach id → await _cleanup.StopCleanupAsync(id, ct) → controller returns 204.
- Redis failure bubbles → FallbackExceptionHandler → 500 (ORCH-STOP-07).

CorrelationId analog (AuditInterceptor.cs:40,61): _httpContextAccessor.HttpContext?.Items["CorrelationId"];
item key literal "CorrelationId" (CorrelationIdMiddleware.cs:52). Coalesce to string.Empty.

DI factory (OrchestrationServiceCollectionExtensions.cs:52-64) — add to the closure + register cleanup
(PATTERNS.md lines 186-198):
```csharp
services.AddScoped<OrchestrationService>(sp => new OrchestrationService(
    sp.GetRequiredService<BaseDbContext>(),
    sp.GetRequiredService<IValidator<IReadOnlyList<Guid>>>(),
    sp.GetRequiredService<IWorkflowGraphLoader>(),
    sp.GetRequiredService<CycleDetector>(),
    sp.GetRequiredService<SchemaEdgeValidator>(),
    sp.GetRequiredService<PayloadConfigSchemaValidator>(),
    sp.GetRequiredService<IRedisProjectionWriter>(),
    sp.GetRequiredService<IRedisL2Cleanup>(),               // NEW
    sp.GetRequiredService<IHttpContextAccessor>(),          // NEW (D-01)
    sp.GetRequiredService<IConnectionMultiplexer>()));      // NEW (Stop EXISTS gate)
services.AddScoped<IRedisL2Cleanup, RedisL2Cleanup>();      // NEW
```
IHttpContextAccessor already registered (AuditInterceptor consumes it); IConnectionMultiplexer Singleton
(RedisServiceCollectionExtensions.cs:57).

Controller (OrchestrationController.cs:43-46 / 59-62) — add to BOTH actions:
[ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status422UnprocessableEntity)]
[ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status500InternalServerError)]
(existing 204/400/404 stay; ProblemDetails + StatusCodes usings already present at lines 2-3.)

OBSERV-REDIS-03 op-name seam (RESEARCH Open Q2; FallbackExceptionHandler.cs:43-58 emits a bare 500 with no
op name). Choose the cleanest seam: catch the Redis exception in StartAsync/StopAsync and rethrow a typed
wrapper carrying the op name ("UpsertAsync"/"KeyExistsAsync"), then have a tiny handler add it to
Extensions; OR wrap with an exception whose Data["redisOp"] the FallbackExceptionHandler reads. Keep the
500 body free of connection strings/stack (Phase 4 discipline). correlationId is added by the Phase 4 customizer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DI — add cleanup + IHttpContextAccessor + IConnectionMultiplexer to the factory</name>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/OrchestrationServiceCollectionExtensions.cs (self — factory closure, lines 52-64)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md (DI factory section, lines 182-198)
  </read_first>
  <action>
    Edit `OrchestrationServiceCollectionExtensions.cs`: add the three new `sp.GetRequiredService<...>()` lines (`IRedisL2Cleanup`, `IHttpContextAccessor`, `IConnectionMultiplexer`) to the `OrchestrationService` factory closure in the SAME ORDER they will appear in the ctor (Task 2). Add `services.AddScoped<IRedisL2Cleanup, RedisL2Cleanup>();` beside the existing writer registration (line 64). Add the needed `using` for `StackExchange.Redis` (IConnectionMultiplexer) and `Microsoft.AspNetCore.Http` (IHttpContextAccessor) and the Projection namespace. Do NOT remove the existing `AddExceptionHandler<OrchestrationValidationExceptionHandler>()` (it claims the Plan-03 MissingRoots exception too).
  </action>
  <verify>
    <automated>dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - The factory closure contains `sp.GetRequiredService<IRedisL2Cleanup>()`, `sp.GetRequiredService<IHttpContextAccessor>()`, and `sp.GetRequiredService<IConnectionMultiplexer>()`.
    - `AddScoped<IRedisL2Cleanup, RedisL2Cleanup>()` is registered.
    - `dotnet build -c Release` exits 0 (ValidateOnBuild passes — all ctor deps resolvable).
  </acceptance_criteria>
  <done>DI factory supplies the three new dependencies; cleanup routine registered; container validates.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure OrchestrationService — per-workflow Start loop + Redis Stop gate/cleanup + op-name wrap</name>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs (self — StartAsync 75-84, StopAsync 91-92, ExistenceCheckAsync 109-143, ctor 50-66)
    - src/BaseApi.Core/Persistence/AuditInterceptor.cs (IHttpContextAccessor Items["CorrelationId"] read, lines 40,61)
    - src/BaseApi.Core/Middleware/CorrelationIdMiddleware.cs (item-key literal "CorrelationId", line 52)
    - src/BaseApi.Service/Features/Orchestration/OrchestrationValidationException.cs (MissingRoots factory from Plan 03)
    - src/BaseApi.Core/Exceptions/Handlers/FallbackExceptionHandler.cs (bare 500 body; op-name attach seam, lines 43-58)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md (OrchestrationService section, lines 154-178)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-RESEARCH.md (Pattern 4 lines 356-363; Pattern 5 lines 365-366; Pitfall 6 lines 437-441; D-03/D-04/D-06/D-07)
  </read_first>
  <action>
    Edit `OrchestrationService.cs`:
    1. Ctor: add `IRedisL2Cleanup cleanup`, `IHttpContextAccessor httpContextAccessor`, `IConnectionMultiplexer multiplexer` params (after `redisProjectionWriter`, matching the Task-1 factory order) with `?? throw new ArgumentNullException(nameof(x))` guards; add private readonly fields. Also inject `IOptions<RedisProjectionOptions> options` (for KeyPrefix in the Stop gate) — add it too and store `_keyPrefix = options.Value.KeyPrefix` or keep the options. Add the matching `sp.GetRequiredService<IOptions<RedisProjectionOptions>>()` to the Task-1 factory closure if you add this param (keep factory + ctor in sync).
    2. StartAsync: REPLACE the body with the D-07 per-workflow loop (see `<interfaces>` target). Keep `ExistenceCheckAsync(workflowIds, ct)` as the unchanged first line. Resolve correlationId ONCE before the loop. Per iteration: tolerant `_cleanup.StopCleanupAsync` → `using var snapshot = await _loader.LoadL1Async(new[] { workflowId }, ct)` → 3 validators in locked order → `await _redisProjectionWriter.UpsertAsync(snapshot, correlationId, ct)`.
    3. StopAsync: REPLACE the body. Keep the null-body guard + `_idsValidator.ValidateAndThrowAsync(ids, ct)` (mirror ExistenceCheckAsync lines 118-127). Then `var db = _multiplexer.GetDatabase();` batch `KeyExistsAsync(RedisProjectionKeys.Root(prefix, id))` for each id (collect tasks, await all), collect ALL ids where EXISTS is false. If `missing.Count > 0` → `throw OrchestrationValidationException.MissingRoots(missing)` (422, NO deletion). Else `foreach (var id in workflowIds) await _cleanup.StopCleanupAsync(id, ct);`.
    4. OBSERV-REDIS-03: wrap the Redis-touching calls so the offending op name reaches the 500 body. Cleanest seam (your choice): catch a `RedisException`/`RedisConnectionException` around `UpsertAsync` (op "UpsertAsync") and around the Stop EXISTS batch (op "KeyExistsAsync"), set `ex.Data["redisOp"] = "..."` and rethrow; then in a SEPARATE tiny edit to `FallbackExceptionHandler` (Plan owns OrchestrationService only — if touching Core is undesirable, instead add the op to a typed exception the existing OrchestrationValidationExceptionHandler ignores and a new minimal handler claims). PREFER: add `exception.Data["redisOp"]` read into `FallbackExceptionHandler` and surface it as `problem.Extensions["redisOp"]` — this is a 2-line Core edit; if you make it, ADD `src/BaseApi.Core/Exceptions/Handlers/FallbackExceptionHandler.cs` to files_modified in the SUMMARY and note the cross-project touch. Keep the 500 detail generic (no connection string/stack).

    Update the class-level XML doc StartAsync/StopAsync summaries to describe the new per-workflow loop and Redis gate/cleanup (the current docs say "Phase 15 swaps StopAsync's body to a Redis EXISTS check" — make it accurate).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~StartLoopFacts|FullyQualifiedName~StopGateFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `OrchestrationService.cs` StartAsync contains a `foreach` over `workflowIds`, `_cleanup.StopCleanupAsync`, `LoadL1Async(new[] { workflowId }`, and `UpsertAsync(snapshot, correlationId`.
    - StopAsync contains `KeyExistsAsync`, collects all missing (not fail-fast), and `MissingRoots`.
    - The validator order in the loop is cycle → schemaEdge → payloadConfig (grep order preserved).
    - `rg "ExistenceCheckAsync" src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs` still present and unchanged in body (404 gate kept).
    - `dotnet test --filter "FullyQualifiedName~StartLoopFacts|FullyQualifiedName~StopGateFacts"` exits 0.
  </acceptance_criteria>
  <done>Start is a per-workflow stop-then-build loop; Stop is a Redis EXISTS gate + cleanup; 404/422/500 paths correct; correlationId resolved once and passed down; op name in 500 body.</done>
</task>

<task type="auto">
  <name>Task 3: Controller ProducesResponseType + Start/Stop integration facts</name>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/OrchestrationController.cs (self — Start 43-46, Stop 59-62)
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (RedisMultiplexer/RedisKeyPrefix; ConfigureWebHost, lines 112-204)
    - tests/BaseApi.Tests/Observability/SchemasLogsE2ETests.cs (TestContext.Current.CancellationToken, per-test X-Correlation-Id, lines 46-65)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-VALIDATION.md (Req→Test map, lines 562-573)
  </read_first>
  <action>
    1. Edit `OrchestrationController.cs` — add to BOTH `Start` and `Stop` actions:
    `[ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status422UnprocessableEntity)]`
    `[ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status500InternalServerError)]`
    (no body change; existing attributes stay).

    2. Create `tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs` and `StopGateFacts.cs` — xUnit.v3 integration, `[Trait("Phase","15")]`, `IClassFixture<Phase8WebAppFactory>`. Seed Postgres via the entity POST endpoints (real workflows/steps/processors/schemas/assignments) so a Start has a valid graph. Read back L2 via `RedisMultiplexer` + `RedisKeyPrefix`. Pass `X-Correlation-Id` header; every async test passes `TestContext.Current.CancellationToken`.

    StartLoopFacts:
    - `Start_Returns204` (ORCH-START-02): valid graph → POST start → 204; root key correlationId == sent header (ORCH-START-07).
    - `ReStart_Removes_Orphan_Step` (ORCH-START-05): Start, then shrink the workflow's step graph in Postgres (remove a NextStep edge / a step), re-Start, then assert the now-orphaned per-step key is GONE (delete-then-write).
    - `Start_RedisDown_500` (ORCH-START-04): use the existing dead-Redis factory path (HealthDeadRedisFixture / skipRedisFixture ctor) → POST start → 500 + RFC 7807 body carries correlationId + `redisOp` == "UpsertAsync"; assert no connection string in the body.

    StopGateFacts:
    - `Stop_AllExist_204` (ORCH-STOP-02): Start then Stop → 204; assert root+step keys gone, processor key remains (ORCH-STOP-04 rev).
    - `Stop_Missing_422_NoDelete` (ORCH-STOP-03): Stop ids with at least one never-Started → 422; body lists ALL missing ids; assert NOTHING was deleted for the existing ids.
    - `Stop_Repeat_422` (ORCH-STOP-06 rev): Start, Stop (204), Stop again → 422 (non-idempotent).
    - `Stop_RedisDown_500` (ORCH-STOP-07): dead-Redis → 500 + RFC 7807 + correlationId + `redisOp` == "KeyExistsAsync".

    Do NOT FLUSHDB; per-class RedisFixture SCAN+DEL handles teardown.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~StartLoopFacts|FullyQualifiedName~StopGateFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `OrchestrationController.cs` contains `Status422UnprocessableEntity` and `Status500InternalServerError` on both actions (grep: each status appears ≥2 times).
    - `StartLoopFacts.cs` contains facts `~Start_Returns204`, `~ReStart_Removes_Orphan_Step`, `~Start_RedisDown_500`.
    - `StopGateFacts.cs` contains facts `~Stop_AllExist_204`, `~Stop_Missing_422_NoDelete`, `~Stop_Repeat_422`, `~Stop_RedisDown_500`.
    - The dead-Redis facts assert the body carries `redisOp` and does NOT contain a connection-string substring.
    - `dotnet test --filter "FullyQualifiedName~StartLoopFacts|FullyQualifiedName~StopGateFacts"` exits 0.
  </acceptance_criteria>
  <done>Controller advertises 422/500; Start loop (204, delete-then-write, Redis-down 500) and Stop gate (204, 422 missing, repeat 422, Redis-down 500) all proven against real Postgres + Redis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /orchestration/start,/stop | untrusted List<Guid> body; X-Correlation-Id header. |
| service → Postgres (Start existence) / Redis (Stop existence + cleanup, Start write) | |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Spoofing/Tampering | malformed/duplicate/empty Guid list | mitigate | `_idsValidator.ValidateAndThrowAsync` (WorkflowIdsValidator) + null-body guard run before any Redis/Postgres touch on both Start and Stop (V5 input validation). |
| T-15-13 | Information Disclosure | RedisConnectionException 500 leaking connection string / stack | mitigate | FallbackExceptionHandler emits generic detail; only `redisOp` (op name) + correlationId added to Extensions; dead-Redis facts assert no connection-string substring (OBSERV-REDIS-03). |
| T-15-14 | Tampering (log injection) | X-Correlation-Id echoed into error body + root value | mitigate | Already ASCII-sanitized (≤128) at CorrelationIdMiddleware before HttpContext.Items; read-only here. |
| T-15-15 | Repudiation/Observability gap | Redis ops not correlated in logs | mitigate | correlationId flows via AsyncLocal MEL scope (Phase 4); no new OTel-Redis instrumentation (OBSERV-REDIS-01 — negative-grep in Plan 05). |
| T-15-16 | DoS | unbounded Start/Stop work | accept | Bounded by the requested workflowId list size + each workflow's reachable graph; v1 has no distributed lock (ORCH-START-06 last-write-wins, accepted partial-state). |
</threat_model>

<verification>
- `dotnet build src/BaseApi.Service -c Release` exits 0 (zero warnings).
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Orchestration"` exits 0 (full orchestration slice).
- `rg "IServer|\.Keys\(|\bKEYS\b" src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs` returns 0 matches (Stop gate uses KeyExistsAsync, not enumeration — L2-PROJECT-07).
- ExistenceCheckAsync body unchanged (Start 404 gate preserved).
</verification>

<success_criteria>
- StartAsync is the D-07 per-workflow stop-then-build loop with the upfront 404 gate and locked validator order (ORCH-START-01..08).
- StopAsync is the D-04/D-06 Redis EXISTS gate (422 full missing list, NO deletion) then per-workflow cleanup → 204; repeat → 422 (ORCH-STOP-01..07).
- Redis failures → 500 + RFC 7807 + correlationId + op name (ORCH-START-04 / ORCH-STOP-07 / OBSERV-REDIS-03).
- correlationId resolved once and passed explicitly to the writer (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-04-SUMMARY.md`
</output>
