---
phase: 15-l2-redis-projection-write-stop-existence-check
plan: 05
type: execute
wave: 4
depends_on: ["15-04"]
files_modified:
  - tests/BaseApi.Tests/Observability/OrchestrationLogsE2ETests.cs
  - tests/BaseApi.Tests/Features/Orchestration/RedisDisciplineGuardFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/ValidationOrderFacts.cs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - OBSERV-REDIS-01
  - OBSERV-REDIS-02
  - OBSERV-REDIS-04

must_haves:
  truths:
    - "A real Start emits a Redis-op MEL log doc carrying the inbound X-Correlation-Id, retrievable from Elasticsearch (OBSERV-REDIS-02)"
    - "No OpenTelemetry.Instrumentation.StackExchangeRedis reference anywhere in the solution (OBSERV-REDIS-01)"
    - "No KEYS / IServer.Keys() in production code; no Mapperly-for-JSON in the Projection folder (L2-PROJECT-07/06)"
    - "Phase 14 ValidationOrderFacts reflect per-workflow first-failure semantics (CONTEXT amendment)"
    - "REQUIREMENTS.md and ROADMAP.md Phase 15 text match the CONTEXT amendments (Stop deletes, jobId NewGuid, TTL, non-idempotent)"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/OrchestrationLogsE2ETests.cs"
      provides: "OBSERV-REDIS-02 logs round-trip E2E"
      contains: "X-Correlation-Id"
    - path: "tests/BaseApi.Tests/Features/Orchestration/RedisDisciplineGuardFacts.cs"
      provides: "Negative-grep / reference guards (OBSERV-REDIS-01, L2-PROJECT-06/07)"
      contains: "StackExchangeRedis"
  key_links:
    - from: "OrchestrationLogsE2ETests"
      to: "Elasticsearch"
      via: "PollEsForLog on correlationId after a Start"
      pattern: "PollEsForLog"
---

<objective>
Close the observability + discipline + documentation surface: an E2E test proving Redis-op logs carry the X-Correlation-Id through to Elasticsearch (OBSERV-REDIS-02), reference/negative guards for the forbidden patterns (OBSERV-REDIS-01 no OTel-Redis package; L2-PROJECT-07 no KEYS/IServer.Keys; L2-PROJECT-06 no Mapperly-for-JSON), the Phase 14 `ValidationOrderFacts` amendment to per-workflow first-failure semantics, and the REQUIREMENTS.md / ROADMAP.md Phase 15 doc reconciliation to match the CONTEXT amendments.

Purpose: This wave makes the amendments authoritative in the durable docs (so the ROADMAP/REQUIREMENTS no longer contradict the shipped behavior) and proves the cross-cutting observability + forbidden-pattern invariants. OBSERV-REDIS-04 (Redis metrics) is explicitly deferred — documented, not implemented.
Output: 1 E2E test, 1 guard-fact class, 1 amended Phase 14 fact class, 2 amended planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-RESEARCH.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-04-SUMMARY.md

<interfaces>
<!-- Verified from codebase this session. -->

E2E analog (tests/BaseApi.Tests/Observability/SchemasLogsE2ETests.cs:34-95) — VERBATIM structure to mirror:
- [Trait("Phase","15")] [Trait("Category","E2E")] [Collection("Observability")]
- IClassFixture<Phase11WebAppFactory> (the E2E-wired factory with OTLP→collector→ES)
- per-test unique corrId = $"{Guid.NewGuid():N}"; client.DefaultRequestHeaders.Add("X-Correlation-Id", corrId)
- ElasticsearchTestClient().PollEsForLog(queryBody, timeoutMs: 30_000, ct) with
  EsIndexNames.CorrelationIdFieldPath term query
- Assert resp echo header; do NOT assert on version string (CHECKER WARNING #7).
- Every async test: TestContext.Current.CancellationToken.

Phase 14 facts to amend (tests/BaseApi.Tests/Features/Orchestration/ValidationOrderFacts.cs) — currently
assert "first failing gate across the whole batch"; CONTEXT amendment: "first failing gate within the
first failing workflow, in iteration order." Read the file fully before editing; only the order/scope
semantics change (the per-workflow loop in Plan 04 means workflow A is fully processed — clean+load+
validate+project — before workflow B is even loaded).

Doc edits (CONTEXT <amendments> lines 98-115 are authoritative):
- REQUIREMENTS.md: ORCH-STOP-04 (Stop DELETES root+step, never processor), ORCH-STOP-06 (non-idempotent,
  repeat → 422), ORCH-START-05 (delete-then-write), L2-PROJECT-03 (jobId = Guid.NewGuid()), L2-PROJECT-01/-05
  (processor TTL), L2-PROJECT-07 (GET-and-follow traversal; KEYS still forbidden).
- ROADMAP.md Phase 15 SC3 (currently "performs NO DELETE ... idempotent") and SC4/SC5 — rewrite to match the
  shipped Stop-deletes / non-idempotent-422 / TTL behavior; ALSO flag Phase 16 SC2/SC5 are now inverted
  (post-Stop root+step gone, processor intact) — Phase 16 owns its own SC edit, but add a note.

Forbidden-package check: Directory.Packages.props + all csproj. OBSERV-REDIS-01 forbids
OpenTelemetry.Instrumentation.StackExchangeRedis (any version).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrchestrationLogsE2ETests (OBSERV-REDIS-02) + Redis-discipline guard facts (OBSERV-REDIS-01, L2-PROJECT-06/07)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/SchemasLogsE2ETests.cs (full file — mirror structure)
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (or Phase11WebAppFactory for E2E — confirm which composes OTLP→ES)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-VALIDATION.md (OBSERV-REDIS-02 E2E + negative-grep rows, lines 560-561, 572, 587)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-PATTERNS.md (E2E + Tests sections, lines 243-245)
  </read_first>
  <action>
    1. Create `tests/BaseApi.Tests/Observability/OrchestrationLogsE2ETests.cs` mirroring `SchemasLogsE2ETests` VERBATIM in structure (traits, collection, fixture, per-test corrId, PollEsForLog). Swap the Schema POST for an orchestration Start: seed a valid workflow graph (via entity POSTs against the same factory, or a minimal known-good graph), POST `/api/v1/orchestration/start` with the unique `X-Correlation-Id`, expect 204, then poll ES for a log doc whose correlationId field matches and that corresponds to the Redis-write request lifecycle. Assert the hit is non-null and the correlationId flowed (OBSERV-REDIS-02). Do NOT assert on the version string (CHECKER WARNING #7). Use `TestContext.Current.CancellationToken`.

    2. Create `tests/BaseApi.Tests/Features/Orchestration/RedisDisciplineGuardFacts.cs` — xUnit.v3 `[Fact]` class, `[Trait("Phase","15")]`, no fixture. These are source/reference guards (they read the solution's own files / loaded assemblies — pure, fast):
    - `No_OtelRedis_Package_Referenced` (OBSERV-REDIS-01): read `Directory.Packages.props` (resolve repo-root path relative to AppContext.BaseDirectory or a known relative climb) and assert it does NOT contain `OpenTelemetry.Instrumentation.StackExchangeRedis`. Optionally also assert no loaded assembly with that name.
    - `No_Keys_Enumeration_In_Projection` (L2-PROJECT-07): read all `.cs` files under `src/BaseApi.Service/Features/Orchestration/` and assert none contains `IServer.Keys(` or a bare `.Keys(` Redis enumeration call or the `KEYS` command literal. (Account for the legitimate `Dictionary.Keys` property — match `IServer` / `server.Keys` / `.KeysAsync(` patterns, not `.Keys` property access.)
    - `No_Mapperly_For_Json_In_Projection` (L2-PROJECT-06): assert no `.cs` under `Features/Orchestration/Projection/` references `Mapperly`/`[Mapper]`/`ToRead(` for JSON assembly.
    Prefer file-content assertions over reflection where the grep is unambiguous; keep paths robust (compute repo root once).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RedisDisciplineGuardFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `OrchestrationLogsE2ETests.cs` exists with `[Trait("Category","E2E")]`, `[Collection("Observability")]`, `X-Correlation-Id`, and `PollEsForLog`.
    - `RedisDisciplineGuardFacts.cs` contains facts `~No_OtelRedis_Package_Referenced`, `~No_Keys_Enumeration_In_Projection`, `~No_Mapperly_For_Json_In_Projection`.
    - `rg "OpenTelemetry.Instrumentation.StackExchangeRedis" Directory.Packages.props` returns 0 matches (confirms the guard's premise).
    - `dotnet test --filter "FullyQualifiedName~RedisDisciplineGuardFacts"` exits 0 (E2E test runs in the full suite under the Observability collection).
  </acceptance_criteria>
  <done>OBSERV-REDIS-02 logs round-trip proven; forbidden-pattern guards green (no OTel-Redis package, no KEYS/IServer enumeration, no Mapperly-for-JSON).</done>
</task>

<task type="auto">
  <name>Task 2: Amend Phase 14 ValidationOrderFacts to per-workflow first-failure semantics</name>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/ValidationOrderFacts.cs (FULL file — current batch-scope assertions)
    - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs (the Plan-04 per-workflow loop — the new behavior these facts must match)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md (amendments: Phase 14 ValidationOrderFacts, line 112)
  </read_first>
  <action>
    Read `ValidationOrderFacts.cs` fully. The Plan-04 change makes Start a per-workflow loop: for a multi-workflow request, workflow A is fully processed (clean → load → validate → project) BEFORE workflow B is loaded. The first-failure semantics therefore change from "first failing gate across the whole batch" to "first failing gate within the FIRST failing workflow, in iteration order."

    Update the affected facts so they assert: given a request `[A, B]` where A is valid and B has a cycle, A is projected (its L2 keys exist) and B fails with 422 (partial-state across workflows is ACCEPTED — D-07). Given a request `[A, B]` where A itself has BOTH a cycle and a schema-edge problem, the cycle gate fires first (gate order within a workflow unchanged: cycle → schema-edge → payload). Do NOT change the within-workflow gate ORDER assertions (still Phase 14's locked order) — only the cross-workflow/batch SCOPE assertions. If any fact's premise no longer holds (e.g. it asserted no keys written for ANY workflow when the FIRST failed), invert it to the per-workflow partial-state expectation.

    Keep all async tests passing `TestContext.Current.CancellationToken`. If a fact is genuinely obsolete (tested a batch-snapshot behavior that no longer exists), mark it `[Fact(Skip="superseded by Phase 15 per-workflow loop — see 15-CONTEXT amendment")]` rather than deleting, and note it in the SUMMARY.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~ValidationOrderFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `ValidationOrderFacts.cs` reflects per-workflow first-failure semantics (a comment or assertion references per-workflow / first-failing-workflow scope).
    - Within-workflow gate order (cycle → schema-edge → payload) assertions are preserved.
    - `dotnet test --filter "FullyQualifiedName~ValidationOrderFacts"` exits 0.
  </acceptance_criteria>
  <done>Phase 14 order facts match the Phase 15 per-workflow loop; cross-workflow partial-state is asserted; within-workflow gate order unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md + ROADMAP.md Phase 15 to the CONTEXT amendments</name>
  <read_first>
    - .planning/REQUIREMENTS.md (L2-PROJECT-01/03/05/07, ORCH-START-05, ORCH-STOP-04/06 line items)
    - .planning/ROADMAP.md (Phase 15 SC1-SC5 + Phase 16 SC2/SC5 note, lines 96-120)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md (amendments, lines 98-115 — AUTHORITATIVE)
  </read_first>
  <action>
    Edit the durable docs so they no longer contradict the shipped behavior (CONTEXT `<amendments>` is authoritative):

    REQUIREMENTS.md — update the line-item text for:
    - ORCH-STOP-04: Stop DELETES the root + per-step keys (never processor keys); still no Postgres.
    - ORCH-STOP-06: Stop mutates (deletes); repeated Stop → 422 (non-idempotent).
    - ORCH-START-05: idempotency is delete-then-write (per-workflow pre-clean), fixing orphaned per-step keys on graph shrink.
    - L2-PROJECT-03: jobId = Guid.NewGuid() per Start (was Guid.Empty); correlationId field name unchanged; liveness.status stays "Pending".
    - L2-PROJECT-01 / L2-PROJECT-05: processor-key writes carry a TTL (new RedisProjectionOptions.ProcessorKeyTtlDays, default 100); root/step no TTL.
    - L2-PROJECT-07: Stop uses targeted GET-and-follow traversal; KEYS / IServer.Keys() remain forbidden (traversal is by GET, not enumeration).
    Keep the requirement IDs and table structure; only the descriptive text changes. Add a short "(amended Phase 15, 2026-05-29)" marker on each edited line.

    ROADMAP.md Phase 15 — rewrite SC3 (currently "Redis-based existence-check ... performs NO DELETE ... idempotent") to the shipped behavior: Stop EXISTS-gates the root keys (422 + full missing list when any absent, NO deletion on the 422 path), and on all-exist runs the GET-and-follow cleanup (deletes root + per-step keys, NEVER processor keys) → 204; repeated Stop → 422. Update SC1 jobId note if it implies Guid.Empty. Update SC5 to reflect GET-and-follow (not enumeration) while keeping the KEYS/IServer.Keys forbidden invariant. Add a one-line note under Phase 16 that SC2/SC5 are now inverted (post-Stop root+step gone, processor intact) — Phase 16 will adjust its own facts.

    Do NOT touch the `**Requirements:**` ID lists or Phase 16 SC bodies beyond the note (Phase 16 owns those).
  </action>
  <verify>
    <automated>rg "Guid.Empty" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md ORCH-STOP-04 text says Stop deletes root + per-step (never processor); ORCH-STOP-06 says non-idempotent / repeat → 422; L2-PROJECT-03 says `Guid.NewGuid()`.
    - `rg "Guid.Empty" .planning/REQUIREMENTS.md` does NOT return the L2-PROJECT-03 jobId line as Guid.Empty (the amended line uses Guid.NewGuid()).
    - ROADMAP.md Phase 15 SC3 no longer contains "NO DELETE" / "idempotent" for Stop; references the cleanup + 422-non-idempotent behavior.
    - REQUIREMENTS.md L2-PROJECT-01/-05 mention the processor TTL (ProcessorKeyTtlDays).
  </acceptance_criteria>
  <done>REQUIREMENTS.md and ROADMAP.md Phase 15 text match the shipped Stop-deletes / non-idempotent / TTL / jobId-NewGuid behavior; Phase 16 inversion flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → OTLP → collector → Elasticsearch | log docs (with correlationId) cross into the observability backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-17 | Repudiation | Redis ops not traceable to a request | mitigate | OBSERV-REDIS-02 E2E proves correlationId reaches ES via the MEL AsyncLocal scope; no action-at-a-distance. |
| T-15-18 | Tampering | accidental re-introduction of OTel-Redis tracing (dropped/duplicate spans; Phase 11 D-03 regression) | mitigate | `No_OtelRedis_Package_Referenced` guard fact (OBSERV-REDIS-01); negative-grep on Directory.Packages.props. |
| T-15-19 | DoS | re-introduction of KEYS/IServer.Keys() enumeration | mitigate | `No_Keys_Enumeration_In_Projection` guard fact (L2-PROJECT-07). |
| T-15-20 | Information Disclosure | correlationId in ES log doc | accept | correlationId is a non-secret request identifier, ASCII-sanitized; intended for traceability. |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RedisDisciplineGuardFacts|FullyQualifiedName~ValidationOrderFacts"` exits 0.
- Full suite (`dotnet test`) green with the Observability E2E included (requires compose stack: Postgres, Redis, ES, collector).
- `rg "OpenTelemetry.Instrumentation.StackExchangeRedis" .` returns 0 matches across the solution (OBSERV-REDIS-01).
- REQUIREMENTS.md / ROADMAP.md Phase 15 text matches CONTEXT amendments.
</verification>

<success_criteria>
- OBSERV-REDIS-02 logs round-trip E2E green (correlationId reaches ES after a Start).
- OBSERV-REDIS-01 / L2-PROJECT-06 / L2-PROJECT-07 forbidden-pattern guards green.
- OBSERV-REDIS-04 documented as deferred (no implementation).
- Phase 14 ValidationOrderFacts match per-workflow first-failure semantics.
- REQUIREMENTS.md + ROADMAP.md Phase 15 reconciled to the amendments; Phase 16 inversion flagged.
</success_criteria>

<output>
After completion, create `.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-05-SUMMARY.md`
</output>
