---
phase: 16-idempotency-concurrency-l1-cleanup-3-green-closeout
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs
autonomous: true
requirements: [TEST-REDIS-07]
must_haves:
  truths:
    - "Each of the 3 HTTP-reachable validation gates (cycle, schemaEdge, payloadConfigSchema) returns 422 with an RFC 7807 problem+json body whose errors.gate names the gate"
    - "After each 422, a SCAN scoped to {prefix}{wfId}* finds ZERO L2 keys for the failed workflowId (no-write proof)"
    - "The missing-next-step gate's no-write property is explicitly resolved (Open Q2) — documented as structurally guaranteed + unit-covered, since FK-Restrict makes it not HTTP-reproducible"
    - "The Phase 14 gate facts (CycleDetectionFacts/MissingStepFacts/SchemaEdgeFacts/PayloadConfigSchemaFacts/ValidationOrderFacts) are LEFT UNTOUCHED"
  artifacts:
    - path: "tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs"
      provides: "TEST-REDIS-07 consolidated 422 + SCAN-no-write fact class"
      contains: "KeysAsync"
      min_lines: 120
  key_links:
    - from: "GateNoWriteFacts"
      to: "POST /api/v1/orchestration/start (gate-failing graph)"
      via: "HttpClient assert 422 + problem+json"
      pattern: "UnprocessableEntity"
    - from: "GateNoWriteFacts"
      to: "Redis SCAN {prefix}{wfId}*"
      via: "IServer.KeysAsync (SCAN under the hood), count == 0"
      pattern: "KeysAsync\\(pattern"
---

<objective>
TEST-REDIS-07 (D-04): ONE consolidated NEW fact class proving that when a validation gate
fails, the Start returns 422 with the RFC 7807 body AND writes ZERO L2 keys for the failed
workflowId (SCAN-confirmed). All gate triggers reuse the exact graph shapes from the Phase 14
gate facts, but those Phase 14 facts are LEFT UNTOUCHED (clean TEST-REDIS-07 ownership).

Open Q2 / A3 (RESOLVED in this plan — see Task 2): the missing-next-step gate is NOT
HTTP-reproducible (FK-Restrict on StepNextSteps blocks a dangling NextStepId through the entity
API), so its HTTP arm is intentionally omitted. Its no-write property is documented as
structurally guaranteed (it throws on the SAME throw-before-UpsertAsync code path as the other
three gates) and is unit-covered by the existing white-box MissingStepFacts.

Purpose: Prove the validation-gate chain (ASVS V5 input-validation surface) fails closed —
malformed workflow graphs 422 and leave Redis untouched. Pairs with plan 02 as a positive SCAN
control (same SCAN finds keys on a happy Start, zero on a gated Start).
Output: One new test class, 3 HTTP-driven facts + 1 documented-structural arm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-CONTEXT.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-RESEARCH.md

<interfaces>
<!-- Phase8WebAppFactory accessors (confirmed) -->
public string RedisKeyPrefix { get; }                       // line 116
public IConnectionMultiplexer RedisMultiplexer { get; }     // line 124

<!-- SCAN enumeration: IServer.KeysAsync IS SCAN under the hood (RedisFixture.DisposeAsync lines 57-66) -->
var server = _factory.RedisMultiplexer.GetServer(_factory.RedisMultiplexer.GetEndPoints()[0]);
await foreach (var key in server.KeysAsync(pattern: $"{prefix}{wfId}*", pageSize: 1000)) { ... }
// KEYS / IServer.Keys() (sync) FORBIDDEN. FLUSHDB FORBIDDEN.

<!-- RFC 7807 body shape (CycleDetectionFacts lines 127-135) -->
// status==422; Content-Type application/problem+json; root.errors.gate == "<gate>"; root.errors.offending.{...}
</interfaces>

<gate_triggers>
<!-- Copy these exact graph shapes from the Phase 14 facts (do NOT edit those files). 16-PATTERNS.md "GateNoWriteFacts.cs" -->
| Gate (errors.gate) | Trigger | Source fact (read for the exact seed) |
|--------------------|---------|----------------------------------------|
| cycle              | A->B->C forward edges, then PUT a back-edge C->A (FK needs both ends first), then Start | CycleDetectionFacts.Cycle_Returns422_WithStepChain (lines 105-143) |
| schemaEdge         | parent.OutputSchemaId=X, child.InputSchemaId=Y (distinct), acyclic parent->child | SchemaEdgeFacts.SchemaEdgeMismatch_Returns422_WithParentAndChild (lines 107-142) |
| payloadConfigSchema| Processor.ConfigSchemaId = {required:[foo],foo:string}; Assignment.Payload = {"foo":123} | PayloadConfigSchemaFacts.BadPayload_Returns422_WithAssignmentIdAndErrors (lines 129-161) |
| missingStep        | dangling NextStepId — NOT HTTP-reproducible (FK-Restrict). White-box only. | MissingStepFacts (white-box; Open Q2 — documented, not HTTP-driven) |
</gate_triggers>

<analog_files>
- tests/BaseApi.Tests/Features/Orchestration/CycleDetectionFacts.cs (cycle trigger + 422 body assert lines 105-143)
- tests/BaseApi.Tests/Features/Orchestration/SchemaEdgeFacts.cs (schemaEdge trigger lines 107-142)
- tests/BaseApi.Tests/Features/Orchestration/PayloadConfigSchemaFacts.cs (payload trigger lines 129-161)
- tests/BaseApi.Tests/Features/Orchestration/MissingStepFacts.cs (white-box missing-step — read to cite in the documented arm; do NOT modify)
- tests/BaseApi.Tests/Composition/RedisFixture.cs (SCAN style lines 57-66)
- tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs (RFC 7807 no-leak discipline lines 207-216)
</analog_files>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GateNoWriteFacts — 3 HTTP gates each (422 + SCAN-zero no-write)</name>
  <files>tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/CycleDetectionFacts.cs (the cycle trigger graph + back-edge PUT + 422 body assert, lines 105-143)
    - tests/BaseApi.Tests/Features/Orchestration/SchemaEdgeFacts.cs (schemaEdge trigger seed, lines 107-142)
    - tests/BaseApi.Tests/Features/Orchestration/PayloadConfigSchemaFacts.cs (payload-vs-config trigger seed, lines 129-161)
    - tests/BaseApi.Tests/Composition/RedisFixture.cs (KeysAsync==SCAN enumeration style, lines 57-66)
    - tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs (RFC 7807 no-leak asserts lines 207-216)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section "GateNoWriteFacts.cs" — verbatim triggers + SCAN snippet)
  </read_first>
  <behavior>
    - Fact `CycleGate_Returns422_AndWritesNoKeys`: build A->B->C then PUT back-edge C->A; POST /start
      -> 422; body is application/problem+json; status==422; errors.gate=="cycle"; then SCAN
      {prefix}{wfId}* count==0.
    - Fact `SchemaEdgeGate_Returns422_AndWritesNoKeys`: parent.OutputSchemaId=X, child.InputSchemaId=Y;
      POST /start -> 422; errors.gate=="schemaEdge"; SCAN {prefix}{wfId}* count==0.
    - Fact `PayloadConfigSchemaGate_Returns422_AndWritesNoKeys`: ConfigSchema requires foo:string,
      payload {"foo":123}; POST /start -> 422; errors.gate=="payloadConfigSchema"; SCAN count==0.
    - Each fact ALSO asserts no connection-string / exception-type leak in the body
      (DoesNotContain "localhost", DoesNotContain "RedisConnectionException") per ASVS V7.
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs`.

    Header: `[Trait("Phase", "16")] public sealed class GateNoWriteFacts :
    IClassFixture<Phase8WebAppFactory>` with the `_factory` ctor; imports mirror
    CycleDetectionFacts plus StackExchange.Redis + System.Text.Json. namespace
    BaseApi.Tests.Features.Orchestration.

    Seed helpers: copy the relevant seed chains from each source fact (CreateStepAsync /
    SetNextStepIdsAsync for cycle; SeedSchemaAsync + SeedProcessorAsync(in/out) for schemaEdge;
    SeedSchemaAsync(configSchema) + SeedProcessorAsync(configSchemaId) + SeedAssignmentAsync for
    payload). SourceHash via HashHelpers.RandomSha256Hex().

    Shared private assert helper to avoid duplication, e.g.:
      private static async Task Assert422Gate(HttpResponseMessage resp, string gate, CancellationToken ct)
      {
          Assert.Equal(HttpStatusCode.UnprocessableEntity, resp.StatusCode);
          Assert.Equal("application/problem+json", resp.Content.Headers.ContentType?.MediaType);
          var body = await resp.Content.ReadAsStringAsync(ct);
          using var docu = JsonDocument.Parse(body);
          Assert.Equal(422, docu.RootElement.GetProperty("status").GetInt32());
          Assert.Equal(gate, docu.RootElement.GetProperty("errors").GetProperty("gate").GetString());
          Assert.DoesNotContain("localhost", body);                  // ASVS V7 no-leak
          Assert.DoesNotContain("RedisConnectionException", body);
      }

    SCAN-no-write helper (mirror RedisFixture.DisposeAsync; scope to {wfId}* per Pitfall 2):
      private async Task<int> ScanKeyCount(Guid wfId)
      {
          var server = _factory.RedisMultiplexer.GetServer(_factory.RedisMultiplexer.GetEndPoints()[0]);
          var prefix = _factory.RedisKeyPrefix;
          var count = 0;
          await foreach (var _ in server.KeysAsync(pattern: $"{prefix}{wfId}*", pageSize: 1000))
              count++;
          return count;
      }

    Each fact: seed gate-failing graph -> capture wfId -> POST /api/v1/orchestration/start with
    new List<Guid>{wfId} -> await Assert422Gate(resp, "<gate>", ct) -> Assert.Equal(0, await
    ScanKeyCount(wfId)).

    Use ONLY default "D" GUID interpolation in the SCAN pattern ($"{prefix}{wfId}*"). NEVER `:N`.
    Do NOT touch CycleDetectionFacts / SchemaEdgeFacts / PayloadConfigSchemaFacts / MissingStepFacts /
    ValidationOrderFacts.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release --filter "FullyQualifiedName~GateNoWriteFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `[Trait("Phase", "16")]` and `IClassFixture<Phase8WebAppFactory>`.
    - `grep -nE "gate == \"|\"cycle\"|\"schemaEdge\"|\"payloadConfigSchema\"" GateNoWriteFacts.cs` shows all three gate names asserted.
    - `grep -n "KeysAsync(pattern" GateNoWriteFacts.cs` returns >=1; `grep -n "IServer.Keys()\|server.Keys(" GateNoWriteFacts.cs` returns ZERO (SCAN-only, no sync KEYS).
    - `grep -n "FLUSHDB\|FlushDatabase\|FlushAll" GateNoWriteFacts.cs` returns ZERO.
    - GUID-FORMAT GUARD: `grep -nE "wfId:N|:N\}" GateNoWriteFacts.cs` returns ZERO matches in SCAN/key expressions ("D" form only).
    - `grep -n "Assert.Equal(0," GateNoWriteFacts.cs` returns 3 (one no-write SCAN assert per gate).
    - `grep -n "DoesNotContain(\"localhost\"" GateNoWriteFacts.cs` present (ASVS V7 no-leak).
    - `dotnet test --filter "FullyQualifiedName~GateNoWriteFacts"` exits 0.
    - Phase 14 gate facts unchanged: `git status` lists ONLY the new GateNoWriteFacts.cs (no edit to CycleDetectionFacts/SchemaEdgeFacts/PayloadConfigSchemaFacts/MissingStepFacts/ValidationOrderFacts).
  </acceptance_criteria>
  <done>Three HTTP gate facts each assert 422 + RFC 7807 + SCAN-zero no-write; Phase 14 facts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve Open Q2 — document the missing-step no-write arm as structurally guaranteed</name>
  <files>tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/MissingStepFacts.cs (the white-box detector drive — read to confirm it is white-box, then cite it; do NOT modify)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-RESEARCH.md (Open Q2 / Assumption A3 — the FK-Restrict reasoning)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section "GateNoWriteFacts.cs" — missingStep arm note)
  </read_first>
  <action>
    DECISION (Open Q2, RESOLVED — option (a) from RESEARCH): the missing-next-step gate is NOT
    driven via HTTP. A dangling NextStepId cannot be created through the entity API because
    StepNextSteps carries an FK-Restrict, so a missing-step 422 is only reachable via the
    white-box snapshot drive that MissingStepFacts already uses — and a white-box drive cannot
    POST /start + SCAN Redis the way the other three arms do.

    Add a single explanatory XML doc comment block at the TOP of the GateNoWriteFacts class
    (above the first [Fact]) recording this decision verbatim, e.g.:

      /// <summary>
      /// TEST-REDIS-07 (D-04): each HTTP-reachable validation gate returns 422 AND writes ZERO L2 keys.
      /// Covers cycle / schemaEdge / payloadConfigSchema via the public POST /start path + SCAN-no-write proof.
      ///
      /// Open Q2 (RESOLVED): the missing-next-step gate is intentionally NOT covered here. A dangling
      /// NextStepId is not HTTP-reproducible (FK-Restrict on StepNextSteps blocks it through the entity API).
      /// Its no-write property is STRUCTURALLY GUARANTEED: missingStep throws on the SAME
      /// throw-before-UpsertAsync code path as the three gates above (the gate runs before any Redis write),
      /// and the gate itself is unit-covered by the white-box MissingStepFacts. No Redis key can be written
      /// for a workflow whose validation throws before UpsertAsync.
      /// </summary>

    Then add ONE `[Fact]` named `MissingStepGate_NoWrite_StructurallyGuaranteed` that is a
    documentation-style assertion proving the shared throw-before-write invariant WITHOUT
    fabricating an FK-forbidden HTTP path. Concretely: seed a VALID single-step workflow, POST a
    DELIBERATELY MALFORMED Start (e.g. /start with a workflowId that does not exist -> 404
    existence gate, OR rely on the documented structural argument) — pick the cheapest HTTP-real
    negative that proves "validation/existence failure -> no L2 write" and SCAN {prefix}{wfId}*
    == 0. If no clean HTTP negative exists that is distinct from the existence gate, instead make
    this a `[Fact]` that XUnit-asserts the documented invariant via a trivial reference assertion
    (e.g. Assert.True(true) is FORBIDDEN — instead assert the structural fact by checking that a
    NON-started, NON-existent workflowId has zero keys: seed nothing, pick a random Guid, SCAN
    {prefix}{randomGuid}* == 0, documenting that an un-Upserted workflow has no keys). Prefer the
    real negative; the comment block carries the structural rationale either way.

    Do NOT modify MissingStepFacts.cs. Keep this within GateNoWriteFacts.cs (single file for this plan).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release --filter "FullyQualifiedName~GateNoWriteFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Open Q2 (RESOLVED)" GateNoWriteFacts.cs` returns >=1 (the decision is recorded in-code).
    - `grep -n "throw-before-UpsertAsync\|structurally guaranteed\|STRUCTURALLY GUARANTEED" GateNoWriteFacts.cs` returns >=1.
    - `grep -n "MissingStepGate" GateNoWriteFacts.cs` returns >=1 (the documented arm exists as a named Fact).
    - `grep -n "Assert.True(true)" GateNoWriteFacts.cs` returns ZERO (no vacuous assertion).
    - MissingStepFacts.cs unchanged (`git status` does not list it).
    - `dotnet test --filter "FullyQualifiedName~GateNoWriteFacts"` exits 0.
  </acceptance_criteria>
  <done>Open Q2 resolved in-code: missing-step arm documented as structurally guaranteed + non-vacuous Fact; MissingStepFacts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/v1/orchestration/start | Pre-existing prod boundary. This plan's facts VERIFY it fails closed (422 + no Redis write) on malformed graphs. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering (malformed workflow graph) | validation gate chain (ASVS V5) | mitigate | The 3 facts prove cycle/schemaEdge/payload graphs 422 and write zero L2 keys (SCAN==0). This is the positive verification that the input-validation surface fails closed. |
| T-16-03-02 | Information Disclosure (error-body leak, ASVS V7) | RFC 7807 422 body | mitigate | Each fact asserts DoesNotContain("localhost") + DoesNotContain("RedisConnectionException") — preserves the Phase 15 no-leak discipline. |
| T-16-03-03 | Tampering (test false-positive — SCAN wrong prefix) | SCAN no-write assertion | mitigate | SCAN uses the LIVE per-class `_factory.RedisKeyPrefix` scoped `{wfId}*`; plan 02's happy-path fact is the positive control proving the same SCAN finds keys when a write DID happen. |

Test-only plan. No production code changes, no FLUSHDB, SCAN-only enumeration. The gate scripts'
`redis-cli --scan` invariant is preserved (per-class prefix + fixture teardown).
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~GateNoWriteFacts"` green.
- `dotnet test ... --filter "FullyQualifiedName~Orchestration"` stays green (Phase 14 gate facts intact).
- `git status` shows exactly one new file.
</verification>

<success_criteria>
- Three HTTP-reachable gates each assert 422 + RFC 7807 + SCAN-zero no-write; the missing-step
  arm's no-write property is resolved in-code (Open Q2) as structurally guaranteed + unit-covered;
  Phase 14 gate facts untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-03-SUMMARY.md`
</output>
