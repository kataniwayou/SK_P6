---
phase: 16-idempotency-concurrency-l1-cleanup-3-green-closeout
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - scripts/phase-16-close.ps1
  - scripts/phase-16-close.sh
autonomous: false
requirements: [TEST-REDIS-06, TEST-REDIS-07, TEST-REDIS-08, TEST-REDIS-09]
must_haves:
  truths:
    - "scripts/phase-16-close.ps1 and .sh exist as verbatim copies of phase-12-close.* with only the Phase-number header/label edited"
    - "The full test suite runs GREEN 3 consecutive times with the SAME Passed count (deterministic, > 142 baseline + new facts)"
    - "psql \\l SHA-256 BEFORE == AFTER (Phase 3 D-15 baseline 0d98b0de...0aac127)"
    - "redis-cli --scan | sort | sha256sum BEFORE == AFTER (zero residual test:cls-* keys)"
    - "Negative EF-migration assertion + HEALTH-01..05 byte-immutable guards pass (test-only phase)"
  artifacts:
    - path: "scripts/phase-16-close.ps1"
      provides: "Phase 16 dual-SHA + 3-GREEN close gate (PowerShell)"
      contains: "Phase 16"
    - path: "scripts/phase-16-close.sh"
      provides: "Phase 16 dual-SHA + 3-GREEN close gate (Bash)"
      contains: "Phase 16"
  key_links:
    - from: "scripts/phase-16-close.ps1"
      to: "dotnet test (x3) + psql \\l SHA + redis-cli --scan SHA"
      via: "verbatim copy of phase-12-close.ps1 logic"
      pattern: "Passed:"
    - from: "phase-16 gate run"
      to: "all 4 TEST-REDIS req IDs (06/07/08/09) GREEN in the suite"
      via: "the new facts from plans 02-04 are part of the 3x GREEN count"
      pattern: "Orchestration"
---

<objective>
D-06: phase-close gate. Copy the proven Phase 12-08 dual-SHA + 3-GREEN gate scripts to
`phase-16-close.{ps1,sh}` (editing only the Phase-number header/labels), then RUN the gate:
3 consecutive GREEN `dotnet test` runs with a deterministic Passed count, byte-identical
`psql \l` SHA-256 BEFORE=AFTER, and byte-identical `redis-cli --scan | sort | sha256sum`
BEFORE=AFTER. This satisfies ROADMAP Phase 16 SC3/SC4/SC5 and closes the phase.

Milestone-close (PROJECT.md "Validated" evolution, milestone archive, version bump) is OUT OF
SCOPE — that is a separate `/gsd-complete-milestone` step run AFTER this gate passes.

Purpose: Make the v3.3.0 phase-close gate pass with the new TEST-REDIS-06..09 coverage in the
suite. Non-autonomous: the gate run requires a healthy docker compose stack and a long-running
operator-observed run.
Output: Two new gate scripts + a captured GREEN gate result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-CONTEXT.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-RESEARCH.md

<analog_files>
- scripts/phase-12-close.ps1 (169 lines — the dual-SHA + 3-GREEN gate; copy whole)
- scripts/phase-12-close.sh (140 lines — bash mirror; copy whole)
</analog_files>

<gate_script_structure>
<!-- Preserve verbatim (16-PATTERNS.md "phase-16-close.*" section). ps1 line refs; sh mirrors. -->
- Pre-flight (ps1 30-37): compose health over postgres, redis, otel-collector, elasticsearch, prometheus; exit 2 if unhealthy (otel-collector exempt).
- BEFORE snapshots (ps1 39-57): psql -U postgres -lqt -> SHA-256; docker exec sk-redis redis-cli --scan | Sort-Object -CaseSensitive -> SHA-256 (sh uses LC_ALL=C sort | sha256sum — Pitfall 6, copy verbatim).
- 3-GREEN loop (ps1 59-96): dotnet test ... --configuration Release --no-build x3; extract "Passed: N"; all three runs MUST report the SAME count; exit 1 on divergence or any RED.
- AFTER snapshots + invariant asserts (ps1 98-132): beforePgHash == afterPgHash (baseline 0d98b0de...0aac127); beforeRedisHash == afterRedisHash.
- Negative EF-migration assertion (ps1 134-142): git status --porcelain src/BaseApi.Service/Migrations/ must be empty (holds trivially — test-only phase).
- HEALTH-01..05 byte-immutable (ps1 144-152): git diff on StartupCompletionService.cs + HealthServiceCollectionExtensions.cs must be empty.
</gate_script_structure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy phase-12-close.{ps1,sh} to phase-16-close.{ps1,sh} with header/label edits</name>
  <files>scripts/phase-16-close.ps1, scripts/phase-16-close.sh</files>
  <read_first>
    - scripts/phase-12-close.ps1 (whole file — the copy source; note header lines 1-18, Write-Host labels, the final STATE.md close-entry label)
    - scripts/phase-12-close.sh (whole file — the bash mirror; header lines 1-18, echo labels)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section "scripts/phase-16-close.*" — exactly what changes)
  </read_first>
  <action>
    Copy scripts/phase-12-close.ps1 -> scripts/phase-16-close.ps1 VERBATIM, then edit ONLY:
    - Header comment block (lines ~1-18): "Phase 12" -> "Phase 16"; update any phase-title text
      to "Idempotency + concurrency + L1 cleanup + 3-GREEN closeout".
    - Write-Host labels containing "Phase 12 close gate" / "Phase 12 P08" -> "Phase 16".
    - Any final STATE.md close-entry label referencing "Phase 12 P08 close entry" -> the Phase-16
      close entry label.
    Do NOT change: the pre-flight service list, the SHA-256 logic, the Sort-Object -CaseSensitive,
    the 3x dotnet test loop, the deterministic-count assertion, the psql \l baseline compare, the
    EF-migration negative assertion, or the HEALTH byte-immutable git-diff guards. The new GREEN
    total will be > 142 — the script already asserts STABILITY across 3 runs, not a fixed number,
    so no count edit is needed (A5).

    Repeat for the bash mirror: scripts/phase-12-close.sh -> scripts/phase-16-close.sh VERBATIM,
    editing ONLY the equivalent header/echo labels (keep LC_ALL=C sort | sha256sum verbatim).

    Use the Write tool (or filesystem copy + Edit) — do NOT hand-retype the script bodies; copy
    them so the dual-SHA logic is byte-faithful.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "if (-not (Test-Path scripts/phase-16-close.ps1)) { throw 'ps1 missing' }; if (-not (Test-Path scripts/phase-16-close.sh)) { throw 'sh missing' }; $p = Get-Content scripts/phase-16-close.ps1 -Raw; if ($p -notmatch 'Phase 16') { throw 'ps1 not relabeled' }; if ($p -match 'Phase 12 close gate') { throw 'stale Phase 12 label remains' }; if ($p -notmatch 'Sort-Object -CaseSensitive') { throw 'redis sort logic disturbed' }; if ($p -notmatch '0d98b0de') { throw 'psql baseline hash missing' }; 'PASS'"</automated>
  </verify>
  <acceptance_criteria>
    - Both scripts/phase-16-close.ps1 and scripts/phase-16-close.sh exist.
    - `grep -c "Phase 16" scripts/phase-16-close.ps1` >= 1; `grep -c "Phase 12 close gate" scripts/phase-16-close.ps1` == 0 (no stale label).
    - `grep -n "Sort-Object -CaseSensitive" scripts/phase-16-close.ps1` present AND `grep -n "LC_ALL=C sort" scripts/phase-16-close.sh` present (Pitfall 6 sort logic intact in both).
    - `grep -n "0d98b0de" scripts/phase-16-close.ps1` present (psql baseline compare preserved).
    - `grep -n "Migrations" scripts/phase-16-close.ps1` AND `grep -n "StartupCompletionService\|HealthServiceCollectionExtensions" scripts/phase-16-close.ps1` present (negative + byte-immutable guards preserved).
    - A structural diff of phase-16-close.ps1 vs phase-12-close.ps1 (ignoring "Phase 12"->"Phase 16" relabels) shows NO logic change.
  </acceptance_criteria>
  <done>phase-16-close.{ps1,sh} are byte-faithful copies of the Phase 12 gate with only Phase-number relabels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the Phase 16 close gate (3-GREEN + dual-SHA)</name>
  <what-built>
    The Phase 16 close gate script (scripts/phase-16-close.ps1, identical logic to the proven
    Phase 12 gate). All Phase 16 fact classes (HappyPathE2EFacts, GateNoWriteFacts,
    IdempotencyFacts, StopScanFacts) are in place from plans 02-04. This checkpoint runs the gate:
    3 consecutive GREEN full-suite runs + byte-identical psql \l and redis-cli --scan SHA-256
    BEFORE=AFTER.
  </what-built>
  <how-to-verify>
    1. Ensure the docker compose stack is healthy (postgres :5433, redis :6380/sk-redis,
       elasticsearch, prometheus, otel-collector). If a prior aborted run left residual keys,
       clear them first: `docker exec sk-redis redis-cli --scan "test:cls-*"` and delete any
       (Pitfall 7) — never FLUSHDB.
    2. Build once: `dotnet build --configuration Release`.
    3. Run the gate: `pwsh -File scripts/phase-16-close.ps1` (or `bash scripts/phase-16-close.sh`).
    4. Confirm the script prints:
       - 3 GREEN runs with the SAME "Passed: N" count (N > 142; includes the new Orchestration facts).
       - psql \l SHA-256 BEFORE == AFTER (== 0d98b0de...0aac127).
       - redis-cli --scan SHA-256 BEFORE == AFTER (zero residual test:cls-* keys).
       - EF-migration assertion clean; HEALTH byte-immutable diff empty.
       - A final success / exit 0.
    5. If a known cold-start flake trips (e.g. OTel LogLevelFilter, ConcurrencyToken race — see
       STATE.md), re-run; the gate's "consecutive is the gate" accommodation covers a re-run.
    Capture the exact Passed count and both SHA-256 values for the SUMMARY.
  </how-to-verify>
  <files>scripts/phase-16-close.ps1, scripts/phase-16-close.sh</files>
  <action>Run the gate (operator step): ensure the docker compose stack is healthy and residual test:cls-* keys are cleared (never FLUSHDB), build Release once, then run `pwsh -File scripts/phase-16-close.ps1` (or the .sh). Observe 3 consecutive GREEN full-suite runs with a deterministic Passed count, psql \l SHA-256 BEFORE=AFTER, and redis-cli --scan SHA-256 BEFORE=AFTER. Re-run on a known cold-start flake.</action>
  <verify>
    <automated>pwsh -File scripts/phase-16-close.ps1</automated>
  </verify>
  <done>The gate exits 0: 3-GREEN deterministic Passed count (> 142, includes the new Orchestration facts), psql \l SHA-256 BEFORE=AFTER (0d98b0de...0aac127), redis-cli --scan SHA-256 BEFORE=AFTER, EF-migration clean, HEALTH byte-immutable.</done>
  <resume-signal>Type "approved" with the captured Passed count + the two SHA-256 values, or describe the failure (RED test / SHA mismatch / count divergence) to debug.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script -> docker compose stack (psql / redis-cli / dotnet test) | The script reads state and runs tests; it must NOT mutate prod state destructively (no FLUSHDB, no DB drop). Copied verbatim from the audited Phase 12 gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering / Destruction (gate script clears state) | phase-16-close.* Redis snapshot step | mitigate | The script SCANs + hashes; it does NOT FLUSHDB. Verbatim copy of the Phase 12 gate preserves SCAN-only + per-class-prefix teardown (the redis-cli --scan BEFORE=AFTER invariant proves no leak/no destructive sweep). Acceptance criteria assert the sort logic and no-FLUSHDB are intact. |
| T-16-05-02 | Repudiation (non-deterministic gate result) | 3-GREEN deterministic count | mitigate | The script asserts the SAME Passed count across 3 runs; a flaky/non-deterministic suite fails the gate rather than silently passing. |
| T-16-05-03 | Tampering (hidden production change slips through) | EF-migration + HEALTH byte-immutable guards | mitigate | The copied guards fail the gate if any migration or HEALTH file changed — enforcing the "no new production behavior" phase boundary. |

Test/tooling-only plan. No new production attack surface. The gate scripts ENFORCE the v3.2.0
invariants (no FLUSHDB, SCAN-only, byte-identical psql \l, no stray migrations) rather than
introducing risk.
</threat_model>

<verification>
- scripts/phase-16-close.{ps1,sh} exist; logic identical to phase-12-close.* modulo relabels.
- Gate run: 3 consecutive GREEN with deterministic Passed count; psql \l SHA BEFORE=AFTER;
  redis-cli --scan SHA BEFORE=AFTER; EF-migration clean; HEALTH byte-immutable.
- All four TEST-REDIS-06..09 req IDs are represented in the GREEN suite (the new fact classes).
</verification>

<success_criteria>
- The v3.3.0 Phase 16 close gate passes: 3-GREEN deterministic + dual-SHA BEFORE=AFTER, with the
  new TEST-REDIS-06..09 facts in the suite. Phase 16 is ready for `/gsd-complete-milestone`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-05-SUMMARY.md`
(record the captured Passed count + the two SHA-256 values).
</output>
