---
phase: 16-idempotency-concurrency-l1-cleanup-3-green-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TEST-REDIS-09]
must_haves:
  truths:
    - "REQUIREMENTS.md TEST-REDIS-09 describes the INVERTED Stop contract (post-Stop root + per-step keys removed, processor keys intact, repeat Stop -> 422)"
    - "ROADMAP.md Phase 16 SC2 no longer claims 'Stop is verified to NOT delete any L2 keys / post-Stop SCAN matches pre-Stop'"
    - "The flagged inversion NOTE on ROADMAP.md line 125 is resolved (folded into the SC text, not left as a standing TODO)"
    - "The 'v3.2.0 invariants MUST NOT regress' block and the redis-cli --scan BEFORE=AFTER balanced-suite invariant are PRESERVED unchanged"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected TEST-REDIS-09 line item matching the 15-CONTEXT inverted Stop semantics"
      contains: "root + per-step"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 16 SC2/SC5; NOTE resolved"
      contains: "root + per-step"
  key_links:
    - from: ".planning/REQUIREMENTS.md TEST-REDIS-09"
      to: "15-CONTEXT.md <amendments> ORCH-STOP-04/-06 + D-06/D-07"
      via: "doc text matches the shipped inverted behavior"
      pattern: "processor keys (remain|intact)"
---

<objective>
Doc-first amendment (D-05, Phase 11/15 precedent): rewrite the two docs that the Phase 15
Stop-semantics inversion left stale, BEFORE any Phase 16 fact asserts against them.

- REQUIREMENTS.md TEST-REDIS-09 currently says "Stop is verified to NOT delete any L2 keys
  (post-Stop SCAN matches pre-Stop)" — this is REVERSED by 15-CONTEXT `<amendments>`
  (ORCH-STOP-04/-06) and shipped GREEN in Phase 15.
- ROADMAP.md Phase 16 SC2 carries the same stale clause, and line 125 holds a flagged NOTE
  declaring SC2/SC5 inverted-but-not-yet-edited.

This plan corrects the locked text so the facts in plans 02-04 follow corrected requirements.

Purpose: Close the only known doc/behavior drift in the v3.3.0 milestone before writing the
coverage facts. This is the FIRST plan in the phase (D-05 locked, Phase 11/15 precedent).
Output: Two edited docs; no code, no tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-CONTEXT.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md

<authoritative_source>
The INVERTED Stop contract this edit must match (15-CONTEXT.md):
- ORCH-STOP-04 (amendment): Stop DELETES the root + per-step keys (never processor keys). Still no Postgres.
- ORCH-STOP-06 (amendment): Stop mutates (deletes); repeated Stop -> 422 (non-idempotent).
- D-06: Stop = L2 cleanup — GET-and-follow traverse of the step graph, collect-then-delete
  root + reachable per-step keys; NEVER delete {processorId} keys (shared, TTL'd).
- D-08: processor keys carry a TTL (default 100 days), never explicitly deleted.
REQUIREMENTS.md already reflects this in ORCH-STOP-06 (line 133, "_amended Phase 15_"). Only
the TEST-REDIS-09 line item (151) and ROADMAP SC2/SC5 still carry the pre-inversion phrasing.
</authoritative_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md TEST-REDIS-09 to the inverted Stop contract</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 151 — the current TEST-REDIS-09 bullet; line 133 ORCH-STOP-06 for the already-amended wording to mirror in tone)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md (<amendments> ORCH-STOP-04/-06; <decisions> D-06/D-08 — authoritative new wording)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section ".planning/REQUIREMENTS.md (MODIFIED ...)" — the exact change delta)
  </read_first>
  <action>
    Replace the TEST-REDIS-09 line item at REQUIREMENTS.md line 151. The current text is:

      - [ ] **TEST-REDIS-09** — Integration facts for Stop existence-check: Stop after Start -> 204; Stop without prior Start -> 422 with missing workflowIds; Stop is verified to NOT delete any L2 keys (post-Stop `SCAN` assertion matches pre-Stop).

    Rewrite to (KEEP the leading `- [ ] **TEST-REDIS-09** —` marker and the 204 / 422-missing
    clauses; DROP the "NOT delete / post-Stop SCAN matches pre-Stop" clause; ADD the inverted
    cleanup contract from 15-CONTEXT D-06/ORCH-STOP-04/-06):

      - [ ] **TEST-REDIS-09** — Integration facts for the Stop existence-gate + L2 cleanup (inverted Phase 15 contract): Stop after Start -> 204 and the workflow's root + reachable per-step keys are removed via GET-and-follow cleanup while the shared per-processor keys remain intact (TTL'd, never deleted); Stop without a prior Start -> 422 with the FULL missing-workflowIds list and NO deletion; a repeated Stop of an already-cleaned workflow re-fails the existence gate -> 422 (non-idempotent by design). Stop does NOT touch Postgres. _(amended Phase 16, 2026-05-29 — INVERTED per 15-CONTEXT ORCH-STOP-04/-06; was "Stop is verified to NOT delete any L2 keys / post-Stop SCAN matches pre-Stop".)_

    Do NOT touch TEST-REDIS-06/07/08 (lines 148-150) — their text is already correct. Do NOT
    touch any other requirement line. Preserve exact markdown list formatting and the surrounding
    blank lines.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$t = Get-Content .planning/REQUIREMENTS.md -Raw; if ($t -match 'TEST-REDIS-09.*root \+ reachable per-step' -and $t -match 'TEST-REDIS-09.*non-idempotent' -and -not ($t -match 'TEST-REDIS-09[^\n]*verified to NOT delete')) { 'PASS' } else { throw 'TEST-REDIS-09 rewrite incomplete' }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TEST-REDIS-09" .planning/REQUIREMENTS.md` shows a single bullet containing the substring "root + reachable per-step" AND "non-idempotent" AND "per-processor keys remain intact".
    - The same bullet does NOT contain "verified to NOT delete" or "post-Stop `SCAN` assertion matches pre-Stop".
    - The bullet still contains "Stop after Start -> 204", "422", and "missing-workflowIds".
    - TEST-REDIS-06, TEST-REDIS-07, TEST-REDIS-08 bullets are byte-unchanged (`git diff` shows changes ONLY on the TEST-REDIS-09 line region).
  </acceptance_criteria>
  <done>TEST-REDIS-09 line item matches the inverted Stop contract; no other REQUIREMENTS.md line changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ROADMAP.md Phase 16 SC2/SC5 and resolve the flagged NOTE</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 115-127 — Phase 16 block: SC2 line 121, SC5 line 124, the flagged NOTE line 125, the "v3.2.0 invariants MUST NOT regress" block line 127)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md (<amendments> "Phase 16 SC2 / SC5 — REVERSED" — the authoritative new wording)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section ".planning/ROADMAP.md (MODIFIED ...)" — exact targets)
  </read_first>
  <action>
    Edit the Phase 16 block in ROADMAP.md (lines 119-127):

    (a) SC2 (line 121): the clause "Stop is verified to NOT delete any L2 keys (post-Stop `SCAN`
    matches pre-Stop)" is REVERSED. Rewrite SC2's Stop sentence to:
      "Stop-after-Start returns 204 and the workflow's root + reachable per-step keys are removed
      (GET-and-follow cleanup) while per-processor keys remain intact (TTL'd); Stop-without-prior-Start
      returns 422 with the full missing list and deletes nothing; a repeated Stop of an already-cleaned
      workflow re-fails the gate -> 422 (non-idempotent)."
    Keep the SC2 Start-idempotency + concurrent-Start clauses (those are correct).

    (b) SC5 (line 124): the `redis-cli --scan | sort | sha256sum` BEFORE=AFTER + "zero residual
    test:cls-* keys" invariant is CORRECT and orthogonal to the Stop inversion — KEEP it verbatim.
    The NOTE warned SC5 was inverted, but SC5's actual text is the scan-invariant, which still
    holds. So SC5 needs NO behavioral change; only the standing NOTE must be removed/folded.

    (c) NOTE (line 125): DELETE the entire flagged block beginning
    "> NOTE (flagged by Plan 15-05): SC2 and SC5 above are now INVERTED ...". Its content is now
    folded into the corrected SC2 above. Replace it with a single resolved-marker line:
      "> RESOLVED (Phase 16 Plan 01, 2026-05-29): SC2 above rewritten to the inverted Stop contract
      (post-Stop root + per-step removed, per-processor intact); the `redis-cli --scan` BEFORE=AFTER
      balanced-suite invariant in SC5 is unchanged and still holds."

    (d) Do NOT touch line 127 ("v3.2.0 invariants MUST NOT regress" block) — it is correct as-is
    (FLUSHDB forbidden, KEYS/IServer.Keys() forbidden, 142/142 baseline, Mapperly RMG codes).
    Do NOT touch SC1/SC3/SC4. Do NOT touch any other phase's section.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$t = Get-Content .planning/ROADMAP.md -Raw; if ($t -match 'NOTE \(flagged by Plan 15-05\)') { throw 'stale NOTE still present' }; if (-not ($t -match 'RESOLVED \(Phase 16 Plan 01')) { throw 'resolved marker missing' }; if (-not ($t -match 'FLUSHDB` is FORBIDDEN')) { throw 'v3.2.0 invariants block was disturbed' }; 'PASS'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "NOTE (flagged by Plan 15-05)" .planning/ROADMAP.md` returns NO matches (stale NOTE removed).
    - `grep -n "RESOLVED (Phase 16 Plan 01" .planning/ROADMAP.md` returns exactly one match.
    - The Phase 16 SC2 region contains "root + reachable per-step" AND "non-idempotent" AND does NOT contain "Stop is verified to NOT delete any L2 keys".
    - SC5 still contains "redis-cli --scan" and "zero residual" / "test:cls-*".
    - Line 127 block still contains "FLUSHDB" + "FORBIDDEN" + "142/142" (`git diff` shows NO change to that block).
    - `git diff .planning/ROADMAP.md` touches only the Phase 16 block (lines ~119-125).
  </acceptance_criteria>
  <done>Phase 16 SC2 inverted; NOTE resolved; SC5 + invariants block unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits two planning markdown docs only. No code, no runtime, no input handling, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering (doc/spec drift) | REQUIREMENTS.md / ROADMAP.md | mitigate | Edit the docs to match the SHIPPED inverted behavior (15-CONTEXT), eliminating spec-vs-code drift that could mislead future security reviews. Verified by grep acceptance criteria. |

This is a docs-only plan with NO production attack surface. No ASVS control is exercised or
weakened. The only genuine concern is spec accuracy (above), which the edit fixes.
</threat_model>

<verification>
- `git diff` shows changes confined to .planning/REQUIREMENTS.md (TEST-REDIS-09 region) and
  .planning/ROADMAP.md (Phase 16 block).
- Both files remain valid markdown (no broken list nesting).
- No `src/` or `tests/` file touched (this is plan #1 — docs precede facts).
</verification>

<success_criteria>
- TEST-REDIS-09 in REQUIREMENTS.md and SC2 in ROADMAP.md both describe the inverted Stop
  contract (root + per-step removed, processor intact, repeat -> 422).
- The flagged inversion NOTE is resolved.
- SC5 scan-invariant and the v3.2.0 invariants block are preserved unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-01-SUMMARY.md`
</output>
