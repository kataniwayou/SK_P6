---
phase: 16-idempotency-concurrency-l1-cleanup-3-green-closeout
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs
autonomous: true
requirements: [TEST-REDIS-06]
must_haves:
  truths:
    - "A full HTTP POST /api/v1/orchestration/start (valid multi-step graph) returns 204"
    - "After the 204, all 3 L2 keyspaces (root, per-step, per-processor) are GET-able and deserialize via System.Text.Json into the typed projection records"
    - "The root record's correlationId equals the sent X-Correlation-Id header and jobId is a non-empty Guid"
    - "The fact reads keys via _factory.RedisKeyPrefix in default 'D' GUID form, never the :N form"
  artifacts:
    - path: "tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs"
      provides: "TEST-REDIS-06 full-HTTP happy-path 3-keyspace round-trip fact"
      contains: "JsonSerializer.Deserialize<WorkflowRootProjection>"
      min_lines: 90
  key_links:
    - from: "HappyPathE2EFacts"
      to: "POST /api/v1/orchestration/start"
      via: "HttpClient.SendAsync with X-Correlation-Id header"
      pattern: "orchestration/start"
    - from: "HappyPathE2EFacts"
      to: "Redis L2 (root/step/processor keys)"
      via: "_factory.RedisMultiplexer.GetDatabase().StringGetAsync"
      pattern: "StringGetAsync\\(\\$\"\\{prefix\\}"
---

<objective>
TEST-REDIS-06 (D-03): a NEW dedicated full-HTTP fact that drives the real
HTTP -> OrchestrationService -> Redis path and asserts all three L2 keyspaces via
System.Text.Json round-trip deserialization against the locked projection records.

Existing `RedisProjectionWriterFacts` exercise the writer in isolation (calling UpsertAsync
directly); this fact instead POSTs to `/api/v1/orchestration/start` so the per-workflow Start
loop, the `X-Correlation-Id` plumbing, and the full HTTP path are all exercised end-to-end.

Purpose: Prove the happy path produces a structurally complete, round-trippable 3-keyspace
projection through the real public API. Also serves as the POSITIVE SCAN control referenced by
plan 03 (the same keys ARE findable, proving the no-write SCAN isn't always-empty).
Output: One new test class, ~1-2 facts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-CONTEXT.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-RESEARCH.md

<interfaces>
<!-- Confirmed accessor surface — Phase8WebAppFactory.cs (read this session) -->
public class Phase8WebAppFactory : WebAppFactory, IAsyncLifetime
{
    public string RedisKeyPrefix { get; }            // line 116 — "test:cls-{Guid:N}:" (the PREFIX uses :N; keys appended to it use default "D")
    public IConnectionMultiplexer RedisMultiplexer { get; }  // line 124
    public string RedisConnectionString { get; }     // line 112
}

<!-- Projection records (internal, visible via InternalsVisibleTo("BaseApi.Tests")) — 16-PATTERNS.md "Projection record shapes" -->
WorkflowRootProjection(List<Guid> EntryStepIds, string? Cron, Guid JobId, LivenessProjection Liveness, string CorrelationId)
    // JSON: entryStepIds / cron / jobId / liveness / correlationId
StepProjection(StepEntryCondition EntryCondition, Guid ProcessorId, string Payload, List<Guid> NextStepIds)
    // JSON: entryCondition (INT — Always == 4, NO string-enum converter) / processorId / payload / nextStepIds
ProcessorProjection(string? InputDefinition, string? OutputDefinition, LivenessProjection Liveness)
    // JSON: inputDefinition / outputDefinition / liveness  (NOT definitionIn/definitionOut)
LivenessProjection(DateTime Timestamp, int Interval, string Status)
    // JSON: timestamp / interval / status — Status default "Pending"

<!-- Seed-helper ctor signatures (exact) — 16-PATTERNS.md "HTTP seed helpers" -->
ProcessorCreateDto(Name, Version, Description, SourceHash, InputSchemaId, OutputSchemaId, ConfigSchemaId)
StepCreateDto(Name, Version, Description, ProcessorId, NextStepIds, EntryCondition)
AssignmentCreateDto(Name, Version, Description, StepId, Payload)
WorkflowCreateDto(Name, Version, Description, EntryStepIds, AssignmentIds, CronExpression)
HashHelpers.RandomSha256Hex()   // unique processor SourceHash per seed
</interfaces>

<analog_files>
<!-- Read these verbatim before writing — they are the copy-source. -->
- tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs (lines 1-37 imports/header; 41-101 seed helpers; 105-134 seed->Start->read pattern with X-Correlation-Id)
- tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs (lines 40-115 RICH seed chain WITH schemas; 152-199 the 3-keyspace deserialize asserts; 274-278 CanonicalJson helper for payload compare)
- tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (the fixture)
- src/BaseApi.Service/Features/Orchestration/Projection/*Projection.cs (the 4 records)
</analog_files>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HappyPathE2EFacts — full-HTTP Start + 3-keyspace round-trip</name>
  <files>tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs (imports + class header lines 1-37; seed helpers 41-101; Start_Returns204 seed->POST->read lines 105-134 — X-Correlation-Id header plumbing is load-bearing)
    - tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs (RICH seed chain with schemas lines 40-115; 3-keyspace deserialize asserts lines 152-199; CanonicalJson lines 274-278)
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (RedisMultiplexer / RedisKeyPrefix accessors lines 108-127)
    - src/BaseApi.Service/Features/Orchestration/Projection/WorkflowRootProjection.cs, StepProjection.cs, ProcessorProjection.cs, LivenessProjection.cs (the [JsonPropertyName] pins + the StepEntryCondition int-enum)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section "HappyPathE2EFacts.cs" — verbatim copy/change delta)
  </read_first>
  <behavior>
    - Fact `Start_HappyPath_WritesAllThreeKeyspaces`: seed a valid graph (>=1 processor with
      input/output schema defs, >=1 step referencing it, 1 workflow with that step as entryStep) ->
      POST /start with an X-Correlation-Id header -> 204.
    - Root key {prefix}{wfId}: deserializes to WorkflowRootProjection; EntryStepIds contains the
      seeded stepId; JobId != Guid.Empty; CorrelationId == the sent header; Liveness.Status == "Pending".
    - Per-step key {prefix}{wfId}:{stepId}: deserializes to StepProjection; ProcessorId == seeded
      procId; NextStepIds is [] for a terminal step (not null); EntryCondition is the int-backed enum
      (StepEntryCondition.Always == 4) — compare the enum value, NOT a string "Always".
    - Per-processor key {prefix}{procId}: deserializes to ProcessorProjection; InputDefinition /
      OutputDefinition non-null when schemas were seeded; Liveness.Status == "Pending".
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs`.

    Header (copy from StartLoopFacts.cs lines 1-37; add the Projection namespace import):
      using System.Net; using System.Net.Http.Json; using System.Text.Json;
      using BaseApi.Service.Features.Orchestration;
      using BaseApi.Service.Features.Orchestration.Projection;
      using BaseApi.Service.Features.Processor; using BaseApi.Service.Features.Step;
      using BaseApi.Service.Features.Workflow; using BaseApi.Tests.Composition;
      using BaseApi.Tests.TestHelpers; using StackExchange.Redis; using Xunit;
      namespace BaseApi.Tests.Features.Orchestration;
      [Trait("Phase", "16")]
      public sealed class HappyPathE2EFacts : IClassFixture<Phase8WebAppFactory>
      {
          private readonly Phase8WebAppFactory _factory;
          public HappyPathE2EFacts(Phase8WebAppFactory factory) => _factory = factory;

    Seed helpers: COPY the RICH chain (Schema -> Processor(in/out) -> Step -> Assignment ->
    Workflow) from RedisProjectionWriterFacts.cs lines 40-115 as `private static async Task<Guid>
    Seed*Async(...)` helpers so InputDefinition/OutputDefinition are non-null on the processor key.
    SourceHash via HashHelpers.RandomSha256Hex().

    Fact body (assemble StartLoopFacts seed->POST + RedisProjectionWriterFacts deserialize):
      var ct = TestContext.Current.CancellationToken;
      using var client = _factory.CreateClient();
      var procId = await SeedProcessorAsync(client, ct);     // with in/out schema
      var stepId = await SeedStepAsync(client, ct, procId);  // terminal, NextStepIds null/empty
      var wfId   = await SeedWorkflowAsync(client, ct, new List<Guid> { stepId });
      var correlationId = $"e2e-corr-{Guid.NewGuid():N}";
      using var req = new HttpRequestMessage(HttpMethod.Post, "/api/v1/orchestration/start")
          { Content = JsonContent.Create(new List<Guid> { wfId }) };
      req.Headers.Add("X-Correlation-Id", correlationId);
      var resp = await client.SendAsync(req, ct);
      Assert.Equal(HttpStatusCode.NoContent, resp.StatusCode);

      var prefix = _factory.RedisKeyPrefix;
      var db = _factory.RedisMultiplexer.GetDatabase();

      // Root  — key $"{prefix}{wfId}"  (DEFAULT "D" form — NEVER :N)
      var rootVal = await db.StringGetAsync($"{prefix}{wfId}");
      Assert.True(rootVal.HasValue, "root key set after 204 Start");
      var root = JsonSerializer.Deserialize<WorkflowRootProjection>(rootVal.ToString());
      Assert.Contains(stepId, root!.EntryStepIds);
      Assert.NotEqual(Guid.Empty, root.JobId);
      Assert.Equal(correlationId, root.CorrelationId);
      Assert.Equal("Pending", root.Liveness.Status);

      // Step  — key $"{prefix}{wfId}:{stepId}"
      var stepVal = await db.StringGetAsync($"{prefix}{wfId}:{stepId}");
      var step = JsonSerializer.Deserialize<StepProjection>(stepVal.ToString());
      Assert.Equal(procId, step!.ProcessorId);
      Assert.Empty(step.NextStepIds);                        // terminal -> [] not null
      Assert.Equal(StepEntryCondition.Always, step.EntryCondition);  // enum compare — NOT "Always" string (int-backed, Always==4)

      // Processor — key $"{prefix}{procId}" (different GUID; NOT under {wfId}*)
      var procVal = await db.StringGetAsync($"{prefix}{procId}");
      var proc = JsonSerializer.Deserialize<ProcessorProjection>(procVal.ToString());
      Assert.NotNull(proc!.InputDefinition);
      Assert.NotNull(proc.OutputDefinition);
      Assert.Equal("Pending", proc.Liveness.Status);

    Use ONLY default "D" GUID interpolation ($"{prefix}{wfId}") for keys. If you prefer
    drift-proofing, use RedisProjectionKeys.Root/Step/Processor (internal, visible via
    InternalsVisibleTo) instead — but NEVER format a key with `:N`.

    Close the class brace. Do not modify any existing file.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release --filter "FullyQualifiedName~HappyPathE2EFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs` exists with `[Trait("Phase", "16")]` and `IClassFixture<Phase8WebAppFactory>`.
    - `grep -n "JsonSerializer.Deserialize<WorkflowRootProjection>" HappyPathE2EFacts.cs` AND `Deserialize<StepProjection>` AND `Deserialize<ProcessorProjection>` each return >=1 match (all 3 keyspaces round-tripped).
    - GUID-FORMAT GUARD: `grep -nE ":N\"|wfId:N|stepId:N|procId:N" HappyPathE2EFacts.cs` returns ZERO matches in key-building expressions (keys use default "D" form or RedisProjectionKeys). The `{Guid.NewGuid():N}` inside the correlationId literal is allowed (it is a header value, not a Redis key).
    - `grep -n "Assert.Equal(correlationId, root" HappyPathE2EFacts.cs` confirms the sent header round-trips.
    - `grep -n "StepEntryCondition.Always" HappyPathE2EFacts.cs` confirms enum-compare (no `"Always"` string assertion against entryCondition).
    - `dotnet test --filter "FullyQualifiedName~HappyPathE2EFacts"` exits 0 (all facts pass).
    - No existing file under tests/ or src/ modified (`git status` shows only the new file).
  </acceptance_criteria>
  <done>HappyPathE2EFacts passes; all 3 keyspaces round-trip via STJ; correlationId + jobId asserted; keys in "D" form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | New integration test file only. The HTTP/Redis boundaries it exercises are pre-existing Phase 8/12-15 production seams; no new production attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering (test passes for wrong reason — key aliasing) | HappyPathE2EFacts key-building | mitigate | Assert the key uses default "D" GUID form / RedisProjectionKeys (acceptance-criteria grep guard against `:N`); the fact asserts `rootVal.HasValue` so a wrong key (GET null) fails loudly rather than passing. |
| T-16-02-02 | Repudiation / Info disclosure (test isolation) | RedisFixture per-class prefix | accept | The fact uses the per-class `_factory.RedisKeyPrefix` and the fixture SCAN-deletes on teardown — no prod isolation weakened, no FLUSHDB. Standard inherited control. |

Test-only plan; no new production attack surface. The only genuine risk is a false-positive test
(aliasing), mitigated above. ASVS V5 (the gate validation surface) is exercised positively here.
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~HappyPathE2EFacts"` green.
- Quick regression: `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release --filter "FullyQualifiedName~Orchestration"` stays green (no Phase 14/15 fact disturbed).
- `git status` shows exactly one new file; no existing file edited.
</verification>

<success_criteria>
- A full-HTTP Start happy-path fact asserts all 3 keyspaces via System.Text.Json round-trip,
  correlationId match, and non-empty jobId, using "D"-form keys against the per-class prefix.
</success_criteria>

<output>
After completion, create `.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-02-SUMMARY.md`
</output>
