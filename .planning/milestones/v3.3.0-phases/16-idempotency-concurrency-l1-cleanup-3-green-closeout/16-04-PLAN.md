---
phase: 16-idempotency-concurrency-l1-cleanup-3-green-closeout
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/StopScanFacts.cs
autonomous: true
requirements: [TEST-REDIS-08, TEST-REDIS-09]
must_haves:
  truths:
    - "Start-twice with the SAME workflowIds leaves L2 reflecting the SECOND write — proven by jobId changing (second != first), not merely != Guid.Empty"
    - "Two parallel POST /start for the same workflowId both return 204 with no crash, and the final root is structurally valid (round-trips) — NO deterministic-winner assertion (D-01 non-flaky)"
    - "A thin confirmatory Stop fact (TEST-REDIS-09) proves post-Stop root + per-step keys are GONE while the processor key is PRESENT (inverted contract)"
  artifacts:
    - path: "tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs"
      provides: "TEST-REDIS-08 sequential second-write-reflected (D-02) + concurrent observational (D-01)"
      contains: "Assert.NotEqual(firstJobId"
      min_lines: 90
    - path: "tests/BaseApi.Tests/Features/Orchestration/StopScanFacts.cs"
      provides: "TEST-REDIS-09 thin confirmatory post-Stop key-state fact (inverted)"
      contains: "KeyExistsAsync"
      min_lines: 40
  key_links:
    - from: "IdempotencyFacts (sequential)"
      to: "root jobId before/after second Start"
      via: "two POSTs + two root GETs, assert jobId changed"
      pattern: "NotEqual\\(firstJobId"
    - from: "IdempotencyFacts (concurrent)"
      to: "Task.WhenAll(two parallel /start POSTs)"
      via: "both 204, root round-trips, no winner assertion"
      pattern: "Task.WhenAll"
    - from: "StopScanFacts"
      to: "post-Stop key existence (root/step gone, processor present)"
      via: "KeyExistsAsync after Stop"
      pattern: "KeyExistsAsync"
---

<objective>
TEST-REDIS-08 (D-01 + D-02) + a thin TEST-REDIS-09 confirmatory fact.

- D-02 (sequential): Start-twice with the SAME workflowIds -> L2 reflects the SECOND write.
  Cheapest observable delta is the fresh `jobId` (Guid.NewGuid() per Start, 15-CONTEXT D-05).
  Assert the SECOND jobId != the FIRST (a positive change assertion — Aliasing Risk D-02 —
  not merely != Guid.Empty, which a no-op second Start would still satisfy).
- D-01 (concurrent, observational/non-flaky): two parallel POST /start for the SAME workflowId
  -> both 204, no crash, final root structurally valid (round-trips). DOCUMENTS last-write-wins;
  NO deterministic-winner assertion (per-workflow Start does delete-then-write; a genuine
  interleave can transiently wipe the other writer — D-01 tolerates this).
- TEST-REDIS-09 thin fact: the inverted Stop behavior is already covered by Phase 15
  StopGateFacts/StopOrchestrationFacts/StopCleanupFacts; this plan adds AT MOST one thin
  confirmatory SCAN/EXISTS fact mirroring the (now-rewritten) SC phrasing — NOT a heavy class.

Purpose: Lock the idempotency + concurrency contract that has no Redis lock (last-write-wins by
design) into a stable, non-flaky regression that survives the 3-GREEN gate cadence.
Output: Two new test classes (IdempotencyFacts; StopScanFacts thin confirmatory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-CONTEXT.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md
@.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-RESEARCH.md
@.planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md

<interfaces>
<!-- Phase8WebAppFactory accessors (confirmed) -->
public string RedisKeyPrefix { get; }                       // line 116
public IConnectionMultiplexer RedisMultiplexer { get; }     // line 124

<!-- WorkflowRootProjection — JobId is the observable delta (Guid.NewGuid() per Start, 15-CONTEXT D-05) -->
WorkflowRootProjection(List<Guid> EntryStepIds, string? Cron, Guid JobId, LivenessProjection Liveness, string CorrelationId)
</interfaces>

<analog_files>
- tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs (ReStart_Removes_Orphan_Step lines 138-171 sequential re-Start shape; Start_Returns204 root-read lines 105-134; seed helpers 41-101; orphan-removal KeyExistsAsync asserts lines 157-170)
- tests/BaseApi.Tests/Features/Orchestration/StopGateFacts.cs (Stop 204/422 + KeyExists asserts — the existing TEST-REDIS-09 coverage to NOT duplicate; read to confirm StopScanFacts is additive)
- tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (fixture)
</analog_files>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IdempotencyFacts — sequential second-write (D-02) + concurrent observational (D-01)</name>
  <files>tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs (ReStart_Removes_Orphan_Step lines 138-171 — the sequential re-Start base; Start_Returns204 lines 105-134 — root read; seed helpers 41-101)
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (RedisMultiplexer / RedisKeyPrefix lines 108-127)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md (D-05 — jobId = Guid.NewGuid() per Start; D-07 delete-then-write Start loop)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section "IdempotencyFacts.cs" — verbatim sequential + concurrent snippets)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-RESEARCH.md (Pattern 3 + Pitfall 5 — observational only; Assumption A1 — two clients if one serializes)
  </read_first>
  <behavior>
    - Fact `ReStart_SameWorkflow_ReflectsSecondWrite` (D-02): seed a valid workflow; POST /start;
      GET root; capture firstJobId. POST /start AGAIN (same workflowIds); GET root; capture
      secondJobId. Assert.NotEqual(firstJobId, secondJobId) — the second write is reflected
      (overwrite, not no-op). Optionally also assert orphan per-step keys are removed when the
      graph shrinks between Starts (mirror ReStart_Removes_Orphan_Step lines 157-170).
    - Fact `ConcurrentStart_SameWorkflow_BothSucceed_FinalStructurallyValid` (D-01): two parallel
      POST /start for the same wfId via Task.WhenAll; Assert.All(responses, r => 204); GET root;
      Assert.NotNull(deserialized root). NO assertion about WHICH jobId won. No exception/crash.
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs`.

    Header: `[Trait("Phase", "16")] public sealed class IdempotencyFacts :
    IClassFixture<Phase8WebAppFactory>` + `_factory` ctor; imports mirror StartLoopFacts +
    StackExchange.Redis + System.Text.Json. namespace BaseApi.Tests.Features.Orchestration.
    Copy the seed helpers from StartLoopFacts.cs lines 41-101.

    Sequential fact (D-02) — assert the CHANGE, not just non-empty (Aliasing Risk D-02):
      var ct = TestContext.Current.CancellationToken;
      using var client = _factory.CreateClient();
      var procId = await SeedProcessorAsync(client, ct);
      var stepId = await SeedStepAsync(client, ct, procId);
      var wfId   = await SeedWorkflowAsync(client, ct, new List<Guid> { stepId });
      var prefix = _factory.RedisKeyPrefix;
      var db = _factory.RedisMultiplexer.GetDatabase();

      await client.PostAsJsonAsync("/api/v1/orchestration/start", new List<Guid> { wfId }, ct);
      var firstRoot = JsonSerializer.Deserialize<WorkflowRootProjection>(
          (await db.StringGetAsync($"{prefix}{wfId}")).ToString());
      var firstJobId = firstRoot!.JobId;

      await client.PostAsJsonAsync("/api/v1/orchestration/start", new List<Guid> { wfId }, ct);
      var secondRoot = JsonSerializer.Deserialize<WorkflowRootProjection>(
          (await db.StringGetAsync($"{prefix}{wfId}")).ToString());
      Assert.NotEqual(firstJobId, secondRoot!.JobId);   // second write reflected (overwrite, not no-op)

    Concurrent fact (D-01) — observational, NO winner (Pattern 3):
      var t1 = client.PostAsJsonAsync("/api/v1/orchestration/start", new List<Guid> { wfId }, ct);
      var t2 = client.PostAsJsonAsync("/api/v1/orchestration/start", new List<Guid> { wfId }, ct);
      var responses = await Task.WhenAll(t1, t2);
      Assert.All(responses, r => Assert.Equal(HttpStatusCode.NoContent, r.StatusCode));
      var root = JsonSerializer.Deserialize<WorkflowRootProjection>(
          (await db.StringGetAsync($"{prefix}{wfId}")).ToString());
      Assert.NotNull(root);   // structurally valid — NOT a deterministic winner

    A1 caution: if a single CreateClient() HttpClient serializes the two sends (observed by both
    completing strictly sequentially), create TWO clients (`using var c1 = _factory.CreateClient();
    using var c2 = _factory.CreateClient();`) and fire one POST per client. Low risk; leave the
    code shaped so the swap is a one-liner. Keys: default "D" form only, NEVER `:N`.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release --filter "FullyQualifiedName~IdempotencyFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `[Trait("Phase", "16")]` and `IClassFixture<Phase8WebAppFactory>`.
    - `grep -n "Assert.NotEqual(firstJobId" IdempotencyFacts.cs` returns >=1 (positive change assertion, not `!= Guid.Empty`).
    - `grep -n "Task.WhenAll" IdempotencyFacts.cs` returns >=1 (true concurrency).
    - `grep -n "Assert.All(responses" IdempotencyFacts.cs` AND a final `Assert.NotNull(root)` present (both 204 + structurally valid, no winner assertion).
    - GUID-FORMAT GUARD: `grep -nE "wfId:N|stepId:N|:N\}" IdempotencyFacts.cs` returns ZERO in key expressions.
    - `grep -n "FLUSHDB\|FlushDatabase" IdempotencyFacts.cs` returns ZERO.
    - `dotnet test --filter "FullyQualifiedName~IdempotencyFacts"` exits 0 across the run.
    - `git status` lists only the new file (no existing file modified).
  </acceptance_criteria>
  <done>IdempotencyFacts proves second-write-reflected (jobId changed) + concurrent both-204-structurally-valid (no winner); non-flaky.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StopScanFacts — thin confirmatory post-Stop key-state (TEST-REDIS-09)</name>
  <files>tests/BaseApi.Tests/Features/Orchestration/StopScanFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/StopGateFacts.cs (the existing TEST-REDIS-09 coverage — read to confirm what is ALREADY covered so this fact is additive, not duplicate)
    - tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs (seed + Start + key-read pattern lines 41-134)
    - .planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-PATTERNS.md (section "StopScanFacts.cs" — the inverted post-Stop assert shape)
    - .planning/phases/15-l2-redis-projection-write-stop-existence-check/15-CONTEXT.md (D-06 — Stop deletes root + per-step, never processor; D-08 processor TTL)
  </read_first>
  <behavior>
    - Fact `Stop_AfterStart_RemovesRootAndStep_KeepsProcessor`: seed valid workflow; POST /start;
      assert root key + per-step key PRESENT and processor key PRESENT. POST /stop; assert root
      key GONE, per-step key GONE, processor key STILL PRESENT (inverted contract, TTL'd).
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Features/Orchestration/StopScanFacts.cs`. This is the thin
    confirmatory fact for TEST-REDIS-09 (D-04's "leave untouched" rule is scoped to the Phase-14
    GATE facts, NOT the Phase-15 Stop facts — but we still do not edit StopGateFacts; we add a
    NEW class).

    Header: `[Trait("Phase", "16")] public sealed class StopScanFacts :
    IClassFixture<Phase8WebAppFactory>` + `_factory` ctor; imports mirror StartLoopFacts +
    StackExchange.Redis. namespace BaseApi.Tests.Features.Orchestration. Copy seed helpers from
    StartLoopFacts.cs lines 41-101.

    Single fact body:
      var ct = TestContext.Current.CancellationToken;
      using var client = _factory.CreateClient();
      var procId = await SeedProcessorAsync(client, ct);
      var stepId = await SeedStepAsync(client, ct, procId);
      var wfId   = await SeedWorkflowAsync(client, ct, new List<Guid> { stepId });
      var prefix = _factory.RedisKeyPrefix;
      var db = _factory.RedisMultiplexer.GetDatabase();

      await client.PostAsJsonAsync("/api/v1/orchestration/start", new List<Guid> { wfId }, ct);
      Assert.True(await db.KeyExistsAsync($"{prefix}{wfId}"));            // root present
      Assert.True(await db.KeyExistsAsync($"{prefix}{wfId}:{stepId}"));   // per-step present
      Assert.True(await db.KeyExistsAsync($"{prefix}{procId}"));          // processor present

      var stopResp = await client.PostAsJsonAsync("/api/v1/orchestration/stop", new List<Guid> { wfId }, ct);
      Assert.Equal(HttpStatusCode.NoContent, stopResp.StatusCode);
      Assert.False(await db.KeyExistsAsync($"{prefix}{wfId}"));           // root deleted (inverted)
      Assert.False(await db.KeyExistsAsync($"{prefix}{wfId}:{stepId}")); // per-step deleted
      Assert.True(await db.KeyExistsAsync($"{prefix}{procId}"));          // processor RETAINED (TTL)

    Keys: default "D" form only, NEVER `:N`. Do NOT modify StopGateFacts/StopOrchestrationFacts/
    StopCleanupFacts.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --configuration Release --filter "FullyQualifiedName~StopScanFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `[Trait("Phase", "16")]` and `IClassFixture<Phase8WebAppFactory>`.
    - `grep -n "Assert.False(await db.KeyExistsAsync(\$\"{prefix}{wfId}\")" StopScanFacts.cs` present (root deleted post-Stop).
    - `grep -n ":{stepId}\")" StopScanFacts.cs` shows the per-step key asserted GONE (Assert.False).
    - `grep -n "Assert.True(await db.KeyExistsAsync(\$\"{prefix}{procId}\")" StopScanFacts.cs` present AFTER Stop (processor retained — inverted contract).
    - GUID-FORMAT GUARD: `grep -nE ":N\}" StopScanFacts.cs` returns ZERO in key expressions.
    - `dotnet test --filter "FullyQualifiedName~StopScanFacts"` exits 0.
    - StopGateFacts.cs / StopOrchestrationFacts.cs / StopCleanupFacts.cs unchanged (`git status`).
  </acceptance_criteria>
  <done>StopScanFacts confirms inverted post-Stop state (root+step gone, processor present); existing Stop facts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/v1/orchestration/start and /stop | Pre-existing prod boundaries. These facts VERIFY idempotency/concurrency/cleanup behavior; no new boundary or input handler introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering (stale-jobId false positive, Aliasing Risk D-02) | sequential idempotency assert | mitigate | Assert secondJobId != firstJobId (a positive change), not merely != Guid.Empty — a no-op second Start would fail the test rather than pass it. |
| T-16-04-02 | Denial of Service / race (concurrent Start interleave) | concurrent observational fact | accept | No Redis lock by design (last-write-wins, PROJECT.md). The fact documents (does not assert a winner) per D-01 to stay non-flaky across the 3-GREEN gate; a strict-winner assertion is a deferred idea, NOT planned. |
| T-16-04-03 | Tampering (test isolation / prod-invariant regression) | RedisFixture teardown | accept | Per-class prefix + SCAN-DEL teardown; NO FLUSHDB; processor TTL keys swept by the prefix SCAN so the gate's redis-cli --scan BEFORE=AFTER invariant holds. Inherited control, not weakened. |

Test-only plan. No production code change. No FLUSHDB. SCAN/EXISTS only (no sync KEYS).
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~IdempotencyFacts"` and `~StopScanFacts` both green.
- `dotnet test ... --filter "FullyQualifiedName~Orchestration"` stays green (Phase 15 Stop facts intact).
- `git status` shows exactly two new files; no existing file edited.
</verification>

<success_criteria>
- TEST-REDIS-08 sequential second-write-reflected (jobId changed) + concurrent observational
  (both 204, structurally valid, no winner) are GREEN and non-flaky; TEST-REDIS-09 thin
  confirmatory inverted-Stop fact is GREEN; no existing fact modified.
</success_criteria>

<output>
After completion, create `.planning/phases/16-idempotency-concurrency-l1-cleanup-3-green-closeout/16-04-SUMMARY.md`
</output>
