---
phase: 58-orchestration-gate-integration-proof-close
plan: 04
type: execute
wave: 2
depends_on: [58-01, 58-02]
files_modified:
  - scripts/phase-58-close.ps1
autonomous: true
requirements: [CFG-08, CFG-09]
must_haves:
  truths:
    - "scripts/phase-58-close.ps1 exists and parses (PowerShell AST-valid)"
    - "It reads BOTH embedded SourceHashes (Processor.Sample.dll + Processor.BadConfig.dll)"
    - "It GET-or-creates two config-schema rows by sentinel Name + two processor rows (CREATE-IF-ABSENT)"
    - "The triple-SHA / skp:msg:* count==0 / skp-dlq-1 depth==0 / N=3 protocol is verbatim from phase-55"
    - "It does NOT add a SHA exclusion for badconfig and does NOT wait for badconfig liveness"
  artifacts:
    - path: "scripts/phase-58-close.ps1"
      provides: "Phase 58 triple-SHA close gate (two-schema/two-processor seed + badconfig profile)"
      contains: "gateA-badconfig-clash"
  key_links:
    - from: "scripts/phase-58-close.ps1"
      to: "Processor.BadConfig.dll embedded SourceHash"
      via: "Assembly.Load + AssemblyMetadata SourceHash read"
      pattern: "Processor.BadConfig.dll"
    - from: "scripts/phase-58-close.ps1"
      to: "/api/v1/schemas"
      via: "GET-all-filter-by-Name then POST-if-absent (never PUT)"
      pattern: "by-source-hash|/api/v1/schemas"
---

<objective>
Clone `scripts/phase-55-close.ps1` → `scripts/phase-58-close.ps1` VERBATIM, changing ONLY the seed block (two schemas + two processors, CREATE-IF-ABSENT), the badconfig profile bring-up reference, the version-string verify, and the title. The proven triple-SHA net-zero machinery (psql `\l` SHA, unfiltered redis `--scan` SHA, rabbitmq `list_queues name` SHA, `skp:msg:*` count==0, separate `skp-dlq-1` depth==0, N=3 identical-fact-count Smell-A guard, steady-state `skp:{procId}`/`_bus_` exclusions) is carried over UNCHANGED — v6 left the slot-array recovery machinery untouched.

Purpose: Produce the close gate (D-08, D-09) the operator runs for the live N=3 GREEN proof. BadConfig needs NO new SHA exclusion (writes no liveness key, binds no queue — D-05/D-09b) and its liveness must NOT be a pre-flight requirement (it intentionally never goes Healthy).

Output: `scripts/phase-58-close.ps1`, syntactically valid. The autonomous deliverable is the script EXISTING and PARSING — its live N=3 execution is operator-gated (Plan 05). No live stack required for any autonomous check here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-CONTEXT.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-PATTERNS.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-02-SUMMARY.md

<interfaces>
<!-- The phase-55 close-script seams to clone. The seed block is the ONLY substantive delta. -->

Embedded-hash read (reuse verbatim, ONCE PER processor — phase-55-close.ps1:99-120):
  $dll = "src/Processor.<X>/bin/Release/net8.0/Processor.<X>.dll"   # fallback Debug, then build-if-absent
  $asm = [System.Reflection.Assembly]::Load([System.IO.File]::ReadAllBytes($dll))
  $sourceHash = ($asm.GetCustomAttributes([System.Reflection.AssemblyMetadataAttribute],$false) |
      Where-Object { $_.Key -eq 'SourceHash' } | Select-Object -First 1).Value
  # validate: ^[a-f0-9]{64}$ else exit 2

Processor GET-or-create seed (reuse verbatim, TWICE — phase-55-close.ps1:122-156):
  GET  $baseApi/api/v1/processors/by-source-hash/$sourceHash   # 200 → reuse .id, 404 → POST
  POST $baseApi/api/v1/processors  body @{ name;version;sourceHash; inputSchemaId; outputSchemaId; configSchemaId }
  $baseApi = 'http://localhost:8080'

Schema GET-or-create seed (NEW, TWICE — RESEARCH Pattern 2):
  GET  /api/v1/schemas → filter where .name == sentinelName → reuse .id
  POST /api/v1/schemas body @{ name; version='1.0.0'; description=$null; definition=<json> }   # NEVER PUT (409 frozen)

Sentinel Names + definitions (MUST match Plan 02/03's E2E sentinels — see 58-02-SUMMARY.md):
  "gateA-sample-compatible" → { "type":"object","properties":{"value":{"type":"string"}} }
  "gateA-badconfig-clash"   → { "type":"object","properties":{"quantity":{"type":"string"}} }

Verbatim invariant block (phase-55-close.ps1:202-379) — DO NOT MODIFY:
  psql \l SHA · redis --scan SHA (skp:{procId} + _bus_ excluded via Where-Object) · rabbitmq list_queues name SHA ·
  skp:msg:* count==0 · skp-dlq-1 depth==0 · N=3 identical-fact-count Smell-A guard.

Version verify (D-09c): Processor.Sample seed version stays '3.5.0' (VERIFIED src/Processor.Sample/appsettings.json).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone phase-55-close.ps1 to phase-58-close.ps1 with the two-schema/two-processor seed + badconfig deltas</name>
  <read_first>
    - scripts/phase-55-close.ps1 (the WHOLE 384-line file — clone source; note the embedded-hash read at 99-120, the processor seed at 122-156, the `$services` health-required list at ~183, the Sample `skp:{procId}` exclusion at ~211/287, the triple-SHA invariant block 202-379, and the title strings)
    - .planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md (the bring-up command the script's header/comments reference — to mirror the `--profile badconfig` addition consistently)
    - .planning/phases/58-orchestration-gate-integration-proof-close/58-01-SUMMARY.md (confirms `Processor.BadConfig.dll` path the second hash-read targets)
    - .planning/phases/58-orchestration-gate-integration-proof-close/58-02-SUMMARY.md (the EXACT sentinel Names + definition JSON to reuse — must match the E2E)
  </read_first>
  <action>
    Create `scripts/phase-58-close.ps1` as a VERBATIM clone of `scripts/phase-55-close.ps1` with these and ONLY these deltas (PATTERNS.md "phase-58-close.ps1" + D-09 delta list):

    1. **Read BOTH embedded hashes** — keep the Sample hash-read block (phase-55:99-120) and ADD a second identical block reading `src/Processor.BadConfig/bin/Release/net8.0/Processor.BadConfig.dll` (Debug fallback, build-if-absent, `^[a-f0-9]{64}$` validation, `exit 2` on failure). Result: `$sampleHash`, `$badConfigHash`.

    2. **Seed TWO schema rows FIRST (D-09a, before the processor seeds)** — GET-or-create by sentinel Name (schemas have NO unique constraint → GET-all `/api/v1/schemas` → `Where-Object { $_.name -eq $sentinel }` → reuse `.id`; POST only if absent; NEVER PUT — 409 frozen-once-referenced). Set `$compatibleSchemaId` (Name `gateA-sample-compatible`, definition `{ "type":"object","properties":{"value":{"type":"string"}} }`) and `$clashSchemaId` (Name `gateA-badconfig-clash`, definition `{ "type":"object","properties":{"quantity":{"type":"string"}} }`). Use the EXACT sentinel Names + definitions from 58-02-SUMMARY.md so the close-script seed and the E2E seed converge on the same rows.

    3. **Seed TWO processor rows** — keep the Sample GET-or-create (phase-55:122-156) but pass `configSchemaId = $compatibleSchemaId` (was `$null`); ADD a second GET-or-create for `processor-badconfig` using `$badConfigHash` with `configSchemaId = $clashSchemaId`, `name='processor-badconfig'`, `version='3.5.0'`.

    4. **(D-09b) Do NOT add a redis SHA exclusion for badconfig** — it writes no liveness key + binds no queue, so it is absent from both snapshots. Keep the existing Sample `skp:{procId}` exclusion (phase-55:211/287) UNCHANGED; add nothing for badconfig.

    5. **(Health pre-flight)** keep `processor-sample` in the `$services` health-required list and its `skp:{procId}` liveness wait UNCHANGED. Do NOT add `processor-badconfig` to the liveness-required set (it intentionally never goes Healthy; its Docker `/ready` passes but its liveness key must NOT be expected — RESEARCH anti-pattern). If the script's `$services` array is used for Docker `/ready` health, badconfig MAY appear there (its `/ready` passes); but it MUST NOT appear in any `skp:{procId}` liveness-required wait.

    6. **(D-09c)** Seed version stays `'3.5.0'` for Sample (and `'3.5.0'` for badconfig — SourceHash, not the version string, distinguishes identity).

    7. **(D-09d / header)** Update the script header/comment that documents the bring-up to add `--profile badconfig` + `processor-badconfig` to the rebuild set (the actual `docker compose up` is the operator's Plan-05 runbook step; if the script itself invokes compose bring-up, add `--profile badconfig` and `processor-badconfig` there).

    8. **Retitle** every "Phase 55" / "v5.0.0" string to "Phase 58" / "v6.0.0" (title, Write-Host banners, comment headers). The triple-SHA invariant block (phase-55:202-379 — psql `\l`, redis `--scan`, rabbitmq `list_queues name`, `skp-dlq-1` depth==0, `skp:msg:*` count==0, N=3 Smell-A guard) stays VERBATIM apart from these title strings.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile('scripts/phase-58-close.ps1', [ref]$null, [ref]$errs); if ($errs) { $errs | ForEach-Object { $_.Message }; exit 1 } else { 'PARSE OK' }"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-58-close.ps1` exists.
    - PowerShell AST parse is clean: `pwsh -NoProfile -Command "$e=$null; [System.Management.Automation.Language.Parser]::ParseFile('scripts/phase-58-close.ps1',[ref]$null,[ref]$e); exit ($e.Count)"` exits 0 (zero parse errors).
    - Reads both dlls: `grep -q "Processor.Sample.dll" scripts/phase-58-close.ps1` AND `grep -q "Processor.BadConfig.dll" scripts/phase-58-close.ps1`.
    - Two schema sentinels present: `grep -q "gateA-sample-compatible" scripts/phase-58-close.ps1` AND `grep -q "gateA-badconfig-clash" scripts/phase-58-close.ps1`.
    - Never PUTs a schema: `! grep -qi "Method Put.*schemas" scripts/phase-58-close.ps1`.
    - Triple-SHA invariant carried over: `grep -q "list_queues" scripts/phase-58-close.ps1` AND `grep -q "skp-dlq-1" scripts/phase-58-close.ps1` AND `grep -q "skp:msg:" scripts/phase-58-close.ps1`.
    - Retitled: `grep -q "Phase 58" scripts/phase-58-close.ps1` AND `! grep -q "Phase 55" scripts/phase-58-close.ps1` (no stale phase-55 title remains).
    - No badconfig liveness pre-flight wait: the script does NOT wait on `skp:{badConfigHash-derived-procId}` (manual review: badconfig appears only in the seed + optional Docker `/ready`, never in a liveness-required loop).
    - NO live stack is started by any of these checks (parse + grep only).
  </acceptance_criteria>
  <done>scripts/phase-58-close.ps1 parses cleanly, reads both embedded hashes, GET-or-creates the two named schemas + two processor rows (CREATE-IF-ABSENT, never PUT), carries the phase-55 triple-SHA / DLQ / msg-count / N=3 protocol verbatim, adds no badconfig SHA exclusion and no badconfig liveness pre-flight, and is retitled Phase 58.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close script → live stack (psql/redis/rabbitmq docker exec, WebApi seed) | The gate snapshots + seeds production-shaped state; idempotency + correct exclusions keep net-zero honest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-09 | Tampering (false net-zero) | redis SHA exclusion set | mitigate | BadConfig adds NO new exclusion (it writes no key, binds no queue) — keeping the exclusion set minimal means a stray key WOULD show as a SHA mismatch rather than being silently masked. The Sample `skp:{procId}` exclusion is the only liveness exclusion and is unchanged. Verified by: acceptance criteria forbid adding a badconfig exclusion; invariant block is verbatim. |
| T-58-10 | Tampering (state churn) | two-schema seed idempotency | mitigate | GET-or-create-by-Name (never PUT) keeps the N=3 run identical-fact-count; a blind POST or a PUT (409 frozen) would churn rows and fail the Smell-A guard. Verified by: acceptance criteria require the by-Name filter + forbid PUT. |
| T-58-11 | Spoofing (identity divergence) | embedded SourceHash read | mitigate | Both hashes are read off the BUILT dlls (genuine, fold-computed), validated `^[a-f0-9]{64}$`, `exit 2` on mismatch — so a stale/mismatched image is caught (the seeded row's hash must equal the running container's or identity never resolves). Verified by: the validation regex + dual-dll read are carried from phase-55 + extended. |

No new endpoints/auth/crypto/input-validation surface — the script uses the existing CRUD + docker-exec snapshot tooling unchanged.
</threat_model>

<verification>
- `scripts/phase-58-close.ps1` parses with zero AST errors.
- Reads both Processor.Sample.dll + Processor.BadConfig.dll embedded hashes.
- GET-or-creates two named schemas + two processor rows (CREATE-IF-ABSENT, never PUT).
- Triple-SHA / `skp:msg:*` count==0 / `skp-dlq-1` depth==0 / N=3 protocol verbatim; retitled Phase 58.
- No badconfig SHA exclusion; no badconfig liveness pre-flight wait.
- No live stack required for any autonomous check (parse + grep only).
</verification>

<success_criteria>
- scripts/phase-58-close.ps1 exists, parses, and embodies the D-09 seed deltas over a verbatim phase-55 triple-SHA protocol.
- Two-schema + two-processor CREATE-IF-ABSENT seed using the shared sentinel Names.
- BadConfig contributes nothing to the SHA and is not a liveness pre-flight requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/58-orchestration-gate-integration-proof-close/58-04-SUMMARY.md`. Record: the script path, the exact bring-up command its header documents (incl. `--profile badconfig`), confirmation the triple-SHA block is verbatim, and the sentinel Names it seeds (so Plan 05's runbook references the same command + names).
</output>
