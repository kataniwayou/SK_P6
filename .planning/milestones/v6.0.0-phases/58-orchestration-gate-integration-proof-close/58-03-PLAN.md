---
phase: 58-orchestration-gate-integration-proof-close
plan: 03
type: execute
wave: 2
depends_on: [58-01, 58-02]
files_modified:
  - compose.yaml
  - tests/BaseApi.Tests/Orchestrator/GateACompositionE2ETests.cs
autonomous: true
requirements: [CFG-08, CFG-09]
must_haves:
  truths:
    - "compose.yaml has a processor-badconfig service behind a 'badconfig' profile (excluded from default up)"
    - "A new Gate-A composition E2E file COMPILES under [Trait(\"Category\",\"RealStack\")] (excluded from hermetic run)"
    - "The CFG-08 test polls ES for the Gate A clash log FIRST, then asserts skp:{badId} stably absent, then Start 422"
    - "The CFG-09 test seeds Sample with a compatible non-null ConfigSchemaId, polls healthy liveness, asserts Start 204"
  artifacts:
    - path: "compose.yaml"
      provides: "processor-badconfig service behind a profile"
      contains: "processor-badconfig"
    - path: "tests/BaseApi.Tests/Orchestrator/GateACompositionE2ETests.cs"
      provides: "CFG-08 + CFG-09 RealStack composition tests"
      contains: "Gate A incompatibility"
  key_links:
    - from: "GateACompositionE2ETests.cs CFG-08"
      to: "Elasticsearch clash log"
      via: "ElasticsearchTestClient.PollEsForLog scoped to service.name processor-badconfig"
      pattern: "PollEsForLog"
    - from: "GateACompositionE2ETests.cs CFG-08"
      to: "skp:{badId} absence"
      via: "inverse of PollForHealthyLivenessAsync (stably-absent window)"
      pattern: "IsNullOrEmpty"
    - from: "compose.yaml processor-badconfig"
      to: "src/Processor.BadConfig/Dockerfile"
      via: "build.dockerfile + profiles"
      pattern: "profiles"
---

<objective>
Wire the badconfig container into compose (behind a profile so the default dev stack stays clean) and author the new Gate-A composition E2E — the autonomously-COMPILING deliverable that the operator's live run executes. The CFG-08 test proves Gate A is the CAUSE of the 422 via three signals (D-06): (a) the Phase-57 D-10 Error clash-log in Elasticsearch (causation + boot-confirmation, polled FIRST), (b) `skp:{badId}` stably absent (mechanism), (c) orchestration-start 422 (outcome). The CFG-09 test proves Gate A is not a false-positive blocker: Sample with a compatible non-null schema passes Gate A → Healthy → liveness written → Start 204.

Purpose: Produce the composition proof harness (D-06, D-07) plus the profile-gated badconfig tier (D-04) it runs against. The test file is excluded from the hermetic suite by `[Trait("Category","RealStack")]` but MUST compile — its live execution is operator-gated (Plan 05).

Output: An edited `compose.yaml` (new profile-gated service) and a new `GateACompositionE2ETests.cs` that compiles 0-warning. NO autonomous check requires the live stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-CONTEXT.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-PATTERNS.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-02-SUMMARY.md

<interfaces>
<!-- Harness seams the new test reuses. Reuse directly — do NOT re-explore the codebase. -->

From SampleRoundTripE2ETests.cs (reuse the SAME factory + helpers — the Gate-A test joins the same collection):
  RealStackWebAppFactory (host overrides + net-zero teardown: L2KeysToCleanup, ParentIndexMembersToSrem)
  PollForHealthyLivenessAsync(Guid procId) → returns when skp:{procId} is fresh (CFG-09 positive poll)
  SeedProcessorAsync(client, sourceHash, ..., Guid? configSchemaId = null)  // Plan 02 added the configSchemaId pass-through
  SeedConfigSchemaAsync(client, sentinelName, definition, ct) → Guid        // Plan 02 added; GET-or-create by Name
  const SampleCompatibleSchemaName = "gateA-sample-compatible"              // Plan 02
  ES query precedent: term on attributes.* + service.name scope (SampleRoundTripE2ETests.cs:150-167)

From ElasticsearchTestClient (tests/BaseApi.Tests/Observability/Helpers/ElasticsearchTestClient.cs):
  Task<JsonElement?> PollEsForLog(string query, int timeoutMs, CancellationToken ct)  // handles 404/empty/backoff

From L2ProjectionKeys (the liveness key shape):
  L2ProjectionKeys.Processor(Guid procId) → "skp:{procId:D}"   // the key CFG-08 asserts ABSENT, CFG-09 asserts PRESENT

Source log (CFG-08 ES target) — ProcessorStartupOrchestrator.cs:187-189:
  logger.LogError("Gate A incompatibility for processor {ProcessorId} config schema {ConfigSchemaId}: {Clash}", ...)
  Scope ES query to resource.attributes.service.name == "processor-badconfig" + match body "Gate A incompatibility".

Orchestration start (both CFG-08 and CFG-09 outcome):
  POST /api/v1/orchestration/start  body: List<Guid> workflowIds
  → 422 UnprocessableEntity when a participant's skp:{procId} is absent (ProcessorLivenessValidator.cs:33-35)
  → 204 NoContent when all participants are live.

Clash schema definition for CFG-08 (BadConfig(int Quantity); schema types quantity string → clash):
  { "$schema": "https://json-schema.org/draft/2020-12/schema", "type":"object",
    "properties": { "quantity": { "type":"string" } } }
  Use sentinel Name "gateA-badconfig-clash" (must match Plan 04's close-script sentinel).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the profile-gated processor-badconfig service to compose.yaml</name>
  <read_first>
    - compose.yaml (the processor-sample tier at lines ~265-290 — the clone template; confirm exact line range and the healthcheck/env/depends_on shape)
    - src/Processor.BadConfig/Dockerfile (created in Plan 01 — confirm the build context path the compose service references)
    - .planning/phases/58-orchestration-gate-integration-proof-close/58-01-SUMMARY.md (confirms the BadConfig project paths)
  </read_first>
  <action>
    Clone the `processor-sample` compose tier (PATTERNS.md compose pattern, lines ~265-290) into a new `processor-badconfig` service with these deltas ONLY (D-04, D-05):
    - service key `processor-sample` → `processor-badconfig`.
    - `container_name: sk-processor-sample` → `sk-processor-badconfig` (MUST be unique — RESEARCH Open Question 2).
    - `build.dockerfile: src/Processor.Sample/Dockerfile` → `src/Processor.BadConfig/Dockerfile`.
    - **Add a `profiles:` key** — `profiles: ["badconfig"]` — so the DEFAULT `docker compose up` EXCLUDES it; the close gate + live run bring it up with `--profile badconfig` (A2: standard Compose profile semantics; verify profile-key placement against the compose file version in use).
    - Keep the `/health/ready` healthcheck UNCHANGED (BadConfig flips MarkReady — `/health/ready` passes; it never goes Healthy, but that is liveness, not the Docker healthcheck).
    - Keep `depends_on` (rabbitmq/redis/baseapi-service service_healthy), the environment block (RabbitMq/Redis/OTel host keys, `Processor__ExecutionDataTtl: "5"` — harmless, BadConfig writes no data keys), `restart: unless-stopped`.
    - The internal health port stays 8082 (NOT host-published — no host port collision with processor-sample, which also doesn't publish it).
    Do NOT touch the default `processor-sample` service or any other tier.
  </action>
  <verify>
    <automated>docker compose -f compose.yaml config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "processor-badconfig:" compose.yaml` and `grep -q "sk-processor-badconfig" compose.yaml`.
    - `grep -q "src/Processor.BadConfig/Dockerfile" compose.yaml`.
    - The service is under a `profiles:` containing `badconfig` (`grep -A30 "processor-badconfig:" compose.yaml | grep -q "badconfig"` in a profiles context).
    - `docker compose -f compose.yaml config --quiet` exits 0 (valid compose schema; this is a static parse, NOT a stack bring-up — no containers start).
    - `docker compose -f compose.yaml config --services` does NOT list `processor-badconfig` without `--profile badconfig`, but `docker compose --profile badconfig -f compose.yaml config --services` DOES (confirms profile gating). [If `docker` is unavailable on the executor host, a YAML-parse + grep of the profiles key under the service is the fallback acceptance check.]
  </acceptance_criteria>
  <done>compose.yaml defines a `processor-badconfig` service behind the `badconfig` profile with a unique container name, building from the BadConfig Dockerfile; the default `docker compose up` is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Author GateACompositionE2ETests.cs (CFG-08 incompatible→422; CFG-09 compatible→204) — compiles under RealStack trait</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the harness to reuse — RealStackWebAppFactory, PollForHealthyLivenessAsync, the ES query precedent at 150-167, net-zero teardown registration at 130-142, the seed helpers extended in Plan 02)
    - tests/BaseApi.Tests/Observability/Helpers/ElasticsearchTestClient.cs (PollEsForLog signature)
    - tests/BaseApi.Tests/Observability/CollectionDefinitions.cs ("Observability" collection: DisableParallelization + ICollectionFixture<RealStackNetZeroSweepFixture>)
    - .planning/phases/58-orchestration-gate-integration-proof-close/58-02-SUMMARY.md (the exact sentinel Name constants + seed-helper signatures from Plan 02 — REUSE them, do not redefine)
    - src/BaseApi.Service/Features/Orchestration/Validation/ProcessorLivenessValidator.cs lines 30-40 (the 422 path — confirms absent → ProcessorNotLive → UnprocessableEntity)
    - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs lines 184-192 (the Gate A clash log emission — the ES poll target)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/GateACompositionE2ETests.cs` (PATTERNS.md "GateACompositionE2ETests" section). Reuse `RealStackWebAppFactory` + the Plan-02 seed helpers + `PollForHealthyLivenessAsync` + `ElasticsearchTestClient.PollEsForLog` wholesale — author NO new harness.

    **Class attributes (mirror SampleRoundTripE2ETests.cs:69-72):**
    ```csharp
    [Trait("Category", "E2E")]
    [Trait("Category", "RealStack")]   // hermetic filter excludes it; build gate still COMPILES it
    [Trait("Phase", "58")]
    [Collection("Observability")]       // DisableParallelization + ICollectionFixture<RealStackNetZeroSweepFixture>
    public sealed class GateACompositionE2ETests { ... }
    ```

    **CFG-08 test (incompatible → clash-log + absent + 422):**
    1. Seed the clash schema via `SeedConfigSchemaAsync(client, "gateA-badconfig-clash", clashDefinition, ct)` (clashDefinition = `{ "type":"object","properties":{"quantity":{"type":"string"}} }` with the `$schema` draft key). Seed the BadConfig processor row via `SeedProcessorAsync(client, badConfigSourceHash, configSchemaId: clashSchemaId)`, capturing `badId`. Build a minimal workflow whose graph includes `badId` (reuse the SC/Sample workflow-seed precedent in the harness), capturing `badWorkflowId`.
    2. **ES poll FIRST (D-06 causation linchpin):** `PollEsForLog` scoped to `resource.attributes.service.name == "processor-badconfig"` + `match body "Gate A incompatibility"` (timeoutMs 120_000). `Assert.NotNull` + `Assert.Contains("Gate A incompatibility", clash!.Value.GetRawText())`. Do this BEFORE the absence check — it proves the container booted AND ran Gate A, so "absent" is causation, not "not running" (RESEARCH Pitfall 1).
    3. **Inverse liveness-absence poll:** assert `db.StringGetAsync(L2ProjectionKeys.Processor(badId))` `IsNullOrEmpty` across ~3 reads with ~5s delays (spanning > one 10s heartbeat interval), failing if the key EVER appears (the inverse of `PollForHealthyLivenessAsync`).
    4. **Start → 422:** `POST /api/v1/orchestration/start` with `List<Guid>{ badWorkflowId }`; `Assert.Equal(HttpStatusCode.UnprocessableEntity, resp.StatusCode)`.

    **CFG-09 test (compatible → Healthy → 204):** reuse the SampleRoundTrip positive flow almost verbatim — seed `SeedConfigSchemaAsync(client, SampleCompatibleSchemaName, compatibleDefinition, ct)` and `SeedProcessorAsync(client, sampleSourceHash, configSchemaId: compatibleSchemaId)` (Gate A RUNS and PASSES, not skipped — D-03), `await PollForHealthyLivenessAsync(sampleId)`, build a sample workflow, `POST .../start` → `Assert.Equal(HttpStatusCode.NoContent, resp.StatusCode)` (204).

    **Net-zero teardown:** register the run's keys exactly as `SampleRoundTripE2ETests.cs:130-142` (`L2KeysToCleanup` / `ParentIndexMembersToSrem`) so the `RealStackNetZeroSweepFixture` (inherited via the collection) reclaims residue. CFG-08 binds no queue / writes no data (BadConfig is harmless — minimal teardown); CFG-09 (cron round-trip) needs the full sweep like SampleRoundTrip.

    **Compile-only goal:** the file must build 0-warning under the RealStack trait (excluded from the hermetic run). The acceptance criteria below verify COMPILE + trait + the load-bearing assertions are PRESENT in source — NOT live-stack behavior (that is Plan 05's operator-gated runbook).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Orchestrator/GateACompositionE2ETests.cs` exists.
    - Carries all four traits: `grep -q 'Trait("Category", "RealStack")'`, `grep -q 'Trait("Phase", "58")'`, `grep -q 'Collection("Observability")'`.
    - CFG-08 ES causation assertion present: `grep -q "PollEsForLog" ...` AND `grep -q "Gate A incompatibility" ...` AND `grep -q "processor-badconfig" ...`.
    - Inverse absence assertion present: `grep -q "IsNullOrEmpty" ...` AND `grep -q "L2ProjectionKeys.Processor" ...`.
    - 422 + 204 outcomes present: `grep -q "UnprocessableEntity" ...` AND `grep -q "NoContent" ...`.
    - Reuses the shared sentinel names: `grep -q "gateA-badconfig-clash" ...` AND `grep -q "gateA-sample-compatible" ...` (or the `SampleCompatibleSchemaName` const from Plan 02).
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 with 0 warnings (the RealStack test COMPILES).
    - Hermetic suite still green and unchanged in count for non-RealStack: `dotnet test ... -- --filter-not-trait "Category=RealStack"` passes (the new file is excluded — confirms it does not run hermetically nor break compilation of the rest).
  </acceptance_criteria>
  <done>GateACompositionE2ETests.cs compiles 0-warning under `[Trait("Category","RealStack")][Trait("Phase","58")][Collection("Observability")]`, with the CFG-08 ES-clash-log-FIRST + stably-absent + 422 assertions and the CFG-09 compatible-Healthy + 204 assertions in source. Its live execution is operator-gated (Plan 05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor-badconfig container → compose stack | The deliberately-broken processor runs concurrently; must not hang the stack or poison net-zero. |
| E2E test → host stack (Redis/ES/WebApi) | The test reads liveness + ES + posts orchestration starts across the host boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06 | Denial of Service | processor-badconfig compose tier | mitigate | Profile-gated (`profiles: ["badconfig"]`) → absent from the default stack; when up, Gate A stay-up posture (D-05) keeps it net-zero-harmless (MarkReady flips, no queue bound, no liveness key). Its Docker `/health/ready` passes so compose does not mark it unhealthy / restart-loop. Verified by: compose `config` parse + the unchanged healthcheck. |
| T-58-07 | Repudiation / false-positive proof | CFG-08 absence assertion | mitigate | The ES clash-log poll runs FIRST and is load-bearing (D-06): it proves the container booted + ran Gate A, so "absent liveness" is provably causation, not "container down." Without it, CFG-08 would pass with the container stopped — a false proof. Verified by: acceptance criteria require PollEsForLog + the message text scoped to the badconfig service. |
| T-58-08 | Information Disclosure | clash log content | accept | The Gate A log (shipped) carries only ProcessorId + ConfigSchemaId + structural clash, no config values. The test only asserts the message text is present; it reads no secrets. Low risk. |

No new endpoints, auth, or crypto are introduced (the orchestration-start gate is unchanged; the badconfig service exposes only the existing `/health/ready`). V5 input validation: the clash schema is meta-schema-validated on write by shipped code; `ConfigSchemaCoverageCheck` is SSRF-safe (no `$ref` resolution).
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 with 0 warnings (the RealStack test compiles).
- `docker compose -f compose.yaml config --quiet` exits 0; `processor-badconfig` is profile-gated.
- Hermetic suite green (RealStack tests excluded).
- The CFG-08 test polls ES FIRST, then asserts absence, then 422; CFG-09 asserts compatible-Healthy + 204.
- No live docker stack bring-up required for any autonomous check (the live run is Plan 05).
</verification>

<success_criteria>
- compose.yaml has a profile-gated `processor-badconfig` service (unique container name, BadConfig Dockerfile).
- GateACompositionE2ETests.cs compiles 0-warning with all four traits and the D-06 three-signal CFG-08 assertions + CFG-09 204 assertion.
- Both-config build clean; hermetic suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/58-orchestration-gate-integration-proof-close/58-03-SUMMARY.md`. Record: the compose profile name (`badconfig`) + service/container names, the test class name, the exact ES query used (so Plan 05's runbook references it), and the two sentinel schema Names used (`gateA-sample-compatible`, `gateA-badconfig-clash`).
</output>
