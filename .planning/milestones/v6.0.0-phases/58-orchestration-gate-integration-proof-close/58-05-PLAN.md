---
phase: 58-orchestration-gate-integration-proof-close
plan: 05
type: execute
wave: 3
depends_on: [58-03, 58-04]
files_modified:
  - .planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md
autonomous: false
requirements: [CFG-08, CFG-09]
must_haves:
  truths:
    - "58-HUMAN-UAT.md exists as the operator runbook for the live N=3 GREEN close-gate run"
    - "The runbook bring-up command uses --profile badconfig and rebuilds processor-badconfig"
    - "The runbook DoD ticks CFG-08 and CFG-09 (only after the operator's GREEN run)"
    - "CFG-08's three-signal note (clash log + absent liveness + 422) is recorded as the causation proof"
  artifacts:
    - path: ".planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md"
      provides: "Operator N=3 GREEN-run runbook + record block"
      contains: "profile badconfig"
  key_links:
    - from: "58-HUMAN-UAT.md"
      to: "scripts/phase-58-close.ps1"
      via: "pwsh -File invocation step"
      pattern: "phase-58-close.ps1"
---

<objective>
Author the operator-gated `58-HUMAN-UAT.md` runbook (mirror `55-HUMAN-UAT.md`), then run the live N=3 GREEN close gate. This is the gate that finally ticks CFG-08 and CFG-09 — the autonomous build deliverables (Plans 01-04) ENABLE the proof; the live run IS the proof (D-12). The runbook records triple-SHA BEFORE==AFTER equality, N=3 consecutive GREEN, `skp-dlq-1` depth==0, `skp:msg:*` count==0, and the CFG-08 three-signal causation (clash log + absent liveness + 422).

Purpose: Satisfy D-12 and finalize the v6.0.0 milestone close — CFG-08/CFG-09 stay UNTICKED in REQUIREMENTS.md until the operator records a GREEN run here.

Output: `58-HUMAN-UAT.md` (runbook + record block) and, after the operator's GREEN run, the recorded N=3 result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-CONTEXT.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-PATTERNS.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-03-SUMMARY.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-04-SUMMARY.md
@.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 58-HUMAN-UAT.md by cloning 55-HUMAN-UAT.md with the v6/badconfig deltas</name>
  <read_first>
    - .planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md (the WHOLE runbook — clone its structure: Steps + record block + DoD + frontmatter)
    - .planning/phases/58-orchestration-gate-integration-proof-close/58-04-SUMMARY.md (the EXACT bring-up command + close-script path the runbook references)
    - .planning/phases/58-orchestration-gate-integration-proof-close/58-03-SUMMARY.md (the ES query + sentinel Names for the CFG-08 three-signal note)
  </read_first>
  <action>
    Create `.planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md` cloning `55-HUMAN-UAT.md`'s structure (PATTERNS.md "58-HUMAN-UAT.md" section), with these deltas:
    - **Frontmatter:** `status: pending` (live run is operator-gated — CFG-08/09 stay unticked until the GREEN run is recorded). Retitle to Phase 58 / v6.0.0.
    - **Step 1 (clean build):** `dotnet clean` + `dotnet build SK_P.sln -c Release` (host SourceHash == container hash — D-12; a stale image's embedded hash diverges from the seeded row → liveness false-pass/timeout).
    - **Step 2 (bring-up):** the v6 stack WITH the badconfig profile — `docker compose --profile badconfig up -d --build baseapi-service orchestrator processor-sample keeper processor-badconfig` (use the EXACT command from 58-04-SUMMARY.md).
    - **Step 3 (close gate):** `pwsh -File scripts/phase-58-close.ps1` from a clean redis keyspace (BEFORE-dirty trap — start clean; reference the close-gate net-zero protocol).
    - **Step 4 (record block):** the three SHA values (psql/redis/rabbitmq) BEFORE==AFTER, N=3 Passed count, `skp-dlq-1` depth==0, `skp:msg:*` count==0 — mirror 55's record table.
    - **CFG-08 three-signal note:** add a callout that CFG-08's proof is the trio — (a) the Gate A clash log in Elasticsearch scoped to `service.name == processor-badconfig` (causation), (b) `skp:{badId}` stably absent (mechanism), (c) orchestration-start 422 (outcome) — asserted by `GateACompositionE2ETests` during the full-suite run; CFG-09 by Gate-A-pass + `skp:{sampleId}` present + 204.
    - **DoD:** ticks **CFG-08 / CFG-09** (NOT TEST-01/TEST-02), gated on N=3 GREEN + triple-SHA equality + both-config 0-warning. Note these remain unticked in REQUIREMENTS.md until the operator records the GREEN run.
  </action>
  <verify>
    <automated>test -f .planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md && grep -q "phase-58-close.ps1" .planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md && grep -q "profile badconfig" .planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md` exists with `status: pending` frontmatter.
    - References the close script: `grep -q "phase-58-close.ps1" 58-HUMAN-UAT.md`.
    - Bring-up uses the profile: `grep -q "profile badconfig" 58-HUMAN-UAT.md` AND `grep -q "processor-badconfig" 58-HUMAN-UAT.md`.
    - DoD ticks the right reqs: `grep -q "CFG-08" 58-HUMAN-UAT.md` AND `grep -q "CFG-09" 58-HUMAN-UAT.md`.
    - The CFG-08 three-signal causation note is present (clash log + absent liveness + 422): `grep -qi "Gate A" 58-HUMAN-UAT.md` AND `grep -q "422" 58-HUMAN-UAT.md`.
    - A record block exists for the triple-SHA / N=3 / DLQ depth / msg count values.
  </acceptance_criteria>
  <done>58-HUMAN-UAT.md exists, status pending, referencing phase-58-close.ps1 + the `--profile badconfig` bring-up, with a record block and a DoD that ticks CFG-08/CFG-09 only after the operator's GREEN run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the live N=3 GREEN close gate and records the result</name>
  <what-built>
    The full autonomous deliverable is in place: the Processor.BadConfig project + SK_P.sln entry (Plan 01), the two-schema seed helpers + SC1/2/3 retag (Plan 02), the profile-gated compose service + the COMPILING Gate-A composition E2E (Plan 03), the verbatim-cloned phase-58-close.ps1 with the two-schema/two-processor CREATE-IF-ABSENT seed (Plan 04), and the 58-HUMAN-UAT.md runbook (Task 1). Both-config build is 0-warning and the hermetic suite is green. The only remaining step is the live, ~50 min/run operator gate — it requires the rebuilt v6 docker stack incl. the badconfig profile and cannot be satisfied autonomously (D-12).
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md` exactly:
    1. Start from a CLEAN redis keyspace (BEFORE-dirty trap — flush/verify empty steady-state first).
    2. `dotnet clean` + `dotnet build SK_P.sln -c Release` (and `-c Debug`) — confirm 0 warnings, so the host SourceHash matches the container build.
    3. `docker compose --profile badconfig up -d --build baseapi-service orchestrator processor-sample keeper processor-badconfig` (rebuild stale images — close-gate trap).
    4. `pwsh -File scripts/phase-58-close.ps1` — expect N=3 consecutive GREEN, triple-SHA (psql/redis/rabbitmq) BEFORE==AFTER, `skp-dlq-1` depth==0, `skp:msg:*` count==0.
    5. Confirm CFG-08's three signals fired during the full-suite run (Gate A clash log in ES scoped to `processor-badconfig`, `skp:{badId}` stably absent, orchestration-start 422) and CFG-09 (Sample Gate-A-pass → `skp:{sampleId}` present → 204).
    6. Record the SHA values, N=3 Passed count, DLQ depth, and msg count in the runbook's record block; flip its `status:` to `passed`; tick CFG-08 / CFG-09 in REQUIREMENTS.md.
    If any run is RED or any SHA mismatches, capture the failure detail in the runbook and return for gap closure rather than ticking the requirements.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded N=3 GREEN result (3 SHA values BEFORE==AFTER + Passed count + DLQ depth==0 + msg count==0), or describe the RED/mismatch for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live v6 stack | The operator brings up real containers + runs the destructive-then-net-zero close gate; a stale image or dirty keyspace would invalidate the proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-12 | Spoofing (identity divergence) | rebuilt v6 images | mitigate | The runbook mandates `--build` + a clean `dotnet build` first so the container's embedded SourceHash matches the seeded row; a stale image is the documented failure mode (D-12). Verified by: Step 1+2 in the runbook + the close script's `^[a-f0-9]{64}$` hash validation. |
| T-58-13 | Repudiation (false GREEN) | net-zero snapshot integrity | mitigate | The runbook requires starting from a CLEAN redis keyspace (BEFORE-dirty trap) and N=3 identical-fact-count consecutive GREEN, so a one-off coincidence cannot pass. CFG-08's ES clash-log requirement prevents an absence-coincidence false GREEN. Verified by: the close script's N=3 Smell-A guard + the runbook's clean-keyspace precondition. |
| T-58-14 | DoS | processor-badconfig under the profile | accept | Gate A stay-up posture keeps it net-zero-harmless (proven by the autonomous plans); the live run is bounded (~50 min) and operator-supervised. No mitigation beyond the shipped stay-up design. |

This plan adds NO product code and NO new attack surface — it is documentation + an operator-supervised live run of already-shipped code.
</threat_model>

<verification>
- 58-HUMAN-UAT.md exists (status pending), references phase-58-close.ps1 + the `--profile badconfig` bring-up, has a record block, and ticks CFG-08/CFG-09 in its DoD.
- The operator checkpoint records N=3 GREEN + triple-SHA equality + DLQ depth==0 + msg count==0 before CFG-08/CFG-09 are ticked.
- CFG-08's three-signal causation (clash log + absent liveness + 422) is confirmed during the live run.
</verification>

<success_criteria>
- 58-HUMAN-UAT.md is the operator runbook with the v6/badconfig deltas and a record block.
- The live N=3 GREEN run is recorded; CFG-08/CFG-09 ticked only after a GREEN result.
- The v6.0.0 milestone close gate holds (triple-SHA net-zero, both-config 0-warning).
</success_criteria>

<output>
After the operator records GREEN, create `.planning/phases/58-orchestration-gate-integration-proof-close/58-05-SUMMARY.md`. Record: the recorded SHA values, N=3 Passed count, DLQ depth, msg count, and confirmation CFG-08/CFG-09 were ticked in REQUIREMENTS.md.
</output>
