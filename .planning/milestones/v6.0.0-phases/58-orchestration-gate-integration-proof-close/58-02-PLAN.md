---
phase: 58-orchestration-gate-integration-proof-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs
  - tests/BaseApi.Tests/Orchestrator/SC1RoundTripE2ETests.cs
  - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
  - tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs
autonomous: true
requirements: [CFG-09]
must_haves:
  truths:
    - "A reusable SeedConfigSchemaAsync helper GET-or-creates a config-schema by sentinel Name (never PUT)"
    - "SeedProcessorAsync can seed Processor.Sample with a compatible non-null ConfigSchemaId (Gate A runs and passes)"
    - "SC1/SC2/SC3 carry [Trait(\"Phase\",\"58\")] so they run in the phase-58 live close gate"
    - "The hermetic suite still compiles and passes after the seed-helper + retag edits"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs"
      provides: "SeedConfigSchemaAsync GET-or-create helper + compatible-schema seed path for Sample"
      contains: "SeedConfigSchemaAsync"
  key_links:
    - from: "SampleRoundTripE2ETests.cs SeedConfigSchemaAsync"
      to: "/api/v1/schemas"
      via: "GET-all-filter-by-Name then POST-if-absent"
      pattern: "GetFromJsonAsync<List<SchemaReadDto>>.*schemas"
    - from: "SC1/SC2/SC3 trait"
      to: "phase-58 close gate"
      via: "[Trait(\"Phase\",\"58\")]"
      pattern: "Trait\\(\"Phase\", ?\"58\"\\)"
---

<objective>
Extend the RealStack E2E seed surface so Gate A can be exercised compatibly, and retag the v5 recovery suite into the phase-58 live run. Add a reusable `SeedConfigSchemaAsync` GET-or-create-by-Name helper (schemas have NO uniqueness constraint, so the seed must filter-by-Name then POST-if-absent — never PUT, which 409s on a frozen-once-referenced schema), and make `SeedProcessorAsync` capable of passing a compatible non-null `ConfigSchemaId` for `Processor.Sample` (flipping today's `ConfigSchemaId: null` so Gate A RUNS AND PASSES — CFG-09's requirement, not Gate-A-skipped). Mechanically retag SC1/SC2/SC3 `[Trait("Phase","55")]` → `[Trait("Phase","58")]` so the milestone close is full regression.

Purpose: Provide the idempotent two-schema seed primitives (D-09a, D-13) the new Gate-A E2E (Plan 03) consumes, and seal the v5 recovery suite into the v6 close gate (D-07).

Output: An extended `SampleRoundTripE2ETests.cs` (new helper + compatible-seed path) and three 1-line trait retags. All edits compile and pass the hermetic suite WITHOUT the live stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-CONTEXT.md
@.planning/phases/58-orchestration-gate-integration-proof-close/58-PATTERNS.md

<interfaces>
<!-- DTOs the seed helpers POST/parse. Field ORDER is load-bearing (positional records). -->

SchemaCreateDto — src/BaseApi.Service/Features/Schema/SchemaDtos.cs (VERIFIED field order):
  (string Name, string Version, string? Description, JsonElement Definition)   // POST /api/v1/schemas
SchemaReadDto exposes: Id, Name, Version, Description, Definition, ...          // GET /api/v1/schemas (list)

ProcessorCreateDto — src/BaseApi.Service/Features/Processor/ProcessorDtos.cs (VERIFIED field order):
  (string Name, string Version, string? Description, string SourceHash,
   Guid? InputSchemaId, Guid? OutputSchemaId, Guid? ConfigSchemaId)            // ConfigSchemaId is the CFG-09 delta
ProcessorReadDto exposes: Id, ...

Endpoints:
  GET  /api/v1/processors/by-source-hash/{sourceHash}   // 200 → reuse Id, 404 → POST (uq_processor_source_hash idempotency)
  GET  /api/v1/schemas                                   // list (NO unique constraint → filter-by-Name for idempotency)
  POST /api/v1/schemas                                   // create
  Frozen-once-referenced: PUT a referenced schema's Definition → 409 (SchemaService.cs). NEVER PUT.

Compatible schema definition for Sample's CFG-09 seed (SampleConfig(string? Value) covers it):
  { "$schema": "https://json-schema.org/draft/2020-12/schema", "type": "object",
    "properties": { "value": { "type": "string" } } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SeedConfigSchemaAsync GET-or-create helper and a compatible-ConfigSchemaId seed path for Processor.Sample</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the WHOLE harness — but focus on SeedProcessorAsync at lines 300-330; the `ConfigSchemaId: null` POST is at ~line 325; the ProcessorCreateDto field order at the call site)
    - src/BaseApi.Service/Features/Schema/SchemaDtos.cs (SchemaCreateDto / SchemaReadDto field order — confirm Definition is the 4th param and its CLR type)
    - src/BaseApi.Service/Features/Schema/SchemaController.cs (the 5 inherited CRUD verbs at /api/v1/schemas — confirm GET-list shape)
    - src/Processor.Sample/SampleConfig.cs (SampleConfig(string? Value) — the type the compatible schema must be covered by)
  </read_first>
  <action>
    In `SampleRoundTripE2ETests.cs`, add a NEW private static helper and a compatible-seed path (PATTERNS.md "two-schema seed helpers", RESEARCH Pattern 2 / D-09a):

    1. **`SeedConfigSchemaAsync(HttpClient client, string sentinelName, JsonElement definition, CancellationToken ct)`** — GET-all-then-filter-by-Name, reuse-or-POST, NEVER PUT:
       ```csharp
       // Schemas have NO uniqueness constraint (only FK indexes) → a blind POST duplicates every run.
       // GET the list, match a fixed sentinel Name, reuse its Id; POST only if absent.
       // A referenced schema's Definition is FROZEN (PUT → 409, Phase-57 D-06 / SchemaService.cs) — CREATE-IF-ABSENT only.
       var all = await client.GetFromJsonAsync<List<SchemaReadDto>>("/api/v1/schemas", ct);
       var existing = all!.FirstOrDefault(s => s.Name == sentinelName);
       if (existing is not null) return existing.Id;
       var dto = new SchemaCreateDto(sentinelName, "1.0.0", null, definition);
       var resp = await client.PostAsJsonAsync("/api/v1/schemas", dto, ct);
       resp.EnsureSuccessStatusCode();
       return (await resp.Content.ReadFromJsonAsync<SchemaReadDto>(cancellationToken: ct))!.Id;
       ```
       Match the file's existing JSON/serializer conventions (use the same `JsonSerializerOptions`/`PostAsJsonAsync` overloads already in the file). Use the VERIFIED `SchemaCreateDto` field order `(Name, Version, Description, Definition)`.

    2. **Compatible-seed path for `Processor.Sample`** — extend `SeedProcessorAsync` so the `ConfigSchemaId` it passes in the `ProcessorCreateDto` POST can be a non-null compatible schema Id (today it is hardcoded `null` at ~line 325). Add a parameter (e.g. `Guid? configSchemaId = null`) so existing callers are unaffected, and pass it through to the `ProcessorCreateDto` `ConfigSchemaId` field (VERIFIED 7th positional field). Do NOT change the GET-by-source-hash idempotency branch — only the POST body's ConfigSchemaId.

    3. Add a const sentinel name for the compatible schema, e.g. `private const string SampleCompatibleSchemaName = "gateA-sample-compatible";`, and the compatible definition JSON `{ "type":"object", "properties": { "value": { "type":"string" } } }` (with the `$schema` draft key), so Plan 03's Gate-A test and Plan 04's close script use the SAME sentinel name (idempotent across the live N=3 run).

    Keep this an ADDITIVE change — `SampleRoundTripE2ETests`'s own test methods must continue to pass unchanged (they can keep seeding `configSchemaId: null` via the defaulted parameter, OR be flipped to the compatible schema; flipping is OPTIONAL here — the load-bearing deliverable is the reusable helper + the compatible-capable seed path, which Plan 03's NEW Gate-A CFG-09 test consumes).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "SeedConfigSchemaAsync" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs`.
    - `grep -q "gateA-sample-compatible" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs`.
    - The helper GETs the list and filters by Name (`grep -q "FirstOrDefault" ...`) and never calls PUT (`! grep -q "PutAsJsonAsync.*schemas" tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs`).
    - `SeedProcessorAsync` accepts/passes a `configSchemaId` through to ProcessorCreateDto (the POST body's ConfigSchemaId is no longer hardcoded null at the call site).
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 with 0 warnings.
    - Hermetic suite passes: `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release -- --filter-not-trait "Category=RealStack"` (RealStack E2E excluded; everything else green — confirms the additive change broke no hermetic test).
  </acceptance_criteria>
  <done>A reusable `SeedConfigSchemaAsync` GET-or-create-by-Name helper exists, `SeedProcessorAsync` can seed a compatible non-null ConfigSchemaId, the sentinel name `gateA-sample-compatible` is shared, and the hermetic suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Retag SC1/SC2/SC3 RealStack suite into the phase-58 close gate</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SC1RoundTripE2ETests.cs (line 72 — `[Trait("Phase", "55")]`; also the XML-doc ref at line 30)
    - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs (line 76 — `[Trait("Phase", "55")]`)
    - tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs (line 96 — `[Trait("Phase", "55")]`; XML-doc ref at line 20)
  </read_first>
  <action>
    Mechanical 1-line trait edit per file (PATTERNS.md "SC1/SC2/SC3 retag", D-07). v6 left the v5 recovery/slot-array machinery UNCHANGED — this is a tag-only change, NO behavior change:
    - SC1RoundTripE2ETests.cs line 72: `[Trait("Phase", "55")]` → `[Trait("Phase", "58")]`.
    - SC2RecoveryPathsE2ETests.cs line 76: `[Trait("Phase", "55")]` → `[Trait("Phase", "58")]`.
    - SC3PauseResumeOutageE2ETests.cs line 96: `[Trait("Phase", "55")]` → `[Trait("Phase", "58")]`.
    Leave `[Trait("Category", "RealStack")]` on all three UNCHANGED (still excluded from the hermetic run, still included in the live close gate). Optionally update the XML-doc comment "Phase 55"/"Phase-55" references (SC1:30, SC3:20) to Phase 58 for accuracy — cosmetic, not load-bearing.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Trait("Phase", "58")' tests/BaseApi.Tests/Orchestrator/SC1RoundTripE2ETests.cs tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs` returns 1 per file.
    - No `[Trait("Phase", "55")]` remains in any of the three files (`! grep -q 'Trait("Phase", "55")' <each file>`).
    - `[Trait("Category", "RealStack")]` still present on all three.
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 with 0 warnings.
  </acceptance_criteria>
  <done>SC1/SC2/SC3 carry `[Trait("Phase","58")]` (RealStack trait retained), so the phase-58 live close gate runs the full v5 recovery regression alongside the new Gate-A composition proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test seed → WebApi schema/processor CRUD | The seed crosses the in-proc HTTP boundary; idempotency must not churn close-gate state across the N=3 run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | Tampering (state churn) | schema seed idempotency | mitigate | The seed GET-all-filter-by-Name then POST-if-absent (never PUT). Schemas have no unique constraint, so a blind POST would accumulate rows and churn the close-gate net-zero snapshot. Mitigated by the GET-or-create-by-sentinel-Name pattern; PUT (which 409s on frozen schemas) is forbidden. Verified by: acceptance criteria assert FirstOrDefault filter + no PUT. |
| T-58-05 | (none new) | SC retag | accept | A trait-only edit changes no behavior, no input parsing, no surface. No threat. |

No new endpoints, auth, crypto, or input-validation surface introduced by this plan (the seed uses existing CRUD; the schema Definition is operator/test-authored and meta-schema-validated on write by shipped code).
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 with 0 warnings.
- Hermetic suite green: `dotnet test ... -- --filter-not-trait "Category=RealStack"`.
- `SeedConfigSchemaAsync` GET-or-creates by Name and never PUTs; `SeedProcessorAsync` can pass a non-null ConfigSchemaId.
- SC1/SC2/SC3 carry `[Trait("Phase","58")]`, RealStack trait retained.
- No live docker stack required for any check in this plan.
</verification>

<success_criteria>
- A reusable `SeedConfigSchemaAsync` GET-or-create-by-Name helper exists and the sentinel name `gateA-sample-compatible` is defined.
- `SeedProcessorAsync` can seed a compatible non-null ConfigSchemaId for Processor.Sample.
- SC1/SC2/SC3 retagged `[Trait("Phase","58")]`.
- Hermetic suite green, both-config compile clean.
</success_criteria>

<output>
After completion, create `.planning/phases/58-orchestration-gate-integration-proof-close/58-02-SUMMARY.md`. Record: the new helper's signature, the sentinel name constant(s) and the compatible definition JSON (so Plan 03 + Plan 04 reuse the exact same sentinel Name), and the seed-path parameter added to SeedProcessorAsync.
</output>
