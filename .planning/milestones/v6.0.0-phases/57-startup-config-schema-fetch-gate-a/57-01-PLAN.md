---
phase: 57-startup-config-schema-fetch-gate-a
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Processor/ConfigSchemaCoverageFacts.cs
  - tests/BaseApi.Tests/Features/Schema/SchemaDefinitionFreezeFacts.cs
  - tests/BaseApi.Tests/Processor/SchemaResolutionFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs
autonomous: true
requirements: [CFG-03, CFG-04, CFG-05, CFG-06, CFG-07, CFG-10]
must_haves:
  truths:
    - "The 3 high-risk STJ verdicts (string-enum→CLR-enum, number→int, null→non-nullable value-type) are confirmed against the REAL ProcessorConfig.SerializerOptions by a runnable Deserialize spike"
    - "The covers-check test file exists and is RED (asserts against a not-yet-existing ConfigSchemaCoverageCheck.Evaluate)"
    - "The freeze integration test exists and is RED (asserts 409 on a frozen-Definition mutation against the real WebAPI)"
    - "The Loop-B config-fetch facts are inverted to expect config IS queried + ConfigDefinition set"
    - "The bind-sequence facts have a clash case asserting log == [\"ready\"] only and a null-skip case"
  artifacts:
    - path: "tests/BaseApi.Tests/Processor/ConfigSchemaCoverageFacts.cs"
      provides: "Table-driven Gate A covers tests + A1/A2/A3 Deserialize spike rows"
      contains: "ConfigSchemaCoverageCheck"
    - path: "tests/BaseApi.Tests/Features/Schema/SchemaDefinitionFreezeFacts.cs"
      provides: "WebApplicationFactory 409 freeze test recording the TOCTOU mechanism"
      contains: "HttpStatusCode.Conflict"
  key_links:
    - from: "ConfigSchemaCoverageFacts.cs (spike rows)"
      to: "ProcessorConfig.SerializerOptions"
      via: "JsonSerializer.Deserialize<T>(json, ProcessorConfig.SerializerOptions)"
      pattern: "Deserialize.*SerializerOptions"
---

<objective>
Wave 0 — lock the STJ rule table by running the BLOCKING spike, and stand up every test seam RED so Waves 1–2 implement against committed contracts.

Purpose: The Gate A "covers" rule table (RESEARCH §STJ Type-Clash Rule Table) carries three `[ASSUMED]` verdicts (rows #13/#5/#22) whose wrongness makes Gate A false-block real configs. This plan runs a one-time `Deserialize` spike against the EXACT `ProcessorConfig.SerializerOptions` instance to confirm or correct those verdicts BEFORE the covers-checker (Plan 02) is written. It also creates/inverts/extends all test files so they are RED, satisfying Nyquist (every later task has an automated verify that already exists).

Output: Two NEW test files (`ConfigSchemaCoverageFacts`, `SchemaDefinitionFreezeFacts`) RED; two MODIFIED test files (`SchemaResolutionFacts` inverted, `DispatchBindSequenceFacts` extended) RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-startup-config-schema-fetch-gate-a/57-CONTEXT.md
@.planning/phases/57-startup-config-schema-fetch-gate-a/57-RESEARCH.md
@.planning/phases/57-startup-config-schema-fetch-gate-a/57-PATTERNS.md

<interfaces>
<!-- The binding contract Gate A models — the EXACT cached options instance. -->
From src/BaseProcessor.Core/Configuration/ProcessorConfig.cs:18-22:
```csharp
public abstract record ProcessorConfig
{
    public static readonly JsonSerializerOptions SerializerOptions = new()
    {
        PropertyNameCaseInsensitive = true,   // D-05
        // default unknown-member handling = ignore (do NOT set JsonUnmappedMemberHandling.Disallow) — D-05
    };
}
```

<!-- The covers-checker signature Plan 02 will create — tests assert against THIS. -->
Planned (Plan 02): `internal static class ConfigSchemaCoverageCheck` in namespace `BaseProcessor.Core.Configuration`
with `public static (bool Covered, string? ClashDetail) Evaluate(string? configDefinition, Type configType)`.
null configDefinition → (true, null) [CFG-07 skip]. Unparseable definition → (false, "<reason>") [terminal clash].

<!-- Existing harness fakes to reuse (do NOT rewrite). -->
From tests/BaseApi.Tests/Processor/SchemaResolutionFacts.cs:38-58: CapturingSchemaResponder + SchemaCapture.NextIsNotFound (one leading NotFound per Id → Found) — already proves CFG-04 per-Id retry.
From tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs:35-95: RecordingConnector (exposes BoundQueueName), RecordingHandle (Ready appends "ready"), RecordingContext (appends "markhealthy", proxies _inner ProcessorContext).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STJ spike — confirm rows #13/#5/#22 against the real SerializerOptions, and create ConfigSchemaCoverageFacts RED</name>
  <files>tests/BaseApi.Tests/Processor/ConfigSchemaCoverageFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/SchemaResolutionFacts.cs (xUnit v3 idiom: `[Fact]`, `TestContext.Current.CancellationToken`, file-scoped namespace, sealed class — MIRROR exactly; this new file needs NO harness)
    - .planning/phases/57-startup-config-schema-fetch-gate-a/57-RESEARCH.md §"STJ Type-Clash Rule Table" (rows #1–#26) and §"Open Questions" 1–3 (the A1/A2/A3 spike targets)
    - src/BaseProcessor.Core/Configuration/ProcessorConfig.cs:18-22 (the options instance under test)
  </read_first>
  <action>
Create `tests/BaseApi.Tests/Processor/ConfigSchemaCoverageFacts.cs`, namespace `BaseApi.Tests.Processor`, `public sealed class ConfigSchemaCoverageFacts`. `using System.Text.Json;`, `using BaseProcessor.Core.Configuration;`, `using Xunit;`.

PART A — the spike (3 `[Fact]` methods, NO dependency on ConfigSchemaCoverageCheck — these run GREEN immediately and GROUND the rule table). Each declares a local positional `record` inheriting `ProcessorConfig` and calls `JsonSerializer.Deserialize<T>(json, ProcessorConfig.SerializerOptions)`. Record the OBSERVED behavior as the assertion; if any differs from RESEARCH's assumption, the executor MUST update the rule-table verdict cited in Plan 02's action before implementing:
  - `Spike_A1_StringEnumSchema_To_ClrEnum` (row #13): `enum E { A, B }`, `record C(E Mode) : ProcessorConfig`. Assert `JsonSerializer.Deserialize<C>("{\"Mode\":\"A\"}", ProcessorConfig.SerializerOptions)` THROWS `JsonException` (confirms #13 CLASH — no JsonStringEnumConverter registered). Use `Assert.Throws<JsonException>(...)`.
  - `Spike_A2a_NumberSchema_To_Int` (row #5): `record C(int N) : ProcessorConfig`. Assert `Deserialize<C>("{\"N\":3.14}", ...)` THROWS `JsonException` (fractional → int). And `Spike_A2b_StringToNumber` (row #8): assert `Deserialize<C>("{\"N\":\"abc\"}", ...)` THROWS (no NumberHandling.AllowReadingFromString).
  - `Spike_A3_Null_To_NonNullableValueType` (row #22): `record C(int N) : ProcessorConfig`. Assert `Deserialize<C>("{\"N\":null}", ...)` THROWS `JsonException`.

PART B — the covers-check tests (table-driven, RED — reference `ConfigSchemaCoverageCheck.Evaluate` which does NOT exist yet, so the file will not COMPILE; that is the RED state for a not-yet-created type). Add `[Theory]` + `[MemberData]` rows mirroring the rule table. Each row: a schema-definition `string` (Draft 2020-12 JSON), a `Type` (the local config record), and an `expectedCovered` bool. Minimum required rows (cite the rule-table # in a comment per row):
  - #7 string↔string → covered=true; #1 integer↔int → true; #14 string-enum↔string → true; #21 nullable-union↔int? → true; #23 no-type → true; #24 schema-only prop → true; #25 TConfig-only prop → true; #19 nested object recurse (both-present, compatible) → true; #16 array↔List<T> recurse → true.
  - #13 string-enum↔CLR-enum → false; #5 number↔int → false; #8 string↔int → false; #17 array↔scalar → false; #20 object↔scalar → false; #22 nullable-union↔non-nullable int → false; #26 union[string,integer]↔int → false; nested clash (object whose inner both-present prop clashes) → false; unparseable definition string → false.
  Assertion body: `var (covered, detail) = ConfigSchemaCoverageCheck.Evaluate(definition, type); Assert.Equal(expectedCovered, covered); if (!expectedCovered) Assert.NotNull(detail);`.

Trait the spike facts so they always run in the hermetic suite (no `[Trait("Category","RealStack")]`). DO NOT delete the spike facts after Plan 02 — they are the permanent ground-truth for the rule table.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~ConfigSchemaCoverageFacts.Spike"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Processor/ConfigSchemaCoverageFacts.cs` exists.
    - grep finds exactly the 4 spike method names: `Spike_A1_StringEnumSchema_To_ClrEnum`, `Spike_A2a_NumberSchema_To_Int`, `Spike_A2b_StringToNumber`, `Spike_A3_Null_To_NonNullableValueType`.
    - grep finds `JsonSerializer.Deserialize` AND `ProcessorConfig.SerializerOptions` in every spike method body.
    - The 4 spike `[Fact]`s pass: `dotnet test --filter "FullyQualifiedName~ConfigSchemaCoverageFacts.Spike"` → 4 passed.
    - grep finds `ConfigSchemaCoverageCheck.Evaluate` referenced by the `[Theory]` body (proves the RED contract is wired to the Plan-02 signature).
    - The `[Theory]` rows include both a covered=true case AND a covered=false case for the string-enum row #13 path (the highest-value clash).
    - SUMMARY records, for each of A1/A2/A3: "confirmed CLASH per RESEARCH" OR "CORRECTED to FINE — Plan 02 rule-table row #N verdict must change."
  </acceptance_criteria>
  <done>The 3 high-risk verdicts are empirically grounded; ConfigSchemaCoverageFacts spike facts GREEN; the [Theory] is RED against the not-yet-existing checker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SchemaDefinitionFreezeFacts RED (CFG-10 integration) + record the TOCTOU mechanism</name>
  <files>tests/BaseApi.Tests/Features/Schema/SchemaDefinitionFreezeFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Observability/SchemasMetricsE2ETests.cs:58-80 (the `IClassFixture<...WebAppFactory>` + `_factory.CreateClient()` + `PostAsJsonAsync("/api/v1/schemas", dto, ct)` + `Assert.Equal(HttpStatusCode.Created, resp.StatusCode)` idiom — MIRROR for PUT)
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs:30-249 (real AppDbContext + 5 entity controllers; `_factory.Services` resolves a scope for seeding a ProcessorEntity; route is `api/v1/schemas`)
    - src/BaseApi.Service/Features/Schema/SchemaDtos.cs:21-25 (`SchemaUpdateDto(Name, Version, Description, Definition)`) and :10-14 (`SchemaCreateDto`)
    - src/BaseApi.Service/Features/Schema/SchemaEntity.cs (`Definition` jsonb) and src/BaseApi.Service/Features/Processor/ — ProcessorEntity has `InputSchemaId`/`OutputSchemaId`/`ConfigSchemaId` nullable Guid FKs (RESEARCH A5/Pitfall 6)
  </read_first>
  <action>
Create `tests/BaseApi.Tests/Features/Schema/SchemaDefinitionFreezeFacts.cs`, namespace `BaseApi.Tests.Features.Schema`, `public sealed class SchemaDefinitionFreezeFacts : IClassFixture<Phase8WebAppFactory>` (mirror the SchemasMetrics fixture-injection ctor). `using System.Net;`, `using System.Net.Http.Json;`, `using BaseApi.Service.Features.Schema;`, `using Xunit;`.

These tests assert the Plan-04 behavior that does NOT exist yet (no override → today a referenced-schema Definition edit returns 200), so the freeze facts will FAIL = RED.

Three `[Fact]`s (use `_factory.CreateClient()` + `TestContext.Current.CancellationToken`):
  1. `Frozen_Definition_Mutation_Returns_409`: POST a schema (Created → capture id). Seed a `ProcessorEntity` whose `ConfigSchemaId == id` via a DI scope on `_factory.Services` (resolve the AppDbContext / `BaseDbContext`, add a minimal valid ProcessorEntity, SaveChanges). PUT `/api/v1/schemas/{id}` with a `SchemaUpdateDto` carrying a DIFFERENT `Definition`. Assert `HttpStatusCode.Conflict` (409); assert the response body is RFC-7807 (`Content-Type` `application/problem+json`, body contains `"status":409`); assert the `X-Correlation-Id` request header is echoed back / `correlationId` present in the problem body (mirror the customizer at ErrorHandlingServiceCollectionExtensions.cs:31-33).
  2. `NameDescription_Edit_On_Referenced_Schema_Returns_200`: same seeding; PUT with the SAME `Definition` but a changed `Name`+`Description`. Assert `HttpStatusCode.OK` (or 200-family the controller returns) — D-07 only `Definition` is frozen.
  3. `Unreferenced_Draft_Definition_Edit_Returns_200`: POST a schema, do NOT seed any referencing ProcessorEntity, PUT a changed `Definition`. Assert success (draft is editable until referenced — D-06).

Add a class-level XML doc paragraph RECORDING the chosen TOCTOU mechanism for ROADMAP SC-5: "frozen-once-referenced — a schema's Definition locks the moment any ProcessorEntity FK (Config/Input/Output SchemaId) references it; enforced in SchemaService.UpdateAsync → SchemaDefinitionFrozenException → 409 (D-05/D-06/D-08)."
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Features/Schema/SchemaDefinitionFreezeFacts.cs` exists.
    - grep finds all 3 method names: `Frozen_Definition_Mutation_Returns_409`, `NameDescription_Edit_On_Referenced_Schema_Returns_200`, `Unreferenced_Draft_Definition_Edit_Returns_200`.
    - grep finds `HttpStatusCode.Conflict` AND `PutAsJsonAsync` AND `IClassFixture<Phase8WebAppFactory>`.
    - grep finds the string `frozen-once-referenced` in the class XML doc (records the SC-5 mechanism).
    - File COMPILES (`dotnet build tests/BaseApi.Tests` succeeds — the test references only existing types: SchemaUpdateDto, Phase8WebAppFactory, the schemas route).
    - Running the freeze facts FAILS today (`Frozen_Definition_Mutation_Returns_409` gets 200 not 409) — confirm RED and note it in SUMMARY. Do NOT make it green here.
  </acceptance_criteria>
  <done>The freeze integration facts exist, compile, are RED (409 expectation unmet pre-Plan-04), and the TOCTOU mechanism is recorded in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Invert SchemaResolutionFacts (config now fetched) + extend DispatchBindSequenceFacts (clash + null-skip) RED</name>
  <files>tests/BaseApi.Tests/Processor/SchemaResolutionFacts.cs, tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/SchemaResolutionFacts.cs (FULL — :117-151 is the fact to invert; :145 `Assert.DoesNotContain(configId, queried)`; reuse the unchanged harness :60-115)
    - tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs (FULL — :97-105 pass-path ordered-log assertion; :122-179 `DriveOrchestratorToHealthy` driver; RecordingConnector/Handle/Context :35-95)
    - .planning/phases/57-.../57-RESEARCH.md §"State of the Art" (the :145 inversion is a PLANNED change, not a regression) and §"Validation Architecture" → "Per-task" map rows CFG-03/06/07
  </read_first>
  <action>
INVERT `SchemaResolutionFacts.cs`: rename `LoopB_Resolves_Input_And_Output_Never_Config` → `LoopB_Resolves_Input_Output_And_Config` (drop "Never_Config"). Change `:145` `Assert.DoesNotContain(configId, queried)` → `Assert.Contains(configId, queried)`. Add `Assert.Equal($"def-for-{configId:N}", context.ConfigDefinition)` (CFG-03 — the fetched config definition stored on context). The `CapturingSchemaResponder` + `SchemaCapture.NextIsNotFound` already exercise one leading NotFound per Id → reuse proves CFG-04 transient retry for the config Id (the responder replies NotFound once for configId then Found). Update the class XML doc (`:18-24`) to say the config Id IS now queried (D-12 lifts the D-05 carve-out). This references `context.ConfigDefinition` which does NOT exist on ProcessorContext yet → RED.

EXTEND `DispatchBindSequenceFacts.cs`:
  - Parametrize/overload `DriveOrchestratorToHealthy` (or add a sibling driver) to inject a config-schema DEFINITION and the config-schema Id into the identity so Loop B fetches it and Gate A runs. Mirror the existing `ResponderSequence` wiring; set a non-null `ConfigSchemaId` and have the schema responder return a definition that either COVERS or CLASHES with the test config type.
  - Add `GateA_Clash_Withholds_MarkHealthy_And_Bind`: drive with a config definition that CLASHES the processor's TConfig. Assert `Assert.Equal(new[] { "ready" }, log)` (ONLY "ready" — NO "connect", NO "markhealthy"), `Assert.Null(connector.BoundQueueName)`, `Assert.False(context.IsHealthy)`, and exactly one Error-level log captured. Replace the `NullLogger` at `:165` with a capturing `ILogger<ProcessorStartupOrchestrator>` (list-recording fake) and assert one `LogLevel.Error` entry mentioning the processor id + config schema id + the clash property.
  - Add `GateA_NullConfigSchemaId_Skips_And_Reaches_Healthy`: drive with `ConfigSchemaId = null`. Assert `Assert.Equal(new[] { "connect", "ready", "markhealthy" }, log)`, `Assert.NotNull(connector.BoundQueueName)`, `Assert.True(context.IsHealthy)` (CFG-07).
  - Extend `RecordingContext` to add a `string? ConfigDefinition => _inner.ConfigDefinition;` member (proxy) so it compiles once ProcessorContext gains the property — for NOW reference it; the file goes RED until Plan 03 adds the property/clash logic.

NOTE in SUMMARY that these RED states are PLANNED inversions/extensions (RESEARCH §State of the Art), not breakage. Do NOT modify ProcessorStartupOrchestrator/ProcessorContext here — that is Plan 02 (checker) + Plan 03 (wiring).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests -c Debug 2>&1 | Select-String "error CS"</automated>
  </verify>
  <acceptance_criteria>
    - grep `SchemaResolutionFacts.cs` finds `Assert.Contains(configId, queried)` and `context.ConfigDefinition` and the renamed fact `LoopB_Resolves_Input_Output_And_Config`; grep finds NO `Assert.DoesNotContain(configId` remaining.
    - grep `DispatchBindSequenceFacts.cs` finds `GateA_Clash_Withholds_MarkHealthy_And_Bind` and `GateA_NullConfigSchemaId_Skips_And_Reaches_Healthy`.
    - grep finds the clash assertion `new[] { "ready" }` and `connector.BoundQueueName` null-check in the clash fact.
    - grep finds a capturing logger asserting `LogLevel.Error` (NullLogger no longer the only logger in the clash driver).
    - These compile-time references (`context.ConfigDefinition`, the clash behavior) are RED — `dotnet build tests/BaseApi.Tests` reports CS errors for the missing `ConfigDefinition` member (expected; Plan 02/03 resolve them). SUMMARY notes which symbols are intentionally unresolved.
  </acceptance_criteria>
  <done>Existing facts inverted/extended to the post-Gate-A contract; RED on the not-yet-added ConfigDefinition + clash wiring; documented as planned changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → ProcessorConfig.SerializerOptions | The spike feeds attacker-shaped JSON strings into the real deserializer; this is the exact seam the milestone hardens. No production surface in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | STJ spike rows feeding crafted JSON into `Deserialize` | accept | Test-only; the spike GROUNDS the rule table that closes the real Tampering gap (T-56-03). No new production code in this plan. |
| T-57-02 | Information Disclosure | SchemaDefinitionFreezeFacts asserting the 409 body | mitigate | Assert the 409 ProblemDetails carries ONLY the schema Guid + a generic message (mirrors NotFoundException IN-02), never DB internals — the test pins the safe-disclosure contract Plan 04 must meet. |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~ConfigSchemaCoverageFacts.Spike"` → 4 spike facts GREEN (rule table grounded).
- `dotnet build tests/BaseApi.Tests -c Debug` → SchemaDefinitionFreezeFacts COMPILES (RED at runtime); ConfigSchemaCoverageFacts + the inverted/extended facts report ONLY the expected unresolved-symbol CS errors (`ConfigSchemaCoverageCheck`, `ConfigDefinition`).
- SUMMARY records the A1/A2/A3 spike verdicts and any rule-table corrections for Plan 02.
</verification>

<success_criteria>
- The 3 `[ASSUMED]` STJ verdicts are empirically confirmed or corrected (BLOCKING input to Plan 02).
- All four test files are at their post-Gate-A contract and RED on not-yet-built production code.
- Nyquist: every Wave 1/2 task now has a pre-existing automated verify to turn GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/57-startup-config-schema-fetch-gate-a/57-01-SUMMARY.md` — MUST include the A1/A2/A3 spike verdict table (confirmed vs corrected) so Plan 02 locks the rule table correctly.
</output>
