---
phase: 57-startup-config-schema-fetch-gate-a
plan: 03
type: execute
wave: 2
depends_on: [57-01, 57-02]
files_modified:
  - src/BaseProcessor.Core/Identity/IProcessorContext.cs
  - src/BaseProcessor.Core/Identity/ProcessorContext.cs
  - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs
autonomous: true
requirements: [CFG-03, CFG-04, CFG-06, CFG-07]
must_haves:
  truths:
    - "Loop B fetches the non-null ConfigSchemaId definition (3rd iteration) and stores it on context.ConfigDefinition; a missing config definition is transient (retries on NotFound/timeout exactly like input/output)"
    - "On a Gate A clash the processor never reaches Healthy: MarkHealthy is withheld, the dispatch endpoint is NOT bound, gate.MarkReady fires (no K8s crash-loop), and a single Error is logged with processor id + ConfigSchemaId + the clash; terminal, no retry"
    - "On Gate A pass OR null ConfigSchemaId, the processor binds the endpoint, awaits Ready, MarkHealthy, MarkReady — reaching Healthy"
  artifacts:
    - path: "src/BaseProcessor.Core/Identity/ProcessorContext.cs"
      provides: "ConfigDefinition property + SetDefinition 3rd branch"
      contains: "ConfigDefinition"
    - path: "src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs"
      provides: "Loop B config fetch + Gate A call + decoupled MarkReady/MarkHealthy"
      contains: "ConfigSchemaCoverageCheck.Evaluate"
  key_links:
    - from: "ProcessorStartupOrchestrator Loop B"
      to: "context.SetDefinition(ConfigSchemaId, def)"
      via: "foreach over { Input, Output, Config }"
      pattern: "context.ConfigSchemaId"
    - from: "ProcessorStartupOrchestrator (after Loop B, before bind)"
      to: "ConfigSchemaCoverageCheck.Evaluate"
      via: "clash → MarkReady + return (no bind, no MarkHealthy)"
      pattern: "ConfigSchemaCoverageCheck\\.Evaluate"
    - from: "Gate A clash path"
      to: "gate.MarkReady() WITHOUT context.MarkHealthy()"
      via: "decoupled completion (D-09)"
      pattern: "MarkReady"
---

<objective>
Wire Gate A into startup: extend Loop B to fetch the config-schema definition, store it on `ProcessorContext.ConfigDefinition`, run `ConfigSchemaCoverageCheck.Evaluate` after Loop B and before the dispatch bind, and decouple `MarkReady` from `MarkHealthy` so a clash stays up-but-not-Healthy.

Purpose: CFG-03/04 (fetch + transient retry), CFG-06 (clash → never Healthy, terminal, logged, no crash-loop), CFG-07 (null skip). This is the heart of the phase — it turns the Plan-01 inverted/extended facts GREEN.

Output: Three MODIFIED files in `BaseProcessor.Core` (interface getter, context property + SetDefinition branch, orchestrator Loop B + Gate A + decoupling).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-startup-config-schema-fetch-gate-a/57-CONTEXT.md
@.planning/phases/57-startup-config-schema-fetch-gate-a/57-RESEARCH.md
@.planning/phases/57-startup-config-schema-fetch-gate-a/57-PATTERNS.md
@.planning/phases/57-startup-config-schema-fetch-gate-a/57-02-SUMMARY.md

<interfaces>
<!-- The checker from Plan 02 to call. -->
`BaseProcessor.Core.Configuration.ConfigSchemaCoverageCheck.Evaluate(string? configDefinition, Type configType)` → `(bool Covered, string? ClashDetail)`. null definition → (true, null).

<!-- Current orchestrator ctor (10 params) — the TConfig-type wiring decision changes this signature; the two harness tests in Plan 01 construct it directly and must be updated to match. -->
From src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs:51-61:
```csharp
public sealed class ProcessorStartupOrchestrator(
    IRequestClient<GetProcessorBySourceHash> identityClient,
    IRequestClient<GetSchemaDefinition> schemaClient,
    ISourceHashProvider sourceHash, IProcessorContext context, IStartupGate gate,
    IReceiveEndpointConnector endpointConnector, MeterProviderHolder meterProviderHolder,
    IOptions<ProcessorLivenessOptions> options, TimeProvider clock,
    ILogger<ProcessorStartupOrchestrator> logger) : BackgroundService
```

From src/BaseProcessor.Core/Identity/ProcessorContext.cs:74-80 (SetDefinition — independent `if`s):
```csharp
public void SetDefinition(Guid schemaId, string definition)
{
    if (schemaId == InputSchemaId)  InputDefinition  = definition;
    if (schemaId == OutputSchemaId) OutputDefinition = definition;
    // NEW: if (schemaId == ConfigSchemaId) ConfigDefinition = definition;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ConfigDefinition to ProcessorContext + IProcessorContext, route SetDefinition's 3rd branch (CFG-03)</name>
  <files>src/BaseProcessor.Core/Identity/ProcessorContext.cs, src/BaseProcessor.Core/Identity/IProcessorContext.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Identity/ProcessorContext.cs (FULL — :51-54 OutputDefinition is the template; :74-80 SetDefinition; :42 ConfigSchemaId)
    - src/BaseProcessor.Core/Identity/IProcessorContext.cs (FULL — :54-58 OutputDefinition getter is the template; :45 ConfigSchemaId doc to update; :74-80 SetDefinition doc; WR-03 memory-visibility paragraph :21-32)
    - tests/BaseApi.Tests/Processor/SchemaResolutionFacts.cs (the inverted fact asserting `context.ConfigDefinition` — Plan 01)
  </read_first>
  <action>
`ProcessorContext.cs`: add `public string? ConfigDefinition { get; private set; }` immediately after `OutputDefinition` (:54), mirroring it verbatim. In `SetDefinition` (:74-80) add a THIRD independent `if` (NOT else-if) after the OutputSchemaId branch: `if (schemaId == ConfigSchemaId) ConfigDefinition = definition;`. (Independent `if`s are correct: if two roles share an Id, one fetch populates both slots — RESEARCH Pattern 1 edge case, idempotent.)

`IProcessorContext.cs`: add the getter `string? ConfigDefinition { get; }` after `OutputDefinition` (:58) with doc `/// <summary>The resolved config schema definition (null until Loop B resolves it — D-14). WR-03: read after IsHealthy.</summary>`. Update the `ConfigSchemaId` doc at :45 — remove "never resolved to a definition — D-05"; replace with "resolved to a definition by Loop B for Gate A (D-12)". Add `ConfigDefinition` to the WR-03 memory-visibility property list (:24-26). Update the SetDefinition doc (:75-79) to mention the ConfigSchemaId → ConfigDefinition routing.

D-14: do NOT touch `ProcessorLivenessHeartbeat` / the L2 `ProcessorProjection` — ConfigDefinition lives on the context only.
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core -c Debug 2>&1 | Select-String "error CS|warning"</automated>
  </verify>
  <acceptance_criteria>
    - grep `ProcessorContext.cs` finds `public string? ConfigDefinition { get; private set; }` and `if (schemaId == ConfigSchemaId)` inside SetDefinition.
    - grep `IProcessorContext.cs` finds `string? ConfigDefinition { get; }`.
    - grep `IProcessorContext.cs` finds NO remaining "never resolved to a definition" text on the ConfigSchemaId doc.
    - `dotnet build src/BaseProcessor.Core -c Debug` succeeds with 0 warnings.
    - The `RecordingContext.ConfigDefinition` proxy added in Plan 01 now compiles against the real interface member.
  </acceptance_criteria>
  <done>Context carries ConfigDefinition; SetDefinition routes the config schema id; interface + docs updated; D-14 L2 projection untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Loop B to fetch ConfigSchemaId, insert Gate A, decouple MarkReady from MarkHealthy (CFG-03/04/06/07)</name>
  <files>src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs, tests/BaseApi.Tests/Processor/SchemaResolutionFacts.cs, tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs (FULL — :27-31 class doc D-05 carve-out to lift; :124-128 Loop B foreach + null-skip; :136-157 unchanged retry body; :164-184 completion block; :51-61 ctor)
    - src/BaseProcessor.Core/Processing/ — BaseProcessor`1.cs / BaseProcessorServiceCollectionExtensions.cs:87-96 (how BaseProcessor is DI-registered — for the TConfig-type wiring; RESEARCH Pattern 2 note + Pitfall 4)
    - .planning/phases/57-.../57-RESEARCH.md Pattern 2 (the three-path MarkReady decoupling + TConfig resolution) + Pitfall 1 (MarkReady-left-red crash-loop) + Pitfall 4 (no captive scoped instance)
    - tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs (the clash + null-skip facts from Plan 01 this turns GREEN; note the driver constructs the orchestrator directly — any ctor change must be reflected there)
    - .planning/phases/57-.../57-CONTEXT.md D-09/D-11/D-12/D-13 (binding decisions)
  </read_first>
  <action>
EDIT 1 — Loop B 3rd iteration (CFG-03/04, D-12). Change `:125` from `foreach (var schemaId in new[] { context.InputSchemaId, context.OutputSchemaId })` to `foreach (var schemaId in new[] { context.InputSchemaId, context.OutputSchemaId, context.ConfigSchemaId })`. The existing `:127` `if (schemaId is not { } id) continue;` null-skips a null config id (CFG-07 fetch-side). The unchanged retry body (`:136-157`, SchemaDefinitionNotFound/RequestTimeoutException loop) handles CFG-04 verbatim for the config id. Update the Loop-B comment `:124` (`Never read the config schema id (D-05)`) → `Resolve input/output AND config definitions (D-12; the D-05 carve-out is lifted).` Update the class XML doc `:27-31` similarly (the config id is now queried; remove the "NEVER queried (D-05)" sentence).

EDIT 2 — TConfig type resolution (D-01, Pitfall 4). Wire the concrete config Type WITHOUT a captive scoped instance. Choose ONE (record the choice in SUMMARY):
  (a) Inject `IServiceProvider` (or the registered `BaseProcessor`) into the ctor; inside `ExecuteAsync`, resolve the processor and read ONLY `processor.GetType().BaseType!.GenericTypeArguments[0]` (the Type is process-stable; do NOT hold the instance — Pitfall 4). Add the ctor param.
  (b) Add a tiny `IConfigTypeProvider` singleton (mirrors `ISourceHashProvider`) the author host registers, returning the `Type`; inject it. Cleaner DI.
RECOMMENDED (a) reading only `.GetType()`. EITHER WAY: the ctor signature changes → UPDATE the two direct constructions in `SchemaResolutionFacts.cs` (:105-108) and `DispatchBindSequenceFacts.cs` (:162-165) to pass the new dependency (a stub provider/IServiceProvider returning a known TConfig type). Capture the resolved `Type` into a local `var configType = ...;` before the completion block.

EDIT 3 — insert Gate A + decouple (CFG-05 call site, CFG-06, D-09/D-11/D-13). Replace the completion block `:164-184`. AFTER Loop B, BEFORE `var queueName`:
```csharp
var coverage = ConfigSchemaCoverageCheck.Evaluate(context.ConfigDefinition, configType);  // null def → Covered (CFG-07)
if (!coverage.Covered)
{
    logger.LogError(
        "Gate A incompatibility for processor {ProcessorId} config schema {ConfigSchemaId}: {Clash}",
        context.Id, context.ConfigSchemaId, coverage.ClashDetail);   // CFG-06 single Error log (D-10)
    gate.MarkReady();   // K8s readiness green — NO crash-loop (D-09, Pitfall 1). MarkHealthy + bind NOT reached.
    return;             // terminal, no retry (D-11).
}
```
Then the EXISTING pass/skip path is unchanged: bind `ConnectReceiveEndpoint(queueName, ...)`, `await handle.Ready;`, `context.MarkHealthy();`, `gate.MarkReady();`. RESULT: `gate.MarkReady()` fires on BOTH the clash path (before return) and the pass/skip path; `context.MarkHealthy()` + the bind fire ONLY on pass/skip. Add `using BaseProcessor.Core.Configuration;` if not present.

DO NOT crash/throw on a clash. DO NOT leave readiness red on a clash (Pitfall 1). DO NOT retry Gate A (D-11). DO NOT add ConfigDefinition to the heartbeat projection (D-14).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~SchemaResolutionFacts | FullyQualifiedName~DispatchBindSequenceFacts"</automated>
  </verify>
  <acceptance_criteria>
    - grep `ProcessorStartupOrchestrator.cs` finds `context.ConfigSchemaId` inside the Loop B `foreach` array.
    - grep finds `ConfigSchemaCoverageCheck.Evaluate(context.ConfigDefinition` and the `if (!coverage.Covered)` block.
    - grep finds, on the clash path, `gate.MarkReady();` followed by `return;` and a `logger.LogError(` with `{ProcessorId}`, `{ConfigSchemaId}`, `{Clash}` template tokens — and NO `context.MarkHealthy()` between them.
    - grep finds NO remaining "Never read the config schema id" / "NEVER queried (D-05)" text.
    - `SchemaResolutionFacts.LoopB_Resolves_Input_Output_And_Config` GREEN (config queried + ConfigDefinition set + transient retry via NextIsNotFound).
    - `DispatchBindSequenceFacts.GateA_Clash_Withholds_MarkHealthy_And_Bind` GREEN (log == ["ready"], BoundQueueName null, !IsHealthy, one Error log).
    - `DispatchBindSequenceFacts.GateA_NullConfigSchemaId_Skips_And_Reaches_Healthy` GREEN (log == ["connect","ready","markhealthy"], bound).
    - `Connect_Then_Ready_Then_MarkHealthy_In_Order` + `Binds_Bare_IdFormat_QueueName` still GREEN (pass path unregressed).
    - 0-warning `dotnet build src/BaseProcessor.Core` (Debug + Release).
  </acceptance_criteria>
  <done>Loop B fetches the config def; Gate A runs once before the bind; clash → MarkReady-only + Error log + terminal return; pass/skip → bind + MarkHealthy + MarkReady; all CFG-03/04/06/07 facts green; no crash-loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebAPI responder → processor startup (over the bus) | The fetched config-schema Definition crosses the bus into the processor; it is operator-authored + meta-schema-validated on write, but is parsed at startup by Gate A. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05 | Denial of Service | Gate A clash left as readiness-red → K8s crash-loop (Pitfall 1) | mitigate | `gate.MarkReady()` fires on the clash path BEFORE `return` (decoupled from MarkHealthy). The `DispatchBindSequenceFacts` clash case asserts the ordered log is exactly `["ready"]`, proving readiness goes green while the processor stays not-Healthy. |
| T-57-06 | Elevation of Privilege / Tampering | A config-incompatible processor binding its dispatch queue and consuming payloads it cannot safely deserialize | mitigate | On a clash the `ConnectReceiveEndpoint` bind is SKIPPED (the `return` precedes it) — the processor never consumes dispatch payloads. Asserted by `connector.BoundQueueName is null` in the clash fact. |
| T-57-07 | Information Disclosure | The Error log leaking sensitive config values | mitigate | The log carries only processor id, ConfigSchemaId, and the structural clash detail (property name + schema-type vs CLR-type) — never payload values or schema bodies (D-10). |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~SchemaResolutionFacts | FullyQualifiedName~DispatchBindSequenceFacts | FullyQualifiedName~ConfigSchemaCoverageFacts"` → all GREEN.
- `dotnet test tests/BaseApi.Tests --filter-not-trait "Category=RealStack"` → full hermetic suite GREEN (no regression vs the 530-green Phase-56 baseline).
- 0-warning Release + Debug for `src/BaseProcessor.Core`.
</verification>

<success_criteria>
- CFG-03/04: config def fetched + stored; missing def transient (retry).
- CFG-06: clash → withheld MarkHealthy, no bind, MarkReady fires, single Error logged, terminal.
- CFG-07: null ConfigSchemaId → skip → Healthy + bound.
</success_criteria>

<output>
After completion, create `.planning/phases/57-startup-config-schema-fetch-gate-a/57-03-SUMMARY.md` — record the chosen TConfig-type wiring (option a vs b) and the resulting ctor signature change.
</output>
