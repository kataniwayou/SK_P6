---
phase: 81-re-run-the-7-scenario-fault-recovery-sweep-test-01-test-07-o
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified: []
autonomous: false
requirements: [SPEC-5, SPEC-6]
user_setup: []

must_haves:
  truths:
    - "The k8s capstone sweep runs all 7 scenarios TEST-01..07 and emits per-scenario + roll-up verdicts"
    - "The roll-up reaches 7/7 VERDICT_PASS with zero VERDICT_FAIL (INCONCLUSIVE exit-2 re-runs permitted)"
    - "The analyzer / PassFailEngine / metric-gate source is unchanged by Phase 81 (reused verbatim — SPEC req 5)"
    - "TEST-08/09/10 and FALSIFY-01/02 are NOT run by this sweep"
  artifacts:
    - path: "analyzer-reports/phase-81-summary.json"
      provides: "the 7-scenario roll-up: verdict + zeroMissing + effectOnce + harnessExit per scenario"
      contains: "TEST-07"
  key_links:
    - from: "phase-81-sweep.ps1 run"
      to: "analyzer-reports/phase-81-summary.json"
      via: "7 child harness runs + roll-up; exit 0 IFF 7/7 PASS"
      pattern: "phase-81-summary.json"
---

<objective>
Run the k8s capstone sweep and gate the phase on **7/7 VERDICT_PASS**. Verify (a) the analyzer / `PassFailEngine` / metric-gate source was reused verbatim (unchanged by Phase 81 — SPEC req 5), (b) the sweep executes exactly the 7 capstone scenarios and NONE of TEST-08/09/10/FALSIFY-01/02 (SPEC req 8), and (c) the roll-up reaches 7/7 VERDICT_PASS with zero VERDICT_FAIL. An INCONCLUSIVE verdict (exit 2 — observability-degraded, e.g. cold-ES flake) is a re-runnable non-failure; a VERDICT_FAIL (exit 1 / Missing>0 / recoverable-but-lost binding) fails the phase.

Purpose: This is the capstone — it proves the two-consumer recovery architecture recovers on k8s the same way it did on compose in v8.0.0, verified SOLELY from the reused Prometheus + Elasticsearch analyzer.
Output: `analyzer-reports/phase-81-summary.json` showing 7/7 VERDICT_PASS, and an operator-confirmed roll-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-re-run-the-7-scenario-fault-recovery-sweep-test-01-test-07-o/81-SPEC.md
@.planning/phases/81-re-run-the-7-scenario-fault-recovery-sweep-test-01-test-07-o/81-CONTEXT.md

<interfaces>
<!-- The reused-verbatim files (SPEC req 5 — must be UNCHANGED by Phase 81). -->
Analyzer + metric-gate source (do NOT modify — reused byte-for-byte):
  tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs         (the ~Analyze_Window_Yields_Pass test)
  tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs  (conservation/recovery metric gate)
  tests/BaseApi.Tests/Observability/Analysis/{AnalyzerReport,RunTrace,StructuralCohortFacts,PassFailEngineFacts}.cs
NOTE: AnalyzerE2ETests.cs already carries a PRE-EXISTING uncommitted change from prior work — that
predates Phase 81 and is NOT a Phase-81 delta. The SPEC req 5 gate is that Phase 81 introduces NO
NEW change to these files; the only files Phase 81 touches are the 3 deliverable scripts.

Sweep entrypoint: pwsh -File scripts/phase-81-sweep.ps1   (build+up once → TEST-01..07 → roll-up)
Roll-up artifact: analyzer-reports/phase-81-summary.json
Exit semantics: 0 IFF 7/7 PASS ; per-scenario harnessExit 0=PASS / 1=VERDICT_FAIL / 2=INCONCLUSIVE.
PRECONDITION: Docker Desktop + its single-node k8s must be running (the sweep's phase-80-up.ps1
guards the docker-desktop context). The stack itself is brought up by the sweep — no manual bring-up.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reused-verbatim guard — analyzer / PassFailEngine / metric-gate unchanged by Phase 81</name>
  <files>tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (read-only verification target)</files>
  <read_first>
    - .planning/phases/81-.../81-01-SUMMARY.md, 81-02-SUMMARY.md, 81-03-SUMMARY.md (confirm the only files Phase 81 modified are the 3 deliverable scripts)
    - .planning/phases/81-.../81-SPEC.md req 5 + Acceptance criterion "analyzer / PassFailEngine / metric-gate source unchanged"
  </read_first>
  <action>
Verify SPEC req 5: Phase 81 introduced NO change to the analyzer / `PassFailEngine` / metric-gate source.

1. Confirm the `PassFailEngine.cs` + the `Analysis/` metric-gate neighbors are byte-clean (they were clean at phase start):
```bash
git diff --stat -- \
  tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs \
  tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs \
  tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs \
  tests/BaseApi.Tests/Observability/Analysis/StructuralCohortFacts.cs \
  tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs
```
Expect EMPTY output (no Phase-81 modification).

2. Confirm the full set of files Phase 81 modified is EXACTLY the 3 deliverable scripts (plus `.planning/` docs). Compare against the phase-81 base commit (the commit before plan 81-01 landed — resolve it from git log / the 81-01-SUMMARY):
```bash
git diff --name-only <phase-81-base>..HEAD -- 'scripts/**' 'src/**' 'tests/**'
```
Expect ONLY: `scripts/phase-80-harness.ps1`, `scripts/phase-80-reset.ps1`, `scripts/phase-81-sweep.ps1`. `AnalyzerE2ETests.cs` may show a pre-existing uncommitted change from BEFORE phase 81 — that is NOT a Phase-81 delta; confirm it is absent from the `<phase-81-base>..HEAD` commit range specifically.
  </action>
  <verify>
    <automated>test -z "$(git diff --stat -- tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs)" && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` is EMPTY (metric gate untouched by Phase 81).
    - The Phase-81 commit range (`<phase-81-base>..HEAD`) modifies under `scripts/`/`src/`/`tests/` ONLY the 3 deliverable scripts; no `tests/BaseApi.Tests/Observability/**` .cs file appears in that range.
  </acceptance_criteria>
  <done>The analyzer / PassFailEngine / metric-gate source is confirmed unchanged by Phase 81 (reused verbatim per SPEC req 5).</done>
</task>

<task type="auto">
  <name>Task 2: Run the k8s capstone sweep → 7/7 VERDICT_PASS</name>
  <files>analyzer-reports/phase-81-summary.json (produced by the sweep run)</files>
  <read_first>
    - scripts/phase-81-sweep.ps1 (the sweep to run — from plan 81-03)
    - .planning/phases/81-.../81-SPEC.md req 6 (7/7 bar; INCONCLUSIVE re-runnable; VERDICT_FAIL fails)
    - MEMORY note "phase-68-sweep stale report read" — trust the harness exit code; TEST-01 can flake on cold ES (re-run, do not treat as FAIL)
  </read_first>
  <action>
Run the capstone sweep and drive it to 7/7 VERDICT_PASS.

1. Ensure Docker Desktop + its k8s are running (precondition — the sweep's `phase-80-up.ps1` fails loud on a non-docker-desktop context). Then run the sweep (it builds + brings up the stack once, loops TEST-01..07, rolls up):
```bash
pwsh -File scripts/phase-81-sweep.ps1
```
This is a long run (~30+ min: ~4 min bring-up + 7 × observation windows + drains). Capture the console roll-up + `analyzer-reports/phase-81-summary.json`.

2. Inspect `analyzer-reports/phase-81-summary.json`. For EACH of TEST-01..07 the row's `harnessExit` must be `0` (PASS) and `verdict` must be `Pass`.

3. **Re-run policy (SPEC req 6):**
   - Any scenario at `harnessExit = 2` (INCONCLUSIVE — observability-degraded, e.g. cold-ES flake, StartedRuns=0 with conservation intact) is a **re-runnable non-failure**. Re-run just that id against the (still-up) warm stack via the bare harness:
     `pwsh -File scripts/phase-80-harness.ps1 -ScenarioId <id> -SkipBringUp`
     then re-check its analyzer report. Do NOT auto-retry blindly; re-run deliberately against warm ES.
   - Any scenario at `harnessExit = 1` (VERDICT_FAIL — Missing>0 / recoverable-but-lost binding) is a REAL finding that **fails the phase** — do NOT re-run it away. Surface it (this becomes a gap-closure input; the recovery architecture or the crash sequencer needs investigation).
   - An INFRA_ABORT (10/20/25/30/40/50/60) is operator-re-runnable — re-run the affected id.

4. The phase gate is met when the roll-up shows **7/7 VERDICT_PASS** (all `harnessExit=0`) with **zero** VERDICT_FAIL.
  </action>
  <verify>
    <automated>node -e "const r=require('./analyzer-reports/phase-81-summary.json'); const pass=r.filter(x=>x.harnessExit===0).length; const fail=r.filter(x=>x.harnessExit===1).length; if(pass===7&&fail===0){console.log('7/7 PASS')}else{console.error('NOT 7/7: pass='+pass+' fail='+fail); process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `analyzer-reports/phase-81-summary.json` contains 7 rows with `scenarioId` TEST-01..TEST-07 (no TEST-08/09/10/FALSIFY rows — SPEC req 8).
    - All 7 rows have `harnessExit == 0` and `verdict == "Pass"`; zero rows have `harnessExit == 1` (VERDICT_FAIL).
    - The sweep's final line reads `CAPSTONE: 7/7 PASS` and the sweep exit code is 0.
  </acceptance_criteria>
  <done>The k8s capstone sweep roll-up shows 7/7 VERDICT_PASS with zero VERDICT_FAIL; INCONCLUSIVE flakes (if any) were cleared by deliberate warm-ES re-runs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirmation of the 7/7 capstone roll-up</name>
  <action>Present the capstone results to the operator for final confirmation before closing the phase (this verifies AFTER the Task 2 automated run — the sweep has already executed and produced the roll-up).</action>
  <what-built>The k8s fault-recovery capstone: a scenario-parameterized harness + kubectl-scale crash sequencer, a rabbitmq-draining reset, and a k8s sweep — driven to a 7/7 VERDICT_PASS roll-up from the byte-for-byte reused analyzer + metric gate.</what-built>
  <how-to-verify>
1. Open `analyzer-reports/phase-81-summary.json` and confirm 7 rows (TEST-01..07), every `harnessExit=0` / `verdict=Pass`, zero `harnessExit=1`.
2. Confirm the sweep console ended with `[phase-81-sweep] CAPSTONE: 7/7 PASS`.
3. Spot-check one crash scenario ran on k8s (not compose): e.g. TEST-04 log shows `kubectl -n skp scale deployment/keeper --replicas=0` then `--replicas=2`, and `kubectl -n skp get deploy keeper` now shows `2/2` Ready.
4. Confirm NO TEST-08/09/10 or FALSIFY-01/02 appears in the sweep output (out of scope — SPEC req 8).
5. Confirm the analyzer/PassFailEngine source shows no Phase-81 change (Task 1 result).
  </how-to-verify>
  <verify>Operator confirms the roll-up shows 7/7 VERDICT_PASS with zero VERDICT_FAIL.</verify>
  <done>Operator approves the 7/7 capstone; the phase closes.</done>
  <resume-signal>Type "approved" to close the phase, or describe which scenario(s) need investigation (a VERDICT_FAIL routes to gap closure; an INCONCLUSIVE is a warm-ES re-run).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep → live k8s stack (namespace `skp`) | the full crash/recover run against real workloads |
| roll-up JSON → operator verdict | the machine artifact the phase gate reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-11 | Repudiation | a stale analyzer report read as the current verdict | mitigate | Trust the harness/sweep exit code + the freshly written per-scenario report (MEMORY: phase-68 stale-report note); the summary is regenerated each sweep run. |
| T-81-12 | Spoofing | an INCONCLUSIVE flake masking or being mistaken for a real FAIL | mitigate | Exit-2 (INCONCLUSIVE) and exit-1 (VERDICT_FAIL) are distinct classes via the reused lib; only exit-1 fails the phase, exit-2 is re-run deliberately (never auto-retried away). |
| T-81-13 | Elevation of Privilege | the sweep running against a non-local cluster | accept | `phase-80-up.ps1` hard-fails (exit 12) on a non-docker-desktop context; all ops are `kubectl -n skp`. Local single-node dev target only. |
</threat_model>

<verification>
- `analyzer-reports/phase-81-summary.json` shows 7 rows TEST-01..07, all `harnessExit=0`/`verdict=Pass`, zero `harnessExit=1`.
- Sweep console: `CAPSTONE: 7/7 PASS`; sweep exit 0.
- `git diff --stat -- tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` empty.
- No TEST-08/09/10/FALSIFY in the sweep output.
</verification>

<success_criteria>
- 7/7 VERDICT_PASS, zero VERDICT_FAIL (INCONCLUSIVE re-runs allowed).
- Analyzer/PassFailEngine/metric-gate unchanged by Phase 81.
- Only the 3 capstone scenarios' tiers were crashed via kubectl scale; no seam negative controls run.
</success_criteria>

<output>
After completion, create `.planning/phases/81-re-run-the-7-scenario-fault-recovery-sweep-test-01-test-07-o/81-04-SUMMARY.md`
</output>
