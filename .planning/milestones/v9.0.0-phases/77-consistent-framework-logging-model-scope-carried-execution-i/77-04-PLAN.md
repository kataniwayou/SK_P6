---
phase: 77-consistent-framework-logging-model-scope-carried-execution-i
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Processor.Sample/SampleProcessor.cs
  - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs
autonomous: true
requirements: [LOG-04, LOG-05]

must_haves:
  truths:
    - "The concrete SampleProcessor emits NO author log — both the seed line and the value-clarifying line are gone (D4)."
    - "The framework verdict never depends on a concrete-processor log (the author writes zero logs)."
    - "Operator freedom is preserved: the bus-wide execution-scope filter stays registered unconditionally, so any author log (had one existed) still gets Tier-1 attributes.* (D5)."
  artifacts:
    - path: "src/Processor.Sample/SampleProcessor.cs"
      provides: "Author-log-free concrete processor (no ILogger dependency)"
      contains: "class SampleProcessor"
    - path: "tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs"
      provides: "Facts asserting behavior via SentData (not logs); no author-log assertions"
  key_links:
    - from: "SampleProcessor.ProcessAsync"
      to: "no logger"
      via: "ILogger dependency removed; behavior proven via send.SentData"
      pattern: "class SampleProcessor"
    - from: "MessagingServiceCollectionExtensions"
      to: "InboundExecutionScopeConsumeFilter (bus-wide, unconditional)"
      via: "UseConsumeFilter — unchanged (D5 operator freedom)"
      pattern: "InboundExecutionScopeConsumeFilter"
---

<objective>
Delete every concrete-processor author log from `Processor.Sample/SampleProcessor.cs` (D4 / LOG-04, full SMP-01). The framework's per-hop record (Plan 01) and result-send record (Plan 02) now carry the full execution evidence, so the sample author's two lines — the Mode-2 seed line (`"{StepLabel} seeded the following numbers: {Numbers}"`) and the Mode-1 value line (`"{StepLabel} received {Received} produced {Produced}"`) — are redundant and, more importantly, the verdict may NEVER depend on a concrete-processor log. Remove both; drop the now-unused `ILogger<SampleProcessor>` dependency so the 0-warning gate holds. Separately, guard D5 (LOG-05): confirm operator freedom is preserved — the bus-wide execution-scope filter stays registered unconditionally, so an operator may write ANY message string and still receive Tier-1 `attributes.*`.

Purpose: the platform-level "did this step run" evidence is framework-owned; concrete authors contribute nothing the verdict relies on.
Output: `SampleProcessor` emits no logs and takes no logger; `SampleProcessorFacts` proves the same Mode-2/Mode-1 behavior via `send.SentData` (not via log assertions); a D5 guard confirms the scope filter registration is unchanged.

PHASE-78 NOTE: the live value-oracle already degrades to N/A when these logs are absent; Phase 78 drops the oracle. For Phase 77 the removal is the deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-consistent-framework-logging-model-scope-carried-execution-i/77-CONTEXT.md

<interfaces>
<!-- Current author logs to DELETE — src/Processor.Sample/SampleProcessor.cs -->
:37  public sealed class SampleProcessor(ILogger<SampleProcessor> logger) : BaseProcessor<SampleConfig>
:68  logger.LogInformation("{StepLabel} seeded the following numbers: {Numbers}", label, string.Join(", ", numbers));
:89  logger.LogInformation("{StepLabel} received {Received} produced {Produced}", label, incomingNumber, accumulated);

<!-- D5 invariant to PRESERVE (do not change) — src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs:53 -->
c.UseConsumeFilter(typeof(InboundExecutionScopeConsumeFilter<>), ctx);  // bus-wide, unconditional

<!-- Test construction — tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs -->
new SampleProcessor(logger)  // becomes new SampleProcessor()
Assert.Contains(logger.Entries, e => e.Message.Contains("seeded the following numbers: 100, 200"));  // DELETE
Assert.Contains(logger.Entries, e => e.Message.Contains("received 7 produced 10"));                   // DELETE
Assert.Single(logger.Entries);  // DELETE (author now logs nothing)
// Behavior stays proven by send.SentData (seeded 100/200; produced 7+3=10) — keep those asserts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the two author logs and remove the logger dependency</name>
  <files>src/Processor.Sample/SampleProcessor.cs</files>
  <read_first>
    - src/Processor.Sample/SampleProcessor.cs (full file — ctor :37, seed log :66-69, value log :85-90)
  </read_first>
  <action>
    1. Delete the Mode-2 seed log (:68-69):
    ```
    logger.LogInformation("{StepLabel} seeded the following numbers: {Numbers}",
        label, string.Join(", ", numbers));
    ```
    (Keep the `var numbers = new[] { 100, 200 };` seed values and the spawn loop — only the log statement is removed.)
    2. Delete the Mode-1 value log (:89-90):
    ```
    logger.LogInformation("{StepLabel} received {Received} produced {Produced}",
        label, incomingNumber, accumulated);
    ```
    (Keep the `accumulated` computation and the `NewResult(...) with { ExecutionId = executionId }` return.)
    3. Remove the now-unused logger dependency: change the class declaration `public sealed class SampleProcessor(ILogger<SampleProcessor> logger) : BaseProcessor<SampleConfig>` → `public sealed class SampleProcessor : BaseProcessor<SampleConfig>`. Remove the `using Microsoft.Extensions.Logging;` import if nothing else uses it. If `BaseProcessor<SampleConfig>` requires an explicit base constructor call, add a parameterless ctor only if the compiler demands it (it currently uses a primary ctor with no base-args, so simply dropping the primary-ctor param list is sufficient).
    4. Update the class XML doc: remove the two sentences describing the seed/value log lines (they no longer exist); note that per D4/LOG-04 the concrete processor emits NO author logs — the framework per-hop and result-send records carry the execution evidence.
    Verify no other `logger.` reference remains (the env-gated delay hook at :49-53 uses no logger).
  </action>
  <acceptance_criteria>
    - `rg -F 'seeded the following numbers' src/Processor.Sample/SampleProcessor.cs` returns 0.
    - `rg -F 'received {Received} produced' src/Processor.Sample/SampleProcessor.cs` returns 0.
    - `rg -c 'logger' src/Processor.Sample/SampleProcessor.cs` returns 0.
    - `rg -c 'ILogger|Microsoft.Extensions.Logging' src/Processor.Sample/SampleProcessor.cs` returns 0.
    - Debug+Release build: 0 Warning(s) (no unused param / unused using).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug --nologo && dotnet build SK_P.sln -c Release --nologo</automated>
  </verify>
  <done>Both author logs and the ILogger dependency are gone; SampleProcessor compiles warning-free in Debug+Release.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update SampleProcessorFacts to behavior-only; add the D5 operator-freedom guard</name>
  <files>tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs (the CapturingLogger + the three facts + the config fact)
    - tests/BaseApi.Tests/Console/ConsoleExecutionScopeFilterTests.cs (proof the scope filter is unconditional/bus-wide — reuse its intent for the D5 guard)
    - src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs (:52-53 filter registration — the D5 invariant)
  </read_first>
  <behavior>
    - Mode-2 entry still spawns two distinct-execId Completed results seeding 100/200 and deletes the entry (proven via send.SentData + db).
    - Mode-1 downstream still returns one Completed result reusing the inbound exec, producing 7+3=10 (proven via dr.Data).
    - No fact asserts any author log; SampleProcessor is constructed with no logger.
    - A D5 guard fact confirms the bus-wide execution-scope filter registration is present and unconditional.
  </behavior>
  <action>
    - Replace every `new SampleProcessor(logger)` with `new SampleProcessor()` and delete the `var logger = new CapturingLogger();` lines that become unused.
    - Delete the author-log assertions: the two `Assert.Contains(logger.Entries, …)` lines (seed / received-produced) and every `Assert.Single(logger.Entries);`. Keep ALL behavior assertions (`send.SentData` seeded == {100,200}; distinct execIds; `dr.Data` number == 10; delete/no-delete; `Assert.Empty(send.Sent)`).
    - Remove the now-unused `CapturingLogger` nested class if nothing references it after the edits (leave it only if another fact still uses it).
    - Add a D5 guard fact `OperatorFreedom_ExecutionScopeFilter_Registered_Unconditionally`. Simplest hermetic form: a source-invariant assertion is out of scope for a runtime fact, so instead assert the FILTER is unconditional at runtime by reusing the ConsoleExecutionScopeFilterTests harness pattern — publish an IExecutionCorrelated probe whose consumer logs a BARE interpolated string (no placeholders) and assert the captured execution scope still carries the five ids (proving the operator need not use placeholders to get Tier-1 attributes). If that harness is heavier than desired, place the guard as a NEW fact in ConsoleExecutionScopeFilterTests instead and keep SampleProcessorFacts behavior-only; either location satisfies LOG-05 — pick the one that keeps files owned by THIS plan (SampleProcessorFacts) unless it forces harness duplication, in which case add it to ConsoleExecutionScopeFilterTests and add that file to this task's edits.
  </action>
  <acceptance_criteria>
    - `rg -c 'new SampleProcessor(logger)' tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs` returns 0.
    - `rg -c 'logger.Entries' tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs` returns 0.
    - `rg -F 'Assert.Equal(new[] { 100, 200 }, seededNumbers)' tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs` returns 1 (behavior assert kept).
    - `rg -c 'InboundExecutionScopeConsumeFilter' src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` returns 1 (D5 invariant intact — filter still bus-wide/unconditional).
    - `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe -- --filter-method "*SampleProcessor*"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug --nologo && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe -- --filter-method "*SampleProcessor*"</automated>
  </verify>
  <done>SampleProcessorFacts proves Mode-2/Mode-1 behavior via SentData with no author-log assertions; the D5 operator-freedom guard passes; the scope filter registration is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concrete author code → framework verdict | after D4 the author contributes ZERO logs the verdict can depend on (removes an author-controlled input to the verdict) |
| operator log string → ES attributes.* | D5: the bus-wide scope attaches Tier-1 regardless of the operator's string; no operator obligation to use placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-10 | Tampering | verdict depending on a concrete-processor log | mitigate | D4 removes all author logs; the verdict is framework-log-only. Grep-asserted absence. |
| T-77-11 | Information disclosure | removed seed/value logs | mitigate | The deleted lines logged synthetic proof integers only; removal strictly reduces the log surface (no payload was or is logged). |
| T-77-12 | Repudiation | operator freedom (D5) | accept | The scope filter stays bus-wide/unconditional so any operator string still carries Tier-1 attributes; verified unchanged, not modified. |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` and `-c Release` both report 0 Warning(s).
- `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe -- --filter-method "*SampleProcessor*"` exits 0 (plus the D5 guard fact wherever placed).
- Full hermetic subset shows zero NEW failures vs baseline.
</verification>

<success_criteria>
- SampleProcessor emits no author logs and has no logger dependency; behavior proven via SentData.
- The verdict never depends on a concrete-processor log (D4/LOG-04).
- Operator freedom preserved: scope filter unchanged, bus-wide, unconditional (D5/LOG-05); 0-warning Debug+Release.
</success_criteria>

<output>
After completion, create `.planning/phases/77-consistent-framework-logging-model-scope-carried-execution-i/77-04-SUMMARY.md`
</output>
