---
phase: 70-two-consumer-processor-design-pre-process-post-process-with
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/processor-keeper-recovery-spec.md
autonomous: true
requirements: [SPEC-req-12]
nyquist_compliant: true

must_haves:
  truths:
    - "docs/design/processor-keeper-recovery-spec.md carries a visible 'superseded by Phase 70' marker pointing at 70-SPEC.md"
  artifacts:
    - path: "docs/design/processor-keeper-recovery-spec.md"
      provides: "Superseded marker (req 12)"
      contains: "Superseded"
  key_links:
    - from: "docs/design/processor-keeper-recovery-spec.md"
      to: ".planning/phases/70-.../70-SPEC.md"
      via: "superseded-by marker reference"
      pattern: "Phase 70"
---

<objective>
Mark the now-superseded slot-array canonical doc (req 12, second half / D-17 discretion: default to a header marker pointing at `70-SPEC.md`).

Purpose: the SPEC's two-consumer pseudocode replaces the slot-array Pre/In/Post + recovery-pass model this doc describes; readers must land on the new source of truth. Doc-only, zero code overlap → Wave 1, parallel with the contract foundation.
Output: one edited doc file with a clear superseded banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-SPEC.md
@.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a 'superseded by Phase 70' banner to the canonical recovery doc</name>
  <files>docs/design/processor-keeper-recovery-spec.md</files>
  <read_first>
    - docs/design/processor-keeper-recovery-spec.md (the file being marked — lines 1-7 carry the title + intro blockquote)
    - 70-SPEC.md (the new source of truth the marker points at)
  </read_first>
  <action>
Insert, immediately AFTER the `# Processor / Keeper Recovery — Canonical Spec` H1 (line 1) and BEFORE the existing intro blockquote (line 3), a blockquote banner (verbatim, D-17 discretion = marker, not replacement):
```markdown
> [!WARNING]
> **SUPERSEDED by Phase 70 (2026-06-16).** This document describes the now-retired
> single-consumer slot-array Pre/In/Post-Process + recovery-pass model
> (gate on `L2[messageId]` HASH, atomic slot writes, recovery pass). It has been
> replaced by the **two-consumer (Pre-Process + Post-Process) design** whose pinned
> pseudocode is the sole source of truth:
> `.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-SPEC.md`.
> Do not implement against this document — it is retained for historical context only.
```
Do not delete or otherwise rewrite the body of the doc; only prepend the banner.
  </action>
  <verify>
    <automated>grep -q "SUPERSEDED by Phase 70" "docs/design/processor-keeper-recovery-spec.md" && grep -q "70-SPEC.md" "docs/design/processor-keeper-recovery-spec.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/design/processor-keeper-recovery-spec.md` contains `SUPERSEDED by Phase 70`
    - `docs/design/processor-keeper-recovery-spec.md` contains a reference to `70-SPEC.md`
    - The banner appears before the original intro blockquote (i.e. within the first ~12 lines)
    - The original H1 title is unchanged and the doc body is otherwise intact
  </acceptance_criteria>
  <done>The canonical doc shows a superseded-by-Phase-70 banner pointing at 70-SPEC.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edit; no code, no runtime surface, no trust boundary touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-13 | N/A | docs/design markdown | accept | Doc-only marker; no untrusted input, no auth/secret surface, no executable path. No security-relevant change. |
</threat_model>

<verification>
- Grep acceptance criteria pass.
- No build/test impact (markdown only).
</verification>

<success_criteria>
- The superseded banner is present and points at `70-SPEC.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/70-two-consumer-processor-design-pre-process-post-process-with-/70-05-SUMMARY.md`
</output>
