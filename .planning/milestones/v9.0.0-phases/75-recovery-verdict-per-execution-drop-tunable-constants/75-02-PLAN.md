---
phase: 75-recovery-verdict-per-execution-drop-tunable-constants
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Keeper/Recovery/ReinjectConsumer.cs
  - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs
autonomous: true
requirements: [D75-4]
user_setup: []

must_haves:
  truths:
    - "The keeper REINJECT drop log carries CorrelationId, ExecutionId, EntryId, MessageId and an outcome discriminator as structured placeholders."
    - "The keeper REINJECT success path emits a symmetric structured log with the same four ids + outcome after the confirmed CountSent."
    - "The four ids ride as message-template {Placeholder} args (not string interpolation) so they surface as ES attributes.*."
    - "The Payload is never logged; the success log never fires on the drop early-return."
  artifacts:
    - path: "src/Keeper/Recovery/ReinjectConsumer.cs"
      provides: "Widened drop log + new reinject-success log with join keys"
      contains: "ReinjectOutcome"
    - path: "tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs"
      provides: "Hermetic facts asserting the structured join fields on both paths"
      contains: "ReinjectOutcome"
  key_links:
    - from: "ReinjectConsumer drop early-return"
      to: "ES attributes.CorrelationId/ExecutionId/EntryId/MessageId/ReinjectOutcome"
      via: "message-template placeholder args → MEL→OTLP bridge"
      pattern: "\\{CorrelationId\\} \\{ExecutionId\\} \\{EntryId\\} \\{MessageId\\}"
---

<objective>
Widen the keeper `ReinjectConsumer` telemetry so the analyzer can join keeper recovery outcomes to lost `(correlationId, executionId)` executions (D75-4). Today the clean-absent DROP log carries only `EntryId` — a structured hole. Add the missing join keys to the drop log and add a symmetric structured success log on the reinject path, both as message-template `{Placeholder}` args (so they surface as ES `attributes.*` via the MEL→OTLP bridge), and extend the hermetic facts to assert the fields.

Purpose: Give the analyzer keeper evidence to classify recoverable-but-lost vs provably-unrecoverable (feeds Plan 01's classifier via Plan 04's live join).
Output: A keeper drop log + reinject-success log carrying `(CorrelationId, ExecutionId, EntryId, MessageId, ReinjectOutcome)`, proven hermetically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-RESEARCH.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-PATTERNS.md

<interfaces>
<!-- Extracted this session — use directly, no exploration needed. -->

The contract already carries all four ids (src/Messaging.Contracts/KeeperReinject.cs:10-13):
```csharp
public Guid CorrelationId { get; init; }
public Guid ExecutionId   { get; init; }
public Guid EntryId       { get; init; }
public Guid MessageId     { get; init; }
```

Current drop log to widen (src/Keeper/Recovery/ReinjectConsumer.cs:43):
```csharp
logger.LogWarning("REINJECT drop: L2 data gone EntryId={EntryId}", m.EntryId);   // structured hole (never log Payload)
return;                                                                          // D-06 silent ack
```

Current success path end (src/Keeper/Recovery/ReinjectConsumer.cs:63-65):
```csharp
// REQ-3 / D-09: count keeper_messages_sent AFTER the confirmed send ...
CountSent(m.WorkflowId, m.ProcessorId);
```

The canonical placeholder-form analog (src/Processor.Sample/SampleProcessor.cs:77) — `{Placeholder}` args become ES `attributes.<Name>` via `ParseStateValues=true`:
```csharp
logger.LogInformation("{StepLabel} received {Received} produced {Produced}", label, incomingNumber, accumulated);
```

CRITICAL (verified this session): `RecoveryConsumerBase.Consume` dispatches straight to `HandleAsync` with NO `BeginScope` — so unlike SampleProcessor, the keeper log gets NO ambient CorrelationId/ExecutionId scope. ALL four ids MUST be explicit placeholder args, NEVER string interpolation ($"...{x}" becomes one opaque message with no attributes.*).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert structured join fields on drop + success logs via a capturing logger (RED)</name>
  <read_first>
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs (both existing facts: Reinject_present_sends_EntryStepDispatch_with_envelope_messageId_override :39 uses the SUCCESS path + asserts keeper_messages_sent==1 via a reference-scoped MeterListener; Reinject_absent_drops_no_throw_no_send_and_emits_no_legacy_drop_counter :101 is the DROP path; both currently pass NullLogger<ReinjectConsumer>.Instance at :77 and :148; the message `m` sets all four ids at :44-47 and :106-109)
    - src/Keeper/Recovery/ReinjectConsumer.cs:30-66 (both log sites)
  </read_first>
  <files>tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs</files>
  <behavior>
    - Drop-path fact: after HandleAsync on an absent-L2 message, a captured LogWarning record exists whose structured state contains CorrelationId, ExecutionId, EntryId, MessageId equal to the message's ids and ReinjectOutcome == "drop".
    - Success-path fact: after HandleAsync on a present-L2 message, a captured LogInformation record exists with the same four ids and ReinjectOutcome == "reinject", AND keeper_messages_sent == 1 still holds (log sits after CountSent).
  </behavior>
  <action>
    In `ReinjectConsumerFacts.cs`, swap `NullLogger<ReinjectConsumer>.Instance` (:77, :148) for a small capturing `ILogger<ReinjectConsumer>` test double that records each log entry's structured state as key/value pairs (implement a minimal `CapturingLogger<T> : ILogger<T>` whose `Log` method materializes the `IReadOnlyList<KeyValuePair<string,object?>>` state into a captured list; `BeginScope` returns a no-op disposable; `IsEnabled` returns true). Then:
    1. Extend the DROP fact (:101) — after `HandleAsync`, assert the captured warning's state pairs include `("CorrelationId", m.CorrelationId)`, `("ExecutionId", m.ExecutionId)`, `("EntryId", m.EntryId)`, `("MessageId", m.MessageId)`, `("ReinjectOutcome", "drop")`. Keep the existing `Assert.Equal(0, sent)` (:153).
    2. Extend the SUCCESS fact (:39) — after `HandleAsync`, assert the captured information entry's state pairs include the same four id keys with the message's values and `("ReinjectOutcome", "reinject")`. Keep the existing `Assert.Equal(1, sent)` (:96).
    The message `m` already sets all four ids, so no fixture change is needed. These assertions MUST fail RED (the logs do not yet carry the fields).
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-class *ReinjectConsumerFacts</automated>
  </verify>
  <acceptance_criteria>
    - `ReinjectConsumerFacts.cs` no longer passes `NullLogger<ReinjectConsumer>.Instance` on either fact (grep returns nothing on those sites; a `CapturingLogger` is used).
    - `ReinjectConsumerFacts.cs` asserts `"ReinjectOutcome"` with values `"drop"` and `"reinject"`.
    - Both facts fail RED (fields not yet emitted).
    - `MeterListener` sent-count assertions (`Assert.Equal(0, sent)` / `Assert.Equal(1, sent)`) are unchanged.
  </acceptance_criteria>
  <done>Both facts capture the structured log state and assert the five join fields; they fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the drop log + add the reinject-success log with join keys (GREEN)</name>
  <read_first>
    - src/Keeper/Recovery/ReinjectConsumer.cs (full — drop at :43, CountSent at :65; class doc-comments forbid logging Payload and require CountSent-only-on-send)
    - src/Processor.Sample/SampleProcessor.cs:77 (placeholder-form analog)
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs (the RED facts from Task 1)
  </read_first>
  <files>src/Keeper/Recovery/ReinjectConsumer.cs</files>
  <behavior>
    - Both RED facts turn GREEN.
    - keeper_messages_sent stays 1 on the success path, 0 on the drop path (log added after CountSent; nothing sends on drop).
  </behavior>
  <action>
    In `ReinjectConsumer.cs`:
    1. Replace the drop log (:43) with the widened placeholder form — all four ids as explicit `{Placeholder}` args plus an outcome discriminator (the keeper consumer has NO ambient scope, so the ids will NOT arrive otherwise):
       ```csharp
       logger.LogWarning(
           "REINJECT drop: L2 data gone {CorrelationId} {ExecutionId} {EntryId} {MessageId} {ReinjectOutcome}",
           m.CorrelationId, m.ExecutionId, m.EntryId, m.MessageId, "drop");
       ```
       Keep the `return;` (:44) and the "never log Payload" constraint — do NOT add `m.Payload` to the template.
    2. Add a symmetric success log AFTER the confirmed `CountSent(m.WorkflowId, m.ProcessorId);` (:65) — never on the drop early-return:
       ```csharp
       CountSent(m.WorkflowId, m.ProcessorId);
       logger.LogInformation(
           "REINJECT sent {CorrelationId} {ExecutionId} {EntryId} {MessageId} {ReinjectOutcome}",
           m.CorrelationId, m.ExecutionId, m.EntryId, m.MessageId, "reinject");
       ```
    Use message-template placeholders ONLY — no `$"..."` interpolation (Pitfall 2: interpolation collapses to one opaque message and surfaces no `attributes.*`). Field names `CorrelationId`/`ExecutionId`/`EntryId`/`MessageId`/`ReinjectOutcome` become `attributes.<Name>` in ES via the existing bridge (`ParseStateValues=true`); the analyzer (Plan 04) queries `attributes.ReinjectOutcome`.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-class *ReinjectConsumerFacts</automated>
  </verify>
  <acceptance_criteria>
    - `ReinjectConsumer.cs` drop log template contains `{CorrelationId} {ExecutionId} {EntryId} {MessageId} {ReinjectOutcome}`.
    - `ReinjectConsumer.cs` contains a `LogInformation` with `"REINJECT sent"` and the same five placeholders, positioned AFTER `CountSent(`.
    - `ReinjectConsumer.cs` contains no `$"` string-interpolation in either log call and no `m.Payload` in any log template.
    - The command exits 0: both facts GREEN; sent==1 (success) / sent==0 (drop) preserved.
  </acceptance_criteria>
  <done>Both keeper logs carry the structured join keys as placeholders; hermetic facts green; Payload never logged; CountSent placement preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper → Elasticsearch (telemetry) | Keeper log records cross into the ES log store via the MEL→OTLP bridge. This plan only widens the structured fields on an existing log path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03 | Information Disclosure | keeper REINJECT drop/success logs → ES | mitigate | Only correlation/execution/entry/message GUIDs + a static outcome label are logged. The `m.Payload` (step config) is explicitly NOT logged (enforced by acceptance criterion — no `m.Payload` in any template), preserving the file's existing "never log Payload" invariant. No secrets/PII. |
| T-75-04 | Tampering | log field names | accept | Field names are compile-time literals; no external input controls the template. No new external surface. |
</threat_model>

<verification>
- `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits 0.
- 0-warning Debug + Release build.
- Live ES-surfacing of the new keeper attributes is validated in Plan 04 (RealStack, deferred-automated per prior phases when Docker unavailable).
</verification>

<success_criteria>
- Drop + success logs carry `(CorrelationId, ExecutionId, EntryId, MessageId, ReinjectOutcome)` as placeholders; hermetic facts green; Payload never logged; full hermetic suite green; 0-warning.
</success_criteria>

<output>
After completion, create `.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-02-SUMMARY.md`
</output>
