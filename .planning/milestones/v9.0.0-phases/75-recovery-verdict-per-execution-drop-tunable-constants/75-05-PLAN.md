---
phase: 75-recovery-verdict-per-execution-drop-tunable-constants
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-67-harness.ps1
autonomous: false
requirements: [D75-7]
user_setup: []

must_haves:
  truths:
    - "The observation window can OPTIONALLY be driven by an execution count (run until K executions observed) instead of the fixed 300s wall-clock."
    - "The default behaviour is unchanged (300s wall-clock) when the K-executions seam is not set — the core verdict rework does NOT depend on this."
    - "The new env seam is set + cleared symmetrically alongside the existing SCENARIO_ID/WINDOW_*_UTC/RECOVERY_UTC seams."
  artifacts:
    - path: "scripts/phase-67-harness.ps1"
      provides: "Optional execution-based observation window with a K_EXECUTIONS seam"
      contains: "K_EXECUTIONS"
  key_links:
    - from: "K_EXECUTIONS env seam"
      to: "the observe loop deadline / stop condition"
      via: "run until K distinct executions observed OR the window deadline"
      pattern: "K_EXECUTIONS"
---

<objective>
OPTIONAL / DEFERRABLE (D75-7): make the observation window execution-based — run until K distinct executions are observed, rather than a fixed 300s wall-clock. The ROADMAP marks this explicitly "Optionally"; `ConservationTol=±1` is already non-binding + drain-settled, so the core verdict decoupling (Plans 01-04) does NOT depend on this. This plan adds the seam without changing default behaviour: absent the seam, the harness runs the existing 300s window unchanged.

Purpose: Improve determinism of the observation window when desired, without letting it block the verdict rework.
Output: An optional `K_EXECUTIONS` harness seam gating the observe loop, default-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-RESEARCH.md
@.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-PATTERNS.md

<interfaces>
<!-- From scripts/phase-67-harness.ps1, extracted this session. -->

The fixed wall-clock window (phase-67-harness.ps1:257-259):
```powershell
# 5-minute observation window (300s). Poll cadence ~5s.
$windowSeconds = 300
$windowDeadline = (Get-Date).AddSeconds($windowSeconds)
```

The observe loop stop is bounded by `$windowDeadline` (STEP F.2 begins :261-263, observes until N fires bounded by the window deadline).

The env seam set + cleared symmetrically (:393-398 set; :414 clear in finally):
```powershell
$env:SCENARIO_ID      = $ScenarioId
$env:WINDOW_START_UTC = $windowStart.ToString('o')
$env:WINDOW_END_UTC   = $windowEnd.ToString('o')
$env:RECOVERY_UTC     = if ($recoveryUtc) { $recoveryUtc.ToString('o') } else { '' }
# ...
Remove-Item Env:SCENARIO_ID, Env:WINDOW_START_UTC, Env:WINDOW_END_UTC, Env:RECOVERY_UTC -ErrorAction SilentlyContinue
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an optional K_EXECUTIONS execution-based window seam (default-off, wall-clock unchanged)</name>
  <read_first>
    - scripts/phase-67-harness.ps1:254-263 (baseline fire count, $windowSeconds=300, $windowDeadline, the observe-loop start)
    - scripts/phase-67-harness.ps1:390-415 (the env-seam set block + the finally-clear block — add + clear the new seam symmetrically here)
  </read_first>
  <files>scripts/phase-67-harness.ps1</files>
  <action>
    Add an OPTIONAL execution-based observation window, DEFAULT-OFF so absent the seam the harness runs exactly as today:
    1. Near the window setup (:257-259), read an optional param/env: `$kExecutions = if ($env:K_EXECUTIONS) { [int]$env:K_EXECUTIONS } else { 0 }`. `0` (or unset) means "wall-clock only" — no behaviour change.
    2. In the observe loop (STEP F.2, :261+), keep the existing `$windowDeadline` upper bound ALWAYS (a hard cap so the loop can never run forever). When `$kExecutions -gt 0`, ALSO stop early once `K` distinct executions have been observed — reuse the existing fire/observation polling (the loop already counts observed fires); count distinct executions from the same Prometheus/observation signal the loop already reads, and `break` when the distinct-execution count reaches `$kExecutions` OR the `$windowDeadline` passes, whichever comes first.
    3. Set + clear the seam symmetrically with the existing env seams: in the :393-398 set block add `$env:K_EXECUTIONS = if ($kExecutions -gt 0) { $kExecutions.ToString() } else { '' }` (only when adopted downstream — otherwise the analyzer ignores an empty value exactly like RECOVERY_UTC), and add `Env:K_EXECUTIONS` to the `Remove-Item` clear list in the `finally` (:414).
    4. Add a comment block documenting this as the OPTIONAL D75-7 execution-based window: "default 0/unset ⇒ fixed 300s wall-clock unchanged; K>0 ⇒ stop at K observed executions, still hard-capped by the window deadline." Do NOT remove or lower the `$windowSeconds = 300` default — the wall-clock stays the hard cap.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; $null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/phase-67-harness.ps1), [ref]$null, [ref]$null); Write-Host PARSE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-67-harness.ps1` contains `K_EXECUTIONS` in the read, the set block, and the `Remove-Item` clear list (all three).
    - `scripts/phase-67-harness.ps1` still contains `$windowSeconds = 300` and `$windowDeadline` as an unconditional upper bound (the wall-clock hard cap is preserved).
    - The observe loop `break`s on the K-executions condition ONLY when `$kExecutions -gt 0` (default-off path leaves behaviour unchanged).
    - `pwsh` parse check prints `PARSE_OK` (script parses; no live run required — this is a script-only optional seam).
  </acceptance_criteria>
  <done>An optional, default-off, hard-capped execution-based window seam exists; the 300s wall-clock default is unchanged; the script parses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Added an OPTIONAL execution-based observation window (`K_EXECUTIONS` seam) to `phase-67-harness.ps1`, default-off (0/unset ⇒ the existing fixed 300s wall-clock, hard-capped either way).</what-built>
  <how-to-verify>
    This is a live-harness behaviour only OBSERVABLE on Docker RealStack (per prior phases 68/73/74, deferred-automated when Docker is unavailable). The script-parse gate is the automated proof of correctness for the seam itself. When Docker IS available and you want to exercise the optional path:
    1. Run a scenario with the seam set: `$env:K_EXECUTIONS = "8"; pwsh -File scripts/phase-68-sweep.ps1 -ScenarioIds TEST-01`.
    2. Expected: the observe loop stops once 8 distinct executions are observed (or the 300s window deadline, whichever first); the verdict is unchanged in kind (still a pure per-execution recovery function — K only bounds WHICH executions are in the window, not the pass/fail rule).
    3. Run without the seam to confirm the default 300s wall-clock path is unchanged.
    Because D75-7 is explicitly OPTIONAL and MUST NOT block the verdict rework, "deferred / not adopted this milestone" is an acceptable resolution — the seam exists, default-off, ready if wanted.
  </how-to-verify>
  <resume-signal>Type "approved", "deferred-automated — Docker unavailable", or "not adopted this milestone (seam left default-off)".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a PowerShell test-harness script, adding an optional env seam that bounds an observe loop. No network endpoint, no persistence, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-09 | Denial of Service | observe loop termination | mitigate | The `$windowDeadline` wall-clock cap is retained UNCONDITIONALLY as an upper bound, so a mis-set `K_EXECUTIONS` (e.g. K never reached) can never hang the loop past the existing 300s window. |
| T-75-10 | Information Disclosure | K_EXECUTIONS env seam | accept | The seam carries a single integer count; no secrets/PII; cleared in `finally` symmetrically with the existing seams. No new external surface. |
</threat_model>

<verification>
- `pwsh` parse check prints `PARSE_OK`.
- The default-off path (no `K_EXECUTIONS`) leaves the 300s wall-clock window behaviour byte-for-byte unchanged.
- Live optional path: deferred-automated / not-adopted per the checkpoint (D75-7 is explicitly optional).
</verification>

<success_criteria>
- Optional, default-off, hard-capped execution-based window seam added; wall-clock default unchanged; script parses; live exercise deferred/optional.
</success_criteria>

<output>
After completion, create `.planning/phases/75-recovery-verdict-per-execution-drop-tunable-constants/75-05-SUMMARY.md`
</output>
