---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 01
subsystem: infra
tags: [kubernetes, k8s, namespace, secret, configmap, otel-collector, prometheus, docker-desktop]

# Dependency graph
requires:
  - phase: 79-falsification-harness-for-the-resilience-sweep-negative-cont
    provides: compose-source-of-truth (compose.yaml, .env, otel/prometheus config) being ported
provides:
  - "Namespace skp — the single namespace every k8s resource lands in (D-02)"
  - "Secret skp-dev-secrets — dev-only postgres + rabbitmq creds via stringData (D-07)"
  - "ConfigMap otel-collector-config — verbatim compose/otel-collector-config.yaml body"
  - "ConfigMap prometheus-config — verbatim prometheus.yml body (D-06)"
affects: [80-02, 80-03, 80-04, 80-05, 80-06, 80-07]  # every StatefulSet/Deployment references these three objects

# Tech tracking
tech-stack:
  added: [kubernetes-manifests]
  patterns:
    - "dev-only Secret via stringData (readable/diff-able against .env; k8s base64-encodes on write)"
    - "config-file port via ConfigMap data block scalar mounted by subPath (D-06)"

key-files:
  created:
    - k8s/00-namespace.yaml
    - k8s/01-secret.yaml
    - k8s/02-configmaps.yaml
  modified: []

key-decisions:
  - "stringData (not data) for the Secret — keeps values diff-able against .env; k8s encodes on write (D-07)"
  - "ConfigMap bodies carried byte-for-byte from source files (verified via diff), not re-authored (D-06)"
  - "ConfigMaps hand-authored inline as the guaranteed-working fallback; 80-07 kustomize configMapGenerator, if used, must reuse these exact names + disableNameSuffixHash: true"

patterns-established:
  - "Foundation config layer: namespace + creds Secret + config ConfigMaps ship first as the shared substrate for all downstream manifests"
  - "Config-file port fidelity gate: derived ConfigMap body diffed against the source repo file to prove verbatim carry"

requirements-completed: [GOAL-80-HEALTHY]

# Metrics
duration: 2min
completed: 2026-07-18
---

# Phase 80 Plan 01: Foundation Config Layer (Namespace + Secret + ConfigMaps) Summary

**The skp namespace, the dev-only skp-dev-secrets creds Secret (five verbatim .env/compose keys via stringData), and the otel-collector-config + prometheus-config ConfigMaps carrying byte-identical config bodies — the shared substrate every downstream k8s manifest references.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-07-18T06:28:12Z
- **Completed:** 2026-07-18T06:29:30Z
- **Tasks:** 2
- **Files modified:** 3 (all created)

## Accomplishments
- Namespace `skp` (D-02) with `app.kubernetes.io/part-of: skp` label — validates client-side.
- Secret `skp-dev-secrets` (D-07): `stringData` with the five verbatim keys (POSTGRES_DB/USER/PASSWORD, RABBITMQ_DEFAULT_USER/PASS), bold `# DEV ONLY` header + `sensitivity: dev-only` label.
- Two ConfigMaps (D-06): `otel-collector-config` (data key `otel-collector-config.yaml`) and `prometheus-config` (data key `prometheus.yml`), bodies proven **byte-identical** to `compose/otel-collector-config.yaml` and `prometheus.yml` via diff.
- Full-plan `kubectl apply --dry-run=client` over all three manifests exits 0.

## Task Commits

Each task was committed atomically:

1. **Task 1: Namespace + dev-only Secret** - `b7840ef` (feat)
2. **Task 2: ConfigMaps for otel-collector-config + prometheus.yml** - `f6ea44a` (feat)

## Files Created/Modified
- `k8s/00-namespace.yaml` - Namespace `skp` (D-02).
- `k8s/01-secret.yaml` - `skp-dev-secrets` Opaque Secret, `stringData` dev creds, dev-only-labeled (D-07).
- `k8s/02-configmaps.yaml` - `otel-collector-config` + `prometheus-config` ConfigMaps with verbatim config bodies (D-06).

## Decisions Made
- `stringData` over `data` for the Secret — readable/diff-able against `.env`, k8s base64-encodes on write (D-07).
- ConfigMap bodies generated by indenting the source files under a `|` block scalar (not hand-transcribed), then diffed against source to prove byte-for-byte fidelity (D-06 / threat T-80-02 mitigation).
- ConfigMaps authored inline as the canonical guaranteed-working fallback; documented in-file that a future 80-07 kustomize `configMapGenerator` must reuse the SAME names (`otel-collector-config`, `prometheus-config`) with `disableNameSuffixHash: true` or the 80-04 volume refs break.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. `kubectl` v1.34.1 available via Docker Desktop; all client-side dry-runs passed on first attempt.

## Threat Register Outcome
- **T-80-01 (Info Disclosure, accept):** Secret carries the SAME public dev creds already committed in `.env`/`compose.yaml` (guest/guest, postgres/postgres) — no new exposure. Mitigated mis-use via bold `# DEV ONLY` header + `sensitivity: dev-only` label; scoped to `skp` on local Docker Desktop.
- **T-80-02 (Tampering / ConfigMap drift, mitigate):** ConfigMap bodies diffed against source repo files → **byte-identical**; exporter (`elasticsearch`/`exporters:`) and scrape target (`otel-collector:8889`) confirmed to survive the copy.

## User Setup Required
None - no external service configuration required (dev-only committed creds; local Docker Desktop k8s).

## Next Phase Readiness
- Foundation substrate ready. The parallel manifest plans (80-02..80-06) can now reference `skp` (namespace), `skp-dev-secrets` (secretKeyRef), and the two ConfigMaps (subPath volume mounts).
- No blockers. Live `kubectl apply` (server-side) is exercised by the bring-up script plan, not this foundation plan.

## Self-Check: PASSED

- All 3 manifests + SUMMARY.md exist on disk.
- Both task commits (`b7840ef`, `f6ea44a`) present in git history.

---
*Phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c*
*Completed: 2026-07-18*
