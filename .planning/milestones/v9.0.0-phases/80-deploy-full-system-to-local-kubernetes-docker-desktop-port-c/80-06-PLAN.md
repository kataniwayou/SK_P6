---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/32-keeper.yaml
  - k8s/33-processor-sample.yaml
autonomous: true
requirements: [GOAL-80-HEALTHY, GOAL-80-ROUNDTRIP]

must_haves:
  truths:
    - "keeper runs as a Deployment with replicas: 2 and NO Service"
    - "processor-sample runs as a Deployment with replicas: 2 and NO Service"
    - "both ship the production TTL literal 900 and OMIT every test-only fault seam entirely (default-off)"
    - "processor-badconfig is NOT present (excluded, D-04)"
  artifacts:
    - path: "k8s/32-keeper.yaml"
      provides: "keeper Deployment replicas:2"
      contains: "replicas: 2"
    - path: "k8s/33-processor-sample.yaml"
      provides: "processor-sample Deployment replicas:2"
      contains: "Processor__ExecutionDataTtl"
  key_links:
    - from: "keeper"
      to: "Recovery__ExecutionDataTtlSeconds literal 900"
      via: "explicit env (test seams dropped)"
      pattern: "Recovery__ExecutionDataTtlSeconds"
    - from: "processor-sample"
      to: "Processor__ExecutionDataTtl literal 900"
      via: "explicit env (EXECUTION_DATA_TTL seam dropped)"
      pattern: "Processor__ExecutionDataTtl"
---

<objective>
Port the compose `keeper` (L233-269) and `processor-sample` (L287-320) blocks to Deployments with `replicas: 2` and NO Service (pure consumers). Both carry the production TTL literal (D-08) and OMIT every test-only fault seam entirely. `processor-badconfig` (compose L328-357, profile-gated) is EXCLUDED — no manifest (D-04).

Purpose: keeper ×2 (two-consumer recovery) and processor-sample ×2 (step execution) are the workers. This is where the D-08 seam discipline is most load-bearing — four fault-injection seams that must be absent so the code defaults to OFF.
Output: `k8s/32-keeper.yaml`, `k8s/33-processor-sample.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-CONTEXT.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
<interfaces>
<!-- Both tiers expose /health/live (always-Healthy self), /health/ready (HARD-on-broker), /health/startup. -->
<!-- Ports: keeper 8083, processor-sample 8082 (probed pod-local; no Service). -->
<!-- D-08 seam discipline for these two files: -->
<!--   OMIT ENTIRELY (absent => code :-0 default => OFF): KEEPER_DEFEAT_REINJECT, KEEPER_REINJECT_DELAY_MS, -->
<!--                                                      PROCESSOR_DEFEAT_READ, PROCESSOR_STEP_DELAY_MS, EXECUTION_DATA_TTL seam -->
<!--   CARRY AS LITERAL "900": Recovery__ExecutionDataTtlSeconds (keeper), Processor__ExecutionDataTtl (processor) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: keeper Deployment (replicas: 2, no Service)</name>
  <read_first>
    - `compose.yaml` lines 233-269 (keeper — build, deploy.replicas:2, RMQ/Redis/OTEL env, the two OMIT seams KEEPER_DEFEAT_REINJECT + KEEPER_REINJECT_DELAY_MS, the `${KEEPER_RECOVERY_TTL:-900}` → literal seam, /health/ready :8083)
    - `80-RESEARCH.md` Pattern 2 (Deployment replicas:2) and Pitfall 3
    - `80-PATTERNS.md` `k8s/32-keeper.yaml` section (D-08 CRITICAL note)
  </read_first>
  <action>
    Create `k8s/32-keeper.yaml` — Deployment ONLY, NO Service (namespace `skp`, label `app: keeper`):
    - `metadata.name: keeper`, `replicas: 2` (ports compose `deploy.replicas: 2`), matchLabels/pod label `app: keeper`.
    - Container `keeper`, `image: keeper:local`, `imagePullPolicy: IfNotPresent`.
    - env:
        RabbitMq__Host: rabbitmq
        RabbitMq__Username ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_USER
        RabbitMq__Password ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_PASS
        ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectTimeout=5000"
        OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4317"
        Recovery__ExecutionDataTtlSeconds: "900"   ← LITERAL (drop the ${KEEPER_RECOVERY_TTL:-900} seam, D-08)
      OMIT ENTIRELY: KEEPER_DEFEAT_REINJECT, KEEPER_REINJECT_DELAY_MS (absent ⇒ code :-0 default ⇒ OFF).
    - `ports: [{containerPort: 8083}]`.
    - readinessProbe: `httpGet: {path: /health/ready, port: 8083}`, `initialDelaySeconds: 30`, `periodSeconds: 10`, `timeoutSeconds: 3`, `failureThreshold: 5`.
    - livenessProbe: `httpGet: {path: /health/live, port: 8083}`, `initialDelaySeconds: 45`, `periodSeconds: 15`, `failureThreshold: 6`.
    - `resources: {requests: {memory: "160Mi"}, limits: {memory: "384Mi"}}`.
    Drop ALL `depends_on`. NO Service, NO container_name.
  </action>
  <verify>
    <automated>kubectl apply -f k8s/32-keeper.yaml --dry-run=client 2>&1 | grep -E "deployment.apps/keeper"; ! grep -qE "KEEPER_DEFEAT_REINJECT|KEEPER_REINJECT_DELAY_MS|kind: Service" k8s/32-keeper.yaml && echo "seams-absent-no-service-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/32-keeper.yaml` contains `image: keeper:local`, `imagePullPolicy: IfNotPresent`, `replicas: 2`
    - contains `Recovery__ExecutionDataTtlSeconds: "900"` (literal, no `${`)
    - does NOT contain `KEEPER_DEFEAT_REINJECT`, `KEEPER_REINJECT_DELAY_MS`, or any `${` interpolation
    - contains readiness `/health/ready` + liveness `/health/live` on port 8083
    - contains NO `kind: Service`
    - `kubectl apply -f k8s/32-keeper.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>keeper Deployment (replicas:2, no Service) validates with the literal 900 TTL, both fault seams omitted, and decoupled :8083 probes.</done>
</task>

<task type="auto">
  <name>Task 2: processor-sample Deployment (replicas: 2, no Service; badconfig excluded)</name>
  <read_first>
    - `compose.yaml` lines 287-320 (processor-sample — build, deploy.replicas:2, RMQ/Redis/OTEL env, the `${EXECUTION_DATA_TTL:-900}` → literal seam, the two OMIT seams PROCESSOR_STEP_DELAY_MS + PROCESSOR_DEFEAT_READ, /health/ready :8082) AND lines 328-357 (processor-badconfig — the EXCLUDED profile-gated block; do NOT port it)
    - `80-PATTERNS.md` `k8s/33-processor-sample.yaml` section (D-08 CRITICAL note; badconfig exclusion)
  </read_first>
  <action>
    Create `k8s/33-processor-sample.yaml` — Deployment ONLY, NO Service (namespace `skp`, label `app: processor-sample`):
    - `metadata.name: processor-sample`, `replicas: 2`, matchLabels/pod label `app: processor-sample`.
    - Container `processor-sample`, `image: processor-sample:local`, `imagePullPolicy: IfNotPresent`.
    - env:
        RabbitMq__Host: rabbitmq
        RabbitMq__Username ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_USER
        RabbitMq__Password ← secretKeyRef skp-dev-secrets/RABBITMQ_DEFAULT_PASS
        ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectTimeout=5000"
        OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4317"
        Processor__ExecutionDataTtl: "900"   ← LITERAL (drop the ${EXECUTION_DATA_TTL:-900} seam — this is the load-bearing production default, D-08)
      OMIT ENTIRELY: PROCESSOR_STEP_DELAY_MS, PROCESSOR_DEFEAT_READ (absent ⇒ code :-0 default ⇒ OFF).
    - `ports: [{containerPort: 8082}]`.
    - readinessProbe: `httpGet: {path: /health/ready, port: 8082}`, `initialDelaySeconds: 30`, `periodSeconds: 10`, `timeoutSeconds: 3`, `failureThreshold: 5`.
    - livenessProbe: `httpGet: {path: /health/live, port: 8082}`, `initialDelaySeconds: 45`, `periodSeconds: 15`, `failureThreshold: 6`.
    - `resources: {requests: {memory: "160Mi"}, limits: {memory: "384Mi"}}`.
    Drop ALL `depends_on` (compose adds baseapi-service — dropped; the app self-heals via readiness). NO Service. Do NOT create any processor-badconfig manifest (D-04 — excluded, mirrors compose default-profile exclusion).
  </action>
  <verify>
    <automated>kubectl apply -f k8s/33-processor-sample.yaml --dry-run=client 2>&1 | grep -E "deployment.apps/processor-sample"; ! grep -qE "PROCESSOR_DEFEAT_READ|PROCESSOR_STEP_DELAY_MS|badconfig|kind: Service" k8s/33-processor-sample.yaml && echo "seams-absent-no-badconfig-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/33-processor-sample.yaml` contains `image: processor-sample:local`, `imagePullPolicy: IfNotPresent`, `replicas: 2`
    - contains `Processor__ExecutionDataTtl: "900"` (literal, no `${`)
    - does NOT contain `PROCESSOR_DEFEAT_READ`, `PROCESSOR_STEP_DELAY_MS`, `badconfig`, or any `${`
    - contains readiness `/health/ready` + liveness `/health/live` on port 8082
    - contains NO `kind: Service`
    - `kubectl apply -f k8s/33-processor-sample.yaml --dry-run=client` exits 0
  </acceptance_criteria>
  <done>processor-sample Deployment (replicas:2, no Service) validates with the literal 900 TTL, both fault seams omitted, badconfig excluded, and decoupled :8082 probes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker pods → broker/redis/baseapi | keeper/processor are pure in-cluster consumers; no inbound Service, only kubelet probes hit the pod IP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-11 | Elevation of Privilege | test-only fault seams shipped enabled | mitigate | All four fault seams (KEEPER_DEFEAT_REINJECT, KEEPER_REINJECT_DELAY_MS, PROCESSOR_DEFEAT_READ, PROCESSOR_STEP_DELAY_MS) are OMITTED; acceptance greps assert their absence. Only production TTL literals ship (D-08). |
| T-80-12 | Tampering | badconfig subject deployed by mistake | mitigate | No processor-badconfig manifest authored; acceptance greps assert `badconfig` absent (D-04). |
</threat_model>

<verification>
- `kubectl apply -f k8s/32-keeper.yaml -f k8s/33-processor-sample.yaml --dry-run=client` exits 0.
- Both are replicas:2, no Service, literal 900 TTL, all fault seams absent; no badconfig manifest exists.
</verification>

<success_criteria>
keeper and processor-sample Deployments (both replicas:2, no Service, literal 900 TTL, every test seam omitted) validate client-side; processor-badconfig is not present.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-06-SUMMARY.md`.
</output>
