---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 09
type: execute
wave: 3
depends_on: ["07"]
files_modified:
  - scripts/phase-80-up.ps1
autonomous: true
requirements: [GOAL-80-HEALTHY, GOAL-80-ROUNDTRIP]

must_haves:
  truths:
    - "phase-80-up.ps1 ensures the compose stack is DOWN, applies the manifests, and waits every tier Ready via kubectl rollout status"
    - "After apply it kubectl rollout restart the 4 app Deployments (image/SourceHash currency) then re-waits Ready"
    - "It starts the port-forwards the harness AND the ~FanOutSeeder in-proc WebApi hard-code, bound to 127.0.0.1, and polls baseapi /health/ready before returning"
  artifacts:
    - path: "scripts/phase-80-up.ps1"
      provides: "k8s bring-up + health gate + port-forwards (parallels phase-65-up.ps1)"
      contains: "kubectl rollout status"
  key_links:
    - from: "phase-80-up.ps1"
      to: "app pods running fresh :local bits"
      via: "kubectl -n skp rollout restart deployment/<app>"
      pattern: "rollout restart"
    - from: "phase-80-up.ps1 port-forwards"
      to: "the harness + seeder hard-coded localhost ports"
      via: "kubectl port-forward svc/<svc> <host>:<container> --address 127.0.0.1"
      pattern: "port-forward"
---

<objective>
Author `scripts/phase-80-up.ps1` — the k8s bring-up paralleling `phase-65-up.ps1` (D-15). It (1) guards against a live compose stack (Pitfall 5 host-port collision), (2) `kubectl apply -k k8s/`, (3) waits infra then apps Ready via `kubectl rollout status` (replacing the hand-rolled NDJSON parse — the ×2 tiers are handled automatically), (4) `kubectl rollout restart` the 4 app Deployments so the freshly built `:local` images actually run (Pitfall 2 SourceHash currency), and (5) starts the port-forwards bound to 127.0.0.1 and polls baseapi readiness before returning.

Purpose: This is the single command that turns the manifests into a running, harness-reachable stack. Getting the rollout-restart and the port-forward list right is what makes the downstream reset/seed/POST/sweep work on the first try.
Output: `scripts/phase-80-up.ps1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
<interfaces>
<!-- PORT-FORWARD LIST — planner finding (load-bearing, read carefully): -->
<!-- D-14 enumerated 6 forwards. But the ~FanOutSeeder proof runs an in-proc WebApi via RealStackWebAppFactory -->
<!-- (tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs L417-458) which hard-codes EIGHT host endpoints: -->
<!--   Postgres localhost:5433, Redis localhost:6380, RabbitMq__Host localhost + RabbitMq__Port 5673 (AMQP), -->
<!--   OTEL_EXPORTER_OTLP_ENDPOINT http://localhost:4317. -->
<!-- So provision ALL EIGHT forwards (the 6 from D-14 PLUS rabbitmq AMQP 5673->5672 and otel 4317->4317). -->
<!-- This COMPLETES D-14's stated rationale ("expose the ports the harness hard-codes") — the seeder is part of the harness. -->
<!-- Full forward set (host->container): 8080->8080, 9090->9090, 9200->9200, 15673->15672, -->
<!--                                     6380->6379, 5433->5432, 5673->5672, 4317->4317 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: apply + phased rollout-status Ready gate + rollout-restart (image currency)</name>
  <read_first>
    - `scripts/phase-65-up.ps1` (FULL — the `$ErrorActionPreference='Stop'`, color-`Write-Host`, fail-loud `exit 2` idiom, the 10-tier canonical service list, the 180s deadline concept, and the otel `running`=ready special-case being ELIMINATED)
    - `80-RESEARCH.md` Pattern 4 (phased bring-up, per-tier `rollout status` timeouts) and Pitfall 2 (rollout restart after build) and Pitfall 5 (compose-down guard)
    - `80-PATTERNS.md` `scripts/phase-80-up.ps1` section
  </read_first>
  <action>
    Create `scripts/phase-80-up.ps1` (Push-Location repo root; `$ErrorActionPreference='Stop'`; `Write-Phase` helper prefixed `[phase-80-up]`):
      1. Pitfall-5 guard: run `docker compose down 2>$null` (or check `docker compose ps -q` is empty) so the compose stack does not hold the forward ports (8080/9090/9200/15673/6380/5433) — fail loud if any target host port is already bound at the end (Task 2 covers the bind check).
      2. Assert context: `kubectl config current-context` should be `docker-desktop` (warn loudly if not).
      3. `kubectl apply -k k8s/` — fail loud (exit 10) on non-zero.
      4. Phased INFRA Ready wait (each fail-loud exit 10):
           kubectl -n skp rollout status statefulset/postgres      --timeout=180s
           kubectl -n skp rollout status statefulset/redis         --timeout=120s
           kubectl -n skp rollout status statefulset/rabbitmq      --timeout=180s
           kubectl -n skp rollout status statefulset/elasticsearch --timeout=240s
           kubectl -n skp rollout status deployment/otel-collector --timeout=120s
           kubectl -n skp rollout status deployment/prometheus     --timeout=120s
      5. IMAGE CURRENCY (Pitfall 2): `kubectl -n skp rollout restart deployment/baseapi-service deployment/orchestrator deployment/keeper deployment/processor-sample` — forces pods onto the freshly built `:local` bits (same tag ⇒ no auto-redeploy).
      6. Phased APP Ready wait (rollout status is replica-aware — keeper/processor ×2 handled automatically; each fail-loud exit 10):
           kubectl -n skp rollout status deployment/baseapi-service  --timeout=180s
           kubectl -n skp rollout status deployment/orchestrator     --timeout=120s
           kubectl -n skp rollout status deployment/keeper           --timeout=120s
           kubectl -n skp rollout status deployment/processor-sample --timeout=120s
    Add a comment: STEP-A0 build (phase-80-build.ps1) MUST have run BEFORE this so the rollout-restart lands the current SourceHash (D-05). Do NOT re-implement the NDJSON per-replica poll — `rollout status` replaces it entirely.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Get-Command -Syntax scripts/phase-80-up.ps1" >/dev/null 2>&1; grep -c "kubectl -n skp rollout status" scripts/phase-80-up.ps1; grep -c "rollout restart deployment/baseapi-service deployment/orchestrator deployment/keeper deployment/processor-sample" scripts/phase-80-up.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-80-up.ps1` contains `kubectl apply -k k8s/`
    - contains `kubectl -n skp rollout status` for all 4 StatefulSets AND all 4 app Deployments AND otel-collector + prometheus (10 tiers)
    - contains a single `kubectl -n skp rollout restart` covering baseapi-service, orchestrator, keeper, processor-sample
    - contains a `docker compose down` (or equivalent) Pitfall-5 guard
    - contains fail-loud `exit` on rollout failures; the script parses under pwsh
  </acceptance_criteria>
  <done>phase-80-up.ps1 applies the stack, waits all 10 tiers Ready via rollout status, and rollout-restarts the 4 app Deployments for image currency.</done>
</task>

<task type="auto">
  <name>Task 2: start the 8 port-forwards (127.0.0.1) + baseapi readiness poll</name>
  <read_first>
    - `80-RESEARCH.md` Pattern 5 (multiple kubectl port-forward from PowerShell — `Start-Process kubectl -PassThru -WindowStyle Hidden`, `--address 127.0.0.1`, the reliability caveat) and the Security Domain threat "port-forward binds 0.0.0.0"
    - the `<interfaces>` block above (the EIGHT-forward list and WHY the seeder needs 5673 + 4317)
    - `tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs` lines 417-458 (confirm the in-proc WebApi host overrides: Postgres 5433 / Redis 6380 / RMQ 5673 / otel 4317) and `tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs` line 63 (Postgres localhost:5433)
  </read_first>
  <action>
    Append to `scripts/phase-80-up.ps1`: start EIGHT `kubectl port-forward` background processes, each `Start-Process kubectl -PassThru -WindowStyle Hidden -ArgumentList @('port-forward', $svc, $map, '-n', 'skp', '--address', '127.0.0.1')`. The forward set (svc host:container):
        svc/baseapi-service  8080:8080
        svc/prometheus       9090:9090
        svc/elasticsearch    9200:9200
        svc/rabbitmq         15673:15672   (management UI — harness Get-ProcQueueDepth / mgmt reads)
        svc/redis            6380:6379
        svc/postgres         5433:5432
        svc/rabbitmq         5673:5672     (AMQP — REQUIRED by the ~FanOutSeeder in-proc WebApi, RabbitMq__Port 5673)
        svc/otel-collector   4317:4317     (OTLP — REQUIRED by the ~FanOutSeeder in-proc WebApi OTEL endpoint)
      Collect the returned PIDs into a script-scoped `$pfProcs` and print a teardown hint (`$pfProcs | ForEach-Object { Stop-Process -Id $_.Id -Force }`). Optionally write the PIDs to a file (e.g. `.k8s-portforward-pids`) so plan 80-10's harness teardown can stop them.
      BEFORE returning, poll `http://localhost:8080/health/ready` until HTTP 200 (bounded ~60s, fail loud exit 11 on timeout) — this both confirms the forward is live and gives a clean hand-off to reset/seed (Pattern 5 reliability caveat).
      Add a comment explaining the 5673 + 4317 forwards complete D-14 for the seeder's in-proc WebApi (planner finding), and that all forwards bind 127.0.0.1 explicitly (never 0.0.0.0) per the security threat model.
  </action>
  <verify>
    <automated>grep -c "port-forward" scripts/phase-80-up.ps1; grep -cE "5673:5672|4317:4317|--address', '127.0.0.1|--address 127.0.0.1" scripts/phase-80-up.ps1; grep -c "localhost:8080/health/ready" scripts/phase-80-up.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-80-up.ps1` starts EIGHT port-forwards including `5673:5672` (rabbitmq AMQP) and `4317:4317` (otel) in addition to the D-14 six
    - every port-forward passes `--address 127.0.0.1` (never 0.0.0.0)
    - contains a bounded poll of `http://localhost:8080/health/ready` before returning
    - port-forward PIDs are captured for teardown
    - the script parses under pwsh
  </acceptance_criteria>
  <done>phase-80-up.ps1 starts all 8 required 127.0.0.1 forwards (D-14 six + the seeder's AMQP 5673 + otel 4317) and gates on baseapi readiness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host loopback → k8s Services | port-forward bridges 127.0.0.1 ports to ClusterIP Services; the only host→cluster ingress, loopback-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-17 | Information Disclosure | port-forward binds 0.0.0.0 → LAN exposure | mitigate | Every `kubectl port-forward` passes `--address 127.0.0.1` explicitly; services stay ClusterIP (no NodePort/ingress). |
| T-80-18 | Denial of Service | stale compose stack holds the forward ports | mitigate | Pitfall-5 `docker compose down` guard + fail-loud on a port already bound; prevents the harness talking to stale compose data. |
| T-80-19 | Tampering | stale image runs after rebuild | mitigate | `kubectl rollout restart` of the 4 app Deployments after build forces fresh `:local` bits (SourceHash currency, D-05). |
</threat_model>

<verification>
- `scripts/phase-80-up.ps1` parses under pwsh.
- Contains the 10-tier rollout-status gate, the 4-app rollout-restart, and 8 loopback-bound port-forwards incl. 5673 + 4317.
- Live smoke (deferred to plan 80-10): running it against Docker Desktop k8s reaches all-Ready and baseapi /health/ready == 200.
</verification>

<success_criteria>
phase-80-up.ps1 brings the stack up (compose-down guard → apply -k → 10-tier rollout-status → 4-app rollout-restart → 8 loopback port-forwards → baseapi readiness poll) with fail-loud semantics throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-09-SUMMARY.md`.
</output>
