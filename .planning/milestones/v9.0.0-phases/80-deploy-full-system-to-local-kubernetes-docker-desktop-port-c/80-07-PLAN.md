---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 07
type: execute
wave: 2
depends_on: ["01", "02", "03", "04", "05", "06"]
files_modified:
  - scripts/phase-80-build.ps1
  - k8s/kustomization.yaml
autonomous: true
requirements: [GOAL-80-HEALTHY, GOAL-80-ROUNDTRIP]

must_haves:
  truths:
    - "phase-80-build.ps1 builds all 5 app images tagged :local (baseapi-service, orchestrator, keeper, processor-sample) with fail-loud exit checks"
    - "kustomization.yaml aggregates all 13 manifests, stamps namespace skp + common labels, and applies cleanly"
    - "kubectl apply -k k8s/ --dry-run=server exits 0 (whole-stack schema + API-server validation)"
  artifacts:
    - path: "scripts/phase-80-build.ps1"
      provides: "5-image local build (SourceHash currency)"
      contains: "docker build -t"
    - path: "k8s/kustomization.yaml"
      provides: "ordered-apply aggregator (namespace + labels)"
      contains: "namespace: skp"
  key_links:
    - from: "kustomization.yaml resources list"
      to: "the 13 manifest files"
      via: "resources: [...]"
      pattern: "resources:"
    - from: "phase-80-build.ps1"
      to: ":local images the kubelet reads"
      via: "docker build -t <name>:local -f <Dockerfile> ."
      pattern: ":local"
---

<objective>
Author the two aggregation deliverables: `scripts/phase-80-build.ps1` (builds the 5 app images `:local`, D-04/D-05) and `k8s/kustomization.yaml` (the D-03 ordered-apply convenience aggregator that lists all manifests + stamps `namespace: skp` and common labels). This plan runs AFTER all manifests exist (Wave 1) so the kustomization can be validated whole via `kubectl apply -k k8s/ --dry-run=server`.

Purpose: The build script preserves the compose STEP-A0 SourceHash-currency guarantee (a stale image → different processor id → POST /start 422). The kustomization gives the bring-up script a single `kubectl apply -k k8s/` command and provides the phase's whole-stack lint gate.
Output: `scripts/phase-80-build.ps1`, `k8s/kustomization.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase-80-build.ps1 — 5 local image builds</name>
  <read_first>
    - `scripts/phase-67-harness.ps1` lines 169-198 (STEP A0/A — the SourceHash-currency rationale + the fail-loud `$LASTEXITCODE` idiom being ported)
    - `80-PATTERNS.md` `scripts/phase-80-build.ps1` section (the 4 confirmed Dockerfile paths)
    - `80-RESEARCH.md` "Build all 5 app images with :local" code example (L497-506)
  </read_first>
  <action>
    Create `scripts/phase-80-build.ps1`. Header comment: it replaces the compose STEP-A0 `docker compose build` with explicit local `:local` tags so the kubelet (sharing the Docker Desktop image store) can run them with `imagePullPolicy: IfNotPresent`. Body:
      `$ErrorActionPreference = 'Stop'`
      `Push-Location` repo root (Resolve-Path `$PSScriptRoot/..`).
      Then FOUR builds (build context = repo root; the Dockerfiles COPY src/ from root):
        docker build -t baseapi-service:local  -f Dockerfile .
        docker build -t orchestrator:local     -f src/Orchestrator/Dockerfile .
        docker build -t keeper:local           -f src/Keeper/Dockerfile .
        docker build -t processor-sample:local -f src/Processor.Sample/Dockerfile .
      After EACH build: `if ($LASTEXITCODE -ne 0) { Write-Host "build <name> failed" -ForegroundColor Red; exit 10 }` (preserve STEP-A0's fail-loud contract).
      Do NOT build `src/Processor.BadConfig/Dockerfile` (D-04 — excluded; add a comment noting it exists but is intentionally skipped).
      End with a success line + `exit 0` in a finally that Pop-Locations.
    (The 5th "image" count in D-04 counts baseapi as one of five app images; only these four Dockerfiles are built — Processor.BadConfig is the excluded fifth.)
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Get-Command -Syntax scripts/phase-80-build.ps1" >/dev/null 2>&1; grep -cE "docker build -t (baseapi-service|orchestrator|keeper|processor-sample):local" scripts/phase-80-build.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-80-build.ps1` contains exactly the 4 `docker build -t <name>:local -f <Dockerfile> .` lines (baseapi-service→Dockerfile, orchestrator/keeper/processor-sample→their src Dockerfiles)
    - does NOT build `Processor.BadConfig`
    - contains a fail-loud `$LASTEXITCODE` check after builds and `$ErrorActionPreference = 'Stop'`
    - the script parses (pwsh loads it without a syntax error)
  </acceptance_criteria>
  <done>phase-80-build.ps1 builds the 4 app images `:local` with fail-loud checks, excludes badconfig, and parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: kustomization.yaml aggregator + whole-stack dry-run gate</name>
  <read_first>
    - `80-RESEARCH.md` Pattern 7 (kustomization skeleton, configMapGenerator, `disableNameSuffixHash: true`) and the "Recommended k8s/ structure" file list (L131-149)
    - `80-PATTERNS.md` `k8s/kustomization.yaml` section
    - the 13 manifest files already authored in `k8s/` (00,01,02,10,11,12,13,20,21,30,31,32,33)
  </read_first>
  <action>
    Create `k8s/kustomization.yaml`:
      `apiVersion: kustomize.config.k8s.io/v1beta1`, `kind: Kustomization`
      `namespace: skp`
      `commonLabels: {app.kubernetes.io/part-of: skp, app.kubernetes.io/managed-by: kustomize}`
      `resources:` listing ALL 13 files in this order: 00-namespace.yaml, 01-secret.yaml, 02-configmaps.yaml, 10-postgres.yaml, 11-redis.yaml, 12-rabbitmq.yaml, 13-elasticsearch.yaml, 20-otel-collector.yaml, 21-prometheus.yaml, 30-baseapi-service.yaml, 31-orchestrator.yaml, 32-keeper.yaml, 33-processor-sample.yaml.
    Keep the ConfigMaps as the hand-authored `02-configmaps.yaml` (plan 80-01) — do NOT add a `configMapGenerator` unless you first REMOVE `02-configmaps.yaml` from `resources` AND set `generatorOptions: {disableNameSuffixHash: true}` with names `otel-collector-config` + `prometheus-config` (otherwise the Deployment volume references break). Default to the hand-authored file (simplest, guaranteed-working — RESEARCH Open-Q4).
    NOTE: `commonLabels` also injects into selectors; since every manifest already sets `app: <tier>` and matchLabels consistently, the added part-of/managed-by labels are additive and safe. If `kubectl apply -k` reports an immutable-selector conflict on the dry-run, move the two common labels to `labels:` (non-selector) form instead of `commonLabels`.
    Then validate the WHOLE stack: `kubectl apply -k k8s/ --dry-run=client` then `--dry-run=server` (server validates against the live API — requires the docker-desktop context; if the cluster is unreachable, client dry-run is the fallback gate and the server gate moves to plan 80-10).
  </action>
  <verify>
    <automated>kubectl apply -k k8s/ --dry-run=client >/dev/null 2>&1 && echo "kustomize-client-dryrun-ok"; grep -c "\.yaml" k8s/kustomization.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/kustomization.yaml` contains `namespace: skp` and a `resources:` list naming all 13 manifest files
    - `kubectl apply -k k8s/ --dry-run=client` exits 0 (whole-stack schema validation)
    - if the docker-desktop cluster is reachable, `kubectl apply -k k8s/ --dry-run=server` also exits 0 (record the result in the SUMMARY; defer to plan 80-10 if unreachable)
  </acceptance_criteria>
  <done>kustomization aggregates all 13 manifests with the skp namespace + labels, and `kubectl apply -k k8s/ --dry-run=client` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host Docker daemon → kubelet image store | `:local` images built on the host are read by the Docker Desktop kubelet; no registry, no network pull. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-13 | Tampering | stale image survives rebuild | mitigate | Build script re-tags `:local` each run; the bring-up script (80-09) `kubectl rollout restart` forces pods onto fresh bits — together they preserve SourceHash currency (D-05). |
| T-80-14 | Denial of Service | malformed manifest reaches the cluster | mitigate | `kubectl apply -k --dry-run` gate on every apply catches schema/label errors before they hit the API server. |
</threat_model>

<verification>
- `kubectl apply -k k8s/ --dry-run=client` exits 0.
- phase-80-build.ps1 builds exactly the 4 app images `:local` and excludes badconfig.
</verification>

<success_criteria>
The build script produces the 4 `:local` app images with fail-loud checks; the kustomization aggregates all 13 manifests under `namespace: skp` and passes the whole-stack client dry-run.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-07-SUMMARY.md`.
</output>
