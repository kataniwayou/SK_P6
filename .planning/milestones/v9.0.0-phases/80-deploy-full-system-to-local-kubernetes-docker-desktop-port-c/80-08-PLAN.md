---
phase: 80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c
plan: 08
type: execute
wave: 2
depends_on: ["01", "02", "05", "06"]
files_modified:
  - scripts/phase-80-reset.ps1
autonomous: true
requirements: [GOAL-80-ROUNDTRIP]

must_haves:
  truths:
    - "The k8s reset FLUSHALLs Redis and FK-safe graph-DELETEs Postgres by reaching k8s pods via kubectl exec (NOT docker exec)"
    - "The 60s heal-wait bounded poll for a fresh per-instance liveness key is preserved"
    - "The static-literal SQL (FK-safe DELETE order) is preserved byte-for-byte; processors + config_schemas are NOT deleted"
  artifacts:
    - path: "scripts/phase-80-reset.ps1"
      provides: "k8s-targeted clean-state reset (Pitfall 1 re-target)"
      contains: "kubectl -n skp exec"
  key_links:
    - from: "reset FLUSHALL/heal-wait/DELETE"
      to: "k8s redis-0 / postgres-0 pods"
      via: "kubectl -n skp exec statefulset/<svc> -- <cmd>"
      pattern: "kubectl -n skp exec statefulset/(redis|postgres)"
---

<objective>
Author `scripts/phase-80-reset.ps1` — the k8s sibling of `phase-65-reset.ps1`. This is the first half of the RESEARCH Pitfall-1 landmine: the compose reset reaches Postgres/Redis via the docker/compose CLI, which CANNOT see k8s pods. Every `docker exec`/`docker compose exec`/`docker compose ps` touch-point is re-targeted to `kubectl exec` / `kubectl get pods`, per the exact swap table in PATTERNS.md — while preserving the FK-safe DELETE order, the 60s heal-wait, the static-literal SQL, and the fail-loud `exit 2` semantics VERBATIM.

Purpose: Without this, the reset aborts immediately on `docker exec sk-redis` ("No such container") and no clean-state proof is possible. This is the difference between a working proof and a first-run failure.
Output: `scripts/phase-80-reset.ps1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-RESEARCH.md
@.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase-80-reset.ps1 — kubectl-exec re-target of the compose reset</name>
  <read_first>
    - `scripts/phase-65-reset.ps1` (FULL — the exact source being ported: STEP 0 pre-flight, STEP 1 FLUSHALL, STEP 2 heal-wait poll + the `^skp:proc:[^:]+$` exclusion regex, STEP 3 FK-safe DELETE transaction + migration Down() order, STEP 4 processor-set assertion + badconfig removal)
    - `80-PATTERNS.md` "k8s reset re-target" section (the concrete 1:1 swap table, file:line → k8s re-target)
    - `80-RESEARCH.md` Pitfall 1 (the docker→kubectl mapping + why port-forward TCP does NOT cover these steps) and the Security Domain note (keep static-literal SQL — no interpolation)
  </read_first>
  <action>
    Create `scripts/phase-80-reset.ps1` by porting `phase-65-reset.ps1` step-for-step, swapping ONLY the infra touch-points (keep `$ErrorActionPreference='Stop'`, the `Write-Phase` helper re-prefixed to `[phase-80-reset]`, every `exit 2`, the 60s deadline, and all comments explaining WHY):
      - STEP 0 pre-flight (postgres running): replace `docker compose ps postgres --format json` with `kubectl -n skp get pods -l app=postgres --field-selector=status.phase=Running -o name` — abort (exit 2) if zero. Hint to run `phase-80-up.ps1` first.
      - STEP 1 FLUSHALL: `docker exec sk-redis redis-cli FLUSHALL` -> `kubectl -n skp exec statefulset/redis -- redis-cli FLUSHALL`. Keep the `$LASTEXITCODE` fail-loud.
      - STEP 2 heal-wait (bounded 60s, `$deadline = (Get-Date).AddSeconds(60)`): `docker exec sk-redis redis-cli --scan --pattern 'skp:proc:*'` -> `kubectl -n skp exec statefulset/redis -- redis-cli --scan --pattern 'skp:proc:*'`. Preserve the `Where-Object { $_ -notmatch '^skp:proc:[^:]+$' }` per-instance-key filter and the `>= 1` heal condition VERBATIM.
      - STEP 3 FK-safe DELETE: `docker compose exec -T postgres psql -U postgres -d stepsdb -c "..."` -> `kubectl -n skp exec statefulset/postgres -- psql -U postgres -d stepsdb -c "BEGIN; DELETE FROM step_next_steps; DELETE FROM workflow_assignments; DELETE FROM workflow_entry_steps; DELETE FROM assignments; DELETE FROM workflows; DELETE FROM steps; COMMIT;"`. Keep the SAME static-literal SQL in the SAME migration-Down() order; PRESERVE processors + config_schemas (NOT in the DELETE list). No interpolated input (threat T-65-06).
      - STEP 4 processor-set assertion: `docker compose ps processor-sample --format json` -> `kubectl -n skp get pods -l app=processor-sample -o json` (assert >= 1 Running; note expected 2). The compose STEP-4 badconfig orphan removal is OMITTED — no badconfig pod exists in k8s (excluded, D-04); replace with a one-line comment saying so.
      - STEP 5 success summary + `exit 0`.
    Add a header note: this is the Pitfall-1 re-target; the HTTP/metrics touch-points (seeder, POST /start, analyzer, sweep) are unchanged — they use localhost ports covered by port-forward (plan 80-09).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Get-Command -Syntax scripts/phase-80-reset.ps1" >/dev/null 2>&1; grep -c "kubectl -n skp exec statefulset/\(redis\|postgres\)" scripts/phase-80-reset.ps1; ! grep -qE "docker exec|docker compose" scripts/phase-80-reset.ps1 && echo "no-docker-cli-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-80-reset.ps1` contains `kubectl -n skp exec statefulset/redis -- redis-cli FLUSHALL`
    - contains `kubectl -n skp exec statefulset/redis -- redis-cli --scan --pattern 'skp:proc:*'` AND the `^skp:proc:[^:]+$` exclusion regex AND a 60s deadline
    - contains `kubectl -n skp exec statefulset/postgres -- psql -U postgres -d stepsdb` with the 6-table FK-safe DELETE in migration-Down() order inside a single BEGIN/COMMIT
    - does NOT contain `docker exec` or `docker compose` anywhere
    - preserves `exit 2` fail-loud semantics; the script parses under pwsh
  </acceptance_criteria>
  <done>phase-80-reset.ps1 reaches redis-0/postgres-0 via kubectl exec, preserves the heal-wait + FK-safe static SQL + fail-loud contract, and contains no docker-CLI calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host reset script → k8s pods | Reset runs `kubectl exec` into redis-0/postgres-0; destructive (FLUSHALL + graph DELETE) but scoped to the local dev `skp` namespace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-15 | Injection | `kubectl exec -- psql/redis-cli` with interpolated input | mitigate | SQL + redis commands are STATIC LITERALS carried verbatim from phase-65-reset.ps1 (no interpolated/untrusted input) — same posture as threat T-65-06 (RESEARCH Security Domain). |
| T-80-16 | Tampering | reset pointed at a shared/prod DB | mitigate | `kubectl -n skp` scopes every op to the local dev namespace; header comment warns dev-only destructive tooling (ported from phase-65-reset.ps1 .NOTES). |
</threat_model>

<verification>
- `scripts/phase-80-reset.ps1` parses under pwsh and contains no `docker` CLI calls.
- FK-safe DELETE order + heal-wait + fail-loud semantics preserved from phase-65-reset.ps1.
</verification>

<success_criteria>
phase-80-reset.ps1 is a faithful kubectl-exec re-target of phase-65-reset.ps1 — FLUSHALL, 60s heal-wait, FK-safe static-SQL graph DELETE, processor-set assertion — with zero docker-CLI coupling.
</success_criteria>

<output>
After completion, create `.planning/phases/80-deploy-full-system-to-local-kubernetes-docker-desktop-port-c/80-08-SUMMARY.md`.
</output>
