---
phase: 74-reshape-business-metrics-into-a-uniform-two-counter-model
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - tests/BaseApi.Tests/Observability/Analysis/PromCounterSnapshot.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs
  - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs
  - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs
  - tests/BaseApi.Tests/Orchestrator/OrchestratorMetricsFacts.cs
  - tests/BaseApi.Tests/Processor/ProcessorMetricsFacts.cs
  - tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs
  - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/OrchestratorReinjectConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs
  - tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs
  - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
autonomous: true
requirements: [1, 2, 3, 5, 6]
must_haves:
  truths:
    - "PassFailEngine + PromCounterSnapshot reference the new *_messages_consumed/*_messages_sent names and no longer key on outcome or the three removed counters"
    - "The non-completed outcome corroboration WARNING path is deleted from PassFailEngine"
    - "AnalyzerE2ETests live PromQL strings target the new metric names and compile"
    - "Hermetic metric-facts subset is GREEN with explicit absence asserts for the 3 removed counters"
    - "0-warning Debug AND Release build of the full solution"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/Analysis/PromCounterSnapshot.cs"
      provides: "renamed delta fields; NonCompletedOutcomes + dedup + reinject-dropped fields removed"
      contains: "MessagesSent"
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      provides: "bindings repointed to new fields; outcome WARNING block deleted"
      contains: "MessagesSent"
  key_links:
    - from: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      to: "PromCounterSnapshot new delta fields"
      via: "Expected = COMPLETE runs × 9 repointed to processor_messages_sent total (D-12)"
      pattern: "MessagesSent"
    - from: "tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs"
      to: "new metric names + inverted workflowId guard"
      via: "PromQL rename + cardinality-guard inversion (workflowId now REQUIRED)"
      pattern: "messages_(consumed|sent)"
---

<objective>
Rebind the live-stack analyzer (`PromCounterSnapshot` + `PassFailEngine` + `AnalyzerE2ETests`) and
migrate the ~11 affected metric test files to the new names/labels, add explicit absence asserts for
the three removed counters, invert the `MetricsRoundTripE2ETests` cardinality guard (the new model
REQUIRES `workflowId`), and run the final hermetic + 0-warning gate (REQ-5, plus the absence/build
acceptance for REQ-1/2/3/6).

Purpose: This is the converging wave. Plans 01-03 renamed the production instruments; the analyzer
and tests still key on the old names + the dropped `outcome` label. This plan repoints them, deletes
the `outcome` corroboration WARNING path (D-13), and proves the whole new model green hermetically.

Output: a green metric-facts subset, a compiling RealStack analyzer, and a 0-warning solution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-SPEC.md
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-CONTEXT.md
@.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-PATTERNS.md

<hermetic_test_command>
The BaseApi.Tests project is xunit.v3 under MTP; `dotnet test` HANGS on Windows. Run the EXE directly:
  tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait "Category=RealStack" --timeout 8m
RealStack tests need the live Docker stack — they are EXCLUDED from the hermetic run but MUST still
COMPILE. Do NOT make a RealStack test a blocking acceptance gate (sandbox lacks Docker). Use
--filter-class "FQN" / --filter-method "*Name*" to run a metric-facts subset. The VSTest --filter
flag is IGNORED under MTP (logs MTP0001) — use the native --filter-* options.
</hermetic_test_command>

<interfaces>
<!-- The new production names this plan must rebind tests/analyzer to (from plans 01-03): -->
orchestrator_messages_consumed, orchestrator_messages_sent   (labels: workflowId, processorId)
processor_messages_consumed,    processor_messages_sent      (labels: workflowId, processorId — NO outcome)
keeper_messages_consumed,       keeper_messages_sent          (labels: workflowId, processorId)
keeper_l2_probe                 (label-less)

Removed (must produce no series / no member — D-14 absence asserts):
  orchestrator_result_deduped (member ResultDeduped), processor_dispatch_deduped (member DispatchDeduped),
  keeper_reinject_dropped (member ReinjectDropped), and the processor `outcome` label.
Also renamed-away (old names must be absent): orchestrator_dispatch_sent, orchestrator_result_consumed,
  processor_dispatch_consumed, processor_result_sent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebind PromCounterSnapshot + PassFailEngine (drop outcome WARNING)</name>
  <files>tests/BaseApi.Tests/Observability/Analysis/PromCounterSnapshot.cs, tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs, tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PromCounterSnapshot.cs (fields lines 23-58; NonCompletedOutcomes line 57; dedup nullables 44/50)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (TriggerCountFrom 105; promImpliedRuns 257; expectedResultConsumed 296; spawnReconExcess 297; NonCompletedOutcomes WARNING block 280-287)
    - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs (doc-comment + ExpectedResultConsumed field — keep field, fix removed-name refs)
    - 74-CONTEXT.md D-12/D-13; 74-SPEC.md REQ-5 acceptance (PassFailEngine compiles, no outcome/removed-counter refs)
    - 74-PATTERNS.md the Analyzer rebinding section (exact field-line map)
  </read_first>
  <action>
    In PromCounterSnapshot.cs (D-13):
    - Rename the surviving delta fields to the new metric names:
        DispatchSentDelta → OrchestratorMessagesSentDelta (orchestrator_messages_sent_total)
        ResultConsumedDelta → OrchestratorMessagesConsumedDelta (orchestrator_messages_consumed_total)
        DispatchConsumedDelta → ProcessorMessagesConsumedDelta (processor_messages_consumed_total)
        ResultSentCompletedDelta → ProcessorMessagesSentDelta (processor_messages_sent_total — D-12: all-complete fixture ⇒ total == completed)
      (Use these names consistently; if you prefer different field identifiers, keep them descriptive of the new metric and update ALL references in PassFailEngine + the fact tests.)
    - DELETE these fields entirely: `KeeperReinjectDroppedDelta` (38), `ResultDedupedDelta` (44), `DispatchDedupedDelta` (50), and `NonCompletedOutcomes` (57 — the outcome-keyed dictionary).
    In PassFailEngine.cs (D-13):
    - Repoint `TriggerCountFrom` (line ~105), `promImpliedRuns = round(prom.DispatchSentDelta / 9)` (line ~257), `expectedResultConsumed = prom.DispatchSentDelta + spawnExtra` (line ~296), and `spawnReconExcess = prom.ResultConsumedDelta - expectedResultConsumed` (line ~297) to the renamed fields (OrchestratorMessagesSentDelta / OrchestratorMessagesConsumedDelta). The "Expected = COMPLETE runs × 9" close-gate math survives unchanged via the repointed ProcessorMessagesSentDelta total (D-12).
    - DELETE the `nonCompletedOutcomes = prom.NonCompletedOutcomes…` line (~280) and the entire non-completed corroboration WARNING block (~281-287). The `outcome` split no longer exists (D-13).
    In AnalyzerReport.cs:
    - It is mostly doc-comment + the `ExpectedResultConsumed` field (which STAYS). Update any doc-comment references to the old `DispatchSentDelta` name; ensure no compile reference to a now-removed PromCounterSnapshot field remains.
  </action>
  <verify>
    <automated>! grep -nE 'NonCompletedOutcomes|KeeperReinjectDroppedDelta|ResultDedupedDelta|DispatchDedupedDelta|"outcome"' tests/BaseApi.Tests/Observability/Analysis/PromCounterSnapshot.cs tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs &amp;&amp; grep -nE 'MessagesSent|MessagesConsumed|messages_sent|messages_consumed' tests/BaseApi.Tests/Observability/Analysis/PromCounterSnapshot.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "NonCompletedOutcomes|KeeperReinjectDroppedDelta|ResultDedupedDelta|DispatchDedupedDelta" tests/BaseApi.Tests/Observability/Analysis/PromCounterSnapshot.cs` returns NOTHING
    - `grep -nE "NonCompletedOutcomes|\"outcome\"" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` returns NOTHING (WARNING block + outcome key gone)
    - PromCounterSnapshot.cs references `processor_messages_sent` / new field names (grep for `messages_sent` or the chosen field identifiers matches)
    - `grep -nE "DispatchSentDelta|ResultConsumedDelta|DispatchConsumedDelta|ResultSentCompletedDelta" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` returns NOTHING (all repointed)
  </acceptance_criteria>
  <done>Analyzer DTO + engine reference only the new field/metric names; the outcome WARNING path is deleted; ExpectedResultConsumed retained.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate metric fact tests + add 3 absence asserts; rework RealStack PromQL</name>
  <files>tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs, tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs, tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs, tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs, tests/BaseApi.Tests/Keeper/OrchestratorReinjectConsumerFacts.cs, tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs, tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs, tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs, tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs</files>
  <read_first>
    - 74-PATTERNS.md the Test Inventory table (exact files, line numbers, old literals per file) + the D-14 absence-assert targets
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs (snapshot field set 55-61, fixtures 132-257)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs (snapshot field set 72-78)
    - tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs (targets the REMOVED dedup counters — DispatchDeduped/ResultDeduped)
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs + OrchestratorReinjectConsumerFacts.cs (keeper_reinject_dropped + Interlocked dropped assert)
    - tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs (old PromQL 124-204; outcome assert; the "NO workflowId" cardinality guard 151-152, 231-256 — INVERT it)
    - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs (live PromQL builders 539-601)
    - 74-CONTEXT.md D-14 (rename-in-place + absence asserts); 74-SPEC.md REQ-5 acceptance (metric-facts GREEN)
  </read_first>
  <action>
    Rename-in-place (D-14). Hermetic fact files (#1/#2 are handled in Plans 01/02; here cover the rest):
    - PassFailEngineFacts.cs + PassFailEngineValueChainFacts.cs: repoint the PromCounterSnapshot field set to the Task-1 renamed fields; DROP the dedup/reinject/outcome fixture rows (the fields are gone).
    - BreakerMetricsFacts.cs: this test targets the now-REMOVED `DispatchDeduped`/`ResultDeduped` members + `processor_dispatch_deduped`/`orchestrator_result_deduped` names. REWRITE it as ABSENCE asserts: assert OrchestratorMetrics has no `ResultDeduped` member and ProcessorMetrics has no `DispatchDeduped` member, and that no `orchestrator_result_deduped_*` / `processor_dispatch_deduped_*` series is emitted (D-14). If member-absence is a compile-time fact, the absence is proven by the file compiling without those references — convert the assertions to assert the new counters increment instead, plus a comment documenting the removed-counter absence.
    - ReinjectConsumerFacts.cs + OrchestratorReinjectConsumerFacts.cs: the drop-counter assert (`keeper_reinject_dropped`, `Interlocked.Read(ref dropped)==1`) is gone. REWRITE the by-design-drop test to assert ack + NO send on the drop path (no `keeper_messages_sent` increment), and assert `keeper_messages_sent` DOES increment on the success path. Remove every `keeper_reinject_dropped` / `ReinjectDropped` reference.
    - RecoveryDeadLetterFacts.cs: keep the `.AddSingleton<KeeperMetrics>()` (line ~53) — verify DI still satisfies the new RecoveryConsumerBase + 4-sender ctor (the new `metrics` param resolves from the singleton). Add the 4 sending consumers / base to the DI graph if the test constructs them directly.
    ADD the three D-14 absence asserts (no-series) for: `orchestrator_result_deduped_*`, `processor_dispatch_deduped_*`, `keeper_reinject_dropped_*`. Place them in the most natural hermetic fact file per counter (e.g. BreakerMetricsFacts for the two dedup counters; ReinjectConsumerFacts for the keeper drop). Where a hermetic MeterCollector-style assert is possible, assert the named counter records zero/no series after the relevant flow.

    RealStack files (excluded from hermetic run but MUST COMPILE):
    - AnalyzerE2ETests.cs (lines 539-601): replace `processor_result_sent_total{outcome=…}` (542) and DELETE the outcome loop (539-543); `orchestrator_dispatch_sent_total` (547) → `orchestrator_messages_sent_total`; `processor_result_sent_total{outcome="completed"}` (551) → `processor_messages_sent_total`; rename the delta-subtraction block fields (594-601) to match the renamed PromCounterSnapshot fields.
    - MetricsRoundTripE2ETests.cs: rename queries `orchestrator_dispatch_sent_total` (124,144,204) → `orchestrator_messages_sent_total`; `orchestrator_result_consumed_total` (128) → `orchestrator_messages_consumed_total`; `processor_dispatch_consumed_total` (134,145,185) → `processor_messages_consumed_total`; `processor_result_sent_total` (138) → `processor_messages_sent_total`. DROP the `outcome` assert (151-152). INVERT the "NO workflowId" cardinality guard (231-256): the new model REQUIRES `workflowId` — assert the series DO carry a `workflowId` label rather than asserting its absence.
    - SC2RecoveryPathsE2ETests.cs: doc/comment only — update the `keeper_reinject_dropped` narrative (38,155) to reflect the removed counter; assert no drop counter.
  </action>
  <verify>
    <automated>! grep -rnE 'orchestrator_dispatch_sent|orchestrator_result_consumed|processor_dispatch_consumed|processor_result_sent|keeper_reinject_dropped|DispatchDeduped|ResultDeduped|ReinjectDropped|outcome=' tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "orchestrator_dispatch_sent|orchestrator_result_consumed|processor_dispatch_consumed|processor_result_sent" tests/BaseApi.Tests/` returns NOTHING (all renamed)
    - `grep -rnE "keeper_reinject_dropped|ReinjectDropped|DispatchDeduped|ResultDeduped" tests/BaseApi.Tests/` returns NOTHING (removed members/counters)
    - `grep -rnE "outcome=" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs` returns NOTHING (outcome label gone from PromQL)
    - MetricsRoundTripE2ETests asserts `workflowId` IS present (grep for `workflowId` shows a positive/required assertion, not a "no workflowId" guard)
    - Explicit absence asserts exist for all three removed counters (grep the test files for the three counter base names appears only inside absence/no-series assertions or comments documenting removal)
  </acceptance_criteria>
  <done>All metric tests reference the new names/labels; the 3 removed counters have absence asserts; the cardinality guard is inverted; RealStack files compile.</done>
</task>

<task type="auto">
  <name>Task 3: Final hermetic metric-facts gate + 0-warning Debug & Release build</name>
  <files>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe</files>
  <read_first>
    - <hermetic_test_command> block in this plan's context (EXE-direct runner; --filter-not-trait Category=RealStack)
    - 74-SPEC.md Acceptance Criteria (metric-facts subset GREEN + 0-warning Debug & Release; live-stack gate is deferred-automated)
    - The 74-01/74-02/74-03 SUMMARY files (to confirm prior-wave member/name changes landed as expected)
  </read_first>
  <action>
    Build gate (0-warning, both configs, full solution):
      dotnet build -c Debug -warnaserror
      dotnet build -c Release -warnaserror
    Both MUST report 0 warnings / 0 errors. Treat any warning as a failure (the deleted ResultOutcome helper + removed members are the likely warning sources if a reference was missed).

    Hermetic metric-facts gate (Debug build of the test EXE must exist first):
      tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait "Category=RealStack" --timeout 8m
    Confirm GREEN. If running the full hermetic suite is too broad, run the metric-facts subset by class:
      …BaseApi.Tests.exe --filter-class "*MetricsFacts" --filter-class "*PassFailEngine*" --filter-class "*ReinjectConsumerFacts" --timeout 8m
    plus the broad run to confirm no collateral breakage. RealStack tests are EXCLUDED (sandbox lacks Docker) but the project MUST compile (the -warnaserror build above proves RealStack files compile).

    Final cross-cutting absence grep over the PRODUCTION tree (proves the removed names emit no series at source):
      ! grep -rnE 'orchestrator_result_deduped|processor_dispatch_deduped|keeper_reinject_dropped' src/
      ! grep -rnE '"outcome"' src/BaseProcessor.Core/Processing/OutputTail.cs

    Record the live-stack PassFailEngine close-gate run as deferred-automated in the SUMMARY (SPEC AC #8 — sandbox lacks Docker); do NOT block on it.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait "Category=RealStack" --timeout 8m</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build -c Debug -warnaserror` → 0 warnings, 0 errors
    - `dotnet build -c Release -warnaserror` → 0 warnings, 0 errors
    - The hermetic run (`--filter-not-trait "Category=RealStack"`) reports all tests passed (GREEN)
    - `grep -rnE "orchestrator_result_deduped|processor_dispatch_deduped|keeper_reinject_dropped" src/` returns NOTHING
    - `grep -rnE "\"outcome\"" src/BaseProcessor.Core/Processing/OutputTail.cs` returns NOTHING
    - SUMMARY records the live-stack close-gate as deferred-automated (Docker unavailable)
  </acceptance_criteria>
  <done>Hermetic metric-facts subset GREEN; 0-warning Debug + Release; no removed-counter names in src/; live-stack gate documented as deferred-automated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyzer (test harness) → Prometheus / Elasticsearch | reads counter series for the close-gate verdict (read-only; existing) |

This plan only touches test + analyzer code. No production surface, no auth, no user input. The
analyzer is a read-only verdict engine over already-exported metrics.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-08 | T (tampering — false-green verdict) | PassFailEngine repoint to processor_messages_sent total (D-12) | mitigate | The repoint preserves the "Expected = COMPLETE × 9" invariant only because the close-gate fixture is all-complete (total == completed). Acceptance asserts the engine compiles and the metric-facts subset is GREEN, guarding against a silently-passing verdict that no longer reflects real completion. |
| T-74-09 | R (repudiation — lost outcome corroboration) | dropped outcome WARNING path | accept | The non-completed-outcome corroboration WARNING is intentionally removed (D-13) because the `outcome` label no longer exists; the primary completion math is unaffected. No security-relevant audit signal is lost (it was a corroboration warning, not a gate). |

No high-severity threats; test/analyzer-only changes over read-only metric data.
</threat_model>

<verification>
- 0-warning Debug + Release (full solution).
- Hermetic metric-facts subset GREEN via the EXE-direct runner (RealStack excluded, must compile).
- No old metric names or removed members anywhere in src/ or tests/.
- Three removed counters have explicit absence asserts (D-14).
- Live-stack PassFailEngine close gate recorded as deferred-automated (SPEC AC #8 — Docker unavailable).
</verification>

<success_criteria>
- PassFailEngine + PromCounterSnapshot reference the new names; no outcome / removed-counter refs (REQ-5).
- All ~11 metric test files migrated in place; the 3 removed counters have absence asserts (REQ-1/2/3, D-14).
- MetricsRoundTripE2ETests cardinality guard inverted (workflowId now required).
- Hermetic metric-facts GREEN + 0-warning Debug & Release (REQ-6, SPEC AC #7).
</success_criteria>

<output>
After completion, create `.planning/phases/74-reshape-business-metrics-into-a-uniform-two-counter-model/74-04-SUMMARY.md`
</output>
