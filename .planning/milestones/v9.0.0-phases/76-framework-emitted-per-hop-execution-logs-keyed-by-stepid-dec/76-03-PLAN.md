---
phase: 76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs
  - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs
  - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs
autonomous: true
requirements: [ANL-01, ANL-02, ANL-03, SMP-01]

must_haves:
  truths:
    - "Structural completeness is computed per (correlationId, executionId) from attributes.StepId on framework records; HopLabels is deleted and no Step_* literal exists on the structural path"
    - "The expected set is derived from ES FW-02 orchestrator dispatch records; a dispatched-but-never-executed step yields Verdict=Fail (TEST-08 closed); no Postgres/graph read on the analyzer path"
    - "A run missing hop N's processor record but carrying the orchestrator record that consumed M_N reconciles as a non-binding telemetry gap with NO seed oracle supplied; a run missing BOTH records stays a binding miss"
    - "A framework record with ExecutionId == Guid.Empty is an entry MARKER — counted as entry-ran, excluded from any (corr,exec) expected/complete set (D-09)"
    - "The verdict is computed from framework records only (no Step_*/Received/Produced needed); with the value oracle absent the value-chain check degrades to not-applicable, never FAIL (SMP-01)"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs"
      provides: "StepIdFieldPath (+ NextStepIdFieldPath); StepLabelFieldPath/Received/Produced KEPT"
      contains: "StepIdFieldPath"
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      provides: "stepId-keyed completeness + ES-derived expected set + generalized framework-redundancy reconciliation; HopLabels deleted"
      contains: "StepId"
    - path: "tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs"
      provides: "DistinctStepIds structural set + FromStepIds build path; Values/DistinctLabels kept for the oracle"
      contains: "DistinctStepIds"
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs"
      provides: "ANL-01/02/03/SMP-01 engine facts built from stepId sets with zero Step_* labels"
  key_links:
    - from: "PassFailEngine completeness"
      to: "RunTrace.DistinctStepIds vs ES-derived expected set"
      via: "stepId-keyed set comparison (no StepLabel)"
      pattern: "DistinctStepIds"
    - from: "PassFailEngine reconciliation (:185-209 generalized)"
      to: "orchestrator FW-02 record consuming M_N"
      via: "framework-redundancy path with no seed oracle"
      pattern: "EntryId|redundan"
---

<objective>
Re-key the analyzer's structural half onto `attributes.StepId` framework records: DELETE `HopLabels`, replace the `StepLabel` set-compare with stepId-keyed completeness against an ES-derived expected set (ANL-02, closes TEST-08), generalize the telemetry-gap reconciliation to framework redundancy (ANL-03, no seed oracle), and treat the empty-executionId record as an entry marker (D-09). The `StepLabel`/`Produced` value oracle stays as a separate, degradable layer (SMP-01, D-16). This is the clean break of D-15 — one structural source of truth.

Purpose: the verdict stops binding to author-chosen `Step_*` strings and works for ANY processor with zero author logs.
Output: `EsIndexNames` const, `RunTrace` structural model, re-keyed `AnalyzerE2ETests` ES readers, re-keyed `PassFailEngine` completeness/expected-set/reconciliation, and the ANL-01/02/03/SMP-01 engine facts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-SPEC.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-CONTEXT.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-RESEARCH.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-PATTERNS.md

<interfaces>
<!-- Confirmed live 2026-07-16. This plan is a re-key of existing tested machinery. -->

PassFailEngine surface to re-key (PassFailEngine.cs):
- :65-66  `HopLabels` HashSet<string> of 9 Step_* strings                → DELETE (ANL-01/D-15)
- :74-76  `IsComplete(RunTrace r)` — DistinctLabels (minus Step_A) SetEquals HopLabels → REPLACE with stepId-keyed completeness vs the ES-derived expected set
- :128    `startedRuns = runs.Count`                                      → KEEP, but each RunTrace is now built from framework StepId records
- :133    `complete = runs.Where(IsComplete)`                            → re-point to the stepId completeness
- :161    `valueOracleSupplied = seedsByExecution is not null`           → KEEP + generalize (SMP-01 degrade-to-N/A)
- :163-209 incomplete classification + TELEMETRY-GAP reconciliation (value-oracle path) → GENERALIZE (ANL-03): add framework-redundancy path (orchestrator record consuming M_N reconciles the hop, NO seed oracle)
- :407-417 `ExpectedHopOffset` (label→depth)                             → KEEP (D-16 value oracle)
- :433-478 `ResolveSeed`/`CheckValueChain`                               → KEEP (value oracle)

The `Analyze(...)` signature already carries optional dictionaries keyed "correlationId|executionId"; the expected set + redundancy evidence should be threaded as new optional params of the same shape (default null ⇒ existing callers compile) OR carried on the RunTrace. Existing HopLabels doc (:50-63) explains Step_A exclusion + Step_G ×2 — the D-09 empty-executionId marker rule REPLACES the Step_A-string special case.

RunTrace (RunTrace.cs): keyed by (CorrelationId, ExecutionId); has `DistinctLabels` (:76) + `Values` label→int (:104) + `FromLabels` build (:114-155). Add `DistinctStepIds` HashSet<string> + a `FromStepIds` build/overload; KEEP DistinctLabels/Values for the oracle.

EsIndexNames analog (EsIndexNames.cs:107): `public const string ExecutionIdFieldPath = "attributes.ExecutionId";` — copy shape, NO `.keyword` (Pitfall 2). KEEP StepLabelFieldPath (:87), SumFieldPath (:142), CorrelationIdFieldPath (:71).

AnalyzerE2ETests ES readers (AnalyzerE2ETests.cs): `BuildStepSearchBody` (:261-276) filters `exists attributes.StepLabel`; `BuildRunTraces` (:438-505) keys the loop on `attributes.StepLabel` (:451). Structural queries switch to `attributes.StepId`; ADD a second query body for the FW-02 orchestrator records (expected set); `attributes.Produced`/`StepLabel` value collection (:480-487) STAYS.

Engine-fact analog (PassFailEngineFacts.cs): `CleanSnapshot` (:48), `Complete_AllStartedRuns_Yields_Pass` (:61) — build synthetic RunTrace via FromLabels/FromStepIds + a PromCounterSnapshot + `new PassFailEngine().Analyze(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StepId field paths + RunTrace structural model + ES reader re-key</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs (:107 ExecutionIdFieldPath analog, :87 StepLabelFieldPath, :71 CorrelationIdFieldPath, :142 SumFieldPath)
    - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs (:76 DistinctLabels, :104 Values, :114-155 FromLabels)
    - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs (:261-276 BuildStepSearchBody, :438-505 BuildRunTraces, :451 StepLabel keying, :480-487 Produced collection)
    - 76-PATTERNS.md § EsIndexNames.cs, § AnalyzerE2ETests.cs, § RunTrace.cs
    - 76-CONTEXT.md D-17 (add StepIdFieldPath, keep StepLabel/Received/Produced), D-09 (empty-executionId marker)
  </read_first>
  <behavior>
    - EsIndexNames exposes `StepIdFieldPath = "attributes.StepId"` (and `NextStepIdFieldPath = "attributes.NextStepId"` for the FW-02 query), no `.keyword`.
    - RunTrace carries a `DistinctStepIds` set plus a `FromStepIds` build path; DistinctLabels/Values remain.
    - The structural ES query filters `exists attributes.StepId`; a second query body targets the FW-02 orchestrator records (exists attributes.NextStepId).
    - BuildRunTraces populates DistinctStepIds from attributes.StepId; a record with ExecutionId == all-zeros is retained as an entry marker (not folded into any spawned (corr,exec) run).
  </behavior>
  <action>
    Add to EsIndexNames.cs, mirroring the :107 const shape verbatim: `public const string StepIdFieldPath = "attributes.StepId";` and `public const string NextStepIdFieldPath = "attributes.NextStepId";` (NO `.keyword` — Pitfall 2). KEEP StepLabelFieldPath/SumFieldPath/Received/Produced. In RunTrace.cs add `public HashSet<string> DistinctStepIds { get; }` and a `FromStepIds(...)` factory (or extend FromLabels to also accept a stepId set) that both the live fixture and hermetic facts call; keep DistinctLabels + Values for the oracle. In AnalyzerE2ETests.cs: change the STRUCTURAL `BuildStepSearchBody` (:261-276) `exists` filter from `{{EsIndexNames.StepLabelFieldPath}}` to `{{EsIndexNames.StepIdFieldPath}}`; add a second query body method (e.g. `BuildDispatchSearchBody`) filtering `exists {{EsIndexNames.NextStepIdFieldPath}}` for the FW-02 expected-set records; re-key the `BuildRunTraces` loop (:451) to read `attributes.StepId` into DistinctStepIds (structural), keep the `attributes.Produced`/`StepLabel` collection (:480-487) as the value oracle. Handle D-09: a framework record with `attributes.ExecutionId` == the all-zeros Guid string is counted as "entry ran" but excluded from any `(corr,exec)` expected/complete set. Ensure NO `Step_*` string literal remains on the structural path (grep-clean).
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - grep of EsIndexNames.cs shows `StepIdFieldPath = "attributes.StepId"` with NO `.keyword`.
    - grep of RunTrace.cs shows `DistinctStepIds`.
    - grep of AnalyzerE2ETests.cs structural query path shows `StepIdFieldPath`/`NextStepIdFieldPath` and NO `Step_` string literal in BuildStepSearchBody/BuildRunTraces structural branch.
    - `dotnet build SK_P.sln -c Debug` 0-warning; hermetic suite still compiles/green.
  </acceptance_criteria>
  <done>StepId field paths added, RunTrace gains DistinctStepIds/FromStepIds, ES readers re-keyed to attributes.StepId + FW-02 dispatch query, value oracle collection preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete HopLabels; stepId-keyed completeness + ES-derived expected set (ANL-01, ANL-02, SMP-01)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (:65-66 HopLabels, :74-76 IsComplete, :128-133 started/complete, :161 valueOracleSupplied, :407-478 KEEP value oracle)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs (:48 CleanSnapshot, :61 Complete_..._Yields_Pass)
    - 76-RESEARCH.md § §6 Analyzer re-key surface + § Empty-executionId entry-marker
    - 76-PATTERNS.md § PassFailEngine.cs surface table + § Hermetic engine-fact structure
    - 76-CONTEXT.md D-15 (clean break, single structural source), D-16 (value oracle layered), D-09 (marker)
  </read_first>
  <behavior>
    - `HopLabels` is gone from PassFailEngine.cs (grep-clean).
    - A run whose DistinctStepIds equals the ES-derived expected set (from FW-02 records) is COMPLETE → contributes to PASS; a run missing an expected stepId is incomplete.
    - ANL-02: a stepId dispatched (present in the expected set) but never executed (absent from any processor record) is a binding miss → Verdict.Fail (the TEST-08 shape now fails).
    - The expected set is derived from ES records only (the fact supplies synthetic ES-shaped records; no Postgres/graph).
    - D-09: an empty-executionId marker record counts as entry-ran and is excluded from every (corr,exec) expected/complete set.
    - SMP-01: with no StepLabel/Received/Produced present, completeness/duplicates/expected-set still compute correctly; the value oracle absent → value-chain check is N/A, not FAIL.
  </behavior>
  <action>
    DELETE `HopLabels` (:65-66) and its doc. REPLACE `IsComplete` (:74-76) with stepId-keyed completeness: a RunTrace is complete when its `DistinctStepIds` equals the expected set for its `(corr, exec)`. Thread the expected set from ES FW-02 records — add an optional param (e.g. `IReadOnlyDictionary<string, IReadOnlySet<string>>? expectedStepIdsByExecution = null` keyed "correlationId|executionId", default null) or carry it on RunTrace; when absent (legacy hermetic callers) fall back to the union of observed stepIds so existing non-ANL-02 facts keep compiling. Re-point `:133 complete = runs.Where(...)` to the new completeness. Implement ANL-02: for each (corr,exec), an expected stepId with no matching executed processor record is a binding miss (feeds `missing`). Apply the D-09 marker rule: exclude empty-executionId records from expected/complete membership but count the entry as ran. KEEP `ExpectedHopOffset`/`ResolveSeed`/`CheckValueChain` (:407-478) and the `valueOracleSupplied` gate (:161) — generalize SMP-01 so the value chain degrades to N/A (not FAIL) when no StepLabel/Produced/seed is present. Add to PassFailEngineFacts.cs: `*PassFailEngine*StepId*` (completeness from stepId sets, zero Step_* labels present → PASS); `*ExpectedSet*`/`*Test08*` (dispatched-but-never-executed → Verdict.Fail); `*OracleAbsent*` (framework records only, no Received/Produced → correct verdict + value-chain N/A). Build the facts via RunTrace.FromStepIds + synthetic expected-set records (NO Step_* literals).
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*PassFailEngine*StepId*"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "HopLabels" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` returns NO match.
    - `BaseApi.Tests.exe -- --filter-method "*PassFailEngine*StepId*"`, `*ExpectedSet*`/`*Test08*`, and `*OracleAbsent*` each exit 0.
    - The ANL-02 fact asserts `Verdict.Fail` for a dispatched-but-never-executed stepId.
    - grep proves no Postgres/graph read on the analyzer path (PassFailEngine.cs + AnalyzerE2ETests structural path reference ES field paths only).
    - The SMP-01 fact asserts value-chain N/A (not FAIL) with the oracle absent.
  </acceptance_criteria>
  <done>HopLabels deleted; completeness + expected set are stepId-keyed and ES-derived; TEST-08 closes; SMP-01 degrade-to-N/A holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generalize telemetry-gap reconciliation to framework redundancy (ANL-03)</name>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (:157-209 telemetry-gap reconciliation, value-oracle gate; :161 valueOracleSupplied)
    - 76-RESEARCH.md § §6 (ANL-03 generalization) + § SPEC ANL-03 acceptance
    - 76-PATTERNS.md § PassFailEngine.cs (:185-209 GENERALIZE row)
    - 76-CONTEXT.md D-01 (non-zero TelemetryGap stays NON-binding on PASS, preserve 6e90216)
  </read_first>
  <behavior>
    - A run missing hop N's own processor record but carrying the orchestrator FW-02 record that consumed hop N's output EntryId (M_N) reconciles as a NON-binding telemetry gap — WITH NO seed oracle supplied (seedsByExecution == null).
    - A run missing BOTH hop N's processor record AND the orchestrator record consuming M_N stays a binding miss.
    - Preserve D-01: a reconciled (non-zero TelemetryGap) run stays PASS, not FAIL (the 6e90216 behavior).
  </behavior>
  <action>
    Generalize the reconciliation block at PassFailEngine.cs:185-209. Today it reclassifies a trace-incomplete run as provably-complete ONLY when the sample value oracle (seedsByExecution) is supplied. Add the framework-redundancy path: an expected stepId whose processor record is absent is reconciled as a non-binding telemetry gap when an orchestrator FW-02 record exists that consumed that hop's output EntryId (M_N) — i.e. the inbound EntryId on a fan-out/terminal record proves the hop produced its output and therefore ran. Thread the redundancy evidence as a new optional param (e.g. `IReadOnlyDictionary<string, IReadOnlySet<string>>? orchestratorConsumedEntryIdsByExecution = null`, or a set of (corr,exec,stepId) proven-by-orchestrator) built from the FW-02 records; this path must NOT require seedsByExecution (the ANL-03 "no seed oracle" acceptance). A stepId absent from BOTH the processor records AND the orchestrator-consumed evidence remains a binding miss (feeds `missing`). Keep the reconciled count in `telemetryGap`/`telemetryGapDetail` and keep it NON-binding on PASS (D-01). Add facts to PassFailEngineFacts.cs: `*Reconcile*Redundancy*` (missing hop record + orchestrator consumed M_N → non-binding gap, seedsByExecution == null, Verdict.Pass) and a sibling (missing BOTH → binding miss, Verdict.Fail).
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*Reconcile*Redundancy*"</automated>
  </verify>
  <acceptance_criteria>
    - `BaseApi.Tests.exe -- --filter-method "*Reconcile*Redundancy*"` exits 0.
    - The reconcile-one fact passes `seedsByExecution: null` and asserts `Verdict.Pass` with a non-zero TelemetryGap (non-binding).
    - The missing-both fact asserts `Verdict.Fail` (binding miss).
    - Existing 6e90216 value-oracle reconciliation facts still pass (no regression).
  </acceptance_criteria>
  <done>Reconciliation reconciles a dropped hop record from framework redundancy with no seed oracle; missing-both stays binding; D-01 non-binding-on-PASS preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ES records → analyzer verdict | untrusted/lossy telemetry crosses into the arbiter; the verdict must not read anything but ES (v8.0.0) |
| scenario id → ES query / path | operator-supplied scenario id composes a query + artifact path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-07 | Spoofing/Elevation | expected set sourced from Postgres/graph instead of ES | mitigate | ANL-02: expected set derived from ES FW-02 records ONLY; grep proves no Postgres/graph read on the analyzer path (v8.0.0 "truth = Prometheus + ES only") |
| T-76-08 | Tampering | path traversal via scenario id | accept | Existing `^[A-Za-z0-9_-]+$` whitelist (AnalyzerE2ETests.cs:68) guards path compose — unchanged here |
| T-76-09 | Information disclosure | analyzer emitting payload from ES records into artifacts | accept | Structural path reads ids/stepIds only; Produced value oracle is synthetic-safe (D-03) — no new payload surface |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` and `-c Release` 0-warning.
- `BaseApi.Tests.exe --filter-not-trait Category=RealStack` green (PassFailEngineFacts included).
- `rg "HopLabels" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs` empty; no Step_* literal on the structural path.
</verification>

<success_criteria>
- Completeness stepId-keyed; HopLabels deleted (ANL-01).
- Expected set ES-derived; dispatched-but-never-executed → Fail; no Postgres/graph read (ANL-02).
- Framework-redundancy reconciliation with no seed oracle; missing-both binding (ANL-03).
- Framework-records-only verdict; value oracle absent → N/A (SMP-01).
</success_criteria>

<output>
After completion, create `.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-03-SUMMARY.md`
</output>
