---
phase: 76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs
  - tests/BaseApi.Tests/Orchestrator/OrchestratorPrePipelineFacts.cs
  - tests/BaseApi.Tests/Orchestrator/OrchestratorNonBlockingLogFacts.cs
autonomous: true
requirements: [FW-02, FW-04]

must_haves:
  truths:
    - "The orchestrator emits one fan-out edge record per next step, carrying (CorrelationId, ExecutionId, WorkflowId, inbound EntryId, next StepId) — NO outbound MessageId (D-11 Option C)"
    - "A 2-way fan-out (C→D1,D2) emits exactly 2 edge records with DISTINCT next StepIds, each carrying the correct inbound EntryId"
    - "A true terminal (Matches.Count==0 && UnresolvedIds.Count==0) emits one terminal-reached record; Step_G fans in twice so it emits 2 per (corr,exec), distinguished by inbound EntryId"
    - "No terminal-reached record is emitted on the L1-miss (:73-80) or clean-absent (:113-119) branches"
    - "The new records are emitted on a guarded non-blocking path (FW-04) and carry ids only (FW-03)"
    - "A throwing ILogger<OrchestratorPrePipeline> neither fails the fan-out send loop nor the terminal-reached ack — RunAsync surfaces no exception and every NextStepHandoff is still sent (FW-04)"
  artifacts:
    - path: "src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs"
      provides: "Guarded fan-out edge record in the :126-145 loop + terminal-reached record in the :87-93 branch"
      contains: "NextStepId"
    - path: "tests/BaseApi.Tests/Orchestrator/OrchestratorPrePipelineFacts.cs"
      provides: "Structured-State CapturingLogger facts for FW-02 fan-out ×2 distinct + terminal ×2"
    - path: "tests/BaseApi.Tests/Orchestrator/OrchestratorNonBlockingLogFacts.cs"
      provides: "FW-04 ThrowingLogger<OrchestratorPrePipeline> facts — fan-out send loop + terminal ack both unaffected by a throwing logger"
  key_links:
    - from: "OrchestratorPrePipeline.cs:126-145 fan-out loop"
      to: "attributes.NextStepId / attributes.EntryId in ES (ANL-02 expected set + ANL-03 M_N proof)"
      via: "explicit {Placeholder} args, one record per next step"
      pattern: "\\{NextStepId\\}"
    - from: "OrchestratorPrePipeline.cs:87-93 completed-terminal branch"
      to: "terminal-reached record ×2 at Step_G by inbound EntryId"
      via: "guarded LogInformation carrying m.EntryId"
      pattern: "terminal reached"
    - from: "ThrowingLogger<OrchestratorPrePipeline> (OrchestratorNonBlockingLogFacts.cs)"
      to: "OrchestratorPrePipeline fan-out send loop + terminal ack (FW-04 guard)"
      via: "throwing Log does not propagate out of RunAsync; all sends/ack unchanged"
      pattern: "ThrowingLogger<OrchestratorPrePipeline>"
---

<objective>
Emit the orchestrator's causal-edge telemetry: a per-fan-out edge record (inbound EntryId → next StepId) in the next-step loop, and a terminal-reached record at the true-terminal branch (×2 at Step_G). Per D-11 Option C the outbound MessageId is OMITTED (not in hand at the Pre loop — Phase-72's "no MessageId override" stays intact). These records give ANL-02 its expected set and ANL-03 its redundancy edge. Both new records are on a guarded non-blocking path proven by a throwing-logger fact (FW-04).

Purpose: processor-only logs have no edges (fresh envelope id minted downstream at RelocateTail) and no expected-set. The orchestrator is the only tier that resolves next steps and detects "reached terminal."
Output: modified `OrchestratorPrePipeline` + extended `OrchestratorPrePipelineFacts` (upgraded to structured-State capture) + a new `OrchestratorNonBlockingLogFacts` for FW-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-SPEC.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-CONTEXT.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-RESEARCH.md
@.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-PATTERNS.md

<interfaces>
<!-- Confirmed live 2026-07-16. -->

OrchestratorPrePipeline.RunAsync (OrchestratorPrePipeline.cs:69):
```csharp
public async Task RunAsync(IStepResult m, StepOutcome outcome, Guid messageId, CancellationToken ct)
```
Constructor already injects `ILogger<OrchestratorPrePipeline> logger` (:67) — no DI change.

Branch map (emit only where noted):
- :73-80  L1-miss "completed-unresolved"  → DO NOT emit terminal-reached
- :87-93  true terminal `if (selection.Matches.Count == 0 && selection.UnresolvedIds.Count == 0)` → EMIT terminal-reached (fires ×2 at Step_G by distinct m.EntryId); this branch acks (business) and returns
- :113-119 clean-absent out: blob         → DO NOT emit terminal-reached
- :126-145 fan-out loop `foreach (var (stepId, step) in selection.Matches)` → EMIT one edge record per iteration; each iteration calls `post.Send((object)handoff, …)` at :135

Identities in hand at both sites: m.CorrelationId, m.ExecutionId, m.WorkflowId, m.EntryId (inbound = M_N), and (in the loop) `stepId` = the next step. Outbound MessageId is NOT in hand — `post.Send((object)handoff, …)` at :135 uses NO id override (comment :122-124: "NO MessageId override (D-11 — MassTransit assigns a fresh envelope id)"). This is Phase-72 D-11 and is NOT reversed (D-11 Option C).

Existing terminal log to sit beside (:89-91):
```csharp
logger.LogInformation(
    "Trip ended (completed-terminal): no matching successor for ({WorkflowId}, {StepId}) outcome={Outcome} — acking (business)",
    m.WorkflowId, m.StepId, outcome);
```

The EXISTING OrchestratorPrePipelineFacts CapturingLogger (:88-101) captures only formatter STRINGS (Messages.Add) — it CANNOT assert NextStepId distinctness. Replace/augment it with the structured-State variant from ReinjectConsumerFacts.cs:24-48 (captures IReadOnlyList<KeyValuePair<string,object?>> State). Reuse scaffolds: `Two_matches_produce_two_post_messages_and_one_delete` (:229) for the 2-way fan-out, `Terminal_step_logs_completed_terminal_and_acks` (:317) for the terminal case.

FW-04 analog (76-01): `ThrowingLogger<ProcessorPipeline> : ILogger<T>` in tests/BaseApi.Tests/Processor/NonBlockingLogFacts.cs — `Log` throws `InvalidOperationException`, `BeginScope`→NullScope, `IsEnabled`→true. Mirror it as `ThrowingLogger<OrchestratorPrePipeline>` (Task 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade OrchestratorPrePipelineFacts to structured-State capture + write the RED FW-02 facts</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/OrchestratorPrePipelineFacts.cs (:88-101 string-only CapturingLogger, :169-175 Build, :229 two-match scaffold, :317 terminal scaffold)
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs:24-48 (structured-State CapturingLogger to adopt)
    - 76-PATTERNS.md § Orchestrator-pipeline hermetic harness + § Capturing-logger hermetic assertion
    - 76-CONTEXT.md D-11 (Option-C 5 fields), D-12/D-13 (terminal ×2 at Step_G)
  </read_first>
  <behavior>
    - The fixture's CapturingLogger now exposes captured Entry.State (list of KeyValuePair), not just strings.
    - A 2-way fan-out fact asserts exactly 2 captured edge Entries with DISTINCT NextStepId values and the same inbound EntryId (initially RED — no emission yet).
    - A Step_G-shaped double fan-in terminal fact asserts exactly 2 terminal-reached Entries with distinct inbound EntryId (initially RED).
    - An L1-miss fact and a clean-absent fact assert ZERO terminal-reached Entries.
  </behavior>
  <action>
    Replace the thin string-only CapturingLogger<T> at OrchestratorPrePipelineFacts.cs:88-101 with the structured-State variant from ReinjectConsumerFacts.cs:24-48 (keep a helper to still read the formatted string if any existing fact needs it, but expose `Entries` with `State`). Add filter helpers to select captured Entries whose State contains a `NextStepId` key (fan-out edge) vs a terminal-reached shape. Write the new facts against the existing scaffolds: extend `Two_matches_produce_two_post_messages_and_one_delete` (:229) into a `*FanOut*` fact asserting 2 edge Entries with distinct `NextStepId`; extend `Terminal_step_logs_completed_terminal_and_acks` (:317) into a `*Terminal*` fact driving the terminal branch twice with distinct `m.EntryId` and asserting 2 terminal-reached Entries; add negative facts on the L1-miss and clean-absent branches asserting zero terminal-reached Entries. Name so `*OrchestratorPrePipeline*FanOut*` and `*Terminal*` select them. These are RED until Task 2 adds emission.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*OrchestratorPrePipeline*"</automated>
  </verify>
  <acceptance_criteria>
    - The fixture compiles with the structured-State CapturingLogger; existing OrchestratorPrePipeline facts still pass.
    - The new `*FanOut*` and `*Terminal*` facts exist and reference `State`/`NextStepId` (grep-confirmable) and FAIL before Task 2 (RED proven).
  </acceptance_criteria>
  <done>Structured-State capture in place; RED FW-02 facts written for fan-out ×2 distinct, terminal ×2, and the two negative branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit the guarded fan-out edge + terminal-reached records (FW-02, D-11 Option C, D-12, D-13, D-14)</name>
  <read_first>
    - src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs (:87-93 terminal branch, :126-145 fan-out loop, :135 post.Send with no override)
    - 76-RESEARCH.md § §3 + § §4 Discrepancy 1 (Option C — outbound MessageId omitted) + Code Examples (fan-out / terminal record shapes)
    - 76-PATTERNS.md § OrchestratorPrePipeline.cs (insertion points, NextStepId key, FW-04 guard)
    - 76-CONTEXT.md D-14 (emission sites: terminal → :87-93 branch, fan-out → :126-145 loop; NOT RelocateTail; NOT L1-miss/clean-absent)
  </read_first>
  <behavior>
    - Turns Task 1's RED facts GREEN: 2 distinct edge records on a 2-way fan-out; 2 terminal-reached records at Step_G double fan-in; zero on L1-miss/clean-absent.
    - Each record is Information-level and carries ids only (no payload — FW-03).
    - A throwing logger does not fail the fan-out (guarded — FW-04; proven by Task 3).
  </behavior>
  <action>
    In the `:126-145` fan-out loop, after the successful `post.Send` + `metrics.MessagesSent.Add` (so the record follows a successful dispatch), add the guarded edge record: `try { logger.LogInformation("fan-out {CorrelationId} {ExecutionId} {WorkflowId} {EntryId} {NextStepId}", m.CorrelationId, m.ExecutionId, m.WorkflowId, m.EntryId, stepId); } catch { }` — Option-C: outbound MessageId is OMITTED (not in hand; do NOT add a NewId or a Send-context override — Phase-72 D-11 "no override" is not reversed). `NextStepId` is the next-step key name (PascalCase, matching the ExecutionLogScope/attributes.* convention). In the `:87-93` completed-terminal branch, beside the existing "Trip ended (completed-terminal)" log and before `return;`, add the guarded terminal-reached record: `try { logger.LogInformation("terminal reached {CorrelationId} {ExecutionId} {WorkflowId} {EntryId} {StepId}", m.CorrelationId, m.ExecutionId, m.WorkflowId, m.EntryId, m.StepId); } catch { }` — no outbound MessageId (D-12). Do NOT add either record on the :73-80 L1-miss branch or the :113-119 clean-absent branch. Both templates are placeholder-only, no payload argument (FW-03).
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*OrchestratorPrePipeline*"</automated>
  </verify>
  <acceptance_criteria>
    - `BaseApi.Tests.exe -- --filter-method "*OrchestratorPrePipeline*"` exits 0 (Task 1 facts now GREEN).
    - grep of OrchestratorPrePipeline.cs shows the `"fan-out {CorrelationId} {ExecutionId} {WorkflowId} {EntryId} {NextStepId}"` template inside the fan-out loop and `"terminal reached …"` inside the :87-93 branch, each wrapped in try/catch.
    - grep shows NO NewId/MessageId override added to the fan-out `post.Send` (Option C preserved).
    - The L1-miss and clean-absent negative facts assert zero terminal-reached Entries.
  </acceptance_criteria>
  <done>Fan-out edge (×2 distinct) and terminal-reached (×2 at Step_G) records emitted on the correct branches, guarded, ids-only; all FW-02 facts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FW-04 — a throwing ILogger<OrchestratorPrePipeline> neither fails the fan-out send loop nor the terminal ack</name>
  <read_first>
    - src/Orchestrator/Dispatch/OrchestratorPrePipeline.cs (:87-93 terminal-reached ack branch, :126-145 fan-out post.Send loop — the two guarded LogInformation sites Task 2 added)
    - tests/BaseApi.Tests/Processor/NonBlockingLogFacts.cs (76-01's `ThrowingLogger<ProcessorPipeline>` pattern to mirror: Log throws InvalidOperationException, BeginScope→NullScope, IsEnabled→true)
    - 76-RESEARCH.md § Pitfall 4 (a throwing ILogger must not fail the hop — FW-04; the guard is required on the NEW calls specifically, existing unguarded warnings are out of scope)
    - tests/BaseApi.Tests/Orchestrator/OrchestratorPrePipelineFacts.cs (:169-175 Build, :229 two-match scaffold, :317 terminal scaffold — reuse the same harness to construct the pipeline with a throwing logger)
  </read_first>
  <behavior>
    - A `ThrowingLogger<OrchestratorPrePipeline>` whose `Log` throws `InvalidOperationException` does NOT propagate out of `RunAsync`.
    - Fan-out case (2-way): under the throwing logger, all `NextStepHandoff`s are still sent (`post.Send` called for every match) and the delete still runs — identical to the normal-logger scaffold at :229.
    - Terminal case (true terminal): under the throwing logger, the message is still acked and `RunAsync` completes — identical to the normal-logger scaffold at :317 — with no surfaced exception.
  </behavior>
  <action>
    Create tests/BaseApi.Tests/Orchestrator/OrchestratorNonBlockingLogFacts.cs. Define `ThrowingLogger<T> : ILogger<T>` mirroring 76-01's `ThrowingLogger<ProcessorPipeline>` in NonBlockingLogFacts.cs (its `Log` throws `InvalidOperationException`; `BeginScope`→NullScope.Instance; `IsEnabled`→true). Build an `OrchestratorPrePipeline` with `ThrowingLogger<OrchestratorPrePipeline>` using the same Build/harness the OrchestratorPrePipelineFacts scaffolds use (:169-175 Build, :229 two-match, :317 terminal). Two facts: (a) `*OrchestratorPrePipeline*NonBlocking*FanOut*` — drive the 2-way fan-out and assert `RunAsync` completes with NO surfaced exception AND every `NextStepHandoff` is still sent (assert the same `post.Send` count + delete as the normal-logger :229 scaffold); (b) `*OrchestratorPrePipeline*NonBlocking*Terminal*` — drive the true-terminal branch and assert `RunAsync` completes with NO surfaced exception AND the message is still acked (same as the :317 scaffold). Name both so `--filter-method "*OrchestratorPrePipeline*NonBlocking*"` selects them.
  </action>
  <verify>
    <automated>tests/BaseApi.Tests/bin/Debug/net9.0/BaseApi.Tests.exe -- --filter-method "*OrchestratorPrePipeline*NonBlocking*"</automated>
  </verify>
  <acceptance_criteria>
    - `BaseApi.Tests.exe -- --filter-method "*OrchestratorPrePipeline*NonBlocking*"` exits 0.
    - The `*FanOut*` fact asserts every `NextStepHandoff` is sent and no exception surfaces from `RunAsync` under the throwing logger.
    - The `*Terminal*` fact asserts the ack still happens and no exception surfaces from `RunAsync` under the throwing logger.
    - `rg "ThrowingLogger<OrchestratorPrePipeline>" tests/BaseApi.Tests/Orchestrator/OrchestratorNonBlockingLogFacts.cs` matches (mirrors the 76-01 ThrowingLogger pattern).
  </acceptance_criteria>
  <done>An orchestrator throwing-logger fact proves FW-04 for BOTH the fan-out send loop and the terminal-reached ack; both facts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator dispatch → telemetry (OTLP→ES) | fan-out/terminal records leave the trusted dispatch path into production log storage |
| exporter → dispatch path | a throwing exporter could fail the fan-out send loop (FW-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04 | Information disclosure | fan-out / terminal-reached records | mitigate | Records carry ids only (CorrelationId/ExecutionId/WorkflowId/EntryId/StepId); the relocated blob `relocated` is NEVER an argument. Covered by plan-01 Task 3 FW-03 grep over OrchestratorPrePipeline.cs |
| T-76-05 | Denial of service | throwing exporter inside the fan-out loop | mitigate | Both new LogInformation calls are try/catch-guarded (FW-04); the send/ack path is unaffected — proven by the `ThrowingLogger<OrchestratorPrePipeline>` fact (Task 3) asserting all sends + the terminal ack survive a throwing logger |
| T-76-06 | Repudiation | dropped fan-out record hides a real hop | accept→mitigated downstream | Redundancy is by design (ANL-03): the inbound EntryId=M_N proves hop N ran two independent ways; a single dropped record does not lose the edge |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` and `-c Release` build 0-warning.
- `BaseApi.Tests.exe -- --filter-method "*OrchestratorPrePipeline*"` green (includes the FW-04 `*NonBlocking*` facts).
- FW-03 grep over OrchestratorPrePipeline.cs clean (asserted in plan 01 Task 3).
</verification>

<success_criteria>
- One edge record per next step, 5 fields, no outbound MessageId; 2-way fan-out = 2 distinct next StepIds (FW-02, D-11 Option C).
- Terminal-reached record on the true-terminal branch only; ×2 at Step_G by inbound EntryId (D-12/D-13).
- Guarded non-blocking emission; ids only (FW-04/FW-03) — the throwing-logger fact proves the fan-out sends and the terminal ack both survive a throwing logger.
</success_criteria>

<output>
After completion, create `.planning/phases/76-framework-emitted-per-hop-execution-logs-keyed-by-stepid-dec/76-02-SUMMARY.md`
</output>
