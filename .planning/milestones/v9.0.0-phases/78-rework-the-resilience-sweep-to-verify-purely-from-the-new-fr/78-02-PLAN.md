---
phase: 78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs
  - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs
autonomous: true
requirements: [D-03]
security_enforcement: true

must_haves:
  truths:
    - "The value-oracle ES query and value-parse path are gone from the fixture — no StepLabel/Produced evidence is fetched or parsed"
    - "The engine's value-CHAIN machinery is gone — CheckValueChain, ResolveSeed, the seedsByExecution param, telemetry-gap path #1, the WR-02 guard, and the value-chain loop no longer exist"
    - "ANL-03 framework-redundancy (telemetry-gap path #2) survives as the SOLE non-binding reconciliation"
    - "PassFailEngineValueChainFacts.cs is deleted entirely (375 lines) — not skipped"
    - "The `ValueChainOk`/`ValueChainDetail` required members are removed from AnalyzerReport.cs (and the moot OracleAbsent fact deleted); TelemetryGap/TelemetryGapDetail are retained (path #2)"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      provides: "Verdict engine with the concrete value chain removed (ExpectedHopOffset scaffold still present — removed in Plan 03)"
    - path: "tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs"
      provides: "Fixture that no longer fetches or parses value-oracle evidence"
  key_links:
    - from: "PassFailEngine.Analyze"
      to: "telemetry-gap path #2 (ANL-03)"
      via: "framework-redundancy reconciliation"
      pattern: "ReconcileRedundancy|framework.redundancy|path #2"
---

<objective>
Delete the concrete value oracle end-to-end (D-03) EXCEPT the `ExpectedHopOffset` completeness scaffold, which is removed atomically with its dependent hermetic facts in Plan 03 (Hazard C). This plan removes: the value-oracle ES query + value-parse path from the fixture; the value-CHAIN machinery from the engine (CheckValueChain, ResolveSeed, seedsByExecution, telemetry-gap path #1, the WR-02 vacuous-green guard, the value-chain loop, `&& valueChainOk` from the verdict, and the vestigial `ValueChainOk`/`ValueChainDetail` report fields); and deletes `PassFailEngineValueChainFacts.cs` entirely.

Purpose: Phase 77 D4 deleted the sample author logs, so the value-oracle query returns nothing — leaving it dormant is a vacuous-green trap. The verdict must stand on structural stepId completeness + ANL-03 redundancy + the metric gate alone.
Output: value axis dark from fixture and engine; ANL-03 path #2 is the sole non-binding reconciliation; the tree stays green (ExpectedHopOffset scaffold intentionally retained until Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-CONTEXT.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-RESEARCH.md

<interfaces>
<!-- From 78-RESEARCH.md precise delete/edit inventory — cite these line anchors. -->

FIXTURE (AnalyzerE2ETests.cs) DELETE/MODIFY:
- `BuildValueOracleSearchBody` (:334-349) — DELETE
- `valueHits` fetch `es.SearchAllHits(BuildValueOracleSearchBody…)` (:190-191) — DELETE
- `BuildRunTraces(...)` call — drop the `valueHits` argument (:193)
- `BuildRunTraces` signature — remove `List<JsonElement> valueHits` param (:528-530)
- value-oracle loop building `valuesByInstance` (:639-656, incl. TryReadSum/TryReadProduced calls :651-652) — DELETE
- `values`/`labels` locals in trace build (:660-662,:673) — pass empty/null to `RunTrace.FromStepIds` (it defaults Values/Labels empty, RunTrace.cs:224-238)
- `seedsByExec` map build (:686-694) — DELETE
- `TraceCohort.SeedsByExecution` field (:483) + `SeedsByExecution = seedsByExec` (:701) — DELETE
- `Analyze(... seedsByExecution: cohort.SeedsByExecution …)` (:243) — remove that argument
- `TryReadSum` (:722-730), `TryReadProduced` (:743-751) — DELETE (only callers are the deleted value loop)
- `TryReadTimestamp` (:760-796) — KEEP (used by the surviving trip-duration span :595)
- `BuildKeeperOutcomeSearchBody`/`BuildKeeperOutcomeMap` (:402-469) — KEEP
- Dispatch in-degree convergent derivation (:604-637) — KEEP (Phase-76 fix)

ENGINE (PassFailEngine.cs) DELETE/MODIFY:
- `seedsByExecution` param (:120) — DELETE
- `valueOracleSupplied` (:209) — DELETE
- telemetry-gap PATH #1 value-chain reconciliation (:235-255, the ResolveSeed + CheckValueChain + terminal-anchor block) — DELETE
- telemetry-gap PATH #2 ANL-03 framework-redundancy (:257-274) — KEEP (sole non-binding reconciliation)
- value-chain assertion loop `valueChainDetail`/`valueChainOk`/`completeRunsWithValues` (:337-377) — DELETE
- WR-02 vacuous-green guard (:379-394) — DELETE
- `valueChainOk` in `pass = …` verdict (:468) — drop the `&& valueChainOk` term
- `ValueChainOk`/`ValueChainDetail` in report + BuildSummary (:491-492,:498,:583-608) are C# `required` members — REMOVE from AnalyzerReport.cs AND the Analyze initializer AND BuildSummary, and DELETE the moot `PassFailEngine_OracleAbsent_...ValueChainNotApplicable...` fact (PassFailEngineFacts.cs:451, asserts ValueChainOk :464). All atomic in Task 2. KEEP `TelemetryGap`/`TelemetryGapDetail` (:212/:216, NOT required) — populated by surviving path #2 (ANL-03), asserted by KEEP facts :486/:488/:505/:627.
- `ResolveSeed` (:536-552), `CheckValueChain` (:560-581) — DELETE
- Metric gate MG-1/2/3 (:399-429) — KEEP unchanged
- three-class verdict / INCONCLUSIVE (:431-470) — KEEP
- `ExpectedHopOffset` (:510), `valueOracleHopSet` (:158), `ExpectedFor` label-fallback (:165) — **DO NOT DELETE IN THIS PLAN** (Hazard C — atomic with fact migration in Plan 03; deleting here goes red).

Report-shape note: `phase-68-sweep.ps1:104-115` reads ONLY Verdict/StartedRuns/CompleteRuns/Missing/Duplicates. Removing ValueChainOk/ValueChainDetail does not break the wrapper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the value-oracle query + value-parse path from the fixture</name>
  <files>tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs (all anchors in the FIXTURE interface block above)
    - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs (`:224-238` FromStepIds defaults Values/Labels empty)
    - 78-RESEARCH.md AnalyzerE2ETests.cs delete/edit table
  </read_first>
  <action>
    Per D-03, remove the value-oracle axis from `AnalyzerE2ETests.cs` using the FIXTURE anchors above:
    - DELETE `BuildValueOracleSearchBody` (:334).
    - DELETE the `valueHits` fetch (:190-191) and drop the `valueHits` argument from the `BuildRunTraces(...)` call (:193).
    - Remove the `List<JsonElement> valueHits` parameter from the `BuildRunTraces` signature (:528-530).
    - DELETE the value-oracle loop that builds `valuesByInstance` (:639-656) including the `TryReadSum`/`TryReadProduced` calls (:651-652). (The `:648` entry-marker guard line was already removed in Plan 01.)
    - Where the trace is built, pass empty/`null` for values/labels to `RunTrace.FromStepIds` (:660-662,:673) — `FromStepIds` defaults Values/Labels empty, so traces build cleanly with no value axis.
    - DELETE the `seedsByExec` map build (:686-694), the `TraceCohort.SeedsByExecution` field (:483), and the `SeedsByExecution = seedsByExec` initializer (:701).
    - In the `Analyze(...)` call (:243), remove the `seedsByExecution: cohort.SeedsByExecution` argument (the engine still accepts it as an optional param at this point — Plan 02 Task 2 removes the param; this ordering keeps each task green).
    - DELETE `TryReadSum` (:722-730) and `TryReadProduced` (:743-751) — their only callers were the deleted value loop.
    - KEEP `TryReadTimestamp` (:760-796) — still used by the surviving trip-duration span (:595).
    - KEEP `BuildKeeperOutcomeSearchBody`/`BuildKeeperOutcomeMap` (:402-469) and the dispatch in-degree convergent derivation (:604-637) untouched.

    Ordering note: remove the `seedsByExecution:` argument from the `Analyze` call BEFORE Task 2 deletes the engine param, so the fixture never references a deleted symbol.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && grep -c "BuildValueOracleSearchBody\|valueHits\|SeedsByExecution\|seedsByExec\|TryReadSum\|TryReadProduced" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs; grep -c "TryReadTimestamp\|BuildKeeperOutcomeMap\|BuildKeeperOutcomeSearchBody" tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BuildValueOracleSearchBody" AnalyzerE2ETests.cs` returns 0.
    - `grep -c "valueHits" AnalyzerE2ETests.cs` returns 0.
    - `grep -c "SeedsByExecution\|seedsByExec" AnalyzerE2ETests.cs` returns 0.
    - `grep -c "TryReadSum\|TryReadProduced" AnalyzerE2ETests.cs` returns 0.
    - `TryReadTimestamp`, `BuildKeeperOutcomeMap`, `BuildKeeperOutcomeSearchBody` still present (each grep count ≥ 1).
    - Solution builds 0-warning Debug + Release.
  </acceptance_criteria>
  <done>The fixture fetches and parses no value-oracle evidence; the surviving structural/dispatch/keeper/trip-duration paths are intact; the `Analyze` call no longer passes a seed map.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the engine value-CHAIN machinery + PassFailEngineValueChainFacts.cs (keep ExpectedHopOffset scaffold)</name>
  <files>tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs, tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs, tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs, tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (all ENGINE anchors above — note the KEEP items: path #2 :257-274, metric gate :399-429, three-class verdict :431-470, and the DO-NOT-DELETE ExpectedHopOffset/valueOracleHopSet/ExpectedFor-fallback)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs (entire file — DELETE)
    - tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs (`:191` `public required bool ValueChainOk`, `:199` `public required IReadOnlyList<string> ValueChainDetail` — these are C# `required` members, so removing them from the Analyze initializer REQUIRES editing this file; KEEP `TelemetryGap` :212 / `TelemetryGapDetail` :216 — path #2 populates them)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs (`:451` PassFailEngine_OracleAbsent_FrameworkRecordsOnly_ValueChainNotApplicable_Yields_Pass — asserts `report.ValueChainOk` at :464; its premise is moot once the value oracle is gone → DELETE this fact)
    - 78-RESEARCH.md PassFailEngine.cs delete/edit table + Hazard C section
  </read_first>
  <action>
    Per D-03, remove the value-CHAIN machinery from `PassFailEngine.cs` using the ENGINE anchors above, but DEFER the `ExpectedHopOffset`/`valueOracleHopSet`/`ExpectedFor` label-fallback deletion to Plan 03 (Hazard C atomicity):
    - DELETE the `seedsByExecution` param (:120) and `valueOracleSupplied` (:209).
    - DELETE telemetry-gap PATH #1 — the value-chain reconciliation block (:235-255: the `ResolveSeed` + `CheckValueChain` + terminal-anchor tolerance). KEEP PATH #2 (ANL-03 framework-redundancy, :257-274) exactly as-is — it becomes the SOLE non-binding reconciliation.
    - DELETE the value-chain assertion loop building `valueChainDetail`/`valueChainOk`/`completeRunsWithValues` (:337-377) and the WR-02 vacuous-green guard (:379-394).
    - In the verdict `pass = …` (:468), drop the `&& valueChainOk` term.
    - REMOVE the `ValueChainOk`/`ValueChainDetail` fields ATOMICALLY across THREE sites in this same task (delete-not-dormant posture; the sweep reads neither): (a) delete the `public required bool ValueChainOk` (AnalyzerReport.cs:191) and `public required IReadOnlyList<string> ValueChainDetail` (:199) members + their doc comments — they are `required` so the initializer will not compile until they are gone; (b) delete the `ValueChainOk = …`/`ValueChainDetail = …` assignments from the `Analyze` object initializer (PassFailEngine.cs:491-492) and any `BuildSummary` reference (:498,:583-608); (c) DELETE the surviving fact `PassFailEngine_OracleAbsent_FrameworkRecordsOnly_ValueChainNotApplicable_Yields_Pass` (PassFailEngineFacts.cs:451-465, asserts `report.ValueChainOk` at :464) — its premise (oracle-absent ⇒ value chain N/A ⇒ PASS) is moot once the value oracle is fully gone, so it no longer tests a live branch. All three land in the same commit so the tree ends compiling + green. KEEP `TelemetryGap`/`TelemetryGapDetail` (AnalyzerReport.cs:212/:216) — they are populated by the surviving path #2 (ANL-03) and asserted by KEEP facts `PassFailEngine_ReconcileRedundancy_MissingHop` (:486/:488), `_MissingBoth` (:505), `Verdict_NonZeroTelemetryGap_StaysPass_D01` (:627); removing them would break those. INFO (no action): `scripts/phase-73-sweep.ps1:136` reads `.ValueChainOk` but is null-guarded and is NOT in the D-04 runbook (only `phase-68-sweep.ps1`), so the field removal degrades it to `$null` harmlessly — out of scope.
    - DELETE `ResolveSeed` (:536-552) and `CheckValueChain` (:560-581).
    - KEEP unchanged: the metric gate MG-1/2/3 (:399-429), the three-class verdict / INCONCLUSIVE (:431-470).
    - DO NOT touch `ExpectedHopOffset` (:510), `valueOracleHopSet` (:158), or the `ExpectedFor` `DistinctLabels`-fallback branch (:165) — they stay as a hermetic completeness scaffold until Plan 03 removes them atomically with the fact migration. (The `scored = runs.ToList();` line from Plan 01 stays.)

    In the same task, DELETE `tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs` entirely (375 lines) — its facts pass `seedsByExecution`/call the value chain and would fail to compile once the param and `CheckValueChain` are gone. Deleting it in the same task keeps the tree green. Confirm no other file references its members (grep clean except `bin`/`obj`).
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && ls tests/BaseApi.Tests/Observability/Analysis/PassFailEngineValueChainFacts.cs 2>&1; grep -c "CheckValueChain\|ResolveSeed\|seedsByExecution\|valueOracleSupplied\|ValueChainOk\|ValueChainDetail" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs; grep -c "ExpectedHopOffset\|valueOracleHopSet" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs; grep -c "ValueChainOk\|ValueChainDetail" tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs; grep -c "OracleAbsent_FrameworkRecordsOnly_ValueChainNotApplicable" tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs; grep -c "TelemetryGap" tests/BaseApi.Tests/Observability/Analysis/AnalyzerReport.cs</automated>
  </verify>
  <acceptance_criteria>
    - `PassFailEngineValueChainFacts.cs` no longer exists (`ls` reports "No such file").
    - `grep -c "CheckValueChain\|ResolveSeed\|seedsByExecution\|valueOracleSupplied\|ValueChainOk\|ValueChainDetail" PassFailEngine.cs` returns 0.
    - `grep -c "ValueChainOk\|ValueChainDetail" AnalyzerReport.cs` returns 0 (the `required` members removed).
    - `grep -c "OracleAbsent_FrameworkRecordsOnly_ValueChainNotApplicable" PassFailEngineFacts.cs` returns 0 (the moot fact deleted).
    - `grep -c "TelemetryGap" AnalyzerReport.cs` returns ≥ 1 (path #2 field pair KEPT).
    - `grep -c "ExpectedHopOffset\|valueOracleHopSet" PassFailEngine.cs` returns ≥ 1 (scaffold intentionally retained for Plan 03).
    - `TelemetryGap` / `TelemetryGapDetail` still present in the report (path #2 KEEP).
    - The metric gate and three-class verdict blocks are unchanged.
    - Solution builds 0-warning Debug + Release; `*PassFailEngineFacts` green with zero NEW failures vs the ~272 baseline (surviving label-based facts still pass via the retained scaffold).
  </acceptance_criteria>
  <done>Value-chain machinery removed from the engine; ANL-03 path #2 is the sole non-binding reconciliation; the vestigial `ValueChainOk`/`ValueChainDetail` `required` members removed from AnalyzerReport.cs + the Analyze initializer + the moot OracleAbsent fact (all atomic, tree ends green); `TelemetryGap`/`TelemetryGapDetail` KEPT; `PassFailEngineValueChainFacts.cs` deleted; the `ExpectedHopOffset` scaffold retained so the surviving facts stay green until Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure deletion within the `Category=RealStack` analyzer fixture + its IO-free hermetic engine + a hermetic fact file. No production surface, no untrusted input, no auth/network boundary added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03 | Tampering / Info-disclosure | ES defensive-read contract on surviving parse paths | mitigate | Deletions must not break the `TryGetProperty`/`ValueKind` defensive contract on the structural/dispatch/keeper/trip-duration paths (T-66-09); odd-shaped JSON stays dropped-never-thrown. Verified by the surviving facts staying green. |
| T-78-04 | Repudiation (verdict integrity) | verdict now stands on fewer axes | accept | By design (D-03): the concrete value oracle is removed; the verdict relies on structural stepId completeness + ANL-03 redundancy + the Prometheus metric gate. RESEARCH proved the baseline exercised ZERO value-chain tolerance, so removing it is verdict-neutral. This is an intended reduction, not a security regression — no attack surface. |
</threat_model>

<verification>
- `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` — `*PassFailEngineFacts` green (surviving label facts pass via the retained scaffold); `PassFailEngineValueChainFacts` gone; zero NEW failures vs the ~272 baseline.
- 0-warning Debug + Release solution build.
</verification>

<success_criteria>
- No value-oracle query/parse in the fixture; no value-chain machinery in the engine; ANL-03 path #2 is the sole non-binding reconciliation.
- `PassFailEngineValueChainFacts.cs` deleted entirely; vestigial `ValueChainOk`/`ValueChainDetail` removed; `TelemetryGap`/`TelemetryGapDetail` retained.
- `ExpectedHopOffset` scaffold retained (Plan 03 removes it atomically); tree green, 0-warning build.
</success_criteria>

<output>
After completion, create `.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-02-SUMMARY.md`.
</output>
