---
phase: 78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr
plan: 06
type: execute
wave: 6
depends_on: ["05"]
gap_closure: true
files_modified:
  - analyzer-reports/phase-68-summary.json
autonomous: false
requirements: [D-04]
security_enforcement: true

must_haves:
  truths:
    - "The SourceHash reseed runs FIRST (rebuild both host configs + docker compose build → graph-DELETE → seed → 204) before the sweep"
    - "After the 78-05 canonical-record fix, all 7 scenarios (TEST-01..07) reproduce their committed-HEAD baseline verdict (all PASS) — the 78-04 PASS→FAIL over-count regression is GONE (no scenario shows Duplicates == StartedRuns)"
    - "Each PASS reconstructs from framework ES logs ALONE — structural stepId completeness (canonical 'hop executed' record) + ANL-03 redundancy + the Prometheus metric gate — no StepLabel/Produced value evidence contributes"
  artifacts:
    - path: "analyzer-reports/phase-68-summary.json"
      provides: "The 7-scenario rerun roll-up from the fixed analyzer, compared to git show HEAD (all 7 PASS), NOT the working-tree stale copy"
  key_links:
    - from: "scripts/phase-68-sweep.ps1"
      to: "scripts/phase-67-harness.ps1"
      via: "per-scenario run-all no-fail-fast"
      pattern: "phase-67-harness"
---

<objective>
Re-run the D-04 terminal live gate AFTER the 78-05 canonical-record fix: reseed-first, then `scripts/phase-68-sweep.ps1` over all 7 scenarios, and confirm every scenario now reproduces its committed-HEAD baseline verdict (all 7 PASS) with the value-oracle axis dark — proving the reworked analyzer reconstructs the resilience verdict PURELY AND CORRECTLY from framework ES logs.

Purpose: 78-04 executed this gate and surfaced the blocking finding (every scenario flipped PASS→FAIL, `Duplicates == StartedRuns`, root-caused to the structural over-count). 78-05 fixed it and proved the fix hermetically. This plan re-runs the exact same live gate to confirm the over-count is gone on real Phase-77 data and the phase's terminal proof holds.
Output: a fresh `analyzer-reports/phase-68-summary.json` roll-up whose 7 verdicts match `git show HEAD:analyzer-reports/phase-68-summary.json` (all PASS). A residual verdict SHIFT → STOP, treat as a finding (do NOT silently re-baseline).

Operator-gated: requires Docker + a live compose stack + the reseed (~3.5h). If Docker is unavailable in this environment, follow the deferred-automated fallback (record the exact runbook to `deferred-items.md`) — the precedent for phases 68/73/74/75.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-CONTEXT.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-RESEARCH.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-04-SUMMARY.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-05-SUMMARY.md
@scripts/phase-68-sweep.ps1
@scripts/phase-67-harness.ps1

<interfaces>
<!-- D-04 runbook (78-RESEARCH.md "Live-gate runbook") + 78-04-SUMMARY traps + memory refs. -->

BASELINE ORACLE: `git show HEAD:analyzer-reports/phase-68-summary.json` = all 7 PASS, startedRuns == completeRuns for every scenario. The working-tree `analyzer-reports/phase-68-summary.json` in `git status` is the 78-04 FAILED-FINDING rerun (6× Fail + 1 INDETERMINATE) — do NOT gate against it, and do NOT read it as the baseline.

78-04 REGRESSION SIGNATURE (must be ABSENT this run): every fresh scenario had `Duplicates == StartedRuns == CompleteRuns`, `Missing == 0`, `InFlightLoss == 0` — the effect-once axis false-failing on every hop. After the 78-05 fix, no scenario should show Duplicates ≈ StartedRuns; each should reproduce its HEAD PASS. A no-fault TEST-01 with Duplicates > 0 means the fix did not fully land — STOP and treat as a finding.

RESEED ORDER ([[rebuild-sourcehash-reseed-order]] — Phase 77 touched BaseProcessor.Core, so the new SourceHash has no processor row; a reset heal-wait FAILS until graph-delete → seed → start):
  1. Rebuild BOTH host configs (Debug + the config the compose images build from).
  2. `docker compose build` the images (so the running container is the just-built image; the harness does `docker compose up -d --force-recreate` at STEP A, phase-67-harness.ps1:154).
  3. graph-DELETE the old graph/processor rows (78-04 hit the heal-wait trap here — reset STEP 2 aborts because the new SourceHash has no processor row; unblock via manual FK-safe graph-DELETE → seed → processor registers → liveness reconverges → 204).
  4. seed the fan-out workflow (registers the processor row; persists in the postgres volume so per-scenario resets pass).
  5. confirm the 204 activation gate.
  6. only THEN run the sweep.

SWEEP: `pwsh -File scripts/phase-68-sweep.ps1` — run-all + collect, no fail-fast; exit 0 iff all 7 PASS. It reads only Verdict/StartedRuns/CompleteRuns/Missing/Duplicates from each `{id}.json`.

KNOWN TRAPS (all hit or noted in 78-04 — carry forward):
- DEBUG-SHADOW STALE-REPORT trap (deeper variant of [[phase-68-sweep-stale-report-read]]): a stale `bin/Debug/{id}.json` can SHADOW the fresh `bin/Release/{id}.json` under `Get-ChildItem -Recurse | Select -First 1`, so the HARNESS ITSELF surfaces a FALSE exit-0 PASS (78-04: TEST-01's stale 2026-06-18 Debug copy did exactly this). AUTHORITATIVE = the FRESH `bin/Release/{id}.json` this run's `-c Release` analyzer wrote — trust it over the harness exit code AND the roll-up row when they disagree. Delete/verify stale Debug copies before trusting any green.
- TEST-01 cold-ES INCONCLUSIVE flake: INCONCLUSIVE (harness exit 2) is NOT FAIL — re-run TEST-01 deliberately against warm ES (`pwsh -File scripts/phase-67-harness.ps1 -ScenarioId TEST-01`). No auto-retry (D-04).
- TEST-07 killed mid-analyze (78-04): the on-disk `bin/Release/TEST-07.json` may be a stale prior-run copy — do NOT read a stale copy as this run's result; TEST-07 must produce a FRESH report this run.
- A legitimate verdict SHIFT (any scenario not reproducing its HEAD PASS) → STOP, treat as a finding, do NOT silently re-baseline. A PASS with startedRuns > completeRuns (a genuinely OTLP-dropped hop caught by ANL-03) is a LEGITIMATE PASS — D-04 gates on verdict reproduction, not count reproduction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm the 78-05 hermetic fix is green, capture the HEAD baseline oracle, rebuild + docker compose build</name>
  <files>analyzer-reports/phase-68-summary.json (read HEAD baseline; no edit), docker images (rebuild)</files>
  <read_first>
    - 78-05-SUMMARY.md (the canonical-record fix + hermetic regression facts landed green)
    - 78-RESEARCH.md "Live-gate runbook" Steps 1-2
    - project memory [[rebuild-sourcehash-reseed-order]], [[hermetic-test-command]], [[phase-68-sweep-stale-report-read]]
    - scripts/phase-67-harness.ps1 (STEP A force-recreate at :154)
  </read_first>
  <action>
    Automate everything that does not require a running live stack, BEFORE the operator checkpoint:
    - Re-confirm the 78-05 hermetic gate is green: `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-namespace "*Observability.Analysis*"` — the prior 29 facts PLUS StructuralCohortFacts green, 0 failed. This is the "the fix is proven hermetically THIS time" precondition — if it is red, STOP (do not run the ~3.5h live gate on an unproven fix).
    - Capture the baseline oracle: `git show HEAD:analyzer-reports/phase-68-summary.json` — record all 7 scenario verdicts (expected: all PASS, startedRuns == completeRuns) into the SUMMARY as the comparison table. Do NOT read the working-tree copy (it is the 78-04 failed-finding rerun).
    - Rebuild BOTH host configs (Debug + the compose-image config) and `docker compose build` the images so the running container carries the Phase-77 SourceHash + the 78-05 analyzer fix.
    - Pre-empt the Debug-shadow trap: delete any stale `tests/BaseApi.Tests/bin/Debug/*.json` and `bin/Release/*.json` scenario reports from prior runs so no stale copy can shadow this run's fresh Release report.
    - If Docker CLI is unavailable in this environment (precedent: phases 68/73/74/75 deferred-automated), record the exact reseed+sweep runbook to `deferred-items.md` under "DEFERRED: 78-06 live baseline reproduction (post-fix)" and mark the checkpoint DEFERRED-AUTOMATED. Do not fabricate a live result.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && git show HEAD:analyzer-reports/phase-68-summary.json | grep -c '"verdict": "Pass"'</automated>
  </verify>
  <acceptance_criteria>
    - 78-05 hermetic analyzer subset green (prior 29 + StructuralCohortFacts, 0 failed) confirmed immediately before the live gate.
    - The HEAD baseline oracle is captured into the SUMMARY (7 scenarios, expected all PASS — `git show HEAD:analyzer-reports/phase-68-summary.json` shows 7 `"verdict": "Pass"`).
    - Stale bin/Debug + bin/Release scenario reports cleared (Debug-shadow trap pre-empted).
    - Both host configs rebuilt + `docker compose build` completed (OR, if Docker unavailable, the runbook recorded to `deferred-items.md` and the checkpoint marked DEFERRED-AUTOMATED).
  </acceptance_criteria>
  <done>78-05 hermetic green re-confirmed; HEAD baseline captured as the oracle; stale reports cleared; images rebuilt (or deferred-automated recorded). Ready for the operator reseed + sweep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator live re-gate — reseed + 7-scenario sweep + HEAD-baseline reproduction (post-fix)</name>
  <files>analyzer-reports/phase-68-summary.json</files>
  <action>Operator-gated live verification — run the reseed-first runbook then the 7-scenario sweep and confirm HEAD-baseline reproduction, per the how-to-verify steps below. Not autonomous: requires Docker + a live compose stack (~3.5h) (or DEFERRED-AUTOMATED if Docker is unavailable).</action>
  <what-built>
    The reworked LIVE analyzer now (78-05) selects only the canonical `"hop executed"` processor consume record as the observed did-run hop, so each genuine hop is counted once — fixing the 78-04 over-count that false-failed effect-once on every hop. The images are rebuilt to the Phase-77 SourceHash + this fix. This checkpoint re-runs the mandatory reseed + the 7-scenario live sweep and confirms every scenario reproduces its committed-HEAD baseline verdict (all 7 PASS) with the value axis dark — the phase's terminal proof.
  </what-built>
  <how-to-verify>
    With Docker + the compose stack available, run the reseed-FIRST runbook ([[rebuild-sourcehash-reseed-order]]):
    1. graph-DELETE the old graph/processor rows.
    2. seed the fan-out workflow (registers the processor row; if reset STEP 2 heal-wait aborts, that is the documented trap — unblock via manual FK-safe graph-DELETE → seed → liveness reconverges).
    3. confirm the 204 activation gate.
    4. Run the sweep: `pwsh -File scripts/phase-68-sweep.ps1` (run-all, no fail-fast).
    5. For EACH scenario, read the FRESH `bin/Release/{id}.json` this run wrote (NOT a stale Debug/prior-Release copy — Debug-shadow trap) and compare its verdict to `git show HEAD:analyzer-reports/phase-68-summary.json` (all 7 PASS). Confirm the 78-04 signature is GONE: no scenario should show `Duplicates == StartedRuns`; the no-fault TEST-01 must show Duplicates == 0.
    6. If TEST-01 flakes to INCONCLUSIVE on cold ES, re-run it deliberately warm: `pwsh -File scripts/phase-67-harness.ps1 -ScenarioId TEST-01`. INCONCLUSIVE (exit 2) is NOT FAIL; no auto-retry.
    Expected outcome: all 7 scenarios reproduce their HEAD baseline PASS, reconstructed from framework ES logs alone (structural canonical-record completeness + ANL-03 redundancy + metric gate; no StepLabel/Produced evidence). A PASS with startedRuns > completeRuns (OTLP-dropped hop caught by ANL-03) is still a PASS. A residual verdict SHIFT (any scenario not PASS, or TEST-01 with Duplicates > 0) → STOP and treat it as a finding (do NOT silently re-baseline).
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 scenarios reproduce their HEAD baseline PASS with the over-count gone (TEST-01 Duplicates == 0), or describe the scenario whose verdict shifted (treat as a finding). If Docker is unavailable, type "deferred" to record DEFERRED-AUTOMATED with the runbook in deferred-items.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The live gate exercises the isolated Docker test stack (compose-local ES/Prometheus/RabbitMQ/Redis/Postgres). The analyzer READS first-party ES/Prom; it writes only local `analyzer-reports/*.json`. No production surface, no untrusted input, no auth/network boundary added. The `scenarioId` path-traversal guard already exists via `ScenarioIdPattern`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-13 | Tampering (baseline integrity) | comparison against the wrong baseline | mitigate | Gate against `git show HEAD:analyzer-reports/phase-68-summary.json`, NOT the working-tree copy (which is now the 78-04 failed-finding rerun). The SUMMARY records the HEAD oracle explicitly. |
| T-78-14 | Repudiation (verdict provenance) | a silent re-baseline hides a residual shift | mitigate | D-04: a residual verdict shift STOPS the gate and is treated as a finding — never a silent re-baseline. The rerun report is compared, not overwritten-as-truth. |
| T-78-15 | Spoofing (stale-report false PASS) | Debug-shadow / prior-Release copy shadows the fresh report | mitigate | Clear stale bin/Debug+bin/Release scenario reports in Task 1; trust the FRESH `-c Release {id}.json` over the harness exit code and roll-up row (78-04 lesson). |
| T-78-16 | DoS (test-env artifact) | cold-ES / stale-report flake mis-scored as FAIL | accept | INCONCLUSIVE (exit 2) ≠ FAIL; deliberate warm re-run per runbook. Test-env only; no production impact. |
</threat_model>

<verification>
- `pwsh -File scripts/phase-68-sweep.ps1` exits 0 (all 7 PASS) after the reseed; each scenario's FRESH Release verdict matches the HEAD baseline; the 78-04 `Duplicates == StartedRuns` signature is absent (TEST-01 Duplicates == 0).
- Verdicts reconstruct from framework ES logs alone (canonical-record structural completeness + ANL-03 redundancy + metric gate) — no value-oracle evidence.
- OR: DEFERRED-AUTOMATED recorded with the exact runbook if Docker is unavailable (precedent phases 68/73/74/75).
</verification>

<success_criteria>
- All 7 scenarios reproduce their committed-HEAD baseline verdict (all PASS), over-count gone, value axis dark — OR deferred-automated with the runbook recorded.
- No silent re-baseline; any residual verdict shift surfaced as a finding.
- D-04 requirement CLOSED: the resilience verdict reconstructs purely AND correctly from framework ES logs.
</success_criteria>

<output>
After completion, create `.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-06-SUMMARY.md`.
</output>
