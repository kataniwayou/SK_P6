---
phase: 78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs
  - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs
  - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs
autonomous: true
requirements: [D-03]
security_enforcement: true

must_haves:
  truths:
    - "The last value-oracle residue is gone from the engine — no ExpectedHopOffset, no valueOracleHopSet, no DistinctLabels label-fallback branch in ExpectedFor"
    - "The ~6 facts that previously leaned on the label-fallback now assert the SAME stepId completeness mechanism the live gate uses (explicit expectedStepIdsByExecution)"
    - "The full hermetic analyzer suite is green with zero NEW failures vs the ~272 pre-existing baseline"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs"
      provides: "Verdict engine with zero value-oracle residue"
    - path: "tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs"
      provides: "Facts migrated to the explicit stepId expected-set — the live mechanism"
  key_links:
    - from: "PassFailEngine.ExpectedFor"
      to: "expectedStepIdsByExecution"
      via: "explicit expected-set resolution (label fallback removed)"
      pattern: "expectedStepIdsByExecution"
---

<objective>
Close out D-03 by removing the last value-oracle residue from the engine — `ExpectedHopOffset`, `valueOracleHopSet`, and the `ExpectedFor` `DistinctLabels`-based label-fallback branch (plus `RunTrace.DistinctLabels` if no surviving consumer remains) — while ATOMICALLY migrating the ~6 surviving hermetic facts that lean on that label fallback to the explicit `expectedStepIdsByExecution` stepId path (the mechanism the live gate actually uses). Then run the full hermetic gate: the analyzer suite green, zero new failures, 0-warning Debug + Release.

Purpose: per the CONTEXT amendment (Option B), the label-fallback branch is DEAD in production once the value oracle is gone — retaining it would leave ~6 facts exercising a never-live branch (the same vacuous-coverage trap D-03 exists to eliminate). Migrating them makes them test the live mechanism.
Output: engine has zero value-oracle residue; facts migrated; hermetic gate green — the hard prerequisite before the live sweep (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-CONTEXT.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-RESEARCH.md
@.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-VALIDATION.md

<interfaces>
<!-- CONTEXT amendment locks Option B (delete + migrate). This is the atomic task. -->

ENGINE residue to DELETE (PassFailEngine.cs):
- `ExpectedHopOffset` static map (:510-520)
- `valueOracleHopSet` (:158-159)
- the `ExpectedFor` label-fallback branch: `if (r.DistinctLabels.Count > 0) return valueOracleHopSet;` (:165)
- Any now-orphaned comments referencing the value oracle's canonical hop set (:50-51,:144,:155-157)
- `RunTrace.DistinctLabels` (RunTrace.cs) — DELETE ONLY IF grep confirms no surviving consumer after the fallback branch is gone; else leave it and note the residual consumer.

`ExpectedFor` after migration resolves the expected set as: (1) explicit `expectedStepIdsByExecution`, else (2) `observedUnion`. The label-fallback middle branch is removed.

FACTS to MIGRATE (PassFailEngineFacts.cs) — these build runs via `RunTrace.FromLabels` (so DistinctLabels>0) WITHOUT passing `expectedStepIdsByExecution`, so they currently score via the label fallback; once it is gone they fall to `observedUnion` and a single stalled run scores "complete" (false PASS). Migrate each to `RunTrace.FromStepIds` + an explicit `expectedStepIdsByExecution` (the pattern the Phase-76 facts already use — see PassFailEngineFacts.cs:386-508 / :610-661 for the working `FromStepIds(..., convergentStepId: ConvergentStepId)` + `expectedStepIdsByExecution: expected` shape):
- `Incomplete_StartedRun_DropsStepF2_Yields_Fail` (:82, single run)
- `Incomplete_StartedAfterRecovery_Yields_Fail` (:241, Missing4Hops only)
- `InFlightLoss_ManyCleanDrops_DoNotFail_Yields_Pass` (:257, six Missing4Hops, no complete run)
- `RecoverableButLost_NoKeeperDrop_AfterRecovery_Yields_Fail` (:297, Missing4Hops only)
- `KeeperReinject_VetoesRedisWipeTolerance_StalledBeforeRecovery_Yields_Fail` (:340, Missing4Hops only)
- `Verdict_PartialEvidence_RealConservationGap_Yields_Fail` (:581, Missing4Hops only)

Do NOT change what each fact ASSERTS (Missing count / verdict) — only change how the run + expected-set are constructed so the fact exercises the stepId path. RESEARCH Hazard C enumerates these; treat the list as authoritative but let the hermetic gate (Task 2) be the final arbiter — any label-based fact left red after migration must also be migrated.

Facts that already use `FromStepIds` + `expectedStepIdsByExecution` (:386-508, :610-661) and facts that carry a complete run in the cohort (observedUnion = full set) are unaffected — do not touch them. Metric-gate / three-class-verdict facts are unaffected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ATOMIC — delete ExpectedHopOffset residue AND migrate the ~6 label-fallback facts (one commit)</name>
  <files>tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs, tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs, tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs (:158-167 ExpectedFor/valueOracleHopSet, :510-520 ExpectedHopOffset, :50-51/:144/:155-157 comments)
    - tests/BaseApi.Tests/Observability/Analysis/PassFailEngineFacts.cs (the 6 FromLabels facts above; the working FromStepIds pattern at :386-508 and :610-661)
    - tests/BaseApi.Tests/Observability/Analysis/RunTrace.cs (DistinctLabels definition + FromStepIds signature)
    - 78-RESEARCH.md "Hazard C" (breaking-facts list + the two resolutions — CONTEXT locks Option B)
  </read_first>
  <action>
    This is ONE atomic task/commit — the hermetic tree must never be red mid-way. Do BOTH halves before running the gate:

    Half A — delete the engine residue (PassFailEngine.cs):
    - DELETE the `ExpectedHopOffset` static map (:510-520).
    - DELETE `valueOracleHopSet` (:158-159).
    - DELETE the `ExpectedFor` label-fallback branch `if (r.DistinctLabels.Count > 0) return valueOracleHopSet;` (:165) so `ExpectedFor` resolves ONLY (1) explicit `expectedStepIdsByExecution` else (2) `observedUnion`.
    - Remove now-orphaned comments that describe the value-oracle canonical hop set (:50-51,:144,:155-157).
    - Check `RunTrace.DistinctLabels`: `grep` for remaining consumers after the fallback is gone. If none remain in production/engine code, DELETE `RunTrace.DistinctLabels` (and the `FromLabels`-populated backing) as value-oracle residue; if a surviving consumer exists, leave it and record the consumer in the SUMMARY.

    Half B — migrate the ~6 breaking facts (PassFailEngineFacts.cs) to the stepId path:
    - For each fact in the migration list, replace `RunTrace.FromLabels(corr, exec, <labels>)` with `RunTrace.FromStepIds(corr, exec, <stepIds>, convergentStepId: ConvergentStepId)` and pass an explicit `expectedStepIdsByExecution: expected` to `Analyze(...)`, following the working pattern at `:386-508`. Map `Missing4Hops` labels to the equivalent short stepId set and `AllTenLabelsWithConvergentGx2` to `FullHopStepIds` so the SAME Missing count / verdict the fact asserts is reproduced via the stepId completeness path.
    - Do NOT weaken or change any assertion (Missing count, verdict class) — only the run/expected-set construction changes.

    The two halves land together so no intermediate state scores a stalled run as vacuously "complete".
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && grep -c "ExpectedHopOffset\|valueOracleHopSet" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs; grep -c "DistinctLabels.Count > 0" tests/BaseApi.Tests/Observability/Analysis/PassFailEngine.cs; tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-namespace "*Observability.Analysis*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ExpectedHopOffset" PassFailEngine.cs` returns 0.
    - `grep -c "valueOracleHopSet" PassFailEngine.cs` returns 0.
    - `grep -c "DistinctLabels.Count > 0" PassFailEngine.cs` returns 0 (label fallback removed from ExpectedFor).
    - The 6 migrated facts now call `RunTrace.FromStepIds(...)` and pass `expectedStepIdsByExecution:` (grep each fact body).
    - `*Observability.Analysis*` facts (PassFailEngineFacts + surviving fact classes) run GREEN — no fact left red by the residue deletion; no assertion weakened.
    - Solution builds 0-warning Debug + Release.
  </acceptance_criteria>
  <done>Zero value-oracle residue in the engine; the ~6 facts migrated to the explicit stepId expected-set (live mechanism); analyzer facts green; tree never went red mid-task.</done>
</task>

<task type="auto">
  <name>Task 2: Hermetic green gate — full analyzer suite, zero new failures (prerequisite for the live sweep)</name>
  <files>tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe (run target; no source edit)</files>
  <read_first>
    - 78-VALIDATION.md (sampling grid + decision-branch → fact map; the gate is zero NEW failures in changed scope, not absolute green)
    - 78-RESEARCH.md "Live-gate runbook Step 1" + "Validation Architecture"
    - Project memory [[hermetic-test-command]] (run the .exe directly; ~272 pre-existing Docker-less failures baseline)
  </read_first>
  <action>
    Run the full hermetic subset directly (`dotnet test` hangs on Windows MTP — run the .exe):
    ```
    tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack
    ```
    Confirm the CHANGED (analyzer) scope has ZERO NEW failures vs the ~272 pre-existing Docker-less baseline ([[hermetic-test-command]]): the pre-existing failures are broker/Postgres/Redis/ES-connection Integration tests in the Docker-less sandbox — none are `*PassFailEngine*` / `*AnalyzerE2E*` (hermetic-excluded) / `*BuildKeeperOutcomeMap*` facts. Cross-check that every surviving decision branch in the 78-VALIDATION.md sampling grid has a green fact and that the deleted branches (value-chain path #1, WR-02, entry-marker D09, PassFailEngineValueChainFacts) have their facts removed (not dangling red).

    Also confirm a 0-warning Debug AND Release solution build. Record the exact pass/fail counts + the analyzer-scope subset result in the SUMMARY so Plan 04's operator has a clean hermetic baseline before the reseed.

    This task is the HARD PREREQUISITE gate: the live sweep (Plan 04) must not start until this is green.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P6" && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - All `*PassFailEngineFacts`, `*BuildKeeperOutcomeMapFacts`, `*FanInHermeticHarnessFacts` analyzer facts PASS.
    - Zero NEW failures in changed scope vs the ~272 pre-existing Docker-less baseline (any failures are the known broker/Postgres/Redis/ES Integration set, 0 analyzer facts among them).
    - `PassFailEngineValueChainFacts` and the D09 entry-marker fact are absent (no dangling red).
    - 0-warning Debug + Release solution build.
    - SUMMARY records the exact counts + the analyzer-scope subset result.
  </acceptance_criteria>
  <done>The full hermetic analyzer sampling grid is green with zero new failures and a 0-warning build — the live sweep prerequisite is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Final residue deletion in the IO-free hermetic engine + hermetic fact migration + a test-run gate. No production surface, no untrusted input, no boundary added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-05 | Repudiation (test-coverage integrity) | migrated facts must not become vacuous | mitigate | The migration replaces a DEAD label-fallback branch with the live stepId path — the atomic gate (Task 2) proves no assertion was weakened and no stalled run scores vacuously "complete". This is the exact vacuous-coverage trap the migration eliminates. |
| T-78-06 | Tampering | residual `DistinctLabels` consumer | accept | If a surviving consumer keeps `RunTrace.DistinctLabels` alive, it is first-party test scaffolding with no external input; the SUMMARY records it. No attack surface. |
</threat_model>

<verification>
- `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` — full analyzer sampling grid green; zero new failures vs the ~272 baseline.
- 0-warning Debug + Release solution build.
</verification>

<success_criteria>
- Zero value-oracle residue in the engine (no ExpectedHopOffset / valueOracleHopSet / label-fallback).
- The ~6 label-fallback facts migrated to the explicit stepId expected-set with unchanged assertions.
- Full hermetic analyzer suite green, zero new failures, 0-warning build — live-sweep prerequisite met.
</success_criteria>

<output>
After completion, create `.planning/phases/78-rework-the-resilience-sweep-to-verify-purely-from-the-new-fr/78-03-SUMMARY.md`.
</output>
