---
phase: 44-processor-pre-in-post-process-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["44-02"]
files_modified:
  - src/Processor.Sample/SampleProcessor.cs
  - src/BaseProcessor.Core/Processing/ProcessResult.cs
  - src/Processor.Sample/appsettings.json
  - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs
autonomous: true
requirements: [PIPE-04, RESIL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Processor.Sample overrides the new ProcessAsync(validatedData, payload, ct) → List<ProcessItem> seam and is the worked example of the new author contract"
    - "Processor.Sample demonstrates at least one status-exception path (e.g. throw FailedException)"
    - "The old ProcessResult record and the old two-arg ProcessAsync seam no longer exist anywhere in the solution"
    - "The full solution builds Release 0-warning and the full hermetic suite is GREEN"
  artifacts:
    - path: "src/Processor.Sample/SampleProcessor.cs"
      provides: "Concrete migrated to the new seam — emits a completed ProcessItem with author-minted ExecutionId, shows one status-exception path"
      contains: "List<ProcessItem>"
  key_links:
    - from: "src/Processor.Sample/SampleProcessor.cs"
      to: "BaseProcessor.ProcessAsync(validatedData, payload, ct) → List<ProcessItem>"
      via: "override"
      pattern: "override Task<List<ProcessItem>>"
---

<objective>
Complete the clean break (D-06/D-07): migrate `Processor.Sample` to the new `ProcessAsync → List<ProcessItem>` seam (the compile-forced worked example), delete the obsolete `ProcessResult.cs`, and ensure a `Retry` config section exists so `Retry:Limit` resolves explicitly. This restores a green FULL-solution build (`SK_P.sln`) that Plan 02 deliberately left red on `Processor.Sample`.

Purpose: the v4.0.0 breaking-posture requires NO compatibility adapter — the old seam is gone, and the one concrete demonstrates the new author API (deserialize both strings, emit ≥1 completed item with an author-minted `ExecutionId`, show one status-exception path). After this plan the phase is hermetically complete (RealStack E2E is Phase 49).
Output: 1 migrated concrete, 1 deleted record, 1 appsettings reconcile. Full solution Release 0-warning; full hermetic suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-processor-pre-in-post-process-pipeline/44-CONTEXT.md
@.planning/phases/44-processor-pre-in-post-process-pipeline/44-PATTERNS.md
@src/Processor.Sample/SampleProcessor.cs

<interfaces>
<!-- The new seam (Plan 02, exists GREEN) the concrete overrides. -->
  abstract class BaseProcessor {
    protected abstract Task<List<ProcessItem>> ProcessAsync(string validatedData, string payload, CancellationToken ct);
  }
  record ProcessItem(ProcessOutcome Result, string Data, Guid ExecutionId)   // ProcessOutcome { Completed, Failed }
  sealed class FailedException(string message) : ProcessStatusException(message)   // (+ ProcessingException, CancelledException)

Current SampleProcessor (the migration target — SampleProcessor.cs):
  - keeps the type alias `using BaseProcessorBase = BaseProcessor.Core.Processing.BaseProcessor;` (avoids CS0118)
  - keeps `ILogger<SampleProcessor>` primary-ctor injection
  - DI registration unchanged: `AddSingleton<BaseProcessorBase, SampleProcessor>()` in Program.cs:17
  - current body deserializes `config` (the payload) and echoes it as one ProcessResult

RetryOptions binding: bound from the "Retry" config section at BaseProcessorServiceCollectionExtensions.cs:89; default Limit=3 if the section is absent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate SampleProcessor to the new seam + delete ProcessResult.cs</name>
  <files>src/Processor.Sample/SampleProcessor.cs, src/BaseProcessor.Core/Processing/ProcessResult.cs, tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs</files>
  <read_first>
    - src/Processor.Sample/SampleProcessor.cs (current override at lines 24-40 — the migration target; keep the alias, ctor, DI shape)
    - .planning/phases/44-processor-pre-in-post-process-pipeline/44-PATTERNS.md (§SampleProcessor.cs — the verbatim new shape)
    - src/BaseProcessor.Core/Processing/ProcessItem.cs (Plan 01 — the record to construct)
    - src/BaseProcessor.Core/Processing/ProcessStatusException.cs (Plan 01 — FailedException for the demonstrated path)
    - CONTEXT.md D-06, D-07; § Specific Ideas (the author-API reading guidance)
    - tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs (the existing concrete fact — it must be updated to the new seam or it will not compile)
  </read_first>
  <behavior>
    - `SampleProcessor.ProcessAsync(validatedData, payload, ct)` deserializes `payload` (JSON string), logs it, and returns a single-element `List<ProcessItem>` with `ProcessOutcome.Completed`, the echoed payload (or the `"processor-sample-ok"` fallback when blank), and an author-minted `Guid.NewGuid()` ExecutionId.
    - It demonstrates one status-exception path: when the deserialized payload equals the literal `"fail"`, it `throw new FailedException("sample requested failure")` (the worked status-exception example).
    - `SampleProcessorFacts` (updated) asserts: a normal payload yields one completed `ProcessItem` with a non-empty `ExecutionId`; the `"fail"` payload throws `FailedException`.
  </behavior>
  <action>
**1a. Rewrite `src/Processor.Sample/SampleProcessor.cs`** to the new seam (D-07). Keep the file `<summary>`/`<remarks>` style, the `BaseProcessorBase` alias (CS0118 guard), and the `ILogger<SampleProcessor>` primary ctor:
```csharp
protected override Task<List<ProcessItem>> ProcessAsync(string validatedData, string payload, CancellationToken ct)
{
    // payload IS the dispatch payload (the step's assignment payload), a JSON string e.g. "\"StepA1\"".
    var cfg = string.IsNullOrWhiteSpace(payload) ? null : JsonSerializer.Deserialize<string>(payload);
    logger.LogInformation("sample payload received: {Payload}", cfg);

    // D-07 worked example: demonstrate ONE author status-exception path (aborts the whole batch).
    if (cfg == "fail")
        throw new FailedException("sample requested failure");

    // Emit ≥1 completed item; the author MINTS ExecutionId per item (D-03).
    return Task.FromResult(new List<ProcessItem>
    {
        new(ProcessOutcome.Completed, cfg ?? "processor-sample-ok", Guid.NewGuid()),
    });
}
```
Update the doc comment to reference the new contract (deserialize `payload`, emit a completed `ProcessItem` with an author-minted `ExecutionId`, and the demonstrated `FailedException` status path). `validatedData` is the L2-read input (empty for this source-style sample) — note it in the doc but the sample echoes `payload`, preserving the live-round-trip ES-log proof.

**1b. Delete `src/BaseProcessor.Core/Processing/ProcessResult.cs`** (D-06). After Plan 02 retyped the seam and Plan 02 deleted the old straight-through facts, the only remaining reference is this migrated concrete — once 1a lands, nothing references `ProcessResult`. Delete the file.

**1c. Update `tests/BaseApi.Tests/Processor/SampleProcessorFacts.cs`** to the new seam: assert the normal path returns one `ProcessItem(Completed, …)` with `ExecutionId != Guid.Empty`, and the `"fail"` payload throws `FailedException`. (The fact must invoke the seam; if it used a `protected`-override test-subclass before, keep that pattern but retype to `List<ProcessItem>`.)

**1d. Verify no lingering references:** grep the whole solution for `ProcessResult` and `IReadOnlyList<ProcessResult>` — there must be ZERO matches outside of historical SUMMARY/doc files (`.planning/`, `docs/`). Any code/test match is a compile blocker to fix.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` succeeds with 0 warnings (full solution — Processor.Sample now compiles on the new seam).
    - Grep `src/Processor.Sample/SampleProcessor.cs` matches `override Task<List<ProcessItem>>` AND `new(ProcessOutcome.Completed,` AND `throw new FailedException(`.
    - `src/BaseProcessor.Core/Processing/ProcessResult.cs` does NOT exist (git status shows deletion).
    - Grep the `src/` and `tests/` trees for `ProcessResult` returns ZERO matches (only `.planning/`/`docs/` historical references may remain).
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter-method *SampleProcessor* --filter-not-trait Category=RealStack` is GREEN.
  </acceptance_criteria>
  <done>Processor.Sample compiles on the new seam and demonstrates a status-exception path; ProcessResult.cs is deleted; no code references the old type; full solution builds Release 0-warning.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure Retry config section + final full-suite green gate</name>
  <files>src/Processor.Sample/appsettings.json</files>
  <read_first>
    - src/Processor.Sample/appsettings.json (check for an existing "Retry" section)
    - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs (line ~89 — the RetryOptions bind from "Retry")
    - .planning/phases/44-processor-pre-in-post-process-pipeline/44-RESEARCH.md (§Runtime State Inventory — the A6 appsettings note)
    - .planning/phases/44-processor-pre-in-post-process-pipeline/44-VALIDATION.md (the full-suite + Release-build wave gate)
  </read_first>
  <action>
**2a.** Open `src/Processor.Sample/appsettings.json`. If it has no `"Retry"` section, add one so `Retry:Limit` resolves explicitly rather than falling through to the default-3 (A6 — make the budget explicit and auditable):
```json
"Retry": { "Limit": 3, "Strategy": "Immediate" }
```
Place it as a sibling of the other top-level config sections (`Liveness`/`Redis`/etc). If a `"Retry"` section already exists, leave it (do not change a non-default value an operator may have set) — just confirm `Limit` is present.

**2b.** Run the full hermetic suite + a clean Release build as the phase-44 close-of-plan gate. This is the wave-merge sample per VALIDATION.md.
  </action>
  <verify>
    <automated>dotnet test SK_P.sln --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` → 0 warnings.
    - `dotnet test SK_P.sln --filter-not-trait Category=RealStack` → full hermetic suite GREEN (all Pipeline*Facts + RetryLoopFacts + SampleProcessorFacts + the unaffected suite).
    - Grep `src/Processor.Sample/appsettings.json` matches a `"Retry"` object containing `"Limit"`.
    - Every VALIDATION.md `--filter-method` glob from the per-task map still resolves to GREEN tests (no regression from the Sample migration).
  </acceptance_criteria>
  <done>Retry:Limit resolves explicitly from Processor.Sample appsettings; the full solution builds Release 0-warning and the full hermetic suite is GREEN — Phase 44 hermetic completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author In-Process (SampleProcessor) → framework | The migrated concrete returns `ProcessItem`s / throws `FailedException`; the framework treats these as untrusted control signals (Plan 02 catch semantics). |
| config (appsettings) → RetryOptions | `Retry:Limit` is operator-set config bounding the retry budget. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-09 | Denial of Service | a mis-configured `Retry:Limit` (0 or huge) | mitigate | `RetryLoop` clamps to `Math.Max(1, limit)` (≥1 attempt, Plan 01); a large value is still bounded immediate-no-backoff and the bus `_error` latch caps amplification. The added appsettings value pins it to the documented default 3. |
| T-44-10 | Tampering | the SampleProcessor `"fail"` demonstrator path | accept | Worked-example control flow only; it routes to a normal business `StepFailed` via the Plan-02 catch — no external surface, no privilege boundary. |

No new secrets, endpoints, or auth surfaces. JSON-schema validation surface unchanged (no new validator). No HIGH-severity unmitigated threat.
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` → 0 warnings (full solution green — the clean break completes).
- `dotnet test SK_P.sln --filter-not-trait Category=RealStack` → full hermetic suite GREEN.
- `ProcessResult` has zero references in `src/`/`tests/`.
- `Processor.Sample` demonstrates the new author contract end-to-end (completed item + status-exception path).
</verification>

<success_criteria>
- PIPE-04: the one concrete (`Processor.Sample`) overrides the new `ProcessAsync → List<ProcessItem>` seam and is the worked example (author-minted ExecutionId, one status-exception path).
- RESIL-01: `Retry:Limit` resolves explicitly from `Processor.Sample` appsettings (shared retry budget made auditable).
- D-06: `ProcessResult.cs` deleted; no compatibility adapter; zero references remain.
- D-07: `Processor.Sample` migrated in-phase, compile-forced by the seam delete.
- Full solution builds Release 0-warning; full hermetic suite GREEN — Phase 44 hermetic completion (RealStack E2E is Phase 49).
</success_criteria>

<output>
After completion, create `.planning/phases/44-processor-pre-in-post-process-pipeline/44-03-SUMMARY.md`
</output>
