---
phase: 47-dlq-consolidation-at-least-once-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs
  - tests/BaseApi.Tests/Resilience/AtLeastOnceStructuralFacts.cs
autonomous: true
requirements: [RESIL-02, RESIL-03]

must_haves:
  truths:
    - "A harness fact proves a processor-flavored send-exhaustion throw routes the faulted message to skp-dlq-1 (ConsolidatedFault on Dlq1)."
    - "A reflection guard fails if any MessageIdentity type (or dedup member) survives on the Orchestrator or BaseProcessor.Core execution-path assemblies."
    - "A source-scan guard fails if any file under src/BaseProcessor.Core/Processing/ or src/Keeper/Recovery/ (excluding KeeperRecoveryHandler.cs) references KeeperQueues.DeadLetter / keeper-dlq."
    - "All three facts carry [Trait(\"Phase\",\"47\")] and are discoverable via --filter-trait \"Phase=47\"."
  artifacts:
    - path: "tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs"
      provides: "Processor send-exhaustion -> skp-dlq-1 sibling [Fact]"
      contains: "ConsolidatedErrorTransportFilter.Dlq1"
    - path: "tests/BaseApi.Tests/Resilience/AtLeastOnceStructuralFacts.cs"
      provides: "Reflection no-dedup guard (R4) + source-scan no-keeper-dlq guard (R1 structural)"
      contains: "MessageIdentity"
  key_links:
    - from: "AtLeastOnceStructuralFacts source-scan"
      to: "src/BaseProcessor.Core/Processing + src/Keeper/Recovery"
      via: "Directory.EnumerateFiles excluding KeeperRecoveryHandler.cs"
      pattern: "KeeperRecoveryHandler\\.cs"
    - from: "AtLeastOnceStructuralFacts reflection guard"
      to: "Orchestrator + BaseProcessor.Core assemblies"
      via: "typeof(StepDispatcher).Assembly / typeof(ProcessorPipeline).Assembly"
      pattern: "typeof\\(.*\\)\\.Assembly"
---

<objective>
Prove the single-DLQ-consolidation structural invariant (RESIL-02) and the no-dedup structural invariant (RESIL-03) with three hermetic facts: (1) a processor-flavored send-exhaustion -> `skp-dlq-1` harness fact (R1 explicit framing), (2) a reflection guard that no `MessageIdentity`/dedup member survives on the execution-path assemblies (R4), and (3) a source-scan guard that no v4 give-up path references `keeper-dlq` (R1 structural).

Purpose: Close the proof gap — there is no standing guard against re-introducing a second DLQ or a dedup key, and no fact frames the processor pipeline's `throw sent.Error!` as landing in `skp-dlq-1`. This is verification only; NO production code change is expected (a production touch is permitted ONLY as an explicitly-surfaced minimal gap-fix if a fact reveals a real defect).
Output: A sibling `[Fact]` in `KeeperDlqConsolidationTests.cs` and a new `AtLeastOnceStructuralFacts.cs`, all `[Trait("Phase","47")]`, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-SPEC.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-RESEARCH.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-VALIDATION.md

<interfaces>
<!-- Extracted from the repo this session. Executor uses these directly — no exploration needed. -->

Consolidated DLQ const (reference this, NEVER the "skp-dlq-1" literal):
  src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs
    public const string Dlq1 = "skp-dlq-1";          // ConsolidatedErrorTransportFilter.Dlq1
    record/type ConsolidatedFault                    // the moved envelope observed via harness.Consumed.Any<ConsolidatedFault>()

Forbidden token for the source-scan (reference the const where asserting, but scan for BOTH forms):
  src/Messaging.Contracts/KeeperQueues.cs
    public const string DeadLetter = "keeper-dlq";   // KeeperQueues.DeadLetter

Reflection anchors (both public sealed classes — verified loadable from the test project, parity with firewall tests):
  Orchestrator.Dispatch.StepDispatcher                 // typeof(StepDispatcher).Assembly  -> Orchestrator assembly
  BaseProcessor.Core.Processing.ProcessorPipeline      // typeof(ProcessorPipeline).Assembly -> BaseProcessor.Core assembly

Existing harness rig to reuse (KeeperDlqConsolidationTests.cs):
  private static ServiceProvider BuildHarness(int retryLimit)   // installs the exact production ConfigureError
    (GenerateFaultFilter + ConsolidatedErrorTransportFilter) via AddConfigureEndpointsCallback + Immediate(N)
    + a no-op handler bound to ConsolidatedErrorTransportFilter.Dlq1 so the moved ConsolidatedFault is observable.
  public sealed record AlwaysFaults(string Id);
  public sealed class AlwaysFaultsConsumer : IConsumer<AlwaysFaults>  // Consume throws InvalidOperationException

Firewall reflection idiom to mirror (KeeperDependencyFirewallTests.cs):
  typeof(global::Keeper.Consumers.FaultEntryStepDispatchConsumer).Assembly.GetReferencedAssemblies()...
  Assert.Empty(violating);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Processor send-exhaustion -> skp-dlq-1 sibling fact</name>
  <files>tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs (the BuildHarness rig at line 50; the Dlq1_Consolidated fact at line 82; the AlwaysFaults/AlwaysFaultsConsumer throwaway consumer at lines 35-41 — the proven template)
    - 47-RESEARCH.md "Processor Send-Exhaustion Seam" (ProcessorPipeline.cs:170-184 `throw sent.Error!`; assessment A2 — a throwing consumer is the proven hermetic equivalent; do NOT boot a real ProcessorPipeline — the rig has no Redis/sendProvider wiring)
  </read_first>
  <behavior>
    - A processor-flavored send-exhaustion throw, run through the same ConfigureError rig, lands in skp-dlq-1 as a ConsolidatedFault (no dedicated keeper-dlq).
    - GenerateFaultFilter is retained (Fault<T> still published on exhaustion).
  </behavior>
  <action>
    Add ONE new `[Fact]` named `ProcessorSendExhaustion_RoutesToDlq1` to `KeeperDlqConsolidationTests` (sibling in the SAME class, SAME `BuildHarness` rig — Claude's-discretion D-04 confirms keep it in this rig). Add `[Trait("Phase","47")]` to the new fact (the existing class facts are untagged — leave them; only the new fact carries the trait).

    Frame it as the processor pipeline's send-exhaustion: add a small throwaway record + consumer that simulates the pipeline's `throw sent.Error!` (mirror the existing `AlwaysFaults`/`AlwaysFaultsConsumer` shape — e.g. a `ProcessorSendExhausted(string Id)` record whose consumer throws an exception representing an exhausted `SendResult`/`SendKeeper` send). Register it in a local harness variant OR reuse the existing rig by publishing the existing `AlwaysFaults` and documenting (in an XML-doc/comment on the fact) that the processor pipeline's `throw sent.Error!` propagates through MassTransit `UseMessageRetry` -> the SAME inherited consolidated `_error` move. Prefer the explicit throwaway consumer so the fact NAME and the assertion frame the processor path, not a generic one.

    Assert exactly as the proven shape: after publish + `harness.Consumed.Any<T>` for the trigger, assert `harness.Published.Any<Fault<...>>` (Fault retained) AND `harness.Consumed.Any<ConsolidatedFault>(ct)` (moved to skp-dlq-1). Reference `ConsolidatedErrorTransportFilter.Dlq1` for the sink endpoint name — NEVER the `"skp-dlq-1"` literal (Pitfall 3).

    Do NOT modify the existing `Dlq1_Consolidated` / `Keeper_SendFault_RetriesToDlq1` / `Dlq_TopologyArgs` facts. Do NOT add a real `ProcessorPipeline` boot (A2 — needs Redis/IDatabase/sendProvider the rig lacks).
  </action>
  <verify>
    <automated>dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*ProcessorSendExhaustion*"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*ProcessorSendExhaustion*"` exits 0 with the new fact green.
    - The new fact asserts `harness.Consumed.Any<ConsolidatedFault>(ct)` is true and references `ConsolidatedErrorTransportFilter.Dlq1` (grep finds `ConsolidatedErrorTransportFilter.Dlq1` in the new fact's body; grep finds NO new `"skp-dlq-1"` string literal in the file).
    - The new fact carries `[Trait("Phase","47")]`.
    - `dotnet build SK_P.sln` stays 0 warnings / 0 errors.
  </acceptance_criteria>
  <done>A processor-flavored send-exhaustion fact proves the route to skp-dlq-1 via the existing rig, tagged Phase 47, green, 0/0 build.</done>
</task>

<task type="auto">
  <name>Task 2: AtLeastOnceStructuralFacts — reflection no-dedup guard + source-scan no-keeper-dlq guard</name>
  <files>tests/BaseApi.Tests/Resilience/AtLeastOnceStructuralFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/KeeperDependencyFirewallTests.cs (the `typeof(X).Assembly` reflection-assertion idiom to mirror — anchor type, collect violators, `Assert.Empty`)
    - 47-RESEARCH.md "Structural Guards (D-03) — Idiom + Landmines" (the exact reflection snippet using StepDispatcher/ProcessorPipeline assemblies; the source-scan snippet with the MANDATORY KeeperRecoveryHandler.cs exclusion) AND "Common Pitfalls" Pitfalls 1 & 2
    - src/Keeper/Recovery/KeeperRecoveryHandler.cs (the ONLY legitimate keeper-dlq sender — lines 136, 173; the file the source-scan MUST exclude)
    - src/Messaging.Contracts/PauseWorkflow.cs + ResumeWorkflow.cs (the legitimate positional `string H` BIT-gate pause key — the reason the no-dedup guard MUST be reflection, NOT a string-scan)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Resilience/AtLeastOnceStructuralFacts.cs` (new namespace `BaseApi.Tests.Resilience` — Claude's-discretion D-03; keep parity with the firewall tests' single-anchor-type style). The class holds TWO `[Fact]`s, BOTH carrying `[Trait("Phase","47")]`.

    FACT A — `No_dedup_machinery_on_execution_path` (R4, RESIL-03), REFLECTION (LANDMINE 2 — reflection, NOT string-scan):
      - Load the two execution-path assemblies via `typeof(Orchestrator.Dispatch.StepDispatcher).Assembly` and `typeof(BaseProcessor.Core.Processing.ProcessorPipeline).Assembly`.
      - Assert NO type named `MessageIdentity` survives in either: `foreach (var asm in new[]{orchestrator, baseProcessor}) Assert.DoesNotContain(asm.GetTypes(), t => t.Name == "MessageIdentity");`
      - Additionally assert no live dedup MEMBER survives: over the same assemblies' types, assert no public/instance property or field named `MessageIdentity` (and no member literally named `flag` representing a dedup gate). Use `GetProperties`/`GetFields` reflection — mirror the `KeeperContractTests` GetProperty absence idiom referenced in RESEARCH.md. Do NOT scan the BIT-gate `string H` member: H is a legitimate positional contract member on PauseWorkflow/ResumeWorkflow (NOT on the execution-path assemblies being reflected here, and NOT dedup) — the type/member-name guard (MessageIdentity / dedup-key) sidesteps it entirely. Document WHY in an XML-doc comment (cite Pitfall 2).

    FACT B — `No_v4_give_up_path_references_keeper_dlq` (R1 structural, RESIL-02), SOURCE-SCAN (LANDMINE 1 — exclude KeeperRecoveryHandler.cs):
      - Resolve the repo root from a known anchor: use `[CallerFilePath]` (or walk up from `AppContext.BaseDirectory` until the dir containing `SK_P.sln`) to locate `src/BaseProcessor.Core/Processing/` and `src/Keeper/Recovery/`. Assert both directories exist (fail loudly if the anchor resolution is wrong — a silently-empty scan is a false pass).
      - Enumerate `*.cs` in both dirs, EXCLUDE the file named `KeeperRecoveryHandler.cs` by `Path.GetFileName(f) != "KeeperRecoveryHandler.cs"` (MANDATORY — it is the dormant reactive keeper-dlq sender, retired Phase 48; document WHY in a comment citing Pitfall 1).
      - Flag any remaining file whose text contains `KeeperQueues.DeadLetter` OR `keeper-dlq`. `Assert.Empty(offenders)` (include the offending file paths in the assertion message so a future regression names the file).
      - Reference the forbidden token as `KeeperQueues.DeadLetter` in the doc-comment and assert against both the symbol and the `"keeper-dlq"` literal in the scan (a re-introduction could use either form).

    Do NOT add any production source change. If FACT B finds a real offender (a v4 file legitimately referencing keeper-dlq that ISN'T KeeperRecoveryHandler.cs), STOP and surface it as a finding in the SUMMARY (do NOT silently widen the exclusion) — that would be a real RESIL-02 defect.
  </action>
  <verify>
    <automated>dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"` runs and BOTH `No_dedup_machinery_on_execution_path` and `No_v4_give_up_path_references_keeper_dlq` are green.
    - FACT A reflects over BOTH the Orchestrator and BaseProcessor.Core assemblies (grep finds both `StepDispatcher` and `ProcessorPipeline` as `typeof(...).Assembly` anchors) and asserts no `MessageIdentity` type/member (grep finds `MessageIdentity` in the assertion).
    - FACT B's source-scan grep contains the literal exclusion `KeeperRecoveryHandler.cs` AND scans for both `KeeperQueues.DeadLetter` and `keeper-dlq`; the fact asserts both scoped directories exist before scanning (no silent empty pass).
    - Both facts carry `[Trait("Phase","47")]`.
    - `dotnet build SK_P.sln` is 0 warnings / 0 errors.
  </acceptance_criteria>
  <done>AtLeastOnceStructuralFacts.cs exists with a reflection no-dedup guard and a directory-scoped no-keeper-dlq source-scan (KeeperRecoveryHandler.cs excluded), both Phase-47-tagged and green, 0/0 build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — hermetic test + structural guard only) | No new runtime input path; no production code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering (false-negative guard) | FACT B source-scan resolving the wrong/empty directory | mitigate | Assert both scoped directories EXIST before enumerating; fail loudly if the repo-root anchor resolves wrong (a silently-empty scan is a false pass that would mask a real keeper-dlq re-introduction). |
| T-47-02 | Tampering (over-broad exclusion) | FACT B excluding more than KeeperRecoveryHandler.cs | accept | Exclusion is a single hard-coded filename; any future real offender surfaces as a finding in SUMMARY, not a silent widening. Low risk — exclusion is explicit and reviewed. |
| T-47-03 | Tampering (false-positive guard masking real removal) | FACT A reflection anchoring on the wrong assembly | mitigate | Anchor on two distinct public sealed execution-path types (StepDispatcher + ProcessorPipeline) verified loadable; a wrong anchor fails to compile, not silently pass. |
</threat_model>

<verification>
- `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"` — all Phase-47 facts in this plan green.
- `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*ProcessorSendExhaustion*"` — the send-exhaustion fact green.
- `dotnet build SK_P.sln` — 0 warnings / 0 errors.
- Full hermetic suite per VALIDATION.md (2 pre-existing broker-dependent E2E failures — SampleRoundTripE2ETests, MetricsRoundTripE2ETests — are NOT regressions).
</verification>

<success_criteria>
- Processor send-exhaustion -> skp-dlq-1 fact exists, Phase-47-tagged, green, references `ConsolidatedErrorTransportFilter.Dlq1` (no literal).
- AtLeastOnceStructuralFacts.cs: reflection no-dedup guard (no `MessageIdentity` type/member on Orchestrator + BaseProcessor.Core) + source-scan no-keeper-dlq guard (excludes KeeperRecoveryHandler.cs, scans both scoped dirs), both green.
- Both landmines honored: reflection (not string-scan) for no-dedup; KeeperRecoveryHandler.cs excluded in the keeper-dlq scan.
- No production source change (gap-fix only if a fact surfaces a real defect — then surfaced explicitly).
- `dotnet build SK_P.sln` 0/0.
</success_criteria>

<output>
After completion, create `.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-01-SUMMARY.md` recording: the new fact names + files, confirmation both landmines are honored (exact exclusion line + reflection-not-string-scan), and any surfaced finding (or "none — verify-only, no production change").
</output>
