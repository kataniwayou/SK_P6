---
phase: 47-dlq-consolidation-at-least-once-semantics
plan: 03
type: execute
wave: 2
depends_on: [47-01, 47-02]
files_modified:
  - .planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md
  - docs/design/2026-06-08-processor-keeper-recovery-redesign.md
autonomous: true
requirements: [RESIL-02, RESIL-03]

must_haves:
  truths:
    - "47-DLQ-AUDIT.md exists with one row per RESIL-02, RESIL-03, and roadmap SC-1/SC-2/SC-3, each mapped to a named proving test (file:method)."
    - "Every audit row resolves to a real GREEN test — no row is left 'unproven'."
    - "The design doc carries a dated at-least-once / no-dedup guarantee amendment that cross-references the existing statements (lines 5/105/112) and cites 47-DLQ-AUDIT.md."
    - "The pending Phase-46 Payload-on-KeeperReinject amendment is bundled into the same design-doc edit."
  artifacts:
    - path: ".planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md"
      provides: "Traceability ledger: Req/SC -> proving test (file:method)"
      contains: "RESIL-02"
    - path: "docs/design/2026-06-08-processor-keeper-recovery-redesign.md"
      provides: "Dated at-least-once guarantee amendment + bundled Payload amendment"
      contains: "at-least-once"
  key_links:
    - from: "47-DLQ-AUDIT.md rows"
      to: "the green Phase-47 (and cited Phase-46) facts"
      via: "file:method citation per row"
      pattern: "KeeperDlqConsolidationTests|RecoveryDeadLetterFacts|TypedResultConsumerFacts|AtLeastOnceStructuralFacts"
    - from: "design-doc amendment line"
      to: "47-DLQ-AUDIT.md"
      via: "cross-reference citation"
      pattern: "47-DLQ-AUDIT"
---

<objective>
Produce the phase's primary human-readable deliverable: the `47-DLQ-AUDIT.md` traceability ledger (D-01) mapping RESIL-02, RESIL-03, and roadmap SC-1/SC-2/SC-3 to their proving tests, and amend the locked design doc (D-02) with the explicit at-least-once / no-dedup guarantee statement (bundling the pending Phase-46 `Payload`-on-`KeeperReinject` amendment).

Purpose: R5 — there is no consolidated audit; proof is scattered. The audit ledger is what the verifier checks (every row -> a real green test). The design-doc amendment elevates the already-present at-least-once prose (lines 5/105/112) to a named, test-cited guarantee. This plan runs in Wave 2 because every audit row must resolve to a test that is already green from Wave 1.
Output: A new `47-DLQ-AUDIT.md` and an additive amendment to the design doc (the ONLY production-adjacent write in the phase — no source code change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-SPEC.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-CONTEXT.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-RESEARCH.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-VALIDATION.md

<coverage_map>
<!-- The audit rows. Every "exists/cite" row is already green; every "Wave 1" row is green after Plans 01/02. -->
| Req | Roadmap SC | Proving test (file:method) | Status |
|-----|-----------|----------------------------|--------|
| RESIL-02 R1 generic exhaustion -> skp-dlq-1 | SC-1 | KeeperDlqConsolidationTests.Dlq1_Consolidated ; KeeperDlqConsolidationTests.Keeper_SendFault_RetriesToDlq1 | cite (existing green) |
| RESIL-02 R1 processor send-exhaustion -> skp-dlq-1 | SC-1 | KeeperDlqConsolidationTests.ProcessorSendExhaustion_RoutesToDlq1 | Plan 01 (green) |
| RESIL-02 R1 structural: no v4 path -> keeper-dlq | SC-1 | AtLeastOnceStructuralFacts.No_v4_give_up_path_references_keeper_dlq | Plan 01 (green) |
| RESIL-02 R2 REINJECT-data-gone -> skp-dlq-1 | SC-3 | RecoveryDeadLetterFacts.DataGone_reinject_faults_and_routes_to_dead_letter | cite (existing green, Phase-47 re-tagged by Plan 02) |
| RESIL-03 R3 duplicate StepCompleted no-collapse | SC-2 | TypedResultConsumerFacts.Duplicate_StepCompleted_reproduces_effect_no_collapse | Plan 02 (green) |
| RESIL-03 R3 duplicate EntryStepDispatch/Reinject | SC-2 | RecoveryDeadLetterFacts.Duplicate_Reinject_reproduces_effect_no_collapse | Plan 02 (green) |
| RESIL-03 R4 structural no-dedup | SC-2 | AtLeastOnceStructuralFacts.No_dedup_machinery_on_execution_path | Plan 01 (green) |
</coverage_map>

<design_doc_anchor>
<!-- docs/design/2026-06-08-processor-keeper-recovery-redesign.md — verified this session -->
Line 3: **Status:** LOCKED 2026-06-08 — source of truth.
Line 4: **Amended 2026-06-08 (A15):** ...   <- the amendment-line STYLE to mirror (add a new dated amendment line here).
Line 5: **Delivery model:** at-least-once; no dedup / idempotency key ...   <- already-present statement to cross-reference.
Line 105 (locked-decisions table): "No dedup / idempotency key ...; at-least-once; duplicates tolerated."
Line 112 (A4): "Single _DLQ1 for all terminal give-ups; keeper-dlq removed."
Pending Phase-46 bundle: KeeperReinject.Payload : string is live (KeeperContractTests.cs:60-69 asserts it; RecoveryDeadLetterFacts.cs:102 sets it) but the design doc has not recorded the Payload-on-KeeperReinject amendment — bundle it here (46-CONTEXT <deferred>).
</design_doc_anchor>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 47-DLQ-AUDIT.md traceability ledger (R5, D-01)</name>
  <files>.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md</files>
  <read_first>
    - .planning/phases/47-dlq-consolidation-at-least-once-semantics/47-VALIDATION.md (the traceability-table style to follow — Claude's-discretion D-01: follow the VALIDATION.md table shape; the Per-Task Verification Map is the row source)
    - .planning/phases/47-dlq-consolidation-at-least-once-semantics/47-RESEARCH.md "Coverage Map" (the authoritative Req/SC -> proving-test mapping; the `<coverage_map>` in this plan's context mirrors it with final method names)
    - 47-SPEC.md Requirement 5 + Acceptance (every RESIL id and roadmap SC has >=1 named test reference; no row left "unproven")
  </read_first>
  <action>
    Create `.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md` — a standalone traceability ledger (D-01: separate from VALIDATION.md and the design doc). Follow the VALIDATION.md table style. Include:

    - A short header (phase, date, purpose: "maps RESIL-02 / RESIL-03 and roadmap SC-1/SC-2/SC-3 to their proving tests; every row resolves to a real green hermetic test").
    - ONE table with columns (Claude's-discretion on exact layout, but at minimum): `Requirement` | `Roadmap SC` | `Invariant / behavior` | `Proving test (file:method)` | `Status (cite-existing / new gap-fill)` | `Verify command`.
    - One row per item in this plan's `<coverage_map>` block (7 rows): the 2 cited-existing generic-consolidation facts, the processor send-exhaustion fact, the no-keeper-dlq structural fact, the data-gone fact (cited; Phase-47 re-tagged), the two duplicate-delivery facts, and the no-dedup reflection fact. Map RESIL-02 to R1/R2 rows (SC-1, SC-3) and RESIL-03 to R3/R4 rows (SC-2). Ensure ALL THREE roadmap SCs (SC-1 consolidation, SC-2 at-least-once/no-dedup, SC-3 data-gone terminal) and BOTH RESIL ids appear, each with >=1 named test.
    - Each `Verify command` cell uses the runner-correct form: `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*<Method>*"` (or `--filter-trait "Phase=47"` for the structural facts).
    - A closing note: the live/real-stack DLQ + TTL proof is deferred to Phase 49 (TEST-01..03) — hermetic-only here; and the at-least-once guarantee statement lives in the design doc (cross-reference the file + the Task-2 amendment).

    No row may be left "unproven" — every method name in the ledger MUST match a real fact created/cited in Plans 01/02. Before finalizing, run the verify command per row mentally against the actual test names produced by Plans 01/02.
  </action>
  <verify>
    <automated>dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md` exists.
    - Grep finds rows for BOTH `RESIL-02` and `RESIL-03`, and for all three roadmap SCs (SC-1, SC-2, SC-3).
    - Each row's `Proving test` cell names a real method present in the suite — grep each method name (`Dlq1_Consolidated`, `ProcessorSendExhaustion_RoutesToDlq1`, `No_v4_give_up_path_references_keeper_dlq`, `DataGone_reinject_faults_and_routes_to_dead_letter`, `Duplicate_StepCompleted_reproduces_effect_no_collapse`, `Duplicate_Reinject_reproduces_effect_no_collapse`, `No_dedup_machinery_on_execution_path`) and confirm it exists in tests/BaseApi.Tests.
    - `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"` is green (every cited new fact resolves).
    - No row contains "unproven" / "TODO" / "TBD".
  </acceptance_criteria>
  <done>47-DLQ-AUDIT.md maps RESIL-02, RESIL-03, and SC-1/2/3 each to >=1 named green proving test, with runner-correct verify commands and no unproven row.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the design doc — at-least-once guarantee + bundled Payload amendment (R-doc, D-02)</name>
  <files>docs/design/2026-06-08-processor-keeper-recovery-redesign.md</files>
  <read_first>
    - docs/design/2026-06-08-processor-keeper-recovery-redesign.md (the A15 amendment-line at line 4 — the STYLE to mirror; the at-least-once statement at line 5; the locked-decisions/A4 statements at lines 105/112 to cross-reference)
    - 47-RESEARCH.md "Design-Doc Amendment (D-02) — Exact Location" (this is a small mostly-cross-referencing amendment, NOT a rewrite; bundle the Phase-46 Payload-on-KeeperReinject amendment — KeeperContractTests.cs:60-69 + RecoveryDeadLetterFacts.cs:102)
    - 47-CONTEXT.md D-02 + <specifics> (amend the locked doc, do not rewrite; bundle both user-owned amendments in one edit)
  </read_first>
  <action>
    Amend `docs/design/2026-06-08-processor-keeper-recovery-redesign.md` (LOCKED — amend, do NOT rewrite or reflow existing content). Add a NEW dated amendment line in the header block immediately after the existing A15 amendment line (line 4), mirroring its `**Amended <date> (Axx):**` style. The amendment MUST:

    1. Elevate the at-least-once / no-dedup property to an explicit NAMED guarantee statement: the v4 execution path is at-least-once, carries no dedup/idempotency key (the v3.x `H` + dedup gate were removed in Phase 43), and tolerates duplicate effects downstream by construction. Cross-reference the existing statements at lines 5, 105 (locked decisions), and 112 (A4 single `_DLQ1`) rather than restating them at length.
    2. Cite the Phase-47 proof: reference `.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md` as the traceability ledger proving the guarantee (consolidation + no-collapse) hermetically; note live proof is Phase 49.
    3. BUNDLE the pending Phase-46 amendment in the SAME edit (one dated line or an adjacent line): record that `KeeperReinject` carries a `Payload : string` (verified live by `KeeperContractTests` and set in `RecoveryDeadLetterFacts`) — the deferred Phase-46 design-doc note (46-CONTEXT `<deferred>`).

    Keep the edit additive and minimal (a header amendment line + optionally one cross-reference line near line 5/112). Do NOT modify any source code. Do NOT renumber existing locked decisions; use the next free `Axx` id consistent with the doc's numbering (A15 is the latest — verify the next id by scanning existing `A##` ids in the doc before choosing).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln</automated>
  </verify>
  <acceptance_criteria>
    - The design doc contains a NEW dated amendment line (post-A15) naming the at-least-once / no-dedup guarantee and cross-referencing lines 5/105/112 (grep finds "at-least-once" in a new amendment line distinct from the existing line 5).
    - The amendment cites `47-DLQ-AUDIT.md` (grep finds `47-DLQ-AUDIT`).
    - The amendment records the `KeeperReinject` `Payload` field (grep finds `Payload` in the new amendment text alongside `KeeperReinject`).
    - The existing lines 3-5 and the locked-decisions table are unchanged except for the additive amendment line(s) (no reflow of existing content; no source code change anywhere — `git diff --stat` shows only the design doc + the audit doc).
    - `dotnet build SK_P.sln` is 0 warnings / 0 errors (a doc-only edit must not affect the build).
  </acceptance_criteria>
  <done>The design doc carries an additive dated amendment naming the at-least-once/no-dedup guarantee, citing 47-DLQ-AUDIT.md, and recording the KeeperReinject.Payload field — no source change, build 0/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation only) | No runtime input; the only non-test write is prose in a design doc. No secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-07 | Repudiation (unproven audit row) | 47-DLQ-AUDIT.md citing a test name that does not exist / is red | mitigate | Wave-2 ordering guarantees every cited fact is already green; acceptance criteria require grepping each method name against the real suite + a green Phase-47 run before sign-off. |
| T-47-08 | Tampering (locked-doc drift) | The design-doc amendment rewriting/reflowing locked content | mitigate | Action mandates additive-only header amendment line(s); acceptance criteria require `git diff --stat` to show ONLY the two doc files and no source change. |
</threat_model>

<verification>
- `47-DLQ-AUDIT.md` exists; every row -> a real green test (grep each method name; `--filter-trait "Phase=47"` green).
- Design doc has the additive at-least-once + Payload amendment citing the audit.
- `git diff --stat` shows ONLY `47-DLQ-AUDIT.md` and the design doc changed (no source code).
- `dotnet build SK_P.sln` — 0 warnings / 0 errors.
</verification>

<success_criteria>
- 47-DLQ-AUDIT.md maps RESIL-02, RESIL-03, and roadmap SC-1/2/3 each to >=1 named green proving test; no unproven row; runner-correct verify commands.
- Design doc carries a dated, additive at-least-once/no-dedup guarantee amendment cross-referencing lines 5/105/112 and citing 47-DLQ-AUDIT.md, with the bundled Phase-46 KeeperReinject.Payload note.
- No source code change (doc-only writes); `dotnet build SK_P.sln` 0/0.
</success_criteria>

<output>
After completion, create `.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-03-SUMMARY.md` recording: the audit-doc row->test mapping (confirm all 7 rows green), the design-doc amendment id + that it bundled the Payload note, and confirmation the only writes were the two doc files (git diff --stat).
</output>
