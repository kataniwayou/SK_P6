---
phase: 47-dlq-consolidation-at-least-once-semantics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/TypedResultConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs
autonomous: true
requirements: [RESIL-02, RESIL-03]

must_haves:
  truths:
    - "Delivering the SAME StepCompleted twice into ONE StepCompletedConsumer with ONE dispatcher fires the advancement effect twice (Calls.Count == 2) — no dedup collapse, no throw, no lost branch."
    - "Delivering the SAME EntryStepDispatch/KeeperReinject twice into the recovery harness reproduces its effect twice — no collapse."
    - "The existing RecoveryDeadLetterFacts.DataGone_reinject_faults_and_routes_to_dead_letter fact is discoverable under --filter-trait \"Phase=47\" (R2 cited, not re-tested)."
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/TypedResultConsumerFacts.cs"
      provides: "StepCompleted duplicate-delivery no-collapse fact (R3)"
      contains: "Assert.Equal(2"
    - path: "tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs"
      provides: "EntryStepDispatch/KeeperReinject duplicate-delivery fact (R3) + Phase-47 trait on the data-gone fact (R2)"
      contains: "[Trait(\"Phase\", \"47\")]"
  key_links:
    - from: "duplicate StepCompleted fact"
      to: "StepCompletedConsumer + RecordingDispatcher"
      via: "double consumer.Consume of the SAME message into ONE dispatcher"
      pattern: "Assert\\.Equal\\(2"
    - from: "RecoveryDeadLetterFacts.DataGone_reinject_faults_and_routes_to_dead_letter"
      to: "Phase=47 trait filter"
      via: "second [Trait(\"Phase\",\"47\")] alongside the existing Phase-46 trait"
      pattern: "Trait\\(\"Phase\", \"47\"\\)"
---

<objective>
Prove the at-least-once / no-collapse invariant (RESIL-03) with duplicate-delivery facts, and make the already-green REINJECT-data-gone proof (RESIL-02 / R2) discoverable under the Phase-47 trait so the audit row resolves.

Purpose: `TypedResultConsumerFacts.Injected_StepCompleted_indistinguishable_from_direct` proves indistinguishability (two dispatchers, one Consume each) — it does NOT deliver the SAME message twice nor assert no-collapse. R2 is ALREADY FULLY PROVEN by `RecoveryDeadLetterFacts.DataGone_reinject_faults_and_routes_to_dead_letter` — it must NOT be re-tested, only re-tagged + cited. This is verification only; NO production code change is expected (gap-fix only if a fact surfaces a real defect, surfaced explicitly).
Output: A StepCompleted duplicate fact + an EntryStepDispatch/KeeperReinject duplicate fact (both new, `[Trait("Phase","47")]`), and a second `[Trait("Phase","47")]` added alongside the existing Phase-46 trait on the data-gone fact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-SPEC.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-RESEARCH.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-VALIDATION.md

<interfaces>
<!-- Extracted from the repo this session. Executor uses these directly — no exploration needed. -->

StepCompleted duplicate seam (TypedResultConsumerFacts.cs):
  new StepCompletedConsumer(WorkflowL1Store store, StepAdvancement advancement, IStepDispatcher dispatcher,
                            <metrics> OrchestratorTestStubs.Metrics(), NullLogger<StepCompleted>.Instance)
  consumer.Consume(OrchestratorTestStubs.Context(new StepCompleted(workflowId, stepId, processorId)
                   { CorrelationId = c, ExecutionId = e, EntryId = id }, ct))
  RecordingDispatcher (private sealed in the file): exposes `List<Call> Calls`; DispatchAsync records each call.
  SeedWorkflow(WorkflowL1Store store, Guid workflowId, IReadOnlyDictionary<Guid, StepProjection> steps)  // existing helper
  Step(int entryCondition, Guid processorId, string payload, params Guid[] nextStepIds)                   // existing helper
  StepProjection(EntryCondition, ProcessorId, Payload, NextStepIds); StepOutcome.Completed == the gate int.

EntryStepDispatch / KeeperReinject duplicate seam (RecoveryDeadLetterFacts.cs):
  private static ServiceProvider BuildHarness(int retryLimit)   // ReinjectConsumer + ConfigureError rig + EmptyMux()/OpenGate()
  KeeperReinject(Guid, Guid, Guid) { CorrelationId, ExecutionId, EntryId, Payload }   // the message published twice
  harness.Bus.Publish(msg, ct); harness.Consumed.Any<KeeperReinject>(...); harness.Consumed.Any<ConsolidatedFault>(ct)
  (RecoveryTestKit.CapturingSendProvider records boxed EntryStepDispatch/StepCompleted sends with endpoint URI — for a
   consumer-level double-Consume variant if the harness double-publish shape is awkward; see RecoveryTestKit.cs.)

R2 re-tag target (RecoveryDeadLetterFacts.cs:88-89):
  [Fact]
  [Trait("Phase", "46")]                       // <- ADD [Trait("Phase", "47")] alongside (xunit.v3 allows multiple)
  public async Task DataGone_reinject_faults_and_routes_to_dead_letter()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StepCompleted duplicate-delivery no-collapse fact (R3)</name>
  <files>tests/BaseApi.Tests/Orchestrator/TypedResultConsumerFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/TypedResultConsumerFacts.cs (the Injected_StepCompleted_indistinguishable_from_direct fact at line 172 — the WRONG shape to copy: it uses TWO dispatchers + one Consume each; the SeedWorkflow/Step helpers at lines 29-40; the RecordingDispatcher at line 235; OrchestratorTestStubs.Context/Metrics usage)
    - 47-RESEARCH.md "Common Pitfalls" Pitfall 5 (must use ONE dispatcher + double-Consume of the SAME message; assert Count == 2, NOT two single-call dispatchers) AND the "Duplicate-delivery no-collapse" code example
  </read_first>
  <behavior>
    - The SAME StepCompleted (identical ids) delivered twice into ONE StepCompletedConsumer with ONE RecordingDispatcher produces `dispatcher.Calls.Count == 2` (no dedup collapse to 1).
    - Neither Consume throws; no branch is lost (both deliveries advance the same matched successor).
  </behavior>
  <action>
    Add ONE new `[Fact]` named `Duplicate_StepCompleted_reproduces_effect_no_collapse` to `TypedResultConsumerFacts`, carrying `[Trait("Phase","47")]`.

    Seed a single-successor L1 fixture with `SeedWorkflow`/`Step` (a Completed-gated successor — reuse the existing helper shape from `Injected_StepCompleted_indistinguishable_from_direct`). Construct ONE `StepCompleted` with fixed ids (CorrelationId/ExecutionId/EntryId). Construct ONE `StepCompletedConsumer` with ONE `RecordingDispatcher` (the existing private sealed RecordingDispatcher). Call `consumer.Consume(OrchestratorTestStubs.Context(msg, ct))` TWICE with the SAME `msg` (LANDMINE / Pitfall 5 — same message, one dispatcher).

    Assert: `Assert.Equal(2, dispatcher.Calls.Count)` (no collapse), no exception thrown (the double Consume completes), and both calls advanced the SAME successor (assert `Calls[0]` and `Calls[1]` carry the same matched StepId/ProcessorId/EntryId — no lost branch). Do NOT assert ExecutionId equality between the two calls (it is regenerated per dispatch — see the existing fact's note at line 217).

    Do NOT modify `Injected_StepCompleted_indistinguishable_from_direct` or any other existing fact. Do NOT copy its two-dispatcher shape.
  </action>
  <verify>
    <automated>dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*Duplicate_StepCompleted*"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*Duplicate_StepCompleted*"` exits 0 with the new fact green.
    - The fact uses ONE dispatcher and calls `Consume` exactly twice on the SAME message instance (grep finds two `.Consume(` calls in the fact body and exactly one `new RecordingDispatcher()`).
    - The fact asserts `Assert.Equal(2, dispatcher.Calls.Count)` (grep finds `Assert.Equal(2`).
    - The fact carries `[Trait("Phase","47")]`.
    - `dotnet build SK_P.sln` is 0/0.
  </acceptance_criteria>
  <done>A StepCompleted duplicate-delivery fact proves no-collapse (Count==2, no throw, no lost branch) via ONE dispatcher + double-Consume, Phase-47-tagged, green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EntryStepDispatch/KeeperReinject duplicate fact + R2 re-tag</name>
  <files>tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs (the BuildHarness rig at line 54; the DataGone fact at lines 87-113 — the re-tag target; the KeeperReinject publish + Consumed.Any pattern)
    - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs (CapturingSendProvider records boxed EntryStepDispatch/StepCompleted sends — the fallback seam for a consumer-level double-delivery if the harness double-publish is awkward)
    - 47-RESEARCH.md Open Question 1 (DispatchTestKit does NOT exist; the EntryStepDispatch duplicate fact reuses the RecoveryDeadLetterFacts harness and publishes the same message twice) AND Open Question 2 (ADD a second [Trait("Phase","47")] alongside the existing Phase-46 trait — xunit.v3 supports multiple Trait attributes)
  </read_first>
  <behavior>
    - The SAME recovery message (KeeperReinject, the available EntryStepDispatch-family seam) published twice into the recovery harness reproduces its effect twice — no collapse, no throw.
    - The existing data-gone fact becomes discoverable under --filter-trait "Phase=47" without behavioral change (R2 cited, not re-tested).
  </behavior>
  <action>
    PART A — duplicate fact: Add ONE new `[Fact]` named `Duplicate_Reinject_reproduces_effect_no_collapse` to `RecoveryDeadLetterFacts`, carrying `[Trait("Phase","47")]`. Reuse the existing `BuildHarness` rig. Publish the SAME `KeeperReinject` message TWICE (`harness.Bus.Publish(msg, ct)` twice with the same instance — the EntryStepDispatch-family duplicate seam per Open Question 1; DispatchTestKit does not exist). Assert the effect reproduces and is NOT collapsed: e.g. the harness `Consumed` count for `KeeperReinject` is 2 (use `await harness.Consumed.SelectAsync<KeeperReinject>(ct)` count, or assert the consolidated-move effect occurs for both — choose the assertion the rig exposes; the goal is to prove the second identical delivery is processed, not deduped) and no consumer throws. If asserting an exact count via the harness is awkward, the documented fallback is the consumer-level double-`Consume` using `RecoveryTestKit.CapturingSendProvider` and asserting `Sent.Count == 2`. Reference `ConsolidatedErrorTransportFilter.Dlq1` if asserting the sink (never the literal).

    PART B — R2 re-tag: On the EXISTING `DataGone_reinject_faults_and_routes_to_dead_letter` fact (line 88-89), ADD a second attribute `[Trait("Phase", "47")]` directly alongside the existing `[Trait("Phase", "46")]` (do NOT remove the 46 trait; do NOT change the fact body — it already asserts BOTH `RecoveryDataGoneException` AND `Consumed.Any<ConsolidatedFault>()`). This makes R2 discoverable under the Phase-47 trait for the audit row — no new test, no behavior change.

    Do NOT add production source change. If the duplicate fact reveals the recovery path DOES collapse/dedup a redelivery (a real RESIL-03 defect), STOP and surface it as a finding in the SUMMARY (do NOT mask it).
  </action>
  <verify>
    <automated>dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"` now includes BOTH the new `Duplicate_Reinject_reproduces_effect_no_collapse` fact AND the existing `DataGone_reinject_faults_and_routes_to_dead_letter` fact (the re-tag worked), all green.
    - `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*DataGone_reinject*"` still passes (R2 unchanged).
    - The DataGone fact carries BOTH `[Trait("Phase", "46")]` and `[Trait("Phase", "47")]` (grep finds both on the fact); its body is unchanged.
    - The new duplicate fact publishes/consumes the SAME message twice and asserts the effect reproduces (count 2 / no collapse), no throw.
    - `dotnet build SK_P.sln` is 0/0.
  </acceptance_criteria>
  <done>An EntryStepDispatch/KeeperReinject duplicate fact proves no-collapse, and the data-gone fact carries a Phase-47 trait alongside Phase-46 (R2 cited, not re-tested). Both green, 0/0 build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — hermetic test only) | No new runtime input path; no production code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Tampering (weak no-collapse proof) | Duplicate fact accidentally using two dispatchers / two messages | mitigate | Acceptance criteria pin ONE dispatcher + double-Consume of the SAME instance + Assert.Equal(2, …); the indistinguishability anti-pattern (two dispatchers) is explicitly forbidden in the action. |
| T-47-05 | Repudiation (R2 row unprovable) | Re-tag removing or replacing the Phase-46 trait | accept | Action requires ADDING a second trait, not replacing; both traits asserted present. Low risk — single-line additive edit, body untouched. |
| T-47-06 | Tampering (hidden dedup defect) | Recovery path actually collapsing a redelivery | mitigate | If the duplicate fact reveals real dedup, the executor STOPS and surfaces a finding rather than weakening the assertion — protects the RESIL-03 invariant. |
</threat_model>

<verification>
- `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-trait "Phase=47"` — all Phase-47 facts (incl. the re-tagged DataGone) green.
- `dotnet run --project tests/BaseApi.Tests -c Debug -- --filter-method "*Duplicate_StepCompleted*"` and `--filter-method "*DataGone_reinject*"` — both green.
- `dotnet build SK_P.sln` — 0 warnings / 0 errors.
- Full hermetic suite per VALIDATION.md (the 2 pre-existing broker-dependent E2E failures are NOT regressions).
</verification>

<success_criteria>
- StepCompleted duplicate fact: ONE dispatcher, double-Consume of the SAME message, `Assert.Equal(2, dispatcher.Calls.Count)`, no throw, no lost branch — Phase-47-tagged, green.
- EntryStepDispatch/KeeperReinject duplicate fact: SAME message twice, effect reproduced (no collapse), no throw — Phase-47-tagged, green.
- DataGone fact carries BOTH Phase-46 and Phase-47 traits; body unchanged (R2 cited, not re-tested).
- No production source change (gap-fix only if a fact surfaces a real defect — then surfaced explicitly).
- `dotnet build SK_P.sln` 0/0.
</success_criteria>

<output>
After completion, create `.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-02-SUMMARY.md` recording: the new fact names + files, confirmation the no-collapse shape is ONE-dispatcher/double-Consume (not the two-dispatcher indistinguishability shape), confirmation the DataGone re-tag is additive (both traits present, body untouched), and any surfaced finding (or "none — verify-only, no production change").
</output>
