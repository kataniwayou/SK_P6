---
phase: 43-message-contracts-l2-key-reshape
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/Keeper/Recovery/KeeperRecoveryHandler.cs
  - src/Keeper/Recovery/L2ProbeRecovery.cs
  - src/Keeper/Consumers/FaultExecutionResultConsumer.cs
  - src/Keeper/Consumers/FaultExecutionResultConsumerDefinition.cs
  - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs
  - src/Keeper/Program.cs
autonomous: true
requirements: [MSG-01, MSG-02]
user_setup: []

must_haves:
  truths:
    - "The reactive Keeper recovery FEATURE remains present and compiling — KeeperRecoveryHandler.cs and L2ProbeRecovery.cs are RETARGETED (not deleted), and a reactive fault consumer survives bound to Fault<EntryStepDispatch> (D-14)"
    - "KeeperRecoveryHandler no longer reads inner.H — it derives a local key from the four-tuple via L2ProjectionKeys.CompositeBackup; L2ProbeRecovery uses ExecutionData(Guid)"
    - "The Fault<ExecutionResult> consumer is neutralized (ExecutionResult no longer exists) WITHOUT deleting the reactive recovery feature wholesale (that is RETIRE-03 = Phase 47/48)"
  artifacts:
    - path: "src/Keeper/Recovery/KeeperRecoveryHandler.cs"
      provides: "the reactive recovery handler, retargeted off inner.H to a 4-tuple-derived local key (DARK but compiling)"
      contains: "CompositeBackup"
    - path: "src/Keeper/Recovery/L2ProbeRecovery.cs"
      provides: "the L2 probe-recovery loop, retyped to a Guid-aware ExecutionData lookup"
  key_links:
    - from: "src/Keeper/Recovery/KeeperRecoveryHandler.cs"
      to: "L2ProjectionKeys.CompositeBackup"
      via: "local identity key derived from corr/wf/proc/exec replaces inner.H"
      pattern: "CompositeBackup"
    - from: "src/Keeper/Program.cs"
      to: "Fault<EntryStepDispatch> consumer"
      via: "the surviving reactive consumer registration; the Fault<ExecutionResult> registration removed"
      pattern: "AddConsumer"
---

<objective>
Keep the reactive Keeper recovery path DARK-but-compiling (D-14, user-authorized): rebind `KeeperRecoveryHandler<T>` off the removed `IExecutionCorrelated.H` to a local four-tuple-derived key; retype `L2ProbeRecovery` to the Guid-aware `ExecutionData(Guid)`; neutralize the `Fault<ExecutionResult>` consumer (its message type no longer exists) while RETAINING the reactive fault-recovery FEATURE bound to `Fault<EntryStepDispatch>`. The reactive recovery path must remain present and compiling — its real retirement (RETIRE-03) is Phases 47/48, NOT here.

Purpose: Plan 02 removed `H` from `IExecutionCorrelated`, deleted `ExecutionResult` (so `Fault<ExecutionResult>` can no longer resolve), and removed `ExecutionData(string)`. The reactive Keeper path structurally depends on all three and would otherwise break the build — but D-14 forbids deleting the feature. This plan applies the minimal compile-preserving retarget so the diff does NOT show the reactive recovery path vanishing.

Output: KeeperRecoveryHandler + L2ProbeRecovery retargeted and compiling; a surviving reactive consumer on `Fault<EntryStepDispatch>`; the `Fault<ExecutionResult>` registration removed. No behavior promised — the path goes dormant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-message-contracts-l2-key-reshape/43-CONTEXT.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-RESEARCH.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-PATTERNS.md

<interfaces>
<!-- The reshaped contracts (Plan 02) the dark path must compile against. -->

Messaging.Contracts (post Plan 02):
  IExecutionCorrelated : ICorrelated  // NO H; Guid EntryId
  EntryStepDispatch : IExecutionCorrelated  // SURVIVES (reactive consumer rebinds to Fault<EntryStepDispatch>)
  ExecutionResult  // DELETED -> Fault<ExecutionResult> no longer resolves
  L2ProjectionKeys.CompositeBackup(Guid corr, Guid wf, Guid proc, Guid exec)  // the local-key source
  L2ProjectionKeys.ExecutionData(Guid entryId)  // sole overload (no string overload)
  L2ProjectionKeys.KeeperProbe(string h)            // KEPT (string-keyed) — caller passes localKey
  L2ProjectionKeys.KeeperRecoverAttempts(string h)  // KEPT (string-keyed) — caller passes localKey
  PauseWorkflow(Guid WorkflowId, string H)   // carries its OWN string H positional (NOT the interface H)
  ResumeWorkflow(..., string H)              // same — independent of IExecutionCorrelated

Keeper (existing, this plan edits):
  KeeperRecoveryHandler<T> where T : class, IExecutionCorrelated  // reads inner.H (lines 76,85,92,98,106,148)
  L2ProbeRecovery.RunAsync(string entryId, string h, string procId, CancellationToken)  // line 24,37
  FaultEntryStepDispatchConsumer : IConsumer<Fault<EntryStepDispatch>>   // SURVIVES (type still exists)
  FaultExecutionResultConsumer : IConsumer<Fault<ExecutionResult>>       // breaks — message type gone
</interfaces>

<dark_path_invariant>
D-14 DIFF GUARD (load-bearing): the reactive recovery FEATURE must remain present and compiling.
- KeeperRecoveryHandler.cs and L2ProbeRecovery.cs files STAY (retargeted, not deleted).
- A reactive fault-recovery consumer STAYS, bound to Fault<EntryStepDispatch>.
- NO D-row in Phase 43 authorizes removing the reactive recovery path — that is RETIRE-03 (Phase 47/48).
- The diff must NOT show KeeperRecoveryHandler.cs / the reactive consumer feature disappearing.
- "Neutralizing the Fault<ExecutionResult> consumer" means: remove its registration and retarget/empty its now-unresolvable generic — the surviving Fault<EntryStepDispatch> consumer carries the feature. Per 43-PATTERNS.md §Dark Path, dropping the Fault<ExecutionResult> consumer file is acceptable ONLY because the Fault<EntryStepDispatch> consumer preserves the feature; if in doubt, neutralize the body and keep the file rather than delete it.
</dark_path_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget KeeperRecoveryHandler off inner.H + L2ProbeRecovery to ExecutionData(Guid)</name>
  <read_first>
    - src/Keeper/Recovery/KeeperRecoveryHandler.cs (all inner.H / inner.EntryId / KeeperRecoverAttempts(inner.H) / new PauseWorkflow(..., inner.H) sites — lines 69,76,85,92,98,106,148)
    - src/Keeper/Recovery/L2ProbeRecovery.cs (RunAsync(string entryId,...) + ExecutionData(entryId) — lines 24,37)
    - src/Messaging.Contracts/PauseWorkflow.cs, ResumeWorkflow.cs (confirm they carry their OWN string H positional, independent of the interface)
    - 43-PATTERNS.md section "Dark Path (D-14)" (KeeperRecoveryHandler local-key snippet + L2ProbeRecovery retype)
    - 43-CONTEXT.md D-14 (every bullet) + 43-RESEARCH.md Pitfall 3 (do NOT silently delete the reactive path)
  </read_first>
  <action>
1. EDIT src/Keeper/Recovery/KeeperRecoveryHandler.cs (D-14) — keep the `where T : class, IExecutionCorrelated` bound (the interface still exists) but STOP reading `.H`:
   - Wherever it read `inner.H`, derive a LOCAL identity string from the four-tuple (this is exactly the new CompositeBackup key shape — reuse it, do NOT re-introduce wire H):
     ```csharp
     var localKey = L2ProjectionKeys.CompositeBackup(
         inner.CorrelationId, inner.WorkflowId, inner.ProcessorId, inner.ExecutionId);
     ```
   - Line ~92/148 `new PauseWorkflow(inner.WorkflowId, inner.H)` / `new ResumeWorkflow(..., inner.H)` — PauseWorkflow/ResumeWorkflow keep their OWN `string H` positional, so pass `localKey` there (NOT a wire H — it is just a string slot).
   - Line ~106 `KeeperRecoverAttempts(inner.H)` — pass `localKey` (the `(string)` builder survives).
   - Line ~98 `recovery.RunAsync(inner.EntryId, inner.H, ...)` — `inner.EntryId` is now a `Guid`; pass it as the Guid arg per the retyped L2ProbeRecovery below; pass `localKey` for the `h` slot (or drop the slot if the retype removes it — keep changes minimal and consistent with Task-1 step 2).
   No behavior is promised — the handler goes dormant. Do NOT delete the file.

2. EDIT src/Keeper/Recovery/L2ProbeRecovery.cs (D-14): retype the `entryId` parameter `string` -> `Guid` and call `L2ProjectionKeys.ExecutionData(entryId)` (the surviving Guid overload). `KeeperProbe(h)` stays `(string)`-keyed (the caller passes `localKey`). Keep the bounded probe-loop body otherwise unchanged.
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj</automated>
  </verify>
  <acceptance_criteria>
    - src/Keeper/Recovery/KeeperRecoveryHandler.cs STILL EXISTS and contains `L2ProjectionKeys.CompositeBackup(`; grep for `inner.H` returns NO match.
    - src/Keeper/Recovery/L2ProbeRecovery.cs STILL EXISTS; the RunAsync signature contains `Guid entryId` (not `string entryId`); grep for `ExecutionData(` matches and resolves to the Guid overload.
    - KeeperRecoveryHandler.cs still declares the generic handler bound `where T : ... IExecutionCorrelated` (the feature is retained, not deleted).
    - `dotnet build src/Keeper/Keeper.csproj` errors ONLY (if at all) on the Fault<ExecutionResult> consumer addressed in Task 2 — the handler + probe compile.
  </acceptance_criteria>
  <done>KeeperRecoveryHandler derives a local 4-tuple key instead of inner.H; L2ProbeRecovery is Guid-typed; both files remain present and compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize the Fault&lt;ExecutionResult&gt; consumer; retain the reactive feature on Fault&lt;EntryStepDispatch&gt;</name>
  <read_first>
    - src/Keeper/Consumers/FaultExecutionResultConsumer.cs + FaultExecutionResultConsumerDefinition.cs (the IConsumer<Fault<ExecutionResult>> that no longer resolves)
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs + its Definition (the SURVIVING reactive consumer on Fault<EntryStepDispatch>)
    - src/Keeper/Program.cs (the AddConsumer registrations — the Fault<ExecutionResult> line to remove)
    - 43-PATTERNS.md section "Dark Path" subsection "FaultExecutionResultConsumer.cs ... — drop the Fault<ExecutionResult> consumer" + the DARK file table
    - 43-CONTEXT.md D-14 (the diff guard) + the dark_path_invariant block above
  </read_first>
  <action>
The `Fault<ExecutionResult>` consumer cannot resolve because `ExecutionResult` is deleted. Per D-14, the reactive recovery FEATURE must survive — it survives via the `Fault<EntryStepDispatch>` consumer (`EntryStepDispatch` still exists). Apply the minimal compile-preserving move:

1. REMOVE the `Fault<ExecutionResult>` consumer registration from src/Keeper/Program.cs (the `x.AddConsumer<FaultExecutionResultConsumer, FaultExecutionResultConsumerDefinition>();` line).

2. For src/Keeper/Consumers/FaultExecutionResultConsumer.cs + FaultExecutionResultConsumerDefinition.cs — because `Fault<ExecutionResult>` is unresolvable, EITHER:
   - (preferred per 43-PATTERNS.md §Dark Path) delete this pair ONLY because the `Fault<EntryStepDispatch>` consumer preserves the reactive feature; OR
   - if any ambiguity remains, neutralize the consumer body (retarget its generic to a surviving type or empty it) and keep the file.
   Pick the option that keeps `dotnet build` green while PRESERVING the reactive recovery feature. Whichever is chosen, the `FaultEntryStepDispatchConsumer` (and the `KeeperRecoveryHandler` it delegates to) MUST remain present and registered — the diff must not show the reactive recovery feature disappearing wholesale (that is RETIRE-03, Phase 47/48).

3. CONFIRM src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs still binds `IConsumer<Fault<EntryStepDispatch>>` and remains registered in Program.cs — this is the surviving reactive consumer that carries the feature.

Do NOT remove KeeperQueues.FaultRecovery / KeeperQueues.DeadLetter, the keeper-fault-recovery binding, or KeeperRecoveryHandler — those retirements are Phases 47/48.
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj</automated>
  </verify>
  <acceptance_criteria>
    - src/Keeper/Program.cs contains NO `Fault<ExecutionResult>` / `FaultExecutionResultConsumer` registration; grep returns no match.
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs EXISTS, binds `Fault<EntryStepDispatch>`, and is registered in Program.cs (the reactive feature survives).
    - src/Keeper/Recovery/KeeperRecoveryHandler.cs still EXISTS and is referenced by the surviving reactive consumer path (the feature is retained, not retired).
    - No reference to the deleted `ExecutionResult` type remains anywhere under src/Keeper/.
    - `dotnet build src/Keeper/Keeper.csproj` exits 0.
  </acceptance_criteria>
  <done>The unresolvable Fault&lt;ExecutionResult&gt; consumer is neutralized; the reactive recovery feature survives on Fault&lt;EntryStepDispatch&gt; via the retained KeeperRecoveryHandler; Keeper builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keeper <- Fault<EntryStepDispatch> (bus event) | The surviving reactive consumer receives fault envelopes. Path is DARK in Phase 43 (no behavior promised). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-09 | Repudiation / dead code (dormant path) | KeeperRecoveryHandler going DARK but registered | accept | The path is intentionally dormant per D-14; it compiles but is not exercised in v4 until its retirement (Phase 47/48). Keeping it registered is a deliberate, user-authorized milestone-sequencing decision, not a vulnerability — no new external input is processed differently. |
| T-43-10 | Tampering (identity-key derivation) | localKey from CompositeBackup replacing inner.H | accept | The local key is derived deterministically from the same four ids already on the (trusted, in-process) message; it is used only for an internal probe-attempts/pause-key slot, not for any auth or dedup decision. No security property depends on it in the dark path. |
</threat_model>

<verification>
- `dotnet build src/Keeper/Keeper.csproj` exits 0.
- KeeperRecoveryHandler.cs and L2ProbeRecovery.cs still exist and are retargeted (CompositeBackup local key; Guid ExecutionData).
- A reactive consumer on Fault<EntryStepDispatch> survives and is registered; the Fault<ExecutionResult> registration is gone.
- No `inner.H`, `ExecutionResult`, or `ExecutionData(string)` references remain under src/Keeper/.
- Diff guard: the reactive recovery feature is present and compiling (not retired).
</verification>

<success_criteria>
- The reactive Keeper recovery feature is DARK-but-compiling (D-14): handler + probe retargeted, not deleted; reactive consumer survives on Fault<EntryStepDispatch>.
- KeeperRecoveryHandler derives a local key from CompositeBackup instead of inner.H.
- L2ProbeRecovery uses ExecutionData(Guid).
- The unresolvable Fault<ExecutionResult> consumer is neutralized without retiring the feature.
- Keeper builds clean.
</success_criteria>

<output>
After completion, create `.planning/phases/43-message-contracts-l2-key-reshape/43-04-SUMMARY.md`
</output>
