---
phase: 43-message-contracts-l2-key-reshape
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Contracts/StepResultContractTests.cs
  - tests/BaseApi.Tests/Contracts/SourceStepTests.cs
  - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs
  - tests/BaseApi.Tests/Keeper/BackupOptionsBoundTests.cs
  - tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs
  - tests/BaseApi.Tests/Processor/EffectFirstDedupFacts.cs
  - tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs
  - tests/BaseApi.Tests/Processor/CheckAndDropFacts.cs
  - tests/BaseApi.Tests/Orchestrator/ManifestFanoutFacts.cs
  - tests/BaseApi.Tests/Orchestrator/MergeCollapseFacts.cs
  - tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs
  - tests/BaseApi.Tests/Contracts/HashHelperGoldenFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchOutputWriteFacts.cs
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03]
user_setup: []

must_haves:
  truths:
    - "Golden/contract tests for the four Step* records, the SourceStep predicate, the five Keeper records, BackupOptions, and the CompositeBackup key exist and express SC-1..SC-4 BEFORE the production symbols exist (RED proofs)"
    - "Every test asserting the RETIRE-01/02 machinery (H, flag[H], CAS, manifest, content-addressing) is deleted so a partial reshape cannot hide behind stale green tests"
  artifacts:
    - path: "tests/BaseApi.Tests/Contracts/StepResultContractTests.cs"
      provides: "SC-1/SC-2 proofs for the four Step* records (six ids, no H, IStepResult, Guid.Empty defaults, diagnostic-field placement)"
    - path: "tests/BaseApi.Tests/Contracts/SourceStepTests.cs"
      provides: "SC-2 single-predicate proof"
    - path: "tests/BaseApi.Tests/Contracts/KeeperContractTests.cs"
      provides: "SC-3 five-record + IKeeperRecoverable 4-tuple proof"
    - path: "tests/BaseApi.Tests/Keeper/BackupOptionsBoundTests.cs"
      provides: "D-10 default TtlDays==2 proof"
  key_links:
    - from: "tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs"
      to: "L2ProjectionKeys.CompositeBackup / ExecutionData(Guid)"
      via: "Assert.Equal golden string pin"
      pattern: "CompositeBackup|skp:data:"
---

<objective>
Write the Wave-0 Nyquist proofs FIRST: golden/contract tests that pin SC-1..SC-4 (the four `Step*` records, the `SourceStep` source-sentinel predicate, the five Keeper-state contracts, `BackupOptions`, and the two L2 key strings) BEFORE the production symbols they reference exist — and DELETE the ~8 test files that assert the RETIRE-01/02 machinery (`H`/`flag[H]`/CAS/manifest/content-addressing) which Phase 43 removes.

Purpose: These tests are the executable definition of "done" for the contract reshape. Because Wave 3 deletes machinery covered by stale tests, the predictable failure mode of this phase is "source compiles but suite red." Writing the proofs first and deleting the obsolete tests up front converts that hidden failure into an explicit, sampled gate.

Output: New RED contract tests (they reference `StepCompleted`/`SourceStep`/`KeeperUpdate`/`BackupOptions`/`CompositeBackup` which Plan 02 creates) plus the obsolete RETIRE test files removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-message-contracts-l2-key-reshape/43-CONTEXT.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-RESEARCH.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-PATTERNS.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-VALIDATION.md

<interfaces>
<!-- Symbols these tests reference. They DO NOT EXIST YET — Plan 02 creates them. -->
<!-- The tests are deliberately RED until Plan 02 lands; that is the Nyquist proof. -->

Will exist after Plan 02 (Messaging.Contracts namespace):
  public interface IStepResult : IExecutionCorrelated { }
  public sealed record StepCompleted(Guid WorkflowId, Guid StepId, Guid ProcessorId) : IStepResult
      { Guid CorrelationId init; Guid ExecutionId init; Guid EntryId init; }        // EntryId = REAL key
  public sealed record StepFailed(Guid WorkflowId, Guid StepId, Guid ProcessorId) : IStepResult
      { Guid CorrelationId init; Guid ExecutionId init; Guid EntryId init = Guid.Empty; string? ErrorMessage init; }
  public sealed record StepCancelled(...) : IStepResult { ... EntryId init = Guid.Empty; string? CancellationMessage init; }
  public sealed record StepProcessing(...) : IStepResult { ... EntryId init = Guid.Empty; }   // no diagnostic field
  public static class SourceStep { public static bool IsSource(Guid entryId); }
  public interface IKeeperRecoverable { Guid CorrelationId; Guid WorkflowId; Guid ProcessorId; Guid ExecutionId; }
  public sealed record KeeperUpdate(Guid WorkflowId, Guid StepId, Guid ProcessorId) : IKeeperRecoverable
      { Guid CorrelationId init; Guid ExecutionId init; string ValidatedData init; }
  public sealed record KeeperReinject(...) : IKeeperRecoverable { ... Guid EntryId init; }
  public sealed record KeeperInject(...)   : IKeeperRecoverable { 5-id base, no extra }
  public sealed record KeeperDelete(...)   : IKeeperRecoverable { ... Guid EntryId init; }
  public sealed record KeeperCleanup(...)  : IKeeperRecoverable { 5-id base, no extra }
  L2ProjectionKeys.CompositeBackup(Guid,Guid,Guid,Guid) returns "skp:{corr:D}:{wf:D}:{proc:D}:{exec:D}"
  L2ProjectionKeys.ExecutionData(Guid) returns "skp:data:{entryId:D}"   // sole overload after Plan 02

Will exist after Plan 02 (Keeper namespace):
  public sealed class BackupOptions { public int TtlDays { get; set; } = 2; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the four contract/predicate/keeper/options golden tests (RED)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/ExecutionResultContractTests.cs (EXACT analog — copy its STJ round-trip structure, absent-property reflection, interface assert; 43-PATTERNS.md golden-test section)
    - tests/BaseApi.Tests/Keeper/ProbeOptionsBoundTests.cs (EXACT analog for BackupOptionsBoundTests — instantiate-defaults-and-assert-invariant)
    - 43-PATTERNS.md section "Golden Test Pattern Assignments" (copy-paste-ready assertions)
    - 43-RESEARCH.md section "Code Examples" (the CompositeBackup golden, the Step* shape-pin, the SourceStep proof) and Pitfall 1 (zero-GUID serialization)
    - 43-CONTEXT.md D-06/D-06a/D-06b/D-07/D-11/D-12/D-10 (the exact id-sets + diagnostic-field placement)
  </read_first>
  <action>
Create FOUR new test files under tests/BaseApi.Tests/ (xUnit v3 / MTP — same framework as the analogs, NO new packages). They reference symbols that do not yet exist (Plan 02 creates them); they are deliberately RED. Use plain [Fact] reflection + STJ round-trip, no harness/broker/Redis.

(a) tests/BaseApi.Tests/Contracts/StepResultContractTests.cs — SC-1/SC-2. For EACH of StepCompleted, StepFailed, StepCancelled, StepProcessing:
   - Assert.Null(typeof(StepCompleted).GetProperty("H")) — H absent (SC-1).
   - The property-name set contains exactly the six ids { CorrelationId, WorkflowId, StepId, ProcessorId, ExecutionId, EntryId } (plus the one diagnostic field where applicable).
   - Assert.True(typeof(IStepResult).IsAssignableFrom(typeof(StepCompleted))) and Assert.True(typeof(IExecutionCorrelated).IsAssignableFrom(typeof(StepCompleted))).
   - Per D-06a/SC-2 defaults: Assert.Equal(Guid.Empty, new StepFailed(Guid.NewGuid(), Guid.NewGuid(), Guid.NewGuid()).EntryId); same for StepCancelled, StepProcessing. (StepCompleted has NO Guid.Empty default — its EntryId is the real key; do NOT assert a default for it.)
   - Per D-06b diagnostic placement: StepFailed HAS ErrorMessage and does NOT have CancellationMessage; StepCancelled HAS CancellationMessage and does NOT have ErrorMessage; StepCompleted/StepProcessing have NEITHER (assert both GetProperty return null).
   - Per Pitfall 1: assert default(StepFailed) round-trips its EntryId to the literal "00000000-0000-0000-0000-000000000000" via System.Text.Json.JsonSerializer.Serialize (the sentinel is a PRESENT zero-GUID, not an omitted field). NO [JsonPropertyName], default STJ.

(b) tests/BaseApi.Tests/Contracts/SourceStepTests.cs — SC-2 single predicate:
   - Assert.True(SourceStep.IsSource(Guid.Empty))
   - Assert.False(SourceStep.IsSource(Guid.NewGuid()))

(c) tests/BaseApi.Tests/Contracts/KeeperContractTests.cs — SC-3. For the five records KeeperUpdate/KeeperReinject/KeeperInject/KeeperDelete/KeeperCleanup:
   - Each : IKeeperRecoverable; Assert.True(typeof(IKeeperRecoverable).IsAssignableFrom(typeof(KeeperUpdate))) for all five.
   - IKeeperRecoverable exposes EXACTLY the 4-tuple { CorrelationId, WorkflowId, ProcessorId, ExecutionId } and does NOT expose StepId (assert typeof(IKeeperRecoverable).GetProperty("StepId") is null) — D-12.
   - Each record carries StepId as a property (D-11: all five carry the 5-id base corr/wf/step/proc/exec); assert GetProperty("StepId") is non-null on each record type.
   - KeeperUpdate HAS ValidatedData; KeeperReinject and KeeperDelete HAVE EntryId; KeeperInject and KeeperCleanup do NOT have EntryId (assert null) and do NOT have ValidatedData.

(d) tests/BaseApi.Tests/Keeper/BackupOptionsBoundTests.cs — D-10. Mirror ProbeOptionsBoundTests exactly: Assert.Equal(2, new BackupOptions().TtlDays).

Use [Trait("Phase","43")] on the new fixtures to match the repo trait convention (see L2ProjectionKeysTests [Trait("Phase","22")]).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist: StepResultContractTests.cs, SourceStepTests.cs, KeeperContractTests.cs, BackupOptionsBoundTests.cs.
    - grep (Grep tool) for `GetProperty("H")` in StepResultContractTests.cs matches (H-absent assertion present).
    - grep for `00000000-0000-0000-0000-000000000000` in StepResultContractTests.cs matches (Pitfall-1 zero-GUID serialization pin present).
    - grep for `IsSource(Guid.Empty)` in SourceStepTests.cs matches.
    - grep for `IKeeperRecoverable` in KeeperContractTests.cs matches AND a `GetProperty("StepId")` null-assertion against IKeeperRecoverable is present.
    - grep for `Assert.Equal(2` with `TtlDays` in BackupOptionsBoundTests.cs matches.
    - The test project FAILS to compile with errors naming the not-yet-created symbols (CS0246/CS0103 for StepCompleted/SourceStep/KeeperUpdate/BackupOptions). This RED state is the expected Nyquist proof; it turns green in Plan 02.
  </acceptance_criteria>
  <done>Four RED contract/golden test files exist, each expressing its SC via grep-verifiable assertions; build fails only on the symbols Plan 02 creates.</done>
</task>

<task type="auto">
  <name>Task 2: Extend L2ProjectionKeysTests with the CompositeBackup golden + Guid ExecutionData pin (RED on CompositeBackup)</name>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs (self — the existing ExecutionData golden; add alongside)
    - 43-RESEARCH.md section "Code Examples" (the exact CompositeBackup + ExecutionData golden strings)
    - 43-CONTEXT.md D-08 (skp:data:{entryId:D}, no TTL) + D-09 (skp:{corr:D}:{wf:D}:{proc:D}:{exec:D}, skp-prefixed)
  </read_first>
  <action>
Edit tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs. Add two facts (the CompositeBackup builder does not exist yet — RED until Plan 02):

```csharp
[Fact]
public void CompositeBackup_Produces_Prefix_Four_HyphenatedGuids_Colon_Joined()
{
    var corr = Guid.Parse("11111111-1111-1111-1111-111111111111");
    var wf   = Guid.Parse("22222222-2222-2222-2222-222222222222");
    var proc = Guid.Parse("33333333-3333-3333-3333-333333333333");
    var exec = Guid.Parse("44444444-4444-4444-4444-444444444444");
    Assert.Equal(
        "skp:11111111-1111-1111-1111-111111111111:22222222-2222-2222-2222-222222222222:" +
        "33333333-3333-3333-3333-333333333333:44444444-4444-4444-4444-444444444444",
        L2ProjectionKeys.CompositeBackup(corr, wf, proc, exec));
}

[Fact]
public void ExecutionData_Guid_Overload_Pins_skp_data_HyphenatedGuid()
    => Assert.Equal("skp:data:55555555-5555-5555-5555-555555555555",
        L2ProjectionKeys.ExecutionData(Guid.Parse("55555555-5555-5555-5555-555555555555")));
```

Do NOT add a test for ExecutionData(string) (that overload is removed in Plan 02). If a fact pinning the (string) overload already exists in this file, DELETE that fact (it will not compile after Plan 02).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - grep for `CompositeBackup(` in L2ProjectionKeysTests.cs matches with the exact 4-GUID expected string present.
    - No fact in the file references ExecutionData with a string literal argument any longer.
    - File contains the ExecutionData(Guid.Parse( golden expecting skp:data:55555555-...
  </acceptance_criteria>
  <done>L2ProjectionKeysTests pins both new key strings; the string-overload fact (if any) is removed.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the RETIRE-01/02 machinery test files</name>
  <read_first>
    - 43-RESEARCH.md section "Blast Radius — Test fallout" (the DELETE bucket)
    - 43-VALIDATION.md section "Wave 0 Requirements" (the DELETE list)
    - Each target file header (confirm it asserts H / flag[H] / CAS / manifest / content-addressing before deleting — do NOT delete a file that tests surviving behavior)
  </read_first>
  <action>
DELETE these test files via `git rm` (each asserts RETIRE-01 H/flag[H]/CAS or RETIRE-02 manifest/content-addressing — the machinery Phase 43 removes; they cannot compile or pass against the new shapes):
  - tests/BaseApi.Tests/Processor/EffectFirstDedupFacts.cs
  - tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs
  - tests/BaseApi.Tests/Processor/CheckAndDropFacts.cs
  - tests/BaseApi.Tests/Orchestrator/ManifestFanoutFacts.cs
  - tests/BaseApi.Tests/Orchestrator/MergeCollapseFacts.cs
  - tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs
  - tests/BaseApi.Tests/Contracts/HashHelperGoldenFacts.cs   (tests MessageIdentity, deleted in Plan 02)
  - tests/BaseApi.Tests/Processor/DispatchOutputWriteFacts.cs (asserts the content-addressed write)

Do NOT delete ExecutionResultContractTests.cs or EntryStepDispatchTests.cs (those are RESHAPED in Plan 05, not deleted). Do NOT delete ExecutionLogScopeKeyTests.cs (key strings unchanged — must stay green).
  </action>
  <verify>
    <automated>git status --porcelain tests/BaseApi.Tests/Processor/EffectFirstDedupFacts.cs tests/BaseApi.Tests/Orchestrator/ManifestFanoutFacts.cs tests/BaseApi.Tests/Contracts/HashHelperGoldenFacts.cs tests/BaseApi.Tests/Processor/DispatchOutputWriteFacts.cs</automated>
  </verify>
  <acceptance_criteria>
    - None of the eight listed files exist on disk (Glob for each returns no match).
    - `git status` shows the eight files staged as deleted (status `D`).
    - The four files in the keep-list (ExecutionResultContractTests.cs, EntryStepDispatchTests.cs, ExecutionLogScopeKeyTests.cs, L2ProjectionKeysTests.cs) still exist.
  </acceptance_criteria>
  <done>The eight RETIRE machinery test files are removed; the reshape-bucket and unchanged-key tests remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only adds/removes test files in the hermetic test project. No runtime, no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering (test integrity) | Deleted RETIRE test files | accept | Deletions are git-staged and reviewable; the four RESHAPE/keep tests are explicitly excluded from deletion, and Plan 05's full-suite gate re-proves coverage. No security-relevant behavior in test scaffolding. |
| T-43-02 | Information disclosure | Golden test fixtures embedding GUIDs | accept | GUIDs are synthetic literals (1111…, 5555…), not secrets or PII. No disclosure risk. |
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj` fails ONLY on the not-yet-created symbols (StepCompleted, StepFailed, StepCancelled, StepProcessing, IStepResult, SourceStep, IKeeperRecoverable, KeeperUpdate/Reinject/Inject/Delete/Cleanup, CompositeBackup, BackupOptions). This RED state is expected and is resolved by Plan 02.
- The eight RETIRE machinery test files are gone (staged as deleted in git).
</verification>

<success_criteria>
- Four new RED contract/golden test files express SC-1..SC-4 + D-10 with grep-verifiable assertions.
- L2ProjectionKeysTests pins CompositeBackup + the Guid ExecutionData overload.
- The eight RETIRE-01/02 machinery test files are deleted.
- Build fails only on Plan-02 symbols (no other compile errors).
</success_criteria>

<output>
After completion, create `.planning/phases/43-message-contracts-l2-key-reshape/43-01-SUMMARY.md`
</output>
