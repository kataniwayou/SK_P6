---
phase: 43-message-contracts-l2-key-reshape
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/ExecutionResultContractTests.cs
  - tests/BaseApi.Tests/Orchestrator/EntryStepDispatchTests.cs
  - tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03]
user_setup: []

must_haves:
  truths:
    - "The full hermetic test suite is GREEN against the reshaped contracts — the RESHAPE/VERIFY-ONLY test bucket (ExecutionResultContractTests split, EntryStepDispatchTests, shared stubs/kit) is actioned so the suite is not red despite a compiling source tree"
    - "The phase gate is FULL-SUITE-GREEN, not just dotnet build (the predictable 'compiles but red' failure mode is closed)"
    - "ROADMAP Phase 43/Phase 48 descriptions + the RETIRE-01/02 REQUIREMENTS traceability rows are reconciled to the actual landing phase (D-01/D-02 pulled RETIRE-01/02 into Phase 43) — docs only"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/EntryStepDispatchTests.cs"
      provides: "reshaped dispatch contract test: no H, Guid EntryId"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RETIRE-01/02 traceability rows reflecting Phase 43 (coupled) landing"
  key_links:
    - from: "tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs"
      to: "StepCompleted / EntryStepDispatch (no H)"
      via: "shared harness constructs the new shapes"
      pattern: "StepCompleted|EntryStepDispatch"
---

<objective>
Close the phase to FULL-SUITE-GREEN: reshape the surviving RETIRE-bucket tests (`ExecutionResultContractTests` -> four `Step*` contract tests, `EntryStepDispatchTests` -> no-H + Guid EntryId) and the shared harness stubs/kit that construct `ExecutionResult`/set `H`, then run the full `dotnet test` as the explicit phase gate. Finally, reconcile the now-inaccurate ROADMAP Phase 43/48 descriptions and the RETIRE-01/02 REQUIREMENTS traceability rows (D-01/D-02 moved those teardowns into Phase 43) — a docs update, user-owned.

Purpose: Wave 3 deletes/reshapes machinery covered by ~6 surviving test files and 2 shared harnesses. Source compiling is necessary but NOT sufficient — the predictable failure mode is "compiles but red." Making full-suite-green an explicit gate task (not an afterthought) is the only thing that proves the phase is actually done. The docs reconciliation keeps the landing phase honest before the milestone proceeds.

Output: A green full hermetic suite against the reshaped contracts + reconciled planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-message-contracts-l2-key-reshape/43-CONTEXT.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-RESEARCH.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-PATTERNS.md
@.planning/phases/43-message-contracts-l2-key-reshape/43-VALIDATION.md

<interfaces>
<!-- The reshaped contracts (Plans 02-04) the surviving tests + harness must construct. -->

Messaging.Contracts (post Plan 02):
  EntryStepDispatch(Guid WorkflowId, Guid StepId, Guid ProcessorId, string Payload) : IExecutionCorrelated
    { Guid CorrelationId init; Guid ExecutionId init; Guid EntryId init = Guid.Empty; }   // no H
  sealed record StepCompleted/StepFailed/StepCancelled/StepProcessing : IStepResult       // ExecutionResult GONE
  static class SourceStep { bool IsSource(Guid); }

Orchestrator (post Plan 03):
  ResultConsumer : IConsumer<StepCompleted>   // no IConnectionMultiplexer ctor param
  IStepDispatcher.DispatchAsync(... Guid entryId ...)  // entryId is Guid
</interfaces>

<gate_note>
This is the milestone's stated PHASE GATE: full hermetic suite GREEN (not just build). Per 43-VALIDATION.md §Sampling Rate, "green requires the DELETE/RESHAPE test bucket to be actioned." Plan 01 already deleted the obsolete bucket; this plan reshapes the surviving bucket + the shared stubs, then runs the gate. If the gate is red, fix the reshaped tests/harness — do NOT weaken assertions to force green.
</gate_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape ExecutionResultContractTests -> four Step* tests + EntryStepDispatchTests (no H, Guid EntryId)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/ExecutionResultContractTests.cs (the ExecutionResult shape test to split — its STJ round-trip + reflection structure)
    - tests/BaseApi.Tests/Orchestrator/EntryStepDispatchTests.cs (currently asserts dispatch.EntryId == "" and dispatch.H == "")
    - tests/BaseApi.Tests/Contracts/StepResultContractTests.cs (the Plan-01 Step* coverage — avoid duplicating; this task may FOLD the four-record coverage into StepResultContractTests and DELETE ExecutionResultContractTests instead of splitting, if cleaner)
    - 43-PATTERNS.md section "ExecutionResultContractTests.cs + EntryStepDispatchTests.cs (RESHAPE/DELETE)"
    - 43-CONTEXT.md D-04, D-05, D-06
  </read_first>
  <action>
1. `ExecutionResultContractTests.cs`: the `ExecutionResult` type is gone. EITHER split it into the four `Step*` contract tests OR (cleaner, per 43-PATTERNS.md) DELETE it because Plan-01's `StepResultContractTests.cs` already covers the four records. Choose deletion IF `StepResultContractTests.cs` fully covers the six-id/no-H/round-trip assertions for all four records; otherwise migrate any uncovered assertion into `StepResultContractTests.cs` first, then delete. Do NOT leave a test referencing the deleted `ExecutionResult` type.

2. EDIT tests/BaseApi.Tests/Orchestrator/EntryStepDispatchTests.cs (D-04/D-05): the current `dispatch.EntryId == ""` and `dispatch.H == ""` assertions no longer compile. RESHAPE to:
   - `Assert.Null(typeof(EntryStepDispatch).GetProperty("H"))` (H absent).
   - `Assert.Equal(Guid.Empty, new EntryStepDispatch(Guid.NewGuid(), Guid.NewGuid(), Guid.NewGuid(), "payload").EntryId)` (EntryId is a Guid defaulting to Guid.Empty).
   - Keep any surviving round-trip / id-set assertions, retyped for `Guid EntryId`.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~EntryStepDispatch|FullyQualifiedName~StepResult"</automated>
  </verify>
  <acceptance_criteria>
    - No test file references the deleted `ExecutionResult` type (Grep for `ExecutionResult` across tests/ returns no match in contract tests).
    - EntryStepDispatchTests.cs contains `GetProperty("H")` null-assertion and a `Guid.Empty` EntryId assertion; grep for `dispatch.H` and `EntryId == ""` returns NO match.
    - `dotnet test --filter "FullyQualifiedName~EntryStepDispatch|FullyQualifiedName~StepResult"` exits 0.
  </acceptance_criteria>
  <done>ExecutionResultContractTests is removed/migrated; EntryStepDispatchTests asserts no H + Guid EntryId; both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update shared harness stubs/kit + run the FULL-SUITE-GREEN phase gate</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs (constructs ExecutionResult / sets H — VERIFY-ONLY bucket, 43-RESEARCH.md Test fallout)
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (constructs dispatch with H / string EntryId)
    - any test still constructing `new ResultConsumer(` with an IConnectionMultiplexer (Plan 03 dropped that ctor param — Pitfall 5)
    - 43-VALIDATION.md §Sampling Rate + §Full suite command; 43-RESEARCH.md §"Blast Radius — Test fallout" (VERIFY-ONLY bucket)
  </read_first>
  <action>
1. EDIT tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs and tests/BaseApi.Tests/Processor/DispatchTestKit.cs: replace every `new ExecutionResult(...)` with the matching `Step*` record (typically `StepCompleted`), drop every `H = ...` initializer, and change any `EntryId = "..."` to `EntryId = <Guid>` (or `Guid.Empty`). Update any `new ResultConsumer(...)` construction in the harness to drop the IConnectionMultiplexer argument (Plan 03 removed that ctor param). Update any `DispatchAsync(...)` test call to pass a `Guid` entryId.

2. Run the FULL hermetic suite as the explicit PHASE GATE: `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj`. It MUST be fully green. If anything is red, fix the reshaped test/harness to match the new contracts — do NOT weaken or delete assertions to force green, and do NOT touch production source (Plans 02-04 own that). Confirm `ExecutionLogScopeKeyTests` (unchanged keys) is green and that the Plan-01 contract/key/predicate/options tests are green.

3. Confirm a clean solution build: `dotnet build SK_P.sln -c Release` exits 0 with 0 warnings (the repo's 0-warning Release convention). If a warning surfaces from the reshape (e.g. an unused using left by a deletion), fix it in the owning file.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Grep for `ExecutionResult`, `H = ` (H initializer), and `EntryId = "` across OrchestratorTestStubs.cs + DispatchTestKit.cs returns NO match.
    - No test constructs `ResultConsumer` with an IConnectionMultiplexer argument.
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj` exits 0 — FULL hermetic suite GREEN (the phase gate).
    - `dotnet build SK_P.sln -c Release` exits 0 with 0 warnings.
  </acceptance_criteria>
  <done>The shared harness constructs the new shapes; the full hermetic suite is green and Release builds 0-warning — the explicit phase gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Docs reconciliation — ROADMAP Phase 43/48 + RETIRE-01/02 traceability rows</name>
  <read_first>
    - .planning/ROADMAP.md §"Milestone v4.0.0" Phase 43 table row + "#### Phase 43" / "#### Phase 48" detail blocks
    - .planning/REQUIREMENTS.md §Traceability (the RETIRE-01 / RETIRE-02 rows currently mapped to Phase 48) + the RETIRE section
    - 43-CONTEXT.md §"Deferred Ideas" (the ROADMAP reconciliation item — surface, apply as a docs update) + D-01/D-02
  </read_first>
  <action>
This is a DOCS-ONLY task (no code). D-01/D-02 moved RETIRE-01 (H/flag[H]/CAS) and RETIRE-02 (content-addressing/manifest/N×M fan-out) into Phase 43 (coupled to the field/type change); Phase 48 shrinks to RETIRE-03 + a final remnant sweep. Reconcile the docs to reflect the actual landing phase:

1. .planning/REQUIREMENTS.md Traceability table: change the RETIRE-01 and RETIRE-02 rows from `Phase 48` to reflect that they land (coupled) in Phase 43 — e.g. `Phase 43 (coupled per D-01)`. Leave RETIRE-03 on Phase 48. Keep the coverage footer count consistent (still 31 REQ-IDs; only the phase mapping for two rows changes — note that RETIRE-01/02 now share Phase 43 with MSG-01/02/03, which is the intended coupling, not a double-map).

2. .planning/ROADMAP.md:
   - Phase 43 detail block ("#### Phase 43") + the milestone Phase-table row: amend the Goal/scope wording so it no longer reads as "just vocabulary" — note that RETIRE-01/02 are coupled in here (the H/flag[H]/CAS removal + content-addressing/manifest removal are compile-forced by the field/type reshape, per D-01).
   - Phase 48 detail block ("#### Phase 48") + its Requirements line: note it shrinks to RETIRE-03 (reactive Fault<T> path + keeper-dlq) + final remnant sweep (the RETIRE-01/02 work moved to Phase 43 per D-02). Adjust the Requirements line for Phase 48 from `RETIRE-01, RETIRE-02, RETIRE-03` to `RETIRE-03` (+ note the coupled-forward RETIRE-01/02).

Do NOT change the locked build order (43->44->...->49). Do NOT renumber phases. Keep edits surgical and factual; do not touch any other phase's rows.
  </action>
  <verify>
    <automated>git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md: the RETIRE-01 and RETIRE-02 traceability rows reference Phase 43 (coupled), not Phase 48; RETIRE-03 still references Phase 48.
    - .planning/ROADMAP.md Phase 43 block no longer describes the phase as "just vocabulary" — it notes the coupled RETIRE-01/02 landing.
    - .planning/ROADMAP.md Phase 48 Requirements line reads RETIRE-03 (with a note that RETIRE-01/02 moved to Phase 43); the build order line is unchanged.
    - `git diff` touches ONLY .planning/ROADMAP.md and .planning/REQUIREMENTS.md (no code, no other phase rows).
  </acceptance_criteria>
  <done>The ROADMAP Phase 43/48 descriptions and the RETIRE-01/02 traceability rows reflect the D-01/D-02 coupling; the build order and phase numbering are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Test reshape + docs reconciliation. No runtime, no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-11 | Tampering (gate integrity) | Full-suite-green phase gate | mitigate | The gate runs the unmodified full hermetic suite; the task explicitly forbids weakening assertions to force green. The Plan-01 contract proofs + ExecutionLogScopeKeyTests remain as independent coverage, so a hollowed-out test cannot silently pass the gate. |
| T-43-12 | Repudiation (doc drift) | ROADMAP / REQUIREMENTS reconciliation | accept | Docs-only, git-tracked, surgical diff scoped to two files; no behavioral or security impact. Reconciliation prevents future mis-planning (RETIRE-01/02 double-execution), reducing risk rather than adding it. |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj` exits 0 — full hermetic suite GREEN (the explicit phase gate).
- `dotnet build SK_P.sln -c Release` exits 0 with 0 warnings.
- No test references the deleted `ExecutionResult` type or constructs `H`/string-EntryId; no `ResultConsumer` constructed with IConnectionMultiplexer.
- ROADMAP Phase 43/48 + RETIRE-01/02 traceability rows reconciled (docs-only diff).
</verification>

<success_criteria>
- The RESHAPE/VERIFY-ONLY test bucket is actioned (ExecutionResultContractTests removed/migrated, EntryStepDispatchTests reshaped, shared stubs/kit updated).
- The full hermetic suite is GREEN — the phase gate (not just dotnet build) passes.
- Release builds 0-warning.
- ROADMAP Phase 43/48 + RETIRE-01/02 traceability rows reconciled to the D-01/D-02 coupling (docs only).
</success_criteria>

<output>
After completion, create `.planning/phases/43-message-contracts-l2-key-reshape/43-05-SUMMARY.md`
</output>
