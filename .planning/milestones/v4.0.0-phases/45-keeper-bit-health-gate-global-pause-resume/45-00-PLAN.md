---
phase: 45-keeper-bit-health-gate-global-pause-resume
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/Messaging.Contracts/PauseAll.cs
  - src/Messaging.Contracts/ResumeAll.cs
  - tests/BaseApi.Tests/Keeper/Health/L2HealthGateTests.cs
  - tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs
  - tests/BaseApi.Tests/Orchestrator/Consumers/PauseAllConsumerTests.cs
  - tests/BaseApi.Tests/Orchestrator/Consumers/ResumeAllConsumerTests.cs
  - tests/BaseApi.Tests/Orchestrator/Consumers/ResumeNoBurstTests.cs
autonomous: true
requirements: [KEEP-01, KEEP-02, KEEP-03, ORCH-02]

must_haves:
  truths:
    - "The two no-H global control contracts PauseAll and ResumeAll exist as ICorrelated records"
    - "Wave 0 test files exist (red stubs) covering the gate, the BIT loop edge-trigger, both consumers, and the no-burst negative"
    - "The solution compiles with the new contracts; stub tests are present and runnable"
  artifacts:
    - path: "src/Messaging.Contracts/PauseAll.cs"
      provides: "no-H global pause broadcast contract"
      contains: "public sealed record PauseAll"
    - path: "src/Messaging.Contracts/ResumeAll.cs"
      provides: "no-H global resume broadcast contract"
      contains: "public sealed record ResumeAll"
    - path: "tests/BaseApi.Tests/Keeper/Health/L2HealthGateTests.cs"
      provides: "KEEP-03 gate test stubs"
    - path: "tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs"
      provides: "KEEP-01 + KEEP-02 BIT loop test stubs"
    - path: "tests/BaseApi.Tests/Orchestrator/Consumers/PauseAllConsumerTests.cs"
      provides: "ORCH-02 pause test stubs"
    - path: "tests/BaseApi.Tests/Orchestrator/Consumers/ResumeAllConsumerTests.cs"
      provides: "ORCH-02 resume test stubs"
    - path: "tests/BaseApi.Tests/Orchestrator/Consumers/ResumeNoBurstTests.cs"
      provides: "ORCH-02 no-herd negative stub (native ResumeAll() NEVER called)"
  key_links:
    - from: "tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs"
      to: "src/Messaging.Contracts/PauseAll.cs"
      via: "publishes/asserts PauseAll + ResumeAll"
      pattern: "PauseAll|ResumeAll"
---

<objective>
Lay the Wave-0 foundation: the two new no-`H` global control contracts (`PauseAll`/`ResumeAll`) and the failing test scaffold for every Phase-45 requirement. Contracts ship in Wave 0 because BOTH downstream branches (the Keeper BIT loop in Plan 01 and the Orchestrator consumers in Plan 02) reference them, and the Wave-0 tests reference them too — so they are the shared interface-first contract.

Purpose: Nyquist validation gate. Per 45-VALIDATION.md, every requirement (KEEP-01/02/03, ORCH-02) must have an automated `dotnet test --filter ...` mapped BEFORE the production code lands. This plan creates the red stubs and the contracts they compile against.
Output: `PauseAll.cs`, `ResumeAll.cs`, and five test files (stubs that compile and run, asserting against not-yet-built production types via small local fakes / `[Fact(Skip=...)]` or `Assert.Fail` placeholders that Plans 01/02 turn green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-keeper-bit-health-gate-global-pause-resume/45-CONTEXT.md
@.planning/phases/45-keeper-bit-health-gate-global-pause-resume/45-VALIDATION.md
@.planning/phases/45-keeper-bit-health-gate-global-pause-resume/45-PATTERNS.md

<interfaces>
<!-- The contract shape to copy (no-H, ICorrelated). From src/Messaging.Contracts/StartOrchestration.cs -->
namespace Messaging.Contracts;
public sealed record StartOrchestration(Guid[] WorkflowIds) : ICorrelated
{
    public Guid CorrelationId { get; init; }
}
<!-- Contrast: the LEGACY H-carrying contract we are NOT copying. src/Messaging.Contracts/PauseWorkflow.cs:4 -->
public sealed record PauseWorkflow(Guid WorkflowId, string H) : ICorrelated { ... }  // carries H — DO NOT replicate

<!-- Test project facts: ONE shared test project tests/BaseApi.Tests (xUnit v3 / MTP). -->
<!-- It already references Keeper.csproj, Orchestrator.csproj, Messaging.Contracts (transitively), -->
<!-- MassTransit.Testing (AddMassTransitTestHarness), and NSubstitute. NO new test project needed. -->
<!-- Existing analog tests to mirror conventions: tests/BaseApi.Tests/Orchestrator/FanOutBroadcastTests.cs -->
<!-- (ITestHarness + AddMassTransitTestHarness), tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs. -->
<!-- xUnit v3 idiom: var ct = TestContext.Current.CancellationToken; -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the no-H PauseAll and ResumeAll global control contracts</name>
  <files>src/Messaging.Contracts/PauseAll.cs, src/Messaging.Contracts/ResumeAll.cs</files>
  <read_first>
    - src/Messaging.Contracts/StartOrchestration.cs  (EXACT analog — the no-H ICorrelated record shape to copy)
    - src/Messaging.Contracts/PauseWorkflow.cs  (CONTRAST — the legacy H-carrying shape we must NOT replicate)
    - .planning/phases/45-keeper-bit-health-gate-global-pause-resume/45-PATTERNS.md  (§"src/Messaging.Contracts/PauseAll.cs + ResumeAll.cs" — no-H posture RETIRE-01)
  </read_first>
  <action>
    Create `src/Messaging.Contracts/PauseAll.cs` byte-for-byte in the StartOrchestration shape, with NO `H`, NO `WorkflowId`, NO dedup key — a pure broadcast carrying only a tracing `CorrelationId`:

    ```csharp
    namespace Messaging.Contracts;

    /// <summary>Global control broadcast (D-08/A14): pause ALL orchestrator jobs (L2 unhealthy). No H — pure broadcast, CorrelationId for tracing only (RETIRE-01 no-H posture).</summary>
    public sealed record PauseAll : ICorrelated
    {
        public Guid CorrelationId { get; init; }
    }
    ```

    Create `src/Messaging.Contracts/ResumeAll.cs` identically:

    ```csharp
    namespace Messaging.Contracts;

    /// <summary>Global control broadcast (D-08/A14): resume ALL orchestrator jobs per-job (L2 healthy). No H — pure broadcast, CorrelationId for tracing only (RETIRE-01 no-H posture).</summary>
    public sealed record ResumeAll : ICorrelated
    {
        public Guid CorrelationId { get; init; }
    }
    ```

    Use file-scoped `namespace Messaging.Contracts;`, `sealed record`, implement `ICorrelated` ONLY (NOT `IKeeperRecoverable` — that is the 5-id partition marker for KeeperUpdate et al, irrelevant here). The `ICorrelated` interface already exists in this project (`ICorrelated.cs`).
  </action>
  <verify>
    <automated>dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `src/Messaging.Contracts/PauseAll.cs` contains `public sealed record PauseAll : ICorrelated`
    - `src/Messaging.Contracts/ResumeAll.cs` contains `public sealed record ResumeAll : ICorrelated`
    - Neither file contains the token ` H ` as a record parameter and neither contains `WorkflowId` (grep: `grep -E "string H|WorkflowId" src/Messaging.Contracts/PauseAll.cs src/Messaging.Contracts/ResumeAll.cs` returns nothing)
    - Both files contain `public Guid CorrelationId { get; init; }`
    - `dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj` exits 0
  </acceptance_criteria>
  <done>Both no-H contracts exist, implement ICorrelated, carry only CorrelationId, and Messaging.Contracts compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold the Keeper Wave-0 test stubs (gate + BIT loop)</name>
  <files>tests/BaseApi.Tests/Keeper/Health/L2HealthGateTests.cs, tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs  (existing Keeper test conventions — NSubstitute, xUnit v3 TestContext.Current.CancellationToken)
    - tests/BaseApi.Tests/Orchestrator/FanOutBroadcastTests.cs  (AddMassTransitTestHarness + ITestHarness publish-assert idiom for the broadcast test)
    - .planning/phases/45-keeper-bit-health-gate-global-pause-resume/45-VALIDATION.md  (§Per-Task Verification Map + §Observable Validation Signals — the exact behaviors to assert)
    - tests/BaseApi.Tests/BaseApi.Tests.csproj  (confirm MassTransit.Testing + NSubstitute already referenced; no package add)
  </read_first>
  <action>
    Create two xUnit v3 test classes under `tests/BaseApi.Tests/Keeper/Health/`. These are Wave-0 RED stubs: they name the exact behaviors and are wired to compile, but assert against production types (`Keeper.Health.IL2HealthGate`, `Keeper.Health.L2HealthGate`, `Keeper.Health.BitHealthLoop`) that do NOT exist yet. Use the file-scoped namespace `BaseApi.Tests.Keeper.Health;`. Because the production types are absent, write the stubs so they FAIL until Plan 01 lands them — preferred: write the full intended test body referencing the production types, but guard each `[Fact]` with `Assert.Fail("RED stub — implemented in 45-01")` at the top AND keep the real assertions below in a way that still compiles once the type exists. If referencing a not-yet-existing type would break compilation of the whole test project (it will), instead place the production-type-referencing bodies but DO NOT add them until Plan 01 — so for THIS task, create the stub with a single `[Fact] public void Stub() => Assert.Fail("RED — 45-01 must implement L2HealthGate");` per behavior, named precisely, and a top-of-file `// TODO(45-01): replace stub bodies with real assertions against Keeper.Health.*` comment listing the behaviors.

    `L2HealthGateTests.cs` — class `L2HealthGateTests`, one named `[Fact]` stub per KEEP-03 behavior:
    - `Gate_Starts_Closed_WaitForOpenAsync_Blocks_Until_Open`
    - `Open_Completes_The_Wait`
    - `Close_After_Open_Re_Blocks`
    - `WaitForOpenAsync_Throws_OperationCanceledException_On_Cancel`
    - `Open_Is_Idempotent` and `Close_Is_Idempotent`

    `BitHealthLoopTests.cs` — class `BitHealthLoopTests`, named `[Fact]` stubs for KEEP-01 + KEEP-02/SC#2:
    - `Probe_RedisException_Reports_Unhealthy_Loop_Survives`
    - `Probe_NonRedis_Throw_Propagates_Not_Swallowed`
    - `StoppingToken_Ends_Loop_Cleanly`
    - `Edge_Trigger_Publishes_PauseAll_Once_On_Healthy_To_Unhealthy`
    - `Edge_Trigger_Publishes_ResumeAll_Once_On_Unhealthy_To_Healthy`
    - `Same_State_Ticks_Publish_Nothing` (drive healthy,healthy,unhealthy,unhealthy,healthy → exactly 1 PauseAll + 1 ResumeAll)

    Each stub body: `Assert.Fail("RED — 45-01 must implement <type>");`. Use `var ct = TestContext.Current.CancellationToken;` where a CT is conceptually needed (document intent in a comment). DO NOT reference `Keeper.Health.*` types yet (they don't compile until 45-01) — keep stubs type-free so the project builds now.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~L2HealthGateTests|FullyQualifiedName~BitHealthLoopTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Keeper/Health/L2HealthGateTests.cs` exists and contains `Gate_Starts_Closed_WaitForOpenAsync_Blocks_Until_Open`, `Open_Completes_The_Wait`, `Close_After_Open_Re_Blocks`, `WaitForOpenAsync_Throws_OperationCanceledException_On_Cancel`
    - File `tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs` exists and contains `Edge_Trigger_Publishes_PauseAll_Once_On_Healthy_To_Unhealthy`, `Edge_Trigger_Publishes_ResumeAll_Once_On_Unhealthy_To_Healthy`, `Same_State_Ticks_Publish_Nothing`, `Probe_NonRedis_Throw_Propagates_Not_Swallowed`
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj` exits 0 (stubs compile)
    - `dotnet test --filter "FullyQualifiedName~L2HealthGateTests|FullyQualifiedName~BitHealthLoopTests"` runs and reports FAILURES (RED — these are stubs); exit code non-zero is expected and acceptable for Wave 0
  </acceptance_criteria>
  <done>Both Keeper test files exist with one named failing stub per KEEP-01/02/03 behavior; the test project compiles and the stubs run RED.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scaffold the Orchestrator Wave-0 test stubs (pause/resume/no-burst)</name>
  <files>tests/BaseApi.Tests/Orchestrator/Consumers/PauseAllConsumerTests.cs, tests/BaseApi.Tests/Orchestrator/Consumers/ResumeAllConsumerTests.cs, tests/BaseApi.Tests/Orchestrator/Consumers/ResumeNoBurstTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FanOutBroadcastTests.cs  (Orchestrator test conventions + harness idiom)
    - src/Orchestrator/L1/IWorkflowL1Store.cs  (the WorkflowIds snapshot the ResumeAll test fakes)
    - src/Orchestrator/Hydration/WorkflowLifecycle.cs lines 175-199  (ResumeAsync TriggerState==Paused guard + fresh-from-now reschedule the no-burst test asserts)
    - .planning/phases/45-keeper-bit-health-gate-global-pause-resume/45-VALIDATION.md  (§ ORCH-02 rows + the load-bearing no-burst negative)
  </read_first>
  <action>
    Create three xUnit v3 stub test classes under `tests/BaseApi.Tests/Orchestrator/Consumers/`, namespace `BaseApi.Tests.Orchestrator.Consumers;`. Same RED-stub discipline as Task 2: one named `[Fact]` per behavior, body `Assert.Fail("RED — 45-02 must implement <type>");`, no reference to not-yet-existing production types (`Orchestrator.Consumers.PauseAllConsumer`/`ResumeAllConsumer`) so the project still compiles. Add a `// TODO(45-02): replace stubs with real assertions` header naming the production types and the fakes they'll need (`IScheduler` spy via NSubstitute, fake `IWorkflowL1Store`, `WorkflowLifecycle`/`WorkflowScheduler`).

    `PauseAllConsumerTests.cs` — class `PauseAllConsumerTests`:
    - `Consume_Calls_Scheduler_PauseAll`
    - `Redelivery_Is_Idempotent_No_Op` (PauseAll twice → PauseAll() invoked twice, second a no-op, no exception)

    `ResumeAllConsumerTests.cs` — class `ResumeAllConsumerTests`:
    - `Consume_Enumerates_WorkflowIds_And_Calls_ResumeAsync_Each`
    - `Resume_Of_Non_Paused_Trigger_Is_Ignored` (TriggerState != Paused → no-op, no spurious resume)

    `ResumeNoBurstTests.cs` — class `ResumeNoBurstTests` (THE load-bearing negative):
    - `Native_ResumeAll_Is_Never_Called` — the spy asserts `scheduler.ResumeAll(...)` is NEVER invoked on the path
    - `Resume_Reschedules_Fresh_From_Now_StartAt_Ge_Now` — after resume, `ScheduleAsync` `StartAt` ≥ now (skip-to-next, no immediate fire)
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~PauseAllConsumerTests|FullyQualifiedName~ResumeAllConsumerTests|FullyQualifiedName~ResumeNoBurstTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `PauseAllConsumerTests.cs` exists and contains `Consume_Calls_Scheduler_PauseAll` and `Redelivery_Is_Idempotent_No_Op`
    - File `ResumeAllConsumerTests.cs` exists and contains `Consume_Enumerates_WorkflowIds_And_Calls_ResumeAsync_Each` and `Resume_Of_Non_Paused_Trigger_Is_Ignored`
    - File `ResumeNoBurstTests.cs` exists and contains `Native_ResumeAll_Is_Never_Called`
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj` exits 0
    - `dotnet test --filter "FullyQualifiedName~PauseAllConsumerTests|FullyQualifiedName~ResumeAllConsumerTests|FullyQualifiedName~ResumeNoBurstTests"` runs and reports FAILURES (RED stubs); non-zero exit expected for Wave 0
  </acceptance_criteria>
  <done>Three Orchestrator test files exist with named failing stubs per ORCH-02 behavior including the no-burst negative; project compiles, stubs run RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keeper BIT loop → orchestrator replicas (MassTransit bus) | Internal control-plane only; no external publisher. PauseAll/ResumeAll carry a Guid CorrelationId, no free-text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | PauseAll/ResumeAll log fields | mitigate | Contracts carry only a Guid CorrelationId — no free-text; downstream consumers (Plans 01/02) log via structured template holes only (T-37-05), never interpolation. This plan introduces no logging. |
| T-45-02 | Information Disclosure | contract payload | accept | No payload beyond a tracing Guid; nothing sensitive crosses the wire. ASVS V5: input is a Guid only — no validation surface. |
</threat_model>

<verification>
- `dotnet build` of the full solution succeeds with the new contracts + test stubs present.
- The five Wave-0 test files exist and run RED (stubs), establishing the Nyquist feedback channel for Plans 01/02.
</verification>

<success_criteria>
- `PauseAll`/`ResumeAll` no-H ICorrelated records exist and compile.
- Five test files exist mapping every requirement (KEEP-01/02/03, ORCH-02) to a named, runnable (RED) test.
- No new NuGet packages; no new test project (uses the shared `BaseApi.Tests`).
</success_criteria>

<output>
After completion, create `.planning/phases/45-keeper-bit-health-gate-global-pause-resume/45-00-SUMMARY.md`
</output>
