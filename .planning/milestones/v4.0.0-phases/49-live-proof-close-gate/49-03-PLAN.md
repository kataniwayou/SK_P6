---
phase: 49-live-proof-close-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs
autonomous: true
requirements: [TEST-02]
user_setup: []

must_haves:
  truths:
    - "An authored RealStack E2E file proves the BIT-gate global pause-all/resume-all across a true transient L2 outage: docker stop sk-redis -> BIT probe throws -> Publish(PauseAll) -> orchestrator PauseAllAsync; docker start sk-redis -> probe succeeds -> Publish(ResumeAll) -> per-job resume (TriggerState==Paused guard)."
    - "The file lives in its OWN non-parallel [Collection] (DisableParallelization=true) so stopping sk-redis cannot destabilize sibling RealStack tests."
    - "The test BLOCKS before returning on: docker start sk-redis -> redis healthy -> gate re-open -> liveness skp:{procId:D} re-written (steady-state re-established). Pause/resume idempotent."
    - "The file compiles 0-warning at Release+Debug and the non-RealStack suite is GREEN."
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs"
      provides: "SC3 RealStack proof of BIT-gate pause-all/resume-all across a docker-stop/start sk-redis outage"
      contains: "docker stop sk-redis"
      min_lines: 250
  key_links:
    - from: "SC3PauseResumeOutageE2ETests.cs"
      to: "BitHealthLoop edge -> Publish(PauseAll)/Publish(ResumeAll)"
      via: "docker stop/start sk-redis shell-out straddling Probe:DelaySeconds"
      pattern: "docker (stop|start) sk-redis"
    - from: "SC3PauseResumeOutageE2ETests.cs teardown"
      to: "steady-state re-established (liveness re-written + gate re-open)"
      via: "PollForHealthyLivenessAsync after docker start"
      pattern: "PollForHealthyLivenessAsync"

acceptance_criteria_global:
  - "dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release => 0 Warning / 0 Error."
  - "dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug => 0 Warning / 0 Error."
  - "dotnet run --project tests/BaseApi.Tests -- --filter-not-trait Category=RealStack => 0 failed (the new RealStack fact is EXCLUDED)."
---

<objective>
Author the SC3 RealStack pause-resume-outage E2E proof: a new sibling file `tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs` (in its OWN non-parallel collection) that proves the BIT-gate global pause-all/resume-all across a TRUE transient L2 outage induced via `docker stop sk-redis` … `docker start sk-redis`.

Purpose: Satisfy SC3 / TEST-02. LIVE-PROOF phase — NO production code changes. Harness cloned from `SampleRoundTripE2ETests.cs`; the TriggerState pause/resume assertion idiom from `ResumeAllConsumerTests.cs`; the edge-driver model from `BitHealthLoop.cs`.

Output: One compiling, hermetically-green RealStack E2E file in a dedicated non-parallel collection. **Definition of done is AUTHORED-AND-HERMETICALLY-GREEN (D-03):** compiles 0-warning Release+Debug, non-RealStack suite GREEN. The live outage run is OPERATOR-GATED — TEST-02 stays UNTICKED until the operator's GREEN live run (49-HUMAN-UAT.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-live-proof-close-gate/49-CONTEXT.md
@.planning/phases/49-live-proof-close-gate/49-PATTERNS.md

<interfaces>
<!-- The BIT edge model to drive against. From src/Keeper/Health/BitHealthLoop.cs:34-49 -->
if (prevHealthy != healthy) {                 // EDGE: transition only
    if (healthy)   { gate.Open();  await bus.Publish(new ResumeAll{...}); }
    else           { gate.Close(); await bus.Publish(new PauseAll{...}); }
}
// probe cadence = Probe:DelaySeconds (BitHealthLoop.cs:28). docker stop/start wait/poll timings must STRADDLE it.

<!-- Control contracts. From src/Messaging.Contracts/PauseAll.cs:4 / ResumeAll.cs:4 -->
public sealed record PauseAll  : ICorrelated { Guid CorrelationId { get; init; } }
public sealed record ResumeAll : ICorrelated { Guid CorrelationId { get; init; } }
// orchestrator binds them on endpoint "orchestrator-global-pauseresume" (PauseAllConsumerDefinition.cs:27)

<!-- Pause/resume assertion idiom (the SHAPE to mirror). From ResumeAllConsumerTests.cs:74-82 -->
Assert.Equal(TriggerState.Paused, await scheduler.GetTriggerState(new TriggerKey(jobId.ToString("D")), ct));
// ... after ResumeAll ...
Assert.Equal(TriggerState.Normal, await scheduler.GetTriggerState(new TriggerKey(jobId.ToString("D")), ct));

<!-- Live-read mechanism (orchestrator owns the scheduler out-of-process — cannot read TriggerState in-process). -->
// Orchestrator seam logs to assert via ES (ElasticsearchTestClient.PollEsForLog precedent, SampleRoundTripE2ETests.cs:150-167):
//   PauseAllConsumer.cs:23  -> LogWarning("Global PauseAll CorrelationId={CorrelationId}")
//   ResumeAllConsumer.cs:31 -> LogInformation("Global ResumeAll ...")

<!-- liveness re-write detection (block before returning). From SampleRoundTripE2ETests.cs:199-240 -->
await PollForHealthyLivenessAsync(procId, ct);   // confirms skp:{procId:D} heartbeat re-written after docker start
</interfaces>

<container_name>
The redis container is `sk-redis` (compose.yaml:137). Shell out via System.Diagnostics.Process (`docker stop sk-redis` / `docker start sk-redis`).
</container_name>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author SC3PauseResumeOutageE2ETests in a dedicated non-parallel collection with the docker-stop/start outage drive</name>
  <files>tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs</files>
  <read_first>
  - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (HARNESS analog — RealStackWebAppFactory 373-451, PollForHealthyLivenessAsync 199-240 for the blocking-teardown liveness re-write check, the ElasticsearchTestClient.PollEsForLog ES-advance precedent 150-167, net-zero teardown)
  - tests/BaseApi.Tests/Orchestrator/Consumers/ResumeAllConsumerTests.cs (IDIOM analog — the TriggerState.Paused/Normal pause/resume assertion shape lines 74-82, and the idempotency precedent)
  - src/Keeper/Health/BitHealthLoop.cs (the edge-triggered probe -> Publish(PauseAll)/Publish(ResumeAll) model lines 34-49; Probe:DelaySeconds cadence line 28 — timings must straddle it)
  - src/Orchestrator/Consumers/PauseAllConsumer.cs (LogWarning "Global PauseAll CorrelationId={CorrelationId}" line 23 — ES assert target) + ResumeAllConsumer.cs (LogInformation "Global ResumeAll ..." line 31)
  - src/Messaging.Contracts/PauseAll.cs / ResumeAll.cs (the ICorrelated contracts) + PauseAllConsumerDefinition.cs (endpoint "orchestrator-global-pauseresume")
  - .planning/phases/49-live-proof-close-gate/49-PATTERNS.md section "SC3PauseResumeOutageE2ETests.cs" (the non-parallel collection + docker-stop/start + blocking-teardown excerpts)
  </read_first>
  <action>
  Create `tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs`. Clone the `RealStackWebAppFactory` nested class + `PollForHealthyLivenessAsync` from `SampleRoundTripE2ETests.cs`.

  **D-02 — dedicated non-parallel collection.** This test stops `sk-redis`, which would destabilize sibling RealStack tests, so it must NOT share the "Observability" collection. Define a dedicated collection-definition class and tag the test class with it (name is discretion — use "RedisOutageSerial"):
  ```csharp
  [CollectionDefinition("RedisOutageSerial", DisableParallelization = true)]
  public sealed class RedisOutageSerialCollection { }

  [Trait("Category", "E2E")]
  [Trait("Category", "RealStack")]
  [Trait("Phase", "49")]
  [Collection("RedisOutageSerial")]
  public sealed class SC3PauseResumeOutageE2ETests
  ```

  **D-01 — true transient outage via docker stop/start sk-redis.** Shell out via `System.Diagnostics.Process` (container name `sk-redis`, compose.yaml:137). The flow:
  1. Seed a processor+step+workflow (cron "* * * * *") and `PollForHealthyLivenessAsync(procId, ct)`; drive Start; prove the round-trip fires (output lands in skp:data:*) BEFORE the outage — the steady-state baseline.
  2. `docker stop sk-redis` -> the `BitHealthLoop` write-then-delete probe throws `RedisException` -> `IL2HealthGate` closes -> Keeper `Publish(PauseAll)` -> orchestrator `PauseAllAsync()`. Wait long enough to STRADDLE the `Probe:DelaySeconds` cadence so the edge is observed.
  3. Assert PAUSE took effect. Live read mechanism (orchestrator owns the scheduler out-of-process — cannot read Quartz TriggerState in-process): assert via the orchestrator seam log in ES using `ElasticsearchTestClient.PollEsForLog` (precedent SampleRoundTripE2ETests.cs:150-167) for `PauseAllConsumer.cs:23` "Global PauseAll CorrelationId=". The `TriggerState.Paused`/`Normal` idiom (ResumeAllConsumerTests.cs:74-82) is the assertion SHAPE you mirror; the live mechanism is the ES seam-log poll. As an additional observable proof, assert that no NEW skp:data:* output key appears during the paused window (the cron is paused -> dispatch stops).
  4. `docker start sk-redis` -> probe succeeds -> gate opens -> `Publish(ResumeAll)` -> per-job resume (`TriggerState == Paused` guard). Assert RESUME via the ES seam log for `ResumeAllConsumer.cs:31` "Global ResumeAll".
  5. Prove idempotency: re-publishing/re-driving resume does not double-resume (mirror ResumeAllConsumerTests' idempotency precedent — assert the resume seam is safe to re-observe; the production guard is `TriggerState == Paused`).

  **D-02 blocking teardown — MUST block before returning on:** `docker start sk-redis` -> redis healthy -> steady-state re-established. Reuse `PollForHealthyLivenessAsync(procId, ct)` to confirm the `skp:{procId:D}` liveness heartbeat is re-written AND prove a post-resume round-trip output lands (gate re-opened). Only then return. Net-zero teardown (`L2KeysToCleanup`/`ParentIndexMembersToSrem`) identical to SC1; best-effort workflow-stop. **CRITICAL:** even if an assertion throws, ensure `docker start sk-redis` runs (try/finally) so the suite never leaves redis stopped for the sibling tests / the close gate.

  Use generous poll budgets straddling the probe cadence. Do NOT change any production code. Use the Write tool (BOM-less UTF-8).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
  - File exists: tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs
  - grep `[CollectionDefinition("RedisOutageSerial", DisableParallelization = true)]` => 1 match (non-parallel isolation, D-02)
  - grep `[Collection("RedisOutageSerial")]` => 1 match; the file does NOT use [Collection("Observability")]
  - grep `[Trait("Phase", "49")]` => 1 match; grep `[Trait("Category", "RealStack")]` => 1 match
  - grep `docker stop sk-redis` => present (or "stop","sk-redis" Process args); grep `docker start sk-redis` => present
  - grep `start sk-redis` appears inside a finally block (the outage is always healed even on assertion failure)
  - grep `PollForHealthyLivenessAsync` => present (blocking-teardown liveness re-write check after docker start)
  - grep `Global PauseAll` => present (ES pause seam assert); grep `Global ResumeAll` => present (ES resume seam assert)
  - `dotnet build ... -c Release` AND `-c Debug` => 0 Warning / 0 Error
  - `dotnet run --project tests/BaseApi.Tests -- --filter-not-trait Category=RealStack` => 0 failed
  - File BOM-less UTF-8, no mojibake; NO production-code file changed
  </acceptance_criteria>
  <done>SC3PauseResumeOutageE2ETests.cs exists in its own non-parallel collection, compiles 0-warning Release+Debug, drives a true docker stop/start sk-redis outage (healed in a finally), asserts pause-all then resume-all via the orchestrator ES seam logs with the TriggerState idiom mirrored, proves idempotency, blocks on liveness re-write before returning, and the hermetic suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> docker daemon (docker stop/start sk-redis) | The test shells out to the local docker daemon to stop/start the redis container. Local process control only. |
| test -> orchestrator seam logs via ES (loopback) | Pause/resume proven by polling the local Elasticsearch for orchestrator seam logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06 | D (Denial of service) | docker stop sk-redis | mitigate | The outage is INTENTIONAL and transient; it is healed in a try/finally `docker start sk-redis` so the stack is never left degraded even on assertion failure. Isolated in a non-parallel collection (D-02) so siblings are unaffected. Blocks on steady-state re-establishment before returning. |
| T-49-07 | E (Elevation of privilege) | docker shell-out from a test | accept | The test invokes the local docker CLI with the developer's existing docker context — no privilege escalation beyond what the developer already holds locally. Test/ops tooling against a local stack. |

No high-severity threats — local process control against a local stack, no new network surface, no secrets handling. The one genuine availability concern (leaving redis stopped) is mitigated by the try/finally heal + non-parallel isolation + blocking steady-state teardown.
</threat_model>

<verification>
- `dotnet build ... -c Release` and `-c Debug` => 0 Warning / 0 Error.
- `dotnet run --project tests/BaseApi.Tests -- --filter-not-trait Category=RealStack` => 0 failed.
- grep confirms: dedicated non-parallel collection, docker stop/start sk-redis (start in a finally), the ES pause+resume seam asserts, the blocking liveness re-write check.
</verification>

<success_criteria>
SC3 pause-resume-outage proof is AUTHORED + COMPILES + hermetic-suite-green, isolated in a non-parallel collection, with the outage always healed. The live run is operator-gated (49-HUMAN-UAT.md); TEST-02 stays UNTICKED.
</success_criteria>

<output>
After completion, create `.planning/phases/49-live-proof-close-gate/49-03-SUMMARY.md`.
</output>
