---
phase: 49-live-proof-close-gate
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-49-close.ps1
  - .planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
user_setup: []

must_haves:
  truths:
    - "scripts/phase-49-close.ps1 clones the phase-39 triple-SHA protocol IDENTICALLY: idempotent steady-state Processor-row seed (genuine embedded SourceHash via reflection), compose-health pre-flight, BOTH-config 0-warning build gate, 3-consecutive-GREEN identical-fact-count cadence, settle-drain, triple-SHA (psql \\l / redis --scan / rabbitmq list_queues) BEFORE==AFTER, separate DLQ depth==0."
    - "v4 deltas applied: single DLQ @('skp-dlq-1') (keeper-dlq dropped), NO composite-TTL settle-wait (the 2-day composite is proven net-zero by E2E teardown, surfaced as a redis SHA mismatch on leak), v4 canonical service list, and the seed version string set to the verified live Processor.Sample value 3.5.0 (was 3.7.0 in phase-39)."
    - "49-HUMAN-UAT.md is the operator runbook: the v4 stack-rebuild set, the gate invocation, and the values the operator records on the GREEN run (3 SHA values + Passed count + skp-dlq-1 depth). It is the artifact that gates the TEST-01/02/03 tick."
    - "The close script is valid PowerShell (parses without error) and the runbook exists; no production code changed."
  artifacts:
    - path: "scripts/phase-49-close.ps1"
      provides: "v4 triple-SHA N=3 GREEN net-zero close gate (single skp-dlq-1, no composite TTL wait)"
      contains: "@('skp-dlq-1')"
      min_lines: 300
    - path: ".planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md"
      provides: "operator runbook: rebuild set, gate invocation, GREEN-run record fields, TEST-01/02/03 tick gate"
      contains: "phase-49-close.ps1"
      min_lines: 40
  key_links:
    - from: "scripts/phase-49-close.ps1"
      to: "skp-dlq-1 depth==0 (sole DLQ)"
      via: "foreach ($q in @('skp-dlq-1'))"
      pattern: "@\\('skp-dlq-1'\\)"
    - from: "49-HUMAN-UAT.md"
      to: "scripts/phase-49-close.ps1"
      via: "gate invocation pwsh -File scripts/phase-49-close.ps1"
      pattern: "phase-49-close.ps1"

acceptance_criteria_global:
  - "PowerShell parses scripts/phase-49-close.ps1 without error (Parser.ParseFile produces 0 errors / pwsh -NoProfile -Command syntax check)."
  - "49-HUMAN-UAT.md exists, BOM-less UTF-8, no mojibake."
---

<objective>
Author the v4 close gate + operator runbook: `scripts/phase-49-close.ps1` (a clone of the proven `scripts/phase-39-close.ps1` triple-SHA protocol with the v4 deltas) and `.planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md` (the operator runbook that gates the TEST-01/02/03 tick).

Purpose: Satisfy SC4 / TEST-03 (the close machinery) and provide the operator-gate artifact for TEST-01/02/03. LIVE-PROOF phase — NO production code changes. **Definition of done is AUTHORED (D-03):** the close script parses and the runbook is committed. The actual live N×GREEN close RUN is OPERATOR-GATED (requires the rebuilt v4 stack) — TEST-01/02/03 stay UNTICKED until the operator's GREEN run, recorded in 49-HUMAN-UAT.md.

Output: One v4 close script + one operator runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-live-proof-close-gate/49-CONTEXT.md
@.planning/phases/49-live-proof-close-gate/49-PATTERNS.md

<verified_values>
<!-- Verified against the live source during planning: -->
- Processor.Sample seed version: src/Processor.Sample/appsettings.json:11 => "Version": "3.5.0". Phase-39 seed used '3.7.0' (STALE for v4). SET the seed version string to '3.5.0' (the live v4 value).
- v4 canonical service list (compose.yaml, keeper has replicas:2):
  $services = @('postgres','redis','rabbitmq','otel-collector','elasticsearch','prometheus','orchestrator','processor-sample','baseapi-service','keeper')
- Single DLQ (keeper-dlq retired Phase 48): @('skp-dlq-1')  -- ConsolidatedErrorTransportFilter.Dlq1 is the const this literal tracks; annotate with a comment.
- v4 rebuild set (breaking wire contract): docker compose up -d --build baseapi-service orchestrator processor-sample keeper
- N = 3 consecutive GREEN, identical-fact-count Smell-A guard.
</verified_values>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Clone phase-39-close.ps1 into phase-49-close.ps1 with the v4 deltas</name>
  <files>scripts/phase-49-close.ps1</files>
  <read_first>
  - scripts/phase-39-close.ps1 (THE ANALOG to clone — read the WHOLE file: header+exit codes 1-74, pre-flight Processor-row seed 84-167 incl. the AssemblyMetadataAttribute reflection 104-112 + GET-or-create over http://localhost:8080/api/v1/processors 117-145 + processor-sample healthy wait 149-167, compose-health pre-flight 169-189, BEFORE triple-SHA 191-210, 0-warning BOTH-config build gate 212-225, 3-GREEN cadence 227-260, settle-drain 262-283, AFTER triple-SHA 285-303, triple invariant assertions 305-341, DLQ depth==0 343-359, PASS summary 361-377)
  - src/Processor.Sample/appsettings.json (CONFIRM the live Version is "3.5.0" — the seed version string to set; line 11)
  - src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs (the Dlq1 const = "skp-dlq-1" — the literal the script tracks; annotate the comment citing the const)
  - .planning/phases/49-live-proof-close-gate/49-PATTERNS.md section "scripts/phase-49-close.ps1" (the full line-map of what to copy verbatim vs. what to change for v4)
  </read_first>
  <action>
  Create `scripts/phase-49-close.ps1` as a clone of `scripts/phase-39-close.ps1`. Keep the triple-SHA protocol IDENTICAL. Apply ONLY these v4 deltas:

  1. **Header (lines 1-74):** rewrite the comment for v4: "Phase 49 close gate — v4.0.0 (triple-SHA)"; drop all `keeper-dlq` references (retired Phase 48 — single `skp-dlq-1`); note the composite `corr:wf:proc:exec` namespace + GUID data keys `skp:data:*` are captured by the UNFILTERED `--scan` (no prefix filter needed) and that the 2-day-TTL composite is proven net-zero by the E2E teardown (registered into L2KeysToCleanup), NOT by waiting out the TTL — a lingering composite surfaces as a redis SHA mismatch (exit 1). Update the rebuild-set note to: `docker compose up -d --build baseapi-service orchestrator processor-sample keeper` (v4 is a fresh breaking wire-contract change). Update exit-code doc: 0 = both configs 0-warning + 3 GREEN + all three SHA held + `skp-dlq-1` depth==0.

  2. **Pre-flight Processor-row seed (84-167):** COPY VERBATIM the AssemblyMetadataAttribute reflection read of the genuine SourceHash off `Processor.Sample.dll`, the GET-or-create over `http://localhost:8080/api/v1/processors`, and the processor-sample-healthy wait. **CHANGE the seed version string** `version = '3.7.0'` (phase-39 line 134) to `version = '3.5.0'` (the verified live v4 `Processor.Sample` value — appsettings.json:11). Update the description string to "Phase 49 close-gate steady-state Processor.Sample row".

  3. **Compose-health pre-flight (169-189):** keep the per-line NDJSON multi-replica parse (keeper has replicas:2). Set `$services` to the v4 canonical list:
  ```powershell
  $services = @('postgres','redis','rabbitmq','otel-collector','elasticsearch','prometheus','orchestrator','processor-sample','baseapi-service','keeper')
  ```

  4. **BEFORE + AFTER triple-SHA snapshots (191-210 / 285-303):** COPY VERBATIM — the same three commands:
  ```
  psql \l   : docker compose exec -T postgres psql -U postgres -lqt | SHA256
  redis     : docker exec sk-redis redis-cli --scan | Sort-Object -CaseSensitive | SHA256   (UNFILTERED — captures composite + GUID data keys; NO new prefix filter, D-07)
  rabbitmq  : docker exec sk-rabbitmq rabbitmqctl -q list_queues name | Sort-Object -CaseSensitive | SHA256   (name only, NOT name messages)
  ```

  5. **0-warning build gate, BOTH configs (212-225):** COPY VERBATIM (`dotnet clean` then `dotnet build SK_P.sln -c Release` + `-c Debug`, fatal on non-zero $LASTEXITCODE).

  6. **3-GREEN cadence (227-260):** COPY VERBATIM — N=3, parse `Passed:\s+(\d+)`, Smell-A identical-fact-count guard across the 3 runs. The MTP runner full-suite invocation stays as phase-39 does.

  7. **Settle-drain (262-283):** keep a short settle for any short-TTL `skp:data:*` round-trip keys IF phase-39 had one, but **DO NOT add a composite-TTL settle-wait** (D-07 — the composite's 2-day TTL cannot be waited out; net-zero is proven by E2E teardown, a leak surfaces as a redis SHA mismatch). Add a comment stating this explicitly.

  8. **Triple invariant assertions (305-341):** COPY VERBATIM (psql / redis / rmq BEFORE==AFTER, exit 1 on mismatch).

  9. **DLQ depth==0 (343-359):** CHANGE from phase-39's `@('keeper-dlq','skp-dlq-1')` to the SINGLE v4 DLQ:
  ```powershell
  foreach ($q in @('skp-dlq-1')) {   # ConsolidatedErrorTransportFilter.Dlq1 — sole surviving DLQ (keeper-dlq retired Phase 48)
      ...   # keep the `list_queues name messages` depth read; depth>0 => exit 1
  }
  ```
  Keep the depth column OUT of the name-SHA (Pitfall 4: name-SHA stays `list_queues name`, the depth check is a separate `list_queues name messages` read).

  10. **PASS summary + operator-append note (361-377):** COPY, update the DLQ line to single `skp-dlq-1=0`.

  Use the Write tool (BOM-less UTF-8), never Set-Content/Out-File. Do NOT change any production code or any test file.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile('scripts/phase-49-close.ps1', [ref]$null, [ref]$e); if ($e) { $e; exit 1 } else { 'PARSE OK' }"</automated>
  </verify>
  <acceptance_criteria>
  - File exists: scripts/phase-49-close.ps1
  - PowerShell parses with 0 errors (Parser.ParseFile)
  - grep `@('skp-dlq-1')` => present (single DLQ loop)
  - grep `keeper-dlq` => 0 matches (retired)
  - grep `version        = '3.5.0'` (or `version = '3.5.0'`) => present (verified live seed version; NOT '3.7.0')
  - grep `'3.7.0'` => 0 matches (stale phase-39 version gone)
  - grep `processor-sample','baseapi-service','keeper'` => present (v4 canonical service list)
  - grep `redis-cli --scan` => present (unfiltered scan, no new prefix filter)
  - grep `list_queues name` => present; the name-SHA input is `list_queues name` (NOT `name messages`)
  - grep `-c Release` AND `-c Debug` => both present (both-config 0-warning gate)
  - grep `Passed` (the 3-GREEN fact-count parse) => present
  - A comment present stating the composite 2-day-TTL is NOT settle-waited (proven net-zero by E2E teardown)
  - File BOM-less UTF-8, no mojibake; NO production code / test file changed
  </acceptance_criteria>
  <done>scripts/phase-49-close.ps1 exists, parses clean, clones the phase-39 triple-SHA protocol identically, and applies exactly the v4 deltas (single skp-dlq-1, seed version 3.5.0, v4 service list, no composite TTL wait, unfiltered scan).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 49-HUMAN-UAT.md operator runbook (the TEST-01/02/03 tick gate)</name>
  <files>.planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md</files>
  <read_first>
  - scripts/phase-49-close.ps1 (the file authored in Task 1 — the gate's invocation surface, exit codes, and the operator-append line it prints; the runbook documents these)
  - scripts/phase-39-close.ps1 (the operator-append line ~372 — the SHA values + Passed count + DLQ depth fields the operator records; mirror that record shape)
  - .planning/phases/49-live-proof-close-gate/49-CONTEXT.md (D-03 deferred-live definition-of-done; the "authored-hermetic + operator-gated-live" pattern)
  - .planning/phases/49-live-proof-close-gate/49-PATTERNS.md section "49-HUMAN-UAT.md" (the required-contents list)
  </read_first>
  <action>
  Create `.planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md` — the operator runbook that gates the TEST-01/02/03 tick (D-03). Include these sections (mirror the project `*-HUMAN-UAT.md` convention):

  1. **Header + purpose:** Phase 49 live-proof + close-gate. State that Phase 49 is AUTHORED + hermetically green; the live N×GREEN run is operator-gated; TEST-01/02/03 stay UNTICKED until the operator records a GREEN run here (D-03).

  2. **Stack rebuild set (breaking v4 contract):** the operator MUST rebuild before a valid live run:
  ```
  docker compose up -d --build baseapi-service orchestrator processor-sample keeper
  ```
  then bring up the full stack (postgres, redis, rabbitmq, otel-collector, elasticsearch, prometheus + the 4 rebuilt). State WHY: v4 is a breaking wire-contract change — a mixed-version stack mis-deserializes the new contract.

  3. **Gate invocation:**
  ```
  pwsh -File scripts/phase-49-close.ps1
  ```
  Note it runs the full suite live (including the 3 RealStack E2E facts SC1/SC2/SC3 — the SC3 outage test runs serialized in its own non-parallel collection) across the 3-GREEN cadence with the triple-SHA net-zero invariants. Note exit codes (0 = pass; 1 = invariant violation / RED / unparseable fact count; 2 = stack not healthy).

  4. **Values to record on the GREEN run** (a fill-in table — mirror phase-39-close.ps1's operator-append line):
     - psql `\l` SHA-256 (BEFORE==AFTER): ____
     - redis `--scan` SHA-256 (BEFORE==AFTER): ____
     - rabbitmq `list_queues` SHA-256 (BEFORE==AFTER): ____
     - Passed fact count (identical across all 3 runs): ____
     - `skp-dlq-1` depth (==0): ____
     - Run date / operator: ____

  5. **DoD gate:** TEST-01, TEST-02, TEST-03 are ticked ONLY after a GREEN run is recorded above. Until then they stay `[ ]` in REQUIREMENTS.md.

  Use the Write tool (BOM-less UTF-8), never Set-Content/Out-File. Use ASCII tokens where natural; genuine UTF-8 glyphs only where intended (no pre-mojibaked bytes).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "if (Test-Path '.planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md') { 'EXISTS' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
  - File exists: .planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md
  - grep `docker compose up -d --build baseapi-service orchestrator processor-sample keeper` => present (rebuild set)
  - grep `pwsh -File scripts/phase-49-close.ps1` => present (gate invocation)
  - grep `psql` AND `redis` AND `rabbitmq` SHA record fields => present (the 3 SHA values to record)
  - grep `Passed` => present (the fact-count field); grep `skp-dlq-1` => present (the depth==0 field)
  - grep `TEST-01` AND `TEST-02` AND `TEST-03` => present (the DoD tick gate)
  - File BOM-less UTF-8 (no EF BB BF prefix), 0 mojibake; >= 40 lines
  - NO production code / test file / REQUIREMENTS.md changed (this task writes only the runbook)
  </acceptance_criteria>
  <done>49-HUMAN-UAT.md exists with the v4 rebuild set, the phase-49-close.ps1 invocation, the GREEN-run record fields (3 SHAs + Passed + skp-dlq-1 depth), and the explicit TEST-01/02/03 tick gate; BOM-less UTF-8, no mojibake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close script -> local docker stack (docker exec / docker compose exec) | The script shells docker/compose commands against the local stack for the SHA snapshots + health checks. |
| close script -> WebApi (http://localhost:8080) | The idempotent Processor-row seed POSTs to the local WebApi over loopback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-08 | E (Elevation of privilege) | phase-49-close.ps1 shelling docker exec / docker compose exec | accept | The script runs the local docker CLI under the operator's existing docker context — no privilege escalation beyond what the operator already holds. Ops tooling against a local stack. |
| T-49-09 | I (Information disclosure) | http://localhost:8080 seed POST | accept | The seed POSTs a Processor row to the loopback WebApi using the existing local-stack dev creds in compose; no new secret, no remote endpoint. |
| T-49-10 | T (Tampering) | triple-SHA net-zero gate | accept | The gate is snapshot-and-compare ONLY (NO destructive whole-db flush, NO gate-side purge); a teardown regression surfaces as a SHA mismatch / depth>0, not a silent pass. Read-only invariant by design. |

No high-severity threats — an operator PowerShell script that shells docker/compose against a local stack and hits the loopback WebApi; no new network surface, no secrets handling beyond the existing local-stack dev creds.
</threat_model>

<verification>
- `pwsh` Parser.ParseFile of scripts/phase-49-close.ps1 => 0 errors.
- grep confirms the v4 deltas: single `@('skp-dlq-1')`, seed version '3.5.0' (no '3.7.0'), v4 service list, unfiltered scan, no composite TTL wait, both-config build gate.
- 49-HUMAN-UAT.md exists with rebuild set + gate invocation + record fields + TEST-01/02/03 tick gate.
</verification>

<success_criteria>
The v4 close gate is AUTHORED (parses clean, triple-SHA protocol identical, v4 deltas applied) and the operator runbook is committed. The live N×GREEN run is operator-gated; TEST-01/02/03 stay UNTICKED until the operator records a GREEN run in 49-HUMAN-UAT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/49-live-proof-close-gate/49-04-SUMMARY.md`.
</output>
