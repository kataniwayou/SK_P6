---
phase: 48-v3-x-teardown
plan: 03
type: execute
wave: 3
depends_on: [48-01, 48-02]
autonomous: true
requirements: [RETIRE-01, RETIRE-02, RETIRE-03]
files_modified:
  - .planning/phases/48-v3-x-teardown/48-TEARDOWN-AUDIT.md
  - .planning/REQUIREMENTS.md
  - docs/design/2026-06-08-processor-keeper-recovery-redesign.md

must_haves:
  truths:
    - "48-TEARDOWN-AUDIT.md has one row per RETIRE-01/02/03 + SC-1..SC-4, each mapped to a named, real, green proving guard test or source-scan (D-04)."
    - "REQUIREMENTS.md marks RETIRE-01, RETIRE-02, RETIRE-03 satisfied (the checkbox lines + the status table) (D-04)."
    - "The locked design doc carries an additive A17 amendment recording the reactive-path + keeper-dlq retirement (D-04)."
    - "The SC-4 close gate is met: hermetic suite GREEN ×3 consecutive + Release AND Debug builds at 0 warnings (D-03)."
  artifacts:
    - path: ".planning/phases/48-v3-x-teardown/48-TEARDOWN-AUDIT.md"
      provides: "RETIRE/SC traceability ledger mirroring 47-DLQ-AUDIT.md"
      min_lines: 25
    - path: ".planning/REQUIREMENTS.md"
      provides: "RETIRE-01/02/03 marked satisfied"
      contains: "[x] **RETIRE-03**"
    - path: "docs/design/2026-06-08-processor-keeper-recovery-redesign.md"
      provides: "A17 retirement amendment"
      contains: "A17"
  key_links:
    - from: ".planning/phases/48-v3-x-teardown/48-TEARDOWN-AUDIT.md"
      to: "the green proving tests (ReactivePathRetiredFacts + AtLeastOnceStructuralFacts)"
      via: "each ledger row cites a named test/scan that resolves green"
      pattern: "ReactivePathRetiredFacts|No_dedup_machinery_on_execution_path|No_v4_give_up_path_references_keeper_dlq"
---

<objective>
Close the v4.0.0 retirement story end-to-end (D-04) and pass the SC-4 close gate (D-03). Three reconciliation deliverables — the `48-TEARDOWN-AUDIT.md` traceability ledger, the `REQUIREMENTS.md` RETIRE-01/02/03 status flips, and the additive A17 design-doc amendment — plus the hermetic-only close gate (suite GREEN ×3 + Release AND Debug 0-warning build).

Purpose: This is the LAST RETIRE phase; the ledger + doc amendment + requirements flips make the retirement traceable and the source-of-truth doc no longer silent on it. The SC-4 gate proves the v4-only path is green and clean.
Output: Audit ledger, updated REQUIREMENTS, amended design doc, and a recorded ×3-green + 0-warning-build result.

Depends on Plans 01 + 02: every audit row must cite an ALREADY-GREEN test, and the ×3 close gate runs against the fully-torn-down + guarded tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-v3-x-teardown/48-CONTEXT.md
@.planning/phases/48-v3-x-teardown/48-RESEARCH.md
@.planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md

<interfaces>
<!-- The proving tests each audit row must cite (all green after Plans 01-02). -->

- SC-1 / RETIRE-01: AtLeastOnceStructuralFacts.No_dedup_machinery_on_execution_path ([Trait("Phase","47")], cite-existing) + (widen) No_v4_give_up_path_references_keeper_dlq (now unconditional).
- SC-2 / RETIRE-02: ReactivePathRetiredFacts SC-2 fact (ExecutionData single-Guid-overload + no *Manifest* type, [Trait("Phase","48")]).
- SC-3 / RETIRE-03: ReactivePathRetiredFacts FACT 1 (no Fault<T> consumer), FACT 2 (no keeper-dlq/keeper-fault-recovery literal in src/Keeper), FACT 3 (KeeperQueues const absence) [Trait("Phase","48")].
- SC-4: the close gate (dotnet test ×3 GREEN + Release/Debug 0-warning build).

REQUIREMENTS.md edit sites (research §canonical_refs / REQUIREMENTS read):
- Checkbox lines 66-68: `[ ] **RETIRE-01/02/03**` → `[x]`.
- Status table lines 117-119: RETIRE-01/02/03 `Pending` → `Satisfied` (RETIRE-01/02 = verified-via-remnant-sweep; RETIRE-03 = removed here).

Design-doc amendment (research §Open Q2 / PATTERNS): next number is A17. Add a top-of-doc `**Amended 2026-06-09 (A17):**` line + a `| A17 | reactive Fault<T> path + keeper-dlq retired in Phase 48; proven by 48-TEARDOWN-AUDIT.md |` row in the locked-decisions table (lines 104-115). Additive only — do NOT edit existing locked rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 48-TEARDOWN-AUDIT.md + flip REQUIREMENTS.md + amend the design doc (A17)</name>
  <files>.planning/phases/48-v3-x-teardown/48-TEARDOWN-AUDIT.md, .planning/REQUIREMENTS.md, docs/design/2026-06-08-processor-keeper-recovery-redesign.md</files>
  <read_first>
    - .planning/phases/47-dlq-consolidation-at-least-once-semantics/47-DLQ-AUDIT.md (the ledger layout to mirror)
    - .planning/REQUIREMENTS.md lines 65-68 (checkboxes) + 115-123 (status table)
    - docs/design/2026-06-08-processor-keeper-recovery-redesign.md lines 100-122 (locked-decisions table + scope note — the A15 row is the amendment-style precedent; A16 is Phase-47's)
    - 48-RESEARCH.md §6 (which test proves each RETIRE/SC), §Validation Architecture (Req→Test Map), §Open Q2 (A17)
    - 48-CONTEXT.md D-04 (the three deliverables)
    - 48-PATTERNS.md "Additive design-doc amendment", "Audit-ledger layout"
  </read_first>
  <action>
    (1) CREATE `.planning/phases/48-v3-x-teardown/48-TEARDOWN-AUDIT.md` — a traceability ledger mirroring 47-DLQ-AUDIT.md. One row per criterion mapping RETIRE-01 / RETIRE-02 / RETIRE-03 AND roadmap SC-1..SC-4 → its named proving guard test / source-scan + the verify command. Columns are discretion (follow 47-DLQ-AUDIT). Use the <interfaces> mapping verbatim:
      - SC-1 / RETIRE-01 → `AtLeastOnceStructuralFacts.No_dedup_machinery_on_execution_path` (cite-existing, Phase-47 green) + the now-unconditional `No_v4_give_up_path_references_keeper_dlq`.
      - SC-2 / RETIRE-02 → `ReactivePathRetiredFacts` SC-2 fact (ExecutionData single-Guid overload + no *Manifest*).
      - SC-3 / RETIRE-03 → `ReactivePathRetiredFacts` FACT 1 + FACT 2 + FACT 3.
      - SC-4 → the close-gate command (dotnet test ×3 + Release/Debug 0-warning build) — cite the result captured in Task 2.
    Every row MUST name a REAL test that resolves green (the verifier checks this). Include the exact `dotnet run --project tests/BaseApi.Tests ... --filter-*` command per row.

    (2) EDIT `.planning/REQUIREMENTS.md` (D-04 deliverable 2):
      - Lines 66-68: flip `- [ ] **RETIRE-01**`, `**RETIRE-02**`, `**RETIRE-03**` → `- [x]`.
      - Status table lines 117-119: RETIRE-01 / RETIRE-02 / RETIRE-03 `Pending` → `Satisfied`. (RETIRE-01/02 = verified-via-remnant-sweep per the mapping note; RETIRE-03 = newly removed here.) Do not alter the Phase mapping text.

    (3) EDIT `docs/design/2026-06-08-processor-keeper-recovery-redesign.md` (D-04 deliverable 3, additive ONLY — research §Open Q2):
      - Add an A17 row to the locked-decisions table: `| A17 | reactive Fault<T> path + keeper-dlq retired in Phase 48; proven by 48-TEARDOWN-AUDIT.md |` (wording is executor discretion per D-04 — must record the reactive-path + keeper-dlq retirement).
      - Add a top-of-doc `**Amended 2026-06-09 (A17):**` line consistent with the existing amendment style.
      - Do NOT edit or remove any existing locked row/text (additive-amendment pattern, Phases 46/47 precedent).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/48-v3-x-teardown/48-TEARDOWN-AUDIT.md` and `grep -c "RETIRE-0" 48-TEARDOWN-AUDIT.md` returns ≥ 3 AND `grep -c "SC-" ...` returns ≥ 4 (all RETIRE + SC rows present).
    - `grep -n "ReactivePathRetiredFacts\|No_dedup_machinery_on_execution_path\|No_v4_give_up_path_references_keeper_dlq" 48-TEARDOWN-AUDIT.md` — each cited test named (rows resolve to real tests).
    - `grep -n "\[x\] \*\*RETIRE-0[123]\*\*" .planning/REQUIREMENTS.md` returns 3 matches; `grep -n "RETIRE-0[123].*Satisfied" .planning/REQUIREMENTS.md` returns 3 matches in the status table.
    - `grep -n "A17" docs/design/2026-06-08-processor-keeper-recovery-redesign.md` present AND the prior A15 row still present (additive, nothing removed).
    - `dotnet build SK_P.sln -c Debug` exits 0 (docs-only changes do not affect the build, confirms tree still compiles).
  </acceptance_criteria>
  <done>48-TEARDOWN-AUDIT.md exists with every RETIRE/SC row citing a named green test; REQUIREMENTS.md marks RETIRE-01/02/03 satisfied (checkboxes + status table); the design doc carries an additive A17 retirement amendment with no locked text altered.</done>
</task>

<task type="auto">
  <name>Task 2: Run the SC-4 close gate (hermetic suite GREEN ×3 + Release AND Debug 0-warning build)</name>
  <files>.planning/phases/48-v3-x-teardown/48-TEARDOWN-AUDIT.md</files>
  <read_first>
    - 48-CONTEXT.md D-03 (close-gate depth — hermetic-only, no triple-SHA infra gate)
    - 48-RESEARCH.md §Validation Architecture (Sampling Rate / Phase gate), §Environment Availability (hermetic — no live RabbitMQ/Redis)
    - 48-VALIDATION.md "Sampling Rate" (phase gate row)
  </read_first>
  <action>
    Execute the SC-4 close gate exactly per D-03 (hermetic-only — no live RabbitMQ/Redis; the in-memory MassTransit harness + FakeRedis double cover everything; NO triple-SHA psql/redis-cli/rabbitmqctl gate — that is Phase-49's job):

    (1) Run the full hermetic suite THREE consecutive times, each GREEN:
        `dotnet test SK_P.sln` ×3 (default Category, RealStack-excluded). Record pass/total for each of the three runs.
    (2) Build Release at 0 warnings: `dotnet build SK_P.sln -c Release` — assert 0 warnings (not just exit 0; scan output for "Warning(s)").
    (3) Build Debug at 0 warnings: `dotnet build SK_P.sln -c Debug` — assert 0 warnings.

    Capture the three suite results + both build warning-counts into the SUMMARY. If any of the three suite runs is RED or either build emits a warning, STOP and report — do NOT flip the gate green.

    Update `48-TEARDOWN-AUDIT.md`'s SC-4 row with the captured result (×3 GREEN counts + the two 0-warning build confirmations).
  </action>
  <verify>
    <automated>dotnet test SK_P.sln</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test SK_P.sln` run THREE consecutive times, each reporting Passed with 0 Failed (record the pass/total for each — e.g. "530/530 ×3").
    - `dotnet build SK_P.sln -c Release` exits 0 AND its output reports `0 Warning(s)`.
    - `dotnet build SK_P.sln -c Debug` exits 0 AND its output reports `0 Warning(s)`.
    - The SC-4 row in 48-TEARDOWN-AUDIT.md is updated with the captured ×3-green + 0-warning result.
  </acceptance_criteria>
  <done>The hermetic suite is GREEN ×3 consecutive and both Release and Debug builds are 0-warning; the SC-4 close gate is met and recorded in the audit ledger. (Live real-stack proof + triple-SHA net-zero are deferred to Phase 49 per D-03.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docs-only reconciliation + a build/test gate. No runtime trust boundary is created or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | Re-introduction of the retired reactive/keeper-dlq path | mitigate | Already mitigated by Plan 02's guards; this plan records the proof (audit ledger cites the green guards) and the SC-4 gate confirms the whole suite — including those guards — is green ×3. |
| T-48-04 | Repudiation | An audit row claiming coverage that no real green test backs (false traceability) | mitigate | Each ledger row cites a named test + its exact verify command; the acceptance criteria grep-assert the cited test names exist, and the SC-4 gate proves the suite (incl. those tests) is green — no row can claim un-backed coverage. |

Net security posture: **unchanged / surface-reducing** — reconciliation + gate only; the retirement itself reduces attack surface (research §Security Domain).
</threat_model>

<verification>
- `dotnet test SK_P.sln` GREEN ×3 consecutive (the D-03 close gate).
- `dotnet build SK_P.sln -c Release` and `-c Debug` both 0 warnings.
- 48-TEARDOWN-AUDIT.md every row resolves to a real green test; REQUIREMENTS.md RETIRE-01/02/03 satisfied; design doc carries A17 additively.
</verification>

<success_criteria>
- SC-4: full hermetic suite GREEN ×3 + Release AND Debug 0-warning build on the v4-only path with all retired machinery gone.
- D-04 fully delivered: audit ledger + REQUIREMENTS flips + A17 design-doc amendment.
- RETIRE-01, RETIRE-02, RETIRE-03 marked satisfied (01/02 verified-via-remnant-sweep, 03 removed).
</success_criteria>

<output>
After completion, create `.planning/phases/48-v3-x-teardown/48-03-SUMMARY.md` recording: the three suite results (pass/total each), the two build warning-counts, the audit ledger row→test mapping, and confirmation the three REQUIREMENTS flips + the A17 amendment landed.
</output>
