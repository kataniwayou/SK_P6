---
phase: 48-v3-x-teardown
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [RETIRE-03]
files_modified:
  - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs
  - src/Keeper/Consumers/FaultEntryStepDispatchConsumerDefinition.cs
  - src/Keeper/Consumers/FaultExecutionResultConsumer.cs
  - src/Keeper/Consumers/FaultExecutionResultConsumerDefinition.cs
  - src/Keeper/Recovery/KeeperRecoveryHandler.cs
  - src/Keeper/Observability/KeeperMetrics.cs
  - src/Keeper/Recovery/L2ProbeRecovery.cs
  - src/Keeper/Program.cs
  - src/Messaging.Contracts/KeeperQueues.cs
  - src/Keeper/ProbeOptions.cs
  - src/Keeper/appsettings.json
  - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
  - tests/BaseApi.Tests/Keeper/KeeperFaultConsumerScopeTests.cs
  - tests/BaseApi.Tests/Keeper/KeeperRecoverCapTests.cs
  - tests/BaseApi.Tests/Keeper/KeeperRoundRobinTests.cs
  - tests/BaseApi.Tests/Keeper/KeeperPausePublishTests.cs
  - tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs
  - tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs
  - tests/BaseApi.Tests/Keeper/KeeperHostBootFixture.cs
  - tests/BaseApi.Tests/Keeper/KeeperDependencyFirewallTests.cs
  - tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs

must_haves:
  truths:
    - "The reactive Fault<EntryStepDispatch>/Fault<ExecutionResult> consumers, their definitions, and KeeperRecoveryHandler no longer exist in src/Keeper/ (SC-3)."
    - "The keeper-dlq (KeeperQueues.DeadLetter) and keeper-fault-recovery (KeeperQueues.FaultRecovery) consts no longer exist; KeeperQueues.Recovery survives (SC-3)."
    - "L2ProbeRecovery exposes only ProbeOnceAsync (BitHealthLoop's live dep) — RunAsync, ProbeOutcome, and the ProbeOptions/KeeperMetrics ctor params are gone (SC-3/SC-4)."
    - "The whole solution still builds 0-warning in Debug with the reactive surface removed (SC-4 build-before-teardown invariant)."
    - "No dead config knob, metric, key-builder, or test references the retired path (SC-4 no dead remnants)."
  artifacts:
    - path: "src/Keeper/Recovery/L2ProbeRecovery.cs"
      provides: "v4 BIT-probe helper — ProbeOnceAsync only, single IConnectionMultiplexer ctor param"
      contains: "ProbeOnceAsync"
    - path: "src/Messaging.Contracts/KeeperQueues.cs"
      provides: "v4 queue consts — Recovery only"
      contains: "Recovery = \"keeper-recovery\""
    - path: "src/Keeper/Program.cs"
      provides: "Keeper composition root with the reactive consumer/handler/metrics wiring removed, the five keeper-recovery consumers + BitHealthLoop kept"
  key_links:
    - from: "src/Keeper/Health/BitHealthLoop.cs"
      to: "src/Keeper/Recovery/L2ProbeRecovery.cs"
      via: "probe.ProbeOnceAsync(stoppingToken)"
      pattern: "ProbeOnceAsync"
    - from: "src/Keeper/Program.cs"
      to: "Keeper.Recovery.{Update,Reinject,Inject,Delete,Cleanup}Consumer"
      via: "x.AddConsumer<...>() in AddBaseConsoleMessaging"
      pattern: "AddConsumer<Keeper.Recovery"
---

<objective>
Delete the v3.x reactive `Fault<T>` Keeper recovery path (RETIRE-03) and sweep every now-orphaned remnant in ONE atomic, build-green wave: the five reactive source files + the orphaned `KeeperMetrics.cs`, the `L2ProbeRecovery` partial-delete, the Program.cs unwiring, the retired queue consts + dead config/key/metric surface, and every test that references the deleted symbols (which share the `BaseApi.Tests` compilation unit and would otherwise break the build).

Purpose: Leave the solution buildable and green on the v4 path alone, with no dead `Ignore<>`/binding/key/metric remnants (SC-3 + SC-4).
Output: The reactive surface gone; `dotnet build SK_P.sln -c Debug` at 0 warnings; the v4 keep-set (5-state `keeper-recovery` consumers, `BitHealthLoop`, `L2HealthGate`, pause/resume) untouched.

CRITICAL — this is ONE wave / one logical commit (optionally two sub-commits, each of which MUST build). Per research §7 the deletes and the dependent test deletes/edits CANNOT be separated: deleting source then building before deleting the dependent tests leaves `BaseApi.Tests` non-compiling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-v3-x-teardown/48-RESEARCH.md
@.planning/phases/48-v3-x-teardown/48-PATTERNS.md
@.planning/phases/48-v3-x-teardown/48-CONTEXT.md

<interfaces>
<!-- Surviving v4 contracts the executor MUST NOT break. Extracted from research §1E / §3 / PATTERNS. -->

SURVIVING — do NOT touch (confirmed v4-shared by grep, research §1E/§2):
- src/Keeper/RecoveryOptions.cs — BOTH PartitionCount + GateWaitSeconds are read by the 5-state recovery consumers (UpdateConsumerDefinition, RecoveryConsumerBase). KEEP the whole file + the "Recovery" appsettings section. (Corrects the 48-CONTEXT "likely reactive-only orphan" hypothesis.)
- src/Keeper/BackupOptions.cs (TtlDays) — v4 composite-backup TTL.
- src/Keeper/ProbeOptions.cs DelaySeconds + MaxAttempts — read by BitHealthLoop:28 + ProbeOptionsBoundTests. KEEP both; remove ONLY RecoverAttemptCap.
- Messaging.Contracts/PauseWorkflow.cs + ResumeWorkflow.cs positional `string H` — the v4 BIT-gate key, NOT the retired dedup H. Do NOT remove.
- L2ProjectionKeys.KeeperProbe + ExecutionData(Guid) — used by the kept ProbeOnceAsync / v4 data path.

KEPT L2ProbeRecovery ctor + method shape after edit (PATTERNS lines 183-202):
```csharp
public sealed class L2ProbeRecovery(IConnectionMultiplexer redis)
{
    private static readonly Guid BitProbeEntryId = Guid.Empty;
    private const string BitProbeH = "bit";
    public async Task<bool> ProbeOnceAsync(CancellationToken ct, Guid? entryId = null, string? h = null) { ... }
}
```

KEPT Program.cs v4 consumer block (PATTERNS lines 160-166) — must survive verbatim:
```csharp
x.AddConsumer<Keeper.Recovery.UpdateConsumer,   Keeper.Recovery.UpdateConsumerDefinition>();
x.AddConsumer<Keeper.Recovery.ReinjectConsumer, Keeper.Recovery.ReinjectConsumerDefinition>();
x.AddConsumer<Keeper.Recovery.InjectConsumer,   Keeper.Recovery.InjectConsumerDefinition>();
x.AddConsumer<Keeper.Recovery.DeleteConsumer,   Keeper.Recovery.DeleteConsumerDefinition>();
x.AddConsumer<Keeper.Recovery.CleanupConsumer,  Keeper.Recovery.CleanupConsumerDefinition>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the reactive source + orphaned metrics + their dependent tests (atomic — same compilation unit)</name>
  <files>src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs, src/Keeper/Consumers/FaultEntryStepDispatchConsumerDefinition.cs, src/Keeper/Consumers/FaultExecutionResultConsumer.cs, src/Keeper/Consumers/FaultExecutionResultConsumerDefinition.cs, src/Keeper/Recovery/KeeperRecoveryHandler.cs, src/Keeper/Observability/KeeperMetrics.cs, src/Keeper/Recovery/L2ProbeRecovery.cs, src/Keeper/Program.cs, tests/BaseApi.Tests/Keeper/KeeperFaultConsumerScopeTests.cs, tests/BaseApi.Tests/Keeper/KeeperRecoverCapTests.cs, tests/BaseApi.Tests/Keeper/KeeperRoundRobinTests.cs, tests/BaseApi.Tests/Keeper/KeeperPausePublishTests.cs, tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs, tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs, tests/BaseApi.Tests/Keeper/KeeperHostBootFixture.cs, tests/BaseApi.Tests/Keeper/KeeperDependencyFirewallTests.cs</files>
  <read_first>
    - src/Keeper/Program.cs (the composition root being unwired)
    - src/Keeper/Recovery/L2ProbeRecovery.cs (the partial-delete target)
    - src/Keeper/Recovery/KeeperRecoveryHandler.cs, src/Keeper/Consumers/Fault*.cs, src/Keeper/Observability/KeeperMetrics.cs (the delete targets)
    - 48-RESEARCH.md §1A (DELETE list), §1C (KeeperMetrics whole-delete + Planner note/A1), §2 (L2ProbeRecovery member boundary + the ctor-param/KeeperMetrics same-commit trap), §3 (Program.cs unwiring), §4 (test inventory + the compile-break warning), §7 sub-commit 1
    - 48-PATTERNS.md "EDIT: src/Keeper/Program.cs", "EDIT: src/Keeper/Recovery/L2ProbeRecovery.cs", "Whole-File Deletes"
    - tests/BaseApi.Tests/Keeper/KeeperHostBootFixture.cs, KeeperDependencyFirewallTests.cs (the two test EDITs in this task)
  </read_first>
  <action>
    DELETE these whole files (research §1A/§1C + §4):
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumerDefinition.cs
    - src/Keeper/Consumers/FaultExecutionResultConsumer.cs
    - src/Keeper/Consumers/FaultExecutionResultConsumerDefinition.cs
    - src/Keeper/Recovery/KeeperRecoveryHandler.cs
    - src/Keeper/Observability/KeeperMetrics.cs (the `KeeperMetrics` class AND the `KeeperMetricTags` static class in the same file — research §1C/A1: whole-file delete, there is NO v4 meter usage to preserve; this is the deliberate deviation from D-02's literal "keep the v4 meter" wording, grep-proven)
    - tests/BaseApi.Tests/Keeper/KeeperFaultConsumerScopeTests.cs
    - tests/BaseApi.Tests/Keeper/KeeperRecoverCapTests.cs
    - tests/BaseApi.Tests/Keeper/KeeperRoundRobinTests.cs
    - tests/BaseApi.Tests/Keeper/KeeperPausePublishTests.cs
    - tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs (no ProbeOnceAsync-only coverage exists to salvage — research §4; every fact calls RunAsync or AddConsumer<FaultEntryStepDispatchConsumer>)
    - tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs (orphaned by the KeeperMetrics.cs delete)

    EDIT src/Keeper/Recovery/L2ProbeRecovery.cs (partial-delete per §1B/§2):
    - Remove the `public enum ProbeOutcome { ... }`.
    - Remove the whole `RunAsync(Guid entryId, string h, string procId, CancellationToken ct)` method.
    - Reduce the ctor to a SINGLE param `IConnectionMultiplexer redis` — remove the `IOptions<ProbeOptions> opts` and `KeeperMetrics metrics` ctor params.
    - Remove `using Keeper.Observability;` and `using Microsoft.Extensions.Options;` (now unused). KEEP `using Messaging.Contracts.Projections;` + `using StackExchange.Redis;`.
    - KEEP `ProbeOnceAsync`, the `BitProbeEntryId`/`BitProbeH` sentinels. Reword the class doc-comment to the BIT-only role (drop the "awaited inside Consume … RunAsync passes the real fault-context values" prose, §1D). Final shape MUST match the <interfaces> block above.

    EDIT src/Keeper/Program.cs (unwire per §3 / PATTERNS):
    - Remove `builder.Services.AddSingleton<Keeper.Recovery.KeeperRecoveryHandler>();`
    - Remove `builder.Services.AddSingleton<KeeperMetrics>();` AND `builder.Services.ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(KeeperMetrics.MeterName));` (the whole KMET-01 block incl. its comment).
    - Remove `x.AddConsumer<FaultEntryStepDispatchConsumer, FaultEntryStepDispatchConsumerDefinition>();` from the AddBaseConsoleMessaging lambda — leave the five v4 keeper-recovery AddConsumer lines (see <interfaces>) intact.
    - Remove the now-unused usings: `using Keeper.Consumers;`, `using Keeper.Observability;`, and `using OpenTelemetry.Metrics;` (only used by the removed ConfigureOpenTelemetryMeterProvider call).
    - Reword the L2ProbeRecovery registration comment "Both fault consumers depend on it" → "the v4 BitHealthLoop's L2 probe helper."
    - KEEP intact: `Configure<RecoveryOptions>`, `AddSingleton<L2ProbeRecovery>()`, `AddSingleton<IL2HealthGate, L2HealthGate>()`, `AddHostedService<BitHealthLoop>()`, the five keeper-recovery consumers, and `Configure<ProbeOptions>`/`<BackupOptions>`/`<RetryOptions>`.

    EDIT tests/BaseApi.Tests/Keeper/KeeperHostBootFixture.cs (research §4 + Open Q1):
    - Remove the two reactive consumer registrations (`AddConsumer<FaultEntryStepDispatchConsumer...>` and any `AddConsumer<FaultExecutionResultConsumer...>` if present) and the `AddSingleton<KeeperRecoveryHandler>()` singleton.
    - DECISION (resolving research Open Q1): use the MINIMAL edit — drop only the reactive registrations; do NOT add the five keeper-recovery consumers. Keep the AddBaseConsole* seam + ProbeOptions/RetryOptions binding + `L2ProbeRecovery`. Rationale: KeeperHostBootTests only asserts `IBusControl` resolves, so the no-reactive-consumers form passes, and it keeps the fixture edit minimal/low-risk for a deletion phase.

    EDIT tests/BaseApi.Tests/Keeper/KeeperDependencyFirewallTests.cs (research §4 + PATTERNS):
    - Re-anchor `KeeperAssembly` from `typeof(global::Keeper.Consumers.FaultEntryStepDispatchConsumer).Assembly` → `typeof(global::Keeper.Health.BitHealthLoop).Assembly` (the type being deleted stops compiling; use the same surviving anchor the new guard will use for consistency).

    DO NOT in this task touch KeeperQueues.cs, ProbeOptions.cs, appsettings.json, L2ProjectionKeys.cs, or KeeperDlqConsolidationTests.cs — those consts/keys are still referenced by code present at this point ONLY via the now-deleted files, but `KeeperQueues.DeadLetter` is also asserted by KeeperDlqConsolidationTests which still compiles here. Defer the const removal to Task 2 so this task's intermediate state builds.

    NOTE on build-order: removing the L2ProbeRecovery ctor params + deleting KeeperMetrics.cs + removing the Program.cs `AddSingleton<KeeperMetrics>` MUST all be in this single task (DI resolves L2ProbeRecovery by the surviving IConnectionMultiplexer singleton). Doing the ctor change without the metrics delete would leave a registered-but-dead meter (an SC-4 remnant); doing the metrics delete without the ctor change won't compile.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs` (and its Definition, the two FaultExecutionResult files, KeeperRecoveryHandler.cs, KeeperMetrics.cs) — all absent.
    - `! test -f tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs` (and the five other deleted test files) — all absent.
    - `grep -rn "KeeperRecoveryHandler\|RunAsync\|ProbeOutcome\|KeeperMetrics" src/Keeper/` returns NO matches (the reactive symbols are gone; ProbeOnceAsync remains).
    - `grep -n "ProbeOnceAsync" src/Keeper/Recovery/L2ProbeRecovery.cs` returns a match AND the ctor reads `L2ProbeRecovery(IConnectionMultiplexer redis)` (single param).
    - `grep -c "AddConsumer<Keeper.Recovery" src/Keeper/Program.cs` returns 5 (the v4 consumers survive); `grep -c "FaultEntryStepDispatchConsumer" src/Keeper/Program.cs` returns 0.
    - `dotnet build SK_P.sln -c Debug` exits 0 with 0 warnings (the build-before-teardown invariant — the intermediate compiles).
  </acceptance_criteria>
  <done>The reactive source + KeeperMetrics + their six dependent tests are deleted, L2ProbeRecovery is reduced to ProbeOnceAsync only, Program.cs is unwired, the two test edits (fixture + firewall re-anchor) land, and `dotnet build SK_P.sln -c Debug` is 0-warning green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the now-unreferenced consts + dead config/key, with their last test referencer (atomic)</name>
  <files>src/Messaging.Contracts/KeeperQueues.cs, src/Keeper/ProbeOptions.cs, src/Keeper/appsettings.json, src/Messaging.Contracts/Projections/L2ProjectionKeys.cs, tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs</files>
  <read_first>
    - src/Messaging.Contracts/KeeperQueues.cs (const removal + the dangling see-cref fix)
    - src/Keeper/ProbeOptions.cs + src/Keeper/appsettings.json (RecoverAttemptCap removal)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (KeeperRecoverAttempts builder removal — KEEP KeeperProbe + ExecutionData)
    - tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs (the DeadLetter assertions to drop — the last referencer)
    - 48-RESEARCH.md §1B (KeeperQueues/ProbeOptions/appsettings rows), §1D (L2ProjectionKeys.KeeperRecoverAttempts), §7 sub-commit 2 (the const+last-referencer same-commit rule)
    - 48-PATTERNS.md "EDIT: KeeperQueues.cs", "EDIT: ProbeOptions.cs", "EDIT: L2ProjectionKeys.cs", "EDIT: KeeperDlqConsolidationTests.cs"
  </read_first>
  <action>
    Now that Task 1 deleted every referencer of these consts (KeeperRecoveryHandler, the two Definitions, the KeeperMetrics comment), they can be removed and the build will still compile.

    EDIT src/Messaging.Contracts/KeeperQueues.cs (§1B):
    - Remove the `FaultRecovery` const + its doc-comment.
    - Remove the `DeadLetter` const + its doc-comment.
    - Fix the dangling `<see cref="FaultRecovery"/>` in the `Recovery` const's doc-comment (it will not compile once FaultRecovery is gone) — reword to drop the cref. KEEP `Recovery = "keeper-recovery"`. Final file should match the PATTERNS "kept file" excerpt.

    EDIT tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs (§4 — the LAST referencer of DeadLetter; MUST be same commit as the const removal):
    - Remove the two assertions referencing the deleted const: `Assert.Equal("keeper-dlq", KeeperQueues.DeadLetter);` and `Assert.NotEqual(ConsolidatedErrorTransportFilter.Dlq1, KeeperQueues.DeadLetter);`.
    - Scrub the doc-comment mention of "DLQ-2 (keeper-dlq)".
    - KEEP the v4 facts (`Dlq1_Consolidated`, `Keeper_SendFault_RetriesToDlq1`, `ProcessorSendExhaustion_RoutesToDlq1`) and the remaining `Dlq1` const + TTL-math assertions — they MUST stay green.

    EDIT src/Keeper/ProbeOptions.cs (§1B):
    - Remove `RecoverAttemptCap` + its doc-comment. KEEP `DelaySeconds` + `MaxAttempts` (read by BitHealthLoop + ProbeOptionsBoundTests — MaxAttempts is the documented broker-timeout invariant, research A3). Final class should match the PATTERNS excerpt.

    EDIT src/Keeper/appsettings.json (§1B):
    - Remove the `"RecoverAttemptCap": 3` key under `"Probe"`. KEEP `Probe.DelaySeconds`/`MaxAttempts`, the whole `Recovery` section, and the `Backup` section.

    EDIT src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (§1D):
    - Remove the `KeeperRecoverAttempts` builder (`skp:keeper:attempts:{h}`) + its doc-comment — referenced ONLY by the deleted KeeperRecoveryHandler. KEEP `KeeperProbe` (used by ProbeOnceAsync) and `ExecutionData(Guid)` (the GUID-only data builder).

    DO NOT touch RecoveryOptions.cs / the appsettings "Recovery" section (confirmed v4-shared, research §1E/Pitfall 1) or BackupOptions.TtlDays.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "FaultRecovery\|DeadLetter" src/Messaging.Contracts/KeeperQueues.cs` returns NO matches; `grep -n "keeper-recovery" src/Messaging.Contracts/KeeperQueues.cs` returns the surviving `Recovery` const.
    - `grep -rn "keeper-dlq\|keeper-fault-recovery\|KeeperQueues.DeadLetter\|KeeperQueues.FaultRecovery" src/` returns NO matches.
    - `grep -n "RecoverAttemptCap" src/Keeper/ProbeOptions.cs src/Keeper/appsettings.json` returns NO matches; `grep -c "DelaySeconds\|MaxAttempts" src/Keeper/ProbeOptions.cs` returns ≥ 2 (both kept).
    - `grep -n "KeeperRecoverAttempts" src/Messaging.Contracts/Projections/L2ProjectionKeys.cs` returns NO matches; `grep -n "KeeperProbe\|ExecutionData" ...` still present.
    - `grep -n "RecoveryOptions\|GateWaitSeconds\|PartitionCount" src/Keeper/RecoveryOptions.cs` still present (the v4-shared file untouched).
    - `dotnet build SK_P.sln -c Debug` exits 0 with 0 warnings.
  </acceptance_criteria>
  <done>The `FaultRecovery`/`DeadLetter` consts, `RecoverAttemptCap` (source + appsettings), and the `KeeperRecoverAttempts` builder are removed; the KeeperDlqConsolidationTests DeadLetter assertions are dropped in the same change; RecoveryOptions/BackupOptions untouched; `dotnet build SK_P.sln -c Debug` is 0-warning green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ → Keeper consumers | Inbound bus messages cross here. This phase REMOVES one consumer (`Fault<EntryStepDispatch>`) and one queue (`keeper-dlq`) — it reduces, never adds, surface across this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | The retired reactive recovery / `keeper-dlq` path (silent re-introduction in a future edit) | mitigate | The Phase-48 negative-guard facts (added in Plan 02) fail the build if a `Fault<T>` consumer or `keeper-dlq` literal returns. This plan performs the removal that makes the guard meaningful; the guard itself enforces it. |
| T-48-02 | Information disclosure | The deleted `KeeperRecoveryHandler` structured-logged exception text | accept | n/a after deletion — the only logging site is removed and no new logging is added. Net posture is surface-reducing (research §Security Domain). |

Net security posture: **unchanged / surface-reducing** — pure deletion, one fewer consumer + one fewer queue, no new input/auth/crypto/data flow (research §Security Domain).
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` is 0-warning green after BOTH tasks (the build-before-teardown invariant holds at every intermediate).
- `dotnet test SK_P.sln` (full hermetic suite) is GREEN — no v4 regression from the deletions (run at wave merge per VALIDATION.md sampling).
- The v4 keep-set survives: `grep -c "AddConsumer<Keeper.Recovery" src/Keeper/Program.cs` == 5, `AddHostedService<BitHealthLoop>` present, `RecoveryOptions.cs` untouched, `L2ProbeRecovery.ProbeOnceAsync` present.
</verification>

<success_criteria>
- SC-3: the reactive `Fault<T>` consumers + `KeeperRecoveryHandler` + `keeper-dlq`/`keeper-fault-recovery` consts are removed from `src/`; the v4 path is the sole recovery mechanism.
- SC-4 (partial — build half): the solution builds 0-warning in Debug with all retired machinery gone and no dead config/metric/key remnant. (The ×3 green + Release build gate is owned by Plan 03.)
</success_criteria>

<output>
After completion, create `.planning/phases/48-v3-x-teardown/48-01-SUMMARY.md` recording: exact files deleted/edited, the Open-Q1 fixture decision taken, confirmation that RecoveryOptions/BackupOptions were left untouched, and the Debug-build-green result.
</output>
