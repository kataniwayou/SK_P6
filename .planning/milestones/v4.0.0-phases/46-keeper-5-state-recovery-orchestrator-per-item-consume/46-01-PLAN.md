---
phase: 46-keeper-5-state-recovery-orchestrator-per-item-consume
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/BaseConsole.Core/Resilience/RetryLoop.cs
  - src/BaseProcessor.Core/Resilience/RetryLoop.cs
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
  - src/Messaging.Contracts/KeeperReinject.cs
  - tests/BaseApi.Tests/Processor/RetryLoopFacts.cs
  - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs
  - tests/BaseApi.Tests/Keeper/UpdateConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/CleanupConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryGateWaitFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs
  - tests/BaseApi.Tests/Orchestrator/TypedResultConsumerFacts.cs
autonomous: true
requirements: [KEEP-04, KEEP-05, KEEP-06, KEEP-07, KEEP-08, KEEP-09, ORCH-01]

must_haves:
  truths:
    - "RetryLoop and RetryOutcome<T> live in BaseConsole.Core.Resilience and both BaseProcessor.Core and Keeper can reference them"
    - "KeeperReinject carries a Payload field so a recovered run preserves its author config"
    - "ProcessorPipeline.BuildReinject stamps the inbound dispatch Payload onto KeeperReinject"
    - "The KeeperReinject golden/contract test pins the new Payload field"
    - "Every Phase-46 behavior has a RED (failing/skipped-pending) test stub carrying [Trait(\"Phase\",\"46\")] so the --filter \"Phase=46\" run discovers them"
  artifacts:
    - path: "src/BaseConsole.Core/Resilience/RetryLoop.cs"
      provides: "Relocated bounded retry helper + RetryOutcome<T> in BaseConsole.Core.Resilience namespace"
      contains: "namespace BaseConsole.Core.Resilience"
    - path: "src/Messaging.Contracts/KeeperReinject.cs"
      provides: "REINJECT contract with Payload"
      contains: "public string Payload"
    - path: "tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs"
      provides: "RED stub for KEEP-06 ordered ops"
      contains: "Phase"
  key_links:
    - from: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs"
      to: "src/BaseConsole.Core/Resilience/RetryLoop.cs"
      via: "using BaseConsole.Core.Resilience"
      pattern: "using BaseConsole\\.Core\\.Resilience"
    - from: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs"
      to: "src/Messaging.Contracts/KeeperReinject.cs"
      via: "BuildReinject stamps Payload = d.Payload"
      pattern: "Payload\\s*=\\s*d\\.Payload"
---

<objective>
Lay the Phase-46 foundation that every later wave builds on: (1) relocate the shared `RetryLoop` from `BaseProcessor.Core` to `BaseConsole.Core` so Keeper (whose csproj firewall references only `BaseConsole.Core` + `Messaging.Contracts`) can reuse the ONE A3 retry implementation (D-05); (2) execute the D-01 `Payload` ripple as ONE atomic edit across the contract, the processor send site, and the golden test (Pitfall 5); (3) create every Wave-0 test stub so each downstream task has an automated `<automated>` verify (Dimension 8 / Nyquist).

Purpose: D-05 and D-01 are hard prerequisites — the Keeper bodies (Wave 1) call the relocated `RetryLoop` and the relocated namespace, and `ReinjectConsumer` reads `KeeperReinject.Payload`. The contract/producer/test must change together or a recovered run silently loses author config.
Output: Relocated `RetryLoop.cs`, edited `KeeperReinject.cs` + `ProcessorPipeline.BuildReinject`, extended `KeeperContractTests`, and 8 new RED test-stub files + the `RetryLoopFacts` using-update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-keeper-5-state-recovery-orchestrator-per-item-consume/46-CONTEXT.md
@.planning/phases/46-keeper-5-state-recovery-orchestrator-per-item-consume/46-RESEARCH.md
@.planning/phases/46-keeper-5-state-recovery-orchestrator-per-item-consume/46-PATTERNS.md
@.planning/phases/46-keeper-5-state-recovery-orchestrator-per-item-consume/46-VALIDATION.md

<interfaces>
RetryLoop (CURRENT, src/BaseProcessor.Core/Resilience/RetryLoop.cs — move verbatim, change ONLY the namespace):
```csharp
namespace BaseProcessor.Core.Resilience;   // → becomes BaseConsole.Core.Resilience
public static class RetryLoop {
    public static async Task<RetryOutcome<T>> ExecuteAsync<T>(Func<Task<T>> op, int limit, CancellationToken ct) { ... }
}
public readonly record struct RetryOutcome<T>(bool Succeeded, T? Value, Exception? Error) {
    public static RetryOutcome<T> Ok(T value) => new(true, value, null);
    public static RetryOutcome<T> Exhausted(Exception error) => new(false, default, error);
}
```

KeeperReinject (CURRENT, src/Messaging.Contracts/KeeperReinject.cs):
```csharp
public sealed record KeeperReinject(Guid WorkflowId, Guid StepId, Guid ProcessorId) : IKeeperRecoverable {
    public Guid CorrelationId { get; init; }
    public Guid ExecutionId   { get; init; }
    public Guid EntryId       { get; init; }   // D-11: REINJECT-only extra
}
```

KeeperUpdate.ValidatedData shape to MIRROR (src/Messaging.Contracts/KeeperUpdate.cs:11):
`public string ValidatedData { get; init; } = "";`

EntryStepDispatch ctor (the REINJECT reconstruction target, src/Messaging.Contracts/EntryStepDispatch.cs:11-16):
`public sealed record EntryStepDispatch(Guid WorkflowId, Guid StepId, Guid ProcessorId, string Payload) : IExecutionCorrelated { Guid CorrelationId/ExecutionId/EntryId init; }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate RetryLoop to BaseConsole.Core and fix the two real using sites (D-05)</name>
  <files>src/BaseConsole.Core/Resilience/RetryLoop.cs, src/BaseProcessor.Core/Resilience/RetryLoop.cs, src/BaseProcessor.Core/Processing/ProcessorPipeline.cs, tests/BaseApi.Tests/Processor/RetryLoopFacts.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Resilience/RetryLoop.cs (the file being moved — copy verbatim)
    - 46-PATTERNS.md Shared Pattern A (relocation note, Pitfall 6)
    - 46-RESEARCH.md Pitfall 6 (confirmed dependents list)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (line 3 — the real using)
    - tests/BaseApi.Tests/Processor/RetryLoopFacts.cs (line 1 — the test using)
    - src/Keeper/Keeper.csproj (the firewall: confirm it references BaseConsole.Core; do NOT add BaseProcessor.Core)
    - src/BaseProcessor.Core/BaseProcessor.Core.csproj (confirm it references BaseConsole.Core — move is non-circular)
  </read_first>
  <action>
    Create src/BaseConsole.Core/Resilience/RetryLoop.cs containing BOTH `RetryLoop` (static class) and `RetryOutcome<T>` (readonly record struct) copied verbatim from src/BaseProcessor.Core/Resilience/RetryLoop.cs, changing ONLY the namespace line `namespace BaseProcessor.Core.Resilience;` → `namespace BaseConsole.Core.Resilience;`. Then DELETE src/BaseProcessor.Core/Resilience/RetryLoop.cs (use the Write tool for the new file; delete the old file via Bash `Remove-Item`).
    Update the TWO real dependents:
    (a) src/BaseProcessor.Core/Processing/ProcessorPipeline.cs line 3: `using BaseProcessor.Core.Resilience;` → `using BaseConsole.Core.Resilience;`. Verify no other type in `BaseProcessor.Core.Resilience` (notably `KeyAbsentException`) is still referenced via that using — `KeyAbsentException` STAYS in BaseProcessor.Core, so if ProcessorPipeline.cs also uses it, ADD a second `using BaseProcessor.Core.Resilience;` line back (both usings coexist).
    (b) tests/BaseApi.Tests/Processor/RetryLoopFacts.cs line 1: `using BaseProcessor.Core.Resilience;` → `using BaseConsole.Core.Resilience;`.
    Do NOT move KeyAbsentException.cs — it is a processor-specific Pre-read sentinel and stays in BaseProcessor.Core.Resilience. Do NOT touch src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs (line 174 is a doc-comment mention only, no code dependency).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~RetryLoopFacts"</automated>
  </verify>
  <acceptance_criteria>
    - src/BaseConsole.Core/Resilience/RetryLoop.cs exists and contains `namespace BaseConsole.Core.Resilience` and both `class RetryLoop` and `record struct RetryOutcome`
    - src/BaseProcessor.Core/Resilience/RetryLoop.cs no longer exists
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs contains `using BaseConsole.Core.Resilience;`
    - tests/BaseApi.Tests/Processor/RetryLoopFacts.cs line 1 contains `using BaseConsole.Core.Resilience;`
    - `dotnet test --filter "FullyQualifiedName~RetryLoopFacts"` exits 0 (existing facts green from the new namespace)
    - `dotnet build` of the solution succeeds (no CS0246 on RetryLoop/RetryOutcome anywhere)
  </acceptance_criteria>
  <done>RetryLoop + RetryOutcome live in BaseConsole.Core.Resilience; ProcessorPipeline and RetryLoopFacts compile and RetryLoopFacts is green; KeyAbsentException untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-01 Payload ripple — contract + BuildReinject + golden test (one atomic edit)</name>
  <files>src/Messaging.Contracts/KeeperReinject.cs, src/BaseProcessor.Core/Processing/ProcessorPipeline.cs, tests/BaseApi.Tests/Contracts/KeeperContractTests.cs</files>
  <read_first>
    - src/Messaging.Contracts/KeeperReinject.cs (the contract being edited)
    - src/Messaging.Contracts/KeeperUpdate.cs (line 11 — the ValidatedData field shape to mirror exactly)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (lines ~201-202, the BuildReinject helper)
    - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs (lines ~60-64, the KeeperReinject_carries_EntryId pin test)
    - 46-PATTERNS.md "KeeperReinject.cs (EDIT, D-01)" + "ProcessorPipeline.cs (EDIT BuildReinject, D-01)"
    - 46-RESEARCH.md Pitfall 5 (the three edits as one atomic task)
  </read_first>
  <action>
    (1) In src/Messaging.Contracts/KeeperReinject.cs, add this member to the record body (mirror KeeperUpdate.ValidatedData exactly — `string`, `init`-only, `= ""` default):
    `    public string Payload { get; init; } = "";   // D-01: REINJECT carries the step config for faithful EntryStepDispatch reconstruction`
    Add it after the `EntryId` line. No other change to the record; do NOT add it to the positional ctor (it is an init property like CorrelationId/ExecutionId/EntryId).
    (2) In src/BaseProcessor.Core/Processing/ProcessorPipeline.cs `BuildReinject` (the helper at ~line 201-202 that constructs `new KeeperReinject(...) { ... }`), add `Payload = d.Payload` to the object initializer (where `d` is the inbound `EntryStepDispatch`). Final shape:
    `new(d.WorkflowId, d.StepId, d.ProcessorId) { CorrelationId = d.CorrelationId, ExecutionId = d.ExecutionId, EntryId = d.EntryId, Payload = d.Payload };`
    (Read the actual BuildReinject signature first — match its exact ctor args and existing initializer members; just ADD `Payload = d.Payload`.)
    (3) In tests/BaseApi.Tests/Contracts/KeeperContractTests.cs, extend the existing `KeeperReinject_carries_EntryId...` test (~line 60-64): add `Assert.NotNull(typeof(KeeperReinject).GetProperty("Payload"));` and assert it is a `string` init-only property: `Assert.Equal(typeof(string), typeof(KeeperReinject).GetProperty("Payload")!.PropertyType);`. Keep the existing `[Trait(...)]`/test attributes unchanged.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~KeeperContractTests"</automated>
  </verify>
  <acceptance_criteria>
    - src/Messaging.Contracts/KeeperReinject.cs contains `public string Payload { get; init; } = "";`
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs BuildReinject contains `Payload = d.Payload`
    - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs contains `GetProperty("Payload")`
    - `dotnet test --filter "FullyQualifiedName~KeeperContractTests"` exits 0
  </acceptance_criteria>
  <done>KeeperReinject carries Payload; BuildReinject stamps it; the golden test pins it; all three change in one commit (Pitfall 5).</done>
</task>

<task type="auto">
  <name>Task 3: Create all Wave-0 RED test stubs for the Keeper + Orchestrator consumers</name>
  <files>tests/BaseApi.Tests/Keeper/UpdateConsumerFacts.cs, tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs, tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs, tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs, tests/BaseApi.Tests/Keeper/CleanupConsumerFacts.cs, tests/BaseApi.Tests/Keeper/RecoveryGateWaitFacts.cs, tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs, tests/BaseApi.Tests/Orchestrator/TypedResultConsumerFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (the substituted IDatabase / CapturingSendProvider / Retry/Metrics helpers to reuse — note CapturingSendProvider ~line 253-275)
    - tests/BaseApi.Tests/Processor/PipelinePreFacts.cs (the consumer-fact style + L2 fake patterns: PresentReadWriteDeleteOkL2 / ReadFaultL2 / AbsentReadL2)
    - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs (the [Trait] attribute convention used on existing tests)
    - 46-VALIDATION.md (the per-requirement test map + Wave 0 Requirements list — each stub maps to one requirement)
    - 46-PATTERNS.md "Tests" table (what each fact must eventually assert)
  </read_first>
  <action>
    Create 8 NEW test files as RED stubs. Each file: a public test class in the matching namespace (Keeper facts under a Keeper test namespace, the orchestrator fact under an Orchestrator test namespace), each test method carrying `[Trait("Phase","46")]` (so `dotnet test --filter "Phase=46"` discovers them) PLUS the xunit.v3 fact attribute the codebase uses (match the attribute KeeperContractTests uses — `[Fact]`). Each test method body must be a RED placeholder that FAILS or is explicitly pending: use `Assert.Fail("Phase 46 Wave 0 stub — implemented in 46-02/46-03/46-04");` so the test is discovered and red until the implementing task makes it green. Use these EXACT class/method names so the VALIDATION filters resolve:
    - UpdateConsumerFacts: `Update_writes_composite_with_TTL_only_when_gate_open` (KEEP-04, filter `FullyQualifiedName~UpdateConsumer`)
    - ReinjectConsumerFacts: `Reinject_present_sends_EntryStepDispatch_with_Payload` and `Reinject_absent_throws_RecoveryDataGone` (KEEP-05, filter `~ReinjectConsumer`)
    - InjectConsumerFacts: `Inject_reads_then_writes_then_sends_then_deletes_in_order` (KEEP-06, filter `~InjectConsumer`; this one must eventually use `Received.InOrder`)
    - DeleteConsumerFacts: `Delete_deletes_execution_data_key` (KEEP-07, filter `~DeleteConsumer`)
    - CleanupConsumerFacts: `Cleanup_deletes_composite_backup` (KEEP-08, filter `~CleanupConsumer`)
    - RecoveryGateWaitFacts: `GateWait_blocks_until_gate_opens` and `GateWait_bound_exhaustion_throws_transient` (KEEP-09/D-03, filter `~GateWait`)
    - RecoveryPartitionFacts: `Partition_key_is_four_tuple_excluding_StepId` (KEEP-09, filter `~Partition`)
    - TypedResultConsumerFacts: `TypedResultConsumer_advances_via_SelectNext_outcome` and `Injected_StepCompleted_indistinguishable_from_direct` (ORCH-01, filter `~TypedResultConsumer`)
    Do NOT reference types that do not exist yet (RecoveryConsumerBase, UpdateConsumer, TypedResultConsumer, etc.) — keep the stubs free of unbuilt symbols so the test project COMPILES now; the implementing tasks will rewrite each stub body against the real types. (A stub that references an unbuilt type would break the whole test build and block every other Wave-0 verify.)
    Add the `[Trait("Phase","46")]` extension to the two tests touched in Task 2 (KeeperContractTests Payload pin) ONLY if KeeperContractTests does not already carry a phase trait — match its existing convention; do not duplicate.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "Phase=46"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 new files exist under tests/BaseApi.Tests/Keeper/ and tests/BaseApi.Tests/Orchestrator/
    - Each contains `[Trait("Phase","46")]` and references NO not-yet-built production type (test project compiles)
    - `dotnet test --filter "Phase=46"` DISCOVERS and runs the stubs and reports them RED (failing) — non-zero exit is expected and acceptable for this scaffolding task; the key proof is that the run finds >= 11 Phase=46 tests
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj` succeeds (no compile error)
  </acceptance_criteria>
  <done>Every Phase-46 requirement has a discoverable RED test stub under [Trait("Phase","46")]; the test project compiles; downstream tasks have a concrete `<automated>` target to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor → bus (KeeperReinject) | The processor stamps Payload onto the recovery envelope; trusted internal producer (own processor). No external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | KeeperReinject.Payload on the wire | accept | Internal bus envelope from a trusted producer (own processor); payload schema validation is the processor's concern (Phase 44), not introduced here. ASVS V5 partial — no new external surface. |
| T-46-02 | Information Disclosure | Payload now carried on the recovery envelope | accept | Same internal broker as the existing dispatch already carrying Payload; no new exposure (it already rode EntryStepDispatch). |
</threat_model>

<verification>
- `dotnet build` of the full solution succeeds (proves the RetryLoop relocation broke nothing).
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~RetryLoopFacts"` exits 0.
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~KeeperContractTests"` exits 0.
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "Phase=46"` discovers >= 11 stubs.
</verification>

<success_criteria>
- RetryLoop + RetryOutcome relocated to BaseConsole.Core.Resilience; old file deleted; two real usings updated; RetryLoopFacts green.
- KeeperReinject.Payload added; BuildReinject stamps it; KeeperContractTests pins it; all three in one commit.
- 8 RED Phase-46 test stubs created and discoverable; test project compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/46-keeper-5-state-recovery-orchestrator-per-item-consume/46-01-SUMMARY.md`
</output>
