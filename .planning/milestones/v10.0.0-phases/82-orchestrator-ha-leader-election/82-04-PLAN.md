---
phase: 82-orchestrator-ha-leader-election
plan: 04
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - tests/BaseApi.Tests/Election/LeaderStateTransitionTests.cs
  - tests/BaseApi.Tests/Observability/OrchestratorRoleEnricherTests.cs
  - tests/BaseApi.Tests/Orchestrator/WorkflowFireJobGateTests.cs
autonomous: true
requirements: [HA-01, HA-02, HA-03, HA-04, HA-05]

must_haves:
  truths:
    - "A hermetic test proves a follower fire performs ZERO DispatchAsync sends yet still refreshes L1 + reschedules; a leader fire performs the sends"
    - "A hermetic test proves the default LeaderState is follower, OnStartedLeading→leader, OnStoppedLeading→follower, and RenewDeadline < LeaseDuration"
    - "A hermetic test proves the enricher stamps attributes.role = follower/leader (never empty) from the live LeaderState, reflecting a post-construction flip"
    - "All new hermetic tests are green and the full test assembly builds 0-warning"
  artifacts:
    - path: "tests/BaseApi.Tests/Election/LeaderStateTransitionTests.cs"
      provides: "HA-02/HA-03/HA-04 single-writer transition + timing-fence tests"
      contains: "OnStoppedLeading"
    - path: "tests/BaseApi.Tests/Observability/OrchestratorRoleEnricherTests.cs"
      provides: "HA-05 role-enricher tests"
      contains: "attributes"
    - path: "tests/BaseApi.Tests/Orchestrator/WorkflowFireJobGateTests.cs"
      provides: "HA-01 leader-only fire-gate tests"
      contains: "DispatchAsync"
  key_links:
    - from: "tests/BaseApi.Tests/Orchestrator/WorkflowFireJobGateTests.cs"
      to: "src/Orchestrator/Scheduling/WorkflowFireJob.cs"
      via: "constructs the real WorkflowFireJob with a follower/leader LeaderState + IStartupGate"
      pattern: "new WorkflowFireJob"
    - from: "tests/BaseApi.Tests/Observability/OrchestratorRoleEnricherTests.cs"
      to: "src/Orchestrator/Observability/OrchestratorRoleLogEnricher.cs"
      via: "runs the real enricher on a real OTel logger provider"
      pattern: "OrchestratorRoleLogEnricher"
---

<objective>
Prove the Phase-82 leader-election seams hermetically (D-06: no live cluster, no IKubernetes stub — drive
the LeaderState singleton, the election callbacks, the enricher, and the real WorkflowFireJob directly).
Three tests, one per requirement cluster: the fire gate (HA-01), the single-writer transitions + timing
fence (HA-02/HA-03/HA-04), and the role enricher (HA-05). These tests ARE the phase's validation strategy.

Purpose: closes Phase 82's acceptance — all live behavior is deferred to Phase 83, so green hermetic tests
plus the 0-warning build are the phase gate.

Output: three test files under `tests/BaseApi.Tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-orchestrator-ha-leader-election/82-SPEC.md
@.planning/phases/82-orchestrator-ha-leader-election/82-PATTERNS.md

<interfaces>
<!-- SUT contracts from Plans 01+02. Use directly. -->

LeaderState (Plan 01): `new LeaderState(bool startAsLeader = false)`; read `IsLeader`/`Role`/`CurrentLeaderId`;
writers `BecomeLeader()`/`BecomeFollower()`/`SetLeaderId(string?)`.
LeaderElectionService constants (Plan 01): `LeaderElectionService.LeaseDuration` (15s),
`.RenewDeadline` (10s), `.RetryPeriod` (2s).
OrchestratorRoleLogEnricher (Plan 02): `new OrchestratorRoleLogEnricher(LeaderState state) : BaseProcessor<LogRecord>`,
appends attribute key lowercase `"role"`.
WorkflowFireJob ctor (Plan 02): `(IWorkflowL1Store store, IStepDispatcher dispatcher, WorkflowScheduler
scheduler, TimeProvider timeProvider, ILogger<WorkflowFireJob> logger, LeaderState leaderState,
IStartupGate startupGate)`.
IStartupGate (BaseConsole.Core.Health): `bool IsReady`, `void MarkReady()` — use the real `StartupGate`;
call `MarkReady()` to make it hydrated.
</interfaces>

<test_analogs>
- tests/BaseApi.Tests/Orchestrator/WorkflowFireJobScopeTests.cs — REUSE its BuildHarness / SeedEntry /
  FireContext / CapturingDispatchConsumer / NewRamSchedulerAsync helpers wholesale; it already drives the
  real WorkflowFireJob with an in-memory MassTransit harness, a real WorkflowL1Store, FakeTimeProvider,
  and OrchestratorTestStubs.Metrics(). The job-construction call is at lines 146-149.
- tests/BaseApi.Tests/Observability/ProcessorIdEnricherTests.cs — REUSE its CapturingProcessor and the
  EmitOneLog real-OTel-provider harness (lines 64-77) verbatim; swap the StubContext seam for a LeaderState.
- tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs — existing test stubs (Metrics()).
Namespace convention `BaseApi.Tests.<Area>`; `sealed class ...Tests`; xUnit `[Fact]`;
`TestContext.Current.CancellationToken`.
</test_analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LeaderState single-writer transition + timing-fence tests (HA-02/HA-03/HA-04)</name>
  <read_first>
    - src/Orchestrator/Election/LeaderState.cs (Plan 01 — the SUT read/writer surface)
    - src/Orchestrator/Election/LeaderElectionService.cs (Plan 01 — the public timing constants)
    - tests/BaseApi.Tests/Observability/ProcessorIdEnricherTests.cs (the plain-[Fact] stub-driven style to mirror)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Election/LeaderStateTransitionTests.cs` (namespace `BaseApi.Tests.Election`,
    `public sealed class LeaderStateTransitionTests`). No harness needed — drive LeaderState directly (D-06:
    NO IKubernetes stub, NO started election service). Facts:
    1. `Default_LeaderState_Is_Follower` — `var s = new LeaderState();` assert `s.IsLeader == false` and
       `s.Role == "follower"` (SPEC HA-02).
    2. `BecomeLeader_Then_BecomeFollower_Flips_Snapshot` — from default: call `s.BecomeLeader()`
       (simulating `OnStartedLeading`) → assert `s.IsLeader == true` && `s.Role == "leader"`; then
       `s.BecomeFollower()` (simulating `OnStoppedLeading`, the HA-04 self-demotion gate-close) → assert
       `s.IsLeader == false` && `s.Role == "follower"`.
    3. `OffCluster_Seed_Starts_Leader` — `new LeaderState(startAsLeader: true)` → assert leader (the D-02
       off-cluster default).
    4. `RenewDeadline_Is_Less_Than_LeaseDuration` — assert
       `LeaderElectionService.RenewDeadline < LeaderElectionService.LeaseDuration` AND the exact values
       (10s / 15s / RetryPeriod 2s) — the self-demotion fence is present and not inverted (SPEC HA-04 acceptance).
  </action>
  <acceptance_criteria>
    - The file has four `[Fact]` methods covering: default-follower, leader↔follower flip, off-cluster leader seed, RenewDeadline < LeaseDuration + exact 15/10/2s values
    - Runs green: the four LeaderState tests pass
    - No IKubernetes/Kubernetes type is referenced; no LeaderElectionService instance is constructed/started
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class BaseApi.Tests.Election.LeaderStateTransitionTests</automated>
  </verify>
  <done>Default is follower; callbacks flip the snapshot leader↔follower; the RenewDeadline<LeaseDuration fence is asserted present — all green.</done>
</task>

<task type="auto">
  <name>Task 2: OrchestratorRoleLogEnricher tests (HA-05)</name>
  <read_first>
    - src/Orchestrator/Observability/OrchestratorRoleLogEnricher.cs (Plan 02 — the SUT)
    - tests/BaseApi.Tests/Observability/ProcessorIdEnricherTests.cs (REUSE CapturingProcessor + EmitOneLog verbatim)
    - src/Orchestrator/Election/LeaderState.cs (Plan 01)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Observability/OrchestratorRoleEnricherTests.cs` (namespace
    `BaseApi.Tests.Observability`, `public sealed class OrchestratorRoleEnricherTests`). Copy the
    `CapturingProcessor : BaseProcessor<LogRecord>` and the `EmitOneLog(...)` OTel-provider harness from
    ProcessorIdEnricherTests, but wire the SUT `new OrchestratorRoleLogEnricher(leaderState)` first, then
    the capture. Parameterize EmitOneLog on a `LeaderState`. Facts:
    1. `Follower_State_Stamps_role_follower` — leaderState default (follower) → assert the captured record
       has exactly one attribute keyed `"role"` with value `"follower"` (never empty — assert value is
       non-null/non-empty).
    2. `Leader_State_Stamps_role_leader` — `new LeaderState(startAsLeader: true)` → assert `"role" == "leader"`.
    3. `Live_Read_Reflects_Post_Construction_Flip` — construct the enricher with a follower LeaderState,
       emit one record (assert `"role" == "follower"`), then `leaderState.BecomeLeader()` and emit a SECOND
       record with the SAME enricher instance → assert the second record's `"role" == "leader"` (live read,
       D-09). (Adapt EmitOneLog to accept a pre-built enricher instance so both records share it, or emit
       twice within one factory.)
    Use the lowercase literal `"role"` as the key everywhere (NOT ExecutionLogScope.*).
  </action>
  <acceptance_criteria>
    - Three `[Fact]` methods: follower→"role"="follower", leader→"role"="leader", live post-flip→"leader"
    - Each asserts the `"role"` attribute value is non-empty (never blank — HA-05)
    - Runs green
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class BaseApi.Tests.Observability.OrchestratorRoleEnricherTests</automated>
  </verify>
  <done>The enricher stamps attributes.role = follower/leader from the live LeaderState, never empty, reflecting a post-construction flip — all green.</done>
</task>

<task type="auto">
  <name>Task 3: WorkflowFireJob leader-only fire-gate tests (HA-01)</name>
  <read_first>
    - src/Orchestrator/Scheduling/WorkflowFireJob.cs (Plan 02 — the gated job; note the extended ctor)
    - tests/BaseApi.Tests/Orchestrator/WorkflowFireJobScopeTests.cs (REUSE BuildHarness/SeedEntry/FireContext/CapturingDispatchConsumer/NewRamSchedulerAsync)
    - src/BaseConsole.Core/Health/IStartupGate.cs (use the real StartupGate; MarkReady() to hydrate)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/WorkflowFireJobGateTests.cs` (namespace
    `BaseApi.Tests.Orchestrator`, `public sealed class WorkflowFireJobGateTests`). Reuse the Plan-analog
    helpers (BuildHarness / SeedEntry / FireContext / CapturingDispatchConsumer / NewRamSchedulerAsync —
    copy them in, or factor into a shared helper if trivial). Construct the job with the EXTENDED ctor
    (add `leaderState` + a real `StartupGate`). Facts:
    1. `Follower_Fire_Sends_Nothing_But_Still_Refreshes_And_Reschedules` — build with
       `leaderState = new LeaderState()` (follower) and a `StartupGate` with `MarkReady()` already called
       (hydrated, so only the leader term is false). Seed an entry step with a known liveness timestamp in
       the past. After `job.Execute(FireContext(...))`: assert the `CapturingDispatchConsumer` received ZERO
       messages (no DispatchAsync fired), AND the L1 liveness timestamp WAS advanced to `fakeTime.GetUtcNow()`
       (the refresh ran), AND a trigger reschedule occurred (assert the scheduler has a next fire time for
       the job — mirror how the analog verifies scheduling, or assert `RescheduleAsync` side-effect via the
       live scheduler's `GetTrigger`/next-fire-time). (SPEC HA-01: follower skips ONLY the sends.)
    2. `Leader_Fire_Sends_Entry_Steps` — build with `leaderState = new LeaderState(startAsLeader: true)`
       (leader) and a hydrated `StartupGate`. After Execute: assert the `CapturingDispatchConsumer` received
       the entry-step dispatch(es) — the sends fire.
    3. `Unhydrated_Leader_Sends_Nothing` — `new LeaderState(startAsLeader: true)` but a `StartupGate` WITHOUT
       `MarkReady()` (not hydrated) → assert ZERO sends (D-05: gate = `IsLeader && hydrated`; the cold-leader
       lost-fire edge is closed) while L1 refresh + reschedule still run.
    Use `FakeTimeProvider` and the in-memory MassTransit harness exactly as the analog does.
  </action>
  <acceptance_criteria>
    - Three `[Fact]` methods: follower→0 sends + refresh + reschedule; hydrated leader→sends fire; unhydrated leader→0 sends
    - The follower/unhydrated cases assert BOTH zero DispatchAsync AND that the L1 liveness timestamp advanced (refresh ran, ungated)
    - Runs green
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug && tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack --filter-class BaseApi.Tests.Orchestrator.WorkflowFireJobGateTests</automated>
  </verify>
  <done>Only a leader-AND-hydrated fire dispatches entry steps; followers and cold leaders skip the sends but still refresh L1 + reschedule — all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → SUT | Tests drive in-process SUT types only; no external credentials, no cluster, no broker (in-memory MassTransit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-13 | Tampering | test seam vs production election | mitigate | Tests NEVER construct/start `LeaderElectionService` and NEVER set `KUBERNETES_SERVICE_HOST`, so no real elector, `InClusterConfig()`, or Lease contention can occur from CI (D-06). The election path is exercised only via the in-process `LeaderState` writer methods |
| T-82-14 | Spoofing | in-memory bus harness | accept | The MassTransit test harness is fully in-process (no real RabbitMQ); the CapturingDispatchConsumer is the only receiver — no external message surface |
</threat_model>

<verification>
- Full hermetic run green: `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-not-trait Category=RealStack` (Windows MTP — run the exe directly, NOT `dotnet test`).
- Test assembly builds 0-warning; the three new classes pass.
</verification>

<success_criteria>
- HA-01 gate proven: follower/cold-leader send nothing but still refresh+reschedule; hydrated leader sends.
- HA-02/HA-03/HA-04 proven: default follower, callback-driven leader↔follower flip, RenewDeadline<LeaseDuration fence.
- HA-05 proven: attributes.role stamped follower/leader, never empty, live-read across a flip.
- All new hermetic tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/82-orchestrator-ha-leader-election/82-04-SUMMARY.md`
</output>
