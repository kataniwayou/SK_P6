# Milestone v8.0.0: E2E Resilience Proof

**Status:** ✅ SHIPPED 2026-06-15
**Phases:** 63–68
**Total Plans:** 15 (63: 3 · 64: 1 · 65: 3 · 66: 3 · 67: 3 · 68: 2)

## Overview

Whole-system **live recovery proof under faults**. Prove perfect (**zero-missing**, **effect-once**) recovery of a fan-out orchestrated workflow under 7 sustained 5-minute fault scenarios, verified **solely** from Prometheus metrics and Elasticsearch logs, fully automated — no human verification. Supersedes v7.0.0's deferred Phase-62 live proof.

**Workflow under test:** a single seeded definition `A → B → C → { D1 → E1 → F1, D2 → E2 → F2 }` (9 steps, entry A, fan-out at C, two sinks F1/F2), all steps backed by **one** shared `processor-sample`, cron `*/30 * * * * *`, each step's assignment payload `{ number:int, label:"Step_*" }`. Step identity comes from the payload label (`Step_*`), **not** the processor.

**Pass bar (per scenario):** every triggered correlationId reaches **both** sinks F1+F2 (**zero-missing**) AND each step's COMPLETED effect lands **exactly once** per correlationId (**effect-once**); message-level redelivery during a crash is **reported, not failed** (matches the documented exactly-once-effect guarantee).

**Source of truth:** **Prometheus + Elasticsearch ONLY.** The prior triple-SHA infra net-zero close gate (`psql \l` / `redis-cli --scan` / `rabbitmqctl list_queues` BEFORE==AFTER) was **explicitly out of scope** for v8.0.0 and was **not** a pass criterion.

**Scope discipline (locked):** This milestone **PROVED the existing recovery machinery** (keeper, exactly-once-effect, slot-array, per-replica liveness) — it added **no new recovery logic**. The **only product code changes** were: (a) enable 6-field seconds-cron (`CronInterval` + the 5-field validator), and (b) the processor int+string payload + random-add + `Step_*` structured logging. Everything else was **test harness + seeder + analyzer**. All 9 steps share one `processor-sample`; `processor-badconfig` was dropped from this stack.

**Build order (locked, dependency-driven):** 63 (seconds-cron) + 64 (processor work/logging, parallel) → 65 (seeder + clean-state stack) → 66 (Prom+ES analyzer/PASS-FAIL) → 67 (fault-injection harness) → 68 (7-scenario live capstone).

## Phases

### Phase 63: Seconds-Granularity Cron
**Goal**: The orchestrator can fire a workflow on a 6-field seconds-granularity cron expression (`*/30 * * * * *` → every 30 seconds). `CronInterval`'s next-occurrence + interval math computes sub-minute intervals correctly in UTC, and the workflow create/update cron validator accepts the 6-field seconds form (previously rejected) while still accepting the 5-field form.
**Depends on**: — (first phase of v8.0.0; independent product change — ran parallel to Phase 64)
**Requirements**: CRON-01, CRON-02
**Plans**: 3
- [x] 63-01 — CronFieldForm shared detector + unit test (pure token-count detector hoisted into `Messaging.Contracts.Projections`, zero Cronos dep; 8/8 facts; 0e48293, ee2b12d)
- [x] 63-02 — CronInterval rewire + */30 sub-minute fact (NextOccurrence + IntervalSeconds resolve CronFormat via shared detector, lifting the 1-minute floor; `IntervalSeconds("*/30 * * * * *")==30`; CRON-01 scheduler-side; e6c9d42, 35e3ccc)
- [x] 63-03 — Both validators rewire + message update + tests (WorkflowCreate/UpdateDtoValidator route through shared detector, no exception-as-control-flow; 6-field accepted + 5-field regression-guarded; messages → "5- or 6-field"; CRON-02; 4fec1a6, 34a9059)

### Phase 64: Processor Work & Structured Logging
**Goal**: The shared `processor-sample` does observable, correlatable work. Its config (`SampleConfig`) carries an integer and a string; `ProcessAsync` generates a random number, adds it to the payload integer, produces the sum as the step's completed result, and emits a structured log entry tagged `Step_<label>` with the computed sum, carrying `correlationId` + `stepId` (+ `workflowId`/`processorId`) so Elasticsearch can aggregate a whole run by correlationId. The data shape the analyzer (Phase 66) parses.
**Depends on**: — (independent product change — ran parallel to Phase 63; builds on the v6.0.0 typed base-config seam)
**Requirements**: PROC-01, PROC-02, PROC-03
**Plans**: 1
- [x] 64-01 — SampleConfig int+string reshape, sum transform + single `Step_*` structured log, 3-fact hermetic rewrite (`SampleConfig(int Number, string? Label)`; sum=(Number)+Random.Shared.Next(0,100) → {number,label} JSON Data + single LogInformation; SampleProcessorFacts 3/3 GREEN, Debug+Release 0/0; PROC-01/02/03; b323286, ddbfd8b, 4fe6142)

### Phase 65: Fan-Out Workflow Seeder & Clean-State Stack
**Goal**: An idempotent seeder creates the fan-out workflow `A→B→C→{D1→E1→F1, D2→E2→F2}` (9 steps, entry A, fan-out at C, sinks F1+F2) with every step referencing one shared `processor-sample`, the `*/30 * * * * *` cron, and a `{ number, label:"Step_*" }` assignment payload per step — re-runnable without duplicating rows — and the proof runs on a minimal clean-state stack (single `processor-sample`, no `processor-badconfig`; full infra + observability tiers) with each test started from clean state (Redis flushed, Postgres workflow/step/assignment rows reset, leftover processor containers removed).
**Depends on**: Phase 63 + Phase 64
**Requirements**: WF-01, WF-02, ENV-01, ENV-02
**Plans**: 3
- [x] 65-01 — Self-verifying RealStack fan-out seeder fixture (FanOutSeederE2ETests: idempotent reverse-topo build 9 steps/8 edges/9 assignments, 1 shared processor, `*/30 * * * * *`, sentinel `v8-fanout-proof` + inline Npgsql self-verify + run-twice idempotency; live 1/1 GREEN; WF-01/02; c9cc582, c16d5fe)
- [x] 65-02 — Clean-state reset script `scripts/phase-65-reset.ps1` (FLUSHALL → bounded heal-wait on per-instance `skp:proc:*:*` liveness key → FK-safe transactional psql DELETE of 6 workflow-graph tables → processor-set assert; ENV-02; e9fa3a6. NOTE: live reset→seed cycle operator-WAIVED per "no human verification required")
- [x] 65-03 — Minimal-stack bring-up script: compose up + 10-service health wait + zero-badconfig assert (ENV-01)

### Phase 66: Prometheus + ES Analyzer & PASS/FAIL Engine
**Goal**: An analyzer determines a run's correctness solely from Prometheus + Elasticsearch. It aggregates all ES logs sharing a `correlationId` into a per-run trace and decides whether all 9 steps and both sinks completed; against the total cron-trigger count it detects MISSING runs/steps and DUPLICATE step effects; it queries Prometheus counters (`orchestrator_dispatch_sent`, `orchestrator_result_consumed`, `processor_dispatch_consumed`, `processor_result_sent{outcome}`, dedupe + keeper counters) and cross-checks dispatched vs completed vs deduped; and it emits a per-test smoke report + automated PASS/FAIL verdict derived only from Prom + ES.
**Depends on**: Phase 64
**Requirements**: OBS-01, OBS-02, OBS-03, OBS-04
**Plans**: 3
- [x] 66-01 — PassFailEngine + models + hermetic decision-branch facts (pure core; OBS-01/02/03; 6/6 PassFailEngineFacts; cd5424c, 178eea6, e7fe63d)
- [x] 66-02 — SearchAllHits multi-hit ES extension + EsIndexNames Step consts + grouping facts (OBS-01; 4/4 facts; 5047f3c, 3c39f25)
- [x] 66-03 — AnalyzerE2ETests RealStack fixture: Wave-0 mapping/windowing probes + gather ES+Prom → engine → write-then-assert report (OBS-01/02/03/04; 0 warnings; 0514d91, e6ea260)

### Phase 67: Fault-Injection Harness
**Goal**: A fully-automated harness drives one scenario end-to-end: activate via `POST /api/v1/orchestration/start`, run a 5-minute/30s-cron window (~10 triggers), inject the scenario's fault mid-run (container kill/restart) and recover within the same window — clean → seed → activate → inject → observe → analyze → tear down with no human step, wiring the Phase 65 seeder/clean-stack and the Phase 66 analyzer.
**Depends on**: Phase 65 + Phase 66
**Requirements**: FAULT-01, FAULT-02, FAULT-03
**Plans**: 3
- [x] 67-01 — D-16 env-var seam in AnalyzerE2ETests.cs (SCENARIO_ID/WINDOW_START_UTC/WINDOW_END_UTC, const/UtcNow fallback); FAULT-01/03; 55b2fef (standalone analyzer-GREEN deferred to 67-03; three env findings handed forward)
- [x] 67-02 — Author `scripts/phase-67-harness.ps1`: scenario table + clean→seed→psql-wf-id→204-gate→observe→whole-tier-crash→health-wait→analyze→teardown, exit mirrors verdict (FAULT-01/02/03; 6ca3f0d, 79a7137, 78fc508)
- [x] 67-03 — Two reference runs: TEST-01 baseline + TEST-02 processor crash, both Pass 10/10 (zero-missing, effect-once); OBS-04 verdict re-founded on an ES-binding arbiter (Prom→corroboration) absorbing the mid-window counter reset — corrects a Phase-66 per-run-denominator defect, spec-owner-approved; deviations d2445fb, a9e42e0, 574739a

### Phase 68: Live Resilience Proof — 7 Scenarios (Capstone)
**Goal**: Run all 7 resilience proofs through the harness and produce a passing automated verdict for each (happy path + processor / orchestrator / keeper / redis / rabbitmq / redis+rabbitmq crash). Each PASSES iff zero-missing + effect-once hold over its 5-minute/30s-cron window; redelivery during the fault reported, not failed. Truth = Prometheus + Elasticsearch only.
**Depends on**: Phase 67
**Requirements**: TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07
**Plans**: 2
- [x] 68-01 — Author the 5 scenario rows (TEST-03..07) + `scripts/phase-68-sweep.ps1` run-all wrapper (exit-0-iff-7/7) + fixture rename/literal-sync; test project 0-warning (3f359f2, f39530d, 7556ee4)
- [x] 68-02 — Run the 7-scenario live sweep. **Result: 7/7 PASS across all fault classes.** Initial sweep 6/7 (TEST-01..05 + TEST-07 zero-missing + effect-once); TEST-06 (rabbitmq) VERDICT_FAIL (MISSING:2) traced to the by-design `ReinjectConsumer.cs:37` silent-DROP — the leftover 5s `ExecutionDataTtl` self-expired across the 45s outage (`KeeperReinjectDroppedDelta:2 == Missing:2`), corroborated by TEST-07 (strictly-harder superset) PASS (no recovery defect); 098f36a. **Resolution (post-checkpoint, spec-owner-directed):** config-only fix — `Processor__ExecutionDataTtl` raised 5→300 in `compose.yaml` (the "5" was an obsolete v6.0.0 close-gate hack, dead once v8.0.0 retired the triple-SHA net-zero gate); TEST-06 re-ran **clean PASS** (Missing:0, `KeeperReinjectDroppedDelta` 2→0); capstone **7/7**, zero production source touched; da91d32. TTL later unified to the const + `SlotArrayOptions` removed via quick-task 260615-dbf.

---

## Milestone Summary

**Delivered:** A fully-automated, Prometheus+Elasticsearch-only live resilience proof harness for the fan-out workflow `A→B→C→{D1→E1→F1, D2→E2→F2}`, exercising the existing recovery machinery across 7 sustained 5-minute fault classes. Capstone result: **7/7 PASS** (zero-missing + effect-once each) across all fault classes — recovery machinery live-proven. (Initial sweep 6/7; TEST-06 RabbitMQ re-ran clean after a config-only TTL fix, below.)

**Stats:** 6 phases · 15 plans · 139 commits (18 feat / 21 fix) · 136 files changed (+18,800 / −1,202) · 2026-06-14 → 2026-06-15.

**Key Decisions:**
- Truth source = Prometheus + Elasticsearch ONLY for this milestone (the prior triple-SHA infra net-zero close gate explicitly out of scope). (✓ Good)
- No new recovery logic — milestone PROVES existing machinery; only product changes are seconds-cron + processor work/logging. (✓ Good)
- OBS-04 verdict re-founded on an ES-binding arbiter (Prometheus demoted to non-fatal corroboration) — corrects a Phase-66 per-run-denominator defect surfaced under the first real fan-out load. (✓ Good)
- TEST-06 resolution: `Processor__ExecutionDataTtl` 5→300 is a **config-only** fix (the "5" was an obsolete v6.0.0 close-gate hack); no recovery-logic change. (✓ Good)
- Per-(correlationId, executionId) multi-execution scoring (orchestrator propagates ExecutionId unchanged; quick-task 260615-kgz). (✓ Good)

**Issues Resolved:**
- Seconds-cron 1-minute floor lifted (CronFieldForm shared detector across scheduler + validators).
- Mid-window Prometheus counter-reset signature absorbed by the ES-binding arbiter.
- **TEST-06 (RabbitMQ) MISSING:2** — root-caused to the by-design `ReinjectConsumer.cs:37` silent-DROP firing because the leftover 5s `Processor__ExecutionDataTtl` self-expired across the 45s outage (corroborated by TEST-07 superset PASS → no recovery defect). Fixed config-only (`ExecutionDataTtl` 5→300; later unified to the const + `SlotArrayOptions` removed via quick-task 260615-dbf); TEST-06 re-ran clean (Missing:0), capstone 7/7.

**Technical Debt / Deferred:**
- 32 open artifacts from earlier un-closed milestones acknowledged as deferred at this close (see STATE.md → Deferred Items). None are v8.0.0 regressions.
- Phase 65-02 live reset→seed cycle operator-WAIVED (manual acceptance step, not an executed live check).

---

*For current project status, see .planning/ROADMAP.md*
