---
phase: 54-terminal-index-delete
plan: 04
type: execute
wave: 3
depends_on: ["54-01", "54-02", "54-03"]
files_modified:
  - tests/BaseApi.Tests/Processor/PipelineEndDeleteFacts.cs
  - tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs
  - tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs
autonomous: true
requirements: [GC-01, GC-02, GC-03]

must_haves:
  truths:
    - "Every delete fact asserts ONE array KeyDeleteAsync call AND DidNotReceive() on both scalar overloads (atomicity — the GC-01 heart)"
    - "The forward happy + recovery all-clear facts assert the array contains BOTH ExecutionData(entryId) and MessageIndex(messageId)"
    - "The source-step fact asserts the index IS deleted (array contains MessageIndex + ExecutionData(Guid.Empty)) with no throw"
    - "REINJECT facts add an array DidNotReceive() (neither key deleted; index survives)"
    - "The exhaust fact asserts KeyPersistAsync(MessageIndex) is called then a KeeperDelete carrying MessageId == messageId is sent"
    - "A NEW EndDelete_PersistExhaust_StillSendsKeeper fact proves the keeper is sent even when persist also throws"
    - "DeleteConsumer facts assert the both-key array DEL, drop-on-absent"
  artifacts:
    - path: "tests/BaseApi.Tests/Processor/PipelineEndDeleteFacts.cs"
      provides: "Inverted forward-tail facts (array DEL) + persist/MessageId asserts + new persist-exhaust fact"
      contains: "EndDelete_PersistExhaust_StillSendsKeeper"
    - path: "tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs"
      provides: "Inverted all-clear fact (array DEL) + hardened REINJECT-exclusion facts"
      contains: "Arg.Is<RedisKey[]>"
    - path: "tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs"
      provides: "Both-key array DEL fact + drop-on-absent fact (array .Returns(0L))"
      contains: "MessageIndex(m.MessageId)"
  key_links:
    - from: "the delete facts"
      to: "the array overload assertion + scalar DidNotReceive"
      via: "Arg.Is<RedisKey[]>(ks => ks.Contains(...)) + DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>(), …)"
      pattern: "DidNotReceive\\(\\).KeyDeleteAsync\\(Arg.Any<RedisKey>"
---

<objective>
Invert and harden the three existing hermetic fact files (D-04 — in place, NO new fact FILE) to assert the A19 behavior shipped in Plan 03, exercised through the array-overload mocks added in Plan 01. Every delete fact asserts the atomicity heart of GC-01: ONE `Received(1)` on the array `KeyDeleteAsync(RedisKey[], …)` overload AND `DidNotReceive()` on BOTH scalar overloads — never two scalar deletes (RESEARCH Pitfall 2). Array-contents are asserted with an `Arg.Is<RedisKey[]>(ks => ks.Contains(...))` predicate matcher (RESEARCH Pitfall 5).

This plan satisfies the full per-requirement fact map in VALIDATION.md (GC-01/02/03, AC-1..AC-7) plus the AC-8 TTL-regression guard and AC-9 no-new-metric check, and gates AC-10 (0-warning build + full suite green).

Purpose: prove the active two-key index reclaim, REINJECT mutual exclusion, persist-on-escalate, and the both-key keeper DELETE — all hermetically.
Output: modified `PipelineEndDeleteFacts.cs`, `PipelineRecoveryFacts.cs`, `DeleteConsumerFacts.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-terminal-index-delete/54-SPEC.md
@.planning/phases/54-terminal-index-delete/54-CONTEXT.md
@.planning/phases/54-terminal-index-delete/54-RESEARCH.md
@.planning/phases/54-terminal-index-delete/54-PATTERNS.md
@.planning/phases/54-terminal-index-delete/54-VALIDATION.md
@.planning/phases/54-terminal-index-delete/54-01-SUMMARY.md
@.planning/phases/54-terminal-index-delete/54-03-SUMMARY.md

<interfaces>
The atomicity assertion shape (RESEARCH NSubstitute Mock Shape #2/#3 — use everywhere a delete is asserted):
```csharp
// ONE array call whose operands contain BOTH keys:
await db.Received(1).KeyDeleteAsync(
    Arg.Is<RedisKey[]>(ks => ks.Length == 2
        && ks.Contains((RedisKey)L2ProjectionKeys.ExecutionData(entryId))
        && ks.Contains((RedisKey)L2ProjectionKeys.MessageIndex(messageId))),
    Arg.Any<CommandFlags>());
// AND zero scalar deletes (the GC-01 heart):
await db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>(), Arg.Any<CommandFlags>());
await db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>());
```
`RedisKey` is a value type with `==`/`Equals` over the key bytes, so `Contains` works; cast the `L2ProjectionKeys` string to `(RedisKey)` explicitly inside the lambda. NSubstitute literal-array matching fails (reference inequality) — the predicate matcher is mandatory.

New muxes/stubs available from Plan 01: `DispatchTestKit` success muxes stub the array overload `.Returns(2L)`; fault muxes throw on the array overload; `ReadOkDeleteFaultL2` stubs persist `.Returns(true)`; new `ReadOkDeleteAndPersistFaultL2` throws on BOTH the array DEL and persist. `RecoveryTestKit.Db()` stubs the array overload `.Returns(2L)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert + harden PipelineEndDeleteFacts (forward tail) and PipelineRecoveryFacts (recovery tail)</name>
  <files>tests/BaseApi.Tests/Processor/PipelineEndDeleteFacts.cs, tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/PipelineEndDeleteFacts.cs (the file being modified — `EndDelete_RunsOnHappyPath` :30-46, `EndDelete_RunsOnBusinessFail` :48-68, `EndDelete_RunsOnInException` :70-85, `EndDelete_Skipped_OnReinject` :87-102, `EndDelete_Skipped_OnSourceStep` :104-118, `EndDelete_Exhaust_Delete` :120-135. Note :132 passes `Guid.NewGuid()` as messageId inline — must bind to a local)
    - tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs (the file being modified — `HGetAllFault_Reinject_NoSourceDelete` :47-62, `MixedSlots_…NoSourceDelete` :64-93, `AllClear_DeletesSource` :116-136; leave `ResentCompleted_CarriesFreshExec` :95-114 and `SendBeforeRetire_SendFail_LeavesSlot` :138-156 UNTOUCHED — AC-8 regression guards)
    - .planning/phases/54-terminal-index-delete/54-VALIDATION.md (Per-Requirement Verification Map — the named fact + observable signal per row)
    - .planning/phases/54-terminal-index-delete/54-PATTERNS.md (PipelineEndDeleteFacts :132-170, PipelineRecoveryFacts :174-187 — exact deltas)
    - .planning/phases/54-terminal-index-delete/54-RESEARCH.md (NSubstitute Mock Shape :205-254, Open Question 1 :395-398 — ordering optional)
  </read_first>
  <action>
**PipelineEndDeleteFacts.cs** — bind `messageId` to a `var messageId = Guid.NewGuid();` local in each fact and pass it to `RunAsync(..., messageId, ct)` (replacing the inline `Guid.NewGuid()` arg) wherever the fact asserts array contents.

- `EndDelete_RunsOnHappyPath` (INVERT, GC-01/AC-1): replace the scalar `Received(1).KeyDeleteAsync(ExecutionData(entryId))` with the array-contents matcher (ExecutionData(entryId) + MessageIndex(messageId)) + both scalar `DidNotReceive()` (see interfaces). Keep `Assert.Empty(send.SentKeeper.OfType<KeeperDelete>())`.
- `EndDelete_RunsOnBusinessFail`, `EndDelete_RunsOnInException` (UPDATE, GC-01): swap the scalar `Received(1).KeyDeleteAsync(ExecutionData(entryId))` to `await db.Received(1).KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>());`.
- `EndDelete_Skipped_OnSourceStep` (INVERT INTENT, GC-01/AC-3, D-06): the source step now DELETES the index. Replace the two scalar `DidNotReceive` lines with a `Received(1)` array assertion whose operands contain `MessageIndex(messageId)` and `ExecutionData(Guid.Empty)`; the test completing without throwing proves the `Guid.Empty` data operand drop-on-absent. (Bind `messageId` and pass it.)
- `EndDelete_Skipped_OnReinject` (KEEP + harden, GC-02/AC-4): keep the existing `Single(OfType<KeeperReinject>())` + both scalar `DidNotReceive`; ADD `await db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>());`.
- `EndDelete_Exhaust_Delete` (UPDATE, GC-03/AC-5/AC-6): bind `messageId`; keep `Single(OfType<KeeperDelete>())`; ADD `await db.Received(1).KeyPersistAsync((RedisKey)L2ProjectionKeys.MessageIndex(messageId), Arg.Any<CommandFlags>());` and `var kd = Assert.Single(send.SentKeeper.OfType<KeeperDelete>()); Assert.Equal(messageId, kd.MessageId);`. (This fact uses `ReadOkDeleteFaultL2`, which now throws on the array DEL but stubs persist `.Returns(true)`.) The `out _` on the mux must become `out var db` so `db` is assertable. Strict persist-before-send ordering is OPTIONAL (RESEARCH Open Question 1) — assert both occurred + the value.
- ADD NEW fact `EndDelete_PersistExhaust_StillSendsKeeper` (GC-03/D-03): use `DispatchTestKit.ReadOkDeleteAndPersistFaultL2(...)` (both array DEL and persist throw); assert `Assert.Single(send.SentKeeper.OfType<KeeperDelete>())` — the keeper is sent despite the persist failure. Mirror the `EndDelete_Exhaust_Delete` scaffolding (Build/Dispatch/CapturingSendProvider).

**PipelineRecoveryFacts.cs**:
- `AllClear_DeletesSource` (INVERT, GC-01/AC-2): `messageId` is already a bound local (:122). Replace the scalar `Received().KeyDeleteAsync(ExecutionData(d.EntryId))` with the array-contents matcher (ExecutionData(d.EntryId) + MessageIndex(messageId)) + both scalar `DidNotReceive()`. Keep `Assert.Empty(send.SentKeeper.OfType<KeeperReinject>())`.
- `HGetAllFault_Reinject_NoSourceDelete` (KEEP + harden, GC-02): keep the existing asserts; ADD `await db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>());`.
- `MixedSlots_OneResent_OneRetire_FaultLeavesSlot_NoSourceDelete` (KEEP + harden, GC-02/AC-4): keep the existing asserts; ADD `await db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>());` (the "index survives" proof — no array DEL of MessageIndex issued).
- Do NOT touch `ResentCompleted_CarriesFreshExec` or `SendBeforeRetire_SendFail_LeavesSlot` (their `HashSetAsync`/retire/TTL assertions are the AC-8 backstop guard — they must keep passing unchanged).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~PipelineEndDeleteFacts|FullyQualifiedName~PipelineRecoveryFacts"</automated>
  </verify>
  <acceptance_criteria>
    - PipelineEndDeleteFacts.cs contains 'Arg.Is<RedisKey[]>' (array-contents matcher present)
    - PipelineEndDeleteFacts.cs contains 'DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>(), Arg.Any<CommandFlags>())' (scalar zero-call on the happy/source facts — atomicity)
    - PipelineEndDeleteFacts.cs contains 'KeyPersistAsync((RedisKey)L2ProjectionKeys.MessageIndex(messageId)' (AC-5 persist assert)
    - PipelineEndDeleteFacts.cs contains 'Assert.Equal(messageId, kd.MessageId)' (AC-6 MessageId stamp)
    - PipelineEndDeleteFacts.cs contains 'EndDelete_PersistExhaust_StillSendsKeeper' and 'ReadOkDeleteAndPersistFaultL2' (new D-03 fact + mux)
    - PipelineRecoveryFacts.cs contains 'Arg.Is<RedisKey[]>' (AC-2 array-contents matcher)
    - PipelineRecoveryFacts.cs contains 'DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>())' (GC-02 array exclusion on the REINJECT facts)
    - PipelineRecoveryFacts.cs still contains 'ResentCompleted_CarriesFreshExec' and 'SendBeforeRetire_SendFail_LeavesSlot' unchanged (AC-8 guards)
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~PipelineEndDeleteFacts|FullyQualifiedName~PipelineRecoveryFacts"` exits 0
  </acceptance_criteria>
  <done>
The forward-tail and recovery-tail facts assert ONE array DEL (both operands) + zero scalar deletes on every happy/source path; the REINJECT facts add the array `DidNotReceive`; the exhaust fact asserts persist + MessageId; the new persist-exhaust fact proves best-effort fall-through; the AC-8 regression guards are untouched. Both filtered fact classes pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Invert DeleteConsumerFacts (both-key keeper DEL) + full-suite + 0-warning gate</name>
  <files>tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs (the file being modified — `NewDelete()` :25-31, `Delete_deletes_execution_data_key` :33-51, `Delete_absent_key_no_throws` :53-74)
    - .planning/phases/54-terminal-index-delete/54-VALIDATION.md (AC-7 rows + AC-10 gate)
    - .planning/phases/54-terminal-index-delete/54-PATTERNS.md (DeleteConsumerFacts :191-210)
    - .planning/phases/54-terminal-index-delete/54-RESEARCH.md (NSubstitute Mock Shape #3 array-contents matcher)
  </read_first>
  <action>
Edit `tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs`:

- `NewDelete()` (:25-31): add `MessageId = Guid.NewGuid(),` to the initializer (mirror the existing `EntryId = Guid.NewGuid()` line) so `m.MessageId` is a real, distinct, assertable value.
- `Delete_deletes_execution_data_key` (INVERT, GC-03/AC-7): replace the scalar `Received(1).KeyDeleteAsync((RedisKey)ExecutionData(m.EntryId), …)` with the array-contents matcher:
```csharp
await db.Received(1).KeyDeleteAsync(
    Arg.Is<RedisKey[]>(ks => ks.Length == 2
        && ks.Contains((RedisKey)L2ProjectionKeys.ExecutionData(m.EntryId))
        && ks.Contains((RedisKey)L2ProjectionKeys.MessageIndex(m.MessageId))),
    Arg.Any<CommandFlags>());
```
- `Delete_absent_key_no_throws` (UPDATE, GC-03/AC-7 drop-on-absent): change the per-test stub from the scalar `db.KeyDeleteAsync(Arg.Any<RedisKey>(), …).Returns(false)` (:60) to the array overload `db.KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>()).Returns(0L);`; replace the closing scalar `Received(1)` assertion with `await db.Received(1).KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>());`. The consume completing without throwing proves drop-on-absent.

Then run the FULL hermetic suite and the dual-config 0-warning build gate (AC-10).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~DeleteConsumerFacts"</automated>
  </verify>
  <acceptance_criteria>
    - DeleteConsumerFacts.cs contains 'MessageId = Guid.NewGuid()' in NewDelete()
    - DeleteConsumerFacts.cs contains 'MessageIndex(m.MessageId)' (AC-7 both-key array assert)
    - DeleteConsumerFacts.cs contains 'KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>()).Returns(0L)' (drop-on-absent array stub)
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~DeleteConsumerFacts"` exits 0
    - `dotnet test` (full hermetic suite, solution root) exits 0 (AC-10 — full suite green)
    - `dotnet build -c Release` reports 0 warnings (AC-10)
    - `dotnet build -c Debug` reports 0 warnings (AC-10)
  </acceptance_criteria>
  <done>
`DeleteConsumerFacts` assert the both-key array DEL (both operands) and drop-on-absent via the array overload; `NewDelete()` stamps a distinct `MessageId`. The filtered keeper facts pass, the full hermetic suite is green, and the solution builds 0-warning in both Release and Debug — closing AC-10.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits hermetic xUnit fact files only — no production code, no runtime surface, no input parsing, no auth/network/secret boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07 | Tampering | Atomicity assertion fidelity (two-scalar false pass) | mitigate | Every delete fact pairs `Received(1)` on the array overload with `DidNotReceive()` on BOTH scalar overloads (RESEARCH Pitfall 2), so a regression to two scalar deletes fails the suite. Test-correctness, not a security threat — captured for completeness. |

This plan introduces NO new external attack surface. It edits hermetic test doubles only — no production code, no new input parsing, no new auth/network boundary, no new secret. No high/critical threats are introduced.
</threat_model>

<verification>
- Quick run: `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~PipelineEndDeleteFacts|FullyQualifiedName~PipelineRecoveryFacts|FullyQualifiedName~DeleteConsumerFacts"` — green.
- Full suite: `dotnet test` (solution root) — green (AC-10).
- Build gate: `dotnet build -c Release` AND `dotnet build -c Debug` — 0 warnings (AC-10).
- Atomicity (GC-01 heart): every delete fact asserts ONE array call + `DidNotReceive()` on both scalar overloads.
- AC-8 guard: `ResentCompleted_CarriesFreshExec` + `SendBeforeRetire_SendFail_LeavesSlot` (TTL/retire path) untouched and still green.
- AC-9: no new metric series referenced or asserted.
</verification>

<success_criteria>
- All three fact files assert the A19 behavior: atomic two-key DEL (both operands), source-step index reclaim, REINJECT exclusion, persist-on-escalate + MessageId, persist-exhaust fall-through, both-key keeper DEL + drop-on-absent.
- Full hermetic suite green; solution builds 0-warning Release + Debug — AC-10 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/54-terminal-index-delete/54-04-SUMMARY.md`.
</output>
