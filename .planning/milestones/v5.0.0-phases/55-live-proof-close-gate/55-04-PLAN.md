---
phase: 55-live-proof-close-gate
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [TEST-01, TEST-02]
user_setup: []

must_haves:
  truths:
    - "dotnet build SK_P.sln -c Release exits 0 with zero warnings (TreatWarningsAsErrors)"
    - "dotnet build SK_P.sln -c Debug exits 0 with zero warnings"
    - "The new/adapted RealStack E2E tests COMPILE and the hermetic suite (Category!=RealStack) is green"
    - "scripts/phase-55-close.ps1 parses under pwsh"
    - "The live N=3xGREEN triple-SHA close run is executed by the operator via 55-HUMAN-UAT.md (D-09 — operator-gated, NOT autonomous)"
    - "TEST-01 and TEST-02 are ticked ONLY after the operator's recorded GREEN run"
  artifacts: []
  key_links:
    - from: "build gate (D-08 autonomous)"
      to: "operator live run (D-09 checkpoint)"
      via: "the build gate is the terminal autonomous verification; the live run is the human checkpoint"
      pattern: "phase-55-close\\.ps1"
---

<objective>
Run the autonomously-verifiable build gate (D-08) — the phase's terminal autonomous deliverable — then hand off to the operator-gated live N=3xGREEN triple-SHA close run (D-09) via the checkpoint below. The build gate proves 0-warning Release+Debug + RealStack tests COMPILE + the hermetic suite green + the close script parses. The live run is operator-gated because it requires the rebuilt v5 docker stack and cannot run autonomously.

Purpose: Satisfy the build-before-proof split (D-08/D-09). TEST-01/02 stay UNTICKED until the operator's recorded GREEN run.
Output: a verified hermetic build gate (autonomous) + an operator checkpoint that gates TEST-01/02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-live-proof-close-gate/55-CONTEXT.md
@.planning/phases/55-live-proof-close-gate/55-RESEARCH.md
@.planning/phases/55-live-proof-close-gate/55-VALIDATION.md
@.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md
@scripts/phase-55-close.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autonomous build gate (D-08) — 0-warning Release+Debug + hermetic suite green + close script parses</name>
  <files></files>
  <read_first>
    - .planning/phases/55-live-proof-close-gate/55-VALIDATION.md (Sampling Rate — the D-08 autonomous deliverable definition)
    - .planning/phases/55-live-proof-close-gate/55-RESEARCH.md (D-08; Assumption A3 — TreatWarningsAsErrors repo-wide; Pitfall 1 — MTP --filter ignored, use --filter-not-trait)
    - scripts/phase-55-close.ps1 (the cloned gate produced by plan 01 — confirm it parses)
  </read_first>
  <action>
This is the phase's terminal AUTONOMOUS verification (D-08). Run all four checks and confirm each passes:

(1) Release build, 0 warnings (TreatWarningsAsErrors is repo-wide — a warning fails the build):
`dotnet build SK_P.sln -c Release`
(2) Debug build, 0 warnings:
`dotnet build SK_P.sln -c Debug`
(3) Hermetic suite green with RealStack EXCLUDED (the adapted/new RealStack tests COMPILE but are not run autonomously — they have no live stack). Use MTP-native trait filtering (Pitfall 1 — `dotnet test --filter` is IGNORED under MTP):
`dotnet run --project tests/BaseApi.Tests -c Release -- --filter-not-trait Category=RealStack`
Expect 0 failed.
(4) The close script parses:
`pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path ./scripts/phase-55-close.ps1), [ref]$null, [ref]$errs); if ($errs.Count -gt 0) { exit 1 } else { exit 0 }"`

If any check fails, STOP and report which one — the build gate is blocking for the phase. Do NOT proceed to the live-run checkpoint until all four pass.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release; dotnet build SK_P.sln -c Debug; dotnet run --project tests/BaseApi.Tests -c Release -- --filter-not-trait Category=RealStack</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` exits 0 (no warnings — TreatWarningsAsErrors).
    - `dotnet build SK_P.sln -c Debug` exits 0 (no warnings).
    - `dotnet run --project tests/BaseApi.Tests -c Release -- --filter-not-trait Category=RealStack` reports 0 failed (RealStack EXCLUDED but COMPILED).
    - `pwsh` parses `scripts/phase-55-close.ps1` with zero errors.
  </acceptance_criteria>
  <done>Both build configs are 0-warning, the hermetic suite is green with the RealStack tests compiling-but-excluded, and the close script parses — the autonomously-verifiable D-08 deliverable is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The v5 close gate (`scripts/phase-55-close.ps1`), the operator runbook (`55-HUMAN-UAT.md`), and the adapted SC1/SC2/SC3 RealStack E2E suite (forward + slot-array index + A19 net-zero; 3-state keeper + both-key DELETE + organic recovery; BIT-gate pause/resume). The autonomous build gate (D-08) has passed: 0-warning Release+Debug, hermetic suite green, RealStack tests compile, close script parses. What CANNOT be automated: the live N=3xGREEN triple-SHA run requires the rebuilt v5 docker stack (a breaking wire contract that mis-deserializes on a mixed-version deploy).
  </what-built>
  <how-to-verify>
Follow `.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md`:

1. Rebuild the v5 stack (breaking wire contract — all four contract-changed services):
   `docker compose up -d --build baseapi-service orchestrator processor-sample keeper`
   FIRST do a clean host build so the embedded `SourceHash` matches the container (a stale incremental hash bit Phase 49 — false-passes/times-out the liveness gate):
   `dotnet clean SK_P.sln; dotnet build SK_P.sln -c Release`
2. Invoke the close gate:
   `pwsh -File scripts/phase-55-close.ps1`
3. Confirm exit 0 with: N=3 consecutive GREEN at an IDENTICAL `Passed` fact count (Smell-A guard, D-10); triple-SHA (psql `\l` / redis `--scan` / rabbitmq `list_queues name`) BEFORE==AFTER; `skp-dlq-1` depth==0; AND the new `skp:msg:*` count==0 (A19 active reclaim, D-06c).
4. Record the three SHAs + the Passed fact count + both 0-checks into `55-HUMAN-UAT.md`'s Live Run #1/#2/#3 blocks.
5. Tick TEST-01 and TEST-02 in `.planning/REQUIREMENTS.md` ONLY after the recorded N=3 GREEN run.

Expected outcome: GATE_EXIT=0, three consecutive GREEN runs at the same Passed count, triple-SHA net-zero held, `skp-dlq-1`==0, `skp:msg:*`==0.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded SHAs + Passed count, or describe the failure (which SHA mismatched / which count was non-zero / which run broke the identical-fact-count guard).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host build artifacts → live v5 containers | The embedded `SourceHash` is the liveness identity; the operator's clean rebuild ensures host==container |
| operator → live docker stack | The live N=3xGREEN run shells into the running v5 stack; the gate's compose-health pre-flight (exit 2) blocks an unhealthy stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-10 | Tampering/DoS | Mixed-version / stale-image deploy mis-deserializes the v5 wire contract during the live run | mitigate | Checkpoint Step 1 forces a clean `dotnet clean/build` + `docker compose up -d --build` of all four contract-changed services; the `SourceHash`==host-build liveness gate makes a false-pass impossible |
| T-55-11 | Repudiation (silent net-zero loss) | Live run passing while an index leaked | mitigate | The gate asserts the additive `skp:msg:*` count==0 (D-06c) alongside the triple-SHA — a leak surfaces as BOTH a redis SHA mismatch AND count>0; net-zero is the A19 active two-key DELETE, not a TTL settle |
| T-55-12 | Tampering | A flaky sibling test breaking the identical-fact-count guard masking a real regression | accept | The N=3 identical-`Passed`-count Smell-A guard (D-10) is designed to catch exactly this; a mismatch is operator time, not a wrong gate — the operator re-runs after stabilizing the sibling |
</threat_model>

<verification>
- Both build configs 0-warning; hermetic suite green; close script parses (autonomous D-08).
- Operator records the live N=3xGREEN run with triple-SHA BEFORE==AFTER, `skp-dlq-1`==0, `skp:msg:*`==0 (D-09).
- TEST-01/02 ticked only after the recorded GREEN run.
</verification>

<success_criteria>
- The autonomous build gate passes (0-warning Release+Debug, hermetic green, RealStack compiles, close script parses).
- The operator completes the live N=3xGREEN triple-SHA net-zero run per `55-HUMAN-UAT.md` and ticks TEST-01/02.
</success_criteria>

<output>
After completion, create `.planning/phases/55-live-proof-close-gate/55-04-SUMMARY.md`
</output>
