---
phase: 55-live-proof-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-55-close.ps1
  - .planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md
autonomous: true
requirements: [TEST-02]
user_setup: []

must_haves:
  truths:
    - "scripts/phase-55-close.ps1 exists and parses under pwsh"
    - "The close gate keeps the verbatim triple-SHA protocol (psql \\l / redis --scan / rabbitmq list_queues name) BEFORE==AFTER"
    - "The close gate seeds the Processor row at version '3.5.0' (Landmine 1 — unchanged)"
    - "The retired Model-B composite settle-GC block is removed (D-06a)"
    - "An additive skp:msg:* count==0 assertion is added parallel to the skp-dlq-1 depth==0 check (D-06c)"
    - "55-HUMAN-UAT.md is the operator runbook for the live N=3xGREEN run (D-09)"
  artifacts:
    - path: "scripts/phase-55-close.ps1"
      provides: "v5 triple-SHA net-zero close gate (clone of phase-49-close.ps1 + 3 deltas)"
      contains: "skp:msg:*"
    - path: ".planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md"
      provides: "Operator runbook for the live N=3xGREEN close run"
      contains: "phase-55-close.ps1"
  key_links:
    - from: "scripts/phase-55-close.ps1"
      to: "skp:msg:* count==0 assertion"
      via: "docker exec sk-redis redis-cli --scan --pattern 'skp:msg:*'"
      pattern: "skp:msg:\\*"
    - from: "55-HUMAN-UAT.md"
      to: "scripts/phase-55-close.ps1"
      via: "Step 2 invokes pwsh -File scripts/phase-55-close.ps1"
      pattern: "phase-55-close\\.ps1"
---

<objective>
Create the v5.0.0 close gate (`scripts/phase-55-close.ps1`) and its operator runbook (`55-HUMAN-UAT.md`) by cloning the proven Phase-49 infrastructure verbatim and applying exactly three v5 deltas: drop the retired Model-B composite settle-GC block (D-06a), add an additive `skp:msg:*` count==0 assertion (D-06c), and retitle to Phase 55. This is the autonomously-verifiable half of TEST-02 (the script exists + parses); the live N=3xGREEN run is operator-gated (handled in plan 04).

Purpose: TEST-02 requires an N-consecutive-GREEN + triple-SHA BEFORE==AFTER net-zero close gate at Release+Debug 0-warning, with the slot-array index keys + data keys proven leak-free by the A19 active two-key DELETE (not a TTL settle).
Output: `scripts/phase-55-close.ps1`, `.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-live-proof-close-gate/55-CONTEXT.md
@.planning/phases/55-live-proof-close-gate/55-RESEARCH.md
@.planning/phases/55-live-proof-close-gate/55-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone phase-49-close.ps1 to phase-55-close.ps1 and apply the three v5 deltas</name>
  <files>scripts/phase-55-close.ps1</files>
  <read_first>
    - scripts/phase-49-close.ps1 (the verbatim template — read the WHOLE file; it is the source of truth)
    - .planning/phases/55-live-proof-close-gate/55-PATTERNS.md (section "scripts/phase-55-close.ps1" — the exact deltas + line citations)
    - .planning/phases/55-live-proof-close-gate/55-RESEARCH.md (Landmine 1 seed version; Landmine 5 unfiltered scan; Pitfall 2 TTL-cannot-settle; "The v5 close-gate deltas" code block)
    - src/Processor.Sample/appsettings.json (confirm "Version": "3.5.0" — Landmine 1)
  </read_first>
  <action>
Create `scripts/phase-55-close.ps1` as a VERBATIM clone of `scripts/phase-49-close.ps1`, then apply EXACTLY these three deltas. Do NOT re-derive the SHA protocol, the steady-state exclusions, the build gate, or the N=3 cadence — clone them character-for-character.

DELTA A (D-06a — DELETE the retired Model-B composite settle-GC block):
- Delete the composite settle-GC loop (the `Settle:` block, phase-49-close.ps1 around lines 272-306) entirely. Do NOT replace it with a `skp:msg:*` settle-wait — the SlotArrayOptions TTL is 300/600s and "cannot be waited out" (Pitfall 2). Net-zero is proven by the A19 active two-key DELETE.
- Delete the composite mentions in the redis-mismatch error text (the four `Write-Host` lines naming `skp:{corr}:{wf}:{proc}:{exec}` / "2-day TTL", around lines 344-347).
- Drop/rewrite the header comment blocks describing the composite namespace (around lines 18-30, 44-50, 280-288).

DELTA B (D-06c — ADD an additive `skp:msg:*` count==0 block). Insert it AFTER the `skp-dlq-1` depth==0 loop and BEFORE the `if (-not $allGood)` finalization. Use this EXACT block (parallel to the DLQ check shape):
```powershell
# D-06c: additive A19 active-reclaim assertion — parallel to the skp-dlq-1 depth==0 check above.
# Net-zero of the slot-array index is a PRODUCTION property (the two-key DEL in ProcessorPipeline.DeleteTerminalAsync
# + DeleteConsumer.cs both-key), NOT a TTL settle (the 300/600s SlotArrayOptions TTL cannot be waited out).
# A lingering index surfaces here as count>0 AND as a redis SHA mismatch — never a silent TTL pass.
$msgCount = (docker exec sk-redis redis-cli --scan --pattern 'skp:msg:*' | Measure-Object).Count
if ($msgCount -ne 0) {
    Write-Host "skp:msg:* count invariant VIOLATED: $msgCount (expected 0 — A19 active reclaim leaked an index)" -ForegroundColor Red
    $allGood = $false
} else {
    Write-Host "skp:msg:* count invariant HELD: 0 (A19 active two-key DEL reclaimed every index)" -ForegroundColor Green
}
```

DELTA C (retitle to Phase 55):
- Replace every "Phase 49" header/comment/exit-code string with "Phase 55".
- In the idempotent Processor-row seed body, change ONLY the `description` text from `'Phase 49 close-gate ...'` to `'Phase 55 close-gate steady-state Processor.Sample row (genuine embedded hash, schema-less).'`. KEEP `version = '3.5.0'` UNCHANGED (Landmine 1 — the row is keyed by `uq_processor_source_hash`; do NOT invent a '5.0.0' seed string).
- Update the final operator-append path (around line 396) to point at `55-HUMAN-UAT.md`.

VERIFY-DO-NOT-EDIT (clone verbatim, confirm present): the service list already includes `keeper`; the BOTH-config 0-warning build gate (`dotnet build SK_P.sln -c Release` AND `-c Debug`); the N=3 identical-fact-count cadence + Smell-A guard; the full-suite no-filter `dotnet test ... --configuration Release --no-build` (the gate runs everything live — Pitfall 1); the compose-health pre-flight (exit 2); the unfiltered `redis-cli --scan` with the SINGLE `skp:{procId:D}` liveness exclusion (Landmine 5); the `_bus_` rabbitmq exclusion (Pitfall 4); `rabbitmqctl -q list_queues name` (NOT `name messages`) for the SHA; the separate `skp-dlq-1` depth==0 additive check.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path ./scripts/phase-55-close.ps1), [ref]$null, [ref]$errs); if ($errs.Count -gt 0) { $errs | Out-String | Write-Error; exit 1 } else { Write-Host 'PARSE OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/phase-55-close.ps1` exists.
    - `pwsh` parses it with zero parse errors (the automated verify above exits 0 with "PARSE OK").
    - `scripts/phase-55-close.ps1` contains the literal string `skp:msg:*` (the D-06c count check).
    - `scripts/phase-55-close.ps1` contains `redis-cli --scan --pattern 'skp:msg:*'`.
    - `scripts/phase-55-close.ps1` contains `version = '3.5.0'` (Landmine 1 — unchanged seed version).
    - `scripts/phase-55-close.ps1` does NOT contain the substring `skp:{corr}` or `Settle:` (composite block removed, D-06a).
    - `scripts/phase-55-close.ps1` does NOT contain the substring `name messages` inside the SHA capture (the name-only SHA is preserved); the `skp-dlq-1` depth read may still use `list_queues name messages`.
    - `scripts/phase-55-close.ps1` contains `dotnet build SK_P.sln -c Release` AND `-c Debug` (both-config build gate preserved).
    - `scripts/phase-55-close.ps1` contains `skp-dlq-1` (the separate DLQ depth check preserved).
    - `grep -c "Phase 49" scripts/phase-55-close.ps1 == 0` (fully retitled).
  </acceptance_criteria>
  <done>The v5 close gate exists, parses, preserves the proven triple-SHA protocol verbatim, and carries exactly the three deltas (composite removed, skp:msg:* count==0 added, retitled with seed version unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Create the 55-HUMAN-UAT.md operator runbook</name>
  <files>.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/49-live-proof-close-gate/49-HUMAN-UAT.md (the structure to clone — heading skeleton + record-block shape)
    - .planning/phases/55-live-proof-close-gate/55-PATTERNS.md (section "55-HUMAN-UAT.md (CREATE)" — heading skeleton + Step 1 rebuild command + clean-rebuild caution)
    - .planning/phases/55-live-proof-close-gate/55-RESEARCH.md (Assumption A1 stale-hash failure mode; Runtime State "Build artifacts"; Security Domain threat table)
  </read_first>
  <action>
Create `.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md` by cloning the section structure of `49-HUMAN-UAT.md`. Use this heading skeleton:
```
# Phase 55 — Live Proof & Close Gate — Operator Runbook (HUMAN-UAT)
## Current Test
## Purpose
## Step 1 — Rebuild the v5 stack (breaking wire contract)
## Step 2 — Invoke the close gate
## Step 3 — Record the GREEN run
## Step 4 — DoD gate (tick TEST-01/02)
## Tests
## Summary
## Gaps
### Live Run #1 / #2 / #3 (record blocks)
```

Step 1 content — the v5 wire contract is BREAKING (slot-array + 3-state + A19), so all four contract-changed services MUST be rebuilt:
```
docker compose up -d --build baseapi-service orchestrator processor-sample keeper
```
ADD the v5-specific clean-rebuild caution (Assumption A1 / Runtime State "Build artifacts"): instruct a clean `dotnet clean SK_P.sln && dotnet build SK_P.sln -c Release` so the host-built `Processor.Sample.dll` embedded `SourceHash` matches the container — a stale incremental hash bit Phase 49 and false-passes/times-out the liveness gate (`PollForHealthyLivenessAsync` polls the REAL container heartbeat by that hash).

Step 2 content: invoke `pwsh -File scripts/phase-55-close.ps1`. Note that the gate runs the FULL test suite (RealStack included) live, ×3, no filter (the `Phase=55` retag does NOT change which tests the gate runs).

Step 3 content (record block per run): record the three SHAs (`psql \l`, `redis --scan`, `rabbitmq list_queues name`), the `Passed` fact count, `skp-dlq-1` depth==0, AND the NEW `skp:msg:*` count==0. The N=3 GREEN guard requires the SAME `Passed` fact count across all three runs (Smell-A guard, D-10).

Step 4 content: tick TEST-01 and TEST-02 in `.planning/REQUIREMENTS.md` ONLY after the N=3 consecutive GREEN run with triple-SHA BEFORE==AFTER, `skp-dlq-1`==0, and `skp:msg:*`==0 — and ONLY by the operator. Leave them UNTICKED in the runbook draft.

Document the three threat mitigations as a short note (from RESEARCH Security Domain): (1) stale-image mis-deserialize → mitigated by the operator rebuild + SourceHash==host-build liveness gate; (2) synthetic-liveness false-pass → mitigated, the gate polls the REAL container heartbeat; (3) net-zero TTL-race false-pass → mitigated by the A19 active two-key DELETE + explicit `skp:msg:*` count==0.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$p='./.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md'; $t=Get-Content $p -Raw; if ($t -match 'phase-55-close\.ps1' -and $t -match 'docker compose up -d --build' -and $t -match 'skp:msg' -and $t -match 'SourceHash') { Write-Host 'RUNBOOK OK'; exit 0 } else { Write-Error 'missing required runbook content'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/55-live-proof-close-gate/55-HUMAN-UAT.md` exists.
    - Contains `pwsh -File scripts/phase-55-close.ps1` (Step 2 invocation).
    - Contains `docker compose up -d --build baseapi-service orchestrator processor-sample keeper` (Step 1 rebuild — all four services).
    - Contains the string `SourceHash` and a clean-rebuild caution (`dotnet clean`).
    - Contains both `skp:msg:*` and `skp-dlq-1` in the Step 3 record block.
    - Contains the headings `Step 1`, `Step 2`, `Step 3`, `Step 4`.
    - TEST-01 and TEST-02 remain UNTICKED (no `[x]` next to them) — the runbook is the operator gate, not the tick.
  </acceptance_criteria>
  <done>The operator runbook exists with the Phase-49 structure cloned, the v5 four-service rebuild + clean-build caution, the Step-3 record block including the new `skp:msg:*` count==0, and TEST-01/02 left unticked pending the operator's GREEN run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator host → live docker stack | The close gate shells `docker exec` into `sk-redis`/`sk-rabbitmq` and seeds a Processor row over HTTP to the in-stack BaseApi; the stack must be the freshly-rebuilt v5 images |
| built Processor.Sample.dll → live container | The embedded `SourceHash` is the liveness identity; a mismatch (stale incremental build) is the documented Phase-49 failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering/DoS | Stale/mixed-version image mis-deserializes the v5 wire contract (slot-array + 3-state + A19) | mitigate | Runbook Step 1 forces `docker compose up -d --build` of all four contract-changed services + a clean `dotnet clean/build`; the `SourceHash`==host-build liveness gate makes a false-pass impossible (the gate polls the real container heartbeat by that hash) |
| T-55-02 | Spoofing | Gate false-pass via a synthetic liveness seed | accept (already mitigated upstream) | The gate seeds the Processor row by the GENUINE embedded `SourceHash` and `PollForHealthyLivenessAsync` polls the REAL container `skp:{procId:D}` heartbeat — no synthetic seed exists in the cloned script |
| T-55-03 | Repudiation (silent net-zero loss) | A lingering `skp:msg:*` index passing via TTL race | mitigate | The script proves net-zero via the A19 active two-key DELETE plus the additive `skp:msg:*` count==0 assertion (D-06c); a leak surfaces as BOTH a redis SHA mismatch AND count>0 — never a silent TTL pass |
</threat_model>

<verification>
- `pwsh` parses `scripts/phase-55-close.ps1` with zero errors.
- `scripts/phase-55-close.ps1` contains `skp:msg:*` and does NOT contain `skp:{corr}` or `Settle:`.
- `grep -c "Phase 49" scripts/phase-55-close.ps1 == 0`.
- `55-HUMAN-UAT.md` exists with all four Step headings + the four-service rebuild + the `skp:msg:*` record block.
</verification>

<success_criteria>
- The v5 close gate exists, parses, and carries exactly the three deltas (composite removed, `skp:msg:*` count==0 added, retitled with seed version unchanged at `3.5.0`).
- The operator runbook exists and gates the live N=3xGREEN run; TEST-01/02 stay unticked.
</success_criteria>

<output>
After completion, create `.planning/phases/55-live-proof-close-gate/55-01-SUMMARY.md`
</output>
