---
phase: 55-live-proof-close-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/SC1RoundTripE2ETests.cs
  - tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs
autonomous: true
requirements: [TEST-01]
user_setup: []

must_haves:
  truths:
    - "SC1 asserts a fresh skp:msg:{messageId} HASH appears on a forward round-trip (allocation-before-data)"
    - "SC1 asserts the A19 two-key net-zero at end-of-message (BOTH skp:data:* AND skp:msg:* gone)"
    - "SC1 and SC3 are tagged [Trait(\"Phase\",\"55\")]"
    - "SC3 behavior is unchanged (A14 BIT-gate pause/resume retained verbatim — Landmine 4)"
    - "The dead v4 composite-sweep teardown block is removed from SC1 and SC3 factories (Landmine 3)"
    - "Both files COMPILE (excluded from the hermetic run by Category=RealStack but must build)"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/SC1RoundTripE2ETests.cs"
      provides: "Forward round-trip + slot-array index assertion + A19 net-zero"
      contains: "msg:"
    - path: "tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs"
      provides: "BIT-gate pause/resume outage proof (retagged, A14 unchanged)"
      contains: "Phase"
  key_links:
    - from: "SC1RoundTripE2ETests.cs"
      to: "skp:msg:{messageId} HASH"
      via: "ScanMessageIndexKeys() scanning pattern skp:msg:*"
      pattern: "msg:\\*"
    - from: "SC1RoundTripE2ETests.cs"
      to: "A19 two-key net-zero"
      via: "PollForKeyAbsentAsync on BOTH skp:data and skp:msg"
      pattern: "PollForKeyAbsent"
---

<objective>
Adapt SC1 (forward round-trip) to additionally assert the v5 slot-array allocation-index write `skp:msg:{messageId}` (written before the data key — allocation-before-data) and the A19 two-key net-zero at end-of-message; retag SC3 (BIT-gate pause/resume outage) to Phase 55 with no behavior change; and delete the dead v4 composite-sweep teardown block from both factories.

Purpose: TEST-01 requires a RealStack E2E proving the forward pass (dispatch → slot-array index write → data write → orchestrator advance) and the BIT-gate pause/resume across a redis outage (A14). SC1 proves the forward + A19 net-zero halves; SC3 proves the A14 pause/resume half (unchanged from v4).
Output: adapted `SC1RoundTripE2ETests.cs`, retagged `SC3PauseResumeOutageE2ETests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-live-proof-close-gate/55-CONTEXT.md
@.planning/phases/55-live-proof-close-gate/55-RESEARCH.md
@.planning/phases/55-live-proof-close-gate/55-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from source — no codebase exploration needed. -->

From src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:
- L2ProjectionKeys.Prefix = "skp:"
- L2ProjectionKeys.ExecutionData(Guid entryId) => "skp:data:{entryId:D}"
- L2ProjectionKeys.MessageIndex(Guid messageId) => "skp:msg:{messageId:D}"  (a Redis HASH of int-slot → entryId)

Production order under proof (src/BaseProcessor.Core/Processing/ProcessorPipeline.cs:259-275 — allocation-before-data):
- (1) entryId = NewId.NextGuid()
- (2) HashSetAsync(MessageIndex(messageId), slot, entryId.ToString("D"))   // ALLOCATION INDEX FIRST (SLOT-01)
- (3) StringSetAsync(ExecutionData(entryId), item.Data, executionDataTtl)  // DATA SECOND (SLOT-02)

A19 two-key DEL at end-of-message (ProcessorPipeline.cs:310-315 — DeleteTerminalAsync):
- db.KeyDeleteAsync(new RedisKey[]{ ExecutionData(d.EntryId), MessageIndex(messageId) })

BitHealthLoop edges (src/Keeper/Health/BitHealthLoop.cs:48-68 — UNCHANGED from v4):
- bus.Publish(new PauseAll{...}) on the unhealthy edge; bus.Publish(new ResumeAll{...}) on the healthy edge
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt SC1 — add skp:msg:* allocation-index assertion + A19 two-key net-zero, retag Phase 55, delete composite sweep</name>
  <files>tests/BaseApi.Tests/Orchestrator/SC1RoundTripE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SC1RoundTripE2ETests.cs (the whole file — current forward round-trip body, the ScanExecutionDataKeys/PollForNewExecutionDataKeyAsync helpers at :266-286, the DisposeAsync teardown at :417-453, the composite sweep at :437-448)
    - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs (for the PollForKeyAbsentAsync helper shape at :224-241 — clone its idiom into SC1)
    - .planning/phases/55-live-proof-close-gate/55-PATTERNS.md (section "SC1RoundTripE2ETests.cs" + "Shared Patterns → Dead composite-sweep removal" + "Net-zero teardown via L2KeysToCleanup")
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (confirm MessageIndex = skp:msg:{messageId:D}, a HASH)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (lines 259-275 allocation-before-data; lines 310-315 the two-key DEL — the production order/net-zero under proof)
  </read_first>
  <behavior>
    - Test asserts: after POST /api/v1/orchestration/start, a NEW skp:msg:* HASH key appears that was not in the before-snapshot (slot-array allocation index written).
    - Test asserts: a NEW skp:data:* key appears (data write) — allocation index appeared, ordered alloc-before-data.
    - Test asserts (existing): the orchestrator-advance ES seam log appears.
    - Test asserts (NEW, A19 net-zero): after the round trip completes, BOTH the minted skp:data:{entryId} AND skp:msg:{messageId} keys become ABSENT (the two-key DEL), polled via PollForKeyAbsentAsync.
  </behavior>
  <action>
Edit `SC1RoundTripE2ETests.cs` in place. Four changes:

(1) RETAG: change `[Trait("Phase","49")]` to `[Trait("Phase","55")]` (around line 62). Keep `[Trait("Category","E2E")]`, `[Trait("Category","RealStack")]`, `[Collection("Observability")]` unchanged. Update any `Phase=49` mentions in XML doc comments to `Phase 55`.

(2) ADD a `ScanMessageIndexKeys()` helper as a sibling of the existing `ScanExecutionDataKeys()` (at :266-286), scanning the index pattern instead of the data pattern:
```csharp
private static HashSet<string> ScanMessageIndexKeys()
{
    using var mux = ConnectionMultiplexer.Connect(HostRedis);
    var keys = new HashSet<string>(StringComparer.Ordinal);
    foreach (var ep in mux.GetEndPoints())
    {
        var server = mux.GetServer(ep);
        if (!server.IsConnected || server.IsReplica) continue;
        foreach (var key in server.Keys(pattern: $"{L2ProjectionKeys.Prefix}msg:*"))
            keys.Add(key.ToString());
    }
    return keys;
}
```

(3) In the forward test body: snapshot `var msgKeysBefore = ScanMessageIndexKeys();` alongside the existing `dataKeysBefore`, BEFORE the POST. After the POST, poll until a NEW `skp:msg:*` key appears (mirror the existing `PollForNewExecutionDataKeyAsync` shape — write a sibling poll over `ScanMessageIndexKeys()` or inline-poll). Assert the new index key is non-null (the slot-array allocation index was written, allocation-before-data — the production order is `HashSetAsync` index at ProcessorPipeline.cs:262 BEFORE `StringSetAsync` data at :275). Register the minted index key: `factory.L2KeysToCleanup.Add(newMsgKey!.Value);` (D-07 belt-and-suspenders — the A19 path self-cleans the happy case; registration surfaces a leak as a SHA mismatch).

(4) ADD the A19 two-key net-zero assertion AFTER the orchestrator-advance ES proof. Clone SC2's `PollForKeyAbsentAsync(IDatabase db, string key, CancellationToken ct)` helper into SC1 (it polls KeyExistsAsync with exponential backoff up to a deadline, returns true when absent). Open a `ConnectionMultiplexer.Connect(HostRedis)` db, then assert BOTH the minted `skp:data:{entryId}` AND `skp:msg:{messageId}` go absent:
```csharp
Assert.True(await PollForKeyAbsentAsync(db, newDataKey!.Value.ToString(), ct),
    $"A19 net-zero: expected {newDataKey} (skp:data) to be deleted by the two-key DEL.");
Assert.True(await PollForKeyAbsentAsync(db, newMsgKey!.Value.ToString(), ct),
    $"A19 net-zero: expected {newMsgKey} (skp:msg index) to be deleted by the two-key DEL.");
```
This proves the production `DeleteTerminalAsync` two-key DEL (ProcessorPipeline.cs:310-315) — net-zero is ACTIVE, not a TTL race.

(5) DELETE the dead composite-sweep block (`SC1...:437-448`, the `GAP-49-8` block scanning `skp:*:{wfId}:*`). Model-B is retired (Landmine 3). KEEP everything above it: the `L2KeysToCleanup` drain + the `ParentIndexMembersToSrem` SREM stay. Do NOT delete the `Restore()` / `base.DisposeAsync()` tail.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `SC1RoundTripE2ETests.cs` contains `[Trait("Phase", "55")]` and does NOT contain `[Trait("Phase", "49")]`.
    - `SC1RoundTripE2ETests.cs` contains a method `ScanMessageIndexKeys` scanning `Prefix}msg:*` (or equivalent `skp:msg:*` literal).
    - `SC1RoundTripE2ETests.cs` contains `PollForKeyAbsentAsync` (cloned from SC2) and asserts it on BOTH a `skp:data` key and a `skp:msg` key.
    - `SC1RoundTripE2ETests.cs` contains `L2KeysToCleanup.Add` for the minted index key.
    - `SC1RoundTripE2ETests.cs` does NOT contain the substring `skp:*:` (the composite glob is gone) and does NOT contain `GAP-49-8`.
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 (the RealStack test COMPILES; it is not run hermetically).
  </acceptance_criteria>
  <done>SC1 is retagged Phase 55, asserts the new skp:msg allocation index appears (allocation-before-data) and that BOTH keys are net-zero at end-of-message, the composite sweep is removed, and the test compiles 0-warning.</done>
</task>

<task type="auto">
  <name>Task 2: Retag SC3 to Phase 55 and delete its composite-sweep block (no behavior change)</name>
  <files>tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SC3PauseResumeOutageE2ETests.cs (the whole file — the [Trait("Phase","49")] at :92, the RedisOutageSerialCollection at :23-24, the composite sweep at :597-608)
    - .planning/phases/55-live-proof-close-gate/55-PATTERNS.md (section "SC3PauseResumeOutageE2ETests.cs" — retag-only + delete dead block)
    - .planning/phases/55-live-proof-close-gate/55-RESEARCH.md (Landmine 4 — A14 BIT gate unchanged; Pitfall 3 — keep the serial collection)
    - src/Keeper/Health/BitHealthLoop.cs (lines 48-68 — confirm bus.Publish(PauseAll)/ResumeAll edges unchanged from v4)
  </read_first>
  <action>
Edit `SC3PauseResumeOutageE2ETests.cs` in place. Exactly two changes — NO assertion or flow change:

(1) RETAG: change `[Trait("Phase","49")]` to `[Trait("Phase","55")]` (around line 92). KEEP the `RedisOutageSerialCollection` isolation VERBATIM (`[CollectionDefinition("RedisOutageSerial", DisableParallelization = true)]` at :23-24, `[Collection("RedisOutageSerial")]` at :93) — SC3 stops `sk-redis` and MUST stay serial (Pitfall 3). Update the `Phase=49` mentions in the XML doc comments (around :20) to `Phase 55`.

(2) DELETE the dead composite-sweep block (`SC3...:597-608`, the `GAP-49-8` block scanning `skp:*:{wfId}:*`). Model-B is retired (Landmine 3). KEEP the `L2KeysToCleanup` drain + `ParentIndexMembersToSrem` SREM + the `BrokerQueuesToPurge`/`BrokerQueuesToDelete` teardown + `Restore()`/`base.DisposeAsync()`.

The `Global PauseAll`/`Global ResumeAll` ES-seam proofs are correct as-is (Landmine 4 — `BitHealthLoop.cs:48-68` still publishes PauseAll/ResumeAll on each health edge). Do NOT touch them.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `SC3PauseResumeOutageE2ETests.cs` contains `[Trait("Phase", "55")]` and does NOT contain `[Trait("Phase", "49")]`.
    - `SC3PauseResumeOutageE2ETests.cs` still contains `RedisOutageSerial` and `DisableParallelization = true` (serial isolation preserved).
    - `SC3PauseResumeOutageE2ETests.cs` does NOT contain the substring `skp:*:` and does NOT contain `GAP-49-8`.
    - `SC3PauseResumeOutageE2ETests.cs` still contains `Global PauseAll` and `Global ResumeAll` assertions (behavior unchanged).
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0.
  </acceptance_criteria>
  <done>SC3 is retagged Phase 55, the composite sweep is removed, the serial collection + A14 pause/resume assertions are untouched, and the test compiles 0-warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-proc WebApi → host Redis (6380) | The SC1 test scans/polls host Redis over localhost to observe the forward index/data writes and the A19 net-zero — no untrusted external input |
| test factory → live container heartbeat | SC1 gates on the genuine `SourceHash`-keyed liveness poll before driving Start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Repudiation (silent net-zero loss) | SC1 asserting net-zero without proving the index is reclaimed | mitigate | SC1 explicitly polls for BOTH `skp:data:*` AND `skp:msg:*` absence via `PollForKeyAbsentAsync` after the round trip — the index reclaim is asserted directly, not assumed by TTL |
| T-55-05 | Tampering | A stale `skp:*:{wfId}:*` composite glob in teardown matching a future `skp:msg:`-adjacent shape | mitigate | The dead composite sweep is DELETED from both SC1 and SC3 factories (Landmine 3); only the explicit `L2KeysToCleanup` registration remains |
| T-55-06 | Spoofing | SC1 false-passing against a not-live processor | accept (already mitigated upstream) | SC1 reuses `PollForHealthyLivenessAsync` polling the real container heartbeat by genuine embedded `SourceHash` — no synthetic seed |
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 (both adapted tests COMPILE).
- `dotnet run --project tests/BaseApi.Tests -c Release -- --filter-not-trait Category=RealStack` → 0 failed (the RealStack facts are EXCLUDED, hermetic suite still green).
- SC1 contains `skp:msg`/`ScanMessageIndexKeys` + `PollForKeyAbsentAsync` on both key families; neither file contains `skp:*:` or `GAP-49-8`.
</verification>

<success_criteria>
- SC1 proves the forward pass with the slot-array index allocation-before-data + the A19 two-key net-zero, tagged Phase 55, composite sweep removed.
- SC3 proves the A14 pause/resume outage unchanged, tagged Phase 55, composite sweep removed.
- Both compile 0-warning and are excluded from the hermetic run.
</success_criteria>

<output>
After completion, create `.planning/phases/55-live-proof-close-gate/55-02-SUMMARY.md`
</output>
