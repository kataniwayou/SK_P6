---
phase: 50-contracts-slot-array-l2-key-reshape
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
  - src/Messaging.Contracts/KeeperUpdate.cs
  - src/Messaging.Contracts/KeeperCleanup.cs
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
  - src/Keeper/Recovery/UpdateConsumer.cs
  - src/Keeper/Recovery/UpdateConsumerDefinition.cs
  - src/Keeper/Recovery/CleanupConsumer.cs
  - src/Keeper/Recovery/CleanupConsumerDefinition.cs
  - src/Keeper/Recovery/ReinjectConsumer.cs
  - src/Keeper/Recovery/ReinjectConsumerDefinition.cs
  - src/Keeper/Recovery/InjectConsumer.cs
  - src/Keeper/Recovery/DeleteConsumer.cs
  - src/Keeper/Recovery/RecoveryConsumerBase.cs
  - src/Keeper/Program.cs
  - src/Keeper/BackupOptions.cs
  - src/Keeper/RecoveryOptions.cs
  - src/Keeper/appsettings.json
  - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs
  - tests/BaseApi.Tests/Keeper/UpdateConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/CleanupConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/BackupOptionsBoundTests.cs
  - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs
  - tests/BaseApi.Tests/Keeper/RecoveryGateWaitFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs
  - tests/BaseApi.Tests/Processor/PipelinePostFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
  - tests/BaseApi.Tests/Resilience/ModelBContractsRetiredFacts.cs
autonomous: true
requirements: [RETIRE-01, RETIRE-02]
must_haves:
  truths:
    - "L2ProjectionKeys.CompositeBackup builder no longer exists (a reflection guard proves it)"
    - "KeeperUpdate and KeeperCleanup record types no longer exist in Messaging.Contracts"
    - "Keeper.BackupOptions type no longer exists; the composite-backup TTL is gone"
    - "UpdateConsumer/CleanupConsumer + their definitions are deleted; their Program.cs registrations are gone"
    - "Exactly ONE ConsumerDefinition (ReinjectConsumerDefinition) owns UseMessageRetry + the partitioner set on keeper-recovery, with exactly 3 surviving partitioner types"
    - "KeeperReinject (EntryId+Payload) and KeeperDelete (EntryId) are unchanged and match A18"
    - "No dangling doc-comment <see cref> to any deleted symbol survives (0-warning Release+Debug)"
    - "Hermetic suite green (survivor consumer bodies stubbed shape-preserving, not throwing)"
  artifacts:
    - path: "tests/BaseApi.Tests/Resilience/ModelBContractsRetiredFacts.cs"
      provides: "Reflection guard proving CompositeBackup/KeeperUpdate/KeeperCleanup/BackupOptions are gone"
      contains: "ModelBContractsRetiredFacts"
    - path: "src/Keeper/Recovery/ReinjectConsumerDefinition.cs"
      provides: "Re-homed single-owner endpoint definition (retry + 3-type partitioner + PartitionKey/PartitionGuid)"
      contains: "UsePartitioner<KeeperReinject>"
    - path: "src/Keeper/Recovery/RecoveryConsumerBase.cs"
      provides: "Base consumer with the IOptions<BackupOptions> ctor param + TtlDays removed"
      exports: ["RecoveryConsumerBase"]
  key_links:
    - from: "tests/.../RecoveryPartitionFacts.cs"
      to: "ReinjectConsumerDefinition.PartitionKey / PartitionGuid"
      via: "static helper reference (re-pointed from UpdateConsumerDefinition)"
      pattern: "ReinjectConsumerDefinition\\.(PartitionKey|PartitionGuid)"
    - from: "tests/.../ModelBContractsRetiredFacts.cs"
      to: "L2ProjectionKeys / Messaging.Contracts assembly / Keeper assembly"
      via: "reflection (GetMethods / GetTypes)"
      pattern: "CompositeBackup|KeeperUpdate|KeeperCleanup|BackupOptions"
---

<objective>
Remove the v4.0.0 Model-B recovery contract surface at the contract level and keep the
solution buildable 0-warning + the hermetic suite green — the build-keeping spine of Phase 50.

Delete `UPDATE`/`CLEANUP` records + their consumers/definitions, the composite backup key
`corr:wf:proc:exec`, and `BackupOptions` (RETIRE-01, RETIRE-02). Re-home the single-owner
endpoint definition off the deleted `UpdateConsumerDefinition` onto `ReinjectConsumerDefinition`
(D-01). Reduce the three surviving consumer bodies + the `ProcessorPipeline` Post Model-B mechanics
to compile-only, shape-preserving stubs (their real A18 rewrites are Phases 51/52). Reconcile the
entire impacted test surface in the SAME plan so the hermetic suite never goes red mid-flight.

Purpose: This is the "dark-but-compiling pending real retirement" step (D-01/D-02, Phase-43
precedent). The locked build order is 50 (contracts/compile-keep) → 51 (pipeline) → 52 (keeper)
→ 53 (remnant-verify RETIRE-03). Phase 50 makes the symbols GONE and nothing reference them; the
full source/reflection remnant sweep is Phase 53.
Output: a 0-warning (Release+Debug) solution with no Model-B contract reference, a green hermetic
suite, and a `ModelBContractsRetiredFacts` reflection guard.

CRITICAL build-keeping notes:
  - The `IOptions<BackupOptions>` ctor injection lives in `RecoveryConsumerBase` AND all five
    consumers — deleting `BackupOptions` breaks ALL FOUR surviving ctors (base + 3 survivors), not
    just the two composite readers. Treat the BackupOptions removal as ONE atomic edit (Pitfall 1).
  - Dangling `<see cref>` to a deleted symbol is CS1574 = FATAL under TreatWarningsAsErrors.
    Sweep doc-comments, not just code (Pitfall 2). Known live crefs: `RecoveryOptions.cs:4`,
    `InjectConsumer.cs` doc-comment, the `L2ProjectionKeys` `<list>` item.
  - `SC2RecoveryPathsE2ETests` is `Category=RealStack` (excluded from hermetic runs) but STILL
    GATES COMPILATION — its `CompositeBackup` ref breaks the 0-warning build (Pitfall 3).
  - Stub survivor bodies SHAPE-PRESERVING / no-op, NOT `throw NotImplementedException`, or hermetic
    tests publishing to them go red (Pitfall 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-contracts-slot-array-l2-key-reshape/50-CONTEXT.md
@.planning/phases/50-contracts-slot-array-l2-key-reshape/50-RESEARCH.md
@.planning/phases/50-contracts-slot-array-l2-key-reshape/50-PATTERNS.md
@.planning/phases/50-contracts-slot-array-l2-key-reshape/50-VALIDATION.md

<interfaces>
<!-- Concrete current-state excerpts. Mirror/delete verbatim per the action text. -->

DELETE this builder (src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:44-47):
```csharp
public static string CompositeBackup(Guid correlationId, Guid workFlowId, Guid processorId, Guid executionId)
    => $"{Prefix}{correlationId:D}:{workFlowId:D}:{processorId:D}:{executionId:D}";
```

Single-owner being deleted (UpdateConsumerDefinition.cs) — MOVE these onto ReinjectConsumerDefinition:
```csharp
// ctor + fields (:30-38)
private readonly IOptions<RetryOptions> _retryOptions;
private readonly IOptions<RecoveryOptions> _recoveryOptions;
public UpdateConsumerDefinition(IOptions<RetryOptions> retryOptions, IOptions<RecoveryOptions> recoveryOptions)
{
    _retryOptions = retryOptions;
    _recoveryOptions = recoveryOptions;
    EndpointName = KeeperQueues.Recovery;
}
// ConfigureConsumer (:47-66) — DROP the KeeperUpdate + KeeperCleanup partitioner lines:
endpointConfigurator.UseMessageRetry(r => r.Immediate(_retryOptions.Value.Limit));
var partition = new Partitioner(_recoveryOptions.Value.PartitionCount, new Murmur3UnsafeHashGenerator());
endpointConfigurator.UsePartitioner<KeeperReinject>(partition, p => PartitionGuid(p.Message));
endpointConfigurator.UsePartitioner<KeeperInject>(partition, p => PartitionGuid(p.Message));
endpointConfigurator.UsePartitioner<KeeperDelete>(partition, p => PartitionGuid(p.Message));
// static helpers (:74-84) — MOVE VERBATIM (byte-pinned by RecoveryPartitionFacts, do NOT re-derive):
public static string PartitionKey(IKeeperRecoverable m) =>
    $"{m.CorrelationId:D}:{m.WorkflowId:D}:{m.ProcessorId:D}:{m.ExecutionId:D}";
public static Guid PartitionGuid(IKeeperRecoverable m)
{
    var hash = SHA256.HashData(Encoding.UTF8.GetBytes(PartitionKey(m)));
    return new Guid(hash.AsSpan(0, 16));
}
// usings to carry: System.Security.Cryptography, System.Text, MassTransit,
// MassTransit.Middleware (Partitioner), Microsoft.Extensions.Options
```

RecoveryConsumerBase current ctor (RecoveryConsumerBase.cs:26-38) — DROP backupOptions param + TtlDays:
```csharp
public abstract class RecoveryConsumerBase<TMessage>(
    IConnectionMultiplexer redis, ISendEndpointProvider sendProvider, IL2HealthGate gate,
    IOptions<RetryOptions> retryOptions, IOptions<RecoveryOptions> recoveryOptions,
    IOptions<BackupOptions> backupOptions) : IConsumer<TMessage>   // <- drop backupOptions
    where TMessage : class, IKeeperRecoverable
{
    ...
    protected int TtlDays => backupOptions.Value.TtlDays;   // <- DELETE (only UpdateConsumer used it)
```

DeleteConsumer (the cleanest composite-free survivor analog, DeleteConsumer.cs:14-21) — drop the 6th arg:
```csharp
public sealed class DeleteConsumer(
    IConnectionMultiplexer redis, ISendEndpointProvider sendProvider, IL2HealthGate gate,
    IOptions<RetryOptions> retryOptions, IOptions<RecoveryOptions> recoveryOptions)   // backupOptions dropped
    : RecoveryConsumerBase<KeeperDelete>(redis, sendProvider, gate, retryOptions, recoveryOptions)  // last arg dropped
{
    protected override async Task HandleAsync(KeeperDelete m, CancellationToken ct)
        => await Guard(() => Db.KeyDeleteAsync(L2ProjectionKeys.ExecutionData(m.EntryId)), ct);
}
```

InjectConsumer current body references CompositeBackup at line 36 (src/Keeper/Recovery/InjectConsumer.cs).
ProcessorPipeline Model-B sends to delete: lines 139 (BuildUpdate) + 148 (BuildCleanup); builders to
delete: 237-238 (BuildUpdate) + 243-244 (BuildCleanup). Survivors BuildReinject/BuildDelete/BuildInject
(231-241) stay.

RecoveryOptions.cs:4 dangling cref: `<see cref="BackupOptions"/>` → remove the clause.
Keeper appsettings.json:33 has a `"Backup": { ... }` section → delete it (dead config).

Reflection-guard analog (tests/BaseApi.Tests/Resilience/ReactivePathRetiredFacts.cs) — assembly anchors:
```csharp
private static readonly Assembly Keeper = typeof(global::Keeper.Health.BitHealthLoop).Assembly;
private static readonly Assembly Contracts = typeof(global::Messaging.Contracts.KeeperInject).Assembly;
private static readonly Assembly BaseProcessorCore =
    typeof(global::BaseProcessor.Core.Processing.ProcessorPipeline).Assembly;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic Model-B deletion across src + single-owner re-home + compile-keeping stubs</name>
  <files>src/Messaging.Contracts/Projections/L2ProjectionKeys.cs, src/Messaging.Contracts/KeeperUpdate.cs, src/Messaging.Contracts/KeeperCleanup.cs, src/BaseProcessor.Core/Processing/ProcessorPipeline.cs, src/Keeper/Recovery/UpdateConsumer.cs, src/Keeper/Recovery/UpdateConsumerDefinition.cs, src/Keeper/Recovery/CleanupConsumer.cs, src/Keeper/Recovery/CleanupConsumerDefinition.cs, src/Keeper/Recovery/ReinjectConsumer.cs, src/Keeper/Recovery/ReinjectConsumerDefinition.cs, src/Keeper/Recovery/InjectConsumer.cs, src/Keeper/Recovery/DeleteConsumer.cs, src/Keeper/Recovery/RecoveryConsumerBase.cs, src/Keeper/Program.cs, src/Keeper/BackupOptions.cs, src/Keeper/RecoveryOptions.cs, src/Keeper/appsettings.json</files>
  <read_first>
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (CompositeBackup at 44-47 + the class &lt;list&gt; doc-comment)
    - src/Messaging.Contracts/KeeperUpdate.cs, KeeperCleanup.cs (the two records to delete)
    - src/Messaging.Contracts/KeeperReinject.cs, KeeperDelete.cs (verify-unchanged — must still match A18)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (lines 35-38 doc, 137-157 Post branch, 139+148 sends, 231-244 builders)
    - src/Keeper/Recovery/UpdateConsumerDefinition.cs (the single-owner being moved — read in full)
    - src/Keeper/Recovery/ReinjectConsumerDefinition.cs (current no-op re-home target)
    - src/Keeper/Recovery/RecoveryConsumerBase.cs, InjectConsumer.cs, ReinjectConsumer.cs, DeleteConsumer.cs (the ctor fan-out + InjectConsumer composite body)
    - src/Keeper/Program.cs (lines 28-30 Configure, 46-55 consumer registrations + doc-comment)
    - src/Keeper/RecoveryOptions.cs (line 4 dangling cref), src/Keeper/BackupOptions.cs, src/Keeper/appsettings.json (Backup section at line 33)
    - .planning/phases/50-contracts-slot-array-l2-key-reshape/50-PATTERNS.md (sections: ProcessorPipeline, single-owner re-home, RecoveryConsumerBase+3 survivors, Program.cs, RecoveryOptions cref-fix)
    - .planning/phases/50-contracts-slot-array-l2-key-reshape/50-RESEARCH.md (Break Analysis B1-B6 + Pitfalls 1-5)
  </read_first>
  <action>
    Do this as ONE coordinated edit so the src projects build 0-warning at task end (the test
    project is reconciled in Task 2). Order within the task: delete records last is fine — the build
    only needs to be green at task completion.

    A. CONTRACT DELETIONS (Messaging.Contracts):
       1. DELETE src/Messaging.Contracts/KeeperUpdate.cs entirely.
       2. DELETE src/Messaging.Contracts/KeeperCleanup.cs entirely.
       3. In L2ProjectionKeys.cs DELETE the `CompositeBackup` builder (lines 44-47) AND its
          `<summary>` doc-comment. In the class `<list>` doc-comment (19-26) DELETE the
          `CompositeBackup` `<item>` (line 25). (MessageIndex item was already added in Plan 01.)
       4. VERIFY (no edit) KeeperReinject.cs carries `EntryId`(Guid)+`Payload`(string="") and
          KeeperDelete.cs carries `EntryId`(Guid) — both unchanged, matching A18 (D-09).

    B. PROCESSORPIPELINE build-keep (src/BaseProcessor.Core/Processing/ProcessorPipeline.cs):
       1. DELETE the `BuildUpdate` builder (lines 237-238) and the `BuildCleanup` builder (243-244).
       2. DELETE the two Post sends that call them: line 139 `await SendKeeper(BuildUpdate(d, item), limit, ct);`
          and line 148 `await SendKeeper(BuildCleanup(d, item), limit, ct);`.
       3. CONFIRM the Completed branch (137-157) still compiles after removing those two lines — the
          L2 write + entryId mint + StepCompleted send + the `write.Succeeded`/BuildInject branch
          (143-147) + the `logger.BeginScope` block (149-156) all stay. Do NOT throw — this is a
          shape-preserving removal (D-01 no-op path).
       4. Sweep the Post-stage doc-comment (lines ~35-38) + the inline comments on 139/148 that name
          `KeeperUpdate`/`KeeperCleanup`/composite — rewrite to plain prose; ensure NO `<see cref>`
          to a deleted symbol remains. Survivors BuildReinject/BuildDelete/BuildInject stay untouched.

    C. KEEPER CONSUMER ATOMIC FAN-OUT (BackupOptions removal — RESEARCH Pitfall 1):
       1. DELETE src/Keeper/BackupOptions.cs.
       2. In RecoveryConsumerBase.cs: remove the `IOptions<BackupOptions> backupOptions` ctor
          parameter (line 32) AND the `protected int TtlDays => backupOptions.Value.TtlDays;`
          property (line 38). Remove the now-unused `using Messaging.Contracts.Configuration;` ONLY
          if nothing else in the file uses it (RetryOptions/RecoveryOptions also live there — verify;
          if still used, keep the using). Update the base doc-comment that says "five Keeper
          recovery-state consumers" → "three" and the per-state list "(UPDATE/REINJECT/INJECT/
          DELETE/CLEANUP)" → "(REINJECT/INJECT/DELETE)".
       3. In ReinjectConsumer.cs, InjectConsumer.cs, DeleteConsumer.cs: drop the
          `IOptions<BackupOptions> backupOptions` ctor param AND drop the trailing `backupOptions`
          arg from the `: RecoveryConsumerBase<T>(...)` base-call (mirror the DeleteConsumer excerpt
          in <interfaces>). Remove any now-unused `BackupOptions`-only using.
       4. InjectConsumer.cs BODY STUB (Pitfall 5 — shape-preserving, NOT throw): the current body
          (34-65) reads `CompositeBackup`, mints entryId, writes ExecutionData, sends StepCompleted,
          deletes composite. The composite read at line 36 references the deleted builder. Reduce the
          body to a no-op shape-preserving stub that compiles 0-warning and does NOT throw — e.g. an
          empty `Task.CompletedTask` return (the real A18 INJECT body — write `L2[m.EntryId]=m.Data`,
          send, delete `m.DeleteEntryId` — is Phase 52). Rewrite the InjectConsumer `<summary>`
          doc-comment so it no longer references the composite read; KEEP/ensure any `<see cref>` it
          retains points only at SURVIVING symbols (StepCompleted survives; RecoveryDataGoneException
          survives) — remove any cref to a deleted symbol.
       5. ReinjectConsumer.cs body references no deleted contract — only the ctor arg drops. Keep its
          gate-then-op shape (do NOT throw) so RecoveryDeadLetterFacts' data-gone path stays green.

    D. SINGLE-OWNER RE-HOME (D-01):
       1. DELETE src/Keeper/Recovery/UpdateConsumer.cs, UpdateConsumerDefinition.cs,
          CleanupConsumer.cs, CleanupConsumerDefinition.cs.
       2. Rewrite src/Keeper/Recovery/ReinjectConsumerDefinition.cs to BE the single owner: add the
          `(IOptions<RetryOptions>, IOptions<RecoveryOptions>)` ctor + fields + `EndpointName =
          KeeperQueues.Recovery;`, the `UseMessageRetry` + shared `Partitioner` + the THREE surviving
          `UsePartitioner<KeeperReinject>/<KeeperInject>/<KeeperDelete>` lines (DROP the
          KeeperUpdate/KeeperCleanup lines — those types are gone), and MOVE the
          `PartitionKey`/`PartitionGuid` static helpers VERBATIM (byte-pinned — do not re-derive).
          Carry the usings (System.Security.Cryptography, System.Text, MassTransit,
          MassTransit.Middleware, Microsoft.Extensions.Options). Use the exact excerpt in <interfaces>.
       3. Leave InjectConsumerDefinition.cs and DeleteConsumerDefinition.cs as intentional no-ops —
          preserve the single-owner invariant (exactly one definition owns retry+partitioner).

    E. PROGRAM + OPTIONS:
       1. In src/Keeper/Program.cs DELETE the `Configure<BackupOptions>` line (28-30, incl. its
          comment), and the two orphan `AddConsumer` lines (51 UpdateConsumer, 55 CleanupConsumer);
          KEEP the three survivor registrations (52-54). Update the doc-comment block (46-50) "five
          gate-open-only recovery consumers" → "three" and re-name the SINGLE OWNER from
          `UpdateConsumerDefinition` to `ReinjectConsumerDefinition`. Remove any unused BackupOptions using.
       2. In src/Keeper/RecoveryOptions.cs remove the dangling `<see cref="BackupOptions"/>` clause at
          line 4 (replace with `<see cref="ProbeOptions"/>` only, or plain prose). No structural change.
       3. In src/Keeper/appsettings.json DELETE the `"Backup": { ... }` section (line 33) — dead config.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - grep across src/ for `CompositeBackup` returns ZERO hits.
    - grep across src/ for `KeeperUpdate` returns ZERO hits; `KeeperCleanup` returns ZERO hits.
    - grep across src/ for `BackupOptions` returns ZERO hits; `BuildUpdate`/`BuildCleanup`/`ValidatedData` return ZERO hits.
    - Files deleted (do not exist): src/Messaging.Contracts/KeeperUpdate.cs, KeeperCleanup.cs; src/Keeper/BackupOptions.cs; src/Keeper/Recovery/{Update,Cleanup}Consumer.cs + {Update,Cleanup}ConsumerDefinition.cs.
    - src/Keeper/Recovery/ReinjectConsumerDefinition.cs contains `EndpointName = KeeperQueues.Recovery`, `UseMessageRetry`, exactly three `UsePartitioner<` occurrences (KeeperReinject, KeeperInject, KeeperDelete), and `public static Guid PartitionGuid` + `public static string PartitionKey`.
    - grep src/ for `UsePartitioner<KeeperUpdate>` and `UsePartitioner<KeeperCleanup>` returns ZERO hits.
    - src/Keeper/RecoveryOptions.cs has NO `<see cref="BackupOptions"/>` (grep returns zero).
    - src/Keeper/appsettings.json has NO `"Backup"` key (grep returns zero).
    - InjectConsumer/ReinjectConsumer/DeleteConsumer bodies contain NO `throw new NotImplementedException` (grep returns zero — shape-preserving stubs).
    - `dotnet build SK_P.sln -c Release` is 0-warning (note: the TEST project may still have refs to deleted symbols until Task 2 — run this build on the src projects; the full-solution 0-warning gate is asserted at Task 3 after Task 2 reconciles tests).
  </acceptance_criteria>
  <done>All Model-B symbols deleted from src; single-owner re-homed onto ReinjectConsumerDefinition with 3 partitioner types; survivors stubbed shape-preserving; no dangling cref in src; src builds 0-warning.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the impacted test surface (delete / re-point / stub)</name>
  <files>tests/BaseApi.Tests/Contracts/KeeperContractTests.cs, tests/BaseApi.Tests/Keeper/UpdateConsumerFacts.cs, tests/BaseApi.Tests/Keeper/CleanupConsumerFacts.cs, tests/BaseApi.Tests/Keeper/BackupOptionsBoundTests.cs, tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs, tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs, tests/BaseApi.Tests/Keeper/RecoveryGateWaitFacts.cs, tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs, tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs, tests/BaseApi.Tests/Processor/PipelinePostFacts.cs, tests/BaseApi.Tests/Processor/DispatchTestKit.cs, tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs</files>
  <read_first>
    - .planning/phases/50-contracts-slot-array-l2-key-reshape/50-RESEARCH.md (Test Impact Map — the authoritative per-file action table)
    - .planning/phases/50-contracts-slot-array-l2-key-reshape/50-PATTERNS.md (section "Test re-home / drop / stub")
    - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs (the 5-contract assertions to reduce to 3)
    - tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs (8 refs to UpdateConsumerDefinition.PartitionKey/PartitionGuid)
    - tests/BaseApi.Tests/Keeper/RecoveryGateWaitFacts.cs (ProbeConsumer : RecoveryConsumerBase<KeeperCleanup> + BackupOptions ctor arg)
    - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs (the Backup() helper at 36-37 + every consumer ctor build)
    - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs (BackupOptions at 66,158 + ReinjectConsumer ctor)
    - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs (composite behavioral assertions)
    - tests/BaseApi.Tests/Processor/PipelinePostFacts.cs (KeeperUpdate/KeeperCleanup via SentKeeper.OfType<>)
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (KeeperUpdate/KeeperCleanup in a doc comment ~250-251)
    - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs (CompositeBackup ref ~line 165, STATE-3 INJECT block 152-196)
  </read_first>
  <action>
    Apply the RESEARCH Test Impact Map exactly. Every change keeps the hermetic suite GREEN and the
    test assembly COMPILING (Pitfall 3: RealStack tests still gate compilation under 0-warning).

    DELETE these whole files (their subjects are gone):
      - tests/BaseApi.Tests/Keeper/UpdateConsumerFacts.cs
      - tests/BaseApi.Tests/Keeper/CleanupConsumerFacts.cs
      - tests/BaseApi.Tests/Keeper/BackupOptionsBoundTests.cs

    REWRITE tests/BaseApi.Tests/Contracts/KeeperContractTests.cs:
      - Reduce the five-contract assertions to the THREE survivors (KeeperReinject, KeeperInject,
        KeeperDelete). DROP all KeeperUpdate/KeeperCleanup assertions (incl. KeeperUpdate.ValidatedData).
      - ADD assertions that KeeperInject now carries EntryId (Guid), Data (string), DeleteEntryId
        (Guid) — mirror the existing KeeperReinject assertion shape for EntryId/Payload.
      - Keep assertions that all three implement IKeeperRecoverable (the 4-tuple).

    RE-POINT tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs:
      - Replace all 8 `UpdateConsumerDefinition.PartitionKey` / `.PartitionGuid` references with
        `ReinjectConsumerDefinition.PartitionKey` / `.PartitionGuid` (the helpers moved, byte-identical).

    RE-POINT + drop-arg tests/BaseApi.Tests/Keeper/RecoveryGateWaitFacts.cs:
      - Change the `ProbeConsumer : RecoveryConsumerBase<KeeperCleanup>` vehicle type to
        `<KeeperDelete>` (a surviving contract — the test exercises base gate-wait behavior, not
        CLEANUP semantics). Drop the `BackupOptions` ctor arg (40,60) consistent with the new base ctor.

    UPDATE tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs:
      - Remove the `Backup()` helper (36-37). Drop the 6th `backupOptions` arg at EVERY consumer
        construction so the ctor calls match the new base/consumer signatures.

    UPDATE tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs:
      - Drop the `BackupOptions` registration + the 6th ctor arg (66,158). The REINJECT data-gone
        path it exercises stays green because Task 1 kept the ReinjectConsumer body shape-preserving
        (no throw). If any assertion depended on a deleted composite/UPDATE behavior, remove that
        assertion (do NOT keep a failing one).

    REWRITE/DELETE tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs:
      - The Model-B INJECT body is now a stub. DELETE the behavioral facts that asserted the
        composite read/write/delete order (they no longer hold). If nothing meaningful remains,
        delete the file (the A18 INJECT behavior is proven in Phase 52).

    REWRITE/DELETE tests/BaseApi.Tests/Processor/PipelinePostFacts.cs:
      - Remove the facts asserting the removed Model-B Post send order (`KeeperUpdate`/`KeeperCleanup`
        via `SentKeeper.OfType<>` at 62-65,110-113). Keep any facts that assert surviving Post
        behavior (BuildInject path / StepCompleted send) if still valid; otherwise delete the
        Model-B-specific facts.

    UPDATE tests/BaseApi.Tests/Processor/DispatchTestKit.cs:
      - Update the doc-comment (~250-251) that names KeeperUpdate/KeeperCleanup to plain prose (it is
        prose, not a cref, but keep it from drifting / referencing gone types).

    UPDATE tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs (RealStack — MUST COMPILE):
      - Remove/rewrite the STATE-3 INJECT block (152-196) that seeds + reads the composite key; the
        `CompositeBackup` reference (~line 165) breaks compilation under 0-warning. Excise the
        composite seed/read so the file COMPILES; the surviving INJECT/DELETE/REINJECT refs (171+)
        stay if they reference surviving contracts. Do NOT attempt to make this test behaviorally
        correct for A18 — it is RealStack (excluded hermetically) and its full A18 rewrite is Phase 54;
        Phase 50 only needs it to COMPILE. Keep its `[Trait("Category","RealStack")]`.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter-not-trait "Category=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - grep across tests/ for `CompositeBackup` returns ZERO hits.
    - grep across tests/ for `KeeperUpdate`, `KeeperCleanup`, `BackupOptions` returns ZERO hits (the doc-comment in DispatchTestKit also cleaned).
    - grep across tests/ for `UpdateConsumerDefinition` returns ZERO hits (RecoveryPartitionFacts re-pointed).
    - Files deleted: tests/BaseApi.Tests/Keeper/UpdateConsumerFacts.cs, CleanupConsumerFacts.cs, BackupOptionsBoundTests.cs.
    - tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs contains `ReinjectConsumerDefinition.PartitionGuid` (grep).
    - tests/BaseApi.Tests/Keeper/RecoveryGateWaitFacts.cs contains `RecoveryConsumerBase<KeeperDelete>` and NO `KeeperCleanup` (grep).
    - KeeperContractTests.cs asserts `DeleteEntryId` (grep) and contains no `ValidatedData` (grep zero).
    - The hermetic run `--filter-not-trait "Category=RealStack"` is fully GREEN (no red, no compile error in the test assembly).
  </acceptance_criteria>
  <done>Test surface reconciled; no deleted-symbol reference anywhere in tests/; hermetic suite green; the RealStack SC2 test compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Add the ModelBContractsRetiredFacts reflection guard + both-config 0-warning gate</name>
  <files>tests/BaseApi.Tests/Resilience/ModelBContractsRetiredFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Resilience/ReactivePathRetiredFacts.cs (the whole file — the Phase-48 RETIRE template; FACT 4 lines 128-147 is the closest single analog; assembly anchors at 31-39)
    - .planning/phases/50-contracts-slot-array-l2-key-reshape/50-PATTERNS.md (section "NEW ModelBContractsRetiredFacts.cs — reflection guard")
    - .planning/phases/50-contracts-slot-array-l2-key-reshape/50-VALIDATION.md (SC-2 row)
  </read_first>
  <behavior>
    - Fact 1: typeof(L2ProjectionKeys).GetMethods(Public|Static).Where(m => m.Name == "CompositeBackup") is EMPTY.
    - Fact 2: the Messaging.Contracts assembly has NO type named "KeeperUpdate" and NO type named "KeeperCleanup".
    - Fact 3: the Keeper assembly has NO type named "BackupOptions".
    - Fact 4 (positive survivor): L2ProjectionKeys DOES retain a single-Guid-overload `MessageIndex` and a single-Guid-overload `ExecutionData`.
  </behavior>
  <action>
    1. CREATE tests/BaseApi.Tests/Resilience/ModelBContractsRetiredFacts.cs mirroring
       ReactivePathRetiredFacts.cs structure verbatim:
       - Assembly anchors:
         ```csharp
         private static readonly Assembly Contracts = typeof(global::Messaging.Contracts.KeeperInject).Assembly;
         private static readonly Assembly Keeper = typeof(global::Keeper.Health.BitHealthLoop).Assembly;
         ```
       - `[Trait("Phase","50")]` on the class; `[Fact]` methods for the four behaviors above.
       - Use the FACT-4 reflection idiom (`GetMethods(BindingFlags.Public | BindingFlags.Static)
         .Where(m => m.Name == "...")` + `Assert.Empty` / `Assert.Single`) and the FACT-1 type idiom
         (`Assert.DoesNotContain(asm.GetTypes(), t => t.Name == "...")`).
       - Fact 1: `Assert.Empty(typeof(L2ProjectionKeys).GetMethods(Public|Static).Where(m => m.Name == "CompositeBackup"))`.
       - Fact 2: `Assert.DoesNotContain(Contracts.GetTypes(), t => t.Name == "KeeperUpdate" || t.Name == "KeeperCleanup")`.
       - Fact 3: `Assert.DoesNotContain(Keeper.GetTypes(), t => t.Name == "BackupOptions")`.
       - Fact 4: `Assert.Single(... where name == "MessageIndex")` with a single Guid parameter, and the
         same for "ExecutionData" (positive survivor assertion).
       NOTE this is the LIGHTWEIGHT in-phase SC-2 guard — the full source/reflection remnant sweep
       (RETIRE-03) is Phase 53. Do not attempt a full sweep here.
    2. Run the both-config 0-warning gate + the full hermetic suite as the phase-close verification.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release; dotnet build SK_P.sln -c Debug; dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter-not-trait "Category=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - tests/BaseApi.Tests/Resilience/ModelBContractsRetiredFacts.cs exists and contains `CompositeBackup`, `KeeperUpdate`, `KeeperCleanup`, `BackupOptions` (as reflection target NAME strings) and `MessageIndex` (positive survivor).
    - The `ModelBContractsRetiredFacts` facts pass (the symbols are confirmed gone via reflection).
    - `dotnet build SK_P.sln -c Release` is 0-warning.
    - `dotnet build SK_P.sln -c Debug` is 0-warning.
    - The full hermetic run `--filter-not-trait "Category=RealStack"` is GREEN.
  </acceptance_criteria>
  <done>Reflection guard green; solution 0-warning Release AND Debug; full hermetic suite green — Phase 50 SC-2/SC-4 met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (boundary REDUCED) | This plan DELETES a trust-relevant surface (the composite-backup key + its consumers). It introduces no new boundary. The keeper still consumes internal-bus recovery messages on `keeper-recovery` — unchanged, just fewer message types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03 | I (Information Disclosure) | Deleted composite backup key `skp:{corr}:{wf}:{proc}:{exec}` | mitigate (by removal) | Deleting the composite-backup key + its 2-day TTL REDUCES attack surface — one fewer L2 key class holding correlated workflow identifiers. Live keys self-expire via TTL; no migration. |
| T-50-04 | D (Denial of Service) | Survivor consumer stubs (no-op, not throwing) | accept | Stubs are shape-preserving no-ops with no I/O — they cannot loop, allocate unboundedly, or amplify load. Real bodies (Phase 51/52) re-introduce the gated/bounded RetryLoop. Gate-closed/exhaustion behavior is a Phase-52 concern, not regressed here. |
| T-50-05 | T (Tampering) | `ReinjectConsumerDefinition` partitioner re-home | accept | `PartitionKey`/`PartitionGuid` moved VERBATIM (byte-pinned by RecoveryPartitionFacts) — no algorithm change, no slot-drift, no new input. The single-owner invariant is preserved (exactly one definition owns retry+partitioner). |

No high/critical threats introduced; this is a contract/code removal + compile-keeping change with NO new runtime behavior, I/O, or external surface. Net security posture: attack surface reduced.
</threat_model>

<verification>
- grep src/ AND tests/ for `CompositeBackup|KeeperUpdate|KeeperCleanup|BackupOptions|BuildUpdate|BuildCleanup|ValidatedData` → ZERO hits.
- `dotnet build SK_P.sln -c Release` AND `-c Debug` both 0-warning (TreatWarningsAsErrors fatal — proves no dangling cref/unused-using).
- `dotnet test ... --filter-not-trait "Category=RealStack"` green (incl. ModelBContractsRetiredFacts + the re-pointed RecoveryPartitionFacts/RecoveryGateWaitFacts).
- Exactly ONE ConsumerDefinition owns retry+partitioner on keeper-recovery; exactly 3 UsePartitioner types.
</verification>

<success_criteria>
- `UPDATE`/`CLEANUP` contracts + composite backup key `corr:wf:proc:exec` + `BackupOptions` deleted; a source/reflection scan finds no references (ROADMAP SC-2 / RETIRE-01, RETIRE-02).
- `INJECT`/`REINJECT`/`DELETE` carry their A18 id sets in Messaging.Contracts (ROADMAP SC-3 — REINJECT/DELETE verified-unchanged; INJECT fields added in Plan 01, asserted by KeeperContractTests here).
- Solution builds 0-warning Release + Debug; hermetic suite green (ROADMAP SC-4).
- Single-owner endpoint invariant preserved on re-home; survivor stubs keep the hermetic suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/50-contracts-slot-array-l2-key-reshape/50-02-SUMMARY.md`
</output>
