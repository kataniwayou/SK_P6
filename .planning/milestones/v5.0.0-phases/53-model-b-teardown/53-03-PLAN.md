---
phase: 53-model-b-teardown
plan: 03
type: execute
wave: 1
depends_on: [53-01]
files_modified:
  - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
  - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
  - src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs
  - src/Keeper/Recovery/RecoveryEndpointBinder.cs
autonomous: true
requirements: [RETIRE-03]

must_haves:
  truths:
    - "The processor dispatch keep-latch (cfg.UseMessageRetry on the EntryStepDispatch endpoint) is removed; a send-exhaust throw → broker nack-requeue redelivery (D-01)."
    - "The dead processor-startup retryOptions (used only for the removed latch) is removed; the processor PIPELINE's IOptions<RetryOptions> STAYS (in-code RetryLoop reads Retry:Limit)."
    - "The ProcessorPipeline + EntryStepDispatchConsumer `→ _error` narrative comments are reconciled to `throw → broker redelivery` (the `throw sent.Error!` LINES are unchanged — comment-only)."
    - "The global ConfigureError(GenerateFaultFilter + ConsolidatedErrorTransportFilter) callback is deleted from BaseConsole.Core and MOVED verbatim into RecoveryEndpointBinder's connect-callback (D-03 keeper-local) — skp-dlq-1 topology declaration is KEPT."
    - "Solution builds 0-warning Release + Debug (SC-3); skp-dlq-1 becomes keeper-only."
  artifacts:
    - path: "src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs"
      provides: "Processor dispatch connect-callback with no bus latch (bare ConfigureConsumer tail)"
    - path: "src/Keeper/Recovery/RecoveryEndpointBinder.cs"
      provides: "Keeper-local ConfigureError filter pair (moved from the deleted global callback)"
  key_links:
    - from: "src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs"
      to: "src/Keeper/Recovery/RecoveryEndpointBinder.cs"
      via: "ConfigureError filter pair relocated (global callback deleted → keeper connect-callback)"
      pattern: "ConfigureError\\(ep =>"
    - from: "src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs"
      to: "RabbitMQ broker (nack-requeue redelivery)"
      via: "absence of cfg.UseMessageRetry → throw → broker redelivery"
      pattern: "cfg\\.UseMessageRetry\\("
---

<objective>
Complete the processor + console half of the A18 retry/error teardown (D-01/D-03):
1. Remove the processor dispatch keep-latch (`cfg.UseMessageRetry` in ProcessorStartupOrchestrator) + its
   now-dead `retryOptions` injection — the processor PIPELINE's IOptions<RetryOptions> STAYS.
2. Reconcile the ProcessorPipeline + EntryStepDispatchConsumer `→ _error` doc-comments to the A18 end-state
   (comment-only; the `throw sent.Error!` lines are UNCHANGED).
3. Move the `ConfigureError(GenerateFaultFilter + ConsolidatedErrorTransportFilter)` pair from the global
   `AddConfigureEndpointsCallback` in BaseConsole.Core into RecoveryEndpointBinder's connect-callback
   (D-03 keeper-local). KEEP the skp-dlq-1 topology declaration.

Purpose: turns the Wave-0 D-01 (processor half) + D-03 guards GREEN; keeps the build 0-warning (SC-3);
makes skp-dlq-1 keeper-only.
Output: 5 source files updated; no new runtime behavior; no library changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-model-b-teardown/53-CONTEXT.md
@.planning/phases/53-model-b-teardown/53-PATTERNS.md
@.planning/phases/53-model-b-teardown/53-RESEARCH.md

<interfaces>
<!-- Verbatim excerpts from PATTERNS.md — paste-ready. No codebase exploration needed. -->

ProcessorStartupOrchestrator.cs:172-182 (BEFORE — remove the latch line):
```csharp
var handle = endpointConnector.ConnectReceiveEndpoint(queueName, (ctx, cfg) =>
{
    // D-09 reconcile comment block (174-179) ...
    cfg.UseMessageRetry(r => r.Immediate(retryLimit));        // ◄── REMOVE (outer dead-letter latch)
    cfg.ConfigureConsumer<EntryStepDispatchConsumer>(ctx);
});
```
AFTER — bare ConfigureConsumer tail (no bus retry → throw nack-requeues → broker redelivery). `retryLimit`
(:171) + the `retryOptions` ctor dependency are used ONLY by this latch → remove both (VERIFIED not used
elsewhere in this class; grep the file to confirm before deleting the ctor param) → 0-warning (SC-3).

COUNTER-EXAMPLE (KEEP — do NOT touch): ProcessorPipeline ctor's IOptions<RetryOptions> STAYS — its in-code
RetryLoop reads Retry:Limit (ProcessorPipeline.cs:55-58, ~16 RetryLoop.ExecuteAsync sites). Only the
*startup orchestrator*'s retryOptions (the deleted latch's sole consumer) goes.

ProcessorPipeline.cs comments to reconcile (the `throw sent.Error!` lines :318/:344 are UNCHANGED — only
the trailing narrative changes):
- :55-58 xml-doc: "...→ the bus UseMessageRetry dead-letter latch → _error; the A18 UseMessageRetry=none
  end-state is a Phase-53 teardown item..." → rewrite to "...a send that exhausts PROPAGATES (throws) → no
  bus retry, no error pipeline → RabbitMQ nack-requeue (broker redelivery), no dead-letter (Phase-53 D-01).
  The in-code RetryLoop owns per-op retries."
- :311 banner: "send-exhaustion PROPAGATES (D-10 → bus _error)." → "...PROPAGATES (throw → broker redelivery, no _error)."
- :318 `throw sent.Error!;   // D-10: propagate → UseMessageRetry → _error` → comment → `// propagate → throw → broker redelivery (no _error, Phase-53 D-01)`
- :344 `throw sent.Error!;   // D-10: propagate → _error` → comment → `// propagate → throw → broker redelivery (Phase-53 D-01)`

EntryStepDispatchConsumer.cs:21-23 xml-doc: "...only a send-exhaustion PROPAGATES (→ the runtime
UseMessageRetry dead-letter latch → _error, D-10)." → "...only a send-exhaustion PROPAGATES (throws → broker
nack-requeue redelivery, no _error, Phase-53 D-01)."

BaseConsole.Core/MessagingServiceCollectionExtensions.cs:56-63 (DELETE this global callback):
```csharp
x.AddConfigureEndpointsCallback((context, name, e) =>
{
    e.ConfigureError(ep =>
    {
        ep.UseFilter(new GenerateFaultFilter());                  // keep Fault<T> publication
        ep.UseFilter(new ConsolidatedErrorTransportFilter());     // move exhausted msg → skp-dlq-1
    });
});
```
KEEP UNCHANGED (RESEARCH Pitfall 5 — do NOT over-delete): the skp-dlq-1 topology at lines 98-108
(`c.DeployPublishTopology = true;` + `c.Publish<ConsolidatedFault>(p => p.BindQueue(...x-message-ttl...))`).
The exchange/queue MUST still exist for the keeper to send to it. After deleting 56-63, remove any
now-unused `using BaseConsole.Core.Messaging;` / `using MassTransit.Middleware;` from THIS file if unused.

RecoveryEndpointBinder.cs:80-108 (ADD the moved pair inside the EXISTING
`connector.ConnectReceiveEndpoint(KeeperQueues.Recovery, (ctx, cfg) => { ... })` callback — after the
policy retry block ~:99, before partitioners ~:101):
```csharp
cfg.ConfigureError(ep =>
{
    ep.UseFilter(new GenerateFaultFilter());                  // Fault<T> publication — keeper-only post-Phase-53
    ep.UseFilter(new ConsolidatedErrorTransportFilter());     // exhausted → skp-dlq-1 (Dlq1 mode)
});
```
ADD `using BaseConsole.Core.Messaging;` to RecoveryEndpointBinder.cs (for the two filter types). It already
has `using MassTransit;` + `using MassTransit.Middleware;`. The binder xml-doc (:26-35) already says
"exhaust → consolidated filter → skp-dlq-1" — this move makes that doc literally true keeper-local.
The existing policy retry (Dlq1 Immediate / SustainedOutage Interval) at :86-99 is UNCHANGED (D-05 keeper untouched).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the processor keep-latch + dead retryOptions; reconcile the pipeline `→ _error` comments</name>
  <files>src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs, src/BaseProcessor.Core/Processing/ProcessorPipeline.cs, src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs:170-189 (the connect-callback, :171 retryLimit, :172-182 latch, ctor for retryOptions)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs:54-58, 311, 318, 344 (comments) + ctor (IOptions<RetryOptions> — STAYS)
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs:20-24 (xml-doc)
    - src/Keeper/Recovery/RecoveryEndpointBinder.cs:80-108 (the post-removal A18-clean connect-callback target shape)
    - .planning/phases/53-model-b-teardown/53-PATTERNS.md (ProcessorStartupOrchestrator + ProcessorPipeline sections)
  </read_first>
  <action>
    1. In ProcessorStartupOrchestrator.cs: delete the `cfg.UseMessageRetry(r => r.Immediate(retryLimit));`
       line (:180) inside the `ConnectReceiveEndpoint` callback, leaving the bare
       `cfg.ConfigureConsumer<EntryStepDispatchConsumer>(ctx);` tail. Reconcile the 174-179 D-09 comment
       block to the A18 end-state (no bus latch; throw → broker redelivery; in-code RetryLoop unchanged).
    2. grep this class for `retryOptions` / `retryLimit` to CONFIRM they are used ONLY by the removed latch.
       Verified-not-used-elsewhere: delete `var retryLimit = retryOptions.Value.Limit;` (:171), the
       `retryOptions` ctor parameter + its field/assignment, and `using Microsoft.Extensions.Options;` if now
       unused IN THIS FILE. (If grep shows another use, leave the minimum needed and note it.)
    3. In ProcessorPipeline.cs: reconcile the comments at :55-58, :311, :318, :344 per the <interfaces>
       block above. The `throw sent.Error!;` statements at :318 and :344 are UNCHANGED — edit ONLY their
       trailing `// → _error` narrative. Do NOT touch the ctor's IOptions<RetryOptions> (it STAYS — the
       in-code RetryLoop reads Retry:Limit).
    4. In EntryStepDispatchConsumer.cs: reconcile the :21-23 xml-doc per <interfaces> (comment-only).
    No logic moves. No new behavior.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cfg.UseMessageRetry(" src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs` returns 0.
    - `grep -c "IOptions<RetryOptions>" src/BaseProcessor.Core/Processing/ProcessorPipeline.cs` returns ≥1 (the pipeline injection STAYS).
    - `grep -c "throw sent.Error!" src/BaseProcessor.Core/Processing/ProcessorPipeline.cs` is unchanged from before this plan (the throw lines survive; only comments changed).
    - `dotnet build SK_P.sln -c Release` AND `-c Debug` both exit 0 with 0 warnings (no dead retryOptions in the startup orchestrator → SC-3).
  </acceptance_criteria>
  <done>Processor keep-latch gone + dead startup-orchestrator retryOptions removed; pipeline comments reconciled; pipeline IOptions<RetryOptions> retained; build 0-warning; processor half of FACT 6 (D-01) GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Move the ConfigureError filter pair from the global callback to RecoveryEndpointBinder (D-03)</name>
  <files>src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs, src/Keeper/Recovery/RecoveryEndpointBinder.cs</files>
  <read_first>
    - src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs:46-119 (the global callback :56-63 to DELETE; the skp-dlq-1 topology :73-108 to KEEP)
    - src/Keeper/Recovery/RecoveryEndpointBinder.cs:26-35 (xml-doc — already describes the filter), :80-108 (connect-callback; :86-99 policy retry KEEP; insertion point ~:99-101)
    - src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs:43-59 (Dlq1 const + exchange-addressed send rationale — context only, unchanged)
    - .planning/phases/53-model-b-teardown/53-PATTERNS.md (MessagingServiceCollectionExtensions + RecoveryEndpointBinder sections; Pitfall 5)
  </read_first>
  <action>
    1. In MessagingServiceCollectionExtensions.cs: DELETE the entire `x.AddConfigureEndpointsCallback((context, name, e) => { e.ConfigureError(ep => { ... }); });`
       block (:56-63). KEEP everything else — especially the skp-dlq-1 topology (`c.DeployPublishTopology = true;`
       + `c.Publish<ConsolidatedFault>(p => p.BindQueue(...x-message-ttl...))`, :98-108). Remove any now-unused
       `using BaseConsole.Core.Messaging;` / `using MassTransit.Middleware;` from THIS file (only if the
       deletion made them unused — verify they aren't used by the surviving topology code).
    2. In RecoveryEndpointBinder.cs: ADD the moved pair VERBATIM inside the existing
       `connector.ConnectReceiveEndpoint(KeeperQueues.Recovery, (ctx, cfg) => { ... })` callback, after the
       policy retry block (~:99) and before the partitioners (~:101):
       ```csharp
       cfg.ConfigureError(ep =>
       {
           ep.UseFilter(new GenerateFaultFilter());                  // Fault<T> publication — keeper-only post-Phase-53
           ep.UseFilter(new ConsolidatedErrorTransportFilter());     // exhausted → skp-dlq-1 (Dlq1 mode)
       });
       ```
       Add `using BaseConsole.Core.Messaging;` to RecoveryEndpointBinder.cs. Leave the :86-99 policy retry
       (Dlq1 Immediate / SustainedOutage Interval) UNCHANGED (D-05 — keeper untouched). Optionally tighten
       the :26-35 xml-doc to note the filter now lives keeper-local (it already describes the behavior).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ConfigureError" src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` returns 0.
    - `grep -c "AddConfigureEndpointsCallback" src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` returns 0.
    - `grep -c "Publish<ConsolidatedFault>" src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` returns ≥1 (skp-dlq-1 topology KEPT — Pitfall 5).
    - `grep -c "ConfigureError(ep" src/Keeper/Recovery/RecoveryEndpointBinder.cs` returns 1 (filter pair moved keeper-local).
    - `dotnet build SK_P.sln -c Release` AND `-c Debug` both exit 0 with 0 warnings.
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~ConfigureError_is_keeper_local_only"` PASSES (FACT 7 GREEN).
  </acceptance_criteria>
  <done>ConfigureError moved from BaseConsole.Core global callback to RecoveryEndpointBinder; skp-dlq-1 topology retained; FACT 7 (D-03) GREEN; build 0-warning. skp-dlq-1 is now keeper-only.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + dual-config 0-warning build gate (SC-3) and end-state confirmation</name>
  <files>(no source change — gate task; verifies SK_P.sln build + hermetic suite)</files>
  <read_first>
    - .planning/phases/53-model-b-teardown/53-VALIDATION.md (Sampling Rate — phase gate)
    - .planning/phases/53-model-b-teardown/53-RESEARCH.md (Validation Architecture — phase gate)
  </read_first>
  <action>
    After Plans 02 + 03 task-level changes are merged, run the phase gate. This task is the explicit SC-3 +
    full-hermetic-suite confirmation (no code changes unless a gate fails — if a gate fails, fix the minimal
    offender per the relevant plan's acceptance criteria, never by simplifying scope). Confirm:
    - All 5 Wave-0 facts (FACT 5 reflection + FACTS 6/7/8 source-scan) are GREEN.
    - The full hermetic suite is green.
    - Both Release and Debug builds are 0-warning.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release; dotnet build SK_P.sln -c Debug; dotnet test SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` exits 0 with 0 warnings.
    - `dotnet build SK_P.sln -c Debug` exits 0 with 0 warnings.
    - `dotnet test SK_P.sln -c Release` — full hermetic suite GREEN (0 failures).
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RetiredFacts"` — all RetiredFacts GREEN, including FACTS 5/6/7/8 added in Plan 01.
  </acceptance_criteria>
  <done>SC-3 dual-config 0-warning build + full hermetic suite green; all 5 Phase-53 standing guards GREEN. Phase end-state reached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor dispatch consumer → RabbitMQ broker | Send-exhaust throw → broker nack-requeue redelivery (no _error). |
| keeper recovery endpoint → skp-dlq-1 exchange | Dlq1-mode exhaust → ConsolidatedErrorTransportFilter sends to exchange:skp-dlq-1. Now the SOLE producer into skp-dlq-1 (D-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-DoS | Denial of Service | Unbounded requeue spin on a permanently-failing poison SEND (processor unreachable on the bus — not an L2 outage, BIT gate does not pause it) | accept | DELIBERATELY accepted residual (D-04, A18 — mirrors keeper SustainedOutage spin). Observability (redelivery-count metric) is the bound, deferred — NOT a code change this phase. Severity below `high`. |
| T-53-Topology | Denial of Service | Over-deleting the skp-dlq-1 topology with the global callback → keeper Dlq1 send hits a missing exchange (routing failure) | mitigate | The skp-dlq-1 topology declaration (:98-108) is explicitly KEPT (Pitfall 5); a guard asserts `Publish<ConsolidatedFault>` survives in the file. Only the per-endpoint ConfigureError *application* moves. |
| T-53-Tamper | Tampering/Repudiation | Silent message loss if the processor endpoint accidentally acks-discards instead of nack-requeues | mitigate | No error pipeline added on the processor endpoint; default with neither retry nor error filter is nack-requeue. Asserted by the Wave-0 D-01 guard (FACT 6) + optional Phase-54 live throw-spike. |
</threat_model>

<verification>
- `grep -c "cfg.UseMessageRetry(" src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs` → 0.
- `grep -c "ConfigureError" src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` → 0; `grep -c "ConfigureError(ep" src/Keeper/Recovery/RecoveryEndpointBinder.cs` → 1.
- `grep -c "Publish<ConsolidatedFault>" src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` → ≥1 (topology KEPT).
- `dotnet build SK_P.sln -c Release` AND `-c Debug` → 0 warnings; `dotnet test SK_P.sln -c Release` GREEN.
</verification>

<success_criteria>
- Processor dispatch keep-latch removed; startup-orchestrator dead retryOptions removed; pipeline IOptions<RetryOptions> retained.
- ProcessorPipeline + EntryStepDispatchConsumer `→ _error` comments reconciled (throw lines unchanged).
- ConfigureError filter pair moved BaseConsole.Core → RecoveryEndpointBinder (D-03); skp-dlq-1 topology kept; skp-dlq-1 keeper-only.
- FACTS 6 (processor half) + 7 GREEN. SC-3 0-warning Release + Debug; full hermetic suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/53-model-b-teardown/53-03-SUMMARY.md`.
</output>
