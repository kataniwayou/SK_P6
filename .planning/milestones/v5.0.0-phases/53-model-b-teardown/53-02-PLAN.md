---
phase: 53-model-b-teardown
plan: 02
type: execute
wave: 1
depends_on: [53-01]
files_modified:
  - src/Orchestrator/Consumers/StartOrchestrationConsumerDefinition.cs
  - src/Orchestrator/Consumers/StopOrchestrationConsumerDefinition.cs
  - src/Orchestrator/Consumers/StepCompletedConsumerDefinition.cs
  - src/Orchestrator/Consumers/PauseWorkflowConsumerDefinition.cs
  - src/Orchestrator/Consumers/PauseAllConsumerDefinition.cs
  - src/Orchestrator/Consumers/ResumeAllConsumerDefinition.cs
autonomous: true
requirements: [RETIRE-03]

must_haves:
  truths:
    - "No orchestrator consumer definition registers UseMessageRetry — Start, Stop, StepCompleted, PauseWorkflow, PauseAll all drop their outer bus retry (D-01/D-02)."
    - "The dual-owner retry on the shared `orchestrator` endpoint (Start AND Stop) is stripped from BOTH (RESEARCH Pitfall 3)."
    - "The dead Ignore<WorkflowRootNotFoundException> dies with the retry block in both Start/Stop (D-07 — pure teardown, no catch/DLQ seam added)."
    - "Every now-dead IOptions<RetryOptions> field+ctor-param is removed; ConcurrentMessageLimit=1 on Pause/PauseAll is KEPT; the solution builds 0-warning Release + Debug (SC-3)."
    - "Now-stale doc-comments that still describe a UseMessageRetry call are reconciled so the D-01 source-scan guard's call-pattern stays clean and the comments don't lie."
  artifacts:
    - path: "src/Orchestrator/Consumers/StartOrchestrationConsumerDefinition.cs"
      provides: "Start definition collapsed to no-op shape (dual EndpointName, no retry, no IOptions, no Ignore<>)"
    - path: "src/Orchestrator/Consumers/PauseWorkflowConsumerDefinition.cs"
      provides: "Pause definition retaining ConcurrentMessageLimit=1, retry removed"
  key_links:
    - from: "src/Orchestrator/Consumers/*ConsumerDefinition.cs"
      to: "RabbitMQ broker (nack-requeue redelivery)"
      via: "absence of UseMessageRetry → throw → broker redelivery (no _error, no DLQ)"
      pattern: "endpointConfigurator\\.UseMessageRetry\\("
---

<objective>
Strip the outer bus `UseMessageRetry` from ALL orchestrator consumer definitions (D-01/D-02): Start, Stop,
StepCompleted, PauseWorkflow, PauseAll. The shared `orchestrator` endpoint has TWO owners (Start AND Stop) —
strip both (RESEARCH Pitfall 3). Delete each now-dead `IOptions<RetryOptions>` field+ctor-param and the dead
`Ignore<WorkflowRootNotFoundException>` (D-07 — it guards a throw that never fires; pure teardown, NO
catch/DLQ seam). KEEP `ConcurrentMessageLimit=1` on Pause/PauseAll (serialization, not retry). Reconcile the
now-stale doc-comments. After this plan, a send that exhausts its in-code RetryLoop throws → broker
nack-requeue redelivery (no `_error`, no dead-letter) on every orchestrator endpoint.

Purpose: turns the Wave-0 D-01 (orchestrator half) + D-07 guards GREEN; keeps the build 0-warning (SC-3).
Output: 5 orchestrator definitions collapsed to the no-op sibling shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-model-b-teardown/53-CONTEXT.md
@.planning/phases/53-model-b-teardown/53-PATTERNS.md

<interfaces>
<!-- The post-removal TARGET shape is the existing no-op sibling. Mirror it. No codebase exploration needed. -->

ANALOG (post-removal target) — src/Orchestrator/Consumers/StepFailedConsumerDefinition.cs:
  parameterless ctor, sets only EndpointName, NO UseMessageRetry, NO IOptions<RetryOptions>.
  StepCompleted should collapse to this shape. ResumeWorkflow/ResumeAll keep ONLY ConcurrentMessageLimit=1.

The complete verified owner map (file:line of each UseMessageRetry CALL to strip — PATTERNS.md):
| Endpoint                          | Definition (line of the call)                    | Also strip                                   | KEEP                       |
| `orchestrator` (Start+Stop SHARE) | StartOrchestrationConsumerDefinition.cs:38        | Ignore<WorkflowRootNotFoundException> (:41); _retryOptions field+ctor | dual EndpointName |
| `orchestrator` (Start+Stop SHARE) | StopOrchestrationConsumerDefinition.cs:33         | Ignore<WorkflowRootNotFoundException> (:36); _retryOptions field+ctor | dual EndpointName |
| `orchestrator-result`             | StepCompletedConsumerDefinition.cs:40             | _retryOptions field+ctor                     | — |
| `orchestrator-pauseresume`        | PauseWorkflowConsumerDefinition.cs:38             | _retryOptions field+ctor                     | ConcurrentMessageLimit=1 (:35) |
| `orchestrator-global-pauseresume` | PauseAllConsumerDefinition.cs:37                  | _retryOptions field+ctor                     | ConcurrentMessageLimit=1 (:35) |

Exact removal excerpt (Start — Stop identical, UnscheduleOnly path):
```csharp
// BEFORE — StartOrchestrationConsumerDefinition.cs:38-42
endpointConfigurator.UseMessageRetry(r =>
{
    r.Immediate(_retryOptions.Value.Limit);
    r.Ignore<WorkflowRootNotFoundException>();   // ◄── whole block goes
});
// AFTER — ConfigureConsumer body becomes empty (or the override is deleted entirely).
// Also delete: the `_retryOptions` field, its ctor param + assignment, and `using Microsoft.Extensions.Options;`
// (+ the WorkflowRootNotFoundException using if now unused) → 0-warning (SC-3, Pitfall 4).
```

DEAD-CODE FACTS (PATTERNS Finding 1 / D-07 — do NOT relitigate): `WorkflowRootNotFoundException` is NEVER
thrown anywhere in the repo (0 grep matches src+tests). `WorkflowLifecycle` handles missing-root as a logged
`return;` → ACK. The Ignore<> is dead defensive code. Delete it WITH the retry block. Do NOT add any
catch/DLQ. Do NOT change WorkflowLifecycle. Do NOT touch StartOrchestrationConsumer.cs / StopOrchestrationConsumer.cs.

Doc-comment caveat (PATTERNS Finding 2): after stripping, the surviving xml-doc lines that still describe a
`UseMessageRetry(Immediate(N))` call (Start :15, Stop :13, StepCompleted :16/:38, PauseWorkflow :14-15,
PauseAll :14-15, ResumeAll :26) now lie. Reconcile each to the A18 end-state (no bus retry; throw → broker
redelivery). The D-01 guard scans the CALL pattern only, so doc text is not strictly required to change for
the guard — but reconcile them so the code self-describes truthfully.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip the dual-owner retry from Start + Stop (the shared `orchestrator` endpoint)</name>
  <files>src/Orchestrator/Consumers/StartOrchestrationConsumerDefinition.cs, src/Orchestrator/Consumers/StopOrchestrationConsumerDefinition.cs</files>
  <read_first>
    - src/Orchestrator/Consumers/StartOrchestrationConsumerDefinition.cs (full — :15 doc, :22/:24-27 ctor+field, :38-42 retry block)
    - src/Orchestrator/Consumers/StopOrchestrationConsumerDefinition.cs (full — :13 doc, :17/:19-23 ctor+field, :33-37 retry block)
    - src/Orchestrator/Consumers/StepFailedConsumerDefinition.cs (full — the no-op TARGET shape)
    - .planning/phases/53-model-b-teardown/53-PATTERNS.md (owner map; Finding 1; the DUAL-OWNER warning)
  </read_first>
  <action>
    For BOTH StartOrchestrationConsumerDefinition.cs AND StopOrchestrationConsumerDefinition.cs:
    1. Delete the entire `endpointConfigurator.UseMessageRetry(r => { r.Immediate(...); r.Ignore<WorkflowRootNotFoundException>(); });`
       block (Start :38-42, Stop :33-37). If `ConfigureConsumer` body becomes empty, leave an empty override
       or delete the override entirely (match the StepFailed no-op shape — keep whatever EndpointName/
       ConfigureConsumer wiring the definition needs, just no retry).
    2. Delete the `_retryOptions` field (Start :22, Stop :17), its ctor parameter + assignment (Start :24-27,
       Stop :19-23). If the ctor becomes parameterless, collapse it (or remove it) to match StepFailed.
    3. Remove now-unused usings: `using Microsoft.Extensions.Options;` and the
       `WorkflowRootNotFoundException` import IF it's no longer referenced anywhere in the file.
    4. Reconcile the stale doc-comment (Start :15 "UseMessageRetry(Immediate(3)) remains...", Stop :13
       "UseMessageRetry bounds infra faults → _error") to the A18 end-state: "No bus retry; a send that
       exhausts the in-code RetryLoop throws → RabbitMQ nack-requeue (broker redelivery); no _error, no
       dead-letter (Phase-53 D-01). WorkflowRootNotFoundException is never thrown (D-07 — dead Ignore removed)."
    DO NOT modify StartOrchestrationConsumer.cs / StopOrchestrationConsumer.cs (the consumers) or WorkflowLifecycle.cs.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "endpointConfigurator.UseMessageRetry(" src/Orchestrator/Consumers/StartOrchestrationConsumerDefinition.cs` returns 0 (same for Stop).
    - `grep -c "Ignore<WorkflowRootNotFoundException>" src/Orchestrator/Consumers/StartOrchestrationConsumerDefinition.cs` returns 0 (same for Stop).
    - `dotnet build SK_P.sln -c Release` AND `-c Debug` both exit 0 with 0 warnings (no dead `_retryOptions` field, no unused using → SC-3).
    - StartOrchestrationConsumer.cs / StopOrchestrationConsumer.cs / WorkflowLifecycle.cs are unchanged (git diff touches only the two *Definition* files).
  </acceptance_criteria>
  <done>Both shared-endpoint owners drop retry + the dead Ignore<>; build 0-warning; FACT 8 (D-07) and the Start/Stop half of FACT 6 (D-01) turn GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Strip retry from StepCompleted + PauseWorkflow + PauseAll (keep ConcurrentMessageLimit=1)</name>
  <files>src/Orchestrator/Consumers/StepCompletedConsumerDefinition.cs, src/Orchestrator/Consumers/PauseWorkflowConsumerDefinition.cs, src/Orchestrator/Consumers/PauseAllConsumerDefinition.cs, src/Orchestrator/Consumers/ResumeAllConsumerDefinition.cs</files>
  <read_first>
    - src/Orchestrator/Consumers/StepCompletedConsumerDefinition.cs (full — :16/:38 doc, :23/:25-29 ctor+field, :40 retry call)
    - src/Orchestrator/Consumers/PauseWorkflowConsumerDefinition.cs (full — :14-15 doc, :22/:24-28 ctor+field, :35 ConcurrentMessageLimit=1 KEEP, :38 retry call)
    - src/Orchestrator/Consumers/PauseAllConsumerDefinition.cs (full — :14-15 doc, ctor+field, :35 ConcurrentMessageLimit=1 KEEP, :37 retry call)
    - src/Orchestrator/Consumers/ResumeWorkflowConsumerDefinition.cs + ResumeAllConsumerDefinition.cs (no-op sibling TARGET shape; ResumeAll :26 has a now-stale doc-comment)
    - .planning/phases/53-model-b-teardown/53-PATTERNS.md (owner map; Pitfall 4)
  </read_first>
  <action>
    For StepCompletedConsumerDefinition.cs, PauseWorkflowConsumerDefinition.cs, PauseAllConsumerDefinition.cs:
    1. Delete the `endpointConfigurator.UseMessageRetry(r => r.Immediate(_retryOptions.Value.Limit));` line
       (StepCompleted :40, PauseWorkflow :38, PauseAll :37).
    2. Delete the `_retryOptions` field + ctor param + assignment (StepCompleted :23/:25-29, and the matching
       members in PauseWorkflow :22/:24-28 and PauseAll). Remove `using Microsoft.Extensions.Options;` if now
       unused in that file. Match the StepFailed / ResumeWorkflow no-op shape.
    3. KEEP `consumerConfigurator.ConcurrentMessageLimit = 1;` (PauseWorkflow :35, PauseAll :35) — it is
       serialization, NOT retry, A18-orthogonal. Do NOT remove it.
    4. Reconcile stale doc-comments to the A18 end-state (StepCompleted :16/:38 "Single-owner ... UseMessageRetry",
       PauseWorkflow :14-15 + PauseAll :14-15 "ONLY this definition registers UseMessageRetry"): rewrite to
       "this endpoint registers NO bus retry; send-exhaust throws → broker redelivery (Phase-53 D-01)."
    5. ResumeAllConsumerDefinition.cs :26 has a trailing comment "...NO second UseMessageRetry (Pitfall 4)";
       reconcile it so it no longer references a retry that no longer exists on the sibling (e.g. "serial;
       no bus retry on this endpoint (Phase-53 D-01)"). This is the ONLY change to ResumeAll — keep
       ConcurrentMessageLimit=1. (Editing ResumeAll's comment keeps the source self-truthful; the D-01 guard
       already ignores it because it scans the call pattern, but leaving a lie is undesirable.)
    Leave StepFailed/StepCancelled/StepProcessing/ResumeWorkflow definitions UNCHANGED (already no-op).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "endpointConfigurator.UseMessageRetry(" src/Orchestrator/Consumers/` reports 0 across ALL files; `grep -rc "cfg.UseMessageRetry(" src/Orchestrator/Consumers/` reports 0.
    - `grep -c "ConcurrentMessageLimit = 1" src/Orchestrator/Consumers/PauseWorkflowConsumerDefinition.cs` returns 1 (KEPT); same for PauseAll.
    - `dotnet build SK_P.sln -c Release` AND `-c Debug` both exit 0 with 0 warnings.
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~No_bus_retry_or_error_transport_on_execution_path_endpoints"` — the src/Orchestrator/Consumers half now contributes ZERO offenders (the fact still RED only if the processor half remains; see Plan 03).
  </acceptance_criteria>
  <done>StepCompleted/PauseWorkflow/PauseAll drop retry + dead IOptions; ConcurrentMessageLimit=1 kept; build 0-warning; all orchestrator-source offenders gone from the D-01 guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator consumer → RabbitMQ broker | A send that exhausts the in-code RetryLoop throws; with no bus retry and no error pipeline the broker nack-requeues (redelivery). Untrusted message payloads were already validated upstream (unchanged this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-DoS | Denial of Service | Unbounded requeue spin on a permanently-failing poison send (orchestrator unreachable, not an L2 outage → BIT gate does not pause it) | accept | DELIBERATELY accepted residual (D-04, A18 — mirrors keeper SustainedOutage spin). NOT a code change this phase; observability (redelivery-count metric) is the bound, deferred. Severity below `high` — does not block. |
| T-53-Tamper | Tampering/Repudiation | Silent message loss if removal accidentally yields ack-discard instead of nack-requeue (DiscardFaultedMessages anti-pattern) | mitigate | Anti-pattern explicitly avoided: NO error pipeline is added on these endpoints; the default with neither UseMessageRetry nor an error filter is nack-requeue (redelivery). Asserted by the Wave-0 D-01 source-scan guard (FACT 6) + optional Phase-54 live spike. |
| T-53-Regress | Tampering | A missed dual-owner retry (Stop left live) silently bounds redelivery | mitigate | The Wave-0 D-01 guard scans BOTH Start and Stop definitions; a missed `endpointConfigurator.UseMessageRetry(` fails FACT 6 loudly. |
</threat_model>

<verification>
- `grep -rc "endpointConfigurator.UseMessageRetry(\|cfg.UseMessageRetry(" src/Orchestrator/Consumers/` → 0.
- `grep -rc "Ignore<WorkflowRootNotFoundException>" src/Orchestrator/Consumers/` → 0.
- `dotnet build SK_P.sln -c Release` AND `-c Debug` → 0 warnings.
- `git diff --name-only` touches ONLY the 6 listed definition files (no consumer / no WorkflowLifecycle change).
</verification>

<success_criteria>
- All 5 orchestrator retry owners (Start, Stop, StepCompleted, PauseWorkflow, PauseAll) drop UseMessageRetry.
- Dual-owner `orchestrator` endpoint stripped on BOTH Start and Stop.
- Dead Ignore<WorkflowRootNotFoundException> removed from both (no DLQ seam added; WorkflowLifecycle untouched).
- Dead IOptions<RetryOptions> injections removed; ConcurrentMessageLimit=1 retained on Pause/PauseAll.
- 0-warning Release + Debug build (SC-3). FACT 8 GREEN; FACT 6 has no orchestrator-source offenders.
</success_criteria>

<output>
After completion, create `.planning/phases/53-model-b-teardown/53-02-SUMMARY.md`.
</output>
