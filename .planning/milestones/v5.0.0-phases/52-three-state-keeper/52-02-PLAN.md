---
phase: 52-three-state-keeper
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/Keeper/Program.cs
  - src/Keeper/Recovery/ReinjectConsumerDefinition.cs
  - src/Keeper/Recovery/InjectConsumerDefinition.cs
  - src/Keeper/Recovery/DeleteConsumerDefinition.cs
  - src/Keeper/Recovery/RecoveryEndpointHandle.cs
  - src/Keeper/Recovery/RecoveryEndpointBinder.cs
  - src/Keeper/appsettings.json
  - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs
  - tests/BaseApi.Tests/Keeper/KeeperPauseAccumulateFacts.cs
  - tests/BaseApi.Tests/Keeper/SustainedOutageFacts.cs
autonomous: true
requirements: [KEEP-04, KEEP-05]
user_setup: []

must_haves:
  truths:
    - "The keeper-recovery endpoint is bound at runtime via IReceiveEndpointConnector.ConnectReceiveEndpoint (NOT static AddConsumer auto-config), and its HostReceiveEndpointHandle is stored in a DI singleton"
    - "The connect callback registers the same UseMessageRetry (Dlq1 mode only) + three UsePartitioner<T> on the 4-tuple + the three ConfigureConsumer<T> that ReinjectConsumerDefinition registered before"
    - "In Dlq1 mode an exhausted op routes to skp-dlq-1 as a ConsolidatedFault"
    - "In SustainedOutage mode an exhausted op does NOT produce a ConsolidatedFault (no dead-letter); the message is redelivered/held"
    - "Stopping the connected endpoint stops consumption (message accumulates, not consumed); starting it drains the backlog"
    - "Exactly one source configures keeper-recovery (no static + connect collision)"
  artifacts:
    - path: "src/Keeper/Recovery/RecoveryEndpointHandle.cs"
      provides: "DI singleton holding the connected HostReceiveEndpointHandle for BitHealthLoop to Stop/Start"
      contains: "HostReceiveEndpointHandle"
    - path: "src/Keeper/Recovery/RecoveryEndpointBinder.cs"
      provides: "Startup BackgroundService (or IHostedService) that ConnectReceiveEndpoint(keeper-recovery) and stores the handle"
      contains: "ConnectReceiveEndpoint"
    - path: "tests/BaseApi.Tests/Keeper/SustainedOutageFacts.cs"
      provides: "KEEP-05 SustainedOutage no-dead-letter fact"
      contains: "ConsolidatedFault"
    - path: "tests/BaseApi.Tests/Keeper/KeeperPauseAccumulateFacts.cs"
      provides: "KEEP-04 endpoint Stop -> no consume -> Start -> drain fact"
      contains: "Stop"
  key_links:
    - from: "src/Keeper/Program.cs"
      to: "keeper-recovery endpoint"
      via: "AddConsumer(...).ExcludeFromConfigureEndpoints() + RecoveryEndpointBinder ConnectReceiveEndpoint"
      pattern: "ExcludeFromConfigureEndpoints"
    - from: "src/Keeper/Recovery/RecoveryEndpointBinder.cs"
      to: "src/Keeper/Recovery/RecoveryEndpointHandle.cs"
      via: "stores HostReceiveEndpointHandle in the singleton after await handle.Ready"
      pattern: "ConnectReceiveEndpoint"
---

<objective>
Convert the keeper-recovery endpoint from static `AddConsumer` auto-config to a runtime `ConnectReceiveEndpoint` handle (OQ-1 / D-04 enabler), store the handle in a singleton for BitHealthLoop (Plan 03), and add the config-driven exhaustion policy (KEEP-05 / D-01..D-03). Prove gate pause/accumulate (KEEP-04 mechanism) and both exhaustion modes with hermetic ITestHarness facts.

Purpose: OQ-1 — in MassTransit 8.5.5 a statically-configured endpoint cannot be runtime-paused (`StopAsync` removes it). The maintainer-blessed pattern is `ConnectReceiveEndpoint` + handle `Stop/Start`, mutually exclusive with static config. The processor already does exactly this (ProcessorStartupOrchestrator) — mirror it. KEEP-05 branches the connect callback on the startup `ExhaustionPolicy` enum (added in Plan 01).
Output: A runtime-bound, pausable keeper-recovery endpoint; a handle singleton; the policy branch; green integration facts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-three-state-keeper/52-CONTEXT.md
@.planning/phases/52-three-state-keeper/52-RESEARCH.md
@.planning/phases/52-three-state-keeper/52-PATTERNS.md
@.planning/phases/52-three-state-keeper/52-01-SUMMARY.md

<interfaces>
<!-- IN-REPO ANALOG (gold standard for OQ-1) — the processor already uses ConnectReceiveEndpoint -->

src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs:170-188 — the exact runtime-bind pattern to mirror:
```csharp
var handle = endpointConnector.ConnectReceiveEndpoint(queueName, (ctx, cfg) =>
{
    cfg.UseMessageRetry(r => r.Immediate(retryLimit));     // outer dead-letter latch
    cfg.ConfigureConsumer<EntryStepDispatchConsumer>(ctx);
});
await handle.Ready;     // queue declared + consumer attached
```
- ctor injects `IReceiveEndpointConnector endpointConnector` (DI-registered by the bus).
- Registration side (BaseProcessorServiceCollectionExtensions.cs:79): `x.AddConsumer<EntryStepDispatchConsumer>().ExcludeFromConfigureEndpoints();` — registers for DI but EXCLUDES from auto endpoint config so no kebab queue is auto-created at bus start.

Verified 8.5.5 API surface (52-RESEARCH):
- IReceiveEndpoint.Start(CancellationToken) / Stop(CancellationToken)  [for Plan 03]
- HostReceiveEndpointHandle.Ready (Task), .ReceiveEndpoint (IReceiveEndpoint)
- HostReceiveEndpointHandle.StopAsync — REMOVES the endpoint; do NOT use for pause
- IReceiveEndpointConnector.ConnectReceiveEndpoint(string, Action<IBusRegistrationContext, IReceiveEndpointConfigurator>)

Current keeper-recovery config to RE-HOME (ReinjectConsumerDefinition.cs:42-67) — KEEP verbatim inside the connect callback:
```csharp
endpointConfigurator.UseMessageRetry(r => r.Immediate(_retryOptions.Value.Limit));   // Dlq1 mode only
var partition = new Partitioner(_recoveryOptions.Value.PartitionCount, new Murmur3UnsafeHashGenerator());
endpointConfigurator.UsePartitioner<KeeperReinject>(partition, p => PartitionGuid(p.Message));
endpointConfigurator.UsePartitioner<KeeperInject>(partition, p => PartitionGuid(p.Message));
endpointConfigurator.UsePartitioner<KeeperDelete>(partition, p => PartitionGuid(p.Message));
```
PartitionKey / PartitionGuid are `public static` (RecoveryPartitionFacts pins them) — KEEP verbatim regardless of where the wiring lands.

Consts: KeeperQueues.Recovery = "keeper-recovery". RecoveryOptions.ExhaustionPolicy (Dlq1 default) added in Plan 01.

RecoveryDeadLetterFacts harness builder (the in-memory ITestHarness analog, RecoveryDeadLetterFacts.cs:59-89) — reuse the AddConfigureEndpointsCallback + ConsolidatedErrorTransportFilter.Dlq1 sink shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert keeper-recovery to ConnectReceiveEndpoint + handle singleton + exhaustion-policy branch</name>
  <files>src/Keeper/Program.cs, src/Keeper/Recovery/RecoveryEndpointHandle.cs, src/Keeper/Recovery/RecoveryEndpointBinder.cs, src/Keeper/Recovery/ReinjectConsumerDefinition.cs, src/Keeper/Recovery/InjectConsumerDefinition.cs, src/Keeper/Recovery/DeleteConsumerDefinition.cs, src/Keeper/appsettings.json</files>
  <read_first>
    - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs:50-189 (the EXACT ConnectReceiveEndpoint + handle.Ready analog to mirror)
    - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs:67-80 (the AddConsumer().ExcludeFromConfigureEndpoints() registration analog)
    - src/Keeper/Program.cs (the file being modified — static AddConsumer block lines 40-50, RecoveryOptions bind line 30)
    - src/Keeper/Recovery/ReinjectConsumerDefinition.cs (the retry + 3x partitioner config to RE-HOME; PartitionKey/PartitionGuid statics to KEEP)
    - src/Keeper/Recovery/InjectConsumerDefinition.cs + DeleteConsumerDefinition.cs (no-op siblings to neutralize)
    - src/Keeper/appsettings.json (Recovery section — has GateWaitSeconds to remove, ExhaustionPolicy to add)
    - 52-RESEARCH.md Pattern 1 (lines 154-188), Pattern 2 (190-200), Pitfall 1 (260-264), Pitfall 3 (272-276), Assumption A1/OQ-2 (447,460)
    - 52-PATTERNS.md sections "Program.cs (MODIFY)" and "ReinjectConsumerDefinition.cs (MODIFY)"
  </read_first>
  <action>
    Mirror the processor's runtime-bind pattern. ONE source configures keeper-recovery (Pitfall 1).

    (a) CREATE src/Keeper/Recovery/RecoveryEndpointHandle.cs — a mutable DI singleton holding the connected handle:
        ```csharp
        using MassTransit;
        namespace Keeper.Recovery;
        /// <summary>D-04: holds the runtime-connected keeper-recovery HostReceiveEndpointHandle so
        /// BitHealthLoop (Plan 03) can Stop/Start the endpoint on BIT health edges. Set ONCE by
        /// RecoveryEndpointBinder after await handle.Ready.</summary>
        public sealed class RecoveryEndpointHandle
        {
            public HostReceiveEndpointHandle? Handle { get; set; }
        }
        ```

    (b) CREATE src/Keeper/Recovery/RecoveryEndpointBinder.cs — an IHostedService/BackgroundService (mirror ProcessorStartupOrchestrator's ctor-injected `IReceiveEndpointConnector` + connect-callback). It ctor-injects `IReceiveEndpointConnector connector`, `IOptions<RetryOptions> retry`, `IOptions<RecoveryOptions> recovery`, `RecoveryEndpointHandle holder`, `ILogger<RecoveryEndpointBinder>`. In ExecuteAsync/StartAsync:
        - Build the SHARED partitioner: `var partition = new Partitioner(recovery.Value.PartitionCount, new Murmur3UnsafeHashGenerator());` (using MassTransit.Middleware).
        - `var handle = connector.ConnectReceiveEndpoint(KeeperQueues.Recovery, (ctx, cfg) => { ... })` with the callback:
          - KEEP-05 branch on `recovery.Value.ExhaustionPolicy`:
            - `ExhaustionPolicy.Dlq1` (default): `cfg.UseMessageRetry(r => r.Immediate(retry.Value.Limit));` (the existing dead-letter latch — exhaustion re-throws -> inherited ConsolidatedErrorTransportFilter -> skp-dlq-1).
            - `ExhaustionPolicy.SustainedOutage`: do NOT wire UseMessageRetry's dead-letter path. Configure broker requeue / no dead-letter so a thrown delivery is redelivered, NOT routed to skp-dlq-1. VERIFY THE EXACT 8.5.5 CALL against the installed assembly (OQ-2 / Assumption A1): the candidate is omitting the consolidated error move so the thrown exception nack-requeues (MassTransit default redelivery). Inspect `~/.nuget/packages/masstransit{,.abstractions}/8.5.5/lib/net8.0/*.xml` for `UseDelayedRedelivery`/`UseScheduledRedelivery`/redelivery and confirm before settling. Whatever call is chosen, the SustainedOutageFacts (Task 3) MUST prove no ConsolidatedFault is produced.
          - Always (both modes): the three `cfg.UsePartitioner<KeeperReinject/KeeperInject/KeeperDelete>(partition, p => ReinjectConsumerDefinition.PartitionGuid(p.Message));` and the three `cfg.ConfigureConsumer<ReinjectConsumer/InjectConsumer/DeleteConsumer>(ctx);`.
        - `await handle.Ready;` then `holder.Handle = handle;` and log.
        - STARTUP POSTURE (Pitfall 3, decided): connect STARTED here (simplest; matches the processor analog which connects started). The gate starting fail-safe-closed plus Plan 03's first-healthy-edge Start keeps the BIT model coherent — but the recovery endpoint being briefly consumable before the first probe is acceptable because consumption only mutates L2 when ops succeed and BitHealthLoop's first tick fires within Probe:DelaySeconds. Document this choice in the binder xml-doc. (If a stricter posture is desired, Plan 03 can Stop-then-conditionally-Start; do not over-engineer here.)

    (c) src/Keeper/Program.cs — replace the static `AddBaseConsoleMessaging` consumer block (lines 40-50). Register the three consumers for DI but EXCLUDE from auto endpoint config (mirror BaseProcessorServiceCollectionExtensions.cs:79):
        ```csharp
        builder.Services.AddBaseConsoleMessaging(builder.Configuration, x =>
        {
            x.AddConsumer<Keeper.Recovery.ReinjectConsumer>().ExcludeFromConfigureEndpoints();
            x.AddConsumer<Keeper.Recovery.InjectConsumer>().ExcludeFromConfigureEndpoints();
            x.AddConsumer<Keeper.Recovery.DeleteConsumer>().ExcludeFromConfigureEndpoints();
        });
        ```
        Note: drop the `, XConsumerDefinition` type args from AddConsumer (the definition's endpoint config no longer drives the static endpoint). Register the new singleton + binder + the KeeperMetrics meter (created in Plan 01):
        ```csharp
        builder.Services.AddSingleton<Keeper.Recovery.RecoveryEndpointHandle>();
        builder.Services.AddHostedService<Keeper.Recovery.RecoveryEndpointBinder>();
        builder.Services.AddSingleton<Keeper.Observability.KeeperMetrics>();
        builder.Services.ConfigureOpenTelemetryMeterProvider(mp => mp.AddMeter(Keeper.Observability.KeeperMetrics.MeterName));
        ```
        Keep the RecoveryOptions/Retry/Probe binds. Ensure the RecoveryEndpointBinder hosted service starts AFTER the bus is running (BaseConsoleMessaging registers MassTransitHostedService which starts the bus; a BackgroundService bound via ConnectReceiveEndpoint on the running bus is exactly the processor precedent — the connector resolves on the started bus).

    (d) ReinjectConsumerDefinition.cs / InjectConsumerDefinition.cs / DeleteConsumerDefinition.cs — the endpoint config has RE-HOMED into the binder. Since the consumers are now registered WITHOUT their definitions, these `ConsumerDefinition` classes are no longer attached. KEEP `ReinjectConsumerDefinition.PartitionKey` / `PartitionGuid` static helpers (RecoveryPartitionFacts pins them) — they MUST remain `public static` and reachable. Options: (i) delete the three definition classes but move PartitionKey/PartitionGuid to a small static helper class in the same file/namespace, OR (ii) keep ReinjectConsumerDefinition as a shell retaining ONLY the two static helpers and delete its ConfigureConsumer body + the two unused sibling definitions. Choose (ii) for minimal churn: strip ReinjectConsumerDefinition's `ConfigureConsumer` + ctor fields, keep the statics; DELETE InjectConsumerDefinition.cs and DeleteConsumerDefinition.cs (they were pure no-ops). Update ReinjectConsumerDefinition xml-doc to state the endpoint config moved to RecoveryEndpointBinder (D-04/OQ-1). Verify RecoveryPartitionFacts still references `ReinjectConsumerDefinition.PartitionGuid` and compiles.

    (e) src/Keeper/appsettings.json — in the "Recovery" section, REMOVE `"GateWaitSeconds": 300` (D-09, removed in Plan 01) and ADD `"ExhaustionPolicy": "Dlq1"` (D-01/D-02 explicit default).
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj 2>&1 | Select-String -Pattern "error|warning"</automated>
  </verify>
  <acceptance_criteria>
    - src/Keeper/Program.cs contains `ExcludeFromConfigureEndpoints` for all three recovery consumers and NO `, *ConsumerDefinition>` type-arg AddConsumer for them
    - src/Keeper/Program.cs registers `AddSingleton<...RecoveryEndpointHandle>`, `AddHostedService<...RecoveryEndpointBinder>`, `AddSingleton<...KeeperMetrics>`, and `AddMeter(...KeeperMetrics.MeterName)`
    - src/Keeper/Recovery/RecoveryEndpointHandle.cs exists, contains `HostReceiveEndpointHandle`
    - src/Keeper/Recovery/RecoveryEndpointBinder.cs contains `ConnectReceiveEndpoint`, `await handle.Ready` (or `.Ready`), `holder.Handle =`, both `ExhaustionPolicy.Dlq1` and `ExhaustionPolicy.SustainedOutage` branches, and three `ConfigureConsumer<` calls + three `UsePartitioner<`
    - `Select-String -Path src/Keeper/Recovery/ReinjectConsumerDefinition.cs -Pattern "public static Guid PartitionGuid|public static string PartitionKey"` returns matches (statics preserved)
    - `Test-Path src/Keeper/Recovery/InjectConsumerDefinition.cs` returns False AND `Test-Path src/Keeper/Recovery/DeleteConsumerDefinition.cs` returns False
    - `Select-String -Path src/Keeper/appsettings.json -Pattern "GateWaitSeconds"` returns NO match; contains `"ExhaustionPolicy"`
    - `dotnet build src/Keeper/Keeper.csproj` reports 0 errors, 0 warnings
  </acceptance_criteria>
  <done>keeper-recovery is runtime-bound via ConnectReceiveEndpoint, handle stored in a singleton, exhaustion policy branched, exactly one endpoint config source, partition statics preserved, appsettings updated.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt RecoveryDeadLetterFacts to op-exhaustion (Dlq1) + new pause/accumulate fact (KEEP-04)</name>
  <files>tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs, tests/BaseApi.Tests/Keeper/KeeperPauseAccumulateFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs (the data-gone fact lines 91-121 contradicts D-06 — repurpose; harness builder 59-89; the `new ReinjectConsumer(...)` call line 153-156 needs the Plan-01 ctor)
    - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs (updated by Plan 01 — Recovery() no longer takes gateWaitSeconds; Metrics() helper)
    - 52-PATTERNS.md section "pause/accumulate fact + SustainedOutage fact (CREATE)" and "RecoveryDeadLetterFacts.cs"
    - 52-VALIDATION.md Per-Task Verification Map rows for KEEP-04 and KEEP-05 Dlq1
  </read_first>
  <action>
    (a) RecoveryDeadLetterFacts.cs — the `DataGone_reinject_faults_and_routes_to_dead_letter` fact (line 94) now CONTRADICTS D-06 (data-gone is a drop, not a dead-letter). REPURPOSE it to op-exhaustion -> dead-letter for Dlq1 mode: instead of an EMPTY L2 (which is now a drop), make the L2 read throw a Redis EXCEPTION (infra) so Guard re-throws -> exhaustion -> ConsolidatedFault. Build the mux so `StringLengthAsync` throws a `RedisException` (or `db.StringLengthAsync(...).Returns<long>(_ => throw new RedisTimeoutException(...))`). Keep the two assertions: the consume faults, and `harness.Consumed.Any<ConsolidatedFault>()` is true. Rename the fact to `OpExhaustion_reinject_faults_and_routes_to_dead_letter`, re-tag `[Trait("Phase","52")]`. Update the `new ReinjectConsumer(...)` call in `Duplicate_Reinject_reproduces_effect_no_collapse` (line 153) to the new 6-param ctor (drop gate, add `RecoveryTestKit.Metrics()` + `NullLogger<ReinjectConsumer>.Instance`). Remove all `RecoveryDataGoneException` references (the type is deleted).

    (b) CREATE tests/BaseApi.Tests/Keeper/KeeperPauseAccumulateFacts.cs (KEEP-04, `[Trait("Phase","52")]`). Use the in-memory ITestHarness builder shape from RecoveryDeadLetterFacts. Register one recovery consumer (DeleteConsumer is simplest — no send). Start the harness. Obtain the receive endpoint handle for keeper-recovery (in the ITestHarness in-memory model, the connected/configured endpoint's Stop/Start mirrors the production HostReceiveEndpointHandle). Sequence:
        - Stop the recovery endpoint.
        - Publish a KeeperDelete; assert it is NOT consumed within a short bounded wait (e.g. `Assert.False(await harness.Consumed.Any<KeeperDelete>(shortCtsToken))` — accumulates, not consumed).
        - Start the endpoint; assert it IS now consumed (`Assert.True(await harness.Consumed.Any<KeeperDelete>(ct))` — drained).
      If the in-memory harness cannot expose endpoint Stop/Start directly, drive the same proof through the production RecoveryEndpointHandle: build the keeper bus with the RecoveryEndpointBinder + RecoveryEndpointHandle, Stop via `holder.Handle.ReceiveEndpoint.Stop(ct)`, assert not consumed, Start, assert consumed. Document which mechanism is used. Note the hermetic scope: this proves consume/no-consume shape, not broker-literal accumulation (live skp-dlq-1/queue depth is Phase-54 Manual-Only per 52-VALIDATION.md).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RecoveryDeadLetter|FullyQualifiedName~KeeperPauseAccumulate"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs -Pattern "RecoveryDataGoneException"` returns NO match
    - RecoveryDeadLetterFacts.cs contains a fact asserting `Consumed.Any<ConsolidatedFault>` driven by a Redis EXCEPTION (not an empty/absent L2)
    - tests/BaseApi.Tests/Keeper/KeeperPauseAccumulateFacts.cs exists, contains `.Stop(` and `.Start(` (or endpoint Stop/Start) and an assertion that the message is NOT consumed while stopped and IS consumed after start
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~RecoveryDeadLetter|FullyQualifiedName~KeeperPauseAccumulate"` exits 0
  </acceptance_criteria>
  <done>Dlq1 op-exhaustion dead-letters to skp-dlq-1 (fact green); stopping the endpoint blocks consume and starting drains it (fact green); no RecoveryDataGoneException references remain.</done>
</task>

<task type="auto">
  <name>Task 3: SustainedOutage no-dead-letter fact (KEEP-05)</name>
  <files>tests/BaseApi.Tests/Keeper/SustainedOutageFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs (the Dlq1 harness — SustainedOutage is its inverse)
    - src/Keeper/Recovery/RecoveryEndpointBinder.cs (the SustainedOutage branch implemented in Task 1 — replicate its endpoint config shape in the harness)
    - src/Keeper/RecoveryOptions.cs (ExhaustionPolicy enum)
    - 52-RESEARCH.md Pattern 2 (190-200), Assumption A1/OQ-2 (447, 460)
    - 52-VALIDATION.md KEEP-05 SustainedOutage row
  </read_first>
  <action>
    CREATE tests/BaseApi.Tests/Keeper/SustainedOutageFacts.cs (`[Trait("Phase","52")]`). Mirror RecoveryDeadLetterFacts' in-memory ITestHarness builder, but configure the endpoint in SustainedOutage mode — i.e. apply the SAME endpoint config the RecoveryEndpointBinder uses for `ExhaustionPolicy.SustainedOutage` (the no-dead-letter/requeue config verified in Task 1), NOT the Dlq1 `UseMessageRetry`+ConsolidatedErrorTransportFilter path. Drive a faulting op (a Redis EXCEPTION on a recovery op so the consumer throws). Assert:
      - `Assert.False(await harness.Consumed.Any<ConsolidatedFault>(boundedToken))` — NO consolidated dead-letter is produced (the inverse of the Dlq1 fact).
      - The message is redelivered/retried rather than dead-lettered. Pick a deterministic observable: e.g. assert the consumer was invoked MORE THAN ONCE (redelivery) within a bounded window, or assert no `Fault<KeeperX>` lands in the consolidated sink. Use a bounded CancellationToken so the test does not hang on the accepted-spin (D-03) — the SustainedOutage mode intentionally requeues forever; the fact asserts "no dead-letter within the window + at least one redelivery", then stops the harness.
    Keep the hermetic scope note: in-memory transport proves the no-dead-letter SHAPE, not broker-literal requeue depth (Phase-54 Manual-Only). If Task 1's OQ-2 verification revealed the exact 8.5.5 redelivery call, assert against the observable it produces.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~SustainedOutage"</automated>
  </verify>
  <acceptance_criteria>
    - tests/BaseApi.Tests/Keeper/SustainedOutageFacts.cs exists, contains `ConsolidatedFault` in a NEGATIVE assertion (`Assert.False(...Any<ConsolidatedFault>...)`)
    - The fact uses a bounded CancellationToken (does NOT rely on the default infinite test token for the accepted-spin path)
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~SustainedOutage"` exits 0
    - `dotnet build` (whole solution) reports 0 warnings AND 0 errors
  </acceptance_criteria>
  <done>SustainedOutage mode proven to NOT dead-letter (no ConsolidatedFault) and to redeliver/hold; solution 0-warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker -> keeper-recovery endpoint | Runtime-bound competing-consumer endpoint; pause/resume governs whether deliveries are dispatched. |
| keeper op exhaustion -> skp-dlq-1 (Dlq1) OR requeue (SustainedOutage) | The policy decides whether exhausted work dead-letters or holds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Tampering / duplicate-endpoint | static AddConsumer + ConnectReceiveEndpoint collision (Pitfall 1) | mitigate | Consumers registered with ExcludeFromConfigureEndpoints(); exactly one source (RecoveryEndpointBinder) configures keeper-recovery. Acceptance grep + bus-start build prove no duplicate. |
| T-52-06 | Repudiation / data-loss | SustainedOutage swallowing a thrown delivery (Assumption A1) | mitigate | OQ-2 verified against the installed 8.5.5 assembly; SustainedOutageFacts asserts the message is redelivered (NOT acked/discarded) so genuine work is not silently lost during an outage. |
| T-52-07 | Elevation / dead-letter integrity | Dlq1 routing to the wrong/per-queue error endpoint | mitigate | Inherit the consolidated skp-dlq-1 filter (no per-consumer ConfigureError, Pitfall 3 of research); RecoveryDeadLetterFacts asserts a single ConsolidatedFault, not per-{queue}_error. |
| T-52-08 | DoS / startup race | endpoint consumable before first BIT probe (Pitfall 3) | accept | Connect STARTED; consumption only mutates L2 on success and the first BIT tick fires within Probe:DelaySeconds. Plan 03 wires Stop/Start on edges; the brief startup window is an accepted low-risk residual (documented in the binder). |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Keeper"` green (Dlq1 op-exhaustion, SustainedOutage no-dead-letter, pause/accumulate).
- `dotnet build` whole solution: 0 warnings, 0 errors.
- grep: exactly one keeper-recovery config source; `ExcludeFromConfigureEndpoints` present; `GateWaitSeconds` absent from appsettings.
</verification>

<success_criteria>
- KEEP-04 (mechanism): keeper-recovery is runtime-bound and pausable; Stop blocks consume, Start drains (fact green). The BIT-edge driver lands in Plan 03.
- KEEP-05: ExhaustionPolicy config branches Dlq1 (dead-letter -> skp-dlq-1) vs SustainedOutage (no dead-letter, redeliver) — both facts green.
- Exactly one source configures keeper-recovery; partition statics preserved; solution 0-warning.
</success_criteria>

<output>
After completion, create `.planning/phases/52-three-state-keeper/52-02-SUMMARY.md`
</output>
