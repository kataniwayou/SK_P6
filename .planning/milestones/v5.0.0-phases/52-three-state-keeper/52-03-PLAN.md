---
phase: 52-three-state-keeper
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/Keeper/Health/BitHealthLoop.cs
  - tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs
autonomous: true
requirements: [KEEP-04]
user_setup: []

must_haves:
  truths:
    - "On the unhealthy edge BitHealthLoop calls the recovery endpoint's Stop (alongside gate.Close() + PauseAll)"
    - "On the healthy edge BitHealthLoop calls the recovery endpoint's Start (alongside gate.Open() + ResumeAll)"
    - "A Stop/Start failure does NOT advance prevHealthy (WR-01 preserved) — the next tick re-applies the edge"
    - "The first healthy tick (prevHealthy=null transition) issues exactly one Start and one ResumeAll"
    - "Same-state ticks issue NO Stop/Start (edge-trigger preserved)"
  artifacts:
    - path: "src/Keeper/Health/BitHealthLoop.cs"
      provides: "Endpoint Stop/Start wired onto the existing BIT health edges (D-04 driver)"
      contains: "ReceiveEndpoint"
    - path: "tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs"
      provides: "Stop-on-unhealthy / Start-on-healthy call-count assertions with a fake handle"
      contains: "RecoveryEndpointHandle"
  key_links:
    - from: "src/Keeper/Health/BitHealthLoop.cs"
      to: "src/Keeper/Recovery/RecoveryEndpointHandle.cs"
      via: "ctor-injected RecoveryEndpointHandle; .Handle?.ReceiveEndpoint.Stop/Start on edges"
      pattern: "ReceiveEndpoint\\.(Stop|Start)"
---

<objective>
Wire the keeper-recovery endpoint Stop/Start onto BitHealthLoop's existing BIT health edges (D-04 / KEEP-04 driver), closing the loop: gate-closed -> endpoint stopped (accumulate), gate-open -> endpoint started (drain). Extend BitHealthLoopTests to assert the Stop/Start calls per edge while preserving the WR-01 and first-tick semantics.

Purpose: Plan 02 made the keeper-recovery endpoint runtime-pausable and stored its handle in a singleton. This plan gives BitHealthLoop access to that handle so the BIT loop — the existing L2-health authority — drives the pause/resume on the same edges it already uses for gate.Open/Close + PauseAll/ResumeAll. This completes the KEEP-04 non-destructive-consume invariant end to end.
Output: BitHealthLoop driving endpoint Stop/Start; extended tests green; the full keeper suite green at 0-warning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-three-state-keeper/52-CONTEXT.md
@.planning/phases/52-three-state-keeper/52-PATTERNS.md
@.planning/phases/52-three-state-keeper/52-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. -->

BitHealthLoop existing edge block to EXTEND (src/Keeper/Health/BitHealthLoop.cs:36-61):
```csharp
try {
    if (healthy) {
        gate.Open();
        await bus.Publish(new ResumeAll { CorrelationId = NewId.NextGuid() }, stoppingToken);
        logger.LogInformation("L2 healthy — gate OPEN, ResumeAll broadcast");
    } else {
        gate.Close();
        await bus.Publish(new PauseAll { CorrelationId = NewId.NextGuid() }, stoppingToken);
        logger.LogWarning("L2 unhealthy — gate CLOSED, PauseAll broadcast");
    }
    prevHealthy = healthy;                 // advance ONLY after the edge work succeeded (WR-01)
}
catch (OperationCanceledException) { break; }
catch (Exception ex) { logger.LogError(ex, "BIT transition broadcast failed — will retry next tick"); }
```
- prevHealthy starts null (first tick is always a transition). Locked by BitHealthLoopTests "first healthy tick -> 1 ResumeAll".
- WR-01: prevHealthy must NOT advance if any edge action throws.

RecoveryEndpointHandle (created by Plan 02 — src/Keeper/Recovery/RecoveryEndpointHandle.cs):
```csharp
public sealed class RecoveryEndpointHandle { public HostReceiveEndpointHandle? Handle { get; set; } }
```
HostReceiveEndpointHandle.ReceiveEndpoint is IReceiveEndpoint with `Task Start(CancellationToken)` / `Task Stop(CancellationToken)` (verified 8.5.5).

BitHealthLoop current ctor (BitHealthLoop.cs:12-17):
```csharp
public sealed class BitHealthLoop(L2ProbeRecovery probe, IL2HealthGate gate, IBus bus,
    IOptions<ProbeOptions> opts, ILogger<BitHealthLoop> logger) : BackgroundService
```

BitHealthLoopTests harness (tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs):
- NewLoop factory (line 94): `new(probe, gate, bus, ZeroDelay(), NullLogger<BitHealthLoop>.Instance)` — needs the new handle param.
- RunScriptThenStop (line 98) drives the ScriptedRedis script.
- Existing edge facts assert `bus.Received(N).Publish(Arg.Any<PauseAll|ResumeAll>(), ...)` — mirror with endpoint Stop/Start counts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject RecoveryEndpointHandle into BitHealthLoop and Stop/Start on edges</name>
  <files>src/Keeper/Health/BitHealthLoop.cs</files>
  <read_first>
    - src/Keeper/Health/BitHealthLoop.cs (the file being modified — ctor 12-17, edge block 36-61, WR-01 catch 53-60, first-tick prevHealthy=null line 27)
    - src/Keeper/Recovery/RecoveryEndpointHandle.cs (created in Plan 02 — the singleton holding HostReceiveEndpointHandle)
    - 52-PATTERNS.md section "BitHealthLoop.cs (MODIFY)" and Code Example 3 in 52-RESEARCH.md (lines 344-361)
    - 52-CONTEXT.md D-04 and D-05 (gate-closed unconditional, both modes)
  </read_first>
  <action>
    Extend BitHealthLoop's ctor with `RecoveryEndpointHandle endpointHandle` (the Plan-02 singleton):
        ```csharp
        public sealed class BitHealthLoop(
            L2ProbeRecovery probe, IL2HealthGate gate, IBus bus,
            RecoveryEndpointHandle endpointHandle,
            IOptions<ProbeOptions> opts, ILogger<BitHealthLoop> logger) : BackgroundService
        ```
    Add `using Keeper.Recovery;` if not present.

    Inside the EXISTING edge `try` block, add the endpoint Start/Stop adjacent to gate.Open()/Close() and BEFORE the Publish (so a Stop/Start failure also leaves prevHealthy un-advanced via the same WR-01 catch):
      - healthy branch: after `gate.Open();` add
        `if (endpointHandle.Handle is { } h) await h.ReceiveEndpoint.Start(stoppingToken);`   // D-04: resume + drain
      - unhealthy branch: after `gate.Close();` add
        `if (endpointHandle.Handle is { } h) await h.ReceiveEndpoint.Stop(stoppingToken);`    // D-04: basic.cancel; accumulate
    The null-guard (`is { } h`) handles the brief startup window before RecoveryEndpointBinder sets the handle — on the next tick the handle is present. Do NOT throw on a null handle (that would needlessly fault the loop during startup).

    Preserve verbatim: the `prevHealthy = healthy;` assignment stays AFTER all edge actions (WR-01 — a Stop/Start throw lands in the existing `catch (Exception ex)` and leaves prevHealthy un-advanced so the next tick re-applies the edge; gate.Open/Close + Start/Stop are idempotent). Keep the `catch (OperationCanceledException) { break; }` for graceful shutdown. Do NOT remove gate.Open()/Close() (OQ-3 — out of D-08 scope; the gate is still read by tests/other code). Update the class xml-doc to note the endpoint Stop/Start is driven on the same edges (D-04).
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj 2>&1 | Select-String -Pattern "error|warning"</automated>
  </verify>
  <acceptance_criteria>
    - src/Keeper/Health/BitHealthLoop.cs ctor contains `RecoveryEndpointHandle endpointHandle`
    - BitHealthLoop.cs contains `ReceiveEndpoint.Start(stoppingToken)` in the healthy branch and `ReceiveEndpoint.Stop(stoppingToken)` in the unhealthy branch
    - The `prevHealthy = healthy;` assignment remains AFTER the Start/Stop + Publish, inside the same try (grep that `prevHealthy = healthy` appears once, after the Publish lines)
    - gate.Open() and gate.Close() are still present (NOT removed)
    - `dotnet build src/Keeper/Keeper.csproj` reports 0 errors, 0 warnings
  </acceptance_criteria>
  <done>BitHealthLoop stops the recovery endpoint on the unhealthy edge and starts it on the healthy edge, under the existing WR-01 guard, with a null-safe startup-window guard.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BitHealthLoopTests with a fake handle asserting Stop/Start per edge</name>
  <files>tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs (the file being modified — NewLoop line 94, RunScriptThenStop 98-104, the five edge facts 106-213; the `bus.Received(N).Publish(...)` assertion idiom to mirror)
    - src/Keeper/Recovery/RecoveryEndpointHandle.cs (created Plan 02) and HostReceiveEndpointHandle / IReceiveEndpoint surface (Start/Stop)
    - src/Keeper/Health/BitHealthLoop.cs (the Task-1-modified ctor)
    - 52-VALIDATION.md KEEP-04 driver row
  </read_first>
  <action>
    Make the loop's RecoveryEndpointHandle a substitutable fake so Stop/Start calls are assertable.

    (a) Add a helper that builds a populated RecoveryEndpointHandle over a substituted IReceiveEndpoint:
        ```csharp
        private static (RecoveryEndpointHandle holder, IReceiveEndpoint endpoint) FakeHandle()
        {
            var endpoint = Substitute.For<IReceiveEndpoint>();
            endpoint.Start(Arg.Any<CancellationToken>()).Returns(Task.CompletedTask);
            endpoint.Stop(Arg.Any<CancellationToken>()).Returns(Task.CompletedTask);
            var host = Substitute.For<HostReceiveEndpointHandle>();
            host.ReceiveEndpoint.Returns(endpoint);
            return (new RecoveryEndpointHandle { Handle = host }, endpoint);
        }
        ```
      (If HostReceiveEndpointHandle is not cleanly substitutable, instead expose a seam: the RecoveryEndpointHandle holder is already substitutable via its Handle property; build a minimal stub HostReceiveEndpointHandle whose ReceiveEndpoint returns the fake — confirm the substitutability at implementation; the IReceiveEndpoint is the surface that matters.)

    (b) Update `NewLoop` to take and pass the holder:
        `private static BitHealthLoop NewLoop(L2ProbeRecovery probe, IL2HealthGate gate, IBus bus, RecoveryEndpointHandle holder) => new(probe, gate, bus, holder, ZeroDelay(), NullLogger<BitHealthLoop>.Instance);`
      Update ALL existing `NewLoop(...)` call sites in the five facts to pass a `FakeHandle().holder` (those facts only assert Publish counts, so a fresh fake per fact is fine).

    (c) ADD two new facts (mirror the existing edge facts), `[Trait("Phase","52")]`:
      - `Healthy_To_Unhealthy_Edge_Stops_Recovery_Endpoint`: script `[null, null, RedisDown()]`; capture `(holder, endpoint)`; after RunScriptThenStop assert `await endpoint.Received(1).Stop(Arg.Any<CancellationToken>());` and (first healthy transition) `await endpoint.Received(1).Start(Arg.Any<CancellationToken>());`.
      - `Same_State_Ticks_No_Stop_Start`: script `[null, null, RedisDown(), RedisDown(), null]` (mirror Same_State_Ticks_Publish_Nothing); assert exactly 1 Stop and 2 Start (transitions: null->healthy Start#1, healthy->unhealthy Stop#1, unhealthy->healthy Start#2) — proving same-state ticks issue NO extra Stop/Start (edge-trigger preserved).
      Preserve the first-tick semantics: the null->healthy transition issues exactly one Start (parallel to the existing "first healthy tick -> 1 ResumeAll" assertion).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~BitHealthLoop"</automated>
  </verify>
  <acceptance_criteria>
    - BitHealthLoopTests.cs contains `RecoveryEndpointHandle` and a substituted `IReceiveEndpoint` with `.Start`/`.Stop`
    - BitHealthLoopTests.cs contains a fact asserting `endpoint.Received(1).Stop(` on the unhealthy edge and `endpoint.Received(...).Start(` on the healthy edge
    - BitHealthLoopTests.cs contains an edge-trigger fact asserting exactly 1 Stop + 2 Start over the 5-tick mixed script
    - All pre-existing BitHealthLoop facts still pass (NewLoop call sites updated, Publish-count assertions unchanged)
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~BitHealthLoop"` exits 0
  </acceptance_criteria>
  <done>BitHealthLoopTests proves Stop-on-unhealthy / Start-on-healthy with correct edge-trigger counts and preserved first-tick + WR-01 semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Full keeper-suite green gate (0-warning, all KEEP requirements)</name>
  <files>tests/BaseApi.Tests/Keeper/Health/BitHealthLoopTests.cs</files>
  <read_first>
    - 52-VALIDATION.md (the full Per-Task Verification Map — all eight KEEP rows must be green)
    - .planning/phases/52-three-state-keeper/52-01-SUMMARY.md and 52-02-SUMMARY.md (what prior plans delivered)
  </read_first>
  <action>
    Final phase gate — no new production code; this task runs the full keeper-scoped suite + a whole-solution build to confirm the three plans compose. If any keeper fact is red or any warning surfaces:
      - Diagnose and fix WITHIN the file ownership of this plan (BitHealthLoop / BitHealthLoopTests). If the failure traces to a Plan 01/02 file (out of this plan's ownership), STOP and report it as a cross-plan integration gap in the SUMMARY (do not edit other plans' files — the orchestrator handles cross-plan fixes).
      - Common integration seams to check: (1) BitHealthLoop's new ctor param is registered in Program.cs (Plan 02 registers RecoveryEndpointHandle as a singleton — confirm BitHealthLoop resolves it); (2) no duplicate keeper-recovery endpoint config (Plan 02 Pitfall 1); (3) RecoveryTestKit changes (Plan 01) didn't leave a stale call site.
    Confirm the KEEP requirement -> fact map is fully green:
      - KEEP-01: ReinjectConsumerFacts (present + absent-drop) — Plan 01
      - KEEP-02: InjectConsumerFacts (write->send->delete order) — Plan 01
      - KEEP-03: DeleteConsumerFacts (delete + absent no-op) — Plan 01
      - KEEP-04: KeeperPauseAccumulateFacts (Plan 02) + BitHealthLoop Stop/Start facts (this plan)
      - KEEP-05: RecoveryDeadLetterFacts (Dlq1 op-exhaustion) + SustainedOutageFacts — Plan 02
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Keeper"; dotnet build 2>&1 | Select-String -Pattern "warning|error"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Keeper"` exits 0 (all keeper facts green)
    - `dotnet build` (whole solution, Debug) reports 0 warnings AND 0 errors
    - Each of KEEP-01..05 has at least one green fact (verified against 52-VALIDATION.md map)
  </acceptance_criteria>
  <done>Full keeper suite green at 0-warning; all five KEEP requirements have a passing hermetic fact; the three plans compose cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BIT health signal -> keeper-recovery endpoint lifecycle | The L2-health probe edge drives whether the recovery endpoint consumes. A gate-bypass (op while gate closed) would violate KEEP-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-09 | Elevation / gate-bypass | endpoint consuming while gate closed | mitigate | The unhealthy edge calls ReceiveEndpoint.Stop BEFORE prevHealthy advances; consumption is paused at the transport so no L2 op runs while the gate is closed (KEEP-04). BitHealthLoopTests asserts the Stop call on the unhealthy edge. |
| T-52-10 | Denial of service / stuck gate | a Stop/Start throw permanently killing the health loop (WR-01) | mitigate | The Start/Stop awaits live inside the existing WR-01 try; a throw lands in `catch (Exception)` leaving prevHealthy un-advanced so the next tick re-applies the edge (idempotent gate + Start/Stop). Preserved verbatim; not regressed. |
| T-52-11 | Tampering / startup window | null handle before binder sets it | accept | Null-guarded (`is { } h`); the brief pre-bind window is the same accepted residual as Plan 02 T-52-08. The next tick (within Probe:DelaySeconds) applies the edge once the handle is present. |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Keeper"` green (all KEEP facts).
- `dotnet build` whole solution: 0 warnings, 0 errors.
- BitHealthLoop drives Stop on unhealthy, Start on healthy, with correct edge-trigger counts; WR-01 + first-tick preserved.
</verification>

<success_criteria>
- KEEP-04 (complete): gate-closed -> endpoint Stop (accumulate); gate-open -> endpoint Start (drain), driven by the BIT loop on the same edges as gate.Open/Close + PauseAll/ResumeAll. Facts green.
- All five KEEP requirements have a passing hermetic fact; solution 0-warning (SC-4).
</success_criteria>

<output>
After completion, create `.planning/phases/52-three-state-keeper/52-03-SUMMARY.md`
</output>
