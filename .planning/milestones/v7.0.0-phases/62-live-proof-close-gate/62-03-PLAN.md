---
phase: 62-live-proof-close-gate
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - scripts/phase-62-close.ps1
  - .planning/phases/62-live-proof-close-gate/62-HUMAN-UAT.md
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]
tags: [close-gate, net-zero, runbook, build-gate, operator-gated]

must_haves:
  truths:
    - "scripts/phase-62-close.ps1 exists, is AST-valid PowerShell, and excludes the per-replica liveness namespace by PREFIX (^skp:proc:) from BOTH the BEFORE and AFTER redis SHA snapshots"
    - "The build gate passes: dotnet build SK_P.sln -c Release AND -c Debug both 0 warnings; the new RealStack tests COMPILE"
    - "62-HUMAN-UAT.md documents the live N=3 close run + the four multi-container lifecycle proofs (2-replica self-register, durably-broken->unhealthy, dead->TTL-expiry+SREM, docker exec /health/live probe)"
    - "TEST-01/02/03 remain UNTICKED in the runbook DoD until the operator records a GREEN live run"
  artifacts:
    - path: "scripts/phase-62-close.ps1"
      provides: "v7 triple-SHA net-zero close gate (clone of phase-58 + D-09 prefix exclusion + retitle)"
      contains: "notmatch '^skp:proc:'"
    - path: ".planning/phases/62-live-proof-close-gate/62-HUMAN-UAT.md"
      provides: "Operator runbook - build/rebuild/close-gate steps + lifecycle proofs + GREEN record block"
      contains: "docker exec"
  key_links:
    - from: "scripts/phase-62-close.ps1 redis SHA"
      to: "steady-state liveness namespace skp:proc:*"
      via: "Where-Object { $_ -notmatch '^skp:proc:' } on BOTH before/after"
      pattern: "notmatch '\\^skp:proc:'"
    - from: "62-HUMAN-UAT.md rebuild step"
      to: "v7 stack (2 replicas + badconfig profile)"
      via: "docker compose --profile badconfig up -d --build"
      pattern: "--profile badconfig up -d --build"
    - from: "62-HUMAN-UAT.md probe step"
      to: "a HEALTHY processor-sample replica /health/live"
      via: "docker exec <name> wget -qO- localhost:8082/health/live"
      pattern: "wget -qO- .*8082/health/live"
---

<objective>
Author the v7.0.0 milestone close gate: clone scripts/phase-58-close.ps1 to scripts/phase-62-close.ps1
with the D-09 prefix exclusion + retitle (D-08/D-12), and write the 62-HUMAN-UAT.md operator runbook
(mirror 58-HUMAN-UAT.md) covering the live N=3 close run plus the four genuinely-multi-container lifecycle
proofs the xUnit harness cannot do (D-03/D-05/D-06/D-07/D-11). Run the autonomous build gate (D-14). The
live N=3xGREEN close run and all lifecycle proofs are OPERATOR-GATED (D-15) - the executor authors the
artifacts and verifies they exist/compile/are AST-valid, but does NOT run the ~50-min live gate.

Purpose: TEST-03 (triple-SHA net-zero N=3 GREEN close gate) and the live halves of TEST-01/TEST-02 (real
multi-replica self-register, durably-broken->unhealthy, dead->TTL-expiry+SREM, live /health/live probe).
The clone is verbatim except the four deltas below; the runbook seals the milestone.
Output: scripts/phase-62-close.ps1 (AST-valid) + 62-HUMAN-UAT.md (status: pending until operator GREEN).

DEPENDS ON Plan 01 (the 2-replica compose reshape + the four retags must exist - the gate runs the FULL
suite live, so the retagged regression + new test must be in the tree) and Plan 02 (the new
GateKeyspaceE2ETests must compile + exist - they run inside the live gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-live-proof-close-gate/62-CONTEXT.md
@.planning/phases/62-live-proof-close-gate/62-RESEARCH.md
@.planning/phases/62-live-proof-close-gate/62-PATTERNS.md
@scripts/phase-58-close.ps1
@.planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md

<interfaces>
<!-- The clone source + the exact deltas. The executor copies phase-58-close.ps1 verbatim, then applies these. -->

DELTA 1 (D-09 - the load-bearing change): the redis-scan exclusion at phase-58 lines 303 (BEFORE) and
379 (AFTER). REPLACE BOTH:
  OLD: $beforeRedis = (docker exec sk-redis redis-cli --scan | Where-Object { $_ -ne "skp:$($procId.ToString().ToLower())" } | Sort-Object -CaseSensitive | Out-String).Trim()
  NEW: $beforeRedis = (docker exec sk-redis redis-cli --scan | Where-Object { $_ -notmatch '^skp:proc:' } | Sort-Object -CaseSensitive | Out-String).Trim()
  (identical replacement for $afterRedis at line 379)
Rationale: v7 steady-state liveness is the index SET skp:proc:{procId:D} + per-instance keys
skp:proc:{procId:D}:{instanceId} (2 healthy Sample replicas + the durably-unhealthy badconfig replica),
non-deterministic instanceIds. The ^skp:proc: prefix excludes both families for ALL processors in one
filter. skp:data:* / skp:msg:* leak detection is unaffected.

DELTA 2 (D-12 version - VERIFIED at plan time): src/Processor.Sample/appsettings.json:11 reads
"Version": "3.5.0" (UNCHANGED for v7). The seed body (phase-58 line ~223) hardcodes version = '3.5.0' -
CARRY FORWARD 3.5.0 (SourceHash, not version, is identity; A2). Retitle the seed description string from
"Phase 58 close-gate seed row..." to "Phase 62 close-gate seed row...".

DELTA 3 (D-08 retitle): every "Phase 58" / "v6.0.0" header/Write-Host/banner/operator-append string ->
"Phase 62" / "v7.0.0". Notably the header comment (lines 1-2), the start Write-Host (~line 104), the
PASS/FAIL banners (~470/474), and the operator-append line (~481 - point it at 62-HUMAN-UAT.md).

UNCHANGED (do NOT touch): dual SourceHash read (Sample + BadConfig, ^[a-f0-9]{64}$, exit 2 on mismatch,
:106-158); two-schema GET-or-create by sentinel Name + two processor rows CREATE-IF-ABSENT never PUT
(:162-242); compose-health pre-flight loop (:276-292 - ALREADY handles replicas:2 all-instances-healthy,
written for keeper:2, so processor-sample:2 needs NO change); the N=3 identical-Passed-count Smell-A
guard (:331-364); dotnet test --no-build full-suite command (:336, no --filter); _bus_ rabbitmq exclusion
(:309/385); list_queues name SHA; skp-dlq-1 depth==0 single-element loop (:436); skp:msg:* count==0
(:461); psql -lqt SHA. CONFIRM the post-seed single-service wait (:251-259) does not assume one instance -
the $services pre-flight (:280-287, all-instances-healthy) is the authoritative gate, so this is
acceptable; leave it unless it hard-fails on the array (RESEARCH Clone Deltas D-12 nuance).

RUNBOOK structure (mirror 58-HUMAN-UAT.md exactly, swap v6->v7 facts):
- Frontmatter: status: pending (flips to passed only on operator GREEN), phase: 62-live-proof-close-gate,
  source: [62-..-PLAN.md].
- Step 1 Clean host build: dotnet clean SK_P.sln + -c Release + -c Debug (SourceHash-match rationale).
- Step 2 Rebuild v7 stack WITH badconfig profile: BEFORE-dirty trap (start from CLEAN redis keyspace),
  then UNCHANGED rebuild cmd:
    docker compose --profile badconfig up -d --build baseapi-service orchestrator processor-sample keeper processor-badconfig
  (processor-sample now scales to 2 via deploy.replicas; the up command is identical.)
- Step 3 Invoke the close gate: pwsh -File scripts/phase-62-close.ps1 + numbered what-it-does + exit codes.
- NEW lifecycle sections (OUTSIDE the close window - D-03/D-11):
  TEST-01a two REAL replicas self-register: SMEMBERS skp:proc:{procId:D} shows 2 distinct instanceIds,
    each with a skp:proc:{procId:D}:{instanceId} key (status=Healthy).
  TEST-01b durably-broken replica observable as unhealthy (NOT absent): --profile badconfig up; confirm
    skp:proc:{badId:D}:* exists with status=Unhealthy (v7 NEW - replaces v6 "stably absent", RESEARCH
    Pitfall 2 / RF-01).
  TEST-01c dead-replica TTL-expiry + lazy-SREM: docker stop a HEALTHY processor-sample replica; wait > 30s
    (heartbeat key TTL = max(interval*2, Ttl) = max(20,30) = 30s per RF-03 - NOT 60s, which is the
    startup/unhealthy key); confirm its per-instance key GET->null; trigger an orchestration-start so the
    absent member is lazily SREM'd; assert SMEMBERS shrinks.
  TEST-02-probe-live: resolve a HEALTHY replica name dynamically then docker exec the probe:
    $name = (docker compose ps processor-sample --format json | ConvertFrom-Json | Select-Object -First 1).Name
    docker exec $name wget -qO- http://localhost:8082/health/live
    expect 200 + summary keys inputSchema/outputSchema/configSchema in body (D-06 live half; the verdict
    half is already hermetic in LivenessWatchdogHealthCheckTests). Do NOT target badconfig's probe - a
    looping-unhealthy startup keeps L1 fresh so its /health/live returns Healthy by design (RESEARCH Open
    Question 1).
- Step 4 Record the GREEN run: copy the record-block TABLE (58:213-228): three SHA values, identical N=3
  Passed count, skp-dlq-1 depth==0, skp:msg:* count==0, gate exit 0, run date/operator. Change the redis
  SHA note from "single Sample exclusion" to "^skp:proc: prefix excluded (2 Sample replicas + index +
  badconfig replica + index)".
- Step 5 DoD gate: tick TEST-01/TEST-02/TEST-03 ONLY after operator GREEN (start UNTICKED).
- Threat mitigations / Tests / Summary / Gaps - mirror 58:267-312.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone phase-58-close.ps1 -> phase-62-close.ps1 with the 3 deltas (D-08/09/12)</name>
  <read_first>
    - scripts/phase-58-close.ps1 (the FULL 487-line clone source - read in full; the D-09 exclusion lines
      303/379, the seed block ~222-225, the header/banners/operator-append, the N=3 cadence :331-364, the
      compose-health pre-flight :276-292)
    - .planning/phases/62-live-proof-close-gate/62-PATTERNS.md (section "scripts/phase-62-close.ps1" - DELTAS 1-4)
    - .planning/phases/62-live-proof-close-gate/62-RESEARCH.md (section "Clone Deltas")
  </read_first>
  <action>
    Copy scripts/phase-58-close.ps1 to scripts/phase-62-close.ps1 VERBATIM, then apply EXACTLY these edits:
    DELTA 1 (D-09): at BOTH the BEFORE redis line (~303) and the AFTER redis line (~379), replace the
      exclusion filter `Where-Object { $_ -ne "skp:$($procId.ToString().ToLower())" }` with
      `Where-Object { $_ -notmatch '^skp:proc:' }`. Keep everything else on those lines (the
      `docker exec sk-redis redis-cli --scan | ... | Sort-Object -CaseSensitive | Out-String).Trim()`)
      identical. There must be ZERO remaining `skp:$(` / `-ne "skp:` single-key exclusion lines.
    DELTA 2 (D-12): leave the seed `version = '3.5.0'` UNCHANGED (confirmed: appsettings.json:11 = "3.5.0";
      SourceHash is identity). Retitle the seed `description` string "Phase 58 close-gate seed row..." ->
      "Phase 62 close-gate seed row...".
    DELTA 3 (D-08 retitle): change every "Phase 58" -> "Phase 62" and every "v6.0.0" -> "v7.0.0" in
      header comments, Write-Host messages, PASS/FAIL banners, and the operator-append line (point the
      operator-append at .planning/phases/62-live-proof-close-gate/62-HUMAN-UAT.md, not the 58 path).
    DO NOT touch: dual SourceHash read + validation; the two-schema/two-processor CREATE-IF-ABSENT seed
    shape (GET-or-create, never PUT); the compose-health pre-flight loop (already handles replicas:2); the
    N=3 identical-Passed-count Smell-A guard; the `dotnet test ... --no-build` full-suite command (no
    --filter); the _bus_ exclusion; the list_queues name SHA; the skp-dlq-1 depth==0 loop; skp:msg:*
    count==0; the psql -lqt SHA. Leave the post-seed single-service wait (~251-259) as-is unless it throws
    on the replicas:2 array (the $services all-instances-healthy pre-flight is the authoritative gate).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$errs=$null; [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/phase-62-close.ps1), [ref]$null, [ref]$errs) | Out-Null; $c = Get-Content scripts/phase-62-close.ps1 -Raw; if ((($c | Select-String -Pattern \"notmatch '\^skp:proc:'\" -AllMatches).Matches.Count -ge 2) -and ($c -notmatch '-ne \"skp:\$\(') -and ($c -notmatch 'Phase 58') -and ($c -match 'Phase 62') -and ($errs.Count -eq 0)) { 'PASS' } else { throw 'phase-62-close.ps1 deltas incomplete' }"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-62-close.ps1` exists AND is AST-valid (Parser.ParseFile yields zero parse errors).
    - `grep -c "notmatch '\^skp:proc:'" scripts/phase-62-close.ps1` >= 2 (BOTH before + after) (D-09).
    - `grep '\-ne "skp:\$('` scripts/phase-62-close.ps1` returns NOTHING (no remaining single-key exclusion).
    - `grep "Phase 58" scripts/phase-62-close.ps1` returns NOTHING; `grep -c "Phase 62"` >= 1 (retitle complete).
    - `grep "version = '3.5.0'" scripts/phase-62-close.ps1` present (version carried forward); seed description says "Phase 62".
    - The N=3 cadence, dual SourceHash read, skp-dlq-1 depth==0, skp:msg:* count==0 blocks are present and unchanged (grep the distinctive strings).
  </acceptance_criteria>
  <done>phase-62-close.ps1 exists, AST-valid, carries the D-09 prefix exclusion on both snapshots, retitled to Phase 62/v7.0.0, version 3.5.0 carried, everything else verbatim from phase-58.</done>
</task>

<task type="auto">
  <name>Task 2: Author 62-HUMAN-UAT.md operator runbook (mirror 58-HUMAN-UAT.md)</name>
  <read_first>
    - .planning/phases/58-orchestration-gate-integration-proof-close/58-HUMAN-UAT.md (the FULL structure
      to mirror - frontmatter, Step 1-5, the GREEN record block :213-228, threat mitigations :267-312)
    - .planning/phases/62-live-proof-close-gate/62-PATTERNS.md (section "62-HUMAN-UAT.md")
    - .planning/phases/62-live-proof-close-gate/62-RESEARCH.md (RF-01/02/03 verdicts + the live probe code example)
    - scripts/phase-62-close.ps1 (the script the runbook invokes - from Task 1)
  </read_first>
  <action>
    Create .planning/phases/62-live-proof-close-gate/62-HUMAN-UAT.md mirroring 58-HUMAN-UAT.md's section
    skeleton, swapping v6->v7 facts. Frontmatter: status: pending, phase: 62-live-proof-close-gate,
    source: [62-03-PLAN.md]. Sections:
    - Purpose: "FINAL phase of v7.0.0; the live run IS the proof; TEST-01/02/03 stay [ ] until operator GREEN."
    - Step 1 Clean host build: dotnet clean SK_P.sln + dotnet build -c Release + -c Debug (the
      SourceHash-match rationale block - D-16; RESEARCH Pitfall 3).
    - Step 2 Rebuild the v7 stack WITH the badconfig profile: start from a CLEAN redis keyspace
      (BEFORE-dirty trap - mirror 58 Step 2), then the UNCHANGED rebuild command:
      `docker compose --profile badconfig up -d --build baseapi-service orchestrator processor-sample keeper processor-badconfig`
      (note: processor-sample now scales to 2 via deploy.replicas; the up command is identical).
    - Step 3 Invoke the close gate: `pwsh -File scripts/phase-62-close.ps1` + the numbered what-it-does
      list + exit codes (mirror 58 Step 3).
    - NEW lifecycle sections (explicitly OUTSIDE the close-gate window so they never perturb BEFORE==AFTER - D-11):
      * TEST-01a (two REAL replicas self-register): `redis-cli SMEMBERS skp:proc:{procId:D}` shows 2
        distinct instanceIds; each `redis-cli GET skp:proc:{procId:D}:{instanceId}` shows status=Healthy.
      * TEST-01b (durably-broken -> unhealthy, NOT absent): with --profile badconfig up, confirm
        `redis-cli GET skp:proc:{badId:D}:{instanceId}` shows status=Unhealthy (v7 NEW; assert Unhealthy,
        NOT "absent" - RESEARCH Pitfall 2 / RF-01).
      * TEST-01c (dead-replica TTL-expiry + lazy-SREM): `docker stop` a HEALTHY processor-sample replica;
        wait > 30s (heartbeat key TTL = max(interval*2, Ttl) = max(20,30) = 30s per RF-03 - state this
        number explicitly; NOT 60s which is the startup/unhealthy key); confirm its per-instance key
        GET->null; trigger an orchestration-start so the absent member is lazily SREM'd; assert
        `SMEMBERS skp:proc:{procId:D}` shrinks by one.
      * TEST-02-probe-live: resolve a HEALTHY replica name dynamically + docker exec the probe:
        `$name = (docker compose ps processor-sample --format json | ConvertFrom-Json | Select-Object -First 1).Name`
        `docker exec $name wget -qO- http://localhost:8082/health/live`
        expect 200 + a JSON body carrying inputSchema/outputSchema/configSchema (D-06 live half). State:
        do NOT target badconfig's probe (a looping-unhealthy startup keeps L1 fresh -> /health/live
        returns Healthy by design; RESEARCH Open Question 1). Note the verdict math is already proven
        hermetically in LivenessWatchdogHealthCheckTests (RF-02) - only the live wiring is manual here.
    - Step 4 Record the GREEN run: copy the record-block TABLE from 58:213-228 (three SHA values,
      identical N=3 Passed count, skp-dlq-1 depth==0, skp:msg:* count==0, gate exit 0, run date/operator).
      Change the redis SHA note to "^skp:proc: prefix excluded (2 Sample replicas + index + badconfig
      replica + index)".
    - Step 5 DoD gate: list TEST-01 / TEST-02 / TEST-03 as `[ ]` (UNTICKED) with a note that they flip to
      `[x]` ONLY after the operator records the GREEN run (D-15).
    - Threat mitigations / Tests / Summary / Gaps: mirror 58:267-312 (the SourceHash spoof, false-GREEN
      net-zero, fabricated-key tamper, badconfig DoS-accept mitigations).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content .planning/phases/62-live-proof-close-gate/62-HUMAN-UAT.md -Raw; if (($c -match 'status: pending') -and ($c -match '--profile badconfig up -d --build') -and ($c -match 'wget -qO- .*8082/health/live') -and ($c -match 'SMEMBERS skp:proc:') -and ($c -match '30s') -and ($c -match 'phase-62-close\.ps1') -and ($c -match 'TEST-01') -and ($c -match 'TEST-02') -and ($c -match 'TEST-03')) { 'PASS' } else { throw 'runbook missing required sections' }"</automated>
  </verify>
  <acceptance_criteria>
    - `62-HUMAN-UAT.md` exists with frontmatter `status: pending`.
    - Contains the rebuild command `docker compose --profile badconfig up -d --build` (Step 2).
    - Contains the live probe step `docker exec ... wget -qO- ...8082/health/live` (TEST-02 live half) targeting a HEALTHY replica.
    - Contains all four lifecycle proofs: `SMEMBERS skp:proc:` (self-register/SREM), `status=Unhealthy` (durably-broken), `docker stop` + a 30s wait note (TTL-expiry), the probe.
    - Invokes `scripts/phase-62-close.ps1`; TEST-01/TEST-02/TEST-03 appear as `[ ]` (unticked) in the DoD.
    - The TTL note explicitly says 30s for the healthy heartbeat key (RF-03), not 60s.
  </acceptance_criteria>
  <done>62-HUMAN-UAT.md mirrors the 58 structure, documents the build/rebuild/close-gate + four lifecycle proofs + GREEN record block, TEST-01/02/03 unticked until operator GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Autonomous build gate + AST/existence verification (D-14)</name>
  <read_first>
    - scripts/phase-62-close.ps1 (from Task 1 - must exist + parse)
    - .planning/phases/62-live-proof-close-gate/62-VALIDATION.md (the sampling/build-gate contract)
  </read_first>
  <action>
    Run the autonomous, machine-verifiable gate (this is the boundary of autonomous work; the LIVE N=3 run
    is operator-gated in Task 4):
    1. `dotnet build SK_P.sln -c Release` - MUST be 0 warnings, 0 errors.
    2. `dotnet build SK_P.sln -c Debug` - MUST be 0 warnings, 0 errors.
    3. Confirm the new RealStack tests COMPILE (they build with the solution; they are excluded from the
       hermetic RUN by Category=RealStack but MUST build) - implied by the two builds succeeding.
    4. Run the hermetic suite: the compiled `BaseApi.Tests.exe --filter-not-trait Category=RealStack`
       (NOT `dotnet test --filter` - MTP0001 ignores it). MUST be green.
    5. Confirm `scripts/phase-62-close.ps1` exists and is AST-valid:
       `[System.Management.Automation.Language.Parser]::ParseFile(...)` yields zero parse errors.
    If any build is non-zero-warning, FIX the offending file before this task completes - the build gate
    is the autonomous DoD (D-14). Do NOT run scripts/phase-62-close.ps1 here (that is the ~50-min
    operator-gated live run, Task 4).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "dotnet build SK_P.sln -c Release 2>&1 | Tee-Object -Variable r | Out-Null; dotnet build SK_P.sln -c Debug 2>&1 | Tee-Object -Variable d | Out-Null; $errs=$null; [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/phase-62-close.ps1), [ref]$null, [ref]$errs) | Out-Null; if (($r -match '0 Warning\(s\)') -and ($r -match '0 Error\(s\)') -and ($d -match '0 Warning\(s\)') -and ($d -match '0 Error\(s\)') -and ($errs.Count -eq 0)) { 'PASS' } else { throw 'build gate or AST check failed' }"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` reports `0 Warning(s)` and `0 Error(s)`.
    - `dotnet build SK_P.sln -c Debug` reports `0 Warning(s)` and `0 Error(s)`.
    - Hermetic run `BaseApi.Tests.exe --filter-not-trait Category=RealStack` exits success (all hermetic tests pass; RealStack excluded).
    - `scripts/phase-62-close.ps1` parses with zero AST errors (Parser.ParseFile).
  </acceptance_criteria>
  <done>Both build configs 0-warning/0-error; new RealStack tests compile; hermetic suite green; close script AST-valid. Autonomous DoD met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator-gated live N=3xGREEN close run + multi-container lifecycle proofs (D-15)</name>
  <action>OPERATOR-GATED (D-15): the executor does NOT run this. Pause and hand control to the operator to follow .planning/phases/62-live-proof-close-gate/62-HUMAN-UAT.md (the ~50-min live close gate + the four multi-container lifecycle proofs). TEST-01/02/03 stay UNTICKED until the operator records a GREEN run. See <how-to-verify> for the exact steps.</action>
  <what-built>
    The autonomous artifacts are complete and machine-verified: the 2-replica compose reshape (Plan 01),
    the four Phase-62 retags (Plan 01), the fabricated-key gate-verdict tests + helper (Plan 02), the
    cloned scripts/phase-62-close.ps1 (AST-valid, D-09 prefix exclusion), the 62-HUMAN-UAT.md runbook, and
    the 0-warning Release+Debug build gate (Task 3). The new RealStack tests compile. NOTHING live has been
    run - this checkpoint hands the live N=3xGREEN close gate + the four multi-container lifecycle proofs to
    the operator (D-15). TEST-01/02/03 remain UNTICKED.
  </what-built>
  <how-to-verify>
    Follow .planning/phases/62-live-proof-close-gate/62-HUMAN-UAT.md end to end:
    1. Step 1 - Clean host build: `dotnet clean SK_P.sln`; `dotnet build SK_P.sln -c Release`; `-c Debug`
       (both 0-warning; this regenerates the embedded SourceHash so it matches the rebuilt images - D-16).
    2. Step 2 - Start from a CLEAN redis keyspace (BEFORE-dirty trap), then rebuild the v7 stack:
       `docker compose --profile badconfig up -d --build baseapi-service orchestrator processor-sample keeper processor-badconfig`
       (processor-sample now comes up as TWO replicas).
    3. Step 3 - Invoke the close gate: `pwsh -File scripts/phase-62-close.ps1`. Expect exit 0 with N=3
       consecutive GREEN, identical Passed counts, triple-SHA (psql \l / redis --scan / rabbitmq
       list_queues) BEFORE==AFTER, skp-dlq-1 depth==0, skp:msg:* count==0 (~50 min). The redis SHA excludes
       the ^skp:proc: liveness namespace (2 Sample replicas + index + badconfig + index).
    4. Lifecycle proofs (OUTSIDE the close window): run TEST-01a (SMEMBERS shows 2 instanceIds), TEST-01b
       (badconfig key status=Unhealthy, NOT absent), TEST-01c (docker stop a HEALTHY replica, wait > 30s,
       key GET->null, trigger a start, SMEMBERS shrinks), TEST-02-probe-live (`docker exec <healthy-replica>
       wget -qO- localhost:8082/health/live` -> 200 + inputSchema/outputSchema/configSchema in body).
    5. Record the GREEN run in Step 4's table; tick TEST-01/TEST-02/TEST-03 in Step 5 ONLY if all pass.
    If the close gate fails (non-zero exit, SHA drift, DLQ depth, warning) OR any lifecycle proof fails,
    do NOT tick the TEST reqs - report the failure for gap closure.
  </how-to-verify>
  <resume-signal>Type "approved - N=3 GREEN recorded" (with the three SHA values + Passed count), or describe the failure (which step, exit code, SHA drift, or lifecycle assertion).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live stack images -> seeded DB identity | the embedded SourceHash must equal the host build hash or the gate false-passes/times out |
| close-script snapshot -> net-zero verdict | the redis/psql/rabbitmq SHAs are the integrity boundary; a wrong exclusion hides a real leak |
| operator -> milestone seal | TEST-01/02/03 tick only on a real GREEN run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Spoofing | stale/mixed-version image identity divergence | mitigate | Clean host build + `--build` rebuild (Step 1/2) + the close script's dual `^[a-f0-9]{64}$` SourceHash validation (carried verbatim, exit 2 on mismatch) + the real-container liveness poll (D-16; RESEARCH Pitfall 3). |
| T-62-08 | Repudiation | false GREEN / net-zero snapshot integrity | mitigate | Start from a CLEAN redis keyspace (BEFORE-dirty trap, Step 2) + the N=3 identical-Passed-count Smell-A guard (carried verbatim) + triple-SHA BEFORE==AFTER (D-11/D-13). The D-09 prefix exclusion is SCOPED to ^skp:proc: only, so skp:data:* / skp:msg:* leaks remain detectable. |
| T-62-09 | Information disclosure | the runbook surfaces secrets (connection strings, instanceIds) | mitigate | The probe body carries only the three SchemaOutcome strings (no secrets); the 422 reason is counts-only (proven by Plan 02 Test B). The runbook documents redis-cli reads, never logs connection strings. |
| T-62-10 | DoS | processor-badconfig under profile destabilizes the gate | accept | Gate A stay-up posture: badconfig MarkReady (no crash-loop), /ready green, withholds MarkHealthy; it is net-zero-harmless because D-09 excludes its ^skp:proc: keys; bounded operator-supervised run (RESEARCH T-58-14). |
</threat_model>

<verification>
- `scripts/phase-62-close.ps1` exists, AST-valid, carries `notmatch '^skp:proc:'` on BOTH snapshots, retitled to Phase 62/v7.0.0, version 3.5.0.
- `62-HUMAN-UAT.md` exists (status: pending) with the build/rebuild/close-gate steps + four lifecycle proofs + GREEN record block; TEST-01/02/03 unticked.
- `dotnet build SK_P.sln -c Release` AND `-c Debug` both 0-warning/0-error; new RealStack tests compile; hermetic suite green.
- (Operator gate) live N=3xGREEN + triple-SHA net-zero + DLQ 0 + the four lifecycle proofs pass before TEST-01/02/03 tick.
</verification>

<success_criteria>
- The autonomous DoD is met: artifacts authored, AST-valid, 0-warning both configs, hermetic green.
- The runbook gives the operator an unambiguous path to the live GREEN gate + lifecycle proofs.
- TEST-01/02/03 are sealed only on a recorded operator GREEN run (D-15).
</success_criteria>

<output>
After completion, create `.planning/phases/62-live-proof-close-gate/62-03-SUMMARY.md`.
</output>
