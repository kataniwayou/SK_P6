---
phase: 62-live-proof-close-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs
  - tests/BaseApi.Tests/Orchestrator/GateKeyspaceE2ETests.cs
autonomous: true
requirements: [TEST-01, TEST-02]
tags: [liveness, gate, realstack, fabricated-keys]

must_haves:
  truths:
    - "A test deterministically proves the gate ADMITS (204) when >=1 fabricated healthy replica exists even alongside a fabricated unhealthy/stale sibling"
    - "A test deterministically proves the gate BLOCKS (422 + RFC 7807) when no fabricated replica qualifies"
    - "A malformed fabricated per-instance value yields 422, never 500"
    - "Every fabricated per-instance key AND its index member is removed in teardown (net-zero - no pollution of a later gate read)"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/GateKeyspaceE2ETests.cs"
      provides: "Deterministic fabricated-key gate verdict tests (>=1-healthy admit / none -> 422 / malformed -> 422)"
      contains: "Trait(\"Phase\", \"62\")"
      min_lines: 80
    - path: "tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs"
      provides: "SeedFabricatedLivenessAsync helper + an InstanceIndex SREM teardown list on the factory"
      contains: "SeedFabricatedLivenessAsync"
  key_links:
    - from: "SeedFabricatedLivenessAsync"
      to: "host Redis skp:proc:{procId}:{instanceId} + SADD skp:proc:{procId}"
      via: "ProcessorLivenessEntry.Create + JsonSerializer.Serialize + L2ProjectionKeys.PerInstance/InstanceIndex"
      pattern: "ProcessorLivenessEntry\\.Create\\("
    - from: "GateKeyspaceE2ETests fabricated keys"
      to: "ProcessorLivenessValidator (in-process gate via POST /api/v1/orchestration/start)"
      via: "RealStackWebAppFactory in-process WebAPI -> host Redis"
      pattern: "PostAsJsonAsync\\(\"/api/v1/orchestration/start\""
    - from: "fabricated key + index member"
      to: "factory teardown (L2KeysToCleanup + InstanceIndex SREM list)"
      via: "DisposeAsync net-zero drain"
      pattern: "L2KeysToCleanup\\.Add"
---

<objective>
Add the deterministic D-04 fabricated-key gate-verdict tests (the xUnit RealStack half of TEST-01/TEST-02)
plus the co-located fabricated-key seeding helper. These prove - repeatably, no timing flake - that the
already-shipped >=1-healthy gate ADMITS when at least one fabricated healthy replica exists even alongside
an unhealthy/stale sibling, and BLOCKS 422+RFC7807 when none qualify. The real multi-replica
self-registration is proven separately in the operator runbook (Plan 03, D-03); this is the clean
regression-gated verdict proof.

Purpose: TEST-01 (deterministic keyspace round-trip through the gate reader) and TEST-02 (>=1-healthy
admit / 422-when-none) need a deterministic, timing-free proof. Fabricating crafted per-instance keys
directly in host Redis (Phase-61 WR-01/WR-02 craft-redis-state style) drives the in-process gate verdict
with zero container lifecycle and zero timing race.
Output: GateKeyspaceE2ETests.cs (new RealStack test) + SeedFabricatedLivenessAsync helper + an
InstanceIndex SREM teardown list on RealStackWebAppFactory.

DO NOT change the gate semantics (ProcessorLivenessValidator.cs) - it is read-only context. This phase
PROVES it; it does not modify it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-live-proof-close-gate/62-CONTEXT.md
@.planning/phases/62-live-proof-close-gate/62-RESEARCH.md
@.planning/phases/62-live-proof-close-gate/62-PATTERNS.md
@src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
@src/Messaging.Contracts/Projections/ProcessorLivenessEntry.cs
@src/Messaging.Contracts/Projections/LivenessStatus.cs
@src/BaseApi.Service/Features/Orchestration/Validation/ProcessorLivenessValidator.cs

<interfaces>
<!-- Key contracts the executor uses directly - no codebase exploration needed. -->

Key builders (L2ProjectionKeys.cs:44-51) - use these, NEVER literals:
  L2ProjectionKeys.PerInstance(Guid procId, string instanceId) => "skp:proc:{procId:D}:{instanceId}"
  L2ProjectionKeys.InstanceIndex(Guid procId)                  => "skp:proc:{procId:D}"

Entry factory (ProcessorLivenessEntry.cs:27) - the ONLY sanctioned construction path:
  public static ProcessorLivenessEntry Create(
      string? inputOutcome, string? outputOutcome, string? configOutcome,
      DateTime timestamp, int interval)
  // null outcome => SchemaOutcome.Success; ANY SchemaOutcome.Fail => Status=LivenessStatus.Unhealthy; else Healthy.
  // Wire shape (pinned by [JsonPropertyName]): top-level timestamp/interval/status/summary;
  //   summary => inputSchema/outputSchema/configSchema. Serialize with JsonSerializer.Serialize(entry) (default opts).

Status/outcome consts - use the const, never the literal:
  LivenessStatus.Healthy = "Healthy"; LivenessStatus.Unhealthy = "Unhealthy"  (LivenessStatus.cs:12-13)
  SchemaOutcome.Success / SchemaOutcome.Fail  (Messaging.Contracts.Projections)

Gate reader behavior (ProcessorLivenessValidator.cs:46-77 - READ-ONLY, do not change):
  SMEMBERS index -> GET each per-instance -> FIRST Healthy+fresh admits (short-circuit :69);
  Status != "Healthy" (ordinal) => unhealthy :67; Timestamp.AddSeconds(Interval*2) <= now => stale :68;
  null/JsonException => malformed :63-64; absent member => lazy SREM fire-and-forget :53-54;
  no qualifier => ProcessorNotLive 422 RFC7807 with COUNTS-ONLY reason :74-77 (no instanceIds/secrets).

Harness reuse (all in SampleRoundTripE2ETests.cs):
  internal sealed class RealStackWebAppFactory : Composition.Phase8WebAppFactory  (:417-495)
    ctor sets host stack: Redis localhost:6380, RMQ localhost:5673, Postgres localhost:5433
    public List<RedisKey> L2KeysToCleanup           - drained (KeyDelete) in DisposeAsync (:477-493)
    public List<RedisValue> ParentIndexMembersToSrem - SREM from bare `skp:` parent index ONLY
    DisposeAsync (:477-493) currently drains L2KeysToCleanup + ParentIndexMembersToSrem; NO skp:proc index SREM
  const HostRedis = "localhost:6380,abortConnect=false,connectTimeout=5000"  (:447)
  static SeedConfigSchemaAsync(client, sentinelName, definition, ct)         (:357)
  static SeedProcessorAsync(client, sourceHash, ct, configSchemaId=null)     (:315)
  static SeedStepAsync(client, procId, ct) / SeedWorkflowAsync(client, entryStepIds, cron, ct)  (:376/:391)
  static PollForHealthyLivenessAsync(procId, ct)                             (:218) - host-Redis connect precedent
  internal const SampleCompatibleSchemaName (:81) / SampleCompatibleSchemaDefinition (:86)

GateACompositionE2ETests.cs analog - header (:51-54), factory wiring (:79-81), seed (:93-102),
  422 assert (:166-168), 204 assert (:214-216), teardown registration (:174-176), imports (:1-8).

LANDMINE (RESEARCH Pitfall 6): RealStackNetZeroSweepFixture does NOT sweep skp:proc:*. Every fabricated
  skp:proc:* key AND every fabricated index member MUST be registered for teardown or it pollutes a
  later test's gate SMEMBERS. Use a DISTINCT throwaway procId for each pure-fabrication test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SeedFabricatedLivenessAsync helper + InstanceIndex SREM teardown list</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the RealStackWebAppFactory nested
      class :417-495, DisposeAsync :477-493, PollForHealthyLivenessAsync :218-255 for the host-Redis
      connect pattern, and the HostRedis const :447)
    - src/Messaging.Contracts/Projections/ProcessorLivenessEntry.cs (the Create factory + JsonPropertyName pins)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (PerInstance/InstanceIndex builders)
    - .planning/phases/62-live-proof-close-gate/62-PATTERNS.md (section "Fabricated-key seeding helper")
  </read_first>
  <behavior>
    - Calling SeedFabricatedLivenessAsync(factory, procId, "fab-healthy", healthyEntry, ct) writes
      skp:proc:{procId:D}:fab-healthy (serialized entry) with a 60s TTL AND SADDs "fab-healthy" to
      skp:proc:{procId:D}.
    - After the helper runs, the per-instance key and the index member are both registered for net-zero
      teardown; on factory DisposeAsync the per-instance key is deleted AND the index member is SREM'd
      from skp:proc:{procId:D} (verified by a follow-up SMEMBERS == empty after dispose).
    - The serialized value round-trips through ProcessorLivenessEntry deserialization (correct casing -
      built via Create + JsonSerializer.Serialize, never hand-authored).
  </behavior>
  <action>
    Add to RealStackWebAppFactory a new public teardown list for skp:proc index members (the existing
    ParentIndexMembersToSrem SREMs the bare skp: parent index, NOT skp:proc:{procId} - so a new list is
    required, RESEARCH Pattern note (a)):
      public List&lt;(Guid ProcId, RedisValue Member)&gt; InstanceIndexMembersToSrem { get; } = new();
    In DisposeAsync (:477-493), after the existing L2KeysToCleanup drain, add a loop that for each
    (procId, member) in InstanceIndexMembersToSrem calls
    await db.SetRemoveAsync(L2ProjectionKeys.InstanceIndex(procId), member);
    reusing the same ConnectionMultiplexer/db already opened for teardown (do not open a second connection).

    Add a co-located internal static async Task SeedFabricatedLivenessAsync(RealStackWebAppFactory
    factory, Guid procId, string instanceId, ProcessorLivenessEntry entry, CancellationToken ct) (next to
    PollForHealthyLivenessAsync) with this body (mirror PATTERNS.md verbatim):
      await using var mux = await ConnectionMultiplexer.ConnectAsync(HostRedis);
      var db = mux.GetDatabase();
      var key = L2ProjectionKeys.PerInstance(procId, instanceId);
      await db.StringSetAsync(key, JsonSerializer.Serialize(entry), TimeSpan.FromSeconds(60));
      await db.SetAddAsync(L2ProjectionKeys.InstanceIndex(procId), instanceId);
      factory.L2KeysToCleanup.Add(key);
      factory.InstanceIndexMembersToSrem.Add((procId, instanceId));
    Use L2ProjectionKeys.PerInstance / .InstanceIndex - NEVER string literals. The entry is built by the
    CALLER via ProcessorLivenessEntry.Create(...) (the helper only serializes + writes + registers
    teardown). Do NOT change any existing helper or test in the file.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs -Raw; if (($c -match 'SeedFabricatedLivenessAsync') -and ($c -match 'InstanceIndexMembersToSrem') -and ($c -match 'L2ProjectionKeys\.PerInstance') -and ($c -match 'SetRemoveAsync')) { 'PASS' } else { throw 'helper/SREM list missing' }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SeedFabricatedLivenessAsync" SampleRoundTripE2ETests.cs` >= 1 (method defined).
    - `grep -c "InstanceIndexMembersToSrem" SampleRoundTripE2ETests.cs` >= 3 (declaration + helper Add + DisposeAsync loop).
    - `grep -q "SetRemoveAsync(L2ProjectionKeys.InstanceIndex" SampleRoundTripE2ETests.cs` (teardown SREMs the proc index).
    - Helper uses `L2ProjectionKeys.PerInstance(` and `L2ProjectionKeys.InstanceIndex(` (no `"skp:proc:` string literal).
    - `dotnet build SK_P.sln -c Debug` succeeds 0 warnings (the helper COMPILES - D-14).
  </acceptance_criteria>
  <done>SeedFabricatedLivenessAsync writes the per-instance key + SADDs the index + registers both for net-zero teardown; DisposeAsync SREMs proc-index members; solution compiles 0-warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GateKeyspaceE2ETests - fabricated-key >=1-healthy admit / 422-when-none / malformed->422</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/GateACompositionE2ETests.cs (the gate-verdict RealStack pattern:
      header :51-54, factory+client :79-81, imports :1-8, seed :93-102, 422 assert :166-168, 204 assert
      :214-216, teardown :174-176)
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the factory + seed helpers + the new
      SeedFabricatedLivenessAsync from Task 1)
    - src/BaseApi.Service/Features/Orchestration/Validation/ProcessorLivenessValidator.cs (READ-ONLY: the
      reader the fabricated keys round-trip through - :46-77)
    - .planning/phases/62-live-proof-close-gate/62-PATTERNS.md (sections "GateKeyspaceE2ETests.cs" + "Craft recipes")
  </read_first>
  <behavior>
    - Test A (>=1-healthy admits with unhealthy + stale siblings): seed a Processor row (distinct
      throwaway procId via SeedProcessorAsync, configSchema = SampleCompatibleSchemaName), build a
      step+workflow on it; fabricate THREE keys on that procId - healthy (Create(null,null,null, now, 10)),
      unhealthy (Create(SchemaOutcome.Fail, null, null, now, 10)), stale (Create(null,null,null,
      now.AddSeconds(-25), 10)); POST /api/v1/orchestration/start [workflowId] => 204 NoContent (the
      healthy replica qualifies first).
    - Test B (422 when NONE qualify): same seed/workflow on a SECOND distinct procId; fabricate only an
      unhealthy + a stale key (NO healthy); POST start => 422 UnprocessableEntity (RFC7807). Assert the
      problem body does NOT contain any fabricated instanceId string (counts-only - V7 no-info-leak).
    - Test C (malformed -> 422 not 500): on a THIRD distinct procId, write a malformed per-instance value
      ("{not-an-entry") + SADD, register both for teardown; POST start => 422 (the reader treats
      null/JsonException as malformed :63-64), NEVER 500.
  </behavior>
  <action>
    Create tests/BaseApi.Tests/Orchestrator/GateKeyspaceE2ETests.cs. Copy the class/trait/collection
    header from GateACompositionE2ETests.cs:51-54 with Phase=62:
      [Trait("Category", "E2E")]
      [Trait("Category", "RealStack")]
      [Trait("Phase", "62")]
      [Collection("Observability")]
      public sealed class GateKeyspaceE2ETests
    Copy the imports block from GateACompositionE2ETests.cs:1-8 (System.Net, System.Net.Http.Json,
    System.Reflection, System.Text.Json, BaseApi.Tests.Observability.Helpers, Messaging.Contracts.Projections,
    StackExchange.Redis, Xunit). Each test opens:
      await using var factory = new SampleRoundTripE2ETests.RealStackWebAppFactory();
      await factory.InitializeAsync();
      using var client = factory.CreateClient();

    Seed per test (reuse promoted-internal helpers), using a DISTINCT throwaway procId per test so the
    fabricated index never collides with a live replica's index (RESEARCH Pitfall 6). Drive identity via
    the genuine embedded Sample SourceHash:
      var hash = typeof(global::Processor.Sample.SampleProcessor).Assembly
          .GetCustomAttributes&lt;AssemblyMetadataAttribute&gt;().First(a =&gt; a.Key == "SourceHash").Value!;
      var schemaId = await SampleRoundTripE2ETests.SeedConfigSchemaAsync(client,
          SampleRoundTripE2ETests.SampleCompatibleSchemaName,
          SampleRoundTripE2ETests.SampleCompatibleSchemaDefinition, ct);
      var procId = await SampleRoundTripE2ETests.SeedProcessorAsync(client, hash, ct, configSchemaId: schemaId);
      var stepId = await SampleRoundTripE2ETests.SeedStepAsync(client, procId, ct);
      var wfId   = await SampleRoundTripE2ETests.SeedWorkflowAsync(client, new List&lt;Guid&gt;{ stepId }, "* * * * *", ct);

    Fabricate keys via the Task-1 helper + Create factory (craft recipes - RESEARCH/PATTERNS verbatim):
      var now = DateTime.UtcNow;
      await SampleRoundTripE2ETests.SeedFabricatedLivenessAsync(factory, procId, "fab-healthy",
          ProcessorLivenessEntry.Create(null, null, null, now, 10), ct);
      await SampleRoundTripE2ETests.SeedFabricatedLivenessAsync(factory, procId, "fab-unhealthy",
          ProcessorLivenessEntry.Create(SchemaOutcome.Fail, null, null, now, 10), ct);
      await SampleRoundTripE2ETests.SeedFabricatedLivenessAsync(factory, procId, "fab-stale",
          ProcessorLivenessEntry.Create(null, null, null, now.AddSeconds(-25), 10), ct);
    Use SchemaOutcome.Fail / LivenessStatus.* consts - never literals.

    Assertions: Test A Assert.Equal(HttpStatusCode.NoContent, resp.StatusCode) (pattern :214-216); Test B
    Assert.Equal(HttpStatusCode.UnprocessableEntity, resp.StatusCode) (pattern :166-168) + read the
    problem JSON body string and Assert.DoesNotContain("fab-", body) (counts-only no-leak guard, V7).
    Test C: write the malformed value directly (the helper requires a valid entry, so for the malformed
    case open a ConnectionMultiplexer to the host Redis const and call
    StringSetAsync(L2ProjectionKeys.PerInstance(procId,"fab-bad"), "{not-an-entry", TimeSpan.FromSeconds(60))
    + SetAddAsync(L2ProjectionKeys.InstanceIndex(procId), "fab-bad"), then
    factory.L2KeysToCleanup.Add(L2ProjectionKeys.PerInstance(procId,"fab-bad")) +
    factory.InstanceIndexMembersToSrem.Add((procId,"fab-bad"))), then
    Assert.Equal(HttpStatusCode.UnprocessableEntity, resp.StatusCode) (NOT 500).
    Register every workflow/step parent-index member for teardown exactly as GateAComposition:174-176
    (ParentIndexMembersToSrem.Add(wfId.ToString("D")) + L2KeysToCleanup.Add($"skp:{wfId}") /
    $"skp:{wfId}:{stepId}"). The fabricated skp:proc keys are auto-registered by the Task-1 helper; for
    Test C's manual malformed write, register both explicitly as above.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content tests/BaseApi.Tests/Orchestrator/GateKeyspaceE2ETests.cs -Raw; if (($c -match 'Trait\(.Category., .RealStack.\)') -and ($c -match 'Trait\(.Phase., .62.\)') -and ($c -match 'ProcessorLivenessEntry\.Create\(') -and ($c -match 'L2ProjectionKeys\.PerInstance\(') -and ($c -match 'HttpStatusCode\.NoContent') -and ($c -match 'HttpStatusCode\.UnprocessableEntity')) { 'PASS' } else { throw 'GateKeyspace test missing required elements' }"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Orchestrator/GateKeyspaceE2ETests.cs` exists and contains `[Trait("Category","RealStack")]` AND `[Trait("Phase","62")]`.
    - Contains `ProcessorLivenessEntry.Create(` AND `L2ProjectionKeys.PerInstance(` AND `L2ProjectionKeys.InstanceIndex(` (no hand-authored skp:proc literal except the deliberate malformed-value test).
    - Contains `HttpStatusCode.NoContent` (admit) AND `HttpStatusCode.UnprocessableEntity` (block) asserts; uses `SchemaOutcome.Fail`.
    - Contains a no-leak guard (`Assert.DoesNotContain("fab-"` against the 422 problem body).
    - `dotnet build SK_P.sln -c Release` AND `-c Debug` both 0 warnings (the new RealStack test COMPILES - D-14; it is excluded from the hermetic run by Category=RealStack).
    - Hermetic run `BaseApi.Tests.exe --filter-not-trait Category=RealStack` stays green (the new test is excluded; nothing else regressed).
  </acceptance_criteria>
  <done>GateKeyspaceE2ETests proves admit/block/malformed verdicts via fabricated keys, no info leak in the 422; net-zero teardown registered; compiles 0-warning both configs; hermetic suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-registered per-instance value -> gate reader | every L2 per-instance value is EXTERNAL data the gate deserializes; fabricated keys model both well-formed and malformed external input |
| gate -> 422 problem response body | the failure reason crosses back to the caller; must not carry instanceIds/secrets |
| fabricated index member -> later test's gate SMEMBERS | uncleaned fabricated members would leak across tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04 | Tampering | fabricated index member leaks into a later live gate read | mitigate | Every fabricated per-instance key registered in L2KeysToCleanup; every index member registered in the new InstanceIndexMembersToSrem list SREM'd in DisposeAsync; DISTINCT throwaway procId per test so the index never collides with a live replica's (RESEARCH Pitfall 6). |
| T-62-05 | Denial of Service / robustness | malformed per-instance value crashes the gate (500) | mitigate | Test C asserts a malformed value yields 422, NOT 500 - locks the ProcessorLivenessValidator malformed-counting path (:63-64) as external-input-safe (V5 input validation). |
| T-62-06 | Information disclosure | 422 reason leaks fabricated instanceIds / connection strings | mitigate | Test B reads the problem body and asserts DoesNotContain("fab-", body) - the counts-only reason (:74-77) carries no instanceIds/secrets (V7 error/logging). |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` AND `-c Debug` both 0 warnings (D-14 - new RealStack test + helper COMPILE).
- Hermetic suite `BaseApi.Tests.exe --filter-not-trait Category=RealStack` green (new test excluded; nothing regressed).
- `GateKeyspaceE2ETests.cs` contains the Phase 62 + RealStack traits, the Create factory, the key builders, admit/block/malformed asserts, and the no-leak guard.
- `SampleRoundTripE2ETests.cs` contains SeedFabricatedLivenessAsync + InstanceIndexMembersToSrem + the DisposeAsync SREM loop.
- (Live, in the gate - Plan 03 operator run) the new tests run green under the full RealStack suite against the v7 stack.
</verification>

<success_criteria>
- The fabricated-key gate verdicts (admit / 422-when-none / malformed->422) are proven deterministically with no timing dependence.
- Net-zero discipline holds: no fabricated skp:proc:* key or index member survives a test.
- The 422 body carries no instanceIds/secrets.
- Solution compiles 0-warning both configs; hermetic suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/62-live-proof-close-gate/62-02-SUMMARY.md`.
</output>
