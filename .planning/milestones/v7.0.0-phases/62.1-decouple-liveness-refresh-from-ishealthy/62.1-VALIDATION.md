---
phase: 62.1
slug: decouple-liveness-refresh-from-ishealthy
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-14
---

# Phase 62.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

Phase 62.1 closes Phase-60 gap **G-62-01** (the Gate-A-clash terminal `return;` let an
alive-but-unhealthy replica's L2 key TTL-expire → absent + false self-watchdog "loop stale").
The product fix lives in `ProcessorStartupOrchestrator.cs`; the hermetic proof is the new
`ClashRefreshFacts` (Plan 02 *was* the validation deliverable). Per CONTEXT **D-04** the live
TEST-01b/TEST-01c re-proof is operator-gated and deferred to a Phase-62 close-gate re-run — it is
the sole manual-only item.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit v3 on Microsoft.Testing.Platform (MTP) — .NET 8 |
| **Config file** | `tests/BaseApi.Tests/BaseApi.Tests.csproj` |
| **Quick run command** | `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-class "*ClashRefreshFacts"` |
| **Full suite command** | `tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe` (hermetic; excludes `[Trait("Category","RealStack")]` live-broker E2E) |
| **Production build gate** | `dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj -c Debug --nologo` (and `-c Release`) — 0 warnings |
| **External dependency** | real Redis at `localhost:6380` (`RedisFixture`); net-zero teardown via `_redis.Track` |
| **Estimated runtime** | ClashRefreshFacts ~15–25s; full hermetic suite ~minutes |

> Note: `dotnet test --filter` is silently ignored by MTP (`warning MTP0001`). Filter via the
> built exe's native `--filter-class` flag instead.

---

## Sampling Rate

- **After every task commit:** Run the quick command (`--filter-class "*ClashRefreshFacts"`) + the production build gate.
- **After every plan wave:** Run the full hermetic suite.
- **Before `/gsd-verify-work`:** Full hermetic suite green.
- **Max feedback latency:** ~30s (quick) / a few minutes (full suite).

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 62.1-01-01 | 01 | 1 | G-62-01, STATE-03, PROBE-02, LOOP-02 | T-62.1-01 | `WriteUnhealthyAsync` interval-parameterized; existing callers still record 30 | build | `dotnet build BaseProcessor.Core -c Debug` (+ Release) → 0 warning | ✅ | ✅ green |
| 62.1-01-02 | 01 | 1 | G-62-01, STATE-03, PROBE-02, LOOP-02 | T-62.1-02 / T-62.1-03 | clash `return;` replaced by cancellation-safe IntervalSeconds-cadence refresh loop | build + behavior | `dotnet build` + `ClashRefreshFacts` (proves the loop behavior) | ✅ | ✅ green |
| 62.1-02-01 | 02 | 2 | G-62-01, STATE-03, PROBE-02, TEST-01 | T-62.1-04 | key re-SET ≥3× w/ fresh timestamp + reset TTL (25,30] + Unhealthy/interval=10; L1 advances | unit (hermetic) | `…BaseApi.Tests.exe --filter-class "*ClashRefreshFacts"` | ✅ | ✅ green |
| 62.1-02-02 | 02 | 2 | G-62-01, PROBE-02, TEST-01 | T-62.1-05 | watchdog reads refreshed-Unhealthy entry as Healthy "live"; clean shutdown on cancel | unit (hermetic) | `…BaseApi.Tests.exe --filter-class "*ClashRefreshFacts"` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

`ClashRefreshFacts` facts: A (re-SET each interval), B (TTL reset band), C (L1 advances),
D (watchdog verdict unchanged — "live" + config=Fail in Data), E (clean shutdown — non-faulted task).
Last run: **Passed! — failed: 0, succeeded: 3** (against live Redis fixture). Regression: Processor
namespace 110/110, Liveness namespace 0 failed.

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.* The xUnit v3 / MTP harness, `RedisFixture`,
in-memory MassTransit, `FakeTimeProvider`, and the `IdentityResolutionFacts` clash-driving stubs all
pre-existed; Plan 02 added `ClashRefreshFacts` on top of them. No framework install needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live durably-broken replica observable as Unhealthy never absent (TEST-01b) | TEST-01 (live half) | Operator-gated live multi-container Docker close-gate run (D-04 / D-15) — cannot be hermetically automated beyond `ClashRefreshFacts`; requires the rebuilt v7 stack with `--profile badconfig` + two `processor-sample` replicas | Follow `62-HUMAN-UAT.md` Steps 1–5: clean host build → `--profile badconfig up` → `pwsh scripts/phase-62-close.ps1` → confirm `GET skp:proc:{badId}:{instanceId}` stays `status=Unhealthy` (never `(nil)`) across >1 interval |
| Live dead-replica TTL-expiry + lazy SREM (TEST-01c) | TEST-01 (live half) | Same — requires `docker stop` of a live replica + >30s TTL window | `62-HUMAN-UAT.md` TEST-01c: stop a healthy replica, wait >30s, assert per-instance GET→`(nil)` and SMEMBERS shrinks after a validator read |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify (build gate + ClashRefreshFacts) or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — existing infra suffices)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (quick) / minutes (full)
- [x] `nyquist_compliant: true` set in frontmatter (automatable scope fully covered; live re-proof is operator-gated manual-only by design — D-04)

**Approval:** approved 2026-06-14

---

## Validation Audit 2026-06-14

| Metric | Count |
|--------|-------|
| Requirements audited | 5 (G-62-01, STATE-03, PROBE-02, LOOP-02, TEST-01) |
| COVERED (automated) | 5/5 automatable requirements |
| Manual-only (by design) | 1 (TEST-01 live half — TEST-01b/01c, operator-gated) |
| Gaps found (fillable) | 0 |
| Tests generated | 0 (hermetic coverage already delivered by Plan 02 `ClashRefreshFacts`) |
