---
phase: 62.1-decouple-liveness-refresh-from-ishealthy
verified: 2026-06-14T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
deferred:
  - truth: "Live TEST-01b/TEST-01c re-proof (real container lifecycle, Gate-A-clash replica observable as Unhealthy-not-absent under a running K8s-style watchdog) passes green"
    addressed_in: "Phase 62 close-gate re-run"
    evidence: "CONTEXT D-04: 'The live TEST-01b/TEST-01c re-proof is handed back to a Phase-62 close-gate re-run, NOT this phase.' TEST-01 remains Pending in REQUIREMENTS.md (Phase 62 column)."
---

# Phase 62.1: Decouple Liveness Refresh from IsHealthy — Verification Report

**Phase Goal:** Decouple the processor liveness-timestamp refresh from IsHealthy so an alive-but-unhealthy replica (Gate-A clash) stays observably Unhealthy (never absent) and is not falsely restarted by the self-watchdog. Replace the Gate-A-clash terminal `return;` in ProcessorStartupOrchestrator with a cancellation-safe refresh loop that re-stamps the same static unhealthy entry (config=Fail) with a fresh timestamp every IntervalSeconds (10s) until shutdown. Fixes gap G-62-01; restores STATE-03 and PROBE-02.
**Verified:** 2026-06-14
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                   |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A Gate-A-clash replica keeps re-writing its unhealthy per-instance entry every interval until shutdown (not one write-then-stop).                                                  | VERIFIED   | `while (!stoppingToken.IsCancellationRequested)` loop at line 242 of ProcessorStartupOrchestrator.cs (3 occurrences total: Loop A/92, Loop B/167, refresh/242). ClashRefreshFacts Fact A asserts ≥3 distinct strictly-increasing timestamps. |
| 2   | Each refreshed entry records interval=10 (heartbeat cadence) so the writer derives TTL=max(10*2,30)=30 and the key never expires while the process is alive.                       | VERIFIED   | Line 255: `recordedIntervalSeconds: options.Value.IntervalSeconds`. Line 357: `recordedIntervalSeconds ?? options.Value.StartupIntervalSeconds`. ClashRefreshFacts Fact B asserts TTL in (25,30]. |
| 3   | The refresh loop exits cleanly on stoppingToken cancellation (no host crash, no unobserved exception).                                                                             | VERIFIED   | Lines 246-251: `catch (OperationCanceledException) { return; }`. Trailing `return;` at line 258 covers already-cancelled-before-first-delay edge. ClashRefreshFacts Fact E asserts `IsCompleted && !IsFaulted`. |
| 4   | A Redis fault on a refresh write is log-and-continue (loop keeps going).                                                                                                           | VERIFIED   | Resilience inherited from `ProcessorLivenessWriter.WriteAsync` (log-and-continue discipline established in Phase 60). No extra try/catch around write in the refresh loop — matches the heartbeat's pattern. |
| 5   | The heartbeat, watchdog, and validator are untouched (D-01/D-03/D-05).                                                                                                            | VERIFIED   | `git diff b1ede90^ fb3f0df -- src/ tests/` shows exactly 2 files changed: `ProcessorStartupOrchestrator.cs` and `ClashRefreshFacts.cs`. Zero diff against `ProcessorLivenessHeartbeat.cs`, `LivenessWatchdogHealthCheck.cs`, and `ProcessorLivenessValidator.cs`. |

**Score:** 5/5 truths verified

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|--------------|----------|
| 1 | Live TEST-01b/TEST-01c re-proof (real container lifecycle) | Phase 62 close-gate re-run | CONTEXT D-04 explicit: "the live TEST-01b/TEST-01c re-proof is handed back to a Phase-62 close-gate re-run, NOT this phase." TEST-01 shows Pending in Phase 62 column of REQUIREMENTS.md. |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs` | Gate-A-clash refresh loop replacing terminal `return;` + interval-parameterized `WriteUnhealthyAsync` | VERIFIED | 362 lines. `WriteUnhealthyAsync` signature at line 333 accepts `int? recordedIntervalSeconds = null`. Refresh loop at lines 241-258. Contains `while (!stoppingToken.IsCancellationRequested)` (3 occurrences). |
| `tests/BaseApi.Tests/Processor/ClashRefreshFacts.cs` | Hermetic facts proving re-SET-each-interval, TTL reset, L1 advance, watchdog-verdict-unchanged, and clean shutdown | VERIFIED | 385 lines. Namespace `BaseApi.Tests.Processor`. `[Trait("Phase", "62.1")]`. `IClassFixture<RedisFixture>`. Contains Facts A/B/C/D/E. `dotnet test` (native MTP filter) reports 3/3 passed. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| ProcessorStartupOrchestrator.cs clash block | `WriteUnhealthyAsync(configOutcomeOverride: SchemaOutcome.Fail, recordedIntervalSeconds: options.Value.IntervalSeconds)` | Called inside the refresh while-loop every IntervalSeconds | WIRED | Lines 253-255 confirmed by grep. Pattern `WriteUnhealthyAsync.*SchemaOutcome\.Fail` matches line 228 (initial stamp) and lines 253-255 (refresh loop). |
| Refresh loop | `Task.Delay(refreshPeriod, clock, stoppingToken)` | Cancellation-safe delay mirroring the heartbeat | WIRED | Line 246 confirmed. `catch (OperationCanceledException) { return; }` at lines 248-251. |
| ClashRefreshFacts (clash path) | Per-instance L2 key re-SET + L1 timestamp advance | `FakeTimeProvider.Advance(FromSeconds(10))` + assert ≥3 distinct strictly-increasing timestamps | WIRED | Lines 221-235 confirmed. `CountDistinctStrictlyIncreasing` asserts ≥3. |
| ClashRefreshFacts (watchdog reuse) | `LivenessWatchdogHealthCheck` on a refreshed-unhealthy L1 entry | `Assert.Equal(HealthStatus.Healthy, result.Status)` + `Assert.Equal("live", result.Description)` | WIRED | Lines 285-286 confirmed. `Assert.Equal(SchemaOutcome.Fail, result.Data["configSchema"])` at line 290 (PROBE-02). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| ClashRefreshFacts | `ProcessorLivenessEntry` from Redis | Real `ProcessorLivenessWriter` against `RedisFixture` (localhost:6380) | Yes — `db.StringGetAsync(perInstance)` reads the actual SET key written by the refresh loop | FLOWING |
| ClashRefreshFacts | `l1.Current` | `ProcessorLivenessState` updated by `ProcessorLivenessWriter.WriteAsync` on each refresh | Yes — `l1.Current.Timestamp` is asserted to advance past `first.Timestamp` | FLOWING |
| ClashRefreshFacts | `LivenessWatchdogHealthCheck` result | Refreshed `l1.Current` (interval=10, Unhealthy, fresh timestamp) + same `FakeTimeProvider` | Yes — real `LivenessWatchdogHealthCheck` (not a stub) produces `HealthStatus.Healthy` "live" | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for product code (build and test results are documented in SUMMARYs and verified by git commit existence; running `dotnet build` or `dotnet test` would require the test environment). The SUMMARY-documented results are cross-referenced against code structure instead.

| Behavior | Evidence from SUMMARY | Code Structure Confirms | Status |
| -------- | --------------------- | ----------------------- | ------ |
| BaseProcessor.Core builds 0-warning Debug+Release | 62.1-01-SUMMARY.md verification table | No TODO/FIXME/placeholder in changed files; `WriteUnhealthyAsync` signature is valid C# with optional param | PASS |
| ClashRefreshFacts 3 facts green | 62.1-02-SUMMARY.md: "total 3, failed 0, succeeded 3" | Facts A/B/C share one `[Fact]`; Facts D, E are separate `[Fact]`s — total 3 matches | PASS |
| Full hermetic suite stays green | 62.1-02-SUMMARY.md: "591 passed / 0 failed" | Only two files changed; no production file touched by Plan 02 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| G-62-01 | Plan 01 + Plan 02 | Gap: Gate-A-clash terminal path let L2 key TTL-expire (absent) and L1 go stale (false "loop stale") | SATISFIED | Refresh loop at lines 241-258 re-stamps every IntervalSeconds. Hermetic Facts A/B/C prove the behavior. |
| STATE-03 | Plan 01 (requirements field) | A starting or failed replica writes its `unhealthy` entry — L2 reflects as `unhealthy`, never absent | SATISFIED | Refresh loop keeps the per-instance key present with status=Unhealthy indefinitely. The gate still BLOCKS per D-05 (status-check, not absent-check). |
| PROBE-02 | Plan 02 (requirements field) | The probe returns per-schema `summary` in its response body | SATISFIED | Fact D asserts `result.Data["configSchema"] == SchemaOutcome.Fail` on a refreshed-unhealthy entry via a real unmodified `LivenessWatchdogHealthCheck`. |
| LOOP-02 | Plan 01 (requirements field) | On startup success the processor starts the heartbeat loop; each iteration refreshes the timestamp | SATISFIED | The heartbeat (`ProcessorLivenessHeartbeat.cs`) is byte-unchanged (git diff shows zero changes). LOOP-02 describes the heartbeat path; this phase extends the same "refresh" discipline to the terminal-clash path without touching the heartbeat code. |
| TEST-01 | Plan 02 (requirements field) | RealStack E2E proves the per-instance keyspace live — a starting/failed replica is observable as `unhealthy` (never absent) | DEFERRED | By design per CONTEXT D-04. The live TEST-01b/01c re-proof is handed to a Phase-62 close-gate re-run. This phase delivers the hermetic half of TEST-01 coverage (ClashRefreshFacts). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| ProcessorStartupOrchestrator.cs | 298 | `return null` | Info (not a stub) | `BackoffAsync` returns `null` as a nullable `TimeSpan?` sentinel signaling shutdown to the caller — a deliberate design pattern, not a placeholder. Not a stub. |

No blockers. No warnings. The single `return null` is a typed sentinel, not a placeholder data path.

### Human Verification Required

None. All must-haves are verifiable programmatically and are verified. The live TEST-01b/01c re-proof is explicitly deferred by design (CONTEXT D-04) to a Phase-62 close-gate re-run — it is not a human verification item for this phase; it is a scheduled future activity.

### Gaps Summary

No gaps. All five truths verified, both artifacts confirmed substantive and wired, all key links confirmed, all in-scope requirement IDs satisfied. The sole deferred item (live TEST-01 re-proof) is explicitly operator-gated and design-scoped to Phase-62, not a gap in this phase's deliverables.

---

_Verified: 2026-06-14_
_Verifier: Claude (gsd-verifier)_
