---
phase: 60-dual-loop-writer-in-memory-l1-liveness-record
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs
  - tests/BaseApi.Tests/Processor/ProcessorLivenessWriterFacts.cs
autonomous: true
requirements: [LOOP-03, LOOP-04]
nyquist_compliant: true

must_haves:
  truths:
    - "A single shared internal writer SETs the per-instance L2 key with a derived TTL, SADDs instanceId to the index, and Updates the L1 holder from the SAME immutable entry"
    - "The written TTL = max(entry.Interval * 2, Ttl-floor): startup entry (interval 30) → 60s, heartbeat entry (interval 10) → 30s"
    - "The index SADD is idempotent — a second WriteAsync keeps the index member count at 1"
    - "A Redis fault is caught and logged; WriteAsync never throws, and L1 is updated regardless"
  artifacts:
    - path: "src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs"
      provides: "WriteAsync(processorId, instanceId, entry): L2 SET(perInstance, ttl) + index SADD + L1 Update + log-and-continue"
      contains: "WriteAsync"
    - path: "tests/BaseApi.Tests/Processor/ProcessorLivenessWriterFacts.cs"
      provides: "RedisFixture facts: PerInstance SET + TTL banding + SADD idempotency + L1==L2 + resilience"
      contains: "ProcessorLivenessWriterFacts"
  key_links:
    - from: "ProcessorLivenessWriter.WriteAsync"
      to: "L2 per-instance key"
      via: "StringSetAsync(L2ProjectionKeys.PerInstance(id, instId), json, expiry: ttl)"
      pattern: "StringSetAsync\\(\\s*L2ProjectionKeys.PerInstance"
    - from: "ProcessorLivenessWriter.WriteAsync"
      to: "instance-index SET"
      via: "SetAddAsync(L2ProjectionKeys.InstanceIndex(id), instanceId)"
      pattern: "SetAddAsync\\(\\s*L2ProjectionKeys.InstanceIndex"
    - from: "ProcessorLivenessWriter.WriteAsync"
      to: "L1 holder"
      via: "_l1.Update(entry) unconditionally (Open Q3 resolved: update even on Redis fault)"
      pattern: "_l1.Update\\(entry\\)"
---

<objective>
Build the single shared internal writer both loops call (RESEARCH primary recommendation + CONTEXT Specifics) so the L2-SET + index-SADD + L1-Update + derived-TTL disciplines cannot drift between the startup and heartbeat loops. Resolve Open Q3: the L1 holder is Updated UNCONDITIONALLY (before/independent of the Redis attempt) — the in-process watchdog wants the latest in-process truth, not Redis reachability.

Purpose: One write path = provably identical L1/L2 values (D-09) and one place the TTL math (D-13) + SADD (D-15) + resilience live.
Output: New `ProcessorLivenessWriter`; new RedisFixture fact file covering TTL banding, SADD idempotency, L1==L2, and resilience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-CONTEXT.md
@.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-PATTERNS.md
@.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-RESEARCH.md

<interfaces>
From Plan 01 (this phase):
  - interface IProcessorLivenessState { void Update(ProcessorLivenessEntry entry); ProcessorLivenessEntry? Current { get; } }
  - ProcessorLivenessOptions.StartupIntervalSeconds (=30), .IntervalSeconds (=10), .TtlSeconds (=30 floor)
From src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:
  - string PerInstance(Guid processorId, string instanceId)   // key: skp:proc:{id}:{instanceId}
  - string InstanceIndex(Guid processorId)                     // SET key: skp:proc:{id}
From src/Messaging.Contracts/Projections/ProcessorLivenessEntry.cs:
  - record with int Interval (the recorded active interval, already baked by Create)
  - static Create(string inputOutcome, string outputOutcome, string configOutcome, DateTime timestamp, int interval)
From src/Messaging.Contracts/Projections/{LivenessStatus,SchemaOutcome}.cs: status/outcome consts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProcessorLivenessWriter (shared L2 SET + index SADD + L1 Update + log-and-continue)</name>
  <read_first>
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs (write+catch block lines ~41-59 ctor/null-guards and ~84-110 the StringSetAsync + catch — lift these disciplines: _clock not needed here since the entry carries timestamp; default JsonSerializer options; no stoppingToken into the write; log-and-continue)
    - src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs (the SetAddAsync precedent at line ~84, and the ctor null-guard pattern ~44-54)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (PerInstance/InstanceIndex signatures — never a literal)
    - src/Messaging.Contracts/Projections/ProcessorLivenessEntry.cs (entry.Interval is the active recorded interval)
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessState.cs and IProcessorLivenessState.cs (Plan 01 — injected L1 holder)
    - src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs (Plan 01 — TtlSeconds floor)
  </read_first>
  <behavior>
    (asserted in Task 2 ProcessorLivenessWriterFacts)
    - WriteAsync with a startup entry (interval 30) → per-instance key TTL in (55, 60] and Status==Unhealthy
    - WriteAsync with a heartbeat entry (interval 10) → per-instance key TTL in (25, 30] and Status==Healthy
    - SMEMBERS InstanceIndex(id) contains instanceId; a second WriteAsync keeps member count == 1
    - After WriteAsync, IProcessorLivenessState.Current is the SAME entry written to L2
    - WriteAsync against a dead/stubbed Redis does NOT throw and still Updates L1
  </behavior>
  <action>
    Create `src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs` as `public sealed class` (so AddBaseProcessorFacts can assert its descriptor and DI resolves it without InternalsVisibleTo), namespace `BaseProcessor.Core.Liveness`.

    Ctor injects `IConnectionMultiplexer redis`, `IProcessorLivenessState l1`, `IOptions<ProcessorLivenessOptions> options`, `ILogger<ProcessorLivenessWriter> logger`, each `?? throw new ArgumentNullException(nameof(...))`; store `_options = options.Value`.

    Implement:
    ```csharp
    public async Task WriteAsync(Guid processorId, string instanceId, ProcessorLivenessEntry entry)
    {
        // D-09 / Open Q3 RESOLVED: update L1 UNCONDITIONALLY — the watchdog wants latest in-process truth,
        // not Redis reachability. The L1 record IS the same immutable entry written to L2 this iteration.
        _l1.Update(entry);
        try
        {
            var db = _redis.GetDatabase();
            var json = JsonSerializer.Serialize(entry);                  // DEFAULT options — the [JsonPropertyName] pins carry the wire shape (Pitfall 2: do NOT reshape)
            var ttl = Math.Max(entry.Interval * 2, _options.TtlSeconds);  // D-13: max(activeInterval*2, Ttl-floor); entry.Interval is the recorded active interval

            // Blind whole-value last-write-wins SET..EX — NO RMW, NO stoppingToken threaded in (command timeout is the bound). Key via L2ProjectionKeys, never a literal.
            await db.StringSetAsync(
                L2ProjectionKeys.PerInstance(processorId, instanceId),
                json,
                expiry: TimeSpan.FromSeconds(ttl));

            // Idempotent SADD (D-15) — set semantics make a double-add by both loops harmless; no "have I added?" flag.
            // instanceId is already a string (InstanceId.Resolve()) — no ToString.
            await db.SetAddAsync(L2ProjectionKeys.InstanceIndex(processorId), instanceId);
        }
        catch (Exception ex)
        {
            // Resilience (D-11 / heartbeat T-26-10): log-and-CONTINUE; never throw, never crash the host / abort resolution.
            _logger.LogWarning(ex, "Liveness write failed for processor {ProcessorId}; will retry next iteration", processorId);
        }
    }
    ```

    Use `LivenessStatus.*` / `SchemaOutcome.*` / `L2ProjectionKeys.*` consts only — NEVER a literal key/status string. Do NOT branch on "which loop am I" — the TTL derives from `entry.Interval` which the caller already baked via `Create(...)`. Do NOT register it in DI here (Plan 04 owns the DI file).
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj -warnaserror</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Math.Max(entry.Interval \* 2, _options.TtlSeconds)" src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs` returns 1
    - `grep -c "L2ProjectionKeys.PerInstance" src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs` returns 1
    - `grep -c "SetAddAsync(L2ProjectionKeys.InstanceIndex" src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs` returns 1
    - `grep -c "_l1.Update(entry)" src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs` returns 1
    - `grep -c "catch (Exception" src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs` returns 1 (log-and-continue; no rethrow)
    - `dotnet build ... -warnaserror` exits 0
  </acceptance_criteria>
  <done>Shared writer compiles 0-warning; SET+SADD+L1 single path; TTL = max(interval*2, Ttl); log-and-continue.</done>
</task>

<task type="auto">
  <name>Task 2: ProcessorLivenessWriterFacts (RedisFixture): TTL banding, SADD idempotency, L1==L2, resilience</name>
  <read_first>
    - tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs (the RedisFixture analog: IClassFixture&lt;RedisFixture&gt;, FakeTimeProvider, _redis.Track(...), KeyTimeToLiveAsync + Assert.InRange TTL banding at lines ~99-108, StringGetAsync + JsonSerializer.Deserialize value assertion)
    - tests/BaseApi.Tests/Composition/RedisFixture.cs (net-zero Track/SREM discipline — no skp:* wildcard SCAN; the index SET is the shared contention point)
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs (Task 1 — the class under test)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (PerInstance/InstanceIndex)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Processor/ProcessorLivenessWriterFacts.cs`, class tagged `[Trait("Phase","60")]`, `IClassFixture<RedisFixture>`. If it touches the shared instance-index SET, place it in the same non-parallel collection the existing index-touching facts use (mirror the Phase-22 parent-index discipline; check RedisFixture/LivenessHeartbeatFacts for the collection attribute).

    Construct the writer over `_redis.Multiplexer` + a real `new ProcessorLivenessState()` + `Options.Create(new ProcessorLivenessOptions())` + `NullLogger<ProcessorLivenessWriter>.Instance`.

    Facts:
    1. Startup entry: `var entry = ProcessorLivenessEntry.Create(SchemaOutcome.Fail, SchemaOutcome.Success, SchemaOutcome.Success, DateTime.UtcNow, interval: 30);` → `await writer.WriteAsync(id, "pod-x", entry);` then GET `L2ProjectionKeys.PerInstance(id, "pod-x")`, deserialize `ProcessorLivenessEntry`, assert `Status == LivenessStatus.Unhealthy` and `Interval == 30`; `KeyTimeToLiveAsync` in `Assert.InRange(remaining.TotalSeconds, 55, 60)` (max(60,30)=60).
    2. Heartbeat entry: `Create(SchemaOutcome.Success, SchemaOutcome.Success, SchemaOutcome.Success, DateTime.UtcNow, interval: 10)` → TTL band `Assert.InRange(.., 25, 30)` (max(20,30)=30), `Status == LivenessStatus.Healthy`.
    3. SADD idempotency: after first WriteAsync, `SMEMBERS InstanceIndex(id)` contains `"pod-x"`; a SECOND WriteAsync → `SetLengthAsync(InstanceIndex(id))` still == 1 (D-15).
    4. L1==L2 (D-09): the `ProcessorLivenessState` instance's `.Current` is the SAME entry passed to WriteAsync (`Assert.Same`).
    5. Resilience: construct a writer over a stubbed `IConnectionMultiplexer` (NSubstitute) whose `GetDatabase()` throws (or points at a dead port); `await writer.WriteAsync(...)` does NOT throw, and `.Current` is still updated.

    Net-zero: `_redis.Track(L2ProjectionKeys.PerInstance(id, "pod-x"))` AND `SREM`/track the `instanceId` member of `InstanceIndex(id)` after the test. Assert statuses via `LivenessStatus.*` consts, never literals.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~ProcessorLivenessWriterFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Assert.InRange" tests/BaseApi.Tests/Processor/ProcessorLivenessWriterFacts.cs` returns >= 2 (startup 60 + heartbeat 30 banding)
    - `grep -c "SetLengthAsync\|SetMembersAsync\|SMEMBERS" tests/BaseApi.Tests/Processor/ProcessorLivenessWriterFacts.cs` returns >= 1 (SADD idempotency check)
    - `grep -c "Assert.Same" tests/BaseApi.Tests/Processor/ProcessorLivenessWriterFacts.cs` returns >= 1 (L1==L2)
    - `grep -c "_redis.Track\|\.Track(" tests/BaseApi.Tests/Processor/ProcessorLivenessWriterFacts.cs` returns >= 1 (net-zero)
    - `dotnet test ... --filter "FullyQualifiedName~ProcessorLivenessWriterFacts"` exits 0 (with localhost:6380 compose up)
  </acceptance_criteria>
  <done>Writer facts green: startup TTL→60, heartbeat TTL→30, recorded intervals match, SADD idempotent (count 1), L1==L2, dead-Redis logs-and-continues; net-zero tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor → Redis (L2) | the writer crosses into the shared Redis keyspace (per-instance key + index SET) |
| Redis fault → host | a Redis failure must not propagate up and crash the host or abort resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Spoofing | per-instance liveness key identity | mitigate | Key built only via `L2ProjectionKeys.PerInstance(id, instanceId)` (SoT builder, golden-pinned in Phase 59) — never a literal; `instanceId` from the Phase-59 `InstanceId.Resolve()` SoT. No path to write under another replica's key by construction. |
| T-60-05 | Tampering / cross-instance collision | instance-index SET | mitigate | Each replica `SADD`s only its OWN `instanceId` (idempotent set semantics); a double-add is harmless (D-15). No replica can clobber another's per-instance key (distinct key segment). |
| T-60-06 | Denial of service | Redis fault crashing the host or aborting resolution (false-absent) | mitigate | `try/catch → LogWarning → continue`; WriteAsync never throws (verified by the resilience fact). TTL = `max(interval*2, Ttl)` so a live key outlives the inter-write gap and never lapses → STATE-03 "never absent" preserved even under a slow loop. |
| T-60-07 | Information disclosure | serialized entry on the wire | accept | Entry carries schema-resolution status/summary, not secrets; same wire shape Phase 59 pinned. No new exposure. |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~ProcessorLivenessWriterFacts"` green (compose up on localhost:6380).
- 0-warning build (Release AND Debug, -warnaserror) of BaseProcessor.Core.
</verification>

<success_criteria>
- ProcessorLivenessWriter exists: one shared path doing L2 SET(perInstance, derived TTL) + index SADD + L1 Update, log-and-continue.
- TTL = max(entry.Interval*2, Ttl-floor): startup→60, heartbeat→30.
- SADD idempotent (index member count stays 1); L1 holder == the entry written to L2 (D-09); dead-Redis does not throw.
- ProcessorLivenessWriterFacts green with net-zero key tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-02-SUMMARY.md`
</output>
