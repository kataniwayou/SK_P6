---
phase: 60-dual-loop-writer-in-memory-l1-liveness-record
plan: 03
type: execute
wave: 3
depends_on: [60-01, 60-02]
files_modified:
  - src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs
  - tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs
autonomous: true
requirements: [LOOP-02, LOOP-04]
nyquist_compliant: true

must_haves:
  truths:
    - "The heartbeat, once IsHealthy, writes the NEW per-instance key with a frozen-healthy ProcessorLivenessEntry (all-SUCCESS → Healthy) and active interval = IntervalSeconds (10)"
    - "The heartbeat routes through the shared writer (Plan 02) so L1 Update + index SADD + TTL come for free and match the startup path"
    - "The old ProcessorProjection / L2ProjectionKeys.Processor(id) write is removed entirely (no dual-write)"
    - "Each healthy beat refreshes the timestamp (monotonic via FakeTimeProvider); TTL band (25,30]"
  artifacts:
    - path: "src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs"
      provides: "Post-Healthy beat that builds a frozen-healthy entry and calls the shared writer"
      contains: "_writer.WriteAsync"
    - path: "tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs"
      provides: "RedisFixture facts re-pointed to PerInstance + ProcessorLivenessEntry + L1 + SADD"
      contains: "PerInstance"
  key_links:
    - from: "ProcessorLivenessHeartbeat (IsHealthy gate)"
      to: "shared ProcessorLivenessWriter"
      via: "_writer.WriteAsync(id, _instanceId, frozenHealthyEntry)"
      pattern: "_writer.WriteAsync\\("
---

<objective>
Swap the heartbeat (D-14, LOOP-02): keep the `IsHealthy && Id is { } id` gate as the "I am the healthy writer" signal, but build a frozen-healthy `ProcessorLivenessEntry` (all-SUCCESS → `Healthy`, active interval = `IntervalSeconds`/10) and route it through the shared writer from Plan 02. Remove the old `ProcessorProjection` / `L2ProjectionKeys.Processor(id)` write ENTIRELY (D-05/Pitfall 5 — no dual-write). The L1 Update + index SADD + derived TTL come for free via the shared writer.

Purpose: The post-Healthy refresh now lands on the new per-instance contract + L1, identical disciplines to the startup writer.
Output: Modified `ProcessorLivenessHeartbeat`; re-pointed `LivenessHeartbeatFacts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-CONTEXT.md
@.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-PATTERNS.md
@.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-RESEARCH.md

<interfaces>
From Plan 02 (this phase):
  - public sealed class ProcessorLivenessWriter { Task WriteAsync(Guid processorId, string instanceId, ProcessorLivenessEntry entry); }
From src/Messaging.Contracts/Projections/ProcessorLivenessEntry.cs:
  - static Create(string inputOutcome, string outputOutcome, string configOutcome, DateTime timestamp, int interval)  // all-SUCCESS → Status=Healthy
From src/Messaging.Contracts/Projections/{LivenessStatus,SchemaOutcome}.cs: Healthy/Unhealthy, Success/Fail
From src/Messaging.Contracts/Identity/InstanceId.cs: string Resolve()  // the {instanceId} segment + SADD member, available from boot
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap the heartbeat write to PerInstance + frozen-healthy entry via the shared writer; remove old write</name>
  <read_first>
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs (self — the file being modified; KEEP gate line ~70, _clock read ~76, the no-stoppingToken comment ~91-96, catch ~102-110, Task.Delay loop tail ~113-120; SWAP the inside of the gate ~78-100 which today builds ProcessorProjection + L2ProjectionKeys.Processor(id) + flat Ttl)
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs (Plan 02 — the injected writer; WriteAsync signature)
    - src/Messaging.Contracts/Projections/ProcessorLivenessEntry.cs (Create factory)
    - src/Messaging.Contracts/Identity/InstanceId.cs (Resolve())
    - src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs (IntervalSeconds = 10 active interval)
  </read_first>
  <behavior>
    (asserted in Task 2 LivenessHeartbeatFacts)
    - Not-yet-Healthy: nothing written (gate unchanged); the PerInstance key is absent
    - Once IsHealthy: PerInstance(id, instanceId) key present with Status==Healthy, Interval==10, TTL in (25,30]
    - Each subsequent beat (FakeTimeProvider.Advance) refreshes the timestamp (advances) — frozen Healthy
    - Index SADD: SMEMBERS InstanceIndex(id) contains instanceId; L1 holder Current == the written entry
    - Old flat L2ProjectionKeys.Processor(id) key is NOT written
  </behavior>
  <action>
    In `ProcessorLivenessHeartbeat.cs`, inside the existing `if (_context.IsHealthy && _context.Id is { } id)` gate, REPLACE the body that today builds a `ProcessorProjection` and calls `StringSetAsync(L2ProjectionKeys.Processor(id), ...)` with:

    ```csharp
    var now = _clock.GetUtcNow().UtcDateTime; // SAME clock the reader uses
    // Frozen healthy (D-14): all outcomes SUCCESS => Create derives Healthy. Does NOT re-read context
    // definition props (WR-03). Active interval = heartbeat IntervalSeconds (D-12).
    var entry = ProcessorLivenessEntry.Create(
        inputOutcome:  SchemaOutcome.Success,
        outputOutcome: SchemaOutcome.Success,
        configOutcome: SchemaOutcome.Success,
        timestamp:     now,
        interval:      _options.IntervalSeconds);
    await _writer.WriteAsync(id, _instanceId, entry); // shared writer: SET(perInstance, ttl=max(20,30)=30) + SADD + L1 Update
    ```

    Grow the ctor: inject `ProcessorLivenessWriter writer` and the instanceId. The heartbeat is DI-resolved (`AddHostedService`) so only `LivenessHeartbeatFacts` (which `new`s it) needs updating — no DI signature change here. Resolve the instanceId via `InstanceId.Resolve()` (store `_instanceId` once at ctor) — it is available from boot.

    REMOVE the old write ENTIRELY (D-05/Pitfall 5): delete the `ProcessorProjection` construction and the `L2ProjectionKeys.Processor(id)` `StringSetAsync`. Do NOT dual-write. Drop any now-unused fields (`_redis`, the old options used only by the removed write) to avoid CS0169/IDE0051 0-warning failures — the writer now owns Redis. Keep the existing `catch → LogWarning → continue` resilience (belt-and-braces; the writer also catches) and the `Task.Delay(period, _clock, stoppingToken)` loop tail unchanged.

    Status/key via consts only (`SchemaOutcome.Success`, never a literal). Confirm no remaining reference to `ProcessorProjection` or `L2ProjectionKeys.Processor(` in this file.
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core/BaseProcessor.Core.csproj -warnaserror</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_writer.WriteAsync(" src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs` returns >= 1
    - `grep -c "L2ProjectionKeys.Processor(" src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs` returns 0 (old key write removed)
    - `grep -c "ProcessorProjection" src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs` returns 0 (old value removed)
    - `grep -c "_options.IntervalSeconds" src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs` returns >= 1 (active interval = 10)
    - `dotnet build ... -warnaserror` exits 0 (no unused-field warnings)
  </acceptance_criteria>
  <done>Heartbeat writes the new per-instance frozen-healthy entry via the shared writer; old write gone; 0-warning build.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point LivenessHeartbeatFacts to PerInstance + ProcessorLivenessEntry + L1 + SADD</name>
  <read_first>
    - tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs (self — the two facts asserting the OLD contract: L2ProjectionKeys.Processor(testProcessorId) lines ~46-47, 73-74; Deserialize&lt;ProcessorProjection&gt; + projection.Liveness.Status ~106-108; the new ProcessorLivenessHeartbeat(...) ctor calls ~54-56, 87-89; the Options(...) helper ~32-39; TTL banding ~99-108)
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessHeartbeat.cs (Task 1 — the grown ctor)
    - src/BaseProcessor.Core/Liveness/ProcessorLivenessWriter.cs (Plan 02 — construct over _redis.Multiplexer for the fact)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (PerInstance/InstanceIndex)
  </read_first>
  <action>
    In `LivenessHeartbeatFacts.cs`:
    - Update the two `new ProcessorLivenessHeartbeat(...)` calls (~54-56, 87-89) for the grown ctor: pass a real `ProcessorLivenessWriter` constructed over `_redis.Multiplexer` + a real `new ProcessorLivenessState()` + the same `Options(...)` + `NullLogger<ProcessorLivenessWriter>.Instance`, and a resolved instanceId string (e.g. `"pod-hb"`).
    - SWAP the key under test from `L2ProjectionKeys.Processor(testProcessorId)` to `L2ProjectionKeys.PerInstance(testProcessorId, instanceId)` for both `_redis.Track` and the assertion.
    - SWAP `JsonSerializer.Deserialize<ProcessorProjection>` + `projection.Liveness.Status` to `JsonSerializer.Deserialize<ProcessorLivenessEntry>(raw!)` + `Assert.Equal(LivenessStatus.Healthy, entry!.Status)` and `Assert.Equal(10, entry.Interval)`.
    - TTL band stays `Assert.InRange(remaining.TotalSeconds, 25, 30)` (heartbeat max(20,30)=30, D-13).
    - ADD assertions: `SMEMBERS InstanceIndex(testProcessorId)` contains the instanceId (D-15); the `ProcessorLivenessState.Current` == the written entry (L1-01). Keep the not-yet-Healthy "writes nothing" case but check-absent the `PerInstance` key.
    - Net-zero: `_redis.Track(PerInstance(...))` AND `SREM`/track the index SET member. If the `Options(...)` helper now needs `StartupIntervalSeconds`, add `= 30` for completeness (the writer derives TTL from `entry.Interval`, so it is not strictly read).
    - Assert statuses via `LivenessStatus.*` consts, never literals.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~LivenessHeartbeatFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "L2ProjectionKeys.Processor(" tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs` returns 0 (re-pointed off the old key)
    - `grep -c "PerInstance" tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs` returns >= 1
    - `grep -c "Deserialize<ProcessorLivenessEntry>" tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs` returns >= 1
    - `grep -c "ProcessorProjection" tests/BaseApi.Tests/Processor/LivenessHeartbeatFacts.cs` returns 0
    - `dotnet test ... --filter "FullyQualifiedName~LivenessHeartbeatFacts"` exits 0 (compose up on localhost:6380)
  </acceptance_criteria>
  <done>Heartbeat facts assert the new PerInstance key + frozen-healthy ProcessorLivenessEntry + index SADD + L1; old-contract assertions gone; green + net-zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| heartbeat thread → Redis (L2) | post-Healthy refresh crosses into the shared keyspace via the shared writer |
| IsHealthy gate | only a Healthy replica writes the `healthy` value (the gate is the authorization signal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-08 | Spoofing / poisoning | writing `healthy` before resolution completes | mitigate | The `IsHealthy && Id is { } id` gate (unchanged, D-14) is the sole path to the `healthy` write; a not-yet-Healthy replica writes nothing healthy (verified by the not-yet-Healthy fact). |
| T-60-09 | Denial of service | TTL-lapse causing false-absent between beats | mitigate | TTL = max(10*2, 30) = 30 (via shared writer); heartbeat interval is 10 → key always outlives the inter-beat gap. |
| T-60-10 | Repudiation / desync | old `ProcessorProjection` key left dual-writing → split-brain / net-zero break | mitigate | Old write removed ENTIRELY (D-05); fact asserts `L2ProjectionKeys.Processor(` count 0 in both src and test. |
| T-60-11 | Tampering | stale context definition props read on the heartbeat thread (WR-03) | mitigate | Frozen-healthy is a fixed all-SUCCESS summary; the heartbeat does NOT re-read context definition props — no cross-thread stale read. |
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~LivenessHeartbeatFacts"` green (compose up).
- `dotnet test ... --filter "Phase=60"` green at wave merge.
- 0-warning build (Release AND Debug, -warnaserror).
</verification>

<success_criteria>
- Heartbeat writes the new per-instance key with a frozen-healthy ProcessorLivenessEntry (Status=Healthy, Interval=10) via the shared writer; L1 + index SADD + TTL=30 come for free.
- Old ProcessorProjection / L2ProjectionKeys.Processor(id) write removed (no dual-write).
- LivenessHeartbeatFacts re-pointed and green; net-zero tracked.
</success_criteria>

<output>
After completion, create `.planning/phases/60-dual-loop-writer-in-memory-l1-liveness-record/60-03-SUMMARY.md`
</output>
