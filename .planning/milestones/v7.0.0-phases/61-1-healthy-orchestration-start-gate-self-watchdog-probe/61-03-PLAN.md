---
phase: 61-1-healthy-orchestration-start-gate-self-watchdog-probe
plan: 03
type: execute
wave: 2
depends_on: [61-02]
autonomous: true
requirements: [PROBE-01, PROBE-02]
files_modified:
  - tests/BaseApi.Tests/Console/ProcessorHealthLiveTests.cs
  - tests/BaseApi.Tests/Console/ProcessorConsoleTestHostFixture.cs

must_haves:
  truths:
    - "A Generic-Host integration fixture composes AddBaseProcessor so the watchdog descriptor arrives transitively and lands on the embedded /health/live."
    - "GET /health/live flips Unhealthy (503) when the seeded IProcessorLivenessState is null or stale, and Healthy (200) when fresh."
    - "The /health/live body carries the per-schema summary (inputSchema, outputSchema, configSchema) when the L1 entry is present (PROBE-02)."
    - "The /health/live body leaks no instanceId, connection string, or stack trace (security)."
  artifacts:
    - path: "tests/BaseApi.Tests/Console/ProcessorHealthLiveTests.cs"
      provides: "Processor /health/live integration facts (PROBE-02)"
      contains: "[Trait(\"Phase\", \"61\")]"
    - path: "tests/BaseApi.Tests/Console/ProcessorConsoleTestHostFixture.cs"
      provides: "Generic-Host fixture composing AddBaseProcessor + seeded L1"
      contains: "AddBaseProcessor"
  key_links:
    - from: "ProcessorHealthLiveTests.cs"
      to: "/health/live embedded listener"
      via: "HttpClient.GetAsync(\"/health/live\") against the AddBaseProcessor host"
      pattern: "/health/live"
    - from: "ProcessorConsoleTestHostFixture.cs"
      to: "IProcessorLivenessState"
      via: "seed Current via the registered singleton before/after host start"
      pattern: "IProcessorLivenessState"
---

<objective>
Add the processor-scoped `/health/live` Generic-Host integration proof (PROBE-02, D-01/04): a fixture that composes `AddBaseProcessor` (so the Plan-02 watchdog descriptor arrives transitively and lands on the embedded `/health/live`), seeds the in-memory `IProcessorLivenessState`, and asserts the live endpoint flips Unhealthy on stale/null L1 and carries the per-schema `summary` in its body with no secret leak.

Purpose: Plan 02 proved the watchdog verdict in isolation; this proves the full wiring path end-to-end through the real embedded Kestrel listener — that `AddBaseProcessor` actually surfaces the watchdog on `/health/live` and the summary actually rides into the response body.
Output: A processor Generic-Host fixture + a Phase=61 integration test class over null/stale/fresh L1 + body-summary + no-secrets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-1-healthy-orchestration-start-gate-self-watchdog-probe/61-CONTEXT.md
@.planning/phases/61-1-healthy-orchestration-start-gate-self-watchdog-probe/61-RESEARCH.md
@.planning/phases/61-1-healthy-orchestration-start-gate-self-watchdog-probe/61-PATTERNS.md
@.planning/phases/61-1-healthy-orchestration-start-gate-self-watchdog-probe/61-02-SUMMARY.md

<interfaces>
<!-- The ConsoleTestHostFixture this plan subclasses (tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs). -->

```csharp
public class ConsoleTestHostFixture : IAsyncLifetime
{
    public IHost Host { get; private set; }
    public int HealthPort { get; }
    public HttpClient HttpClient { get; }           // points at http://127.0.0.1:{HealthPort}
    public IReadOnlyList<ServiceDescriptor> RegisteredDescriptors { get; }

    public ConsoleTestHostFixture();                // picks a free port, builds the host
    protected virtual void ConfigureBuilder(IHostApplicationBuilder builder);   // OVERRIDE this
    protected static Dictionary<string, string?> BuildConfig(int healthPort);   // dead Redis/RMQ + ConsoleHealth:Port
    public async ValueTask InitializeAsync();        // Host.StartAsync (starts the embedded listener)
    public async ValueTask DisposeAsync();
}
```
Base ConfigureBuilder composes: `AddBaseConsoleObservability`, `AddBaseConsole`, `AddBaseConsoleMessaging(cfg, x => {})`.

The watchdog descriptor is registered by `AddBaseProcessor` (Plan 02). `IProcessorLivenessState` is a Singleton resolvable from `Host.Services`; seed `Current` by resolving the singleton and calling `.Update(ProcessorLivenessEntry.Create(...))`.

ProcessorLivenessEntry.Create(inputOutcome, outputOutcome, configOutcome, timestamp, interval) — fresh: `(null,null,null, DateTime.UtcNow, 300)`; stale: `(null,null,null, DateTime.UtcNow.AddDays(-1), 0)`.

ConsoleHealthLiveTests no-secrets pattern: assert body DoesNotContain "Password=", "abortConnect", "   at ".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Processor Generic-Host fixture composing AddBaseProcessor</name>
  <read_first>
    - tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs (subclass it; override ConfigureBuilder :88-93; reuse BuildConfig/HealthPort/HttpClient/InitializeAsync as-is)
    - src/Processor.Sample/Program.cs (the canonical AddBaseProcessor composition — match its three-call + AddBaseProcessor shape; the processor needs its config-type/processor registration to build)
    - src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs (what AddBaseProcessor requires to build — e.g. a registered BaseProcessor<TConfig>/config type; mirror Processor.Sample)
    - 61-PATTERNS.md "NEW processor /health/live Generic-Host fixture" spec
    - .planning/phases/61-1-healthy-orchestration-start-gate-self-watchdog-probe/61-02-SUMMARY.md (confirm the watchdog descriptor + AddBaseProcessor registration shape Plan 02 landed)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Console/ProcessorConsoleTestHostFixture.cs`, namespace `BaseApi.Tests.Console`, subclassing `ConsoleTestHostFixture`. Override `ConfigureBuilder` to compose the base console seam PLUS `AddBaseProcessor` exactly as `Processor.Sample/Program.cs` does (read it first — copy the AddBaseProcessor call and any required processor/config-type registration so the host BUILDS). Keep the dead-Redis/dead-RMQ soft-dep config from the base `BuildConfig` (the watchdog never touches Redis, so dead deps are fine — D-01).

    Expose a helper to seed L1 after start:
    ```csharp
    public void SeedLiveness(DateTime timestamp, int interval)
        => Host.Services.GetRequiredService<IProcessorLivenessState>()
               .Update(ProcessorLivenessEntry.Create(null, null, null, timestamp, interval));
    ```
    (Leave `Current` unseeded to exercise the null verdict.) Add the necessary usings (`Microsoft.Extensions.DependencyInjection`, `BaseProcessor.Core.Liveness`, `Messaging.Contracts.Projections`).

    If `AddBaseProcessor` requires a concrete `BaseProcessor<TConfig>` subtype to be registered to build, define a minimal no-op test processor in this file (or reuse Processor.Sample's) — mirror whatever Processor.Sample registers. The fixture MUST start without throwing despite dead deps (the embedded listener answers /health/live regardless).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - File tests/BaseApi.Tests/Console/ProcessorConsoleTestHostFixture.cs exists and contains `AddBaseProcessor`
    - The fixture overrides `ConfigureBuilder` and contains a `SeedLiveness` helper resolving `IProcessorLivenessState`
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug` succeeds
  </acceptance_criteria>
  <done>A Generic-Host fixture composes AddBaseProcessor (watchdog arrives transitively) over a free-port embedded listener and can seed/leave-null the L1 holder.</done>
</task>

<task type="auto">
  <name>Task 2: /health/live integration facts — null/stale -> Unhealthy, fresh -> Healthy + summary in body + no secrets</name>
  <read_first>
    - tests/BaseApi.Tests/Console/ConsoleHealthLiveTests.cs (the GET /health/live + body-assert pattern + Live_Body_Has_No_Secrets to mirror)
    - tests/BaseApi.Tests/Console/ProcessorConsoleTestHostFixture.cs (the fixture from Task 1 — IClassFixture target; SeedLiveness helper)
    - 61-RESEARCH.md PROBE-02 test map row + Validation Architecture Wave 0 gaps
    - 61-VALIDATION.md (Phase=61 trait + quick-run filter)
  </read_first>
  <behavior>
    - Unseeded L1 (Current == null) -> GET /health/live returns 503 (ServiceUnavailable) and body status is Unhealthy.
    - Stale L1 seeded (now far past Timestamp + Interval*2) -> 503 / Unhealthy.
    - Fresh L1 seeded -> 200 / Healthy AND body contains inputSchema/outputSchema/configSchema.
    - Fresh L1 body contains no "Password=", "abortConnect", or "   at " stack frame.
  </behavior>
  <action>
    Because a stale/null verdict must NOT be polluted by a prior fresh seed (shared fixture state), use the appropriate xUnit isolation: prefer SEPARATE fixture instances per verdict (a non-shared fixture constructed per test, or three IClassFixture subclasses) so each test controls the L1 snapshot deterministically; OR seed BEFORE the first GET within each test and accept the shared listener (the watchdog re-resolves Current on every check, so re-seeding then GET-ing within one test is deterministic). Pick the per-test-deterministic approach: in each `[Fact]`, call `_fixture.SeedLiveness(...)` (or leave null for the null case by using a dedicated fixture that never seeds) immediately before the GET.

    Create `tests/BaseApi.Tests/Console/ProcessorHealthLiveTests.cs`, `[Trait("Phase", "61")]`, `IClassFixture<ProcessorConsoleTestHostFixture>`. Facts:
    1. `Live_Is_Unhealthy_When_L1_Null` — do NOT seed; GET /health/live; assert `HttpStatusCode.ServiceUnavailable` (503). (If fixture sharing makes null unreliable because another fact seeded, give the null case its own dedicated never-seeding fixture subclass.)
    2. `Live_Is_Unhealthy_When_L1_Stale` — `SeedLiveness(DateTime.UtcNow.AddDays(-1), 0)`; GET; assert 503 + body contains `"status":"Unhealthy"`.
    3. `Live_Is_Healthy_And_Carries_Summary_When_L1_Fresh` — `SeedLiveness(DateTime.UtcNow, 300)`; GET; assert `HttpStatusCode.OK` (200) AND body Contains `inputSchema`, `outputSchema`, `configSchema`.
    4. `Live_Body_Has_No_Secrets` — `SeedLiveness(DateTime.UtcNow, 300)`; GET; assert body DoesNotContain `Password=`, `abortConnect`, `   at ` (mirror ConsoleHealthLiveTests).

    Thread `TestContext.Current.CancellationToken` through the async GETs (xUnit1051). Use `_fixture.HttpClient.GetAsync("/health/live", ct)`.

    NOTE on the null case: the embedded /health/live aggregates the always-Healthy "self" check AND the watchdog; an Unhealthy watchdog makes the aggregate Unhealthy -> 503. Confirm the framework returns 503 for an Unhealthy aggregate (the default HealthCheckOptions maps Unhealthy -> 503). If a fresh seed leaks across the shared fixture into the null fact, isolate the null fact with its own non-seeding fixture subclass.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~ProcessorHealthLive" -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - File tests/BaseApi.Tests/Console/ProcessorHealthLiveTests.cs exists, contains `[Trait("Phase", "61")]` and `/health/live`
    - The class asserts ServiceUnavailable (503) for null AND stale L1, and OK (200) for fresh L1
    - The fresh fact asserts the body Contains `inputSchema`, `outputSchema`, `configSchema`
    - A no-secrets fact asserts the body DoesNotContain `Password=`, `abortConnect`, `   at `
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~ProcessorHealthLive" -c Debug` passes
  </acceptance_criteria>
  <done>The embedded /health/live flips Unhealthy on null/stale L1 and Healthy on fresh, carries the per-schema summary in its body, and leaks no secret — proving the full AddBaseProcessor -> descriptor -> listener wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedded /health/live listener -> probe consumer (future K8s) | The integration test exercises the real response body the K8s liveness probe would consume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Information Disclosure | /health/live response body | mitigate | The integration test asserts the live body carries only status + the three summary outcomes — no connection-string token (`abortConnect`/`Password=`) and no stack frame (`   at `), reusing the established ConsoleHealthLiveTests.Live_Body_Has_No_Secrets guard (T-18-08). |
| T-61-08 | Denial of Service | /health/live reflecting a dead-dep host | accept | The fixture boots with DEAD Redis/RMQ; /health/live still answers (D-01 — the watchdog reads only in-process L1). A dependency blip cannot flip liveness; only a genuinely null/stale L1 loop does. |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` AND `-c Debug` at 0 warnings.
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~ProcessorHealthLive" -c Debug` green.
- `dotnet test SK_P.sln --filter "Trait=Phase&Phase=61" -c Debug` green (full Phase=61 set including this plan).
</verification>

<success_criteria>
PROBE-01: /health/live flips Unhealthy when the seeded L1 is null or stale beyond interval*2. PROBE-02: the per-schema summary is present in the /health/live response body. The full AddBaseProcessor -> HealthCheckDescriptor -> embedded listener path is proven end-to-end; the body leaks no secret.
</success_criteria>

<output>
After completion, create `.planning/phases/61-1-healthy-orchestration-start-gate-self-watchdog-probe/61-03-SUMMARY.md`
</output>
