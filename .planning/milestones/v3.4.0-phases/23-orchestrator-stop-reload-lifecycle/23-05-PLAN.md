---
phase: 23-orchestrator-stop-reload-lifecycle
plan: 05
type: execute
wave: 4
depends_on: [23-04]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/CapturingDispatchConsumer.cs
  - tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs
  - tests/BaseApi.Tests/Orchestrator/StartConsumerLifecycleTests.cs
  - tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs
  - tests/BaseApi.Tests/Orchestrator/AckSemanticsTests.cs
  - tests/BaseApi.Tests/Orchestrator/NoGlobalLockTests.cs
autonomous: false
requirements: [ORCH-FIRE-01, ORCH-CONSUME-01, ORCH-STOP-01, ORCH-SCALE-01, ORCH-ACK-01]
must_haves:
  truths:
    - "A synthetic consumer on queue:{processorId} receives one EntryStepDispatch per entry step per fire with correct fields; correlationId differs across two consecutive fires; transport is Send"
    - "StartOrchestration([wfX]) hydrates only wfX into L1 and schedules only wfX; the synthetic consumer receives wfX entry-step messages on fire"
    - "StopOrchestration([wfX]) resolves wfX jobId from L1, deletes the Quartz job, clears L1 entries, and leaves an L2 snapshot byte-identical before/after (zero orchestrator L2 writes)"
    - "Absent-workflow start/stop acks with no _error; Redis-unreachable consume propagates; startup with one corrupt entry hydrates the rest and stays up"
    - "No static/global singleton lock or process-uniqueness assumption gates the lifecycle"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/CapturingDispatchConsumer.cs"
      provides: "Synthetic in-memory consumer of EntryStepDispatch bound to ReceiveEndpoint({processorId:D})"
      contains: "IConsumer<EntryStepDispatch>"
    - path: "tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs"
      provides: "Fire -> dispatch + correlationId-differs + liveness-advance assertions"
      contains: "Consumed.Select<EntryStepDispatch>"
    - path: "tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs"
      provides: "Stop teardown + byte-identical L2 snapshot + zero-L2-write assertion"
      contains: "DidNotReceive()"
  key_links:
    - from: "tests/BaseApi.Tests/Orchestrator/CapturingDispatchConsumer.cs"
      to: "queue:{processorId} <-> ReceiveEndpoint({processorId:D})"
      via: "short-name endpoint binding on the in-memory harness"
      pattern: "ReceiveEndpoint\\(\\$\"\\{processorId"
    - from: "tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs"
      to: "zero-L2-write invariant"
      via: "DidNotReceive StringSetAsync/SetAddAsync/KeyDeleteAsync + byte-identical snapshot"
      pattern: "DidNotReceive\\(\\)\\.(StringSetAsync|SetAddAsync|KeyDeleteAsync)"
---

<objective>
Close the goal-backward verification gap with the harness + review tests that prove the live lifecycle from the USER's perspective: a synthetic MassTransit consumer on `queue:{processorId}` (the short-name endpoint a `Send` targets) asserting fire->dispatch field correctness + per-fire correlationId difference + L1 liveness advance (ORCH-FIRE-01); the Start consumer single-workflow hydrate+schedule+fire (ORCH-CONSUME-01); the Stop consumer jobId-addressed teardown with a byte-identical L2 snapshot before/after + extended `DidNotReceive` guards (ORCH-STOP-01); the absent/gated/Redis-unreachable/corrupt-entry ack semantics (ORCH-ACK-01); and the no-global-lock review guard (ORCH-SCALE-01).

Purpose: these are the SPEC acceptance-criteria proofs. Plan 03/04 built the behavior; this plan samples it at the observable signals named in 23-VALIDATION.md.
Output: 1 reusable synthetic consumer + 5 test files (FireDispatch, StartConsumerLifecycle, StopConsumerLifecycle, AckSemantics, NoGlobalLock). Tests only — no production-code change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-SPEC.md
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-RESEARCH.md
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-PATTERNS.md
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-VALIDATION.md
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-03-SUMMARY.md
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-04-SUMMARY.md

<interfaces>
<!-- Test analogs + the production types under test, extracted this session. -->

Synthetic harness + queue:<->ReceiveEndpoint mapping (RESEARCH "Synthetic test consumer", assumption A2):
```csharp
await using var provider = new ServiceCollection()
    .AddSingleton(muxStub)                                    // PresentL2/SetMembers stub
    .AddSingleton<IWorkflowL1Store, WorkflowL1Store>()
    .AddLogging()
    .AddMassTransitTestHarness(x =>
    {
        x.AddConsumer<CapturingDispatchConsumer>();
        x.UsingInMemory((ctx, cfg) =>
        {
            cfg.ReceiveEndpoint($"{processorId:D}", e => e.ConfigureConsumer<CapturingDispatchConsumer>(ctx));  // queue:{id} -> endpoint {id}
            cfg.ConfigureEndpoints(ctx);
        });
    })
    .BuildServiceProvider(true);
var harness = provider.GetRequiredService<ITestHarness>();
await harness.Start();
// ... fire ...
Assert.True(await harness.Consumed.Any<EntryStepDispatch>(ct));
var dispatched = harness.Consumed.Select<EntryStepDispatch>(ct).ToList();
```

Existing harness analog — tests/BaseApi.Tests/Orchestrator/OutboundFilterSyntheticTests.cs (AddMassTransitTestHarness + Published/Consumed.Select<T>).
Mux-stub helpers (reuse/extend) — tests/BaseApi.Tests/Orchestrator/StartStopConsumerAckTests.cs:
  AbsentL2(out IDatabase db)  -> StringGetAsync => RedisValue.Null
  PresentL2(out IDatabase db) -> StringGetAsync => serialized WorkflowRootProjection
  InfraFaultL2()              -> StringGetAsync => .Throws(new RedisConnectionException(...))
Existing zero-L2-write assertion to EXTEND — StartStopConsumerAckTests.cs:
```csharp
await db.DidNotReceive().StringSetAsync(Arg.Any<RedisKey>(), Arg.Any<RedisValue>(), Arg.Any<TimeSpan?>(), Arg.Any<When>(), Arg.Any<CommandFlags>());
// ADD: .DidNotReceive().SetAddAsync(...) and .DidNotReceive().KeyDeleteAsync(...) on any skp: key
```

Production types under test (from Plans 01/03/04):
  Messaging.Contracts.EntryStepDispatch (WorkflowId, StepId, ProcessorId, Payload, CorrelationId, ExecutionId=Empty, EntryId=Empty)
  Orchestrator.Scheduling.WorkflowFireJob (IJob — Execute fires the dispatch)
  Orchestrator.L1.IWorkflowL1Store / WorkflowL1Store (Count, TryGet, WorkflowIds)
  Orchestrator.Scheduling.WorkflowScheduler (over a Quartz IScheduler — GetJobKeys/CheckExists)
  Orchestrator.Hydration.WorkflowLifecycle (HydrateAndScheduleAsync / TeardownAsync)
  Orchestrator.Consumers.StartOrchestrationConsumer / StopOrchestrationConsumer
FakeTimeProvider (Microsoft.Extensions.TimeProvider.Testing) for the cron clock + liveness advance.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CapturingDispatchConsumer + FireDispatchTests (ORCH-FIRE-01)</name>
  <files>tests/BaseApi.Tests/Orchestrator/CapturingDispatchConsumer.cs, tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/OutboundFilterSyntheticTests.cs (AddMassTransitTestHarness + ReceiveEndpoint + Consumed.Select)
    - tests/BaseApi.Tests/Orchestrator/FanOutBroadcastTests.cs (harness lifecycle)
    - src/Orchestrator/Scheduling/WorkflowFireJob.cs (the IJob to drive — how Execute reads workflowId from job data + L1)
    - 23-RESEARCH.md "Synthetic test consumer" + Pattern 3
  </read_first>
  <behavior>
    - Test (FireDispatchTests.OneMessagePerEntryStep_CorrectFields): seed L1 (real WorkflowL1Store) with a workflow having 2 entry steps, each with a distinct processorId + payload. Bind a CapturingDispatchConsumer per processorId's short-name endpoint. Drive a fire (call WorkflowFireJob.Execute directly with a substituted IJobExecutionContext carrying workflowId). Assert harness.Consumed has one EntryStepDispatch per entry step; each message's StepId/ProcessorId/Payload match the seeded step; ExecutionId == Guid.Empty && EntryId == Guid.Empty.
    - Test (FireDispatchTests.CorrelationIdDiffersAcrossTwoFires): fire twice; the two captured CorrelationId values are non-empty and differ (per-fire NewId.NextGuid()).
    - Test (FireDispatchTests.LivenessTimestampAdvancesOnFire_NoL2Write): record L1 liveness timestamp before; fire; assert the L1 entry's liveness timestamp advanced to the FakeTimeProvider's now; assert the Redis db mux stub received NO StringSetAsync/SetAddAsync/KeyDeleteAsync (transport was Send to the in-memory queue, not an L2 write).
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/CapturingDispatchConsumer.cs` — `public sealed class CapturingDispatchConsumer : IConsumer<EntryStepDispatch>` whose `Consume` returns `Task.CompletedTask` (the harness `Consumed` filter does the capturing); reusable across FIRE + CONSUME tests.
    Create `tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs` per the behavior block. Build the harness binding a `ReceiveEndpoint($"{processorId:D}", ...)` per distinct entry-step processorId (RESEARCH "Synthetic test consumer" — the short-name `queue:{id}` maps to endpoint `{id}`; verify assumption A2 here). Use a `FakeTimeProvider`. Drive the fire by resolving `ISendEndpointProvider` from the harness, constructing a `WorkflowFireJob` with the harness's send provider + the seeded L1 store + a RAMJobStore-backed WorkflowScheduler + FakeTimeProvider, and calling `Execute` with a substituted `IJobExecutionContext` whose `MergedJobDataMap` holds `workflowId` (NSubstitute). Assert via `harness.Consumed.Select<EntryStepDispatch>(ct)`.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class BaseApi.Tests.Orchestrator.FireDispatchTests</automated>
  </verify>
  <acceptance_criteria>
    - tests/BaseApi.Tests/Orchestrator/CapturingDispatchConsumer.cs contains 'IConsumer<EntryStepDispatch>'
    - tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs contains 'ReceiveEndpoint($"{processorId' and 'Consumed.Select<EntryStepDispatch>'
    - tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs asserts 'Guid.Empty' (ExecutionId/EntryId) and a correlationId inequality across two fires
    - dotnet test ... --filter-class BaseApi.Tests.Orchestrator.FireDispatchTests exits 0
  </acceptance_criteria>
  <done>A synthetic consumer on queue:{processorId} receives one EntryStepDispatch per entry step with correct StepId/ProcessorId/Payload and Guid.Empty execution/entry ids; two fires produce differing correlationIds; the L1 liveness timestamp advances on fire with zero L2 writes.</done>
</task>

<task type="auto">
  <name>Task 2: StartConsumerLifecycleTests + StopConsumerLifecycleTests (ORCH-CONSUME-01, ORCH-STOP-01)</name>
  <files>tests/BaseApi.Tests/Orchestrator/StartConsumerLifecycleTests.cs, tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs</files>
  <read_first>
    - src/Orchestrator/Consumers/StartOrchestrationConsumer.cs + StopOrchestrationConsumer.cs (the gated lifecycle under test)
    - src/Orchestrator/Hydration/WorkflowLifecycle.cs (HydrateAndScheduleAsync / TeardownAsync)
    - tests/BaseApi.Tests/Orchestrator/StartStopConsumerAckTests.cs (mux-stub + DidNotReceive guard to mirror/extend)
    - 23-RESEARCH.md "Consumer gate-drop" + "Stop teardown" + Validation Architecture (byte-identical snapshot)
  </read_first>
  <behavior>
    - Test (StartConsumerLifecycleTests.StartHydratesOnlyConsumedWorkflow_AndSchedules): with the gate marked ready and a PresentL2 mux stub for wfX (root + steps), invoke StartOrchestrationConsumer.Consume(StartOrchestration([wfX])); assert the L1 store has exactly the wfX entry (Count==1, TryGet(wfX) true), and the WorkflowScheduler's underlying IScheduler has a job keyed by wfX's jobId.
    - Test (StartConsumerLifecycleTests.GateClosed_DropsStart_NoSchedule): with gate NOT ready, Consume acks (returns) and the store stays empty / scheduler has no job.
    - Test (StopConsumerLifecycleTests.StopDeletesJob_ClearsL1_ZeroL2Writes): seed L1 + schedule wfX (via WorkflowLifecycle.HydrateAndScheduleAsync against a PresentL2 stub); invoke StopOrchestrationConsumer.Consume(StopOrchestration([wfX])); assert the scheduler no longer has wfX's job (CheckExists false), the L1 store has no wfX entry, and the Redis db received NO StringSetAsync / SetAddAsync / KeyDeleteAsync on any skp: key (byte-identical L2 — zero orchestrator writes).
    - Test (StopConsumerLifecycleTests.GateClosed_DropsStop): gate not ready -> ack, no teardown.
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/StartConsumerLifecycleTests.cs` and `tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs` (namespace `BaseApi.Tests.Orchestrator`, xunit.v3, NSubstitute). Construct the consumers with a real `WorkflowL1Store`, a real `WorkflowScheduler` over a RAMJobStore `IScheduler` (`SchedulerBuilder.Create().Build()` or `StdSchedulerFactory`), a `WorkflowLifecycle` over the PresentL2 mux stub, a `StartupGate` (call `MarkReady()` for the ready cases), and `FakeTimeProvider`. For the scheduler job-presence assertion use `scheduler.CheckExists(new JobKey(jobId.ToString("D")))` / `GetJobKeys(GroupMatcher<JobKey>.AnyGroup())`. For the zero-L2 proof, EXTEND the `StartStopConsumerAckTests` `DidNotReceive` pattern to also cover `SetAddAsync` + `KeyDeleteAsync` (RESEARCH Observable signals). Reuse the `ConsumeContext<T>` substitution idiom from `StartStopConsumerAckTests`.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class BaseApi.Tests.Orchestrator.StartConsumerLifecycleTests; dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class BaseApi.Tests.Orchestrator.StopConsumerLifecycleTests</automated>
  </verify>
  <acceptance_criteria>
    - tests/BaseApi.Tests/Orchestrator/StartConsumerLifecycleTests.cs asserts store Count == 1 for the consumed workflow and a scheduled job for its jobId
    - tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs contains 'DidNotReceive().StringSetAsync' and 'DidNotReceive().SetAddAsync' and 'DidNotReceive().KeyDeleteAsync'
    - tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs asserts the job is gone (CheckExists false / GetJobKeys empty) and the L1 entry removed
    - dotnet test ... --filter-class BaseApi.Tests.Orchestrator.StartConsumerLifecycleTests exits 0
    - dotnet test ... --filter-class BaseApi.Tests.Orchestrator.StopConsumerLifecycleTests exits 0
  </acceptance_criteria>
  <done>Start hydrates+schedules only the consumed workflow (and gate-closed drops); Stop resolves the jobId from L1, deletes the Quartz job, clears L1, and the Redis mux receives zero write/delete calls on any skp: key (byte-identical L2).</done>
</task>

<task type="auto">
  <name>Task 3: AckSemanticsTests (extend) + NoGlobalLockTests (ORCH-ACK-01, ORCH-SCALE-01)</name>
  <files>tests/BaseApi.Tests/Orchestrator/AckSemanticsTests.cs, tests/BaseApi.Tests/Orchestrator/NoGlobalLockTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/StartStopConsumerAckTests.cs (Absent/Present/InfraFault stubs — the ack model to extend)
    - src/Orchestrator/Consumers/StartOrchestrationConsumer.cs + StopOrchestrationConsumer.cs + src/Orchestrator/L1/WorkflowL1Store.cs + src/Orchestrator/Hydration/HydrationBackgroundService.cs (the surfaces the no-global-lock review/guard inspects)
    - 23-VALIDATION.md (ORCH-ACK-01 + ORCH-SCALE-01 observable signals + Manual-Only Verifications)
  </read_first>
  <behavior>
    - Test (AckSemanticsTests.AbsentWorkflow_Start_Acks_NoThrow): gate ready, AbsentL2 stub; Consume(StartOrchestration([absentWf])) completes without throwing (ack) — business path returns, no _error.
    - Test (AckSemanticsTests.AbsentWorkflow_Stop_Acks_NoThrow): same for Stop (teardown of an unknown workflow is a no-op ack).
    - Test (AckSemanticsTests.RedisUnreachable_Consume_Propagates): gate ready, InfraFaultL2 stub; Consume(StartOrchestration([wf])) THROWS (infra fault propagates — does NOT ack-swallow), so the bounded retry pipeline can route to _error.
    - Test (AckSemanticsTests.StartupCorruptEntry_HydratesRest_HostStaysUp): a SetMembers stub returning [wfGood, wfCorrupt] where wfCorrupt's root JSON is malformed; running the hydration unit hydrates wfGood, skips wfCorrupt, throws nothing (host stays up); store Count == 1.
    - Test (NoGlobalLockTests.NoStaticLockFieldsGateLifecycle): reflection over Orchestrator.L1.WorkflowL1Store, WorkflowScheduler, both consumers, HydrationBackgroundService asserts NO static lock/mutex/SemaphoreSlim field (the per-wf stripe must be an INSTANCE ConcurrentDictionary, not static) and no static singleton-process gate.
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/AckSemanticsTests.cs` (namespace `BaseApi.Tests.Orchestrator`) reusing the `StartStopConsumerAckTests` `AbsentL2`/`InfraFaultL2`/`SetMembers` mux-stub helpers (extract to a shared internal static helper if they are private). For the "no _error" assertion, assert the consumer `Consume` task completes (does not throw) for business cases, and `await Assert.ThrowsAsync<RedisConnectionException>(...)` (or the StackExchange.Redis infra exception type the InfraFault stub throws) for the infra case. For the startup-corrupt case, run `HydrationBackgroundService.StartAsync`/`ExecuteAsync` (or the `WorkflowLifecycle` loop) against the corrupt-entry stub and assert no throw + store Count == 1.
    Create `tests/BaseApi.Tests/Orchestrator/NoGlobalLockTests.cs` — reflection guard: for each of the four production types, enumerate `type.GetFields(BindingFlags.Static | BindingFlags.NonPublic | BindingFlags.Public)` and assert none has a field type of `SemaphoreSlim`, `System.Threading.Mutex`, or a static lock-`object`, and no `static` field gates a singleton-process assumption. This is the automatable half of the ORCH-SCALE-01 review (the "no process-uniqueness assumption" half remains a documented manual review per 23-VALIDATION.md Manual-Only Verifications — reference it in a test comment).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class BaseApi.Tests.Orchestrator.AckSemanticsTests; dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class BaseApi.Tests.Orchestrator.NoGlobalLockTests</automated>
  </verify>
  <acceptance_criteria>
    - tests/BaseApi.Tests/Orchestrator/AckSemanticsTests.cs asserts a business absent-workflow Consume does NOT throw and an InfraFault Consume DOES throw (Assert.ThrowsAsync)
    - tests/BaseApi.Tests/Orchestrator/AckSemanticsTests.cs asserts the corrupt-startup-entry case leaves store Count == 1 with no throw
    - tests/BaseApi.Tests/Orchestrator/NoGlobalLockTests.cs uses 'BindingFlags.Static' reflection and asserts no static SemaphoreSlim/Mutex/lock field on the four types
    - dotnet test ... --filter-class BaseApi.Tests.Orchestrator.AckSemanticsTests exits 0
    - dotnet test ... --filter-class BaseApi.Tests.Orchestrator.NoGlobalLockTests exits 0
  </acceptance_criteria>
  <done>Absent-workflow start/stop acks without throwing (no _error); a Redis-unreachable consume propagates; a corrupt startup entry is skipped while the rest hydrate (host stays up); reflection confirms no static/global lock gates the lifecycle (per-instance state only).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Full-suite green + ORCH-SCALE-01 manual design review</name>
  <files>(no source files — full-suite gate run + manual design review + operator confirmation)</files>
  <action>Claude runs the full test suite and the orchestrator slice (see how-to-verify), performs the ORCH-SCALE-01 no-process-uniqueness design review over the four production surfaces, then pauses for operator confirmation. This is a verification gate, not a code change.</action>
  <what-built>
    The complete Phase 23 orchestrator lifecycle: contracts (Plan 01), Quartz pin + keys (Plan 02), L1 store + scheduler + fire job (Plan 03), hydration + gated consumers + Program wiring (Plan 04), and the full harness/review test suite (this plan). All new test classes pass under the orchestrator slice; the full suite is green.
  </what-built>
  <how-to-verify>
    1. Run the orchestrator slice — all new classes green:
       `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-class BaseApi.Tests.Orchestrator.*`
    2. Run the FULL suite — no regressions, zero-warning build:
       `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj`
       Expect Passed with 0 Failed; Release + Debug builds 0 Warning(s)/0 Error(s).
    3. ORCH-SCALE-01 manual design review (the non-automatable half — per 23-VALIDATION.md Manual-Only Verifications): read `src/Orchestrator/L1/WorkflowL1Store.cs`, `src/Orchestrator/Scheduling/WorkflowScheduler.cs`, both consumers, and `src/Orchestrator/Hydration/HydrationBackgroundService.cs`, and confirm NO process-uniqueness assumption gates the lifecycle (a 2nd replica would N×-dispatch — the accepted/deferred behavior — not crash). All state (L1 dictionary, Quartz RAMJobStore, the per-wf stripe) is per-instance; cross-replica dedup is explicitly deferred.
  </how-to-verify>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj</automated>
  </verify>
  <done>Orchestrator slice + full suite GREEN (0 Failed); Release + Debug builds 0 Warning(s)/0 Error(s); the ORCH-SCALE-01 no-process-uniqueness design review passes; operator typed "approved".</done>
  <resume-signal>Type "approved" once the full suite is green and the no-process-uniqueness design review passes, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic test harness -> production lifecycle | Tests exercise the real lifecycle through the in-memory bus + Redis mux stub; no real broker/Redis. |
| Verification of the zero-L2-write invariant | The Stop/fire tests are the enforcement point that the orchestrator never mutates L2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-12 | Repudiation/Integrity (silent L2 write regression) | StopConsumerLifecycleTests + FireDispatchTests | mitigate | The extended `DidNotReceive().StringSetAsync/SetAddAsync/KeyDeleteAsync` guards + byte-identical-L2 assertion fail the build if any future change introduces an orchestrator L2 write — making the zero-L2-write invariant continuously enforced, not a one-time review. |
| T-23-13 | Denial of Service (untested crash path) | AckSemanticsTests corrupt-entry case | mitigate | The corrupt-startup-entry test asserts the host stays up (no throw, rest hydrate) — pinning the ORCH-ACK-01 resilience so a malformed-L2 DoS via corrupt hydration data is caught by CI. |
| T-23-14 | Elevation (cross-replica process-uniqueness regression) | NoGlobalLockTests + manual review | mitigate | The reflection guard fails if a static lock/mutex is introduced; the blocking checkpoint adds the human design review for the non-reflectable process-uniqueness half (ORCH-SCALE-01). |
</threat_model>

<verification>
- `dotnet test ... --filter-class BaseApi.Tests.Orchestrator.FireDispatchTests` exits 0
- `dotnet test ... --filter-class BaseApi.Tests.Orchestrator.StartConsumerLifecycleTests` exits 0
- `dotnet test ... --filter-class BaseApi.Tests.Orchestrator.StopConsumerLifecycleTests` exits 0
- `dotnet test ... --filter-class BaseApi.Tests.Orchestrator.AckSemanticsTests` exits 0
- `dotnet test ... --filter-class BaseApi.Tests.Orchestrator.NoGlobalLockTests` exits 0
- Full suite `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj` green, 0 Warning(s)
- ORCH-SCALE-01 manual design review approved (no process-uniqueness assumption)
</verification>

<success_criteria>
- Synthetic consumer on queue:{processorId} proves one EntryStepDispatch per entry step per fire with correct fields, differing correlationIds, advancing liveness, zero L2 writes.
- Start hydrates+schedules only the consumed workflow; Stop deletes the job + clears L1 with a byte-identical L2 snapshot.
- Absent start/stop acks with no _error; Redis-unreachable propagates; corrupt startup entry skipped, host stays up.
- No static/global lock gates the lifecycle (reflection + design review).
- Full suite green; phase ready for /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/23-orchestrator-stop-reload-lifecycle/23-05-SUMMARY.md`
</output>
