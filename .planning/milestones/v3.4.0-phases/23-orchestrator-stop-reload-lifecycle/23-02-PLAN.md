---
phase: 23-orchestrator-stop-reload-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - src/Orchestrator/Orchestrator.csproj
  - src/Orchestrator/Messaging/OrchestratorL2Keys.cs
autonomous: true
requirements: [ORCH-SCHED-01, ORCH-STARTUP-01]
must_haves:
  truths:
    - "The Quartz dependency is pinned and referenced so scheduling code compiles"
    - "The orchestrator can build the parent-index SET key and per-step key via the shared L2ProjectionKeys forwarder"
  artifacts:
    - path: "Directory.Packages.props"
      provides: "Quartz + Quartz.Extensions.Hosting 3.18.1 CPM pins"
      contains: "PackageVersion Include=\"Quartz.Extensions.Hosting\" Version=\"3.18.1\""
    - path: "src/Orchestrator/Orchestrator.csproj"
      provides: "PackageReference to Quartz.Extensions.Hosting (no Version=)"
      contains: "PackageReference Include=\"Quartz.Extensions.Hosting\""
    - path: "src/Orchestrator/Messaging/OrchestratorL2Keys.cs"
      provides: "ParentIndex() + Step() reader forwarders"
      contains: "ParentIndex()"
  key_links:
    - from: "src/Orchestrator/Messaging/OrchestratorL2Keys.cs"
      to: "Messaging.Contracts L2ProjectionKeys"
      via: "thin forwarder (single source of truth, HARDEN-03)"
      pattern: "L2ProjectionKeys\\.(ParentIndex|Step)"
---

<objective>
Land the one genuinely-new dependency (Quartz) and the two reader key-forwarders that ALL downstream scheduling and hydration code requires. This is the load-bearing Wave-0 gap: no scheduling code compiles until `Quartz` + `Quartz.Extensions.Hosting` 3.18.1 are pinned in CPM and referenced by the Orchestrator csproj. The L2Keys forwarders give hydration the `skp:` parent-index SET key and the per-step key via the shared `L2ProjectionKeys` (HARDEN-03 single source of truth — never inline the format).

Purpose: unblock Plan 03 (L1 store + scheduling) and Plan 04 (hydration + consumers). ORCH-SCHED-01 needs Quartz; ORCH-STARTUP-01 needs `ParentIndex()` (SMEMBERS source) and `Step()` (per-step GET).
Output: 2 CPM pins, 1 PackageReference, 2 forwarder methods. No new C# logic beyond the thin forwarders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-SPEC.md
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-RESEARCH.md
@.planning/phases/23-orchestrator-stop-reload-lifecycle/23-PATTERNS.md

<interfaces>
<!-- Extracted this session. -->

Source key builders (forward to these, do NOT inline) — src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:
```csharp
public const string Prefix = "skp:";
public static string ParentIndex() => Prefix;                              // skp: SET key (parent index)
public static string Root(Guid workflowId) => $"{Prefix}{workflowId:D}";
public static string Step(Guid workflowId, Guid stepId) => $"{Prefix}{workflowId}:{stepId}";
public static string Processor(Guid processorId) => $"{Prefix}{processorId}";
```

Existing forwarder (the file to EDIT) — src/Orchestrator/Messaging/OrchestratorL2Keys.cs:
```csharp
internal static class OrchestratorL2Keys
{
    public static string Root(Guid workflowId) => L2ProjectionKeys.Root(workflowId);
}
```

Existing pins (already present — do NOT re-add) — Directory.Packages.props:
  line 91:  <PackageVersion Include="Cronos" Version="0.13.0" />
  line 131: <PackageVersion Include="MassTransit" Version="8.5.5" />

CPM convention (Orchestrator.csproj): NO Version= on any PackageReference; versions pinned only in Directory.Packages.props.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Quartz 3.18.1 in CPM and reference it from Orchestrator.csproj</name>
  <read_first>
    - Directory.Packages.props (find the existing alphabetical/grouped PackageVersion block; Cronos is line ~91, MassTransit ~131)
    - src/Orchestrator/Orchestrator.csproj (the existing PackageReference ItemGroup — MassTransit + MassTransit.RabbitMQ, no Version=)
  </read_first>
  <action>
    1. In `Directory.Packages.props`, add two new `PackageVersion` entries (keep the file's existing ordering convention — place near the other third-party pins; alphabetical "Q" sits after MassTransit/before StackExchange is fine):
    ```xml
    <PackageVersion Include="Quartz" Version="3.18.1" />
    <PackageVersion Include="Quartz.Extensions.Hosting" Version="3.18.1" />
    ```
    Pin BOTH for auditability even though Quartz.Extensions.Hosting transitively brings Quartz (repo CPM convention — RESEARCH Standard Stack install note).
    2. In `src/Orchestrator/Orchestrator.csproj`, add to the existing PackageReference ItemGroup (the one with MassTransit / MassTransit.RabbitMQ) a single reference WITH NO Version= attribute:
    ```xml
    <PackageReference Include="Quartz.Extensions.Hosting" />
    ```
    Quartz.Extensions.Hosting transitively brings `Quartz` + `Quartz.Extensions.DependencyInjection` — one PackageReference suffices.
  </action>
  <verify>
    <automated>dotnet restore src/Orchestrator/Orchestrator.csproj; dotnet build src/Orchestrator/Orchestrator.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Directory.Packages.props contains 'PackageVersion Include="Quartz" Version="3.18.1"'
    - Directory.Packages.props contains 'PackageVersion Include="Quartz.Extensions.Hosting" Version="3.18.1"'
    - src/Orchestrator/Orchestrator.csproj contains 'PackageReference Include="Quartz.Extensions.Hosting"'
    - src/Orchestrator/Orchestrator.csproj does NOT contain 'Quartz' with a 'Version=' attribute (CPM convention)
    - dotnet build src/Orchestrator/Orchestrator.csproj exits 0 with 0 Warning(s) / 0 Error(s)
  </acceptance_criteria>
  <done>Both Quartz CPM pins are present at 3.18.1; the Orchestrator csproj references Quartz.Extensions.Hosting with no Version=; the Orchestrator project restores and builds zero-warning (Quartz types now resolvable for downstream plans).</done>
</task>

<task type="auto">
  <name>Task 2: Add ParentIndex() and Step() reader forwarders to OrchestratorL2Keys</name>
  <read_first>
    - src/Orchestrator/Messaging/OrchestratorL2Keys.cs (the file to EDIT — currently forwards only Root)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (the source ParentIndex()/Step() to forward to)
  </read_first>
  <action>
    Edit `src/Orchestrator/Messaging/OrchestratorL2Keys.cs`. Add two thin forwarders next to the existing `Root` (do NOT inline the key format — forward to `L2ProjectionKeys`, single source of truth per HARDEN-03 / D-03):
    ```csharp
    public static string ParentIndex() => L2ProjectionKeys.ParentIndex();

    public static string Step(Guid workflowId, Guid stepId) => L2ProjectionKeys.Step(workflowId, stepId);
    ```
    Keep the class `internal static`. The `using Messaging.Contracts.Projections;` at the top already covers `L2ProjectionKeys`.
  </action>
  <verify>
    <automated>dotnet build src/Orchestrator/Orchestrator.csproj</automated>
  </verify>
  <acceptance_criteria>
    - src/Orchestrator/Messaging/OrchestratorL2Keys.cs contains 'public static string ParentIndex() => L2ProjectionKeys.ParentIndex();'
    - src/Orchestrator/Messaging/OrchestratorL2Keys.cs contains 'public static string Step(Guid workflowId, Guid stepId) => L2ProjectionKeys.Step(workflowId, stepId);'
    - src/Orchestrator/Messaging/OrchestratorL2Keys.cs does NOT contain a literal '"skp:"' or '$"{' inline key format (must forward, not inline)
    - dotnet build src/Orchestrator/Orchestrator.csproj exits 0
  </acceptance_criteria>
  <done>OrchestratorL2Keys forwards ParentIndex() and Step() to L2ProjectionKeys (no inlined formats); the Orchestrator project builds. Hydration (Plan 04) can now read the parent-index SET and per-step keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supply chain -> build | A new NuGet dependency (Quartz) enters the build graph. |
| Key construction -> Redis access | Key strings derived from L2ProjectionKeys address Redis reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering (supply chain) | Quartz NuGet pin | mitigate | Exact version pinned (3.18.1) in CPM — no floating range; verified against nuget.org this session (RESEARCH version-verification). CPM central pinning prevents a transitive version drift. |
| T-23-04 | Spoofing/Elevation (key collision) | OrchestratorL2Keys forwarders | mitigate | Forwarders delegate to the single-source-of-truth L2ProjectionKeys (HARDEN-03); no inlined format can desynchronize reader and writer key namespaces. Acceptance asserts no inline format string present. |
</threat_model>

<verification>
- `dotnet build src/Orchestrator/Orchestrator.csproj` exits 0 with 0 Warning(s) / 0 Error(s)
- Quartz types (`Quartz.IScheduler`, `Quartz.JobKey`) resolve in the Orchestrator project (downstream plan compile-check)
- OrchestratorL2Keys exposes Root + ParentIndex + Step, all forwarding to L2ProjectionKeys
</verification>

<success_criteria>
- Quartz + Quartz.Extensions.Hosting 3.18.1 pinned in CPM; Orchestrator references Quartz.Extensions.Hosting (no Version=).
- OrchestratorL2Keys forwards ParentIndex() and Step() with no inlined key formats.
- Orchestrator project restores + builds zero-warning — scheduling code in Plan 03 will compile.
</success_criteria>

<output>
After completion, create `.planning/phases/23-orchestrator-stop-reload-lifecycle/23-02-SUMMARY.md`
</output>
