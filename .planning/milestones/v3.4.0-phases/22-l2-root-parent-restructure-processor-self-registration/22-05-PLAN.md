---
phase: 22-l2-root-parent-restructure-processor-self-registration
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - tests/BaseApi.Tests/Composition/RedisFixture.cs
  - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs
  - tests/BaseApi.Tests/Composition/RedisFixtureFacts.cs
  - tests/BaseApi.Tests/Composition/RedisProjectionOptionsBindingFacts.cs
  - tests/BaseApi.Tests/Composition/AppsettingsFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/ProcessorLivenessFacts.cs
autonomous: false
requirements: [L2IDX-01, L2PREFIX-01, PROC-NOCREATE-01, PROC-LIVE-01, PROC-EDGE-01]
must_haves:
  truths:
    - "Tests run on the prod keyspace (const 'skp:' prefix); per-class isolation seam removed; cleanup deletes only the known GUID keys each test created."
    - "Parent-index-touching tests run in a single non-parallel xUnit collection; each SREMs its own ids so the index is empty between tests."
    - "New ProcessorLivenessFacts prove 204 all-live / 422 absent / 422 stale, seeding L2 directly to simulate self-registration."
    - "Existing golden/writer/cleanup/gate/binding/appsettings tests updated to the new shapes."
    - "Full suite GREEN ×3 and the triple-SHA close gate exits 0 (BEFORE==AFTER)."
  artifacts:
    - path: "tests/BaseApi.Tests/Features/Orchestration/ProcessorLivenessFacts.cs"
      provides: "204 all-live / 422 absent / 422 stale facts"
      contains: "processorLiveness"
      min_lines: 60
    - path: "tests/BaseApi.Tests/Composition/RedisFixture.cs"
      provides: "known-key cleanup (no per-class prefix, no SCAN-MATCH)"
  key_links:
    - from: "ProcessorLivenessFacts"
      to: "L2 processor key seed"
      via: "db.StringSetAsync(L2ProjectionKeys.Processor(procId), serialized ProcessorProjection)"
      pattern: "L2ProjectionKeys\\.(Prefix|Processor)"
    - from: "RedisProjectionWriterFacts"
      to: "parent index"
      via: "SMEMBERS assertion"
      pattern: "SetMembersAsync\\(.*ParentIndex|ParentIndex\\(\\)"
---

<objective>
Complete the Phase 22 test surface and prove the close gate. Rewrite the test-isolation seam
(D-20..D-23): hardcoding the prefix to `"skp:"` removes the per-class `Redis:KeyPrefix` injection, so
`RedisFixture` drops its unique prefix + SCAN-MATCH cleanup in favor of deleting the specific known GUID
keys each test created; `Phase8WebAppFactory` stops injecting `Redis:KeyPrefix` and its `RedisKeyPrefix`
consumers switch to `L2ProjectionKeys.Prefix`. Add the new `ProcessorLivenessFacts` (204 all-live / 422
absent / 422 stale, seeding L2 directly). Update the golden/writer/cleanup/gate/binding/appsettings tests
to the new shapes (D-24). Then run the BLOCKING full-suite GREEN ×3 + triple-SHA close gate.

Purpose: Closes PROC-LIVE-01 acceptance (the three 204/422 facts), proves L2IDX-01 (SMEMBERS), PROC-NOCREATE-01
(zero processor keys), L2PREFIX-01 (binding/appsettings facts dropped), and PROC-EDGE-01 (SchemaEdge tests
stay green). Depends on ALL production changes (Plans 01-04). Wave 4.

Output: Updated test infra + golden tests; new ProcessorLivenessFacts; full suite GREEN ×3; triple-SHA gate exit 0.

Tests stay on DB0 / the prod keyspace (D-20) so the close gate scans the same keyspace the tests use.
Per-workflow/step/processor keys (`skp:{guid}...`) stay collision-free via unique GUIDs; the ONLY contention
point is the shared `skp:` parent-index SET — those tests serialize in one collection and SREM their own ids.

LOCKED liveness unit (from Plan 04): `interval` is SECONDS; validator math is `timestamp + interval*2 > now`.
Seed accordingly: live = `now` + positive interval (e.g. 300s); stale = far-past timestamp OR interval=0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-SPEC.md
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-CONTEXT.md
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-PATTERNS.md

<interfaces>
<!-- Key builders (Plan 01): L2ProjectionKeys.Prefix == "skp:"; .ParentIndex() == "skp:";
     .Root(Guid)/.Step(Guid,Guid)/.Processor(Guid) no-prefix. Writer forwarder RedisProjectionKeys mirrors these. -->
<!-- Parent-index member format: wf.Id.ToString("D"). SE.Redis: SetMembersAsync (SMEMBERS), SetRemoveAsync (SREM). -->
<!-- ProcessorProjection(string? inputDefinition, string? outputDefinition, LivenessProjection liveness). -->
<!-- LivenessProjection(DateTime timestamp, int interval, string status). -->
<!-- New gate string: "processorLiveness"; offending: { procId, reason } reason in {"absent","stale"}. -->

<!-- StopCleanupFacts seeding helpers TODAY use _factory.RedisKeyPrefix (RootKey/StepKey/ProcKey at lines 38-40)
     + serialize the record via JsonSerializer.Serialize. Those keys switch to L2ProjectionKeys.* builders. -->
<!-- GateNoWriteFacts has Assert422Gate(resp, gate, ct) helper (~lines 60-70) + 3 existing gate arms (190-264). -->
<!-- SchemaEdgeFacts is the 204/422 + HarnessWebAppFactory analog (HTTP-seeds Schema->Processor->Step->Workflow). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-isolation rewrite — RedisFixture known-key cleanup + Phase8WebAppFactory no prefix injection</name>
  <files>tests/BaseApi.Tests/Composition/RedisFixture.cs, tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs, tests/BaseApi.Tests/Composition/RedisFixtureFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Composition/RedisFixture.cs (full — KeyPrefix line 40, SCAN-MATCH cleanup 52-94)
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (lines 110-200 — RedisKeyPrefix property 116-117, ["Redis:KeyPrefix"] injection line 193)
    - tests/BaseApi.Tests/Composition/RedisFixtureFacts.cs (full — asserts the test:cls- prefix + residual-key fail-loud)
    - .planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-PATTERNS.md (test-isolation rewrite section D-20..D-23)
  </read_first>
  <action>
    A. `tests/BaseApi.Tests/Composition/RedisFixture.cs` (D-23):
       1. Remove the per-class `public string KeyPrefix { get; } = $"test:cls-{Guid.NewGuid():N}:";` (line 40).
       2. Replace the SCAN-MATCH `DisposeAsync` cleanup (lines 52-94) with KNOWN-KEY deletion: expose a way for
          tests to register the keys they create (e.g. a `public ConcurrentBag<RedisKey> TrackedKeys { get; }` or a
          `public void Track(RedisKey key)` method), and in `DisposeAsync` `KeyDeleteAsync(TrackedKeys.ToArray())`
          then dispose the multiplexer. Do NOT SCAN `skp:*` (would catch sibling classes' keys now that the prefix
          is shared). Keep the multiplexer-dispose in a `finally`. Update the XML doc to describe known-key cleanup.
          (Implementation shape is discretion — the load-bearing rule: no `skp:*` SCAN; delete only keys this class made.)
    B. `tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs` (D-23):
       1. Delete the `["Redis:KeyPrefix"] = RedisKeyPrefix,` injection (line 193).
       2. Remove the `RedisKeyPrefix` property (lines 116-117) OR repoint it to return `L2ProjectionKeys.Prefix`
          ("skp:") so existing call sites compile. Prefer repointing to `L2ProjectionKeys.Prefix` to minimize churn
          in the writer/cleanup/gate facts (they read `_factory.RedisKeyPrefix`). Add `using Messaging.Contracts.Projections;`.
    C. `tests/BaseApi.Tests/Composition/RedisFixtureFacts.cs` (D-23):
       Rewrite the facts that assert the `test:cls-{Guid:N}:` prefix and the residual-key fail-loud to the known-key
       cleanup model — assert that tracked keys are deleted on dispose and no `test:cls-` prefix is referenced.
       Remove any assertion of a per-class unique prefix.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep "test:cls-" tests/BaseApi.Tests/Composition/RedisFixture.cs tests/BaseApi.Tests/Composition/RedisFixtureFacts.cs` returns zero matches.
    - `grep "Redis:KeyPrefix" tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs` returns zero matches.
    - `RedisFixture.cs` `DisposeAsync` does NOT SCAN/KeysAsync with a `skp:*` or `{KeyPrefix}*` pattern.
    - If kept, `Phase8WebAppFactory.RedisKeyPrefix` returns `L2ProjectionKeys.Prefix`.
    - `tests/BaseApi.Tests/BaseApi.Tests.csproj` builds clean.
  </acceptance_criteria>
  <done>RedisFixture cleans up by known keys (no SCAN-MATCH, no per-class prefix); Phase8WebAppFactory no longer injects Redis:KeyPrefix; RedisFixtureFacts rewritten; tests compile.</done>
</task>

<task type="auto">
  <name>Task 2: Golden + binding + appsettings + writer + cleanup + gate test updates (D-24)</name>
  <files>tests/BaseApi.Tests/Composition/RedisProjectionOptionsBindingFacts.cs, tests/BaseApi.Tests/Composition/AppsettingsFacts.cs, tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs, tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs, tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Composition/RedisProjectionOptionsBindingFacts.cs (full — KeyPrefix binding facts ~47-72)
    - tests/BaseApi.Tests/Composition/AppsettingsFacts.cs (full — Redis_Section_KeyPrefix fact ~50-54)
    - tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs (full — ProcessorProjection_Ttl ~204-238, processor-keyspace asserts 184-199, key construction 120-238)
    - tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs (full — seeding helpers 36-61, facts 63-92)
    - tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs (full — Assert422Gate helper 60-70, 3 gate arms 190-264)
  </read_first>
  <action>
    1. `RedisProjectionOptionsBindingFacts.cs` (D-24): DELETE the two `KeyPrefix` binding facts (~lines 47-53, 63-72).
       KEEP the `Serialization.JsonOptions` + `ProcessorKeyTtlDays` facts.
    2. `AppsettingsFacts.cs` (D-24): DELETE `Appsettings_Has_Redis_Section_KeyPrefix_skp` (~lines 50-54). KEEP the
       connection-string + abortConnect facts. (Optionally add a fact asserting BaseApi.Service appsettings Redis
       section has NO KeyPrefix but DOES have ProcessorKeyTtlDays.)
    3. `RedisProjectionWriterFacts.cs` (D-24): switch all key construction to the no-prefix builders (or
       `L2ProjectionKeys.*` / `_factory.RedisKeyPrefix` now == "skp:"). REMOVE/adjust `ProcessorProjection_Ttl`
       (~204-238) and the processor-keyspace asserts (~184-199). ADD: after Start, assert ZERO `{prefix}{procId}`
       keys exist (PROC-NOCREATE-01), and assert `SMEMBERS` of `RedisProjectionKeys.ParentIndex()` CONTAINS
       `wf.Id.ToString("D")` (L2IDX-01) via `db.SetMembersAsync(...)`. Each such test must SREM its own wf id in
       cleanup (see Task 4 collection note) and is added to the parent-index collection.
    4. `StopCleanupFacts.cs` (D-24): change `RootKey/StepKey/ProcKey` helpers (lines 38-40) to use the no-prefix
       builders (`L2ProjectionKeys.Root(wf)` etc.). Seed the parent-index SET in setup (`SADD skp: {wf:D}`) so the
       SREM is observable, then assert after Stop that `SMEMBERS ParentIndex()` does NOT contain the wf id (D-10).
    5. `GateNoWriteFacts.cs` (D-24): add a `processorLiveness` arm mirroring the three existing gate arms
       (~190-264): trigger the gate (e.g. a participating processor with no seeded L2 entry → absent → 422) and
       assert `Assert422Gate(resp, "processorLiveness", ct)` + zero L2 keys written. Use the existing helper signature.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter-class BaseApi.Tests.Features.Orchestration.RedisProjectionWriterFacts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "KeyPrefix" tests/BaseApi.Tests/Composition/RedisProjectionOptionsBindingFacts.cs tests/BaseApi.Tests/Composition/AppsettingsFacts.cs` returns zero matches.
    - `RedisProjectionWriterFacts.cs` contains a `SetMembersAsync` assertion against `ParentIndex()` and an assertion of ZERO processor keys post-Start.
    - `grep "ProcessorProjection_Ttl" tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs` returns zero matches (TTL fact removed).
    - `StopCleanupFacts.cs` asserts the wf id is absent from `ParentIndex()` SMEMBERS after Stop.
    - `GateNoWriteFacts.cs` contains `Assert422Gate(` with `"processorLiveness"`.
    - RedisProjectionWriterFacts class is GREEN.
  </acceptance_criteria>
  <done>Golden/binding/appsettings/writer/cleanup/gate tests updated to the new shapes; writer facts prove SADD + zero processor keys; cleanup facts prove SREM; gate facts cover processorLiveness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New ProcessorLivenessFacts — 204 all-live / 422 absent / 422 stale</name>
  <files>tests/BaseApi.Tests/Features/Orchestration/ProcessorLivenessFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/SchemaEdgeFacts.cs (full — HarnessWebAppFactory, HTTP-seed Schema->Processor->Step->Workflow, 204/422 + errors.gate/offending assertions, gate assert ~line 137)
    - tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs (lines 34-61 — direct L2 seed via _factory.RedisMultiplexer.GetDatabase() + JsonSerializer.Serialize(record))
    - tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs (lines 60-70 — Assert422Gate helper, no-leak pattern)
    - src/BaseApi.Service/Features/Orchestration/Projection/ProcessorProjection.cs (full)
    - src/Messaging.Contracts/Projections/LivenessProjection.cs (full)
  </read_first>
  <behavior>
    - 204 all-live: every participating processor's skp:{procId} seeded with LivenessProjection(now, 300, "Live") (interval seconds, now + 300*2 > now) → POST /start returns 204 NoContent.
    - 422 absent: one participating processor's key NOT seeded → 422 with errors.gate=="processorLiveness", errors.offending.reason=="absent", offending.procId == that processor's id.
    - 422 stale: processor seeded with a far-past timestamp (or interval=0) so timestamp + interval*2 <= now → 422 with reason=="stale".
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Features/Orchestration/ProcessorLivenessFacts.cs`, mirroring `SchemaEdgeFacts`
    (HarnessWebAppFactory, HTTP-seed Schema→Processor→Step→Workflow, POST `/api/v1/orchestration/start`) combined
    with the direct-L2-seed pattern from `StopCleanupFacts`:
    1. HTTP-seed the graph; capture each created processor's returned `Id`.
    2. BEFORE calling `/start`, seed each participating processor's L2 key directly:
       `db.StringSetAsync(L2ProjectionKeys.Processor(procId), JsonSerializer.Serialize(new ProcessorProjection(null, null, new LivenessProjection(DateTime.UtcNow, 300, "Live"))))`.
       Serialize the RECORD (not hand-written JSON) so camelCase names stay correct.
       Use `DateTime.UtcNow` (the service uses real `TimeProvider.System` in integration).
    3. Fact 1 (204): seed all processors live → assert `HttpStatusCode.NoContent`.
    4. Fact 2 (422 absent): skip seeding one processor → assert `UnprocessableEntity`, `errors.gate=="processorLiveness"`,
       `errors.offending.reason=="absent"`, `errors.offending.procId` == the unseeded processor id.
    5. Fact 3 (422 stale): seed with `new LivenessProjection(DateTime.UtcNow.AddDays(-1), 0, "Live")` (deadline <= now)
       → assert `UnprocessableEntity`, `reason=="stale"`.
    6. Each fact cleans up the keys it seeded (track + delete, consistent with Task 1's known-key model). Add this
       class to the parent-index xUnit collection (Task 4) ONLY if it touches the shared `skp:` SET — if it only
       seeds per-processor `skp:{procId}` keys (unique GUIDs) and a /start writes the parent index, it DOES touch
       the shared SET, so include it in the collection and SREM its wf ids.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter-class BaseApi.Tests.Features.Orchestration.ProcessorLivenessFacts</automated>
  </verify>
  <acceptance_criteria>
    - `ProcessorLivenessFacts.cs` exists with three facts asserting 204, 422-absent, 422-stale.
    - It seeds via `L2ProjectionKeys.Processor(` + `JsonSerializer.Serialize(new ProcessorProjection(`.
    - The absent fact asserts `errors.gate` == `"processorLiveness"` and `reason` == `"absent"`; the stale fact asserts `"stale"`.
    - The live fact uses a positive interval (seconds) so `now + interval*2 > now`.
    - ProcessorLivenessFacts class is GREEN.
  </acceptance_criteria>
  <done>ProcessorLivenessFacts proves the three PROC-LIVE-01 acceptance paths with direct L2 seeding; class is GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: Serialize parent-index-touching tests into one non-parallel collection</name>
  <files>tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs, tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs, tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs, tests/BaseApi.Tests/Features/Orchestration/ProcessorLivenessFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs (class attributes/collection)
    - tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs (class attributes/collection)
    - tests/BaseApi.Tests/Features/Orchestration/GateNoWriteFacts.cs (class attributes/collection)
    - .planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-PATTERNS.md (D-22 single non-parallel collection)
  </read_first>
  <action>
    D-22: the shared `skp:` parent-index SET is the only contention point on the now-shared keyspace. Put EVERY test
    class that SADDs/SREMs/SMEMBERS the parent index into a SINGLE non-parallel xUnit collection so they serialize:
    1. Define a collection definition once (e.g. `[CollectionDefinition("ParentIndex", DisableParallelization = true)]
       public sealed class ParentIndexCollection { }`) in a small new helper file under `tests/BaseApi.Tests/Features/Orchestration/`
       OR reuse an existing collection-definition file if one already serves this purpose (search first).
    2. Annotate the parent-index-touching classes with `[Collection("ParentIndex")]`: `RedisProjectionWriterFacts`,
       `StopCleanupFacts`, `GateNoWriteFacts` (its processorLiveness arm Starts → SADD), and `ProcessorLivenessFacts`
       (Start writes the parent index). Do NOT add classes that never touch the shared SET.
    3. Ensure each such class SREMs its own workflow ids in cleanup so the index is empty between tests (collision-free).
    NOTE: per-workflow/step/processor keys remain collision-free via unique GUIDs (D-21) — only the shared SET needs serialization.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter-class BaseApi.Tests.Features.Orchestration.StopCleanupFacts</automated>
  </verify>
  <acceptance_criteria>
    - A `[CollectionDefinition(... DisableParallelization = true)]` for the parent index exists.
    - `RedisProjectionWriterFacts`, `StopCleanupFacts`, `GateNoWriteFacts`, and `ProcessorLivenessFacts` each carry `[Collection("ParentIndex")]` (or the chosen collection name).
    - Running StopCleanupFacts in isolation is GREEN (no cross-test parent-index contamination).
  </acceptance_criteria>
  <done>All parent-index-touching test classes serialize in one non-parallel collection; each cleans its own ids.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: BLOCKING — full suite GREEN ×3 + triple-SHA close gate exit 0</name>
  <files>(no source files — gate run + operator confirmation)</files>
  <what-built>
    All Phase 22 production + test changes (Plans 01-05). Now prove the triple-SHA close gate: full suite GREEN
    three consecutive times, with psql \l + redis-cli --scan + rabbitmqctl list_queues SHA-256 BEFORE==AFTER.
    The phase introduced the new `skp:` parent-index SET to a keyspace that tests share (DB0/prod keyspace) — the
    gate's `redis-cli --scan` must return to its BEFORE state (every test SREMs its own ids + deletes its known keys).
  </what-built>
  <action>
    Claude runs the existing close-gate procedure (Phase 20/21 procedure — full v3.4.0 stack up healthy), then
    pauses for operator confirmation. This is a verification gate, not a code change. See how-to-verify for the steps.
  </action>
  <how-to-verify>
    Claude runs the existing close-gate procedure (the one used at Phase 20/21 — full v3.4.0 stack up healthy):
    1. Bring the real stack up healthy (postgres, redis, rabbitmq, otel-collector, orchestrator, baseapi-service).
    2. Capture BEFORE SHAs: `psql \l` SHA-256, `redis-cli --scan` SHA-256, `rabbitmqctl list_queues` SHA-256.
    3. Run the full test suite 3 consecutive times — each MUST report Failed: 0 (record Passed/Total + durations),
       including the real-stack CorrelationPropagationE2ETests.
    4. Capture AFTER SHAs; assert BEFORE == AFTER for all three (redis-cli --scan must be the empty-keyspace SHA
       e3b0c442...855 or otherwise identical to BEFORE).
    5. Zero-warning build both configs (Release + Debug = 0 Warning / 0 Error).
    Operator: confirm the gate output (paste the three SHAs + the 3× GREEN counts) and authorize finalization.
  </how-to-verify>
  <verify>
    <automated>Full-suite GREEN ×3 + triple-SHA BEFORE==AFTER (operator-confirmed gate evidence). No single automated command — this is the multi-step close-gate procedure above.</automated>
  </verify>
  <done>Full suite GREEN ×3; psql/redis/rabbitmq SHA-256 BEFORE==AFTER; zero-warning build both configs; operator typed "approved" with evidence.</done>
  <resume-signal>Type "approved" with the gate evidence (3× GREEN counts + BEFORE==AFTER SHAs), or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test seed → shared `skp:` keyspace | Tests write to the prod keyspace (no per-class prefix); leaked keys would corrupt the close-gate SHA |
| test L2 seed → validator | Tests simulate external self-registration by seeding `ProcessorProjection` JSON directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Tampering | shared keyspace cross-test contamination | mitigate | Known-key cleanup (delete only the GUID keys each test created) + a single non-parallel collection for parent-index tests + per-test SREM keep the keyspace clean; the triple-SHA BEFORE==AFTER gate is the fail-loud proof. No `skp:*` SCAN that could mask or delete sibling keys. |
| T-22-15 | Denial of service | residual keys fail the close gate | mitigate | Each parent-index test SREMs its own ids; the gate's `redis-cli --scan` SHA must equal BEFORE — a leak surfaces immediately as a SHA mismatch (Task 5 blocks finalization until BEFORE==AFTER). |
| T-22-16 | Spoofing | test-seeded liveness JSON validates the live/stale boundary | accept | Tests deliberately seed both live and stale/absent shapes to exercise the validator's fail-safe behavior; this is test-only and confined to the throwaway keyspace. Low risk. |
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug` succeeds (0 warnings, 0 errors).
- ProcessorLivenessFacts, RedisProjectionWriterFacts, StopCleanupFacts, GateNoWriteFacts classes are GREEN.
- SchemaEdgeFacts (and the SchemaEdge cycle/payload gate facts) remain GREEN (PROC-EDGE-01 — no behavioral change).
- `grep -rn "test:cls-\|Redis:KeyPrefix" tests/BaseApi.Tests/Composition/` returns zero matches.
- Full suite GREEN ×3; triple-SHA close gate exit 0 (psql/redis/rabbitmq BEFORE==AFTER); operator-confirmed.
</verification>

<success_criteria>
- Test-isolation seam rewritten to known-key cleanup on the shared "skp:" keyspace; parent-index tests serialized.
- ProcessorLivenessFacts proves 204 all-live / 422 absent / 422 stale.
- Writer facts prove SADD parent index + zero processor keys; cleanup facts prove SREM; gate facts cover processorLiveness; binding/appsettings KeyPrefix facts dropped.
- SchemaEdgeValidator tests unchanged and GREEN (PROC-EDGE-01).
- Full suite GREEN ×3 and triple-SHA close gate exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-05-SUMMARY.md`
</output>
