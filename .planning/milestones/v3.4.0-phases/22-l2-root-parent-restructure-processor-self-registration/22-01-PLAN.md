---
phase: 22-l2-root-parent-restructure-processor-self-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
  - src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionKeys.cs
  - src/Orchestrator/Messaging/OrchestratorL2Keys.cs
  - tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs
autonomous: true
requirements: [L2PREFIX-01, L2IDX-01]
must_haves:
  truths:
    - "All L2 key construction flows through the single shared L2ProjectionKeys with a compile-time const prefix."
    - "A ParentIndex() builder returns the bare prefix const — the parent-index SET key."
    - "Writer and reader forwarders expose parameterless (no-prefix) signatures in lockstep."
  artifacts:
    - path: "src/Messaging.Contracts/Projections/L2ProjectionKeys.cs"
      provides: "const Prefix + ParentIndex() + no-prefix Root/Step/Processor builders"
      contains: "public const string Prefix = \"skp:\""
    - path: "src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionKeys.cs"
      provides: "writer-side no-prefix forwarders + ParentIndex()"
      contains: "ParentIndex()"
    - path: "src/Orchestrator/Messaging/OrchestratorL2Keys.cs"
      provides: "reader-side no-prefix Root forwarder"
      contains: "Root(Guid workflowId)"
  key_links:
    - from: "RedisProjectionKeys.cs"
      to: "L2ProjectionKeys.cs"
      via: "static forwarder"
      pattern: "L2ProjectionKeys\\.(Root|Step|Processor|ParentIndex)"
    - from: "OrchestratorL2Keys.cs"
      to: "L2ProjectionKeys.cs"
      via: "static forwarder"
      pattern: "L2ProjectionKeys\\.Root"
---

<objective>
Make the L2 key prefix a compile-time `const` on the single shared `L2ProjectionKeys`, drop the
`string prefix` parameter from every builder, and add the `ParentIndex()` builder that returns the
bare prefix (the parent-index SET key). Update both thin forwarders (writer `RedisProjectionKeys`,
reader `OrchestratorL2Keys`) to the new parameterless signatures in lockstep (HARDEN-03
single-source-of-truth), and update the `L2ProjectionKeysTests` golden strings.

Purpose: This is the foundational ripple of Phase 22 — every writer, cleanup, service, reader
consumer, and test compiles against these signatures. It MUST land first (Wave 1) so the rest of
the phase has a stable contract to build against. Implements L2PREFIX-01 (const prefix) and the
key-builder half of L2IDX-01 (the `ParentIndex()` SET key).

Output: New `const Prefix` + `ParentIndex()` builder + 3 no-prefix builders in `L2ProjectionKeys`;
matching no-prefix forwarders (+ `ParentIndex()` on the writer side); green golden tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-SPEC.md
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-CONTEXT.md
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-PATTERNS.md

<interfaces>
<!-- Current builders in L2ProjectionKeys.cs (lines 26-30) — each takes `string prefix`: -->
public static string Root(string prefix, Guid workflowId) => $"{prefix}{workflowId:D}";
public static string Step(string prefix, Guid workflowId, Guid stepId) => $"{prefix}{workflowId}:{stepId}";
public static string Processor(string prefix, Guid processorId) => $"{prefix}{processorId}";

<!-- Writer forwarder RedisProjectionKeys.cs (lines 13-17) forwards to L2ProjectionKeys with prefix. -->
<!-- Reader forwarder OrchestratorL2Keys.cs (line 14): Root(string prefix, Guid) only — its sole call site. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Const prefix + ParentIndex() + no-prefix builders in L2ProjectionKeys</name>
  <read_first>
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (full — current builders + XML doc lines 14-17 that say "prefix stays a parameter")
    - .planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-PATTERNS.md (the L2ProjectionKeys section, D-01/D-02)
  </read_first>
  <behavior>
    - Root(wfId) returns "skp:{wfId:D}" (hyphenated D format) — byte-identical to old Root("skp:", wfId).
    - Step(wfId, stepId) returns "skp:{wfId}:{stepId}".
    - Processor(procId) returns "skp:{procId}".
    - ParentIndex() returns exactly "skp:" (the bare Prefix const, verbatim).
  </behavior>
  <action>
    In `src/Messaging.Contracts/Projections/L2ProjectionKeys.cs`:
    1. Add at the top of the class body: `public const string Prefix = "skp:";`
    2. Add builder: `public static string ParentIndex() => Prefix;` (D-02 — bare prefix, the parent-index SET key).
    3. Change the three existing builders to DROP the `string prefix` param and use the const:
       - `public static string Root(Guid workflowId) => $"{Prefix}{workflowId:D}";`
       - `public static string Step(Guid workflowId, Guid stepId) => $"{Prefix}{workflowId}:{stepId}";`
       - `public static string Processor(Guid processorId) => $"{Prefix}{processorId}";`
    4. Update the XML doc: the `<para>` at lines 14-17 currently states `prefix stays a parameter on every builder (D-05)` — REVERSE it to state the prefix is now a compile-time `const Prefix = "skp:"` owned here (D-01, Phase 22), consumed by both forwarders; no host config. Keep the `:D` / "D" (hyphenated) GUID-format note for `Root`. Add a bullet documenting `ParentIndex: {Prefix}` (the SET key).
    Keep `Root`'s `:D` specifier. Do NOT add any member-set builder (L2IDX-02 dropped — D-03/D-13).
  </action>
  <verify>
    <automated>dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `L2ProjectionKeys.cs` contains `public const string Prefix = "skp:"`.
    - `L2ProjectionKeys.cs` contains `public static string ParentIndex() => Prefix;`.
    - `L2ProjectionKeys.cs` contains `Root(Guid workflowId) => $"{Prefix}{workflowId:D}"` and the Step/Processor no-prefix forms.
    - `grep "string prefix" src/Messaging.Contracts/Projections/L2ProjectionKeys.cs` returns zero matches.
    - `grep "Members(" src/Messaging.Contracts/Projections/L2ProjectionKeys.cs` returns zero matches (L2IDX-02 not planned).
  </acceptance_criteria>
  <done>Const + ParentIndex() + 3 no-prefix builders compile; no `string prefix` parameter remains; no member-set builder exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update both forwarders to no-prefix signatures (HARDEN-03)</name>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionKeys.cs (full)
    - src/Orchestrator/Messaging/OrchestratorL2Keys.cs (full)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (the new signatures from Task 1)
  </read_first>
  <action>
    Writer forwarder `src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionKeys.cs` — replace the three forwarders and ADD a `ParentIndex()` forwarder (writer is the only side that SADD/SREMs):
    ```csharp
    public static string ParentIndex() => L2ProjectionKeys.ParentIndex();
    public static string Root(Guid workflowId) => L2ProjectionKeys.Root(workflowId);
    public static string Step(Guid workflowId, Guid stepId) => L2ProjectionKeys.Step(workflowId, stepId);
    public static string Processor(Guid processorId) => L2ProjectionKeys.Processor(processorId);
    ```
    Reader forwarder `src/Orchestrator/Messaging/OrchestratorL2Keys.cs` — drop the prefix param on its sole builder:
    ```csharp
    public static string Root(Guid workflowId) => L2ProjectionKeys.Root(workflowId);
    ```
    Do NOT add a `ParentIndex` forwarder on the reader side (reader performs no L2 writes this phase).
    NOTE: callers of these forwarders (`RedisProjectionWriter`, `RedisL2Cleanup`, `OrchestrationService`,
    the two Orchestrator consumers) will NOT compile after this change — that is expected; those call sites
    are fixed in Plans 02 and 03. This plan deliberately leaves the solution non-building at the consuming
    projects; build only the two changed projects in isolation here.
  </action>
  <verify>
    <automated>dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `RedisProjectionKeys.cs` contains `ParentIndex() => L2ProjectionKeys.ParentIndex()`.
    - `RedisProjectionKeys.cs` contains `Root(Guid workflowId) => L2ProjectionKeys.Root(workflowId)` (no `string prefix`).
    - `OrchestratorL2Keys.cs` contains `Root(Guid workflowId) => L2ProjectionKeys.Root(workflowId)` (no `string prefix`).
    - `grep "string prefix" src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionKeys.cs src/Orchestrator/Messaging/OrchestratorL2Keys.cs` returns zero matches.
  </acceptance_criteria>
  <done>Both forwarders expose parameterless signatures; writer forwarder adds `ParentIndex()`; reader forwarder has only `Root(Guid)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update L2ProjectionKeysTests golden strings</name>
  <read_first>
    - tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs (full — current golden strings lines 21-57, incl. the per-class-prefix fact at 49-57)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (the new signatures)
  </read_first>
  <behavior>
    - Root(wf) golden == $"skp:{wf:D}".
    - Step(wf, step) golden == $"skp:{wf}:{step}".
    - Processor(proc) golden == $"skp:{proc}".
    - ParentIndex() golden == "skp:".
  </behavior>
  <action>
    In `tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs`:
    1. Rewrite all existing facts to call the new parameterless builders (drop the `prefix` arg passed in
       today's tests). Use a fixed GUID in each fact and assert the golden string against the `"skp:"` literal.
       Example: a fact asserting `L2ProjectionKeys.Root(wf)` equals `$"skp:{wf:D}"`.
    2. Add a new fact `ParentIndex_Returns_Bare_Prefix` asserting `L2ProjectionKeys.ParentIndex() == "skp:"`.
    3. DELETE the per-class-prefix fact (the one that fed a configurable/test prefix into the builders,
       ~lines 49-57) — prefix is now a const, not a parameter (D-24).
    Do NOT add any member-set test.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter-class BaseApi.Tests.Features.Orchestration.Projection.L2ProjectionKeysTests</automated>
  </verify>
  <acceptance_criteria>
    - `L2ProjectionKeysTests.cs` contains an assertion that `ParentIndex()` equals `"skp:"`.
    - `L2ProjectionKeysTests.cs` calls `L2ProjectionKeys.Root(` with a single GUID argument (no string-prefix arg).
    - `grep "test:cls-" tests/BaseApi.Tests/Features/Orchestration/Projection/L2ProjectionKeysTests.cs` returns zero matches.
    - The L2ProjectionKeysTests class passes (test command exits 0).
  </acceptance_criteria>
  <done>L2ProjectionKeysTests uses the new signatures, asserts ParentIndex() == "skp:", and the per-class-prefix fact is removed; class is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → key builder | Workflow/processor GUIDs flow into key string construction (in-process, typed `Guid`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `L2ProjectionKeys` key construction | mitigate | Prefix is now a compile-time `const` with NO caller-supplied input feeding it; key segments are strongly-typed `Guid` values rendered via `:D` — no string concatenation of untrusted text. Removing the configurable prefix eliminates any config-injection path into key names. |
| T-22-02 | Information disclosure | builder XML doc / golden tests | accept | Keys contain only GUIDs and the fixed `"skp:"` literal — no secrets, PII, or connection data. Low risk. |
</threat_model>

<verification>
- `dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug` succeeds.
- L2ProjectionKeysTests class is GREEN.
- `grep -rn "string prefix" src/Messaging.Contracts/Projections/L2ProjectionKeys.cs src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionKeys.cs src/Orchestrator/Messaging/OrchestratorL2Keys.cs` returns zero matches.
- NOTE: the full solution does NOT build at the end of this plan (consuming call sites are fixed in Plans 02/03) — this is expected and acceptable mid-wave.
</verification>

<success_criteria>
- `L2ProjectionKeys.Prefix` is a `const string = "skp:"`; `ParentIndex()` returns it verbatim.
- All builders are parameterless (no `string prefix`); HARDEN-03 single source of truth preserved.
- Both forwarders updated in lockstep; writer side has `ParentIndex()`.
- L2ProjectionKeysTests golden strings updated and GREEN; no member-set builder/test exists (L2IDX-02 dropped).
</success_criteria>

<output>
After completion, create `.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-01-SUMMARY.md`
</output>
