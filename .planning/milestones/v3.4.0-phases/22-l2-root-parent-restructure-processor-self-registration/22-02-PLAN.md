---
phase: 22-l2-root-parent-restructure-processor-self-registration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/Orchestrator/Messaging/OrchestratorRedisOptions.cs
  - src/Orchestrator/Program.cs
  - src/Orchestrator/Consumers/StartOrchestrationConsumer.cs
  - src/Orchestrator/Consumers/StopOrchestrationConsumer.cs
  - src/Orchestrator/appsettings.json
autonomous: true
requirements: [L2PREFIX-01]
must_haves:
  truths:
    - "The orchestrator reader constructs L2 keys with no configurable prefix — it calls OrchestratorL2Keys.Root(wf) directly."
    - "No Redis:KeyPrefix config or OrchestratorRedisOptions type remains on the reader side."
  artifacts:
    - path: "src/Orchestrator/Consumers/StartOrchestrationConsumer.cs"
      provides: "no-prefix L2 root read"
      contains: "OrchestratorL2Keys.Root(workflowId)"
    - path: "src/Orchestrator/Consumers/StopOrchestrationConsumer.cs"
      provides: "no-prefix L2 root read"
      contains: "OrchestratorL2Keys.Root(workflowId)"
  key_links:
    - from: "StartOrchestrationConsumer.cs"
      to: "OrchestratorL2Keys.Root"
      via: "Redis StringGet key construction"
      pattern: "OrchestratorL2Keys\\.Root\\(workflowId\\)"
---

<objective>
Remove the configurable key-prefix from the entire Orchestrator (reader) side: delete the
`OrchestratorRedisOptions` record (its only field was the prefix), drop its DI registration in
`Program.cs`, drop the ctor param from both consumers, switch their L2 root reads to the
parameterless `OrchestratorL2Keys.Root(workflowId)` forwarder (from Plan 01), and remove the now-empty
`Redis` config section from the Orchestrator `appsettings.json`.

Purpose: Implements the reader-side half of L2PREFIX-01. This file set is fully disjoint from the
writer/service-side plan (03), so the two run in parallel in Wave 2.

Output: Reader side compiles against the no-prefix `OrchestratorL2Keys.Root(Guid)`; zero `Redis:KeyPrefix`
reads on the reader side; `OrchestratorRedisOptions` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-SPEC.md
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-CONTEXT.md
@.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-PATTERNS.md

<interfaces>
<!-- From Plan 01 (Wave 1), the reader forwarder is now: -->
public static string Root(Guid workflowId) => L2ProjectionKeys.Root(workflowId);  // OrchestratorL2Keys.cs

<!-- Current consumer call sites (both consumers): -->
<!-- StartOrchestrationConsumer.cs:33  db.StringGetAsync(OrchestratorL2Keys.Root(options.KeyPrefix, workflowId)) -->
<!-- StopOrchestrationConsumer.cs:29   db.StringGetAsync(OrchestratorL2Keys.Root(options.KeyPrefix, workflowId)) -->
<!-- Both take `OrchestratorRedisOptions options` as a primary-ctor param. -->
<!-- Program.cs:18-20 registers AddSingleton(new OrchestratorRedisOptions(Configuration["Redis:KeyPrefix"] ?? "skp:")). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop OrchestratorRedisOptions from both consumers and switch to no-prefix Root</name>
  <read_first>
    - src/Orchestrator/Consumers/StartOrchestrationConsumer.cs (full — primary ctor param at line 24, call site at line 33)
    - src/Orchestrator/Consumers/StopOrchestrationConsumer.cs (full — primary ctor param at line 20, call site at line 29)
    - src/Orchestrator/Messaging/OrchestratorL2Keys.cs (the no-prefix Root(Guid) from Plan 01)
  </read_first>
  <action>
    In `src/Orchestrator/Consumers/StartOrchestrationConsumer.cs`:
    1. Remove the `OrchestratorRedisOptions options` primary-ctor parameter (line 24). The ctor becomes
       `StartOrchestrationConsumer(IConnectionMultiplexer redis, ILogger<StartOrchestrationConsumer> logger)`.
    2. Change the line-33 read from `db.StringGetAsync(OrchestratorL2Keys.Root(options.KeyPrefix, workflowId))`
       to `db.StringGetAsync(OrchestratorL2Keys.Root(workflowId))`.
    3. Remove `using Orchestrator.Messaging;` ONLY if `OrchestratorL2Keys` is still referenced via that
       namespace — `OrchestratorL2Keys` lives in `Orchestrator.Messaging`, so KEEP the using.
    Apply the identical change to `src/Orchestrator/Consumers/StopOrchestrationConsumer.cs` (ctor param at
    line 20, call site at line 29 → `OrchestratorL2Keys.Root(workflowId)`).
  </action>
  <verify>
    <automated>dotnet build src/Orchestrator/Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `StartOrchestrationConsumer.cs` contains `OrchestratorL2Keys.Root(workflowId)` and NOT `options.KeyPrefix`.
    - `StopOrchestrationConsumer.cs` contains `OrchestratorL2Keys.Root(workflowId)` and NOT `options.KeyPrefix`.
    - `grep "OrchestratorRedisOptions" src/Orchestrator/Consumers/` returns zero matches.
  </acceptance_criteria>
  <done>Both consumers read L2 roots with no prefix arg and no longer take OrchestratorRedisOptions.</done>
</task>

<task type="auto">
  <name>Task 2: Delete OrchestratorRedisOptions + its registration + Orchestrator Redis config</name>
  <read_first>
    - src/Orchestrator/Messaging/OrchestratorRedisOptions.cs (full — record(string KeyPrefix), prefix is its only field)
    - src/Orchestrator/Program.cs (lines 1-30 — the AddSingleton at 18-20 + usings)
    - src/Orchestrator/appsettings.json (full — the `"Redis": { "KeyPrefix": "skp:" }` section at lines 20-22)
  </read_first>
  <action>
    1. DELETE the file `src/Orchestrator/Messaging/OrchestratorRedisOptions.cs` entirely (its only purpose
       was the prefix; D-07).
    2. In `src/Orchestrator/Program.cs`: delete lines 18-20 (the comment + `builder.Services.AddSingleton(
       new OrchestratorRedisOptions(builder.Configuration["Redis:KeyPrefix"] ?? "skp:"));`). Remove
       `using Orchestrator.Messaging;` (line 7) ONLY if no other symbol from that namespace is referenced
       in Program.cs — if `StartOrchestrationConsumerDefinition`/consumers come from `Orchestrator.Consumers`
       (they do, line 6), and nothing else uses `Orchestrator.Messaging`, remove the line-7 using.
    3. In `src/Orchestrator/appsettings.json`: remove the entire `"Redis": { "KeyPrefix": "skp:" }` section
       (lines 20-22) — the `ConnectionStrings:Redis` entry (lines 13-15) carries the connection, so the whole
       `Redis` section is removable. Ensure the resulting JSON is valid (no trailing comma left behind).
  </action>
  <verify>
    <automated>dotnet build src/Orchestrator/Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `src/Orchestrator/Messaging/OrchestratorRedisOptions.cs` does NOT exist.
    - `grep -r "OrchestratorRedisOptions" src/Orchestrator/` returns zero matches.
    - `src/Orchestrator/appsettings.json` does NOT contain `KeyPrefix`.
    - `src/Orchestrator/appsettings.json` parses as valid JSON.
    - `src/Orchestrator/Orchestrator.csproj` builds clean (0 warnings, 0 errors).
  </acceptance_criteria>
  <done>OrchestratorRedisOptions deleted, its registration removed from Program.cs, and the Orchestrator Redis config section gone; Orchestrator project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis → consumer | Externally-stored L2 root JSON is GET-then-deserialized by the consumers (untrusted at rest) |
| config → key construction | Previously `Redis:KeyPrefix` config influenced key names; this plan removes that path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | reader key construction | mitigate | Removing `OrchestratorRedisOptions`/`Redis:KeyPrefix` eliminates the last config-driven input into reader key names; the prefix is now a compile-time const (Plan 01). No caller can influence the constructed key. |
| T-22-04 | Information disclosure | consumer deserialize of L2 root | accept | Behavior unchanged this plan — the consumer already `Deserialize<WorkflowRootProjection>`s the root and logs only the WorkflowId. No new surface introduced; out-of-scope reload/error-shape is Phase 23. |
</threat_model>

<verification>
- `dotnet build src/Orchestrator/Orchestrator.csproj -c Debug` succeeds (0 warnings, 0 errors).
- `grep -rn "OrchestratorRedisOptions" src/Orchestrator/` returns zero matches.
- `grep -rn "KeyPrefix" src/Orchestrator/` returns zero matches.
- Orchestrator `appsettings.json` is valid JSON with no `Redis` section.
</verification>

<success_criteria>
- Reader side constructs L2 keys via the parameterless `OrchestratorL2Keys.Root(Guid)`.
- `OrchestratorRedisOptions` type deleted; no DI registration; no config key.
- Orchestrator project builds clean.
</success_criteria>

<output>
After completion, create `.planning/phases/22-l2-root-parent-restructure-processor-self-registration/22-02-SUMMARY.md`
</output>
