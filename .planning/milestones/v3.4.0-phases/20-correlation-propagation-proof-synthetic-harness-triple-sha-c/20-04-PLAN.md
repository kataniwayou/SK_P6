---
phase: 20-correlation-propagation-proof-synthetic-harness-triple-sha-c
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - scripts/phase-20-close.ps1
  - scripts/phase-20-close.sh
autonomous: false
requirements: [TEST-RMQ-04, TEST-RMQ-05]
user_setup: []
must_haves:
  truths:
    - "phase-20-close.{ps1,sh} capture a third SHA-256 snapshot of `rabbitmqctl -q list_queues name` (sorted) alongside psql \\l and redis-cli --scan, asserting BEFORE==AFTER"
    - "All three close-gate smells are fixed: unparseable fact-count is a HARD failure (no -1 false-green), `compose ps --format json` tolerates object-OR-array, and PS1/SH share ONE canonical Phase-20 service list"
    - "The canonical service list includes rabbitmq, orchestrator, and baseapi-service (now healthy via the D-12 wget fix)"
    - "The gate runs 3× consecutive GREEN with the full Docker stack up (including TEST-RMQ-02) and all three SHA invariants hold"
  artifacts:
    - path: "scripts/phase-20-close.ps1"
      provides: "Triple-SHA PowerShell close gate"
      contains: "rabbitmqctl"
    - path: "scripts/phase-20-close.sh"
      provides: "Triple-SHA bash close gate"
      contains: "rabbitmqctl"
  key_links:
    - from: "phase-20-close.ps1"
      to: "sk-rabbitmq queue list"
      via: "docker exec sk-rabbitmq rabbitmqctl -q list_queues name"
      pattern: "rabbitmqctl -q list_queues name"
    - from: "phase-20-close.sh"
      to: "sk-rabbitmq queue list"
      via: "docker exec sk-rabbitmq rabbitmqctl -q list_queues name"
      pattern: "rabbitmqctl -q list_queues name"
---

<objective>
Extend the dual-SHA close gate to a TRIPLE-SHA gate (add the rabbitmqctl queue-list arm), fix the three deferred-at-v3.3.0 close-gate smells, and run the milestone-closing 3×-consecutive-GREEN gate with the full Docker stack up (so TEST-RMQ-02 runs live) — asserting psql + redis + rabbitmq snapshots all hold BEFORE==AFTER.

Purpose: TEST-RMQ-05 (triple-SHA leak gate) and TEST-RMQ-04 (net-zero queue leak from temporary/auto-delete per-class endpoints) are proven; the v3.4.0 milestone closes cleanly with a de-smelled gate.
Output: 2 new scripts (phase-20-close.ps1 + .sh) + the operator-approved 3×GREEN evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-CONTEXT.md
@.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-RESEARCH.md
@.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-PATTERNS.md

<interfaces>
<!-- The two scripts to copy + extend (read in full this session). -->

scripts/phase-17-close.ps1 — dual-SHA PowerShell gate:
- Pre-flight service list (:34): postgres, redis, otel-collector, elasticsearch, prometheus
- compose ps --format json read (:36): (... | ConvertFrom-Json).Health  — reads .Health off an OBJECT
- redis BEFORE arm (:54-58); AFTER arm (:132-136); redis invariant block (:150-157)
- fact-count parse (:96-97): -1 sentinel + a WARNING

scripts/phase-16-close.sh — dual-SHA bash gate:
- Pre-flight service list (:30): postgres redis elasticsearch prometheus  — MISSES otel-collector
- compose ps --format json read (:31): jq -r '.[].Health'  — reads .Health off an ARRAY
- redis BEFORE arm (:47-49); AFTER arm (:83-85); redis invariant block (:99-107)
- fact-count parse (:64): `|| echo "-1"`  — coerces unparseable to -1 (false-green if all 3 fail to parse)
- Note: phase-16-close.sh also has Phase-16-specific EF-migration + HEALTH-immutable arms (:109-127);
  phase-17-close.ps1 stripped those. The Phase-20 scripts should NOT carry the Phase-16 EF/HEALTH arms.

D-09 rabbitmq arm to ADD:
  PS1: docker exec sk-rabbitmq rabbitmqctl -q list_queues name | Sort-Object -CaseSensitive
  SH:  docker exec sk-rabbitmq rabbitmqctl -q list_queues name | LC_ALL=C sort
  Use -q (quiet) so the "Listing queues ..." header does not pollute the SHA (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase-20-close.ps1 — copy phase-17-close.ps1, add the rabbitmq SHA arm, fix all three smells</name>
  <read_first>
    - scripts/phase-17-close.ps1 (FULL — the dual-SHA PowerShell gate to copy + extend; note the redis arm :54-58/:132-136, invariant block :150-157, service list :34, --format json read :36, fact-count -1 :96-97)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-RESEARCH.md (Pattern 4, Code Example 5 smell fixes, Pitfall 3, D-09 net-zero reasoning)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-PATTERNS.md (phase-20-close.ps1 section + the three smell fixes A/B/C)
    - compose.yaml (confirm container name sk-rabbitmq + the v3.4.0 service set)
  </read_first>
  <action>
    Copy scripts/phase-17-close.ps1 → scripts/phase-20-close.ps1, retitle the header to "Phase 20 close gate — v3.4.0 (triple-SHA)", and make these changes:

    1. D-09 RABBITMQ ARM (triple-SHA). Mirror the redis arm in BOTH the BEFORE block (after :58) and the
       AFTER block (after :136). Use -q to strip the header (Pitfall 3):
    ```powershell
    $beforeRmq = (docker exec sk-rabbitmq rabbitmqctl -q list_queues name | Sort-Object -CaseSensitive | Out-String).Trim()
    $beforeRmqHash = (Get-FileHash -Algorithm SHA256 -InputStream (
        [IO.MemoryStream]::new([Text.Encoding]::UTF8.GetBytes($beforeRmq)))).Hash.ToLower()
    Write-Host "  rabbitmqctl list_queues SHA-256 BEFORE = $beforeRmqHash" -ForegroundColor Gray
    ```
    (and the symmetric `$afterRmq`/`$afterRmqHash` in the AFTER block). Add a THIRD invariant-assertion
    block mirroring the redis invariant (:150-157):
    ```powershell
    if ($beforeRmqHash -ne $afterRmqHash) {
        Write-Host "TEST-RMQ-04/05 INVARIANT VIOLATED: rabbitmqctl list_queues SHA-256 BEFORE != AFTER" -ForegroundColor Red
        Write-Host "  BEFORE = $beforeRmqHash" -ForegroundColor Red
        Write-Host "  AFTER  = $afterRmqHash" -ForegroundColor Red
        $allGood = $false
    } else {
        Write-Host "TEST-RMQ-04/05 invariant HELD: rabbitmqctl list_queues SHA-256 = $afterRmqHash" -ForegroundColor Green
    }
    ```

    2. SMELL A (-1 fact-count false-green): replace the `-1` sentinel + WARNING (:96-97) with a HARD failure —
       if `Passed:\s+(\d+)` does NOT match, write an error and `exit 1` (do NOT coerce to -1). An unparseable
       count must never reach the 3-GREEN equality check.

    3. SMELL B (compose ps --format json object-vs-array): the `(... | ConvertFrom-Json).Health` read (:36)
       assumes an OBJECT. Make it tolerate object-OR-array, e.g.:
    ```powershell
    $parsed = docker compose ps $svc --format json | ConvertFrom-Json
    $health = if ($parsed -is [array]) { $parsed[0].Health } else { $parsed.Health }
    ```

    4. SMELL C (service-list divergence): replace the :34 list with the ONE canonical Phase-20 list:
       `postgres, redis, rabbitmq, otel-collector, elasticsearch, prometheus, orchestrator, baseapi-service`.
       Keep the existing otel-collector special-case (it has no healthcheck — allowed non-healthy). The D-12
       wget fix (Plan 20-01) makes baseapi-service report healthy; the gate now requires it healthy.

    5. Drop the Phase-17 fact-count baseline WARNING tied to 235 (the v3.3.0 baseline) — the Phase 20 suite
       adds new facts. Keep the 3-GREEN equality check (all three runs identical Passed count); replace the
       235-comparison with a neutral informational print of the observed count.

    6. Update the final operator-evidence print + the closing "append to STATE.md" line to reference Phase 20
       and the three SHA values. Keep the dotnet clean → build (Release+Debug, zero-warning) → 3× `dotnet test`
       (full suite, no `Category!=RealStack` filter — TEST-RMQ-02 MUST run with the stack up) scaffold.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/phase-20-close.ps1).Path, [ref]$null, [ref]$null); if ($?) { 'parse-ok' }"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase-20-close.ps1 exists and contains `rabbitmqctl -q list_queues name`
    - The rabbitmq arm appears in BOTH a BEFORE and an AFTER position (grep count of `rabbitmqctl -q list_queues name` >= 2)
    - A third invariant block compares `$beforeRmqHash` to `$afterRmqHash`
    - SMELL A: no `-1` fact-count sentinel remains as a passing fallback — an unparseable count calls `exit 1`
    - SMELL B: the compose-ps Health read tolerates object-OR-array (grep for `-is [array]` or equivalent)
    - SMELL C: the service list contains `rabbitmq`, `orchestrator`, AND `baseapi-service`
    - The script parses without syntax error (the ParseFile verify prints parse-ok)
  </acceptance_criteria>
  <done>phase-20-close.ps1 is a triple-SHA gate with all three smells fixed and the canonical Phase-20 service list including the now-healthy baseapi-service.</done>
</task>

<task type="auto">
  <name>Task 2: phase-20-close.sh — copy phase-16-close.sh, add the rabbitmq SHA arm, fix all three smells, drop Phase-16-specific arms</name>
  <read_first>
    - scripts/phase-16-close.sh (FULL — the dual-SHA bash gate; redis arm :47-49/:83-85, invariant :99-107, service list :30, jq read :31, -1 fallback :64, Phase-16 EF/HEALTH arms :109-127 to DROP)
    - scripts/phase-20-close.ps1 (the PS1 written in Task 1 — keep the two scripts behaviorally identical)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-RESEARCH.md (Pattern 4, Code Example 5, Pitfall 3)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-PATTERNS.md (phase-20-close.sh section)
  </read_first>
  <action>
    Copy scripts/phase-16-close.sh → scripts/phase-20-close.sh, retitle the header to "Phase 20 close gate —
    v3.4.0 (triple-SHA)", and make these changes (keep behavior identical to the PS1 from Task 1):

    1. D-09 RABBITMQ ARM. Mirror the redis arm in BOTH BEFORE (after :49) and AFTER (after :85). Use -q
       (Pitfall 3):
    ```bash
    BEFORE_RMQ=$(docker exec sk-rabbitmq rabbitmqctl -q list_queues name | LC_ALL=C sort)
    BEFORE_RMQ_HASH=$(printf '%s' "$BEFORE_RMQ" | sha256sum | awk '{print $1}')
    echo "  rabbitmqctl list_queues SHA-256 BEFORE = $BEFORE_RMQ_HASH"
    ```
    (and symmetric AFTER_RMQ / AFTER_RMQ_HASH). Add a third invariant block mirroring the redis invariant
    (:99-107):
    ```bash
    if [[ "$BEFORE_RMQ_HASH" != "$AFTER_RMQ_HASH" ]]; then
        echo "TEST-RMQ-04/05 INVARIANT VIOLATED: rabbitmqctl list_queues SHA-256 BEFORE != AFTER" >&2
        echo "  BEFORE = $BEFORE_RMQ_HASH" >&2
        echo "  AFTER  = $AFTER_RMQ_HASH" >&2
        ALL_GOOD=0
    else
        echo "TEST-RMQ-04/05 invariant HELD: rabbitmqctl list_queues SHA-256 = $AFTER_RMQ_HASH"
    fi
    ```

    2. SMELL A (-1 false-green at :64): replace `|| echo "-1"` with a hard failure — if the Passed count
       cannot be parsed, echo an error to stderr and `exit 1`. Never coerce to -1.

    3. SMELL B (jq array-only at :31): `jq -r '.[].Health'` assumes an ARRAY. Make it tolerate object-OR-array:
       `jq -r '(.[0].Health // .Health)'` (with the existing `2>/dev/null || echo "missing"` guard).

    4. SMELL C (service list at :30 misses otel-collector): replace with the ONE canonical Phase-20 list,
       identical to the PS1: `postgres redis rabbitmq otel-collector elasticsearch prometheus orchestrator
       baseapi-service`. Add the otel-collector "allowed non-healthy" special-case the PS1 has (skip the
       healthy assertion for otel-collector — it has no healthcheck). baseapi-service must be healthy (D-12).

    5. DROP the Phase-16-specific arms (:109-127): the EF-migration negative assertion and the
       HEALTH-01..05 byte-immutable assertion are Phase-16-specific and NOT applicable to Phase 20 (mirrors
       how phase-17-close.ps1 stripped them). Remove both blocks.

    6. Keep the build/test scaffold; run the FULL suite (no Category filter) so TEST-RMQ-02 runs with the
       stack up. Update the closing operator-evidence echoes to reference Phase 20 + the three SHA values.
  </action>
  <verify>
    <automated>bash -n scripts/phase-20-close.sh && echo parse-ok</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase-20-close.sh exists and contains `rabbitmqctl -q list_queues name` (>= 2 occurrences: BEFORE + AFTER)
    - A third invariant block compares `$BEFORE_RMQ_HASH` to `$AFTER_RMQ_HASH`
    - SMELL A: no `|| echo "-1"` fallback remains; unparseable count calls `exit 1`
    - SMELL B: the jq Health read is `(.[0].Health // .Health)` (object-or-array tolerant)
    - SMELL C: the service list contains `rabbitmq`, `otel-collector`, `orchestrator`, AND `baseapi-service`
    - The Phase-16 EF-migration + HEALTH-immutable blocks are removed (grep for `NEW_MIGRATIONS` and `HEALTH_DIFF` == 0)
    - `bash -n scripts/phase-20-close.sh` exits 0 (parse-ok)
  </acceptance_criteria>
  <done>phase-20-close.sh mirrors the PS1 triple-SHA gate exactly, with all three smells fixed and the Phase-16-specific arms dropped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the triple-SHA close gate 3× GREEN with the full Docker stack (operator checkpoint)</name>
  <read_first>
    - scripts/phase-20-close.ps1 (the gate written in Task 1)
    - scripts/phase-20-close.sh (the gate written in Task 2)
    - .planning/STATE.md (the prior phase close entries showing how 3×GREEN evidence is recorded)
    - compose.yaml (the full v3.4.0 service set + the phase-8 profile)
  </read_first>
  <what-built>
    Both phase-20-close scripts (triple-SHA: psql + redis + rabbitmq, BEFORE==AFTER, 3× consecutive GREEN,
    all three smells fixed) and the full Phase 20 test suite (FanOut, OutboundFilterSynthetic,
    HealthDeadRabbit, CorrelationPropagationE2E + the D-13 fixes). The baseapi-service image carries the
    D-12 wget fix so it reports healthy.
  </what-built>
  <action>
    Claude AUTOMATES everything it can BEFORE pausing for the operator:
    1. Rebuild the baseapi-service image so the D-12 wget layer is live: `docker compose build baseapi-service`.
    2. Bring the full v3.4.0 stack up healthy (postgres, redis, rabbitmq, otel-collector, elasticsearch,
       prometheus, orchestrator, baseapi-service): `docker compose --profile phase-8 up -d` (or the project's
       documented full-stack up command — read how prior phases brought the stack up; include the orchestrator
       + rabbitmq tier).
    3. Poll `docker compose ps` until all healthchecks report healthy (otel-collector is allowed non-healthy —
       no healthcheck).
    4. Run the gate: `pwsh -File scripts/phase-20-close.ps1` — zero-warning build (both configs), 3× full-suite
       `dotnet test` (TEST-RMQ-02 runs live with the stack up), triple-SHA BEFORE==AFTER. Exit 0 = PASS.
    5. If the gate fails on a known transient (ES cold-start indexing latency on TEST-RMQ-02, or a slow
       auto-delete queue lingering — A5), apply the documented mitigation (re-run / short settle delay before
       the AFTER rabbitmq snapshot) and re-run.
    THEN pause for the operator to confirm the gate result.
  </action>
  <how-to-verify>
    The OPERATOR confirms:
    - The gate printed "Phase 20 close gate PASSED."
    - 3× consecutive runs reported the SAME Passed count (no divergence).
    - All THREE SHA invariants HELD: psql \l, redis-cli --scan, rabbitmqctl list_queues — BEFORE == AFTER.
    - TEST-RMQ-02 (CorrelationPropagationE2ETests) was included in the suite and GREEN.
  </how-to-verify>
  <verify>
    <automated>pwsh -File scripts/phase-20-close.ps1</automated>
  </verify>
  <done>The triple-SHA close gate exits 0: zero-warning build both configs, 3× identical Passed count, all three SHA invariants BEFORE==AFTER, TEST-RMQ-02 GREEN live; operator approves with the three SHA values + Passed count.</done>
  <resume-signal>Type "approved" with the three SHA values + Passed count, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close-gate script → running containers | `docker exec` / `docker compose exec` reach into postgres, redis, rabbitmq for snapshots |
| rabbitmqctl output → SHA-256 | queue-name list hashed as an integrity snapshot (not security crypto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering | the `-1` fact-count false-green smell (Smell A) | mitigate | Smell A fix converts an unparseable count to a HARD failure (exit 1) in BOTH scripts — closing the aliasing bug where three failed parses (-1==-1==-1) silently green the 3-GREEN gate. This is itself a verification-integrity hardening. |
| T-20-10 | Information disclosure | rabbitmqctl / psql / redis-cli output captured by the script | accept | Output is queue/db/key NAMES only (`list_queues name`, `psql -lqt`, `redis-cli --scan`) — no payloads, no credentials; hashed locally, never transmitted. Dev-only host execution. |
| T-20-11 | Spoofing | dev-only guest/guest broker creds on the live stack during the gate | accept | Local-dev posture (T-19-broker-creds); the gate runs on the developer host against the compose stack; prod/k8s override creds. Not a HIGH threat. |

**No HIGH-severity threats.** The close gate is a host-side integrity-snapshot tool over dev containers; the one genuine integrity bug (the `-1` false-green) is FIXED by this plan, strengthening the gate.
</threat_model>

<verification>
- Both scripts parse cleanly (PS1 ParseFile, `bash -n`).
- The full Docker stack comes up healthy (baseapi-service healthy via D-12).
- `phase-20-close.ps1` exits 0: zero-warning build both configs, 3× identical Passed count, all three SHA invariants BEFORE==AFTER, TEST-RMQ-02 GREEN in the suite.
</verification>

<success_criteria>
- TEST-RMQ-05: triple-SHA gate (psql + redis + rabbitmq) runs 3× consecutive GREEN with BEFORE==AFTER on all three snapshots; all three smells fixed.
- TEST-RMQ-04: rabbitmq BEFORE==AFTER proves net-zero queue leak (temporary/auto-delete per-class endpoints + the stable orchestrator-{InstanceId} queue present in both snapshots).
- The milestone v3.4.0 closes cleanly with a de-smelled gate and the full stack genuinely healthy.
</success_criteria>

<output>
After completion, create `.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-04-SUMMARY.md`. Record the three SHA values (psql / redis / rabbitmq, BEFORE==AFTER), the 3× Passed count, the full-stack up command used, and any A5 settle-delay applied.
</output>
