---
phase: 20-correlation-propagation-proof-synthetic-harness-triple-sha-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Orchestrator/Consumers/StopOrchestrationConsumer.cs
  - tests/BaseApi.Tests/Orchestrator/StartStopConsumerAckTests.cs
  - src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs
  - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs
  - tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs
  - tests/BaseApi.Tests/Composition/HarnessWebAppFactory.cs
  - Dockerfile
autonomous: true
requirements: [CORR-04, TEST-RMQ-02]
must_haves:
  truths:
    - "Stop consumer logs a distinct 'Scheduler job stop (seam)' string, not the Start string"
    - "AddBaseApiMessaging reads RabbitMq:Port (default 5672) so the in-process WebApi can target host port 5673"
    - "OrchestrationService logs 'Published StartOrchestration CorrelationId={guid}' at Information level with the minted body Guid"
    - "A HarnessWebAppFactory swaps the real bus for the in-memory MassTransit test harness so /start|/stop publish in-process (204)"
    - "The root Dockerfile installs wget in the runtime stage before USER app"
  artifacts:
    - path: "src/Orchestrator/Consumers/StopOrchestrationConsumer.cs"
      provides: "Corrected Stop seam log string"
      contains: "Scheduler job stop (seam)"
    - path: "src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs"
      provides: "RabbitMq:Port config read"
      contains: "RabbitMq:Port"
    - path: "src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs"
      provides: "Publish-side correlation log + ILogger injection"
      contains: "Published StartOrchestration CorrelationId"
    - path: "tests/BaseApi.Tests/Composition/HarnessWebAppFactory.cs"
      provides: "In-memory MassTransit harness swap on the integration host"
      contains: "AddMassTransitTestHarness"
    - path: "Dockerfile"
      provides: "wget install in runtime stage"
      contains: "apt-get install -y --no-install-recommends wget"
  key_links:
    - from: "tests/BaseApi.Tests/Composition/HarnessWebAppFactory.cs"
      to: "MassTransit in-memory transport"
      via: "RemoveMassTransit() then AddMassTransitTestHarness() in ConfigureTestServices"
      pattern: "RemoveMassTransit\\(\\)"
    - from: "src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs"
      to: "RabbitMQ host:port"
      via: "busCfg.Host with port"
      pattern: "RabbitMq:Port"
---

<objective>
Land all source-side and test-infrastructure prerequisites that the Phase 20 proof tests and close gate depend on, and resolve the Wave-0 open questions (A1 RemoveMassTransit surface, A3/OQ#1 RabbitMq:Port). This plan produces NO new runtime behavior beyond test-enablement: a port-config read, one publish-side log line, a copy-paste-bug fix, a Dockerfile layer, and the in-memory harness factory.

Purpose: Unblock Plan 20-02 (hermetic in-memory tests need the harness factory + port read), Plan 20-03 (real-stack ES proof needs RabbitMq:Port + the D-07 publish log + the corrected D-13 Stop string), and Plan 20-04 (close gate needs a healthy baseapi-service via wget).
Output: 1 source consumer fix (D-13), 1 test assertion fix (D-13), 1 messaging-extension port read (OQ#1), 1 OrchestrationService publish-log + ctor change (D-07) + its caller fix, 1 new HarnessWebAppFactory (D-01), 1 Dockerfile wget layer (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-CONTEXT.md
@.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-RESEARCH.md
@.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-PATTERNS.md

<interfaces>
<!-- Verified from the live repo this session — executor should use these directly. -->

OrchestrationService internal ctor (src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs:69-95)
currently injects NO ILogger. Fields are declared at :51-62. The Start publish call is at :163-165:
```csharp
await _publishEndpoint.Publish(
    new StartOrchestration(workflowIds.ToArray()) { CorrelationId = NewId.NextGuid() }, ct);
```
The internal ctor is constructed directly by BuildService(...) in
tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs:104-119 (via InternalsVisibleTo).

AddBaseApiMessaging (src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs:40-65)
reads RabbitMq:Host/Username/Password only (no port); the host bind is:
```csharp
busCfg.Host(host, h => { h.Username(user); h.Password(pass); });
```

StopOrchestrationConsumer.cs:42 (the IN-02 mislog):
```csharp
logger.LogInformation("Scheduler job start (seam) for {WorkflowId}", workflowId);
```

Phase8WebAppFactory is the integration test host base; WebAppFactory.cs:37-52 demonstrates the
ConfigureTestServices seam (runs AFTER Program.cs's AddBaseApiMessaging → AddMassTransit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the D-13 Stop seam mislog + its locked assertion, and add the D-07 publish-side correlation log</name>
  <read_first>
    - src/Orchestrator/Consumers/StopOrchestrationConsumer.cs (current state — the mislog is at line 42)
    - src/Orchestrator/Consumers/StartOrchestrationConsumer.cs (the CORRECT Start seam string this must stay distinct from)
    - tests/BaseApi.Tests/Orchestrator/StartStopConsumerAckTests.cs (the line-217 Stop assertion to fix; line ~156 is the Start test — DO NOT touch it)
    - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs (ctor :69-95, fields :51-62, Start publish :163-165)
    - tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs (BuildService helper :104-119 — constructs the internal ctor directly)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-PATTERNS.md (sections for StopOrchestrationConsumer + OrchestrationService edits)
  </read_first>
  <action>
    THREE edits, all source/test corrections (D-13 + D-07):

    1. D-13 Stop seam fix — src/Orchestrator/Consumers/StopOrchestrationConsumer.cs:42. Change the WRONG line:
       `logger.LogInformation("Scheduler job start (seam) for {WorkflowId}", workflowId);`
       to the corrected, distinct Stop vocabulary (OQ#3 confirmed wording):
       `logger.LogInformation("Scheduler job stop (seam) for {WorkflowId}", workflowId);`
       This is a Stop consumer that was copy-pasting the Start string. The Stop string MUST stay distinct from the Start seam string so it does not muddy the TEST-RMQ-02 ES proof.

    2. D-13 assertion fix — tests/BaseApi.Tests/Orchestrator/StartStopConsumerAckTests.cs line ~217 (inside Stop_Present_In_L2_Logs_Seam_And_Writes_Nothing). Change:
       `Assert.Contains(logs.Messages, m => m.Contains("Scheduler job start"));`
       to:
       `Assert.Contains(logs.Messages, m => m.Contains("Scheduler job stop"));`
       CRITICAL: do NOT change the line ~156 assertion in the Start_Present_... test — that one correctly asserts "Scheduler job start".

    3. D-07 publish-side log — src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs.
       a. Add a field beside the others (:51-62): `private readonly ILogger<OrchestrationService> _logger;`
       b. Add `ILogger<OrchestrationService> logger` as a parameter to the internal ctor (:69-81) and assign with the same null-guard style: `_logger = logger ?? throw new ArgumentNullException(nameof(logger));`. Add `using Microsoft.Extensions.Logging;` if not present.
       c. At the Start publish call (:163-165), capture the minted Guid into a local then log AFTER publish at Information level (symmetry with the orchestrator seam log so both ES docs land at the same level):
       ```csharp
       var startCorr = NewId.NextGuid();
       await _publishEndpoint.Publish(
           new StartOrchestration(workflowIds.ToArray()) { CorrelationId = startCorr }, ct);
       _logger.LogInformation("Published StartOrchestration CorrelationId={CorrelationId}", startCorr);
       ```
       d. Update the caller helper tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs:104-119 BuildService(...) to pass a logger arg: `NullLogger<OrchestrationService>.Instance` (add `using Microsoft.Extensions.Logging.Abstractions;`). DI in Program.cs resolves ILogger automatically — no Program.cs change needed.

    Do NOT add a Stop publish-side log (D-07 names only Start).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter-class *StartStopConsumerAckTests</automated>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter-class *OrchestrationServicePublishTests</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Scheduler job stop (seam)" src/Orchestrator/Consumers/StopOrchestrationConsumer.cs == 1
    - grep -c "Scheduler job start (seam)" src/Orchestrator/Consumers/StopOrchestrationConsumer.cs == 0
    - StartStopConsumerAckTests.cs contains exactly one occurrence of `m.Contains("Scheduler job stop")` AND still contains `m.Contains("Scheduler job start")` (the Start test, unchanged)
    - OrchestrationService.cs contains `_logger.LogInformation("Published StartOrchestration CorrelationId={CorrelationId}", startCorr)`
    - OrchestrationService.cs internal ctor signature includes `ILogger<OrchestrationService> logger`
    - OrchestrationServicePublishTests.cs BuildService passes `NullLogger<OrchestrationService>.Instance`
    - dotnet build -c Release exits 0 (zero-warning under TreatWarningsAsErrors)
    - both filtered test classes exit 0
  </acceptance_criteria>
  <done>Stop consumer logs the distinct Stop string, its locked assertion tracks it, the Start test is untouched, OrchestrationService logs the minted body Guid at the publish seam, and the direct-ctor caller compiles with the new logger arg.</done>
</task>

<task type="auto">
  <name>Task 2: Add RabbitMq:Port read to AddBaseApiMessaging (OQ#1 / A3) so the in-process WebApi can reach host broker 5673</name>
  <read_first>
    - src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs (full — the Host bind is at :57-61, host read at :43)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-RESEARCH.md (Open Question #1, Pitfall 5, Pattern 3 — the 5673-vs-5672 gap)
    - compose.yaml (rabbitmq tier ~line 161-167 — host AMQP port 5673 maps to container 5672; the orchestrator/baseapi-service inside compose use rabbitmq:5672)
  </read_first>
  <action>
    OQ#1 resolution — add a port-aware host bind to AddBaseApiMessaging. This is TEST-ENABLEMENT, not new runtime behavior: the default MUST stay 5672 so the compose-internal `rabbitmq:5672` path (used by the running containers) is byte-unaffected; only the in-process TEST WebApi will set RabbitMq__Port=5673 to reach the host-mapped broker port.

    In src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs:
    1. After the existing reads (`var host = cfg.Require("RabbitMq:Host");` ...), add an OPTIONAL port read defaulting to 5672:
       ```csharp
       var port = cfg.GetValue("RabbitMq:Port", (ushort)5672);
       ```
       Use `cfg.GetValue<ushort>("RabbitMq:Port", 5672)` if `GetValue` overload resolution needs the explicit type — match the existing `Microsoft.Extensions.Configuration` usings already imported in the file.
    2. Change the Host bind (:59) from the host-only form to the host+port form. MassTransit 8.5.5 supports `Host(string host, ushort port, string virtualHost, Action<IRabbitMqHostConfigurator>)`. Use:
       ```csharp
       busCfg.Host(host, port, "/", h => { h.Username(user); h.Password(pass); });
       ```
    3. Update the XML doc-comment near :37-39 to note: host/port/credentials read via cfg; RabbitMq:Port defaults to 5672 (compose-internal unaffected); the in-process TEST WebApi overrides Port=5673 to reach the host-mapped broker (TEST-RMQ-02).

    Do NOT change the Degraded health-check cap or the publish-only posture (no consumers, no filters).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter-class *HealthEndpointsTests</automated>
  </verify>
  <acceptance_criteria>
    - MessagingServiceCollectionExtensions.cs contains `RabbitMq:Port`
    - MessagingServiceCollectionExtensions.cs Host bind uses the 4-arg `Host(host, port, "/", ...)` form (grep for `Host(host, port`)
    - The default value 5672 is present in the GetValue call (grep for `5672`)
    - dotnet build -c Release exits 0 (zero-warning)
    - HealthEndpointsTests still pass (the existing bus-health facts are unaffected by the default-5672 path)
  </acceptance_criteria>
  <done>AddBaseApiMessaging reads RabbitMq:Port defaulting to 5672, binds host+port, compose-internal behavior is unchanged, and the test WebApi has a config seam to reach host port 5673.</done>
</task>

<task type="auto">
  <name>Task 3: Create HarnessWebAppFactory (D-01 in-memory harness swap) + install wget in the Dockerfile (D-12)</name>
  <read_first>
    - tests/BaseApi.Tests/Composition/Phase8WebAppFactory.cs (the base factory + its ctor variants + ConfigureWebHost/ConfigureAppConfiguration seam)
    - tests/BaseApi.Tests/Middleware/WebAppFactory.cs (the ConfigureTestServices seam at :37-52 — runs AFTER Program.cs's AddMassTransit)
    - tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs (in-memory AddMassTransitTestHarness precedent + ITestHarness usage)
    - Dockerfile (root — runtime stage at :13-19, no wget/curl in aspnet:8.0-bookworm-slim)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-PATTERNS.md (HarnessWebAppFactory + Dockerfile sections)
    - .planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-RESEARCH.md (Pattern 1 + Pitfall 2 + Assumption A1)
  </read_first>
  <action>
    Two edits — the D-01 harness factory and the D-12 Dockerfile fix.

    A. D-01 — create tests/BaseApi.Tests/Composition/HarnessWebAppFactory.cs as a sibling of WebAppFactory, inheriting Phase8WebAppFactory (so it keeps the Postgres/Redis fixture wiring). Override ConfigureWebHost, call base first, then in ConfigureTestServices REMOVE the real bus (Program.cs already calls AddBaseApiMessaging → AddMassTransit at startup, so calling AddMassTransitTestHarness again throws `ConfigurationException("AddMassTransit() was already called")` — Pitfall 2) and add the in-memory harness:
    ```csharp
    using MassTransit;
    using MassTransit.Testing;
    using Microsoft.AspNetCore.Hosting;
    using Microsoft.AspNetCore.TestHost;
    using Microsoft.Extensions.DependencyInjection;

    namespace BaseApi.Tests.Composition;

    /// <summary>
    /// D-01: the in-memory MassTransit harness is the DEFAULT test transport for HTTP integration
    /// facts that publish. Program.cs already calls AddBaseApiMessaging → AddMassTransit, so we must
    /// RemoveMassTransit() first (ConfigureTestServices runs AFTER the app's own ConfigureServices),
    /// then AddMassTransitTestHarness(). The harness IPublishEndpoint satisfies OrchestrationService's
    /// ctor injection → /start|/stop return 204 in-process (kills the ~1m40s publish-timeout hangs).
    /// </summary>
    public sealed class HarnessWebAppFactory : Phase8WebAppFactory
    {
        protected override void ConfigureWebHost(IWebHostBuilder builder)
        {
            base.ConfigureWebHost(builder);
            builder.ConfigureTestServices(services =>
            {
                services.RemoveMassTransit();          // A1: verify exposed in 8.5.5 (fallback below)
                services.AddMassTransitTestHarness();  // in-memory transport; registers IPublishEndpoint + ITestHarness
            });
        }
    }
    ```
    A1 VERIFICATION (do this WHILE writing the file): if `services.RemoveMassTransit()` does NOT compile against MassTransit 8.5.5 (no such extension on IServiceCollection), fall back to manual removal BEFORE AddMassTransitTestHarness: `services.RemoveAll(typeof(IBusControl)); services.RemoveAll(typeof(IBus)); services.RemoveAll(typeof(IPublishEndpoint)); services.RemoveAll(typeof(ISendEndpointProvider));` plus remove the MassTransit `IHostedService` descriptor (the bus hosted service registered by AddMassTransit). Add `using Microsoft.Extensions.DependencyInjection.Extensions;` for RemoveAll. Document in the SUMMARY which path was taken (A1 resolution).

    Match the Phase8WebAppFactory base ctor signature: if Phase8WebAppFactory has no parameterless ctor, give HarnessWebAppFactory a ctor that calls the base with the "all fixtures live" args (mirror how WebAppFactory/other subclasses construct the base). Read Phase8WebAppFactory.cs:86 ctor before deciding.

    B. D-12 — Dockerfile (root) runtime stage. The aspnet:8.0-bookworm-slim image ships neither wget nor curl, but compose.yaml's baseapi-service healthcheck runs `wget --spider`. Insert an apt layer BEFORE `USER app` (mirrors Phase 19 orchestrator fix e4fcf67):
    ```dockerfile
    FROM mcr.microsoft.com/dotnet/aspnet:8.0-bookworm-slim AS runtime
    WORKDIR /app
    RUN apt-get update \
     && apt-get install -y --no-install-recommends wget \
     && rm -rf /var/lib/apt/lists/*
    COPY --from=build /publish .
    USER app
    ...
    ```
    Keep the rest of the runtime stage (ENV/EXPOSE/ENTRYPOINT) byte-identical. No compose.yaml change is needed; the image is rebuilt in Plan 20-04 before the close gate.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - tests/BaseApi.Tests/Composition/HarnessWebAppFactory.cs exists and contains `class HarnessWebAppFactory`
    - HarnessWebAppFactory.cs contains `AddMassTransitTestHarness`
    - HarnessWebAppFactory.cs contains either `RemoveMassTransit()` OR the manual RemoveAll fallback (A1 — SUMMARY records which)
    - tests project compiles (dotnet build of BaseApi.Tests.csproj exits 0) — proves the harness API surface resolves
    - Dockerfile contains `apt-get install -y --no-install-recommends wget`
    - The wget RUN line appears BEFORE the `USER app` line in Dockerfile (grep line numbers: the apt-get line number < the USER app line number)
    - dotnet build SK_P.sln -c Release exits 0
  </acceptance_criteria>
  <done>HarnessWebAppFactory compiles and is ready to back HTTP publish tests via the in-memory harness (A1 resolved and recorded), and the Dockerfile installs wget before dropping to the app user so baseapi-service can pass its healthcheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → in-memory bus | hermetic; no network, no broker — no untrusted input crosses |
| in-process WebApi → host RabbitMQ (5673) | only exercised by tests in Plan 20-03; this plan only adds the config seam |
| Docker build (apt-get wget) | supply-chain surface — pulls wget from the Debian bookworm apt repos at image build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering (supply-chain) | Dockerfile wget apt layer (D-12) | accept | Local-dev / single-stage runtime image; informational IN-04 already acknowledged in CONTEXT.md as no-action. `--no-install-recommends` + `rm -rf /var/lib/apt/lists/*` keep the layer minimal. Pinned base image `aspnet:8.0-bookworm-slim`. NOT a HIGH threat (no prod registry push gate in scope). |
| T-20-02 | Information disclosure | RabbitMq:Port config read | accept | Port is a non-secret integer; credentials still read via the existing `cfg.Require` fail-fast (key name only, never value, in exceptions). No new secret surface. |
| T-20-03 | Tampering | publish-side log (D-07) | mitigate | The minted Guid is logged via a structured property `{CorrelationId}` (not string-concatenated), so it cannot inject into the log template; it is a server-generated `NewId.NextGuid()`, not untrusted input. |

**No HIGH-severity threats.** This plan adds test-enablement config + one structured log line + a minimal apt layer; the dev-only `guest/guest` broker creds are an accepted local-dev posture (T-19-broker-creds) and unchanged here.
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` and `-c Debug` both exit 0 (zero-warning, TreatWarningsAsErrors).
- The two D-13 filtered test classes pass; HealthEndpointsTests still pass (port default unaffected).
- HarnessWebAppFactory + the tests project compile (harness API surface resolves; A1 resolved).
</verification>

<success_criteria>
- D-13: Stop consumer logs "Scheduler job stop (seam)"; its locked assertion tracks it; the Start test is untouched.
- D-07: OrchestrationService injects ILogger and logs "Published StartOrchestration CorrelationId={CorrelationId}" with the minted body Guid; the direct-ctor test caller compiles.
- OQ#1/A3: AddBaseApiMessaging reads RabbitMq:Port (default 5672), binds host+port; compose-internal path unchanged.
- D-01/A1: HarnessWebAppFactory swaps the real bus for the in-memory harness and compiles; the A1 RemoveMassTransit path is recorded in the SUMMARY.
- D-12: Dockerfile installs wget before USER app.
</success_criteria>

<output>
After completion, create `.planning/phases/20-correlation-propagation-proof-synthetic-harness-triple-sha-c/20-01-SUMMARY.md`. Record the A1 resolution (RemoveMassTransit() vs manual fallback) and the exact RabbitMq:Port GetValue form used.
</output>
