---
phase: 19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/BaseApi.Core/BaseApi.Core.csproj
  - src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs
  - src/BaseApi.Service/Program.cs
  - src/BaseApi.Service/appsettings.json
  - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs
  - src/BaseApi.Service/Features/Orchestration/OrchestrationServiceCollectionExtensions.cs
  - tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs
autonomous: true
requirements: [MSG-WEBAPI-01, MSG-WEBAPI-02, MSG-WEBAPI-03, MSG-WEBAPI-04]
must_haves:
  truths:
    - "BaseApi.Core exposes a publish-only AddBaseApiMessaging(cfg) that wires AddMassTransit + UsingRabbitMq with NO consumers, NO receive endpoints, and NO BaseConsole.Core reference"
    - "BaseApi.Service.Program.cs calls AddBaseApiMessaging after AddBaseApi<AppDbContext> and before Build()"
    - "OrchestrationService publishes StartOrchestration{WorkflowIds[]} after the L2 write and StopOrchestration{WorkflowIds[]} after the Stop gate, each with a body CorrelationId minted via NewId.NextGuid()"
    - "The bus health check is capped at MinimalFailureStatus = Degraded so CRUD /health/ready stays 200 when the broker is down"
    - "BaseApi.* references Messaging.Contracts only, never BaseConsole.Core (dependency firewall)"
  artifacts:
    - path: "src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs"
      provides: "Publish-only AddBaseApiMessaging with Degraded bus health"
      contains: "AddBaseApiMessaging"
    - path: "src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs"
      provides: "Publish StartOrchestration/StopOrchestration after L2 write / Stop gate"
      contains: "_publishEndpoint.Publish"
    - path: "src/BaseApi.Service/appsettings.json"
      provides: "RabbitMq connection section for the WebApi host"
      contains: "RabbitMq"
    - path: "tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs"
      provides: "Harness publish assertions for Start/Stop"
      contains: "harness.Published"
  key_links:
    - from: "src/BaseApi.Service/Program.cs"
      to: "src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs"
      via: "AddBaseApiMessaging(builder.Configuration)"
      pattern: "AddBaseApiMessaging"
    - from: "src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs"
      to: "MassTransit IPublishEndpoint"
      via: "Publish(new StartOrchestration(...) { CorrelationId = NewId.NextGuid() })"
      pattern: "NewId\\.NextGuid"
    - from: "src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs"
      to: "bus health check"
      via: "ConfigureHealthCheckOptions MinimalFailureStatus Degraded"
      pattern: "MinimalFailureStatus"
---

<objective>
Join the WebApi to the MassTransit bus as a publish-only participant (Stream B, part 1) and publish Start/Stop from `OrchestrationService` with a body-carried, freshly-minted CorrelationId.

Covers: MSG-WEBAPI-01 (publish-only join referencing Messaging.Contracts, never BaseConsole.Core), MSG-WEBAPI-02 (Start publishes StartOrchestration{ids}, Stop publishes StopOrchestration{ids}), MSG-WEBAPI-03 (broker hard-dep for Start/Stop only — 5xx + RFC 7807 on broker-down, CRUD unaffected), MSG-WEBAPI-04 (bus health Degraded-not-Unhealthy so CRUD /health/ready stays 200).

Purpose: the publisher side of the fan-out — the WebApi mints the bus-stage CorrelationId at the publish boundary (per-stage handoff, D-01/D-02) and broadcasts the control message. Output: `AddBaseApiMessaging` in BaseApi.Core, the publish call sites in OrchestrationService, the WebApi RabbitMq appsettings section, and the in-memory publish harness test.

This plan does NOT touch compose.yaml or any Dockerfile (Plan 04). It does NOT set the MassTransit envelope CorrelationId (body-only — D-02). It does NOT add a correlation filter or AsyncLocal accessor to the WebApi (publish-only — D-02). The broker-down real-CRUD-health test is Phase 20 (TEST-RMQ-03) — this plan asserts the Degraded option is set in-process.

MSG-WEBAPI-03 (Start/Stop fail 5xx + RFC 7807 when the broker is unreachable; CRUD unaffected) is asserted in-phase at the service boundary: Task 3 proves a faulted `IPublishEndpoint` makes `OrchestrationService.StartAsync`/`StopAsync` PROPAGATE the publish exception (it is not swallowed), and that the existing `FallbackExceptionHandler` (D-06 chain handler #4) maps such an unhandled exception to a 500 ProblemDetails. The end-to-end real-broker-down HTTP test (5xx on Start/Stop while CRUD stays 2xx) is Phase 20 (TEST-RMQ-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-CONTEXT.md
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-RESEARCH.md
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-PATTERNS.md

<interfaces>
<!-- Target shapes (RESEARCH Pattern 3/4 + PATTERNS). -->

AddBaseApiMessaging (NEW, public static — publish-only):
```csharp
public static IServiceCollection AddBaseApiMessaging(this IServiceCollection services, IConfiguration cfg)
{
    var host = cfg.Require("RabbitMq:Host");
    var user = cfg.Require("RabbitMq:Username");
    var pass = cfg.Require("RabbitMq:Password");
    services.AddMassTransit(bus =>
    {
        bus.ConfigureHealthCheckOptions(o =>
        {
            o.MinimalFailureStatus = HealthStatus.Degraded;   // never escalate to Unhealthy (D-05)
            // DO NOT touch o.Tags — overriding REPLACES defaults ["ready","masstransit"] (Pitfall 7)
        });
        bus.UsingRabbitMq((context, busCfg) =>
        {
            busCfg.Host(host, h => { h.Username(user); h.Password(pass); });
            // NO ConfigureEndpoints — publish-only
        });
    });
    return services;
}
```

Publish call shape (D-02 verbatim — body only, NewId):
```csharp
await _publishEndpoint.Publish(
    new StartOrchestration(workflowIds.ToArray()) { CorrelationId = NewId.NextGuid() }, ct);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddBaseApiMessaging (publish-only) + BaseApi.Core package refs + Program.cs chain + RabbitMq appsettings</name>
  <files>src/BaseApi.Core/BaseApi.Core.csproj, src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs, src/BaseApi.Service/Program.cs, src/BaseApi.Service/appsettings.json</files>
  <read_first>
    - src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs (lines 25-49 — the config-read + AddMassTransit + UsingRabbitMq skeleton to copy, then DROP the consumer lambda/filters/accessor/ConfigureEndpoints)
    - src/BaseApi.Core/DependencyInjection/HealthServiceCollectionExtensions.cs (lines 21-26 — tag discipline; note it is internal static — make AddBaseApiMessaging public static instead)
    - src/BaseApi.Core/BaseApi.Core.csproj (existing CPM PackageReference pattern, no Version=)
    - src/BaseApi.Service/Program.cs (lines 5-14 — the composition chain; insert one line)
    - src/BaseApi.Service/appsettings.json (lines 13-27 — ConnectionStrings/Redis sections to mirror for RabbitMq)
    - 19-PATTERNS.md §"MessagingServiceCollectionExtensions.cs (NEW)" (lines 44-81) + §"Program.cs (MODIFY)" (lines 85-96) + §"appsettings.json" (lines 355-361)
    - 19-RESEARCH.md Pattern 3 (lines 358-393) + Pitfall 7 (lines 506-508)
  </read_first>
  <action>
    Add to `src/BaseApi.Core/BaseApi.Core.csproj` (CPM — NO Version=):
    ```xml
    <ItemGroup>
      <PackageReference Include="MassTransit" />
      <PackageReference Include="MassTransit.RabbitMQ" />
    </ItemGroup>
    <ItemGroup>
      <ProjectReference Include="..\Messaging.Contracts\Messaging.Contracts.csproj" />
    </ItemGroup>
    ```
    NO reference to `BaseConsole.Core` (forbidden — MSG-WEBAPI-01 / D-03).

    Create `src/BaseApi.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs` with a **public static** `AddBaseApiMessaging(this IServiceCollection services, IConfiguration cfg)` matching the interfaces block above. Read the three RabbitMq keys via `cfg.Require(...)` (RequiredConfig.Require exists in BaseApi.Core). Inside `AddMassTransit(bus => ...)`: call `bus.ConfigureHealthCheckOptions(o => o.MinimalFailureStatus = HealthStatus.Degraded)` (do NOT touch `o.Tags` — Pitfall 7), then `bus.UsingRabbitMq((context, busCfg) => busCfg.Host(host, h => { h.Username(user); h.Password(pass); }))`. NO `AddConsumer`, NO `ConfigureEndpoints`, NO correlation filter/accessor (publish-only — D-02).

    In `src/BaseApi.Service/Program.cs`, insert `builder.Services.AddBaseApiMessaging(builder.Configuration);` after `AddBaseApi<AppDbContext>(builder.Configuration)` and before `Build()`.

    Add a `RabbitMq` section to `src/BaseApi.Service/appsettings.json` mirroring the ConnectionStrings/Redis placement:
    ```json
    "RabbitMq": { "Host": "rabbitmq", "Username": "guest", "Password": "guest" }
    ```
    Keep the file valid JSON, no comments.
  </action>
  <verify>
    <automated>dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --nologo</automated>
  </verify>
  <acceptance_criteria>
    - BaseApi.Core.csproj contains `MassTransit` and `MassTransit.RabbitMQ` PackageReferences (no Version=) and a `Messaging.Contracts` ProjectReference
    - BaseApi.Core.csproj does NOT contain `BaseConsole.Core`
    - MessagingServiceCollectionExtensions.cs contains `public static` `AddBaseApiMessaging`, `cfg.Require("RabbitMq:Host")`, `MinimalFailureStatus = HealthStatus.Degraded`, `UsingRabbitMq`; does NOT contain `ConfigureEndpoints`, `AddConsumer`, `UseConsumeFilter`, or `o.Tags`
    - Program.cs contains `AddBaseApiMessaging(builder.Configuration)` after `AddBaseApi<AppDbContext>` and before `Build()`
    - appsettings.json contains a `RabbitMq` section with Host/Username/Password and remains valid JSON
    - `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --nologo` exits 0 (0 warnings)
  </acceptance_criteria>
  <done>WebApi joins the bus publish-only with Degraded bus health and a Messaging.Contracts-only reference. (Validation anchors: MSG-WEBAPI-01 dep-firewall; MSG-WEBAPI-04 Degraded health.)</done>
</task>

<task type="auto">
  <name>Task 2: Publish Start/Stop from OrchestrationService (body CorrelationId, NewId)</name>
  <files>src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs, src/BaseApi.Service/Features/Orchestration/OrchestrationServiceCollectionExtensions.cs</files>
  <read_first>
    - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs (the internal ctor block lines 49-90 field/guard pattern; StartAsync param `IReadOnlyList<Guid> workflowIds` ~line 101 + L2-write loop ending ~line 149; StopAsync param ~line 163 + post-gate cleanup ~line 218; the HTTP-stage correlationId string ~line 108 from HttpContext.Items — leave untouched)
    - src/BaseApi.Service/Features/Orchestration/OrchestrationServiceCollectionExtensions.cs (lines 56-67 — the explicit AddScoped factory; the ctor is internal so DI goes through this factory)
    - 19-PATTERNS.md §"OrchestrationService.cs (MODIFY)" (lines 100-122) + §"OrchestrationServiceCollectionExtensions.cs (MODIFY)" (lines 126-136)
    - 19-RESEARCH.md Pattern 4 (lines 395-407) + the body-only / no-envelope rule (line 414); A5 NewId (line 769)
  </read_first>
  <action>
    Inject `IPublishEndpoint` into `OrchestrationService`: add `private readonly IPublishEndpoint _publishEndpoint;`, add an `IPublishEndpoint publishEndpoint` parameter to the internal ctor (mirror the existing field block ordering), and add the guard `_publishEndpoint = publishEndpoint ?? throw new ArgumentNullException(nameof(publishEndpoint));`.

    In `StartAsync`, AFTER the per-workflow L2-write loop completes (all roots written — the existing loop end ~line 149, before method return), publish:
    ```csharp
    await _publishEndpoint.Publish(
        new StartOrchestration(workflowIds.ToArray()) { CorrelationId = NewId.NextGuid() },
        ct);
    ```
    In `StopAsync`, AFTER the existence/Stop gate passes (the post-gate cleanup ~line 218), publish `StopOrchestration` the same way (`new StopOrchestration(workflowIds.ToArray()) { CorrelationId = NewId.NextGuid() }`).

    Do NOT set the MassTransit envelope CorrelationId (body-only — D-02). Use `NewId.NextGuid()` (sequential — better broker/ES locality; D-02 verbatim shape). Leave the existing HTTP-stage `correlationId` string (from `HttpContext.Items["CorrelationId"]`) untouched — it is a SEPARATE stage and must NOT be carried onto the bus message (per-stage handoff, D-01).

    In `src/BaseApi.Service/Features/Orchestration/OrchestrationServiceCollectionExtensions.cs`, add `sp.GetRequiredService<IPublishEndpoint>()` to the explicit `AddScoped<OrchestrationService>(sp => new OrchestrationService(...))` factory call, in the parameter ORDER matching the edited ctor signature. `IPublishEndpoint` is registered by `AddBaseApiMessaging`'s `AddMassTransit`.
  </action>
  <verify>
    <automated>dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --nologo</automated>
  </verify>
  <acceptance_criteria>
    - OrchestrationService.cs contains `IPublishEndpoint _publishEndpoint`, a ctor guard for it, and two `_publishEndpoint.Publish(...)` calls — one `new StartOrchestration(workflowIds.ToArray()) { CorrelationId = NewId.NextGuid() }`, one `new StopOrchestration(workflowIds.ToArray()) { CorrelationId = NewId.NextGuid() }`
    - OrchestrationService.cs does NOT set the envelope CorrelationId (no `context.CorrelationId =` / `SetCorrelationId`); does NOT carry the HTTP-stage correlationId onto the published message
    - OrchestrationServiceCollectionExtensions.cs factory call includes `sp.GetRequiredService<IPublishEndpoint>()`
    - `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --nologo` exits 0 (0 warnings)
  </acceptance_criteria>
  <done>Start publishes StartOrchestration and Stop publishes StopOrchestration after their L2 stage, each with a freshly-minted body CorrelationId. (Validation anchor: MSG-WEBAPI-02 publish with body CorrelationId.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: In-memory harness publish tests for Start/Stop</name>
  <files>tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs (xUnit v3 + AddMassTransitTestHarness lifecycle to copy)
    - src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs (the post-Task-2 publish call sites)
    - 19-PATTERNS.md §"tests/.../Orchestrator/*" — the OrchestrationServicePublishTests bullet (line 396)
    - 19-RESEARCH.md §"Phase Requirements → Test Map" MSG-WEBAPI-02 row (line 715) + Wave 0 Gaps (line 734)
    - 19-VALIDATION.md the MSG-WEBAPI-02 + MSG-WEBAPI-03 rows
    - src/BaseApi.Core/Exceptions/Handlers/FallbackExceptionHandler.cs (the D-06 chain catch-all — TryHandleAsync sets 500 + writes a ProblemDetails via IProblemDetailsService; the MSG-WEBAPI-03 5xx-mapping target)
  </read_first>
  <behavior>
    - MSG-WEBAPI-02: invoking OrchestrationService.StartAsync (with all WorkflowIds present in the stub L2) → `harness.Published.Any<StartOrchestration>()` is true, and the published message carries the expected WorkflowIds[] + a non-empty body CorrelationId.
    - MSG-WEBAPI-02: invoking StopAsync (gate passes) → `harness.Published.Any<StopOrchestration>()` is true.
    - The published message's body CorrelationId is set (NewId), and the envelope CorrelationId is NOT set (body-only — D-02).
    - MSG-WEBAPI-03 (failure-path, in-phase at the service boundary): when the injected IPublishEndpoint faults (a stub whose Publish throws, e.g. a broker-unreachable simulation), OrchestrationService.StartAsync PROPAGATES the exception (it is NOT caught/swallowed) — proving the broker is a hard dep for the Start/Stop path. Separately, FallbackExceptionHandler.TryHandleAsync maps an arbitrary unhandled exception to HTTP 500 + a ProblemDetails body (RFC 7807). Together these prove the 5xx-on-broker-down contract at the unit boundary; the real-broker end-to-end HTTP assertion is Phase 20 (TEST-RMQ-03).
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Orchestration/OrchestrationServicePublishTests.cs` using the in-memory `AddMassTransitTestHarness` (NO real broker — Phase 19). Register `OrchestrationService` (via its explicit factory or a minimal scope) against the harness's `IPublishEndpoint`, with stub/fake collaborators for its other dependencies (BaseDbContext existence gate, Redis L2 writer, validators) sufficient to reach the publish boundary — or, if wiring the full service is heavy, register a thin seam that exercises the same publish call shape. Prefer driving the real `OrchestrationService` so the test proves the actual call site.

    Assertions:
    - After StartAsync over WorkflowIds present in the stub L2: `await harness.Published.Any<StartOrchestration>(ct)` is true; capture the published message and assert `WorkflowIds` equals the input and `CorrelationId != Guid.Empty`.
    - After StopAsync (gate passes): `await harness.Published.Any<StopOrchestration>(ct)` is true.
    - Assert the envelope CorrelationId was NOT set (the published context's envelope `CorrelationId` is null) — proving body-only correlation (D-02).
    Use the xUnit v3 idioms (`TestContext.Current.CancellationToken`, `await using ... BuildServiceProvider(true)`, `harness.Start()`/`Stop(ct)` in try/finally).

    MSG-WEBAPI-03 failure-path assertion (in-phase, unit boundary):
    - Construct OrchestrationService with a faulting IPublishEndpoint stub (`Publish(...)` throws, e.g. a `new Exception("broker unreachable")` or a faulted Task) and the same stub L2/collaborators used in the publish-success case; assert `await Assert.ThrowsAnyAsync<Exception>(() => svc.StartAsync(...))` — i.e. the publish failure PROPAGATES out of StartAsync (it is not swallowed). This proves the broker is a hard dependency for the Start path (MSG-WEBAPI-03).
    - Assert FallbackExceptionHandler maps an unhandled exception to a 500 ProblemDetails: build a `DefaultHttpContext`, call `new FallbackExceptionHandler(problemDetailsService, logger).TryHandleAsync(httpContext, new Exception("x"), ct)`, and assert it returns `true` and sets `httpContext.Response.StatusCode == 500` (the RFC 7807 ProblemDetails body is written by the registered IProblemDetailsService — register the framework default via a `ServiceCollection().AddProblemDetails()` provider, or use a minimal IProblemDetailsService stub and assert the ProblemDetailsContext carries Status 500). This proves Start/Stop publish failures surface as 5xx + ProblemDetails through the existing D-06 fallback handler, in-phase. (The end-to-end real-broker-down HTTP path = Phase 20 TEST-RMQ-03.)
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~OrchestrationServicePublish" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - OrchestrationServicePublishTests.cs contains `AddMassTransitTestHarness`, `harness.Published.Any<StartOrchestration>`, `harness.Published.Any<StopOrchestration>`
    - Test asserts the published StartOrchestration carries the input WorkflowIds and a non-empty body CorrelationId
    - Test asserts the envelope CorrelationId is NOT set (body-only)
    - Test asserts a faulting IPublishEndpoint makes StartAsync THROW/propagate (publish failure not swallowed) — MSG-WEBAPI-03 hard-dep
    - Test asserts FallbackExceptionHandler.TryHandleAsync sets Response.StatusCode 500 and returns true for an unhandled exception (5xx + ProblemDetails mapping) — MSG-WEBAPI-03 failure surface
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~OrchestrationServicePublish" --nologo` exits 0
  </acceptance_criteria>
  <done>Harness proves Start/Stop publish the correct message types with a body CorrelationId and no envelope id; a faulting IPublishEndpoint propagates out of StartAsync and FallbackExceptionHandler maps it to 5xx + ProblemDetails; publish subset GREEN. (Validation anchors: MSG-WEBAPI-02 publish observed in harness with body CorrelationId; MSG-WEBAPI-03 publish-failure propagation + 5xx ProblemDetails at the service boundary, real-broker-down = Phase 20.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/orchestration/start\|stop | Untrusted WorkflowIds[] enter the CRUD surface (validated by v3.3.0 gates before publish). |
| WebApi → broker | The WebApi publishes control messages to RabbitMQ using dev credentials from config. |
| broker availability → CRUD readiness | The broker is a hard dep for Start/Stop but must NOT couple to CRUD /health/ready. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-dep-firewall | Elevation / coupling | BaseApi.* → BaseConsole.Core | mitigate | BaseApi.Core references Messaging.Contracts only; no BaseConsole.Core ProjectReference. Forward direction (BaseApi.* must NOT reference BaseConsole.Core) is asserted by the Task 1 csproj grep ("BaseApi.Core.csproj does NOT contain BaseConsole.Core"). NOTE: the existing `ConsoleDependencyFirewallTests` asserts the REVERSE direction (BaseConsole.Core must not reference BaseApi.Core/EF/Npgsql, CONSOLE-05) and does NOT cover this boundary; Plan 04 adds no firewall test. MSG-WEBAPI-01. |
| T-19-broker-down | Denial of Service | bus health → CRUD readiness | mitigate | `MinimalFailureStatus = Degraded` caps the auto-registered bus check so broker-down never flips CRUD `/health/ready` to 503; `o.Tags` left at defaults (Pitfall 7). Start/Stop themselves return 5xx + RFC 7807 (existing FallbackExceptionHandler) on publish failure. MSG-WEBAPI-03/04. Real broker-down CRUD test = Phase 20 (TEST-RMQ-03). |
| T-19-broker-creds | Information Disclosure | RabbitMq creds in appsettings | accept | Dev-only `guest/guest`; compose/k8s env override (`RabbitMq__*`); never commit prod creds. Documented dev-only posture (RESEARCH Security Domain). |
| T-19-envelope-leak | Tampering (correlation confusion) | publish call site | mitigate | Correlation set only on the message body (`{ CorrelationId = NewId.NextGuid() }`); the MT envelope CorrelationId is left unset (single source of truth); HTTP-stage id is not carried onto the bus (per-stage handoff). |
</threat_model>

<verification>
- `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --nologo` exits 0 (0 warnings)
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~OrchestrationServicePublish" --nologo` exits 0
- Grep: BaseApi.Core.csproj has no BaseConsole.Core reference; MessagingServiceCollectionExtensions has no ConfigureEndpoints/AddConsumer/o.Tags
</verification>

<success_criteria>
- AddBaseApiMessaging is publish-only (no consumers, no endpoints, no filters), capped at Degraded bus health, referencing Messaging.Contracts only.
- Program.cs chains AddBaseApiMessaging after AddBaseApi.
- OrchestrationService publishes StartOrchestration/StopOrchestration after their L2 stage with a NewId body CorrelationId and no envelope id.
- Publish harness tests GREEN; dependency firewall intact.
</success_criteria>

<output>
After completion, create `.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-03-SUMMARY.md`
</output>
