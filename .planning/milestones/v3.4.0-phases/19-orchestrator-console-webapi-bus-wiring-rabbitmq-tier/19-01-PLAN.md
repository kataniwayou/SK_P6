---
phase: 19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Messaging.Contracts/ICorrelated.cs
  - src/Messaging.Contracts/StartOrchestration.cs
  - src/Messaging.Contracts/StopOrchestration.cs
  - src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs
  - tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs
autonomous: true
requirements: [ORCH-CON-03]
must_haves:
  truths:
    - "ICorrelated declares exactly one member: Guid CorrelationId { get; }"
    - "StartOrchestration and StopOrchestration implement ICorrelated with an init-set CorrelationId, keeping Guid[] WorkflowIds"
    - "The inbound consume filter opens the CorrelationId log scope from the message BODY (message is ICorrelated), not the envelope"
    - "ConsoleCorrelationFilterTests compiles with a slim 1-Guid ProbeMessage and asserts the body-sourced scope value"
    - "The full v3.3.0 + Phase 17 + Phase 18 suite stays GREEN after the contract slim + filter re-point"
  artifacts:
    - path: "src/Messaging.Contracts/ICorrelated.cs"
      provides: "Slim universal correlation contract { Guid CorrelationId }"
      contains: "Guid CorrelationId"
    - path: "src/Messaging.Contracts/StartOrchestration.cs"
      provides: "StartOrchestration : ICorrelated with init-set CorrelationId"
      contains: ": ICorrelated"
    - path: "src/Messaging.Contracts/StopOrchestration.cs"
      provides: "StopOrchestration : ICorrelated with init-set CorrelationId"
      contains: ": ICorrelated"
    - path: "src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs"
      provides: "Body-carried correlation read into the CorrelationId log scope"
      contains: "as ICorrelated"
    - path: "tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs"
      provides: "Rewritten filter test asserting body-sourced scope value"
      contains: "ICorrelated"
  key_links:
    - from: "src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs"
      to: "src/Messaging.Contracts/ICorrelated.cs"
      via: "context.Message as ICorrelated"
      pattern: "as ICorrelated"
    - from: "src/Messaging.Contracts/StartOrchestration.cs"
      to: "src/Messaging.Contracts/ICorrelated.cs"
      via: "interface implementation"
      pattern: ": ICorrelated"
---

<objective>
Reconcile the shipped Phase 17 + Phase 18 code to the body-carried, per-stage correlation model (CONTEXT D-01). This is the compile prerequisite for BOTH Phase 19 streams (Orchestrator console + WebApi publish join) — it must land first, alone, in its own wave.

Three coupled changes plus a test rewrite:
1. Slim `ICorrelated` from 6 get-only Guids to a single `{ Guid CorrelationId }` get-only member.
2. Make `StartOrchestration` / `StopOrchestration` implement the slim `ICorrelated` (init-set member, `Guid[] WorkflowIds` payload retained).
3. Re-point `InboundCorrelationConsumeFilter` to read the correlation from the message BODY (`message is ICorrelated`) rather than the MassTransit envelope (`context.CorrelationId`).
4. REWRITE `ConsoleCorrelationFilterTests` — its 6-Guid `ProbeMessage` will not compile after the slim, and it asserts the envelope value (semantically wrong after the body re-point). This is the Wave 0 test-scaffold prerequisite.

Purpose: every downstream Phase 19 plan compiles against the slim contract and the body-reading filter. The slim is a narrowing (6→1, zero production implementers per Phase 17 D-09), so the only compile break is the filter test, which this plan rewrites.
Output: a slim `ICorrelated`, two control records implementing it, a body-reading inbound filter, a rewritten filter test, and a GREEN full suite.

This plan does NOT define `IExecutionCorrelated` — explicitly DEFERRED to the Processor milestone (CONTEXT Deferred Ideas). Do not add the 5 execution ids anywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-CONTEXT.md
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-RESEARCH.md
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-PATTERNS.md

<interfaces>
<!-- Target shapes (from RESEARCH §Code Examples + PATTERNS). Executor uses these directly. -->

ICorrelated (slim target — Messaging.Contracts):
```csharp
namespace Messaging.Contracts;

/// <summary>Universal correlation contract — body-carried correlation id (v3.4.0 model, D-01).</summary>
public interface ICorrelated
{
    Guid CorrelationId { get; }   // interface get-only; implementers use { get; init; }
}
```

StartOrchestration / StopOrchestration (Pitfall 5 — non-positional init member):
```csharp
public sealed record StartOrchestration(Guid[] WorkflowIds) : ICorrelated
{
    public Guid CorrelationId { get; init; }
}
// StopOrchestration mirrors exactly.
```

InboundCorrelationConsumeFilter re-point (current envelope read at line 34; keep `where T : class`, keep the string accessor contract — Pitfall 6):
```csharp
// BEFORE: var corrId = context.CorrelationId?.ToString() ?? Guid.NewGuid().ToString();
// AFTER:
var corrId = (context.Message as ICorrelated)?.CorrelationId.ToString()
             ?? context.CorrelationId?.ToString()   // optional envelope fallback (may drop for body-only)
             ?? Guid.NewGuid().ToString();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim ICorrelated and make Start/Stop implement it</name>
  <files>src/Messaging.Contracts/ICorrelated.cs, src/Messaging.Contracts/StartOrchestration.cs, src/Messaging.Contracts/StopOrchestration.cs</files>
  <read_first>
    - src/Messaging.Contracts/ICorrelated.cs (the current 6-Guid get-only interface — full file)
    - src/Messaging.Contracts/StartOrchestration.cs (current bare record)
    - src/Messaging.Contracts/StopOrchestration.cs (current bare record)
    - 19-PATTERNS.md §"src/Messaging.Contracts/ICorrelated.cs" (lines 140-152) + §"StartOrchestration.cs + StopOrchestration.cs" (lines 156-165)
    - 19-RESEARCH.md Pitfall 5 (lines 492-497) — positional record cannot add init to a positional param; add CorrelationId as a separate non-positional property
    - 19-CONTEXT.md D-01 (lines 40-69) — slim contract, init-set, deferred IExecutionCorrelated
  </read_first>
  <action>
    Replace the body of `src/Messaging.Contracts/ICorrelated.cs` so the interface declares exactly one member:
    ```csharp
    namespace Messaging.Contracts;

    /// <summary>Universal correlation contract — body-carried correlation id (v3.4.0 model, D-01).</summary>
    public interface ICorrelated
    {
        Guid CorrelationId { get; }
    }
    ```
    Remove the five execution Guids (ExecutionId, WorkflowId, StepId, ProcessorId, EntryId). Keep `get;` only on the interface (init lives on the implementer — Pitfall 5). Do NOT define `IExecutionCorrelated` (DEFERRED — Processor milestone, per D-01 / CONTEXT Deferred Ideas).

    Edit `src/Messaging.Contracts/StartOrchestration.cs` to implement the slim contract as a NON-positional init member:
    ```csharp
    public sealed record StartOrchestration(Guid[] WorkflowIds) : ICorrelated
    {
        public Guid CorrelationId { get; init; }
    }
    ```
    Mirror the identical change in `src/Messaging.Contracts/StopOrchestration.cs` (StopOrchestration : ICorrelated, same init member). Keep the existing `Guid[] WorkflowIds` positional parameter and any existing XML doc comments. Do not set or carry the MassTransit envelope CorrelationId anywhere — correlation is body-only (D-01/D-02).
  </action>
  <verify>
    <automated>dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug --nologo</automated>
  </verify>
  <acceptance_criteria>
    - ICorrelated.cs contains `Guid CorrelationId` and contains `interface ICorrelated`
    - ICorrelated.cs does NOT contain `ExecutionId`, `StepId`, `ProcessorId`, or `EntryId`
    - ICorrelated.cs does NOT contain `IExecutionCorrelated`
    - StartOrchestration.cs contains `: ICorrelated` and `public Guid CorrelationId { get; init; }` and retains `Guid[] WorkflowIds`
    - StopOrchestration.cs contains `: ICorrelated` and `public Guid CorrelationId { get; init; }` and retains `Guid[] WorkflowIds`
    - `dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug --nologo` exits 0 with 0 warnings
  </acceptance_criteria>
  <done>ICorrelated is a single-member interface; Start/Stop implement it with init-set CorrelationId; Messaging.Contracts builds zero-warning. (Validation anchor: MSG-CONTRACTS-02/03 amended.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-point inbound filter to body and REWRITE ConsoleCorrelationFilterTests</name>
  <files>src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs, tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs</files>
  <read_first>
    - src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs (full file — note line 34 envelope read, `where T : class` at line 30, accessor.Set + BeginScope at lines 35-37)
    - tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs (full file — ProbeMessage 6 Guids lines 25-31, harness lifecycle lines 51-106, envelope assertion lines 92-94/101)
    - 19-PATTERNS.md §"InboundCorrelationConsumeFilter.cs" (lines 169-180) + §"ConsoleCorrelationFilterTests.cs REWRITE" (lines 365-397)
    - 19-RESEARCH.md Pitfall 6 (lines 499-504) — accessor stores string?, keep .ToString(); do NOT change accessor to Guid
    - 19-RESEARCH.md §"Inbound filter re-point" (lines 536-551) — keep `where T : class`, tolerate non-correlated messages via `as ICorrelated`
    - src/BaseConsole.Core/Messaging/ICorrelationAccessor.cs + AsyncLocalCorrelationAccessor.cs (the string? accessor contract)
  </read_first>
  <behavior>
    - Test: publishing a `ProbeMessage(someGuid)` (body CorrelationId = someGuid) → the consumer's captured scope/accessor value equals `someGuid.ToString()` (body-sourced, NOT the envelope).
    - Test: a message that does NOT implement ICorrelated still consumes without throwing (filter tolerates non-correlated messages; `where T : class` preserved).
  </behavior>
  <action>
    In `src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs`, re-point the correlation read (currently `var corrId = context.CorrelationId?.ToString() ?? Guid.NewGuid().ToString();` at line ~34) to read the body first:
    ```csharp
    var corrId = (context.Message as ICorrelated)?.CorrelationId.ToString()
                 ?? context.CorrelationId?.ToString()
                 ?? Guid.NewGuid().ToString();
    ```
    Keep `where T : class` (the filter is registered bus-wide via open-generic `UseConsumeFilter` and must tolerate non-correlated messages — the `as ICorrelated` handles them). Keep the `accessor.Set(corrId)` + `logger.BeginScope(... [CorrelationKeys.LogScope] = corrId ...)` lines unchanged (the accessor stays `string?` — Pitfall 6; do NOT change it to Guid). `Messaging.Contracts` is already imported.

    REWRITE `tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs`:
    - Change `ProbeMessage` from the 6-Guid record to `private sealed record ProbeMessage(Guid CorrelationId) : ICorrelated;` so it compiles against the slim contract.
    - Keep the xUnit v3 + `AddMassTransitTestHarness` lifecycle (`TestContext.Current.CancellationToken`, `await using var provider = ...BuildServiceProvider(true)`, `ITestHarness`, `harness.Start()` / `harness.Stop(ct)` in try/finally) verbatim from the existing structure.
    - Publish a `new ProbeMessage(someGuid)` with a fixed `someGuid`; assert the inbound filter opened the `CorrelationKeys.LogScope` scope (and/or set the accessor) to `someGuid.ToString()` — i.e. the BODY value, NOT `p.Context.CorrelationId` (the envelope). Use the consumer-captured accessor value or a captured scope value, matching how the existing ProbeConsumer captures it.
    - Add (or retain) a case asserting a published message that does NOT implement ICorrelated is still consumed without throwing (filter tolerance).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Console" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - InboundCorrelationConsumeFilter.cs contains `as ICorrelated` and still contains `where T : class`
    - InboundCorrelationConsumeFilter.cs still contains `accessor.Set(` and `CorrelationKeys.LogScope`
    - InboundCorrelationConsumeFilter.cs does NOT change the accessor type (no `ICorrelationAccessor` signature edit; still `string`)
    - ConsoleCorrelationFilterTests.cs contains `record ProbeMessage(Guid CorrelationId) : ICorrelated`
    - ConsoleCorrelationFilterTests.cs does NOT declare 6 Guids on ProbeMessage (no `ExecutionId`/`StepId`/`ProcessorId`/`EntryId`)
    - ConsoleCorrelationFilterTests.cs asserts the body value (the published someGuid), not `Context.CorrelationId`
    - `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Console" --nologo` exits 0
  </acceptance_criteria>
  <done>Inbound filter reads body CorrelationId; the Console filter test compiles and asserts the body-sourced scope value; Console subset GREEN. (Validation anchor: CORR-01 amended — inbound consume filter reads BODY.)</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate (no GREEN regression from the slim + re-point)</name>
  <files>(verification only — no file modifications; runs dotnet test SK_P.sln)</files>
  <read_first>
    - 19-VALIDATION.md §"Sampling Rate" + §"Validation Sign-Off"
    - 19-RESEARCH.md §"Reconciliation impact of slimming ICorrelated" (lines 450-466) — confirm zero production member access on the removed 5 ids
  </read_first>
  <action>
    Confirm the contract narrowing + filter re-point introduced no regression. First grep the production tree to confirm nothing reads the removed members:
    `Grep` for `\.ExecutionId|\.StepId|\.ProcessorId|\.EntryId` across `src/` — expect zero hits on any `ICorrelated`-typed value (WorkflowId on the WorkflowRootProjection L2 read-shape is unrelated and may remain). If any production access on an `ICorrelated`-typed value exists, STOP and report (the blast-radius assumption is violated).

    Then run the full solution suite. It must stay GREEN — the v3.3.0 + Phase 17 + Phase 18 suites are behavior-preserving under this narrowing (Start/Stop gained a member; the filter now reads the body but still opens the same scope key).
  </action>
  <verify>
    <automated>dotnet test SK_P.sln --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `Grep` for `\.ExecutionId|\.StepId|\.ProcessorId|\.EntryId` in src/ returns no hits on ICorrelated-typed values (the removed members are not accessed in production)
    - `dotnet test SK_P.sln --nologo` exits 0 (Failed: 0)
    - Release build is zero-warning: `dotnet build SK_P.sln -c Release --nologo` exits 0
  </acceptance_criteria>
  <done>Full suite GREEN after the contract reconciliation; zero-warning Release build; no production access to the removed members. (Validation anchor: ICorrelated slim regression — Phase 17/18 + v3.3.0 suites stay GREEN.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bus message body → inbound consume filter | The correlation value now comes from the deserialized message body (`ICorrelated.CorrelationId`), an untrusted field, instead of the MT envelope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-corr-integrity | Tampering / Information Disclosure | InboundCorrelationConsumeFilter body read | mitigate | Read is a typed `Guid` via `as ICorrelated` (no string injection); the value is only `.ToString()`'d into a structured log-scope key (`CorrelationKeys.LogScope`), never used in a query, path, or command. Non-ICorrelated messages fall back to a fresh Guid — no crash, no unscoped log. |
| T-19-corr-null | Denial of Service | filter on non-correlated messages | accept | `where T : class` + `as ICorrelated` null-safe fallback chain (`?? context.CorrelationId?.ToString() ?? Guid.NewGuid().ToString()`) guarantees a non-null scope value; bus-wide registration cannot NRE on a non-correlated message. |
</threat_model>

<verification>
- `dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug --nologo` exits 0
- `dotnet test tests/BaseApi.Tests --filter "FullyQualifiedName~Console" --nologo` exits 0
- `dotnet test SK_P.sln --nologo` exits 0 (Failed: 0)
- `dotnet build SK_P.sln -c Release --nologo` exits 0 (0 warnings)
</verification>

<success_criteria>
- ICorrelated is a single-member `{ Guid CorrelationId }` interface; IExecutionCorrelated is NOT defined.
- StartOrchestration and StopOrchestration implement ICorrelated with an init-set CorrelationId.
- InboundCorrelationConsumeFilter reads the body CorrelationId (`as ICorrelated`) into the CorrelationId log scope; accessor contract unchanged.
- ConsoleCorrelationFilterTests compiles against the slim contract and asserts the body-sourced value.
- Full SK_P.sln suite GREEN; Release build zero-warning.
</success_criteria>

<output>
After completion, create `.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-01-SUMMARY.md`
</output>
