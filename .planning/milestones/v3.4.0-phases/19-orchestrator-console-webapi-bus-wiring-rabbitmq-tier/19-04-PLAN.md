---
phase: 19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier
plan: 04
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - compose.yaml
  - src/Orchestrator/Dockerfile
autonomous: false
requirements: [INFRA-RMQ-02, INFRA-RMQ-03]
must_haves:
  truths:
    - "compose.yaml has a rabbitmq:4.1.8-management-alpine service with a rabbitmq-diagnostics -q ping healthcheck"
    - "The WebApi Start/Stop path (baseapi-service) depends_on rabbitmq: condition: service_healthy"
    - "A runnable orchestrator compose service builds from src/Orchestrator/Dockerfile and depends_on rabbitmq + redis service_healthy"
    - "Both compose services carry RabbitMq__Host/Username/Password env (compose-DNS host 'rabbitmq', not the host-side 5673 port)"
    - "src/Orchestrator/Dockerfile is a net8.0 multi-stage build using the aspnet:8.0 runtime base"
  artifacts:
    - path: "compose.yaml"
      provides: "rabbitmq + orchestrator services + baseapi-service rabbitmq depends_on"
      contains: "rabbitmq:4.1.8-management-alpine"
    - path: "src/Orchestrator/Dockerfile"
      provides: "Multi-stage net8.0 Orchestrator container image"
      contains: "aspnet:8.0"
  key_links:
    - from: "compose.yaml orchestrator service"
      to: "src/Orchestrator/Dockerfile"
      via: "build.dockerfile"
      pattern: "src/Orchestrator/Dockerfile"
    - from: "compose.yaml baseapi-service"
      to: "compose.yaml rabbitmq service"
      via: "depends_on condition: service_healthy"
      pattern: "service_healthy"
---

<objective>
Stand up the RabbitMQ infrastructure tier and the runnable Orchestrator container (Stream B, part 2) — the "first runnable Orchestrator stack" deliverable (D-09).

Covers: INFRA-RMQ-02 (`rabbitmq:4.1.8-management-alpine` service with `rabbitmq-diagnostics -q ping` healthcheck; Start/Stop path depends_on service_healthy), INFRA-RMQ-03 (RabbitMQ connection config reaches both hosts — the orchestrator's appsettings RabbitMq section ships in Plan 02, the WebApi's in Plan 03; this plan wires the compose-env host/credentials for both runnable services).

Purpose: a live broker + a runnable orchestrator container so the full stack comes up healthy. Output: a `rabbitmq` service, an `orchestrator` service, the WebApi rabbitmq depends_on + env, and `src/Orchestrator/Dockerfile`.

This plan is infra-only and file-disjoint from Plans 02/03 (it touches only compose.yaml + the new Dockerfile). It does NOT edit any appsettings.json (Plan 02 owns the Orchestrator appsettings, Plan 03 owns the WebApi appsettings). The real-broker stack-health verification is a manual checkpoint (Docker daemon required); the triple-SHA `rabbitmqctl list_queues` close gate is Phase 20 (TEST-RMQ-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-CONTEXT.md
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-RESEARCH.md
@.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the rabbitmq service + baseapi-service depends_on/env + orchestrator service to compose.yaml</name>
  <files>compose.yaml</files>
  <read_first>
    - compose.yaml (the redis block ~lines 135-151 — sk-* container_name, CMD-form healthcheck, host:container port offset, restart: unless-stopped; the baseapi-service block ~lines 153-184 — depends_on condition: service_healthy + __-delimited env overrides incl. ConnectionStrings__Redis ~line 163)
    - 19-PATTERNS.md §"compose.yaml (MODIFY)" (lines 329-351)
    - 19-RESEARCH.md §"Compose RabbitMQ service" (lines 584-605) + §"Orchestrator service + WebApi depends_on" (lines 607-644) — verified blocks
    - 19-CONTEXT.md D-09 (lines 131-139) — image, healthcheck, host ports 5673:5672 + 15673:15672, guest/guest, both depends_on
  </read_first>
  <action>
    Add a `rabbitmq` service to `compose.yaml`, mirroring the redis healthcheck idiom (CMD form, sk-* container_name):
    ```yaml
    rabbitmq:
      image: rabbitmq:4.1.8-management-alpine
      container_name: sk-rabbitmq
      restart: unless-stopped
      ports:
        - "5673:5672"
        - "15673:15672"
      environment:
        RABBITMQ_DEFAULT_USER: guest
        RABBITMQ_DEFAULT_PASS: guest
      healthcheck:
        test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
        interval: 10s
        timeout: 5s
        retries: 5
        start_period: 40s
    ```
    Add to `baseapi-service.depends_on` a `rabbitmq: { condition: service_healthy }` entry (mirror the existing redis/postgres entries — INFRA-RMQ-02 Start/Stop hard-dep) and add `RabbitMq__Host: rabbitmq`, `RabbitMq__Username: guest`, `RabbitMq__Password: guest` to its `environment:` (compose DNS host `rabbitmq`, NOT the host-side 5673 port).

    Add a NEW `orchestrator` service:
    ```yaml
    orchestrator:
      build:
        context: .
        dockerfile: src/Orchestrator/Dockerfile
      container_name: sk-orchestrator
      restart: unless-stopped
      depends_on:
        rabbitmq:
          condition: service_healthy
        redis:
          condition: service_healthy
      environment:
        RabbitMq__Host: rabbitmq
        RabbitMq__Username: guest
        RabbitMq__Password: guest
        ConnectionStrings__Redis: "redis:6379,abortConnect=false,connectTimeout=5000"
        Orchestrator__InstanceId: orchestrator-1
      healthcheck:
        test: ["CMD", "wget", "--spider", "-q", "http://localhost:8081/health/ready"]
        interval: 10s
        timeout: 3s
        retries: 5
        start_period: 30s
    ```
    Copy the `ConnectionStrings__Redis` value verbatim from the existing `baseapi-service` env (~line 163) to keep the Redis connection shape identical. Use the redis service's compose-internal port (6379), NOT the host-side offset. The orchestrator `/health/ready` is hard-on-broker (Phase 18 BusReadyHealthCheck) — the `depends_on rabbitmq: service_healthy` + the healthcheck enforce it. Keep the file valid compose v2 YAML.
  </action>
  <verify>
    <automated>docker compose -f compose.yaml config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - compose.yaml contains `rabbitmq:4.1.8-management-alpine`, `container_name: sk-rabbitmq`, and the healthcheck `["CMD", "rabbitmq-diagnostics", "-q", "ping"]`
    - compose.yaml maps host ports `5673:5672` and `15673:15672`
    - baseapi-service.depends_on contains a `rabbitmq:` entry with `condition: service_healthy` and the service has `RabbitMq__Host: rabbitmq` env
    - compose.yaml contains an `orchestrator` service with `dockerfile: src/Orchestrator/Dockerfile`, `depends_on` rabbitmq + redis service_healthy, and a `/health/ready` healthcheck on port 8081
    - `docker compose -f compose.yaml config --quiet` exits 0 (valid compose schema; no Docker daemon needed for `config`)
  </acceptance_criteria>
  <done>compose.yaml has a healthy rabbitmq service, the WebApi Start/Stop hard-dep, and a runnable orchestrator service with broker+redis health gates. (Validation anchor: INFRA-RMQ-02/03 — compose service + depends_on + env both hosts.)</done>
</task>

<task type="auto">
  <name>Task 2: Create src/Orchestrator/Dockerfile (net8.0 multi-stage, aspnet:8.0 runtime)</name>
  <files>src/Orchestrator/Dockerfile</files>
  <read_first>
    - Dockerfile (root — the multi-stage net8.0 build to mirror, lines 1-19: COPY props/global.json, csproj restore-cache layer, publish, runtime stage)
    - 19-PATTERNS.md §"src/Orchestrator/Dockerfile (NEW)" (lines 323-325)
    - 19-RESEARCH.md §"Dockerfile" (lines 646-671) — the verified file (.NET 8, aspnet:8.0, COPY list, port 8081)
  </read_first>
  <action>
    Create `src/Orchestrator/Dockerfile` as a net8.0 multi-stage build (mirror the root Dockerfile structure but swap the COPY list + publish target to Orchestrator). Use the verified RESEARCH block:
    ```dockerfile
    # syntax=docker/dockerfile:1.7
    FROM mcr.microsoft.com/dotnet/sdk:8.0-bookworm-slim AS build
    WORKDIR /src
    COPY ["Directory.Packages.props", "Directory.Build.props", "global.json", "./"]
    COPY ["src/Messaging.Contracts/Messaging.Contracts.csproj", "src/Messaging.Contracts/"]
    COPY ["src/BaseConsole.Core/BaseConsole.Core.csproj", "src/BaseConsole.Core/"]
    COPY ["src/Orchestrator/Orchestrator.csproj", "src/Orchestrator/"]
    RUN dotnet restore "src/Orchestrator/Orchestrator.csproj"
    COPY src/ src/
    RUN dotnet publish "src/Orchestrator/Orchestrator.csproj" -c Release -o /publish --no-restore /p:UseAppHost=false

    FROM mcr.microsoft.com/dotnet/aspnet:8.0-bookworm-slim AS runtime
    WORKDIR /app
    COPY --from=build /publish .
    USER app
    ENV ASPNETCORE_URLS=http://+:8081
    EXPOSE 8081
    ENTRYPOINT ["dotnet", "Orchestrator.dll"]
    ```
    Use `aspnet:8.0` (NOT `runtime:8.0`) — BaseConsole.Core has `FrameworkReference Microsoft.AspNetCore.App` for the embedded Kestrel health listener, which needs the ASP.NET Core shared framework at runtime. Port 8081 matches `ConsoleHealth:Port` (Phase 18 D-04). The COPY restore-cache layer must include Messaging.Contracts + BaseConsole.Core + Orchestrator csproj (the orchestrator's full reference closure, excluding BaseApi.*). Do NOT reference any BaseApi.* csproj in the COPY list.
  </action>
  <verify>
    <automated>dotnet publish src/Orchestrator/Orchestrator.csproj -c Release -o /tmp/orch-publish --nologo</automated>
  </verify>
  <acceptance_criteria>
    - src/Orchestrator/Dockerfile contains `sdk:8.0-bookworm-slim`, `aspnet:8.0-bookworm-slim`, `ENTRYPOINT ["dotnet", "Orchestrator.dll"]`, and `EXPOSE 8081`
    - Dockerfile does NOT use `runtime:8.0` (must be aspnet) and does NOT COPY any `BaseApi` csproj
    - Dockerfile COPY list includes Messaging.Contracts.csproj, BaseConsole.Core.csproj, and Orchestrator.csproj
    - `dotnet publish src/Orchestrator/Orchestrator.csproj -c Release` exits 0 (publish path the Dockerfile build stage exercises)
  </acceptance_criteria>
  <done>The Orchestrator has a net8.0 multi-stage Dockerfile on the aspnet:8.0 runtime; the published assembly is the container entrypoint. (Validation anchor: INFRA-RMQ-02 — runnable orchestrator container, manual stack-health verify.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the live RabbitMQ + Orchestrator stack comes up healthy</name>
  <files>(checkpoint — operator verifies the live stack; no file modifications)</files>
  <what-built>The RabbitMQ compose tier (`rabbitmq:4.1.8-management-alpine`), the runnable `orchestrator` container, and the WebApi Start/Stop broker hard-dep — the first runnable Orchestrator stack. Automated `docker compose config` validation and the Orchestrator publish both pass; this checkpoint confirms the live stack comes up healthy (Docker daemon required, so it cannot be fully automated in-process).</what-built>
  <how-to-verify>
    1. `docker compose up -d rabbitmq` — wait ~40s, then `docker inspect --format "{{.State.Health.Status}}" sk-rabbitmq` should print `healthy`.
    2. `docker compose up -d --build orchestrator` — then `docker inspect --format "{{.State.Health.Status}}" sk-orchestrator` should print `healthy` (its `/health/ready` is hard-on-broker; it goes healthy only after the bus starts).
    3. Optional: open the management UI at http://localhost:15673 (guest/guest) and confirm an `orchestrator-{InstanceId}` queue appears (temporary/auto-delete) once the orchestrator is up.
    4. `docker compose down` to tear the stack down (no `-v` — preserve volumes; the triple-SHA leak gate is Phase 20).
  </how-to-verify>
  <resume-signal>Type "approved" if both containers report healthy, or describe what failed (e.g. broker never healthy, orchestrator /health/ready stuck).</resume-signal>
  <action>Operator brings up the live stack (`docker compose up -d --build rabbitmq orchestrator`) and confirms both containers report healthy per the steps above. Claude has already verified the compose schema (`docker compose config --quiet`) and the Orchestrator publish; this checkpoint confirms the runtime stack since a Docker daemon is required.</action>
  <verify>Operator confirms `docker inspect --format "{{.State.Health.Status}}" sk-rabbitmq` and `sk-orchestrator` both print `healthy`.</verify>
  <done>Both sk-rabbitmq and sk-orchestrator report `healthy`; operator types "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host network → broker management UI | The RabbitMQ management UI is exposed on host port 15673. |
| compose env → broker credentials | RabbitMQ credentials are supplied via compose env to both runnable services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-broker-creds | Information Disclosure | RabbitMq creds in compose env | accept | Dev-only `guest/guest`; local compose only; k8s/prod override via secrets, never committed. Documented dev-only posture (RESEARCH Security Domain). |
| T-19-mgmt-ui-exposure | Information Disclosure | management UI on 15673 | accept | Dev-only local exposure; not bound in any non-dev environment; default guest is loopback-restricted by RabbitMQ for non-localhost in real deployments. |
| T-19-durable-queue-accum | Resource exhaustion | per-replica queues | mitigate | Orchestrator binds temporary/auto-delete queues (Plan 02 `e.Temporary`); no durable per-replica queues accumulate on rescale (REQUIREMENTS Out-of-Scope). |
| T-19-broker-cold-start | Availability | rabbitmq healthcheck | mitigate | `start_period: 40s` + `retries: 5` accommodate broker cold-start before `service_healthy` gates dependents; dependents wait via `condition: service_healthy`. |
</threat_model>

<verification>
- `docker compose -f compose.yaml config --quiet` exits 0
- `dotnet publish src/Orchestrator/Orchestrator.csproj -c Release` exits 0
- Manual checkpoint: `sk-rabbitmq` and `sk-orchestrator` both report `healthy` under `docker compose up -d --build`
</verification>

<success_criteria>
- compose.yaml has a healthy rabbitmq:4.1.8-management-alpine service with the ping healthcheck.
- baseapi-service depends_on rabbitmq service_healthy + carries RabbitMq__* env.
- A runnable orchestrator service builds from src/Orchestrator/Dockerfile, depends_on rabbitmq + redis, and carries RabbitMq__* + Redis env.
- src/Orchestrator/Dockerfile is a net8.0 multi-stage build on the aspnet:8.0 runtime.
- The live stack comes up healthy (operator-verified).
</success_criteria>

<output>
After completion, create `.planning/phases/19-orchestrator-console-webapi-bus-wiring-rabbitmq-tier/19-04-SUMMARY.md`
</output>
