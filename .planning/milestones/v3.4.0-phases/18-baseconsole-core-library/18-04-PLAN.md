---
phase: 18-baseconsole-core-library
plan: 04
type: execute
wave: 3
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - tests/BaseApi.Tests/BaseApi.Tests.csproj
  - tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs
  - tests/BaseApi.Tests/Console/ConsoleHealthLiveTests.cs
  - tests/BaseApi.Tests/Console/ConsoleBusReadyHealthCheckTests.cs
  - tests/BaseApi.Tests/Console/ConsoleStartupGateTests.cs
  - tests/BaseApi.Tests/Console/ConsoleObservabilityTests.cs
  - tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs
autonomous: false
requirements: [CONSOLE-01, CONSOLE-02, CONSOLE-03, CONSOLE-04, CONSOLE-05, CONSOLE-HEALTH-01, CONSOLE-HEALTH-02, CONSOLE-HEALTH-03, CONSOLE-HEALTH-04, CORR-01, CORR-02]
user_setup: []

must_haves:
  truths:
    - "The test project references BaseConsole.Core and a ConsoleTestHostFixture boots Host.CreateApplicationBuilder + all three AddBaseConsole* calls + Build() with dead Redis/RabbitMQ ports, IBus resolvable, no throw"
    - "/health/live returns 200 + Healthy with both Redis and RabbitMQ ports dead"
    - "/health/startup returns 200 after host init; 503 when StartupCompletionService is removed by type identity"
    - "BusReadyHealthCheck.CheckHealthAsync returns HealthCheckResult.Unhealthy when the outer IBusHealth is null/not-Healthy (CONSOLE-HEALTH-03 Unhealthy-while-broker-unreachable is positively proven, not optional)"
    - "No TracerProvider is resolvable from the console container; a MeterProvider is resolvable and AspNetCore/HttpClient instrumentation services are absent"
    - "Via AddMassTransitTestHarness: the inbound filter populates the accessor + CorrelationId scope, and the outbound filter stamps SendContext.CorrelationId for a Guid-parseable ICorrelated message"
    - "The full dotnet test suite is GREEN 3-consecutive and dual-SHA (psql \\l + redis-cli --scan) BEFORE=AFTER"
  artifacts:
    - path: "tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs"
      provides: "The in-memory Generic-Host validation vehicle (D-02)"
      contains: "AddBaseConsole"
    - path: "tests/BaseApi.Tests/Console/ConsoleObservabilityTests.cs"
      provides: "No-TracerProvider + MassTransit-meter assertions"
      contains: "TracerProvider"
    - path: "tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs"
      provides: "Harness-based filter behavior proof"
      contains: "AddMassTransitTestHarness"
    - path: "tests/BaseApi.Tests/BaseApi.Tests.csproj"
      provides: "ProjectReference to BaseConsole.Core"
      contains: "BaseConsole.Core.csproj"
  key_links:
    - from: "tests/BaseApi.Tests/BaseApi.Tests.csproj"
      to: "src/BaseConsole.Core/BaseConsole.Core.csproj"
      via: "ProjectReference"
      pattern: "BaseConsole.Core.csproj"
    - from: "tests/BaseApi.Tests/Console/ConsoleHealthLiveTests.cs"
      to: "src/BaseConsole.Core/Health/EmbeddedHealthEndpointService.cs"
      via: "HTTP GET /health/live against the embedded listener"
      pattern: "/health/live"
    - from: "tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs"
      to: "src/BaseConsole.Core/Messaging/OutboundCorrelationSendFilter.cs"
      via: "harness publish → envelope CorrelationId assertion"
      pattern: "CorrelationId"
---

<objective>
Add the BaseConsole.Core ProjectReference to the test project, build the `ConsoleTestHostFixture`
(the D-02 in-memory Generic-Host validation vehicle), and write the five Console validation test classes
that prove all six D-02 proof points. Then run the phase close gate: full suite GREEN 3-consecutive +
dual-SHA snapshot.

Purpose: This is the standalone validation of the whole library (D-02). It maps every behavior in
18-VALIDATION.md to an automated test. No concrete console or real broker ships — the in-memory
`Host.CreateApplicationBuilder` fixture + `AddMassTransitTestHarness` are the vehicles. The close gate is
dual-SHA (`psql \l` + `redis-cli --scan`), NOT triple-SHA — no real broker resources exist this phase (D-03).

Output: One test ProjectReference, one fixture, five test classes, and a green 3-consecutive close gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-baseconsole-core-library/18-CONTEXT.md
@.planning/phases/18-baseconsole-core-library/18-RESEARCH.md
@.planning/phases/18-baseconsole-core-library/18-PATTERNS.md
@.planning/phases/18-baseconsole-core-library/18-VALIDATION.md

<interfaces>
<!-- Surfaces under test, from Plans 01-03. -->

Composition seam (Plans 01-03):
```csharp
builder.AddBaseConsoleObservability(cfg);                        // IHostApplicationBuilder
builder.Services.AddBaseConsole(cfg);                            // Redis + health + embedded listener
builder.Services.AddBaseConsoleMessaging(cfg, x => { /* empty this phase */ });  // bus + filters + accessor
```
- ICorrelationAccessor / AsyncLocalCorrelationAccessor (BaseConsole.Core.Messaging)
- InboundCorrelationConsumeFilter : IFilter<ConsumeContext> ; OutboundCorrelationSendFilter<T> : IFilter<SendContext<T>>
- EmbeddedHealthEndpointService → /health/live|ready|startup on ConsoleHealth:Port (default 8081)
- StartupCompletionService : IHostedService (removable by type identity for the 503 negative test)

From Messaging.Contracts (Phase 17): `CorrelationKeys.LogScope == "CorrelationId"`; `ICorrelated` (6 get-only Guids).

Test analog — tests/BaseApi.Tests/Observability/HealthEndpointsTests.cs:
- dead-port fixture + HTTP GET /health/* pattern; body assertion `Assert.Contains("\"status\":\"Healthy\"", body)`
- startup-negative: remove the hosted service by type identity (ServiceDescriptor where ImplementationType == typeof(StartupCompletionService))
- body-no-secrets: `Assert.DoesNotContain("Password=", body)`
- xUnit v3: `var ct = TestContext.Current.CancellationToken;` threaded through async calls (TreatWarningsAsErrors → xUnit1051).

Test csproj (tests/BaseApi.Tests/BaseApi.Tests.csproj): add `<ProjectReference Include="..\..\src\BaseConsole.Core\BaseConsole.Core.csproj" />` to the existing ProjectReference ItemGroup.
AddMassTransitTestHarness ships in the core MassTransit 8.5.5 package — no extra NuGet.

Quick run: `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter FullyQualifiedName~Console`
Full suite: `dotnet test`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test ProjectReference + ConsoleTestHostFixture (the D-02 vehicle)</name>
  <files>tests/BaseApi.Tests/BaseApi.Tests.csproj, tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs</files>
  <read_first>
    - tests/BaseApi.Tests/BaseApi.Tests.csproj (the ProjectReference ItemGroup to extend, lines 111-116)
    - tests/BaseApi.Tests/Observability/HealthEndpointsTests.cs (dead-port fixture + env-var discipline + xUnit v3 ct threading)
    - src/BaseConsole.Core/DependencyInjection/* (the three Add* calls the fixture composes)
    - .planning/phases/18-baseconsole-core-library/18-PATTERNS.md (test section lines 380-397)
    - .planning/phases/18-baseconsole-core-library/18-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements)
  </read_first>
  <action>
    1. Edit `tests/BaseApi.Tests/BaseApi.Tests.csproj`: add to the existing ProjectReference ItemGroup
       (the one containing BaseApi.Core/BaseApi.Service):
       `<ProjectReference Include="..\..\src\BaseConsole.Core\BaseConsole.Core.csproj" />`.

    2. Create `tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs`, namespace `BaseApi.Tests.Console`.
       This is the in-memory Generic-Host fixture (NOT WebApplicationFactory). It:
       - Builds via `Host.CreateApplicationBuilder()` (an `IHostApplicationBuilder`).
       - Supplies an in-memory IConfiguration (`builder.Configuration.AddInMemoryCollection(...)`) with:
         `Service:Name`=`"orchestrator-test"`, `Service:Version`=`"18.0.0"`,
         `ConnectionStrings:Redis`=`"127.0.0.1:6399,abortConnect=false"` (DEAD port — soft dep proves boot-safety),
         `RabbitMq:Host`=`"localhost"` (or a dead/unreachable host), `RabbitMq:Username`/`Password`=`"guest"`,
         `ConsoleHealth:Port`= an EPHEMERAL/free port chosen at fixture construction (find a free TCP port,
         store it for the HTTP tests; do NOT use the 8081 default to avoid collisions across parallel tests).
         Set OTLP endpoint config if `AddOtlpExporter` requires it; otherwise rely on the SDK default
         (the exporter does not connect at build time).
       - Calls `builder.AddBaseConsoleObservability(builder.Configuration);`
         `builder.Services.AddBaseConsole(builder.Configuration);`
         `builder.Services.AddBaseConsoleMessaging(builder.Configuration, x => { });` (empty consumer lambda).
       - `Host = builder.Build();` exposes the `IHost`, the chosen health port, and an `HttpClient`
         pointing at `http://127.0.0.1:{port}` for the health tests.
       - Implements `IAsyncLifetime` (xUnit v3): `InitializeAsync` calls `await Host.StartAsync()`
         (this runs StartupCompletionService → gate.MarkReady, and starts EmbeddedHealthEndpointService);
         `DisposeAsync` calls `await Host.StopAsync()` + disposes.
       - Thread `TestContext.Current.CancellationToken` through async calls (xUnit1051 under TreatWarningsAsErrors).
       Use the dead-port + restore-on-dispose discipline from the analog. The fixture is the shared vehicle
       for the boot test (CONSOLE-01/03/04) and the health tests.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter FullyQualifiedName~Console</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/BaseApi.Tests.csproj` contains `..\..\src\BaseConsole.Core\BaseConsole.Core.csproj`
    - `tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs` exists, contains `Host.CreateApplicationBuilder`, `AddBaseConsoleObservability`, `AddBaseConsole(`, `AddBaseConsoleMessaging(`
    - Fixture contains a DEAD Redis port (e.g. `6399`) and `abortConnect=false`
    - Fixture exposes the chosen `ConsoleHealth:Port` and an HttpClient for the health tests
    - `dotnet test ... --filter FullyQualifiedName~Console` exits 0 (at least the fixture-backed boot fact passes; more added in Tasks 2-3)
    - Solution-root `dotnet build -c Release` exits 0 (test project compiles with the new reference)
  </acceptance_criteria>
  <done>The test project references BaseConsole.Core and the ConsoleTestHostFixture boots the full three-call chain in-memory with dead deps; IBus resolvable, host starts without throwing. CONSOLE-01/03/04/05 boot path validated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Health + observability validation tests</name>
  <files>tests/BaseApi.Tests/Console/ConsoleHealthLiveTests.cs, tests/BaseApi.Tests/Console/ConsoleBusReadyHealthCheckTests.cs, tests/BaseApi.Tests/Console/ConsoleStartupGateTests.cs, tests/BaseApi.Tests/Console/ConsoleObservabilityTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Console/ConsoleTestHostFixture.cs (Task 1 — the vehicle these tests consume)
    - tests/BaseApi.Tests/Observability/HealthEndpointsTests.cs (live-200/startup-200/startup-503/no-secrets patterns, lines 30-134)
    - src/BaseConsole.Core/Health/BusReadyHealthCheck.cs (Plan 18-03 — the unit under test for the CONSOLE-HEALTH-03 Unhealthy proof)
    - tests/BaseApi.Tests/Observability/MetricsExportTests.cs (MeterProvider/instrumentation-shape assertion analog)
    - .planning/phases/18-baseconsole-core-library/18-VALIDATION.md (rows for CONSOLE-HEALTH-02/03/04 and CONSOLE-02)
    - .planning/phases/18-baseconsole-core-library/18-RESEARCH.md (D-02 facts 2-5, lines 56-58; Pitfall 1 fact #4)
  </read_first>
  <action>
    Create four test classes under `tests/BaseApi.Tests/Console/`, namespace `BaseApi.Tests.Console`.
    The fixture-backed classes use the `ConsoleTestHostFixture`; `ConsoleBusReadyHealthCheckTests` is a
    pure unit test (no fixture). Thread `TestContext.Current.CancellationToken`.

    1. `ConsoleHealthLiveTests.cs`:
       - `Live_Returns_200_When_Redis_And_RabbitMQ_Dead`: boot fixture (dead Redis + dead RabbitMQ ports);
         GET `http://127.0.0.1:{port}/health/live`; assert `HttpStatusCode.OK` and body contains
         `"status":"Healthy"` (CONSOLE-HEALTH-02 / D-02 fact #2).
       - `Live_Body_Has_No_Secrets`: GET /health/live (and /health/ready); assert
         `Assert.DoesNotContain("Password=", body)` and no stack-trace marker (T-18-08).
       (Note: the booted-fixture `/health/ready` end-to-end Unhealthy assertion is timing-sensitive in-memory;
       the POSITIVE proof of CONSOLE-HEALTH-03 lives in the committed `ConsoleBusReadyHealthCheckTests` below,
       not as an optional fixture assertion.)

    2. `ConsoleBusReadyHealthCheckTests.cs` (COMMITTED — positively proves CONSOLE-HEALTH-03 / SC-3
       Unhealthy-while-broker-unreachable, testable with no real broker):
       - `BusReadyHealthCheck_Returns_Unhealthy_When_Bus_Not_Healthy`: construct `BusReadyHealthCheck`
         directly with an outer `IServiceProvider` whose `IBusHealth` is EITHER null (use an empty
         `new ServiceCollection().BuildServiceProvider()` so `GetService<IBusHealth>()` returns null — the
         "bus not started" path) OR an NSubstitute `IBusHealth` substitute whose `CheckHealth()` returns a
         non-Healthy `BusHealthResult` (`BusHealthStatus.Unhealthy`/`Degraded`). Call
         `await sut.CheckHealthAsync(new HealthCheckContext(), TestContext.Current.CancellationToken)` and
         assert the returned `HealthCheckResult.Status == HealthStatus.Unhealthy`. This is the positive
         broker-unreachable proof — it does not depend on a real broker or fixture timing.
       - (Optional companion) `BusReadyHealthCheck_Returns_Healthy_When_Bus_Healthy`: NSubstitute `IBusHealth`
         returning a Healthy `BusHealthResult` → assert `HealthStatus.Healthy`. Confirm the exact
         `IBusHealth.CheckHealth()` / `BusHealthResult` member names against MassTransit 8.5.5 at build time.

    3. `ConsoleStartupGateTests.cs`:
       - `Startup_Returns_200_After_Host_Init`: GET /health/startup on the booted fixture; assert 200 +
         `"status":"Healthy"` (StartupCompletionService flipped the gate) (CONSOLE-HEALTH-04 / D-02 fact #3).
       - `Startup_Returns_503_When_CompletionService_Removed`: build a SECOND fixture variant that removes
         the `StartupCompletionService` registration by type identity
         (`services.RemoveAll(...)` / remove the ServiceDescriptor where
         `ImplementationType == typeof(StartupCompletionService)`) BEFORE Build(); GET /health/startup;
         assert `ServiceUnavailable` + `"status":"Unhealthy"` (mirror HealthNoStartupCompletionFixture).

    4. `ConsoleObservabilityTests.cs`:
       - `No_TracerProvider_Resolvable`: from the fixture's `Host.Services`, assert
         `Host.Services.GetService<TracerProvider>()` is null (D-02 fact #4 / Pitfall 1 — console analog of
         the deleted TraceExportTests). `using OpenTelemetry.Trace;`.
       - `MeterProvider_Resolvable`: assert `Host.Services.GetService<MeterProvider>()` is not null
         (`using OpenTelemetry.Metrics;`).
       - `No_AspNetCore_Or_HttpClient_Instrumentation`: assert no AspNetCore/HttpClient instrumentation
         services are registered (scan the IServiceCollection/provider for the instrumentation marker types,
         mirroring the API metrics-shape assertions; OR assert the source files contain no such registration
         via the already-built extension — prefer a container/service assertion). D-02 fact #5 (CONSOLE-02).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter FullyQualifiedName~Console</automated>
  </verify>
  <acceptance_criteria>
    - `ConsoleHealthLiveTests.cs` exists, contains `"/health/live"` and asserts `HttpStatusCode.OK` + `"status":"Healthy"` with dead deps
    - `ConsoleHealthLiveTests.cs` contains `Assert.DoesNotContain("Password=", body)` (no-secrets)
    - `ConsoleBusReadyHealthCheckTests.cs` exists and contains a method named `BusReadyHealthCheck_Returns_Unhealthy_When_Bus_Not_Healthy` that constructs `BusReadyHealthCheck`, calls `CheckHealthAsync`, and asserts `HealthStatus.Unhealthy` (CONSOLE-HEALTH-03 broker-unreachable POSITIVELY proven — not optional)
    - `ConsoleStartupGateTests.cs` exists, contains a `/health/startup` 200 test AND a 503 test removing `StartupCompletionService` by type identity (`typeof(StartupCompletionService)`)
    - `ConsoleObservabilityTests.cs` exists, contains `GetService<TracerProvider>()` asserted null and `GetService<MeterProvider>()` asserted non-null
    - `ConsoleObservabilityTests.cs` asserts AspNetCore/HttpClient instrumentation absent
    - `dotnet test ... --filter FullyQualifiedName~Console` exits 0
  </acceptance_criteria>
  <done>Health (live-with-deps-dead, startup 200/503, no-secrets) and observability (no TracerProvider, MeterProvider present, no AspNetCore/HttpClient instrumentation) are proven against the in-memory fixture, and CONSOLE-HEALTH-03 broker-unreachable is positively proven by the committed `BusReadyHealthCheck_Returns_Unhealthy_When_Bus_Not_Healthy` unit test. D-02 facts 2-5 GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Correlation filter behavior via AddMassTransitTestHarness</name>
  <files>tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs</files>
  <read_first>
    - src/BaseConsole.Core/Messaging/InboundCorrelationConsumeFilter.cs + OutboundCorrelationSendFilter.cs + OutboundCorrelationPublishFilter.cs (Plan 02 — under test)
    - src/BaseConsole.Core/Messaging/ICorrelationAccessor.cs + AsyncLocalCorrelationAccessor.cs (Plan 01)
    - src/Messaging.Contracts/ICorrelated.cs (the gate the outbound filter checks)
    - .planning/phases/18-baseconsole-core-library/18-RESEARCH.md (D-02 fact #6 lines 59-60; Pattern 4/5; Open-Q 2 / Assumption A3 — use a Guid-parseable id)
    - .planning/phases/18-baseconsole-core-library/18-PATTERNS.md (ConsoleCorrelationFilterTests, line 397)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Console/ConsoleCorrelationFilterTests.cs`, namespace `BaseApi.Tests.Console`.
    Build an in-memory harness host (does NOT need RabbitMQ — `AddMassTransitTestHarness` uses the in-memory transport):
    - Register a `ServiceCollection` with `AddSingleton<ICorrelationAccessor, AsyncLocalCorrelationAccessor>()`
      and `AddMassTransitTestHarness(x => { ... })` where the bus is configured to use the two outbound
      filters (`UseSendFilter(typeof(OutboundCorrelationSendFilter<>))` / `UsePublishFilter(...)`) + the
      inbound consume filter (`UseConsumeFilter(typeof(InboundCorrelationConsumeFilter), ctx)`), plus a probe
      consumer for a test `ICorrelated` message so the inbound filter runs.
    - Define a tiny test record implementing `ICorrelated` (6 Guids) for the harness.
    - `await harness.Start();`
    - INBOUND proof: set the ambient accessor to a known Guid string; publish/send the test message so the
      probe consumer receives it; assert the inbound filter populated the accessor and that the consume
      executed under the `CorrelationKeys.LogScope` scope (assert the accessor value, and/or capture the
      MEL scope via a test ILogger/ILoggerProvider and assert a `"CorrelationId"` scope entry).
    - OUTBOUND proof: set the ambient accessor to a Guid-parseable id; publish the `ICorrelated` test
      message via the harness bus; assert the published/sent envelope's `CorrelationId` equals that Guid
      (read it from `harness.Published` / the sent context — the SAME envelope surface Phase 20's synthetic
      harness will assert). Use a Guid-parseable id (Open-Q 2 / Assumption A3) so `context.CorrelationId` is set.
    - `await harness.Stop();`
    Confirm the exact `ITestHarness` API surface (`harness.Bus`, `harness.Published`, `IPublishEndpoint`)
    against MassTransit 8.5.5 at build time. Thread `TestContext.Current.CancellationToken`.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter FullyQualifiedName~Console</automated>
  </verify>
  <acceptance_criteria>
    - `ConsoleCorrelationFilterTests.cs` exists, contains `AddMassTransitTestHarness`
    - Contains a test record implementing `ICorrelated`
    - INBOUND assertion: asserts the `ICorrelationAccessor` value is populated after consume (and/or the `CorrelationId` scope is present)
    - OUTBOUND assertion: asserts the published/sent envelope `CorrelationId` equals the ambient Guid id
    - Uses a Guid-parseable correlation id (e.g. `Guid.NewGuid()`)
    - `dotnet test ... --filter FullyQualifiedName~Console` exits 0
  </acceptance_criteria>
  <done>Both filters are exercised via the in-memory harness: inbound populates the accessor + CorrelationId scope, outbound stamps the envelope CorrelationId. D-02 fact #6 GREEN; CORR-01/CORR-02 validated standalone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase close gate — full suite GREEN 3-consecutive + dual-SHA</name>
  <files>.planning/STATE.md</files>
  <action>
    Claude automates the gate run first (capture BEFORE snapshot, run the suite 3x, capture AFTER snapshot,
    confirm zero-warning build), THEN pauses for the operator to confirm the result. This is a
    checkpoint:human-verify task — the automation below is mandatory before the pause.
  </action>
  <what-built>
    The full BaseConsole.Core library validated standalone: all five Console test classes pass alongside
    the existing v3.3.0 suite. This task runs the phase close gate (D-03): full `dotnet test` GREEN
    3-consecutive + dual-SHA (`psql \l` + `redis-cli --scan`) BEFORE=AFTER. NOT triple-SHA — no real
    broker resources exist this phase (in-memory harness only).
  </what-built>
  <how-to-verify>
    Claude automates first, then the operator confirms:
    1. Capture BEFORE snapshot SHAs:
       - `psql \l` listing (the existing dev Postgres tier) → SHA
       - `redis-cli --scan` keyspace dump (the existing dev Redis tier) → SHA
       Use the existing phase-close gate script pattern from prior phases (e.g. the Phase 16 close scripts)
       adapted to dual-SHA (drop any rabbitmqctl line — D-03).
    2. Run the FULL suite three consecutive times: `dotnet test` (solution root). All three must be GREEN
       (note: the existing Observability tests can need the OTel Collector warmed — re-run per the documented
       fixture-lifecycle robustness; the Console subset must be GREEN every run).
    3. Capture AFTER snapshot SHAs (`psql \l` + `redis-cli --scan`); assert BEFORE == AFTER (no leaked
       test databases, no leaked Redis keys — the Console tests use in-memory transport + dead Redis port,
       so neither tier should change).
    4. Confirm zero-warning build both configs: `dotnet build -c Release` and `dotnet build -c Debug`.
  </how-to-verify>
  <verify>
    <automated>dotnet test</automated>
  </verify>
  <resume-signal>Type "approved" once 3-consecutive GREEN + dual-SHA BEFORE=AFTER are confirmed, or describe failures.</resume-signal>
  <done>Full suite GREEN 3-consecutive, dual-SHA (psql \l + redis-cli --scan) BEFORE=AFTER, zero-warning build both configs; operator approved. Phase 18 closeout complete (D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → library | Tests are trusted code; the in-memory harness uses no real network. |
| dual-SHA gate → dev Postgres/Redis | Read-only snapshot of existing dev tiers; no writes expected from Console tests. |

## STRIDE Threat Register

This plan is test + validation only — it ships no production surface. Security of the production surfaces
is covered by the threat models in Plans 01-03; this plan PROVES those mitigations:
- T-18-08 (health body disclosure) is asserted by `ConsoleHealthLiveTests.Live_Body_Has_No_Secrets`.
- T-18-09 (live flips on dep blip) is asserted by `Live_Returns_200_When_Redis_And_RabbitMQ_Dead`.
- T-18-07 (traces resurrection) is asserted by `ConsoleObservabilityTests.No_TracerProvider_Resolvable`.
- T-18-11 (false-Healthy ready) is asserted by the ready-reflects-bus-state assertion.

No new trust boundary or HIGH threat is introduced by adding tests. The dual-SHA gate (vs triple-SHA) is
the correct disposition because no real broker resource is created this phase (D-03).
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter FullyQualifiedName~Console` exits 0.
- Full `dotnet test` GREEN 3-consecutive.
- `dotnet build -c Release` and `-c Debug` zero-warning.
- Dual-SHA (`psql \l` + `redis-cli --scan`) BEFORE == AFTER.
</verification>

<success_criteria>
- Test project references BaseConsole.Core; ConsoleTestHostFixture boots the full chain in-memory with dead deps (CONSOLE-01/03/04/05).
- /health/live = 200 with both deps dead; /health/startup 200/503; no-secrets body (CONSOLE-HEALTH-01/02/04).
- /health/ready reflects bus state (CONSOLE-HEALTH-03).
- No TracerProvider; MeterProvider present; no AspNetCore/HttpClient instrumentation (CONSOLE-02).
- Both correlation filters exercised via harness — accessor + scope inbound, envelope CorrelationId outbound (CORR-01/02).
- Phase close gate: full suite GREEN 3-consecutive + dual-SHA BEFORE=AFTER (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/18-baseconsole-core-library/18-04-SUMMARY.md`.
</output>
