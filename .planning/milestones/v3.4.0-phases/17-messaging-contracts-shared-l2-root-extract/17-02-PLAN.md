---
phase: 17-messaging-contracts-shared-l2-root-extract
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/BaseApi.Service/Features/Orchestration/Projection/ProcessorProjection.cs
  - src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs
  - src/BaseApi.Service/Features/Orchestration/Projection/RedisL2Cleanup.cs
  - tests/BaseApi.Tests/Features/Orchestration/Projection/ProjectionRecordRoundTripTests.cs
  - tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs
  - tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs
autonomous: false
requirements:
  - MSG-CONTRACTS-04
must_haves:
  truths:
    - "All 8 consumers of the moved records resolve them from Messaging.Contracts.Projections via a using-swap"
    - "The solution builds zero-warning in Release AND Debug"
    - "ProjectionRecordRoundTripTests stays GREEN (the byte-identical wire-shape proof for MSG-CONTRACTS-04 / SC#3)"
    - "The full v3.3.0 test suite stays GREEN across 3 consecutive runs (behavior-preserving using-swap)"
    - "Redis (redis-cli --scan) and psql (\\l) snapshots are byte-identical BEFORE=AFTER (no leak, no runtime change)"
  artifacts:
    - path: "src/BaseApi.Service/Features/Orchestration/Projection/RedisL2Cleanup.cs"
      provides: "L2 cleanup that deserializes the moved root after using-swap (the CONTEXT-omitted consumer)"
      contains: "using Messaging.Contracts.Projections;"
    - path: "tests/BaseApi.Tests/Features/Orchestration/Projection/ProjectionRecordRoundTripTests.cs"
      provides: "Wire-shape regression guard that must stay GREEN through the move"
      contains: "using Messaging.Contracts.Projections;"
  key_links:
    - from: "src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs"
      to: "Messaging.Contracts.Projections.WorkflowRootProjection"
      via: "using Messaging.Contracts.Projections; + new WorkflowRootProjection(...)"
      pattern: "using Messaging.Contracts.Projections;"
    - from: "src/BaseApi.Service/Features/Orchestration/Projection/ProcessorProjection.cs"
      to: "Messaging.Contracts.Projections.LivenessProjection"
      via: "using Messaging.Contracts.Projections; + nested Liveness property"
      pattern: "using Messaging.Contracts.Projections;"
---

<objective>
Swap all eight consumers of the moved `WorkflowRootProjection` / `LivenessProjection` records to
the new `Messaging.Contracts.Projections` namespace (a behavior-preserving using-swap, no logic
change), then run the terminal gate: zero-warning Release + Debug, 3-consecutive-GREEN full suite,
and byte-identical Redis/psql snapshots BEFORE=AFTER.

Purpose: This closes MSG-CONTRACTS-04 (single-source-of-truth L2 root read-shape) and the
cross-cutting SC#5 (zero-warning + v3.3.0 suite stays GREEN). The complete consumer set is 8 files
(3 production + 5 test) — CONTEXT.md's canonical list named only 4 of them; RESEARCH Pitfall 1 +
PATTERNS.md enumerate all 8 by path. Critically, `RedisL2Cleanup.cs` (production) and four of the
five test files are NOT in CONTEXT's canonical list. A solution-wide `dotnet build` surfacing CS0246
is the safety net, not the plan — every file is enumerated below.

Output: A solution that builds and passes the full v3.3.0 suite unchanged, proving the extract was
behavior-preserving.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-messaging-contracts-shared-l2-root-extract/17-CONTEXT.md
@.planning/phases/17-messaging-contracts-shared-l2-root-extract/17-RESEARCH.md
@.planning/phases/17-messaging-contracts-shared-l2-root-extract/17-PATTERNS.md
@.planning/phases/17-messaging-contracts-shared-l2-root-extract/17-01-SUMMARY.md

<interfaces>
<!-- The complete verified consumer set (RESEARCH Pitfall 1 + PATTERNS.md). Line numbers verified against live v3.3.0 code 2026-05-30. -->

Plan 01 created the records at namespace `Messaging.Contracts.Projections` (flat-root for the
net-new types `ICorrelated`/`Start`/`Stop`/`CorrelationKeys`; `Projections` sub-namespace for the
two moved records). Confirm the actual namespace from 17-01-SUMMARY.md before swapping — if Plan 01
used flat `Messaging.Contracts` for the records instead, use `using Messaging.Contracts;` here.

PRODUCTION consumers (in `namespace BaseApi.Service.Features.Orchestration.Projection;` — previously
needed NO using since same-namespace; MUST now ADD the new using):

| # | File | Reference site (verified) |
|---|------|---------------------------|
| 1 | src/BaseApi.Service/Features/Orchestration/Projection/ProcessorProjection.cs | line 14: `[property: JsonPropertyName("liveness")] LivenessProjection Liveness` (stays internal sealed record — D-06) |
| 2 | src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs | line 60: `new LivenessProjection(...)`; line 66: `new WorkflowRootProjection(...)`; `<see cref>` in docs lines 12,21 (stays internal sealed class — D-06) |
| 3 | src/BaseApi.Service/Features/Orchestration/Projection/RedisL2Cleanup.cs | line 45: `JsonSerializer.Deserialize<WorkflowRootProjection>(rootJson!)!.EntryStepIds` (stays internal sealed class — D-06; NOT in CONTEXT canonical list — Pitfall 1) |

TEST consumers (already carry an explicit `using BaseApi.Service.Features.Orchestration.Projection;`
— that line STAYS, because StepProjection/ProcessorProjection remain there; ADD the new using
alongside it). Public types flow transitively via BaseApi.Service's ProjectReference — NO test-csproj
ProjectReference change needed (Pitfall 2):

| # | File | using line | Moved-type reference (verified) |
|---|------|-----------|---------------------------------|
| 4 | tests/BaseApi.Tests/Features/Orchestration/Projection/ProjectionRecordRoundTripTests.cs | :4 | constructs Root (:40) + Liveness (:34-35); asserts exact camelCase keys (:49-57) — the SC#3 byte-identical guard, must stay GREEN |
| 5 | tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs | :5 | `Deserialize<WorkflowRootProjection>` (:137) |
| 6 | tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs | :4 | `Deserialize<WorkflowRootProjection>` ×3 (:124,:136,:177) |
| 7 | tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs | :2 | `new LivenessProjection` (:35) + `new WorkflowRootProjection` (:43) |
| 8 | tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs | :4 | `Deserialize<WorkflowRootProjection>` (:155) |

Established close-gate cadence (STATE.md): 3-consecutive-GREEN full suite + zero-warning Release AND
Debug + byte-identical Redis (`redis-cli --scan`) and psql (`\l`) snapshots BEFORE=AFTER. The
triple-SHA `rabbitmqctl` arm is NOT applicable this phase (no broker wired — RESEARCH line 492);
this is the existing DUAL-snapshot gate. Reference: scripts/phase-16-close.ps1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Using-swap all 8 consumers to Messaging.Contracts.Projections and build the solution</name>
  <files>src/BaseApi.Service/Features/Orchestration/Projection/ProcessorProjection.cs, src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs, src/BaseApi.Service/Features/Orchestration/Projection/RedisL2Cleanup.cs, tests/BaseApi.Tests/Features/Orchestration/Projection/ProjectionRecordRoundTripTests.cs, tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs, tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs, tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs, tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs</files>
  <read_first>
    - .planning/phases/17-messaging-contracts-shared-l2-root-extract/17-01-SUMMARY.md (CONFIRM the exact namespace Plan 01 used for the moved records)
    - src/BaseApi.Service/Features/Orchestration/Projection/ProcessorProjection.cs
    - src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs
    - src/BaseApi.Service/Features/Orchestration/Projection/RedisL2Cleanup.cs
    - tests/BaseApi.Tests/Features/Orchestration/Projection/ProjectionRecordRoundTripTests.cs
    - tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs
    - tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs
    - tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs
    - tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs
  </read_first>
  <action>
    Add `using Messaging.Contracts.Projections;` to each of the 8 files below (use `using Messaging.Contracts;`
    instead IF 17-01-SUMMARY.md reports the moved records landed in the flat root namespace). Make NO other
    change — no logic edits, no record/visibility edits, no test-assertion edits. The records stay where Plan 01
    put them; these consumers only re-point their import.

    PRODUCTION (3 files — these were in the same namespace as the records and had NO using for them; ADD the import
    alongside their existing usings, keeping alphabetical/existing ordering):
    1. src/BaseApi.Service/Features/Orchestration/Projection/ProcessorProjection.cs — add `using Messaging.Contracts.Projections;`
    2. src/BaseApi.Service/Features/Orchestration/Projection/RedisProjectionWriter.cs — add `using Messaging.Contracts.Projections;`
       (place it with the existing usings, e.g. after `using BaseApi.Service.Features.Orchestration;`)
    3. src/BaseApi.Service/Features/Orchestration/Projection/RedisL2Cleanup.cs — add `using Messaging.Contracts.Projections;`

    TESTS (5 files — each already has `using BaseApi.Service.Features.Orchestration.Projection;`; KEEP that line
    (StepProjection/ProcessorProjection still live there) and ADD the new using alongside it):
    4. tests/BaseApi.Tests/Features/Orchestration/Projection/ProjectionRecordRoundTripTests.cs — add `using Messaging.Contracts.Projections;`
    5. tests/BaseApi.Tests/Features/Orchestration/HappyPathE2EFacts.cs — add `using Messaging.Contracts.Projections;`
    6. tests/BaseApi.Tests/Features/Orchestration/IdempotencyFacts.cs — add `using Messaging.Contracts.Projections;`
    7. tests/BaseApi.Tests/Features/Orchestration/StopCleanupFacts.cs — add `using Messaging.Contracts.Projections;`
    8. tests/BaseApi.Tests/Features/Orchestration/RedisProjectionWriterFacts.cs — add `using Messaging.Contracts.Projections;`

    Do NOT add a `ProjectReference` to `Messaging.Contracts` in `BaseApi.Tests.csproj` — the public types flow
    transitively via the `BaseApi.Service` ProjectReference (Pitfall 2). Do NOT remove the existing
    `using BaseApi.Service.Features.Orchestration.Projection;` from the test files (still needed for the
    types that stayed internal). After editing, build the whole solution in Debug — any missed file surfaces
    as CS0246 ("type or namespace not found") on `WorkflowRootProjection`/`LivenessProjection` (the safety net).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 8 files contains `using Messaging.Contracts.Projections;` (or `using Messaging.Contracts;` if Plan 01 used flat root — match 17-01-SUMMARY.md)
    - The 3 production files keep their original `internal sealed` declarations (ProcessorProjection `internal sealed record`; RedisProjectionWriter + RedisL2Cleanup `internal sealed class`) — D-06 unchanged
    - The 5 test files STILL contain `using BaseApi.Service.Features.Orchestration.Projection;` (not removed)
    - `tests/BaseApi.Tests/BaseApi.Tests.csproj` does NOT add a new `ProjectReference` to `Messaging.Contracts` (transitive flow — Pitfall 2)
    - `dotnet build SK_P.sln -c Debug` exits 0 with zero warnings and zero CS0246 errors
  </acceptance_criteria>
  <done>All 8 consumers resolve the moved records from the new namespace, the solution builds zero-warning in Debug, and no consumer was missed (no CS0246).</done>
</task>

<task type="auto">
  <name>Task 2: Terminal gate — zero-warning Release + Debug, 3-consecutive-GREEN, dual-snapshot BEFORE=AFTER</name>
  <files>scripts/phase-17-close.ps1 (optional copy of phase-16-close.ps1, EF-migration + HEALTH-immutable arms stripped)</files>
  <read_first>
    - scripts/phase-16-close.ps1 (the established dual-snapshot 3-GREEN close-gate analog — psql \l + redis-cli --scan SHA-256 BEFORE=AFTER; reuse its structure, OMIT the EF-migration + HEALTH-immutable arms which are Phase-16-specific)
    - .planning/STATE.md (gate cadence: 3-consecutive-GREEN + byte-identical Redis/psql snapshots; SDK 8.0.421)
    - tests/BaseApi.Tests/Features/Orchestration/Projection/ProjectionRecordRoundTripTests.cs (the SC#3 wire-shape guard)
  </read_first>
  <action>
    Run the terminal verification (no production code change — this is the proof the extract was behavior-preserving).
    Per RESEARCH line 492, the rabbitmqctl arm of the triple-SHA is NOT applicable this phase (no broker wired);
    use the existing DUAL-snapshot gate (psql \l + redis-cli --scan).

    1. Pre-flight: confirm the compose stack is healthy (`postgres`, `redis`, `otel-collector`, `elasticsearch`,
       `prometheus`) as in phase-16-close.ps1 — the v3.3.0 integration suite needs Postgres + Redis live.
    2. Capture BEFORE snapshots: psql `\l` (`docker compose exec -T postgres psql -U postgres -lqt`) and
       `redis-cli --scan` (`docker exec sk-redis redis-cli --scan | Sort-Object -CaseSensitive`), SHA-256 each.
    3. Zero-warning build gate, BOTH configs (TreatWarningsAsErrors is global — a warning is a non-zero exit):
       `dotnet build SK_P.sln -c Release` then `dotnet build SK_P.sln -c Debug`. Recommend `dotnet clean` first
       to avoid stale-assembly false-greens (the old BaseApi.Service assembly no longer carries the moved types —
       RESEARCH Runtime State Inventory).
    4. Run the wire-shape guard explicitly first (fast feedback on the byte-identical proof / SC#3 / MSG-CONTRACTS-04):
       `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter-class *ProjectionRecordRoundTripTests`
    5. 3-consecutive-GREEN full suite: run `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj` three times;
       all three must exit 0 with an identical Passed-fact count (the v3.3.0 baseline was 235 facts — confirm the
       count is unchanged, proving no test was dropped or added by the swap).
    6. Capture AFTER snapshots; assert psql SHA-256 BEFORE=AFTER AND redis SHA-256 BEFORE=AFTER (no leak, no
       runtime state change). You may copy scripts/phase-16-close.ps1 to scripts/phase-17-close.ps1 and strip the
       Phase-16-specific EF-migration + HEALTH-immutable assertion blocks, or run the steps inline.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release; dotnet build SK_P.sln -c Debug; dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` exits 0 with zero warnings
    - `dotnet build SK_P.sln -c Debug` exits 0 with zero warnings
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter-class *ProjectionRecordRoundTripTests` exits 0 (the byte-identical wire-shape proof — MSG-CONTRACTS-04 / SC#3)
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj` exits 0 on 3 consecutive runs with an identical Passed count each run (matching the v3.3.0 baseline of 235 facts — no facts dropped/added by the swap)
    - psql `\l` SHA-256 BEFORE == AFTER (recorded in the SUMMARY)
    - redis-cli `--scan` SHA-256 BEFORE == AFTER (recorded in the SUMMARY)
  </acceptance_criteria>
  <done>The solution builds zero-warning in both configurations, the wire-shape guard and full v3.3.0 suite are GREEN 3× consecutively at the unchanged fact count, and both snapshots are byte-identical BEFORE=AFTER — proving the extract is behavior-preserving (MSG-CONTRACTS-04 + SC#5).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 17 close checkpoint — operator confirms gate results</name>
  <action>Pause for operator confirmation of the Task 2 gate results before closing the phase. No code change — this is a verification gate. The executor presents the 3-GREEN fact count, the two dual-snapshot SHA-256 pairs, the CPM-pin-only grep result, and a spot-check of one moved record, then waits for the resume signal.</action>
  <what-built>Phase 17 complete: the `Messaging.Contracts` leaf library exists with the frozen vocabulary + moved L2 records, MassTransit is CPM-pinned at 8.5.5, all 8 consumers are using-swapped, and the terminal gate ran (zero-warning Release+Debug, 3-consecutive-GREEN at the v3.3.0 fact count, dual-snapshot BEFORE=AFTER).</what-built>
  <how-to-verify>
    1. Confirm the 3-GREEN run reported the same Passed-fact count all three times and it matches the v3.3.0 baseline (235) — no facts silently dropped/added.
    2. Confirm the psql `\l` and redis-cli `--scan` SHA-256 values are byte-identical BEFORE=AFTER (paste both pairs).
    3. Confirm `grep -rn "PackageReference Include=\"MassTransit" src/` is still empty (the CPM-pin-only invariant — D-03).
    4. Spot-check one moved record on disk: `src/Messaging.Contracts/Projections/WorkflowRootProjection.cs` is `public sealed record` and still carries all five `[property: JsonPropertyName(...)]` camelCase targets verbatim.
    Then append the two SHA-256 pairs + fact count to STATE.md's Phase 17 close entry.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 17, or describe any gate failure / fact-count drift / snapshot divergence to investigate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The using-swap is a compile-time re-import only; no I/O, no network, no new untrusted-input path. The moved records continue to deserialize only from the self-written Redis L2 (trusted), unchanged from v3.3.0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering (integrity) | The WebApi-writes / Orchestrator-reads L2 root contract surviving the using-swap | mitigate | `ProjectionRecordRoundTripTests` (serializes under default `JsonSerializerOptions` and asserts exact camelCase keys) MUST stay GREEN through the swap — the byte-identical wire-shape proof (SC#3 / MSG-CONTRACTS-04). Plus the full integration suite (`RedisProjectionWriterFacts`, `HappyPathE2EFacts`, `IdempotencyFacts`, `StopCleanupFacts`) exercises real write/read/cleanup round-trips. |
| T-17-05 | (data-integrity / leak) | Residual Redis/psql state from the behavior-preserving move | mitigate | Dual-snapshot SHA-256 (`psql \l` + `redis-cli --scan`) BEFORE=AFTER asserts no schema/keyspace change — the C# type identity (assembly/namespace) is decoupled from the JSON wire shape by the `[JsonPropertyName]` pins (RESEARCH Runtime State Inventory: stored values are unaffected). |
| T-17-06 | (regression / scope-of-move) | A missed consumer leaving the solution in a broken/partial state | mitigate | All 8 consumers enumerated by path (RESEARCH Pitfall 1 — including the CONTEXT-omitted `RedisL2Cleanup.cs` + 4 extra test files); solution-wide `dotnet build` surfaces any miss as CS0246 (the safety net); zero-warning Release+Debug gate is terminal. |

No `high`-severity attacker-driven threats: behavior-preserving refactor, no new attack surface (RESEARCH Security Domain).
</threat_model>

<verification>
- All 8 consumers using-swapped; `dotnet build SK_P.sln -c Debug` and `-c Release` both exit 0 zero-warning.
- `ProjectionRecordRoundTripTests` GREEN (byte-identical wire shape — MSG-CONTRACTS-04 / SC#3).
- Full v3.3.0 suite GREEN 3× consecutively at the unchanged baseline fact count (235).
- psql `\l` + redis-cli `--scan` SHA-256 BEFORE=AFTER (dual-snapshot; rabbitmqctl arm N/A this phase).
- No MassTransit PackageReference repo-wide; transitive test visibility (no test-csproj ProjectReference added).
</verification>

<success_criteria>
- MSG-CONTRACTS-04: L2 root read-shape lives in `Messaging.Contracts`, single source of truth — `BaseApi.Service` writes it and (future) `Orchestrator` reads it; no duplicated shape; wire shape byte-identical (proven by `ProjectionRecordRoundTripTests`). ✓
- SC#5 (cross-cutting): zero-warning Release + Debug; v3.3.0 suite stays GREEN 3× consecutively; dual-snapshot BEFORE=AFTER — the using-swap is behavior-preserving. ✓
</success_criteria>

<output>
After completion, create `.planning/phases/17-messaging-contracts-shared-l2-root-extract/17-02-SUMMARY.md`
</output>
