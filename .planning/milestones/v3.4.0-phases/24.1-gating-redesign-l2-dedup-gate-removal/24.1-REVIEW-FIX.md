---
phase: 24.1-gating-redesign-l2-dedup-gate-removal
fixed_at: 2026-06-01T00:00:00Z
review_path: .planning/phases/24.1-gating-redesign-l2-dedup-gate-removal/24.1-REVIEW.md
iteration: 2
findings_in_scope: 6
fixed: 2
skipped: 2
already_fixed: 2
status: all_actionable_resolved
---

# Phase 24.1: Code Review Fix Report

**Fixed at:** 2026-06-01
**Source review:** .planning/phases/24.1-gating-redesign-l2-dedup-gate-removal/24.1-REVIEW.md
**Iteration:** 2

**Summary:**
- Findings in scope: 6 (2 Warning, 4 Info — `fix_scope: all`)
- Fixed this pass: 2 (IN-01, IN-03)
- Already fixed (prior pass): 2 (WR-01, WR-02)
- Skipped / no-action: 2 (IN-02 won't-fix-intentional, IN-04 no-defect)

All actionable findings are resolved. The Orchestrator project builds green (0 warnings,
0 errors; TreatWarningsAsErrors on) after the IN-03 change.

## Fixed Issues

### IN-01: Stale doc reference to the Phase-22 cleanup loop in test comment

**File modified:** `tests/BaseApi.Tests/Features/Orchestration/StopGateFacts.cs`
**Commit:** 96374da
**Applied fix:** Replaced the stale Phase-22 "root KeyDeleteAsync returned true → tolerant
traverse-and-delete of the per-step keys" wording in the comment above
`Stop_RedisDown_OnPostDeleteCleanup_500_KeyDeleteAsync` with the 24.1 probe-then-cleanup
ordering: "the per-id KeyExistsAsync probe returned true → StopCleanupAsync reads the root,
BFS-collects step keys, and deletes root+steps in one batch." Comment-only change; the test
body and assertions (redisOp="KeyDeleteAsync") were untouched.

### IN-03: Redundant explicit `using System.Linq;` under enabled ImplicitUsings

**File modified:** `src/Orchestrator/Dispatch/StepAdvancement.cs`
**Commit:** 4bf56cd
**Applied fix:** Verified `<ImplicitUsings>enable</ImplicitUsings>` is set in repo-root
`Directory.Build.props` (line 31), which the Orchestrator project inherits per its csproj
comment. Removed the explicit `using System.Linq;` (line 1). Confirmed
`dotnet build src/Orchestrator/Orchestrator.csproj` stays green — `Enumerable.Empty<Guid>()`
still resolves via implicit usings (Build succeeded, 0 Warning(s), 0 Error(s)).

## Already-Fixed Issues (prior pass — not re-touched)

### WR-01: Start-path R2 compensation over-SREM / misleading comment

**Status:** Already fixed in commits 5f794bc + a41e8ba (prior run). Not modified this pass.

### WR-02: `RedisL2Cleanup.StopCleanupAsync` non-atomic across full sequence / doc tightening

**Status:** Already fixed in commit b21eef1 (prior run). Not modified this pass.

## Skipped Issues

### IN-02: `redisOp` tag value "UpsertAsync" obscures actual faulting op

**File:** `src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs:146-152, 168-176, 193-201`
**Reason:** Won't-fix — intentional. The single stable op name is a documented, tested contract
(OBSERV-REDIS-03): one caller-stable `redisOp` for the whole Start L2-write path so the 500 body
is deterministic. The review itself states "No code change required if the stable-op-name contract
is intended" — it is intended. Adding catch-site logging of the true faulting op would be a
behavior change / scope creep and was explicitly declined.
**Original issue:** The surfaced op name does not identify which Redis call actually faulted, which
can complicate operational debugging from the 500 body alone (logs/traces still carry the real op).

### IN-04: `Program.cs` instanceId fallback / gate-removal comment observations

**File:** `src/Orchestrator/Program.cs:23-42`
**Reason:** No action — the review explicitly records "No defect" / "None required — recorded for
completeness of the gate-removal verification." Acknowledged; no change made. (The fresh-GUID
fallback under auto-delete `Temporary = true` endpoints is intentional and consistent with prior
phases; no residual delayed-scheduler / plugin reference remains.)
**Original issue:** Recorded for completeness only — no defect.

---

_Fixed: 2026-06-01_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
