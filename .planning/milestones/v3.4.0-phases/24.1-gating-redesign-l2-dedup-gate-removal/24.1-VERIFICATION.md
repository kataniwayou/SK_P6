---
phase: 24.1-gating-redesign-l2-dedup-gate-removal
verified: 2026-06-01T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 24.1: Gating Redesign Verification Report

**Phase Goal:** Close the FAILED Phase 24 verification by applying the locked 24.1 gating redesign — L2-existence dedup with parent-index compensation (R1/R2), atomic discover-then-delete Stop (R3), orphan-free reachability invariant (R4), removal of the boot gate + scheduled redelivery + delayed-message-scheduler plugin dependency with L1-only graceful result handling (R5), terminal-step NextStepIds guard / WR-02 (R6), and a clean-build green suite (R7).
**Verified:** 2026-06-01
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | R1: No `KeyDeleteAsync(Root(` as the lead Stop op; `KeyExistsAsync` probe present on both Start and Stop | VERIFIED | `OrchestrationService.cs:146` (Start probe), `:312` (Stop probe); only `KeyDeleteAsync(Root(` in codebase is inside `RedisL2Cleanup.cs:92` — inside the batch, never as the lead caller op |
| 2 | R2: Compensation SREM (Start fault path) and SADD (Stop fault path) present | VERIFIED | `OrchestrationService.cs:222` — `SetRemoveAsync(ParentIndex(), workflowId)` in Start catch; `:343` — `SetAddAsync(ParentIndex(), workflowId)` in Stop catch |
| 3 | R3/R4: Atomic root+steps delete in `RedisL2Cleanup` + R4 reachability invariant comment recorded | VERIFIED | `RedisL2Cleanup.cs:91-95` — one `CreateBatch()` / `Execute()` deletes root + all step keys; `:80-90` — R4 invariant comment verbatim: "L2 holds only the validated reachable graph and is NEVER mutated between Start and Stop...no unreachable orphan per-step key can exist" |
| 4 | R5: Zero `GateClosedException`, zero `gate.IsReady` in consumers, zero `UseScheduledRedelivery`, zero `UseDelayedMessageScheduler`/`AddDelayedMessageScheduler` in src; `GateClosedException.cs` deleted | VERIFIED | Grep on `src/`: `GateClosedException` — 0 matches; `gate.IsReady` in consumers — 0 matches (2 matches in health check files only — preserved by design); `UseScheduledRedelivery` — 0 matches; `UseDelayedMessageScheduler`/`AddDelayedMessageScheduler` in src — 0 live calls (2 comment-only matches); `GateClosedException.cs` — file not found in repo |
| 5 | R5 (L1-only graceful result): `ResultConsumer` has no gate block; L1 miss acks cleanly | VERIFIED | `ResultConsumer.cs:48-55` — body is direct `TryGet` / `TryGetValue` miss → `log + return` (ack), no gate, no throw; `ResultAckTests.cs:51` — `ResultForIdAbsentFromL1_AcksGracefully_NoThrow_NoDispatch` test present |
| 6 | R6: `SelectNext` guards null `NextStepIds` with `?? Enumerable.Empty<Guid>()` | VERIFIED | `StepAdvancement.cs:40` — `foreach (var nextId in completed.NextStepIds ?? Enumerable.Empty<Guid>())`; `StepAdvancementTests.cs:118` — `TerminalStep_NullNextStepIds_YieldsZeroNext_NoThrow` test present |
| 7 | R7: Clean-build green — from-scratch `dotnet clean` + `dotnet test SK_P.sln -c Debug` = Failed 0 / Passed 335 / Total 335; Release build 0 warnings | VERIFIED | Operator-verified at Task 6 checkpoint (approved): 335/335, Features.Orchestration.* 78/78, Orchestrator.* 66/66, real-stack E2E 5/5; Release build confirmed this session: `Build succeeded. 0 Warning(s) 0 Error(s)` |

**Score:** 7/7 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/BaseApi.Service/Features/Orchestration/OrchestrationService.cs` | L2-root dedup probe + parent compensation | VERIFIED | Start: `KeyExistsAsync` probe at :146, SREM compensation at :222; Stop: `KeyExistsAsync` probe at :312, SADD compensation at :343; no lead `KeyDeleteAsync(Root(` |
| `src/BaseApi.Service/Features/Orchestration/Projection/RedisL2Cleanup.cs` | Atomic batch delete + R4 invariant comment | VERIFIED | Single `CreateBatch()` at :91-95; R4 comment at :80-85; hoisted SREM at :45 (pre-batch, idempotent) |
| `src/Orchestrator/Consumers/ResultConsumer.cs` | No gate block; L1-only graceful result | VERIFIED | Constructor: `IWorkflowL1Store`, `StepAdvancement`, `IStepDispatcher`, `ILogger` — no `IStartupGate`; body: `TryGet`/`TryGetValue` miss → return |
| `src/Orchestrator/Consumers/StartOrchestrationConsumer.cs` | No gate block | VERIFIED | Constructor: `WorkflowLifecycle`, `ILogger` — no `IStartupGate`; no `gate.IsReady` |
| `src/Orchestrator/Consumers/StopOrchestrationConsumer.cs` | No gate block | VERIFIED | Constructor: `WorkflowLifecycle`, `ILogger` — no `IStartupGate`; no `gate.IsReady` |
| `src/Orchestrator/Consumers/ResultConsumerDefinition.cs` | No `UseScheduledRedelivery` | VERIFIED | Only `UseMessageRetry(r => r.Immediate(3))` |
| `src/Orchestrator/Consumers/StartOrchestrationConsumerDefinition.cs` | No `UseScheduledRedelivery` | VERIFIED | Only `UseMessageRetry` + `Ignore<WorkflowRootNotFoundException>` |
| `src/Orchestrator/Consumers/StopOrchestrationConsumerDefinition.cs` | No `UseScheduledRedelivery` | VERIFIED | Only `UseMessageRetry` + `Ignore<WorkflowRootNotFoundException>` |
| `src/Orchestrator/Dispatch/StepAdvancement.cs` | `NextStepIds ?? Enumerable.Empty<Guid>()` guard | VERIFIED | :40 — `foreach (var nextId in completed.NextStepIds ?? Enumerable.Empty<Guid>())` |
| `src/Orchestrator/Program.cs` | No `AddDelayedMessageScheduler`/`UseDelayedMessageScheduler` live calls | VERIFIED | Only comment at :39-41 documenting removal; no live call |
| `src/Orchestrator/Consumers/GateClosedException.cs` | DELETED | VERIFIED | Glob search: no matches; grep on `src/` for `GateClosedException`: 0 matches |
| `tests/BaseApi.Tests/Orchestrator/GateClosedRedeliverTests.cs` | DELETED | VERIFIED | Glob search: no matches |
| `tests/BaseApi.Tests/Features/Orchestration/StartLoopFacts.cs` | `ReStart_IsFirstWin_NoOp` (first-win semantics) | VERIFIED | `:153` — `ReStart_IsFirstWin_NoOp` test asserting first-win no-op; old `ReStart_Removes_Orphan_Step` not present |
| `tests/BaseApi.Tests/Orchestrator/ResultAckTests.cs` | Graceful L1-miss ack test | VERIFIED | `:51` — `ResultForIdAbsentFromL1_AcksGracefully_NoThrow_NoDispatch` present |
| `tests/BaseApi.Tests/Orchestrator/StepAdvancementTests.cs` | Terminal null-NextStepIds test | VERIFIED | `:118` — `TerminalStep_NullNextStepIds_YieldsZeroNext_NoThrow` present |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `OrchestrationService.StopAsync` | `RedisL2Cleanup.StopCleanupAsync` | Called after `KeyExistsAsync` probe (R3) | WIRED | `:336` — `await _cleanup.StopCleanupAsync(workflowId, ct)` invoked only when `rootPresent=true`; no pre-delete of root |
| `StopAsync` R2 compensation | `db.SetAddAsync(ParentIndex())` | Catch on `StopCleanupAsync` throw | WIRED | `:343` — SADD in the catch block before rethrow |
| `StartAsync` R2 compensation | `db.SetRemoveAsync(ParentIndex())` | Catch on `UpsertAsync` throw | WIRED | `:222` — SREM in the catch block before rethrow |
| `ResultConsumer` | `StepAdvancement.SelectNext` | `:57` — `advancement.SelectNext(m.Outcome, completed, wf.Steps)` | WIRED | Called after confirmed L1 hit; terminal step produces empty enumerable (no NRE) |
| `StepAdvancement.SelectNext` | `Enumerable.Empty<Guid>()` null-guard | `?? Enumerable.Empty<Guid>()` at :40 | WIRED | Guard present; closes WR-02 NRE path |

---

## Data-Flow Trace (Level 4)

Not applicable — this phase modifies control-flow and lifecycle wiring, not data-rendering components. The key data flows (L2 root read, L1 store TryGet) were traced structurally via grep:

- `RedisL2Cleanup`: reads root via `StringGetAsync` before the delete batch — root is present at read time because `OrchestrationService` now probes (not deletes) first. Data flows correctly from root GET into the BFS walk.
- `ResultConsumer`: reads from `IWorkflowL1Store.TryGet` — no L2 read on this path (D-08 preserved).

---

## Behavioral Spot-Checks

| Behavior | Method | Result | Status |
|----------|--------|--------|--------|
| R5: No `GateClosedException` in `src/` | `grep GateClosedException src/` | 0 matches | PASS |
| R5: No `gate.IsReady` in consumers dir | `grep gate.IsReady src/Orchestrator/Consumers/` | 0 matches | PASS |
| R5: No `UseScheduledRedelivery` in `src/` | `grep UseScheduledRedelivery src/` | 0 matches | PASS |
| R5: No live `AddDelayedMessageScheduler`/`UseDelayedMessageScheduler` calls in `src/` | grep on `src/` | 0 live calls (2 comment-only) | PASS |
| R1/R3: No `KeyDeleteAsync(Root(` as lead Stop op in `OrchestrationService` | read file | Only `KeyExistsAsync` probe in StopAsync | PASS |
| R6: `NextStepIds ?? Enumerable.Empty<Guid>()` present | grep `StepAdvancement.cs` | Match at :40 | PASS |
| R7: Release build 0 warnings | `dotnet build SK_P.sln -c Release` | `0 Warning(s) 0 Error(s)` | PASS |
| R7: Clean-build green 335/335 | Operator-verified Task 6 checkpoint | Failed: 0 / Passed: 335 / Total: 335 | PASS (operator-approved) |

---

## Requirements Coverage

Phase 24.1 PLAN frontmatter declares `requirements: [R1, R2, R3, R4, R5, R6, R7]` — these are SPEC-local labels defined in `24.1-SPEC.md`, not REQUIREMENTS.md IDs. The SPEC maps them to REQUIREMENTS.md IDs as follows:

| SPEC Req | REQUIREMENTS.md ID | Description | Status | Evidence |
|----------|--------------------|-------------|--------|---------|
| R1 | WEBAPI-SUPPRESS-01 (supersedes D-04) | L2-root existence dedup on Start/Stop | SATISFIED | `KeyExistsAsync` probe in both Start (:146) and Stop (:312) of `OrchestrationService.cs` |
| R2 | WEBAPI-SUPPRESS-01 (parent compensation) | Parent index SREM/SADD on fault paths | SATISFIED | SREM at :222 (Start fault), SADD at :343 (Stop fault) |
| R3 | Absorbs Phase 24 locked fix (a) | Atomic discover-then-delete Stop | SATISFIED | `RedisL2Cleanup.cs` single batch :91-95; caller uses probe not pre-delete |
| R4 | — (invariant documentation) | Orphan-free reachability invariant recorded | SATISFIED | R4 comment block :80-85 in `RedisL2Cleanup.cs`; `ReStart_IsFirstWin_NoOp` test |
| R5 | ORCH-GATE-01 (supersedes D-06) | Boot gate removed; L1-only graceful result | SATISFIED | `GateClosedException.cs` deleted; no `gate.IsReady` in consumers; no `UseScheduledRedelivery`; no `AddDelayedMessageScheduler`; `ResultConsumer` L1-miss → return |
| R6 | WR-02 | `SelectNext` null `NextStepIds` terminal guard | SATISFIED | `:40` of `StepAdvancement.cs`; `TerminalStep_NullNextStepIds_YieldsZeroNext_NoThrow` test |
| R7 | — (green gate) | Clean-build green suite | SATISFIED | Operator-approved checkpoint: 335/335; Release build 0W/0E confirmed this session |

Note on REQUIREMENTS.md traceability: `ORCH-GATE-01`, `ORCH-RESULT-ACK-01`, and `ORCH-RESULT-02` appear as `[x]`/`[ ]` in REQUIREMENTS.md under Phase 24 requirements. The Phase 24.1 SPEC supersedes `ORCH-GATE-01` (removed, not patched). The orchestrator marks traceability at completion — not within scope of this verification, as stated in the phase brief.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `StepAdvancement.cs` | 1 | `using System.Linq;` explicit import (redundant under ImplicitUsings) | Info | No runtime impact — consistency-only, flagged in code review as IN-03 |
| `OrchestrationService.cs` | 207-212 | R2 catch-block SREM comment claims enforcement but pre-clean SREM already covers it | Info | Correctness-of-reasoning issue only; runtime behavior is correct; flagged in REVIEW WR-01 |
| `RedisL2Cleanup.cs` | 44 | SREM is a separate op before the batch; doc says "atomic" but only the delete batch is atomic | Info | Known, documented; `StopAsync` SADD compensation covers the SREM→delete window; flagged in REVIEW WR-02 |

No blockers. The two REVIEW warnings (WR-01, WR-02) are documentation/reasoning clarity issues, not runtime defects. They do not affect goal achievement.

---

## Human Verification Required

None. All acceptance criteria are verifiable programmatically via grep and build output. R7 green-suite evidence was operator-approved at the Task 6 checkpoint.

---

## Gaps Summary

No gaps. All 7 requirements verified against the actual codebase:

- R1/R3: `KeyExistsAsync` probe confirmed on both Start and Stop paths; `KeyDeleteAsync(Root(` appears only inside `RedisL2Cleanup`'s atomic batch — never as a lead caller op.
- R2: SREM compensation (Start write fault) and SADD compensation (Stop delete fault) both present in `OrchestrationService.cs`.
- R4: R4 invariant comment block present in `RedisL2Cleanup.cs`; `ReStart_IsFirstWin_NoOp` test replaces the superseded orphan-GC test.
- R5: `GateClosedException.cs` deleted, `GateClosedRedeliverTests.cs` deleted, zero `gate.IsReady` in consumers, zero `UseScheduledRedelivery`, zero live `AddDelayedMessageScheduler`/`UseDelayedMessageScheduler` calls; `ResultConsumer` is L1-only graceful-miss.
- R6: `?? Enumerable.Empty<Guid>()` guard at `StepAdvancement.cs:40`; terminal-step test present.
- R7: Release build 0 warnings/errors confirmed this session; full-suite 335/335 operator-verified.

The Phase 24 FAILED verification is closed by this phase. All previously-failing tests (3 via R3, 1 rewritten via R4) now pass.

---

_Verified: 2026-06-01_
_Verifier: Claude (gsd-verifier)_
