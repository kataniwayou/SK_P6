---
phase: 10-remove-schemaid-on-assignmententity-and-add-configschemaid-o
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs
  - src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs
  - src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs
  - src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs
autonomous: true
requirements:
  - ENTITY-07
  - VALID-15
must_haves:
  truths:
    - "AssignmentEntity no longer declares a SchemaId property; only StepId + Payload (+ inherited BaseEntity fields)"
    - "AssignmentCreateDto / AssignmentUpdateDto are 5-positional records (Name, Version, Description, StepId, Payload)"
    - "AssignmentReadDto is a 10-positional record (Id, Name, Version, Description, StepId, Payload, CreatedAt, UpdatedAt, CreatedBy, UpdatedBy)"
    - "AssignmentCreateDtoValidator + AssignmentUpdateDtoValidator both enforce only StepId NotEqual(Guid.Empty) — no SchemaId rule"
    - "AssignmentEntityConfiguration declares exactly ONE HasOne<>() block (StepEntity) — the SchemaEntity FK block at lines 44-49 is deleted"
    - "dotnet build SK_P.sln -c Release & -c Debug exit 0 with zero warnings (TreatWarningsAsErrors=true global gate)"
  artifacts:
    - path: "src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs"
      provides: "Post-Phase-10 Assignment entity shape (StepId + Payload only)"
      contains: "public sealed class AssignmentEntity : BaseEntity"
    - path: "src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs"
      provides: "5/5/10 positional DTO records — no SchemaId"
      contains: "public sealed record AssignmentCreateDto"
    - path: "src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs"
      provides: "Validators with single StepId rule only"
      contains: "public sealed class AssignmentCreateDtoValidator"
    - path: "src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs"
      provides: "EF config with single FK (StepEntity) — no SchemaEntity FK block"
      contains: "HasConstraintName(\"fk_assignment_step_id\")"
  key_links:
    - from: "AssignmentEntity.cs (no SchemaId property)"
      to: "AssignmentDtos.cs (no SchemaId in any of 3 records)"
      via: "symmetric field-shape removal — both entity and DTOs lose SchemaId in the same commit (Mapperly RMG012/RMG020 zero-fire invariant per CONTEXT D-10)"
      pattern: "SchemaId"
    - from: "AssignmentDtoValidator.cs (no SchemaId rule)"
      to: "AssignmentDtos.cs (no SchemaId field)"
      via: "FluentValidation RuleFor target reachability — removing the field WITHOUT removing the validator rule would produce a compile error (RuleFor expects a property selector); both must drop together"
      pattern: "RuleFor\\(x => x.SchemaId\\)"
    - from: "AssignmentEntityConfiguration.cs (no HasOne<SchemaEntity>() block)"
      to: "AssignmentEntity.cs (no SchemaId property)"
      via: "EF property-existence invariant — HasForeignKey(e => e.SchemaId) on a non-existent property would compile-fail; both must drop together"
      pattern: "fk_assignment_schema_id"
---

<objective>
Per CONTEXT D-02 commit #2: remove `SchemaId` from `AssignmentEntity` and its 4 satellite files atomically in a single commit. After this commit, the Assignment surface is `(StepId, Payload)`-only; `dotnet build` is still zero-warning (build gate preserves bisect-friendliness per D-02 — every commit is independently revertable).

Purpose: Strip one half of the Phase 10 field-shape revision in a single atomic refactor commit. Plan 03 adds `ConfigSchemaId` to Processor independently (D-02 — different feature folders, independent file sets, can ship in either order within Wave 2). Plan 04 then regenerates the migration with both deltas captured.

Output: 4 modified source files with all `SchemaId` references removed from the Assignment feature folder + EF configuration; `dotnet build` zero-warning Release + Debug; 1 git commit landing the refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-CONTEXT.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-SPEC.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md

<interfaces>
<!-- Current AssignmentEntity shape (read by planner) -->

src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs:
```csharp
public sealed class AssignmentEntity : BaseEntity
{
    public Guid StepId { get; set; }
    public Guid SchemaId { get; set; }     // DELETE
    public string Payload { get; set; } = string.Empty;
}
```

src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs (record shapes — exact positional order):
- CreateDto positional: (Name, Version, Description, StepId, SchemaId, Payload) → 5 after delete
- UpdateDto positional: same shape as Create
- ReadDto positional: (Id, Name, Version, Description, StepId, SchemaId, Payload, CreatedAt, UpdatedAt, CreatedBy, UpdatedBy) → 10 after delete

src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs current rules (VALID-15):
- Create lines 42-44: `RuleFor(x => x.SchemaId).NotEqual(Guid.Empty).WithMessage(...);`
- Update lines 83-85: same shape

src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs lines 44-49:
```csharp
entity.HasOne<SchemaEntity>()
    .WithMany()
    .HasForeignKey(e => e.SchemaId)
    .HasConstraintName("fk_assignment_schema_id")
    .OnDelete(DeleteBehavior.Restrict);
```

Mapperly mappers (NOT touched in this plan):
- src/BaseApi.Service/Features/Assignment/AssignmentEntityMapper.cs — zero changes (symmetric removal — entity + DTOs lose SchemaId together → RMG012/RMG020 do not fire per CONTEXT D-10)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove SchemaId property + update XML doc on AssignmentEntity</name>
  <files>src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs</files>
  <read_first>
    - src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs (current file — confirm line 27 is `public Guid SchemaId { get; set; }`)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (Assignment entity removal at lines 157-175)
  </read_first>
  <action>
    Edit `src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs` in place.

    Step 1 — XML `<summary>` doc-comment edits (currently lines 5-23):
    - Replace `"<c>StepId</c> and <c>SchemaId</c> are NON-nullable FKs to <c>StepEntity</c> and <c>SchemaEntity</c> respectively"` → `"<c>StepId</c> is a NON-nullable FK to <c>StepEntity</c>"`
    - Replace `"<c>fk_assignment_step_id</c> + <c>fk_assignment_schema_id</c> (wired by <c>AssignmentEntityConfiguration</c>) both use <c>OnDelete(Restrict)</c> — deleting a Step or Schema while an Assignment references it returns 23503 → 422"` → `"<c>fk_assignment_step_id</c> (wired by <c>AssignmentEntityConfiguration</c>) uses <c>OnDelete(Restrict)</c> — deleting a Step while an Assignment references it returns 23503 → 422"`
    - The VALID-21 deferred-to-v2 para (existing lines 16-22 inside the second `<para>`) MUST be amended to reflect that Phase 10 has made VALID-21 structurally impossible at this layer (the SchemaId reference no longer exists on Assignment); deferral semantics preserved. Suggested wording: `"Schema conformance (VALID-21) is now structurally impossible at this layer — Phase 10 removed Assignment.SchemaId. Any future Payload-vs-Schema validation would need a new design (e.g., a Processor.ConfigSchemaId-referenced schema). Deferred to v2."`

    Step 2 — Delete line 27 entirely:
    - BEFORE: lines 24-29 contain `public sealed class AssignmentEntity : BaseEntity { public Guid StepId { get; set; } public Guid SchemaId { get; set; } public string Payload { get; set; } = string.Empty; }`
    - AFTER:  the `public Guid SchemaId { get; set; }` line is removed. Class body has exactly 2 properties (`StepId` + `Payload`).

    No new imports needed; no other file-level changes.
  </action>
  <verify>
    <automated>! grep "public Guid SchemaId" src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs (negation — must be absent); ! grep "fk_assignment_schema_id" src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs (negation); grep -c "public Guid StepId" src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs returns 1; grep -c "public string Payload" src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs returns 1; grep "VALID-21" src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs returns 1 (deferral note preserved)</automated>
  </verify>
  <done>AssignmentEntity has StepId + Payload only; XML doc reflects single-FK shape; VALID-21 deferral updated to reflect structural impossibility.</done>
</task>

<task type="auto">
  <name>Task 2: Remove SchemaId from all 3 AssignmentDto records + arity comments</name>
  <files>src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs</files>
  <read_first>
    - src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs (current shape — Create/Update each 6 positional; Read 11 positional)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (Assignment DTOs removal at lines 177-196)
  </read_first>
  <action>
    Three positional-record edits in `src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs`.

    Edit 1 — `AssignmentCreateDto`:
    - BEFORE (6 positional): `Name, Version, Description, StepId, SchemaId, Payload`
    - AFTER  (5 positional): `Name, Version, Description, StepId, Payload`
    - Delete the `Guid SchemaId` line between `Guid StepId` and `string Payload`.
    - Update the XML doc-comment arity (currently says "6 positional params: Name, Version, Description, StepId, SchemaId, Payload" — change to "5 positional params: Name, Version, Description, StepId, Payload").

    Edit 2 — `AssignmentUpdateDto`: identical removal pattern (6→5 positional); update arity comment.

    Edit 3 — `AssignmentReadDto`:
    - BEFORE (11 positional): `Id, Name, Version, Description, StepId, SchemaId, Payload, CreatedAt, UpdatedAt, CreatedBy, UpdatedBy`
    - AFTER  (10 positional): `Id, Name, Version, Description, StepId, Payload, CreatedAt, UpdatedAt, CreatedBy, UpdatedBy`
    - Delete the `Guid SchemaId` line between `Guid StepId` and `string Payload`.
    - Update the XML doc-comment arity from "11" to "10".

    Preserve all other identifiers/types/markers byte-identical — `IBaseDto`, `IHasId`, `sealed record` modifiers, attribute decorations (if any), file-leading `namespace` + `using` directives.
  </action>
  <verify>
    <automated>! grep "SchemaId" src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs (negation — SchemaId must be entirely absent); grep -c "public sealed record" src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs returns 3 (Create + Update + Read preserved); grep -c "Guid StepId" src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs returns 3 (one per DTO); grep -c "string Payload" src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs returns 3 (one per DTO); arity-count verification: count commas inside `AssignmentCreateDto(...)` constructor → 4 commas = 5 positional params</automated>
  </verify>
  <done>All 3 DTOs lose SchemaId; arity comments updated; record positional ordering preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Remove SchemaId rule from both AssignmentDtoValidators + update XML doc</name>
  <files>src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs</files>
  <read_first>
    - src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs (current shape — Create lines 41-44 SchemaId block; Update lines 83-85 SchemaId block; both `Include(new BaseDtoValidator<...>())` plus StepId rule plus Payload rule)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (Assignment validator removal at lines 199-220)
  </read_first>
  <action>
    Two validator-class edits in `src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs`.

    Edit 1 — `AssignmentCreateDtoValidator` ctor:
    - Delete the block:
      ```csharp
      // VALID-15 — SchemaId must not be Guid.Empty.
      RuleFor(x => x.SchemaId)
          .NotEqual(Guid.Empty)
          .WithMessage("SchemaId must not be Guid.Empty.");
      ```
    - The `StepId` rule above and the `Payload` rule below MUST remain byte-identical.

    Edit 2 — `AssignmentUpdateDtoValidator` ctor:
    - Delete the block:
      ```csharp
      RuleFor(x => x.SchemaId)
          .NotEqual(Guid.Empty)
          .WithMessage("SchemaId must not be Guid.Empty.");
      ```
    - The `StepId` rule above and the `Payload` rule below MUST remain byte-identical.

    Edit 3 — XML doc-comment at the class/file header:
    - Replace the VALID-15 bullet "`StepId / SchemaId must not be Guid.Empty`" → "`StepId must not be Guid.Empty`".
    - Replace any "`fk_assignment_step_id + fk_assignment_schema_id`" enumeration → "`fk_assignment_step_id`" only.
    - VALID-16 (Payload) bullet stays byte-identical.
    - VALID-21 deferred-to-v2 para — same treatment as the AssignmentEntity Task 1 edit: acknowledge Phase 10 makes VALID-21 structurally impossible at this layer.
  </action>
  <verify>
    <automated>! grep "SchemaId" src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs (negation — must be absent); ! grep "fk_assignment_schema_id" src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs (negation); grep -c "RuleFor(x => x.StepId)" src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs returns 2 (one per validator — preserved); grep -c "public sealed class" src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs returns 2 (Create + Update validators preserved)</automated>
  </verify>
  <done>Both validators contain only StepId + Payload rules + Include(BaseDtoValidator) — no SchemaId reference.</done>
</task>

<task type="auto">
  <name>Task 4: Remove Schema FK block + unused import from AssignmentEntityConfiguration; build verify</name>
  <files>src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs</files>
  <read_first>
    - src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs (current — lines 44-49 contain the SchemaEntity FK block; line 2 contains `using BaseApi.Service.Features.Schema;` which becomes unused)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (Assignment config removal at lines 222-243)
  </read_first>
  <action>
    Three edits to `src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs`.

    Edit 1 — delete the entire SchemaEntity FK block (lines 44-49 verbatim — including the `// ENTITY-07 + ERROR-11` comment above it if present, and the trailing semicolon):
    ```csharp
    // ENTITY-07 + ERROR-11 — non-nullable FK to Schema; Restrict per cascade table line 585.
    entity.HasOne<SchemaEntity>()
        .WithMany()
        .HasForeignKey(e => e.SchemaId)
        .HasConstraintName("fk_assignment_schema_id")
        .OnDelete(DeleteBehavior.Restrict);
    ```
    After the edit, only the `HasOne<StepEntity>()` block (lines ~38-42) remains. The `Payload` jsonb config block stays byte-identical.

    Edit 2 — delete the now-unused `using BaseApi.Service.Features.Schema;` import (line 2). Required under TreatWarningsAsErrors=true (would otherwise trigger CS8019 / IDE0005 as a build-fatal warning under Directory.Build.props EnforceCodeStyleInBuild=true — Phase 1 D-02 gate).

    Edit 3 — XML doc-comment at the class/file header:
    - Replace "Two NON-nullable FKs: StepId... and SchemaId..." → "One NON-nullable FK: StepId..."
    - Replace "fk_assignment_step_id + fk_assignment_schema_id" → "fk_assignment_step_id"

    After all 4 tasks complete: run `dotnet build SK_P.sln -c Release --no-restore` AND `dotnet build SK_P.sln -c Debug --no-restore` from the repo root. Both MUST exit 0 with zero warnings. If either fails (e.g., a leftover SchemaId reference in another file, or an analyzer warning from the unused import that wasn't caught), the executor MUST debug and fix before commit — bisect-friendliness requires every commit be a green build.
  </action>
  <verify>
    <automated>! grep "fk_assignment_schema_id" src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs (negation); ! grep "HasOne<SchemaEntity>" src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs (negation); ! grep "using BaseApi.Service.Features.Schema" src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs (negation); grep -c "HasConstraintName" src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs returns 1 (only fk_assignment_step_id remains); dotnet build SK_P.sln -c Release --no-restore exits 0 with zero warnings; dotnet build SK_P.sln -c Debug --no-restore exits 0 with zero warnings</automated>
  </verify>
  <done>EF config has 1 FK block + 1 jsonb config; unused import removed; both build configurations zero-warning.</done>
</task>

<task type="auto">
  <name>Task 5: Commit Assignment SchemaId removal as commit #2 of the Phase 10 sequence</name>
  <files>
    src/BaseApi.Service/Features/Assignment/AssignmentEntity.cs
    src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs
    src/BaseApi.Service/Features/Assignment/AssignmentDtoValidator.cs
    src/BaseApi.Service/Persistence/Configurations/AssignmentEntityConfiguration.cs
  </files>
  <read_first>
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-CONTEXT.md (D-02 commit #2 message — verbatim)
    - git status (confirm only the 4 expected files are modified — no test files yet, those land in commit #5)
  </read_first>
  <action>
    Stage ONLY the 4 files listed in this plan's `files_modified` frontmatter (no test files — those land in commit #5 per D-02). Create commit #2 with the exact message from CONTEXT D-02:

    ```
    refactor(asm): remove SchemaId from AssignmentEntity (entity + DTOs + validator + EF config)
    ```

    Use HEREDOC for the commit body. Verify `git status` is clean after the commit. Do NOT push.

    Note: This commit will leave the test tree in a RED state — `AssignmentsIntegrationTests.cs` still references `AssignmentEntity.SchemaId` / `AssignmentCreateDto(SchemaId: ...)` etc. That is EXPECTED per D-02 (bisect-friendly = each commit compiles for the production source set, but tests may temporarily fail until commit #5 lands the test edits). The `dotnet build SK_P.sln` gate covers the production projects (BaseApi.Core + BaseApi.Service). The test project (BaseApi.Tests) WILL fail to build between commits #2 and #5 — the executor should NOT attempt to run `dotnet test` until commit #5 is complete.

    To make the build gate honest: run `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release` AND `dotnet build src/BaseApi.Core/BaseApi.Core.csproj -c Release` (NOT the full solution) — both production projects MUST exit 0 with zero warnings. The solution-level build is deferred until commit #5.
  </action>
  <verify>
    <automated>git log -1 --format=%s returns "refactor(asm): remove SchemaId from AssignmentEntity (entity + DTOs + validator + EF config)"; git show --stat HEAD lists exactly 4 files changed (the 4 Assignment files); git status --porcelain returns empty; dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --no-restore exits 0 with zero warnings; dotnet build src/BaseApi.Core/BaseApi.Core.csproj -c Release --no-restore exits 0 with zero warnings</automated>
  </verify>
  <done>Single 4-file commit landed; production projects build zero-warning Release; working tree clean; test project intentionally RED until commit #5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Internal refactor commit. No new endpoints, no new auth surfaces, no new error shapes, no public-API contract changes beyond the locked field-shape revision documented in SPEC.md REQ-1..3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | T (Tampering) | AssignmentEntityConfiguration FK constraint | mitigate | FK constraint regex invariant (PostgresExceptionMapper.cs:54-67) preserved — `fk_assignment_step_id` continues to parse cleanly as table="assignment", column="step_id" under `^fk_[a-z0-9]+_(?<col>[a-z0-9_]+)$`. No risk of 23503/23505 misclassification at the remaining FK. |
| T-10-02-02 | I (Information disclosure) | AssignmentReadDto | accept | DTO loses a Guid field; no PII removed, no security boundary altered. Existing GET endpoints continue to return only non-secret entity fields. |
| T-10-02-03 | D (Denial of service) | FluentValidation rule set on AssignmentDtoValidator | accept | Validator loses one `NotEqual(Guid.Empty)` check; remaining StepId + Payload rules continue to bound request shape (Payload still capped at 1 MB per VALID-16). No new unbounded input surface. |

No new attack surface introduced. FK constraint regex invariant (`fk_<table>_<column>` with no underscore in `<table>` segment per PostgresExceptionMapper.cs:54-60) explicitly preserved — only `fk_assignment_step_id` survives in this entity's config, and it satisfies the invariant trivially ("assignment" contains no underscore). ASVS L1: no L1 controls touched (no auth, no session, no input validation surface change beyond the symmetric removal of an unenforced rule for a deleted field).
</threat_model>

<verification>
- `grep -rn "SchemaId" src/BaseApi.Service/Features/Assignment/` returns zero matches in non-binary files (entity, DTOs, validator, configuration all clean — XML doc-comment paras stripped of SchemaId mentions per Tasks 1+3).
- `grep -rn "fk_assignment_schema_id" src/BaseApi.Service/` returns zero matches in non-binary files (negation — must be absent).
- `grep -rn "ix_assignments_schema_id" src/BaseApi.Service/` returns zero matches in non-binary files (negation — the index disappears with the FK; will also disappear from the migration file once Plan 04 regenerates it).
- `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --no-restore` exits 0 with zero warnings.
- `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Debug --no-restore` exits 0 with zero warnings.
- `dotnet build src/BaseApi.Core/BaseApi.Core.csproj -c Release --no-restore` exits 0 with zero warnings.
- Mapperly RMG012 / RMG020 / RMG089 do NOT fire on AssignmentEntityMapper.cs — symmetric removal invariant per CONTEXT D-10 (entity + DTOs lose SchemaId together → no target-unmapped / source-unmapped diagnostics).
- `git log -1 --format=%s` matches `refactor(asm): remove SchemaId from AssignmentEntity (entity + DTOs + validator + EF config)` verbatim.
- `git show --stat HEAD` lists exactly 4 files (the 4 in `files_modified`).
- `git status --porcelain` returns empty.
- EXPECTED: `dotnet build SK_P.sln` (whole solution) will FAIL between this commit and commit #5 — the test project still references `AssignmentEntity.SchemaId` and the old DTO shapes. This is intentional per D-02 (each commit is bisect-friendly for the production source set; test edits are bundled in commit #5 to keep the cleanup churn out of the surface-change commits).
</verification>

<success_criteria>
1. AssignmentEntity.cs has 2 properties (StepId + Payload) on top of BaseEntity; XML doc reflects single-FK shape.
2. AssignmentDtos.cs declares 3 records with arity 5/5/10 (Create/Update/Read); no SchemaId field in any record.
3. AssignmentDtoValidator.cs declares 2 validators (Create + Update) each containing only StepId + Payload rules (via Include of BaseDtoValidator + 2 explicit rules each).
4. AssignmentEntityConfiguration.cs declares exactly 1 `HasOne<>()` block (StepEntity) + the jsonb Payload config; unused `using BaseApi.Service.Features.Schema;` import removed.
5. Both production projects (`BaseApi.Core`, `BaseApi.Service`) build zero-warning in Debug + Release under `TreatWarningsAsErrors=true` + `EnforceCodeStyleInBuild=true` (Phase 1 D-02 gate preserved).
6. Single git commit `refactor(asm): remove SchemaId from AssignmentEntity (entity + DTOs + validator + EF config)` at HEAD; modifies exactly 4 files; working tree clean post-commit.
7. Mapperly diagnostics do NOT fire on `AssignmentEntityMapper.cs` (zero-touch confirmation of the symmetric-removal invariant per CONTEXT D-10).
</success_criteria>

<output>
After completion, create `.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-02-SUMMARY.md`.
</output>
