---
phase: 10-remove-schemaid-on-assignmententity-and-add-configschemaid-o
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/BaseApi.Service/Features/Processor/ProcessorEntity.cs
  - src/BaseApi.Service/Features/Processor/ProcessorDtos.cs
  - src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs
  - src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs
autonomous: true
requirements:
  - ENTITY-04
  - VALID-11
must_haves:
  truths:
    - "ProcessorEntity declares a 4th non-base property `public Guid? ConfigSchemaId { get; set; }` immediately after `OutputSchemaId`, mirroring the nullable Guid? shape of Input/Output"
    - "ProcessorCreateDto + ProcessorUpdateDto are 7-positional records (Name, Version, Description, SourceHash, InputSchemaId, OutputSchemaId, ConfigSchemaId) — ConfigSchemaId is the 7th positional param"
    - "ProcessorReadDto is a 12-positional record with ConfigSchemaId inserted between OutputSchemaId and CreatedAt (Id, Name, Version, Description, SourceHash, InputSchemaId, OutputSchemaId, ConfigSchemaId, CreatedAt, UpdatedAt, CreatedBy, UpdatedBy)"
    - "ProcessorCreateDtoValidator + ProcessorUpdateDtoValidator each contain a 3rd `When(x => x.ConfigSchemaId.HasValue, () => RuleFor(x => x.ConfigSchemaId!.Value).NotEqual(Guid.Empty))` block mirroring the existing Input/Output rules verbatim"
    - "ProcessorEntityConfiguration declares a 3rd `HasOne<SchemaEntity>().WithMany().HasForeignKey(e => e.ConfigSchemaId).HasConstraintName(\"fk_processor_config_schema_id\").OnDelete(DeleteBehavior.SetNull)` block, mirroring the existing Input/Output FK blocks line-for-line"
    - "dotnet build SK_P.sln -c Release & -c Debug exit 0 with zero warnings (TreatWarningsAsErrors=true + Mapperly RMG007/RMG012/RMG020/RMG089 promotion preserved)"
  artifacts:
    - path: "src/BaseApi.Service/Features/Processor/ProcessorEntity.cs"
      provides: "Post-Phase-10 Processor entity shape with 3 nullable Schema FKs"
      contains: "public Guid? ConfigSchemaId"
    - path: "src/BaseApi.Service/Features/Processor/ProcessorDtos.cs"
      provides: "7/7/12 positional DTO records carrying ConfigSchemaId"
      contains: "ConfigSchemaId"
    - path: "src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs"
      provides: "Validators with 3 When().HasValue → NotEqual(Guid.Empty) blocks"
      contains: "ConfigSchemaId.HasValue"
    - path: "src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs"
      provides: "EF config declaring fk_processor_config_schema_id (SetNull cascade, lambda-less HasOne)"
      contains: "fk_processor_config_schema_id"
  key_links:
    - from: "ProcessorEntity.cs (ConfigSchemaId property)"
      to: "ProcessorDtos.cs (ConfigSchemaId positional param in all 3 records)"
      via: "symmetric field-shape addition — entity + DTOs gain ConfigSchemaId in the same commit (Mapperly RMG012/RMG020 zero-fire invariant per CONTEXT D-10)"
      pattern: "ConfigSchemaId"
    - from: "ProcessorDtoValidator.cs (third When block)"
      to: "ProcessorDtos.cs (ConfigSchemaId field on Create/Update DTOs)"
      via: "FluentValidation RuleFor target reachability — adding the When block WITHOUT the DTO field would compile-fail; both land together"
      pattern: "When\\(x => x.ConfigSchemaId.HasValue"
    - from: "ProcessorEntityConfiguration.cs (HasOne<SchemaEntity> FK block 3)"
      to: "ProcessorEntity.cs (ConfigSchemaId property)"
      via: "EF property-existence invariant — HasForeignKey(e => e.ConfigSchemaId) requires the entity property to exist"
      pattern: "fk_processor_config_schema_id"
    - from: "ProcessorEntityConfiguration.cs (fk_processor_config_schema_id constraint name)"
      to: "src/BaseApi.Core/Persistence/Exceptions/PostgresExceptionMapper.cs:54-67 (FK regex)"
      via: "Phase 4 Option A regex `^fk_[a-z0-9]+_(?<col>[a-z0-9_]+)$` parses `fk_processor_config_schema_id` as table=\"processor\" (no underscore — invariant satisfied), column=\"config_schema_id\""
      pattern: "fk_processor_config_schema_id"
---

<objective>
Per CONTEXT D-02 commit #3: add `ConfigSchemaId` (nullable `Guid?`) to `ProcessorEntity` + 3 DTOs + 2 validators + 1 EF config block, mirroring `InputSchemaId` behavior exactly. Independent of Plan 02 (different feature folder, different files) — can ship in either order within Wave 2 since they share zero `files_modified` overlap.

Purpose: Add the "Processor.ConfigSchemaId" half of the Phase 10 field-shape revision in a single atomic feature commit. The new FK constraint name `fk_processor_config_schema_id` is load-bearing for the Phase 4 PostgresExceptionMapper Option A regex (`^fk_[a-z0-9]+_(?<col>[a-z0-9_]+)$`) — "processor" satisfies the "no underscore in table segment" invariant, so 23503 (FK violation) at insert time continues to surface as HTTP 422 with `config_schema_id` extracted as the field name.

Output: 4 modified source files with `ConfigSchemaId` added verbatim per the existing `InputSchemaId`/`OutputSchemaId` pattern; `dotnet build` zero-warning Release + Debug; 1 git commit landing the feature addition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-CONTEXT.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-SPEC.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md
@src/BaseApi.Service/Features/Processor/ProcessorEntity.cs
@src/BaseApi.Service/Features/Processor/ProcessorDtos.cs
@src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs
@src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs

<interfaces>
<!-- Current ProcessorEntity shape (read by planner, verified on disk) -->

ProcessorEntity.cs lines 23-28:
```csharp
public sealed class ProcessorEntity : BaseEntity
{
    public string SourceHash { get; set; } = string.Empty;
    public Guid? InputSchemaId { get; set; }
    public Guid? OutputSchemaId { get; set; }
}
```

ProcessorDtos.cs record shapes (current arity):
- CreateDto positional: (Name, Version, Description, SourceHash, InputSchemaId, OutputSchemaId) → 7 after add
- UpdateDto positional: same shape as Create → 7 after add
- ReadDto positional: (Id, Name, Version, Description, SourceHash, InputSchemaId, OutputSchemaId, CreatedAt, UpdatedAt, CreatedBy, UpdatedBy) → 12 after add

ProcessorDtoValidator.cs current rules (VALID-11):
- ProcessorCreateDtoValidator lines 35-47: 2 `When` blocks (InputSchemaId, OutputSchemaId) → 3 after add
- ProcessorUpdateDtoValidator lines 66-78: 2 `When` blocks → 3 after add
- Pattern (Input block lines 35-41):
  ```csharp
  When(x => x.InputSchemaId.HasValue, () =>
  {
      RuleFor(x => x.InputSchemaId!.Value)
          .NotEqual(Guid.Empty)
          .WithMessage("InputSchemaId must not be Guid.Empty when provided.");
  });
  ```

ProcessorEntityConfiguration.cs lines 31-41 (current 2 FK blocks):
```csharp
entity.HasOne<SchemaEntity>()
    .WithMany()
    .HasForeignKey(e => e.InputSchemaId)
    .HasConstraintName("fk_processor_input_schema_id")
    .OnDelete(DeleteBehavior.SetNull);

entity.HasOne<SchemaEntity>()
    .WithMany()
    .HasForeignKey(e => e.OutputSchemaId)
    .HasConstraintName("fk_processor_output_schema_id")
    .OnDelete(DeleteBehavior.SetNull);
```

Mapperly mapper (NOT touched in this plan):
- src/BaseApi.Service/Features/Processor/ProcessorEntityMapper.cs — zero changes (symmetric addition — entity + DTOs gain ConfigSchemaId together → RMG012/RMG020 do not fire per CONTEXT D-10)

PostgresExceptionMapper INVARIANT (zero-touch confirmation only):
- src/BaseApi.Core/Persistence/Exceptions/PostgresExceptionMapper.cs:54-67 — FK regex `^fk_[a-z0-9]+_(?<col>[a-z0-9_]+)$` requires the `<table>` segment to contain no underscore. `fk_processor_config_schema_id` parses as table="processor" (no underscore — OK), column="config_schema_id". File requires NO edits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ConfigSchemaId property + extend XML doc on ProcessorEntity</name>
  <files>src/BaseApi.Service/Features/Processor/ProcessorEntity.cs</files>
  <read_first>
    - src/BaseApi.Service/Features/Processor/ProcessorEntity.cs (current — verify lines 23-28 hold the 3-property body with InputSchemaId + OutputSchemaId at lines 26-27)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (Entity addition at lines 38-56)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-CONTEXT.md (D-15 discretion on trinary source/sink/config wording)
  </read_first>
  <action>
    Edit `src/BaseApi.Service/Features/Processor/ProcessorEntity.cs` in place.

    Step 1 — Add the new property as line 28 (directly after `OutputSchemaId` on line 27):
    ```csharp
    public Guid? ConfigSchemaId { get; set; }
    ```
    Class body becomes 4 properties: SourceHash, InputSchemaId, OutputSchemaId, ConfigSchemaId (in this exact order — preserves the existing positional convention and matches the DTO record positional order in Task 2).

    Step 2 — Extend the XML `<para>` block (currently lines 15-21 describing the binary "source/sink" semantic) to cover the trinary {Input, Output, Config} symmetric case. Suggested wording (CONTEXT D-15 discretion — preserve "null permitted" semantics):

    > `<c>InputSchemaId</c> / `<c>OutputSchemaId</c>` / `<c>ConfigSchemaId</c>` are nullable Guid FK references to `<c>SchemaEntity</c>`. Null is permitted on all three — supports source processors (no input), sink processors (no output), and unconfigured processors (no config). The Postgres FK constraints are still enforced when the value is non-null (constraint names `<c>fk_processor_input_schema_id</c>` / `<c>fk_processor_output_schema_id</c>` / `<c>fk_processor_config_schema_id</c>` match the Phase 4 Option A regex naming convention).

    Mapperly invariant: because the property is added to BOTH entity AND all 3 DTOs in this plan (symmetric change per CONTEXT D-10), RMG012 (target unmapped) and RMG020 (source unmapped) do NOT fire on `ProcessorEntityMapper.cs`. The 5 existing `[MapperIgnoreTarget]` attributes for Id + 4 audit fields stay byte-identical — zero mapper edits needed.
  </action>
  <verify>
    <automated>grep -c "public Guid\\? ConfigSchemaId" src/BaseApi.Service/Features/Processor/ProcessorEntity.cs returns 1; grep -c "fk_processor_config_schema_id" src/BaseApi.Service/Features/Processor/ProcessorEntity.cs returns 1 (in the XML doc); grep -E "source processors \\(no input\\), sink processors \\(no output\\), and unconfigured processors \\(no config\\)" src/BaseApi.Service/Features/Processor/ProcessorEntity.cs returns 1 (trinary wording locked); grep -cE "public Guid\\?" src/BaseApi.Service/Features/Processor/ProcessorEntity.cs returns 3 (Input + Output + Config)</automated>
  </verify>
  <done>ProcessorEntity declares 4 non-base properties (SourceHash + 3 nullable Schema FKs); XML doc reflects trinary "source/sink/config" semantic.</done>
</task>

<task type="auto">
  <name>Task 2: Add ConfigSchemaId positional param to all 3 ProcessorDto records + arity comments</name>
  <files>src/BaseApi.Service/Features/Processor/ProcessorDtos.cs</files>
  <read_first>
    - src/BaseApi.Service/Features/Processor/ProcessorDtos.cs (current — Create/Update each 6 positional; Read 11 positional)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (DTO addition at lines 60-97)
  </read_first>
  <action>
    Three positional-record edits in `src/BaseApi.Service/Features/Processor/ProcessorDtos.cs`. CRITICAL: positional order is load-bearing — `ConfigSchemaId` is inserted DIRECTLY AFTER `OutputSchemaId` in all 3 records (NOT at the end of the param list). This matches the new ProcessorEntity property order (Task 1) and locks the positional-arg convention for all downstream test call-site updates (Plan 05).

    Edit 1 — `ProcessorCreateDto` (6→7 positional):
    ```csharp
    public sealed record ProcessorCreateDto(
        string Name,
        string Version,
        string? Description,
        string SourceHash,
        Guid? InputSchemaId,
        Guid? OutputSchemaId,
        Guid? ConfigSchemaId) : IBaseDto;     // NEW — 7th positional
    ```
    Update XML doc-comment arity from "6 positional params" → "7 positional params"; extend the param enumeration to include `ConfigSchemaId` at the end.

    Edit 2 — `ProcessorUpdateDto` (6→7 positional): identical insertion pattern. Update arity comment.

    Edit 3 — `ProcessorReadDto` (11→12 positional):
    ```csharp
    public sealed record ProcessorReadDto(
        Guid Id,
        string Name,
        string Version,
        string? Description,
        string SourceHash,
        Guid? InputSchemaId,
        Guid? OutputSchemaId,
        Guid? ConfigSchemaId,                  // NEW — 8th positional
        DateTime CreatedAt,
        DateTime UpdatedAt,
        string? CreatedBy,
        string? UpdatedBy) : IBaseDto, IHasId;
    ```
    Update arity comment from "11 positional params" → "12 positional params"; extend param enumeration.

    Preserve all interface implementations (`IBaseDto`, `IHasId`), `sealed record` modifiers, attribute decorations, namespace, and using directives byte-identical.

    Mapperly symmetric-addition invariant (CONTEXT D-10): because entity + DTOs gain ConfigSchemaId together, RMG012/RMG020 do not fire. No `[MapperIgnoreTarget]` / `[MapperIgnoreSource]` attribute changes needed on `ProcessorEntityMapper.cs`.
  </action>
  <verify>
    <automated>grep -c "Guid\\? ConfigSchemaId" src/BaseApi.Service/Features/Processor/ProcessorDtos.cs returns 3 (one per DTO); grep -c "public sealed record" src/BaseApi.Service/Features/Processor/ProcessorDtos.cs returns 3 (Create + Update + Read preserved); arity check — count commas inside `ProcessorCreateDto(...)` constructor param list returns 6 commas = 7 positional params; ditto Update; ProcessorReadDto returns 11 commas = 12 positional params; grep -B1 "Guid\\? ConfigSchemaId" src/BaseApi.Service/Features/Processor/ProcessorDtos.cs shows OutputSchemaId on the immediately preceding line in ALL 3 occurrences (positional order locked)</automated>
  </verify>
  <done>All 3 DTOs gain ConfigSchemaId as the positional slot immediately after OutputSchemaId; arity comments updated; record positional ordering preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add 3rd When().HasValue block to both ProcessorDtoValidators + update XML doc</name>
  <files>src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs</files>
  <read_first>
    - src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs (current — verify Create ctor has 2 When blocks at ~lines 35-47; Update ctor has 2 When blocks at ~lines 66-78)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (Validator addition at lines 101-126)
  </read_first>
  <action>
    Two validator-class edits in `src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs`. Both edits append a 3rd `When` block immediately after the existing `OutputSchemaId` block in each validator's ctor — verbatim 5-line mirror of the existing pattern (PATTERNS.md lines 107-114).

    Edit 1 — `ProcessorCreateDtoValidator` ctor (append after the OutputSchemaId `When` block ending at ~line 47):
    ```csharp
    When(x => x.ConfigSchemaId.HasValue, () =>
    {
        RuleFor(x => x.ConfigSchemaId!.Value)
            .NotEqual(Guid.Empty)
            .WithMessage("ConfigSchemaId must not be Guid.Empty when provided.");
    });
    ```

    Edit 2 — `ProcessorUpdateDtoValidator` ctor (append after the OutputSchemaId `When` block ending at ~line 78): identical 5-line block. Validator is rule-symmetric per the file's existing convention.

    Edit 3 — XML doc-comment at the class/file header:
    - Update the VALID-11 bullet from "InputSchemaId / OutputSchemaId" → "InputSchemaId / OutputSchemaId / ConfigSchemaId".
    - The existing Postgres FK SQLSTATE 23503 → HTTP 422 reference covers the new `fk_processor_config_schema_id` constraint via the same Option A regex — optionally enumerate the third constraint name explicitly (CONTEXT D-15 discretion). Suggested addition: append `"or `fk_processor_config_schema_id`"` to the existing FK constraint enumeration in the doc-comment.
  </action>
  <verify>
    <automated>grep -c "When(x => x.ConfigSchemaId.HasValue" src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs returns 2 (one per validator); grep -c "ConfigSchemaId must not be Guid.Empty when provided" src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs returns 2; grep -c "When(x => x.InputSchemaId.HasValue" src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs returns 2 (existing rule preserved); grep -c "When(x => x.OutputSchemaId.HasValue" src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs returns 2 (existing rule preserved); grep -c "public sealed class" src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs returns 2 (Create + Update validators)</automated>
  </verify>
  <done>Both validators contain exactly 3 When().HasValue → NotEqual(Guid.Empty) blocks (Input + Output + Config), each with matching WithMessage text.</done>
</task>

<task type="auto">
  <name>Task 4: Add 3rd FK block to ProcessorEntityConfiguration; verify regex invariant; build verify</name>
  <files>src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs</files>
  <read_first>
    - src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs (current — lines 31-41 hold 2 FK blocks; verify both use `OnDelete(DeleteBehavior.SetNull)`)
    - src/BaseApi.Core/Persistence/Exceptions/PostgresExceptionMapper.cs (lines 54-67 — FK regex invariant; READ-ONLY confirmation)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (EF config addition at lines 130-154)
  </read_first>
  <action>
    Edit `src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs` in place.

    Edit 1 — append a 3rd `HasOne<SchemaEntity>().WithMany()` FK block immediately after the existing OutputSchemaId block (lines 37-41). The new block mirrors the existing pattern line-for-line:
    ```csharp
    entity.HasOne<SchemaEntity>()
        .WithMany()
        .HasForeignKey(e => e.ConfigSchemaId)
        .HasConstraintName("fk_processor_config_schema_id")
        .OnDelete(DeleteBehavior.SetNull);
    ```
    CRITICAL constraints:
    - `OnDelete(DeleteBehavior.SetNull)` — required because `ConfigSchemaId` is nullable; Postgres rejects SET NULL on a non-nullable column. Cascade mirroring Input/Output is locked per SPEC.md REQ-4.
    - `HasConstraintName("fk_processor_config_schema_id")` — exact string. Must NOT be misspelled or pluralized — this name is consumed by the Phase 4 PostgresExceptionMapper Option A regex (`^fk_[a-z0-9]+_(?<col>[a-z0-9_]+)$`) AT RUNTIME during 23503 → 422 mapping.
    - Lambda-less `HasOne<SchemaEntity>().WithMany()` — required by ENTITY-09 (no navigation properties between entities). Adding a lambda `(s => s.Processors)` would create a nav prop and violate the invariant.
    - `WithMany()` (no argument) — same lambda-less requirement on the inverse side.

    Edit 2 — XML doc-comment at the class/file header:
    - The existing comment lists `fk_processor_input_schema_id` / `fk_processor_output_schema_id` explicitly. Append `fk_processor_config_schema_id` to the enumeration.
    - The "Lambda-less HasOne" para and the "Phase 4 Option A regex" sentence stay byte-identical — they already cover the new constraint generically.

    Edit 3 — Zero-touch confirmation of the PostgresExceptionMapper invariant:
    - DO NOT edit `src/BaseApi.Core/Persistence/Exceptions/PostgresExceptionMapper.cs`.
    - VERIFY: the regex `^fk_[a-z0-9]+_(?<col>[a-z0-9_]+)$` at line 61-63 matches `fk_processor_config_schema_id` with capture groups table="processor" (no underscore — satisfies invariant at lines 54-60), column="config_schema_id". This is a thought-step / explicit verification — record it in the executor's working notes; no file change.

    Final step — run `dotnet build SK_P.sln -c Release --no-restore` AND `dotnet build SK_P.sln -c Debug --no-restore` from the repo root. Both MUST exit 0 with zero warnings.

    EXPECTED: between this plan and Plan 04 (migration regen), `dotnet test` will NOT work — the production model now declares a `config_schema_id` column the existing migration doesn't create, so `MigrateAsync` at test-fixture boot will throw on a missing column when the EF model snapshot diverges. Plan 04 fixes this. Plan 03 stops at `dotnet build` zero-warning.

    IMPORTANT — full-solution build status: even though tests won't RUN against a DB until Plan 04, the test project's COMPILATION may also break here because `AssignmentsIntegrationTests.cs` still references the old `AssignmentEntity.SchemaId` shape (left there by Plan 02 — D-02 explicitly says test edits land in commit #5). Therefore: if Plan 02 has landed (commit #2 in the git log), the test project compilation will fail on `SchemaId` references in `AssignmentsIntegrationTests.cs`. That is EXPECTED and intentional. Use the per-project build command (`dotnet build src/BaseApi.Service/BaseApi.Service.csproj`) to gate this commit — the test-project red-state is bundled to commit #5.

    Production-project build commands for the gate:
    - `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --no-restore`
    - `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Debug --no-restore`
    - `dotnet build src/BaseApi.Core/BaseApi.Core.csproj -c Release --no-restore`
    All three MUST exit 0 with zero warnings.
  </action>
  <verify>
    <automated>grep -c "fk_processor_config_schema_id" src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs returns at least 1 (in HasConstraintName) and possibly 2 (also in XML doc); grep -c "HasOne<SchemaEntity>" src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs returns 3 (Input + Output + Config FK blocks); grep -c "DeleteBehavior.SetNull" src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs returns 3 (all 3 nullable Schema FKs); grep "HasForeignKey(e => e.ConfigSchemaId)" src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs returns 1; dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --no-restore exits 0 zero warnings; dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Debug --no-restore exits 0 zero warnings; dotnet build src/BaseApi.Core/BaseApi.Core.csproj -c Release --no-restore exits 0 zero warnings; PostgresExceptionMapper.cs is NOT modified — git diff src/BaseApi.Core/Persistence/Exceptions/PostgresExceptionMapper.cs returns empty</automated>
  </verify>
  <done>EF config has 3 FK blocks (Input + Output + Config) all with SetNull cascade + lambda-less HasOne; PostgresExceptionMapper untouched; production projects build zero-warning Release + Debug.</done>
</task>

<task type="auto">
  <name>Task 5: Commit Processor ConfigSchemaId addition as commit #3 of the Phase 10 sequence</name>
  <files>
    src/BaseApi.Service/Features/Processor/ProcessorEntity.cs
    src/BaseApi.Service/Features/Processor/ProcessorDtos.cs
    src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs
    src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs
  </files>
  <read_first>
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-CONTEXT.md (D-02 commit #3 message — verbatim)
    - git status (confirm only the 4 expected files are modified)
  </read_first>
  <action>
    Stage ONLY the 4 files listed in this plan's `files_modified` frontmatter (no test files — those land in commit #5 per D-02). Create commit #3 with the exact message from CONTEXT D-02:

    ```
    feat(proc): add ConfigSchemaId to ProcessorEntity (entity + DTOs + validator + EF config; mirrors InputSchemaId)
    ```

    Use HEREDOC for the commit body. Verify `git status` is clean after the commit. Do NOT push.

    This commit can land in either order relative to Plan 02's commit (refactor(asm)...) since they share zero `files_modified` overlap (different feature folders, different EF config files, different validators). The orchestrator chose Wave 2 = {Plan 02, Plan 03} parallel; if executed sequentially the git log order is implementation-detail (both are no-op-relative-to-each-other).
  </action>
  <verify>
    <automated>git log -1 --format=%s returns "feat(proc): add ConfigSchemaId to ProcessorEntity (entity + DTOs + validator + EF config; mirrors InputSchemaId)"; git show --stat HEAD lists exactly 4 files changed (the 4 Processor files); git status --porcelain returns empty</automated>
  </verify>
  <done>Single 4-file commit landed; production projects build zero-warning Release; working tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Feature-addition commit. No new endpoints (CRUD shape unchanged), no new auth surfaces, no new error shapes. Only thing crossing a trust boundary is the new ConfigSchemaId field on `POST /api/v1/processors` request body — which is bounded by the validator + Postgres FK constraint exactly like InputSchemaId. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | T (Tampering) | ProcessorEntity.ConfigSchemaId — invalid Guid in request body | mitigate | `When(x => x.ConfigSchemaId.HasValue, () => RuleFor(x => x.ConfigSchemaId!.Value).NotEqual(Guid.Empty))` validator block rejects `Guid.Empty` at HTTP 400 (Task 3). Non-existent (but non-empty) Guids surface as SQLSTATE 23503 → HTTP 422 via Phase 4 PostgresExceptionMapper Option A regex parsing `fk_processor_config_schema_id` (Task 4). Symmetric with existing InputSchemaId/OutputSchemaId controls. |
| T-10-03-02 | E (Elevation of privilege) | ProcessorEntity.ConfigSchemaId — referenced Schema row revocation | mitigate | `OnDelete(DeleteBehavior.SetNull)` cascade — if a referenced SchemaEntity row is later deleted, the Processor's ConfigSchemaId column is silently set to null (not cascade-deleted). Symmetric with Input/Output. ASVS L1: no privilege elevation surface (no role/permission entity touched). |
| T-10-03-03 | I (Information disclosure) | ProcessorReadDto.ConfigSchemaId — GET response | accept | DTO gains a nullable Guid field; this is the same shape as existing InputSchemaId/OutputSchemaId fields already in the public Read DTO. No new PII, no secret exposure. The Guid is opaque without a corresponding `/api/v1/schemas/{id}` GET (which itself returns no secrets — Phase 8 P02 surface). |

FK constraint regex INVARIANT preserved (PostgresExceptionMapper.cs:54-67 unchanged): `fk_processor_config_schema_id` parses cleanly as table="processor" (no underscore — invariant satisfied), column="config_schema_id". ASVS L1: V5.1.5 (FK constraint enforcement) and V11.1.2 (input validation rejects invalid Guids before persistence) both satisfied by the symmetric mirroring of existing controls.

No new attack surface introduced beyond a symmetric extension of an existing controlled surface (nullable Schema FK). No new error shapes; no new exception types; the existing Phase 4 → HTTP mapping pipeline absorbs ConfigSchemaId errors via the same Option A regex path.
</threat_model>

<verification>
- `grep -c "public Guid? ConfigSchemaId" src/BaseApi.Service/Features/Processor/ProcessorEntity.cs` returns 1.
- `grep -c "Guid\\? ConfigSchemaId" src/BaseApi.Service/Features/Processor/ProcessorDtos.cs` returns 3 (one per DTO record).
- `grep -c "When(x => x.ConfigSchemaId.HasValue" src/BaseApi.Service/Features/Processor/ProcessorDtoValidator.cs` returns 2 (one per validator).
- `grep -c "fk_processor_config_schema_id" src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs` returns at least 1 (HasConstraintName) — may be 2 if the XML doc enumerates the third constraint name.
- `grep -c "DeleteBehavior.SetNull" src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs` returns 3 (Input + Output + Config — all nullable Schema FKs).
- `grep -c "HasOne<SchemaEntity>" src/BaseApi.Service/Persistence/Configurations/ProcessorEntityConfiguration.cs` returns 3.
- `git diff src/BaseApi.Core/Persistence/Exceptions/PostgresExceptionMapper.cs` returns empty (zero-touch confirmation — the regex invariant holds without source change).
- `git diff src/BaseApi.Service/Features/Processor/ProcessorEntityMapper.cs` returns empty (Mapperly symmetric-addition invariant per CONTEXT D-10).
- `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Release --no-restore` exits 0 with zero warnings.
- `dotnet build src/BaseApi.Service/BaseApi.Service.csproj -c Debug --no-restore` exits 0 with zero warnings.
- `dotnet build src/BaseApi.Core/BaseApi.Core.csproj -c Release --no-restore` exits 0 with zero warnings.
- `git log -1 --format=%s` matches `feat(proc): add ConfigSchemaId to ProcessorEntity (entity + DTOs + validator + EF config; mirrors InputSchemaId)` verbatim.
- `git show --stat HEAD` lists exactly 4 files (the 4 in `files_modified`).
- `git status --porcelain` returns empty.
- EXPECTED: `dotnet build SK_P.sln` (whole solution) will FAIL — the test project still references the old DTO/entity shapes (Plans 02+03 only touched production source). This is intentional per D-02; commit #5 reconciles the tests.
- EXPECTED: `dotnet test` against any test class touching `ProcessorEntity` will be DOWN until Plan 04 regenerates the migration — the model now declares `config_schema_id` but the migration doesn't create the column, so `MigrateAsync` would throw at fixture boot. Do NOT attempt `dotnet test` between this commit and commit #4.
</verification>

<success_criteria>
1. ProcessorEntity.cs declares 4 properties on top of BaseEntity (SourceHash, InputSchemaId?, OutputSchemaId?, ConfigSchemaId?); XML doc reflects trinary "source/sink/config" semantic; constraint name `fk_processor_config_schema_id` enumerated in doc.
2. ProcessorDtos.cs declares 3 records with arity 7/7/12 (Create/Update/Read); ConfigSchemaId is the 7th positional in Create+Update and the 8th positional (between OutputSchemaId and CreatedAt) in Read.
3. ProcessorDtoValidator.cs declares 2 validators each with 3 `When().HasValue → NotEqual(Guid.Empty)` blocks (Input + Output + Config).
4. ProcessorEntityConfiguration.cs declares 3 lambda-less `HasOne<SchemaEntity>().WithMany()` FK blocks with explicit constraint names (`fk_processor_input_schema_id`, `fk_processor_output_schema_id`, `fk_processor_config_schema_id`) all using `OnDelete(DeleteBehavior.SetNull)`.
5. PostgresExceptionMapper.cs is byte-identical (zero-touch invariant — regex satisfied by `fk_processor_config_schema_id`).
6. ProcessorEntityMapper.cs is byte-identical (Mapperly symmetric-addition zero-fire invariant per CONTEXT D-10).
7. Both production projects (BaseApi.Core, BaseApi.Service) build zero-warning Debug + Release.
8. Single git commit `feat(proc): add ConfigSchemaId to ProcessorEntity (entity + DTOs + validator + EF config; mirrors InputSchemaId)` at HEAD; modifies exactly 4 files; working tree clean post-commit.
</success_criteria>

<output>
After completion, create `.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-03-SUMMARY.md`.
</output>
