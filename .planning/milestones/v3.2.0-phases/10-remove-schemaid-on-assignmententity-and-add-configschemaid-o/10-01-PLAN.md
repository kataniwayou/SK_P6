---
phase: 10-remove-schemaid-on-assignmententity-and-add-configschemaid-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ENTITY-04
  - ENTITY-07
  - VALID-11
  - VALID-15
must_haves:
  truths:
    - "REQUIREMENTS.md ENTITY-04 lists InputSchemaId + OutputSchemaId + ConfigSchemaId as the nullable Schema FK enumeration (3 fields) with constraint names fk_processor_input_schema_id / fk_processor_output_schema_id / fk_processor_config_schema_id"
    - "REQUIREMENTS.md ENTITY-07 describes AssignmentEntity as having only StepId + Payload (no SchemaId)"
    - "REQUIREMENTS.md VALID-11 enumerates the When().HasValue → NotEqual(Guid.Empty) rule for InputSchemaId, OutputSchemaId, AND ConfigSchemaId"
    - "REQUIREMENTS.md VALID-15 covers only StepId (no SchemaId rule)"
    - "REQUIREMENTS.md footer reads: *Last updated: 2026-05-28 — Phase 10 amendments for Assignment.SchemaId removal + Processor.ConfigSchemaId addition*"
    - "REQ-IDs ENTITY-04, ENTITY-07, VALID-11, VALID-15 are NOT renumbered or superseded — same IDs, edited in place (D-01 preservation invariant)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Post-Phase-10 spec — ENTITY-04/07 + VALID-11/15 reflect new field shape; footer carries Phase 10 amendment note"
      contains: "ConfigSchemaId"
  key_links:
    - from: ".planning/REQUIREMENTS.md ENTITY-04"
      to: ".planning/REQUIREMENTS.md VALID-11"
      via: "ConfigSchemaId nullable Guid? appears in BOTH amended REQs with mirrored semantics (null permitted; non-null Guid.Empty → 400; non-null non-existent FK → 422)"
      pattern: "ConfigSchemaId"
    - from: ".planning/REQUIREMENTS.md ENTITY-07"
      to: ".planning/REQUIREMENTS.md VALID-15"
      via: "SchemaId reference removed from BOTH amended REQs symmetrically (entity field + validator rule disappear together)"
      pattern: "(?!.*SchemaId).*StepId"
---

<objective>
Doc-first commit per CONTEXT.md D-01 / D-02 commit #1. Amend the 4 locked REQ-IDs (ENTITY-04, ENTITY-07, VALID-11, VALID-15) in `.planning/REQUIREMENTS.md` BEFORE any code, EF config, migration, or test changes land. After this commit, REQUIREMENTS.md reflects the post-Phase-10 shape; later commits (#2..#5) point back to the amended REQ-IDs as the source of truth. If execution stops at any later commit, REQUIREMENTS.md still describes the target shape (not v1's old shape) — the next session can resume from a self-consistent spec.

Purpose: Forensic property — every commit between #1 and #5 is independently revertable; the spec is never out of sync with the code that follows it. Mirrors Plan 03-01's PERSIST-16 doc-first introduction pattern verbatim.

Output: REQUIREMENTS.md with 4 amended REQ-IDs (in-place edits, IDs preserved) + updated footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-CONTEXT.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-SPEC.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md

<interfaces>
<!-- Current REQ-ID locations in REQUIREMENTS.md (verified by Read) -->

ENTITY-04 — current text at lines 59-60:
```
- [x] **ENTITY-04
**: `ProcessorEntity : BaseEntity` adds `SourceHash` string (SHA-256 hex, unique), `InputSchemaId` Guid? (nullable FK→Schema), `OutputSchemaId` Guid? (nullable FK→Schema). Null permitted on both — supports source processors (no input) and sink processors (no output). DB columns are nullable; Postgres FK constraint still enforced when value is non-null.
```

ENTITY-07 — current text at lines 65-66:
```
- [x] **ENTITY-07
**: `AssignmentEntity : BaseEntity` adds `StepId` Guid (FK→Step), `SchemaId` Guid (FK→Schema), `Payload` string (jsonb)
```

VALID-11 — current text at lines 131-132 (multi-line, ends before VALID-12):
```
- [x] **VALID-11**: `ProcessorCreate/UpdateDto.InputSchemaId`/`OutputSchemaId`: nullable `Guid?` — null is valid (source/sink processor). When present, must not equal `Guid.Empty`. FluentValidation pattern: `When(x => x.InputSchemaId.HasValue, () => RuleFor(x => x.InputSchemaId!.Value).NotEqual(Guid.Empty));` (same for `OutputSchemaId`). `Guid.Empty` (`00000000-0000-0000-0000-000000000000`) is rejected at HTTP 400 by the validator, NOT at the DB layer. FK existence for non-empty Guids is still enforced by Postgres at persist time (SQLSTATE 23503 → HTTP 422 per ERROR-04
).
```

VALID-15 — current text at lines 139-140:
```
- [x] **VALID-15
**: `AssignmentCreate/UpdateDto.StepId`/`SchemaId`: `NotEmpty` Guid
```

Footer — current text at line 397:
```
*Last updated: 2026-05-26 — ENTITY-04 and VALID-11 amended: ProcessorEntity InputSchemaId/OutputSchemaId are now nullable `Guid?`*
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ENTITY-04 to add ConfigSchemaId to the nullable Schema FK enumeration</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current ENTITY-04 at lines 59-60)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (ENTITY-04 amend block at lines 567-577 — verbatim target text)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-SPEC.md (REQ-5 ENTITY-04 target)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` ENTITY-04 (lines 59-60) in place. Preserve the `- [x] **ENTITY-04`+newline+`**:` header bytes EXACTLY as they appear (the ID, status checkbox, and the newline split inside the bold marker — Phase 3 D-03b precedent for ID preservation invariant).

    Replace the body sentence with:
    `\`ProcessorEntity : BaseEntity\` adds \`SourceHash\` string (SHA-256 hex, unique), \`InputSchemaId\` Guid? (nullable FK→Schema), \`OutputSchemaId\` Guid? (nullable FK→Schema), \`ConfigSchemaId\` Guid? (nullable FK→Schema). Null permitted on all three — supports source processors (no input), sink processors (no output), and unconfigured processors (no config). DB columns are nullable; Postgres FK constraints (\`fk_processor_input_schema_id\`, \`fk_processor_output_schema_id\`, \`fk_processor_config_schema_id\`) still enforced when value is non-null.`

    Wording exactly per PATTERNS.md lines 567-577. Constraint names enumerated explicitly so a reader can trace REQ-5 → Phase 4 PostgresExceptionMapper Option A regex (`^fk_[a-z0-9]+_(?<col>[a-z0-9_]+)$`) without re-deriving the invariant.
  </action>
  <verify>
    <automated>grep -c "ConfigSchemaId" .planning/REQUIREMENTS.md returns at least 1 (will be more after Task 3); grep -c "fk_processor_config_schema_id" .planning/REQUIREMENTS.md returns exactly 1 (introduced by ENTITY-04); grep -E "^\\- \\[x\\] \\*\\*ENTITY-04" .planning/REQUIREMENTS.md returns 1 (ID preserved); grep "source processors (no input), sink processors (no output), and unconfigured processors (no config)" .planning/REQUIREMENTS.md returns 1 (trinary wording locked)</automated>
  </verify>
  <done>ENTITY-04 lists 3 nullable Schema FK fields with all 3 constraint names; "source/sink/unconfigured" trinary wording present; REQ-ID byte-preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Amend ENTITY-07 to drop SchemaId; amend VALID-15 to drop SchemaId rule</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current ENTITY-07 at lines 65-66; current VALID-15 at lines 139-140)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (ENTITY-07 + VALID-15 amend text at lines 579-583 and 595-598)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-SPEC.md (REQ-5 ENTITY-07 + VALID-15 target)
  </read_first>
  <action>
    Two in-place edits in `.planning/REQUIREMENTS.md`. Preserve REQ-ID headers byte-identical (the `- [x] **ENTITY-07`+newline+`**:` and `- [x] **VALID-15`+newline+`**:` patterns are load-bearing — Plan 09-02-style ID preservation per D-01).

    Edit 1 (ENTITY-07 body, lines 65-66):
    - BEFORE: `\`AssignmentEntity : BaseEntity\` adds \`StepId\` Guid (FK→Step), \`SchemaId\` Guid (FK→Schema), \`Payload\` string (jsonb)`
    - AFTER:  `\`AssignmentEntity : BaseEntity\` adds \`StepId\` Guid (FK→Step), \`Payload\` string (jsonb)`

    Edit 2 (VALID-15 body, lines 139-140):
    - BEFORE: `\`AssignmentCreate/UpdateDto.StepId\`/\`SchemaId\`: \`NotEmpty\` Guid`
    - AFTER:  `\`AssignmentCreate/UpdateDto.StepId\`: \`NotEmpty\` Guid`

    The PERSIST-12/13/14 + ERROR-04/05/11 cross-references in CONTEXT.md `<canonical_refs>` still hold — `fk_assignment_step_id` survives as the sole assignments FK; the constraint-naming regex invariant continues to map any future 23503/23505 from the assignments table cleanly. No other REQ-IDs reference `Assignment.SchemaId`, so no cascade edits needed.
  </action>
  <verify>
    <automated>! grep -E "AssignmentEntity.*SchemaId|AssignmentCreate/UpdateDto.StepId.*SchemaId" .planning/REQUIREMENTS.md (negation — must be absent); grep -E "^\\- \\[x\\] \\*\\*ENTITY-07" .planning/REQUIREMENTS.md returns 1; grep -E "^\\- \\[x\\] \\*\\*VALID-15" .planning/REQUIREMENTS.md returns 1; grep "AssignmentEntity : BaseEntity\\` adds \\`StepId\\` Guid (FK→Step), \\`Payload\\`" .planning/REQUIREMENTS.md returns 1 (ENTITY-07 new body present)</automated>
  </verify>
  <done>ENTITY-07 lists only StepId + Payload; VALID-15 lists only StepId; REQ-IDs preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Amend VALID-11 to add ConfigSchemaId to the When().HasValue rule list; update footer</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current VALID-11 at lines 131-132; current footer at line 397)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-PATTERNS.md (VALID-11 amend at lines 587-593; footer amend at lines 601-603)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-SPEC.md (REQ-5 VALID-11 + footer)
  </read_first>
  <action>
    Two in-place edits in `.planning/REQUIREMENTS.md`. Preserve REQ-ID header bytes.

    Edit 1 (VALID-11 body, lines 131-132) — replace with the verbatim PATTERNS.md target (lines 587-593):
    `\`ProcessorCreate/UpdateDto.InputSchemaId\`/\`OutputSchemaId\`/\`ConfigSchemaId\`: nullable \`Guid?\` — null is valid (source/sink/unconfigured processor). When present, must not equal \`Guid.Empty\`. FluentValidation pattern: \`When(x => x.InputSchemaId.HasValue, () => RuleFor(x => x.InputSchemaId!.Value).NotEqual(Guid.Empty));\` (same for \`OutputSchemaId\` and \`ConfigSchemaId\`). \`Guid.Empty\` (\`00000000-0000-0000-0000-000000000000\`) is rejected at HTTP 400 by the validator, NOT at the DB layer. FK existence for non-empty Guids is still enforced by Postgres at persist time (SQLSTATE 23503 → HTTP 422 per ERROR-04`+newline+`).`

    Note: the trailing newline + `).` two-character sequence in the current body is preserved (Markdown rendering of the ERROR-04 cross-reference; do not collapse).

    Edit 2 (footer, line 397):
    - BEFORE: `*Last updated: 2026-05-26 — ENTITY-04 and VALID-11 amended: ProcessorEntity InputSchemaId/OutputSchemaId are now nullable \`Guid?\`*`
    - AFTER:  `*Last updated: 2026-05-28 — Phase 10 amendments for Assignment.SchemaId removal + Processor.ConfigSchemaId addition*`
  </action>
  <verify>
    <automated>grep -c "ConfigSchemaId" .planning/REQUIREMENTS.md returns at least 2 (ENTITY-04 from Task 1 + VALID-11 from this task — actual is 3 because VALID-11 mentions it twice in the parenthetical); grep "same for \\`OutputSchemaId\\` and \\`ConfigSchemaId\\`" .planning/REQUIREMENTS.md returns 1 (rule enumeration locked); grep "2026-05-28 — Phase 10 amendments" .planning/REQUIREMENTS.md returns 1 (footer locked); grep -E "^\\- \\[x\\] \\*\\*VALID-11" .planning/REQUIREMENTS.md returns 1 (ID preserved)</automated>
  </verify>
  <done>VALID-11 enumerates 3 nullable-Guid Schema FK fields; footer carries Phase 10 amendment note.</done>
</task>

<task type="auto">
  <name>Task 4: Commit doc amendments as commit #1 of the Phase 10 sequence</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (just-edited; do not re-read full file — use git diff to confirm scope)
    - .planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-CONTEXT.md (D-02 commit message)
  </read_first>
  <action>
    Stage ONLY `.planning/REQUIREMENTS.md` (no other files — this commit is doc-only per D-01) and create commit #1 with the exact message from CONTEXT D-02:

    ```
    docs(req): amend ENTITY-04/07 + VALID-11/15 for Phase 10 shape
    ```

    Use a HEREDOC for the commit body. Verify `git status` is clean after the commit (no other files staged or modified). Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --format=%s returns "docs(req): amend ENTITY-04/07 + VALID-11/15 for Phase 10 shape"; git show --stat HEAD lists exactly 1 file changed (.planning/REQUIREMENTS.md); git status --porcelain returns empty</automated>
  </verify>
  <done>Single doc-only commit landed; working tree clean; commit message matches D-02 verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only commit. No code paths, no endpoints, no auth surfaces, no error shapes touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | T (Tampering) | .planning/REQUIREMENTS.md | accept | Repository-local file under developer git control; tampering risk is the same as for any other repo file. No special mitigation needed beyond standard git history audit (forensic-friendly per Phase 3 D-03b doc-first precedent). |
| T-10-01-02 | I (Information disclosure) | .planning/REQUIREMENTS.md | accept | File contains no secrets, no PII, no credentials. Already public in the git repo. No new attack surface introduced. |

No new endpoints, no new error shapes, no new exception types. FK constraint regex invariant in `src/BaseApi.Core/Persistence/Exceptions/PostgresExceptionMapper.cs:54-67` (`fk_<table>_<column>` with no underscore in table segment) remains untouched by this commit — `fk_processor_config_schema_id` (introduced in Plans 03/04) parses cleanly as table="processor", column="config_schema_id". ASVS L1: doc-only commit, no L1 requirements applicable.
</threat_model>

<verification>
- `grep -c "ConfigSchemaId" .planning/REQUIREMENTS.md` returns at least 3 (ENTITY-04 has 1, VALID-11 has 2 — "ConfigSchemaId" in the field list + the second mention in the parenthetical).
- `grep -c "fk_processor_config_schema_id" .planning/REQUIREMENTS.md` returns exactly 1 (ENTITY-04 enumeration).
- `! grep -E "AssignmentCreate/UpdateDto.StepId.*SchemaId" .planning/REQUIREMENTS.md` (negation — VALID-15 must no longer list SchemaId).
- `! grep "AssignmentEntity.*adds.*SchemaId" .planning/REQUIREMENTS.md` (negation — ENTITY-07 must no longer list SchemaId in its property enumeration).
- `grep "2026-05-28 — Phase 10 amendments for Assignment.SchemaId removal" .planning/REQUIREMENTS.md` returns 1 (footer locked).
- `git log -1 --format=%s` matches `docs(req): amend ENTITY-04/07 + VALID-11/15 for Phase 10 shape` verbatim.
- All 4 REQ-ID headers (`- [x] **ENTITY-04`, `- [x] **ENTITY-07`, `- [x] **VALID-11`, `- [x] **VALID-15`) still present and unrenumbered — `grep -cE "^\\- \\[x\\] \\*\\*(ENTITY-04|ENTITY-07|VALID-11|VALID-15)" .planning/REQUIREMENTS.md` returns 4.
</verification>

<success_criteria>
1. REQUIREMENTS.md ENTITY-04 describes 3 nullable Schema FK fields (InputSchemaId + OutputSchemaId + ConfigSchemaId) with all 3 constraint names enumerated.
2. REQUIREMENTS.md ENTITY-07 lists only StepId + Payload (no SchemaId).
3. REQUIREMENTS.md VALID-11 enumerates the `When().HasValue → NotEqual(Guid.Empty)` rule for all 3 nullable Schema FK fields.
4. REQUIREMENTS.md VALID-15 covers only `AssignmentCreate/UpdateDto.StepId`.
5. REQUIREMENTS.md footer reads `*Last updated: 2026-05-28 — Phase 10 amendments for Assignment.SchemaId removal + Processor.ConfigSchemaId addition*`.
6. All 4 amended REQ-IDs preserve their original IDs — no superseding, no new REQ-IDs introduced.
7. Single git commit `docs(req): amend ENTITY-04/07 + VALID-11/15 for Phase 10 shape` exists at HEAD; modifies exactly 1 file (`.planning/REQUIREMENTS.md`); working tree clean post-commit.
</success_criteria>

<output>
After completion, create `.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-01-SUMMARY.md`.
</output>
