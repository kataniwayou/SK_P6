---
phase: 11-migrate-prometheus-and-elastic-containers-from-compose-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - OBSERV-12
  - OBSERV-03
  - OBSERV-04
  - OBSERV-08
  - INFRA-06
  - OBSERV-13
  - OBSERV-14
  - INFRA-08
  - TEST-07
must_haves:
  truths:
    - "REQUIREMENTS.md OBSERV-12 is marked SUPERSEDED (Phase 11 D-03) and moved to Out of Scope with rationale: no traces backend in v1; pipeline removed Phase 11"
    - "REQUIREMENTS.md INFRA-06 text is extended in place to declare elasticsearch + prometheus services with healthchecks AND that baseapi-service depends_on extends to all four (postgres + otel-collector + elasticsearch + prometheus)"
    - "REQUIREMENTS.md has 4 NEW REQ-IDs introduced: OBSERV-13 (logs land in ES index logs-generic-default with Attributes.CorrelationId preserved), OBSERV-14 (HTTP server metrics scraped by Prometheus with service_name='sk-api' label), INFRA-08 (compose stack: elasticsearch + prometheus image pins + healthcheck shape), TEST-07 (E2E round-trip test class drives a real HTTP request and asserts both backends ingested the resulting telemetry within < 60s)"
    - "REQUIREMENTS.md Out of Scope table appends a new row 'OTel tracing pipeline (traces backend)' with rationale referencing D-03 + sk2_1 CLAUDE.md non-negotiable #2"
    - "REQUIREMENTS.md footer reads: *Last updated: 2026-05-28 — Phase 11 amendments: OBSERV-12 superseded (traces removed); INFRA-06 extended (ES + Prom); new REQ-IDs OBSERV-13/14 + INFRA-08 + TEST-07*"
    - "All existing REQ-ID headers preserved byte-identical (no renumbering; OBSERV-12 keeps its ID; new IDs use the next available number per category)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Post-Phase-11 spec — OBSERV-12 superseded; INFRA-06 extended; OBSERV-13/14 + INFRA-08 + TEST-07 newly defined; footer carries Phase 11 amendment note"
      contains: "logs-generic-default"
  key_links:
    - from: ".planning/REQUIREMENTS.md OBSERV-13"
      to: ".planning/REQUIREMENTS.md INFRA-08"
      via: "ES exporter wiring on the collector (INFRA-08 declares the backend) and the index/field shape the test code queries (OBSERV-13)"
      pattern: "logs-generic-default"
    - from: ".planning/REQUIREMENTS.md OBSERV-14"
      to: ".planning/REQUIREMENTS.md INFRA-08"
      via: "Prom exporter on otel-collector:8889 (INFRA-08 declares the scrape target) and the service_name label (OBSERV-14 asserts the label)"
      pattern: "otel-collector:8889"
    - from: ".planning/REQUIREMENTS.md OBSERV-12 (superseded)"
      to: ".planning/REQUIREMENTS.md Out of Scope table"
      via: "OBSERV-12 row points readers to Out of Scope row explaining why traces are dropped"
      pattern: "traces backend"
---

<objective>
Doc-first commit per Phase 10 D-01/D-02 precedent. Amend `.planning/REQUIREMENTS.md` BEFORE any code, compose, or test changes land:

1. Mark `OBSERV-12` (OTel tracing enabled — logs + metrics + traces with `.AddNpgsql` DB spans) as `SUPERSEDED — Phase 11 D-03` and move it to Out of Scope with rationale: no traces backend in v1; pipeline removed Phase 11; mirrors sk2_1 CLAUDE.md non-negotiable #2.
2. Extend `INFRA-06` text in place to declare elasticsearch + prometheus services with healthchecks AND that baseapi-service `depends_on` extends to all four (postgres + otel-collector + elasticsearch + prometheus).
3. Add 4 new REQ-IDs (numbered after existing maxima per category):
   - `OBSERV-13`: Logs land in ES at index `logs-generic-default` with OTLP `Attributes.CorrelationId` preserved (per CONTEXT D-06 `mapping.mode: none`).
   - `OBSERV-14`: HTTP server metrics are scraped by Prometheus from `otel-collector:8889` and carry the `service_name="sk-api"` label (`resource_to_telemetry_conversion: true` per CONTEXT D-07).
   - `INFRA-08`: Compose stack declares `elasticsearch` (image `docker.elastic.co/elasticsearch/elasticsearch:8.15.5`, dev posture, healthcheck `start_period: 60s`) + `prometheus` (image `prom/prometheus:v3.11.3`, scrape config bind-mounted from `prometheus.yml`, healthcheck `wget --spider /-/healthy`). Otel-collector image bumped to `0.152.0`.
   - `TEST-07`: At least one E2E round-trip test drives a real HTTP request against a sk_p business endpoint (Schema POST per D-17), then polls Elasticsearch for the corresponding log record AND Prometheus for the resulting `http_server_request_duration_seconds_count` data point; total wall-clock budget per fact < 60s.
4. Append an Out of Scope row "OTel tracing pipeline (traces backend)" with rationale referencing D-03 + sk2_1 lock-in.
5. Update the footer with the Phase 11 amendment note.

Single atomic commit `docs(req): amend OBSERV-12 + INFRA-06 + add OBSERV-13/14 + INFRA-08 + TEST-07 for Phase 11 shape`.

Purpose: Forensic property — every later commit (compose changes, code strip, helper authoring, test migration) points back to the amended REQ-IDs; the spec is never out of sync with the code that follows it. Phase 10 5-commit bisect-friendly precedent honored.

Output: REQUIREMENTS.md with OBSERV-12 superseded + INFRA-06 extended + 4 new REQ-IDs + new Out of Scope row + amended footer; single doc-only git commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-RESEARCH.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-PATTERNS.md
@.planning/phases/10-remove-schemaid-on-assignmententity-and-add-configschemaid-o/10-01-PLAN.md

<interfaces>
<!-- Current REQ-ID locations in .planning/REQUIREMENTS.md (verified by Read) -->

OBSERV-12 (current text at lines 176-177):
```
- [x] **OBSERV-12
**: OTel tracing enabled (logs + metrics + traces) with `AddAspNetCoreInstrumentation`, `AddHttpClientInstrumentation`, `AddNpgsql` for DB spans
```

INFRA-06 (current text at lines 22-23):
```
- [x] **INFRA-06
**: `docker-compose.yml` defines `postgres:17-alpine` with `pg_isready` healthcheck plus `BaseApi.Service` with `depends_on: postgres: condition: service_healthy`
```

Traceability table OBSERV-12 row (current at line 356):
```
| OBSERV-12 | Phase 5 | Complete |
```

Phase summary footnote (current at line 390):
```
- Phase 5 (Observability + Health Probes): 14 requirements (OBSERV-01..08, OBSERV-12, HEALTH-01..05)
```

Out of Scope table (current ends around line 268 with the FluentValidation.AspNetCore row). The table format is:
```
| Feature | Reason |
|---------|--------|
| ... | ... |
```

Footer (current at line 397):
```
*Last updated: 2026-05-28 — Phase 10 amendments for Assignment.SchemaId removal + Processor.ConfigSchemaId addition*
```

Current max REQ-IDs by category (verified via grep — the new IDs to introduce):
- OBSERV: 12 → next free is 13, 14
- INFRA: 07 → next free is 08
- TEST: 06 (TEST-03/04 deferred in place) → next free is 07
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supersede OBSERV-12 and append Out of Scope row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current OBSERV-12 at lines 176-177; current Out of Scope table starts around line 248 with "## Out of Scope" header)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-03 + D-19)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-RESEARCH.md (lines 22-23 — verbatim D-03 lock-in)
  </read_first>
  <action>
    Two in-place edits in `.planning/REQUIREMENTS.md`.

    Edit 1 — OBSERV-12 body (lines 176-177). Preserve the `- [x] **OBSERV-12`+newline+`**:` header bytes EXACTLY (Phase 3 D-03b ID preservation invariant — same pattern as Plan 10-01). Change the leading checkbox from `[x]` to `[ ]` and rewrite the body to mark superseded:
    - BEFORE:
      ```
      - [x] **OBSERV-12
      **: OTel tracing enabled (logs + metrics + traces) with `AddAspNetCoreInstrumentation`, `AddHttpClientInstrumentation`, `AddNpgsql` for DB spans
      ```
    - AFTER:
      ```
      - [ ] **OBSERV-12 [SUPERSEDED — Phase 11 D-03]
      **: OTel tracing pipeline removed in Phase 11 (D-03). Moved to Out of Scope. Rationale: no traces backend in v1 (mirrors sk2_1 CLAUDE.md non-negotiable #2); `.WithTracing()` registration stripped from `AddBaseApiObservability`; collector traces pipeline + Npgsql tracing instrumentation no longer wired. See "OTel tracing pipeline (traces backend)" Out of Scope row.
      ```

    Edit 2 — append a new row to the Out of Scope table (located under the "## Out of Scope" H2 heading; the table is the `| Feature | Reason |` block ending with the `EF Core InMemory / SQLite for tests` row). Append a new row immediately after the last existing row, BEFORE the `## Traceability` H2 that follows:
    ```
    | OTel tracing pipeline (traces backend) | Phase 11 D-03 — no traces backend in v1; mirrors sk2_1 CLAUDE.md non-negotiable #2. Supersedes OBSERV-12. `.WithTracing()` stripped from `AddBaseApiObservability`; collector traces pipeline deleted; `TraceExportTests` deleted. Revival is a future-milestone candidate when request-flow debugging becomes painful. |
    ```

    Do NOT touch the Traceability table row for OBSERV-12 yet — that edit lives in Task 4 (which also bumps the phase summary footnote for symmetry).
  </action>
  <verify>
    <automated>grep -E "^\- \[ \] \*\*OBSERV-12 \[SUPERSEDED — Phase 11 D-03\]" .planning/REQUIREMENTS.md returns 1; grep -E "^\- \[x\] \*\*OBSERV-12" .planning/REQUIREMENTS.md returns 0 (old checkbox state replaced); grep -c "OTel tracing pipeline (traces backend)" .planning/REQUIREMENTS.md returns at least 1; grep "Phase 11 D-03 — no traces backend in v1" .planning/REQUIREMENTS.md returns 1; grep -c "Out of Scope" .planning/REQUIREMENTS.md returns at least 1 (header intact)</automated>
  </verify>
  <done>OBSERV-12 header byte-preserved with `[ ]` checkbox + `[SUPERSEDED — Phase 11 D-03]` marker; body rewritten to reference Out of Scope row; new Out of Scope row appended verbatim with D-03 rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Extend INFRA-06 text in place</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current INFRA-06 at lines 22-23)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-12 / D-13 / D-15 verbatim service shapes)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` INFRA-06 (lines 22-23) in place. Preserve the `- [x] **INFRA-06`+newline+`**:` header bytes EXACTLY.

    Replace the body sentence with:
    ```
    `docker-compose.yml` (filename `compose.yaml` per Compose v2 default per Phase 2 D-10) declares `postgres:17-alpine` (with `pg_isready` healthcheck), `elasticsearch:8.15.5` (with `curl -fs '/_cluster/health?wait_for_status=yellow&timeout=5s'` healthcheck and `start_period: 60s` per Phase 11 D-12; dev posture per sk2_1 — `discovery.type=single-node`, `xpack.security.enabled=false`, no volume), `otel-collector:0.152.0` (distroless — no in-container healthcheck per Phase 5 Plan 05-01 deviation #1), and `prom/prometheus:v3.11.3` (with `wget --spider /-/healthy` healthcheck per Phase 11 D-13). `baseapi-service.depends_on` extends to ALL four services: `postgres: service_healthy`, `otel-collector: service_started`, `elasticsearch: service_healthy`, `prometheus: service_healthy` (per Phase 11 D-15). Compose-up therefore blocks for ~60s on ES cold-start; acceptable for dev + CI.
    ```
  </action>
  <verify>
    <automated>grep -E "^\- \[x\] \*\*INFRA-06" .planning/REQUIREMENTS.md returns 1 (header byte-preserved); grep "elasticsearch:8.15.5" .planning/REQUIREMENTS.md returns 1; grep "prom/prometheus:v3.11.3" .planning/REQUIREMENTS.md returns 1; grep "otel-collector:0.152.0" .planning/REQUIREMENTS.md returns 1; grep -c "service_healthy" .planning/REQUIREMENTS.md returns at least 3 (postgres + elasticsearch + prometheus); grep "Phase 11 D-15" .planning/REQUIREMENTS.md returns 1</automated>
  </verify>
  <done>INFRA-06 text enumerates all 4 services with their image pins + healthcheck shapes + the extended depends_on chain; REQ-ID preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add OBSERV-13 + OBSERV-14 + INFRA-08 + TEST-07 in correct category sections</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Observability section ends at line 177 OBSERV-12; Infrastructure section ends at line 24 INFRA-07; Testing section ends at line 226 TEST-06)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-06/D-07/D-08 + D-12/D-13/D-15 + D-17/D-18)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-RESEARCH.md (Pattern 1/2/3 + Pitfall 1/2 + Open Q1)
  </read_first>
  <action>
    Three in-place edits in `.planning/REQUIREMENTS.md` to append 4 new REQ-IDs in their correct category sections. Use the same bullet style as existing entries (split header line per the `- [x] **NAME-NN`+newline+`**:` pattern).

    Edit 1 — append OBSERV-13 + OBSERV-14 in the Observability section, immediately AFTER the newly-superseded OBSERV-12 block (which lives at the end of the `### Observability` section, just before the `### Health Probes` H3):
    ```
    - [ ] **OBSERV-13
    **: OTel collector ships logs to Elasticsearch via the contrib `elasticsearch` exporter (`mapping.mode: none` per Phase 11 D-06). Logs land in the data stream `logs-generic-default` (backing index `.ds-logs-generic-default-NNNN`) with the OTLP raw field shape preserved — `Attributes.CorrelationId` matches the Phase 4 `X-Correlation-Id` middleware value; resource attributes `service.name=sk-api` and `service.version=3.2.0` appear under `Resource.attributes` on every log doc. ES exporter `endpoints: [http://elasticsearch:9200]`; no auth, no TLS (dev posture). Verification: Wave 0 manual `curl /_cat/indices` after first compose-up confirms actual index name and field shape (RESEARCH Open Q1).
    - [ ] **OBSERV-14
    **: OTel collector exposes a Prometheus exporter on `0.0.0.0:8889` with `resource_to_telemetry_conversion: { enabled: true }` (Phase 11 D-07; `service.name` becomes the `service_name` Prom label — load-bearing for test assertions) and `send_timestamps: true`. The standalone Prometheus container scrapes `otel-collector:8889` at 15s intervals (Phase 11 D-08, verbatim from sk2_1). Test code queries `http://localhost:9090/api/v1/query?query=...` and assertions reference Prom-form metric names (e.g., `http_server_request_duration_seconds_count` — dot-to-underscore + `_seconds_count` histogram suffix per OTel→Prom spec, RESEARCH Pitfall 1). `filter/health_metrics` processor still drops `/health/*` data points before they reach the Prom exporter (D-04; Phase 5 fix-forward preserved).
    ```

    Edit 2 — append INFRA-08 to the Infrastructure section, immediately AFTER the existing INFRA-07 bullet (which lives at the end of the `### Infrastructure` section, just before the `### Persistence` H3):
    ```
    - [ ] **INFRA-08
    **: Phase 11 compose-stack additions. `compose.yaml` declares `elasticsearch` service (image `docker.elastic.co/elasticsearch/elasticsearch:8.15.5` per Phase 11 D-10; env `discovery.type=single-node` + `xpack.security.enabled=false` + `xpack.security.enrollment.enabled=false` + `ES_JAVA_OPTS=-Xms512m -Xmx512m`; port `9200:9200`; healthcheck `curl -fs 'http://localhost:9200/_cluster/health?wait_for_status=yellow&timeout=5s' || exit 1` with `start_period: 60s`; no volume — ephemeral dev posture per D-12) AND `prometheus` service (image `prom/prometheus:v3.11.3` per Phase 11 D-11; command `--config.file=/etc/prometheus/prometheus.yml --web.enable-lifecycle`; bind-mount `./prometheus.yml:/etc/prometheus/prometheus.yml:ro`; port `9090:9090`; healthcheck `wget --no-verbose --tries=1 --spider http://localhost:9090/-/healthy`; depends on `otel-collector: service_started`; no volume — ephemeral dev posture per D-13). `otel-collector` image bumped to `otel/opentelemetry-collector-contrib:0.152.0` (per Phase 11 D-09; D-14 adds port `8889:8889` to the existing ports list and removes the `./tests/.otel-out:/var/otel-out` bind-mount + `user: "0:0"` override no longer needed without file exporter). `prometheus.yml` (NEW file at repo root) carries the single `job_name: 'otel-collector'` scrape config targeting `otel-collector:8889` per D-08. Container-name collision with sibling sk2_1 stack is acknowledged Out of Scope (RESEARCH Pitfall 4) — sk_p and sk2_1 stacks are mutually exclusive on a given Docker daemon.
    ```

    Edit 3 — append TEST-07 to the Testing section, immediately AFTER the existing TEST-06 bullet (which lives at the end of the `### Testing` section, just before the `## v2 Requirements` H2):
    ```
    - [ ] **TEST-07
    **: Round-trip E2E test class(es) under `tests/BaseApi.Tests/Observability/` (Phase 11 D-17) drive a real HTTP request against a sk_p business endpoint (Schema POST per D-17) via in-process `Phase11WebAppFactory : Phase8WebAppFactory` then poll BOTH backends within a bounded budget: (a) Elasticsearch `POST /logs-generic-default/_search` with `Attributes.CorrelationId` term query — must return a hit within 30s (`ElasticsearchTestClient` helper with exponential backoff 200ms→3.2s and HTTP 404 + empty-hits tolerance per RESEARCH Pattern 2); (b) Prometheus `GET /api/v1/query?query=http_server_request_duration_seconds_count{service_name="sk-api",http_route="api/v1/schemas"}` — must return a sample with cumulative count ≥ requests-issued within 60s (`PrometheusTestClient` helper with mandatory 15s initial sleep then 3s poll interval per RESEARCH Pattern 3 / Pitfall 7). Per-test unique correlation-id (`$"{Guid.NewGuid():N}"`) is the ES cleanup discipline (T-11-03 mitigation; analog to Phase 3 D-15 psql `\l` byte-identical proof). `Phase11WebAppFactory` overrides `PeriodicExportingMetricReaderOptions.ExportIntervalMilliseconds = 1_000` (RESEARCH Pattern 4 / Pitfall 7 — default 60s would exceed budget). Tests carry `[Trait("Phase","11")]` + `[Trait("Category","E2E")]` for `dotnet test --filter "Category!=E2E"` fast-path.
    ```
  </action>
  <verify>
    <automated>grep -cE "^\- \[ \] \*\*(OBSERV-13|OBSERV-14|INFRA-08|TEST-07)" .planning/REQUIREMENTS.md returns 4; grep "logs-generic-default" .planning/REQUIREMENTS.md returns at least 2 (OBSERV-13 + TEST-07); grep "otel-collector:8889" .planning/REQUIREMENTS.md returns at least 2 (OBSERV-14 + INFRA-08); grep "ExportIntervalMilliseconds = 1_000" .planning/REQUIREMENTS.md returns 1; grep "Phase11WebAppFactory" .planning/REQUIREMENTS.md returns at least 1; grep "Phase 11 D-06" .planning/REQUIREMENTS.md returns 1; grep "Phase 11 D-13" .planning/REQUIREMENTS.md returns 1; grep "Phase 11 D-17" .planning/REQUIREMENTS.md returns 1</automated>
  </verify>
  <done>4 new REQ-IDs (OBSERV-13, OBSERV-14, INFRA-08, TEST-07) appended in their correct category sections; each references back to the locking CONTEXT decision (D-06 through D-17) so a reader can trace REQ → decision → implementation.</done>
</task>

<task type="auto">
  <name>Task 4: Update Traceability table, phase summary footnotes, and document footer</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Traceability table starts at line 273; OBSERV-12 row at line 356; per-category footnote at lines 386-393; document footer at line 397)
  </read_first>
  <action>
    Four in-place edits in `.planning/REQUIREMENTS.md`.

    Edit 1 — update the Traceability table row for OBSERV-12 (line 356):
    - BEFORE: `| OBSERV-12 | Phase 5 | Complete |`
    - AFTER:  `| OBSERV-12 | Phase 5 → Phase 11 (Superseded) | Out of Scope |`

    Edit 2 — append 4 new rows to the Traceability table, immediately AFTER the last existing row (the table ends just before the `**Coverage:**` paragraph block around line 379):
    ```
    | OBSERV-13 | Phase 11 | Pending |
    | OBSERV-14 | Phase 11 | Pending |
    | INFRA-08 | Phase 11 | Pending |
    | TEST-07 | Phase 11 | Pending |
    ```

    Edit 3 — update the per-phase coverage footnotes block (lines 386-393):
    - REMOVE `OBSERV-12` from the Phase 5 footnote — change `OBSERV-01..08, OBSERV-12, HEALTH-01..05` to `OBSERV-01..08, HEALTH-01..05` (OBSERV-12 superseded).
    - APPEND a new Phase 11 footnote line BEFORE the `**Coverage:**` paragraph (the per-phase block is a bullet list; insert as last bullet):
      ```
      - Phase 11 (Migrate Prometheus and Elasticsearch from compose stack sk2_1): 4 requirements (OBSERV-13, OBSERV-14, INFRA-08, TEST-07) — plus amendments to OBSERV-12 (superseded → Out of Scope) and INFRA-06 (extended for ES + Prom + collector image bump)
      ```
    - Update the `**Total v1 requirements:**` line: currently `102`; after Phase 11 the active total becomes `102 - 1 + 4 = 105` (OBSERV-12 leaves; OBSERV-13/14 + INFRA-08 + TEST-07 join). Rewrite the parenthesized breakdown to read: `7 INFRA (now 8 with INFRA-08) + 15 PERSIST + 10 ENTITY + 16 HTTP + 20 VALID + 12 OBSERV (now 13 active: OBSERV-12 superseded + OBSERV-13/14 added) + 5 HEALTH + 11 ERROR + 6 TEST (now 7 with TEST-07; TEST-03/04 still v2-deferred)`. Update the count headline to `105 total active v1 (one OBSERV superseded to Out of Scope; 4 new IDs added Phase 11)`.

    Edit 4 — update the document footer (line 397):
    - BEFORE: `*Last updated: 2026-05-28 — Phase 10 amendments for Assignment.SchemaId removal + Processor.ConfigSchemaId addition*`
    - AFTER:  `*Last updated: 2026-05-28 — Phase 11 amendments: OBSERV-12 superseded (traces removed); INFRA-06 extended (ES + Prom + collector image bump); new REQ-IDs OBSERV-13/14 + INFRA-08 + TEST-07*`
  </action>
  <verify>
    <automated>grep "OBSERV-12 | Phase 5 → Phase 11 (Superseded) | Out of Scope" .planning/REQUIREMENTS.md returns 1; grep -E "^\| (OBSERV-13|OBSERV-14|INFRA-08|TEST-07) \| Phase 11 \| Pending \|" .planning/REQUIREMENTS.md returns 4; grep "Phase 11 (Migrate Prometheus and Elasticsearch from compose stack sk2_1)" .planning/REQUIREMENTS.md returns 1; grep "2026-05-28 — Phase 11 amendments" .planning/REQUIREMENTS.md returns 1; ! grep "OBSERV-01..08, OBSERV-12, HEALTH-01..05" .planning/REQUIREMENTS.md (negation — old footnote replaced); grep "OBSERV-01..08, HEALTH-01..05" .planning/REQUIREMENTS.md returns 1</automated>
  </verify>
  <done>Traceability table reflects OBSERV-12 supersession + 4 new Phase 11 rows; per-phase footnotes updated (Phase 5 loses OBSERV-12; new Phase 11 line appears); coverage headline rebalanced; document footer carries Phase 11 amendment note.</done>
</task>

<task type="auto">
  <name>Task 5: Commit doc amendments as commit #1 of the Phase 11 sequence</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (just-edited; do not re-read full file — use `git diff` to confirm scope is ONLY this single file)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-19 commit-message guidance)
  </read_first>
  <action>
    Stage ONLY `.planning/REQUIREMENTS.md` (no other files — this commit is doc-only per the Phase 10 D-01 precedent). Create commit #1 with the exact message:

    ```
    docs(req): amend OBSERV-12 + INFRA-06 + add OBSERV-13/14 + INFRA-08 + TEST-07 for Phase 11 shape
    ```

    Use a HEREDOC for the commit message body to preserve formatting. Verify `git status --porcelain` returns empty after the commit (no other files staged or modified). Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --format=%s returns "docs(req): amend OBSERV-12 + INFRA-06 + add OBSERV-13/14 + INFRA-08 + TEST-07 for Phase 11 shape"; git show --stat HEAD lists exactly 1 file changed (.planning/REQUIREMENTS.md); git status --porcelain returns empty</automated>
  </verify>
  <done>Single doc-only commit landed; working tree clean; commit message verbatim. Later Phase 11 plans (compose, code strip, helpers, tests) can now reference the amended REQ-IDs as authoritative source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only commit. No code paths, no endpoints, no auth surfaces, no error shapes touched. Repository-local Markdown file under git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | T (Tampering) | .planning/REQUIREMENTS.md | accept | Standard git history audit suffices — same posture as Plan 10-01 (forensic-friendly per Phase 3 D-03b doc-first precedent). |
| T-11-01-02 | I (Information disclosure) | .planning/REQUIREMENTS.md | accept | File contains no secrets, no PII, no credentials. Image pins (8.15.5, v3.11.3, 0.152.0) are public Docker Hub tags. |

No new endpoints, no new error shapes, no new exception types. ASVS L1: doc-only commit, no L1 requirements applicable. The supply-chain hardening posture (exact-version image pins from D-09/10/11) is documented IN this commit but not yet implemented — the implementation lives in Plan 11-02 (compose.yaml mutation) where T-11-05 (image supply-chain) becomes load-bearing.
</threat_model>

<verification>
- `grep -E "^\- \[ \] \*\*OBSERV-12 \[SUPERSEDED — Phase 11 D-03\]" .planning/REQUIREMENTS.md` returns 1 (header byte-preserved with new state marker).
- `grep -cE "^\- \[ \] \*\*(OBSERV-13|OBSERV-14|INFRA-08|TEST-07)" .planning/REQUIREMENTS.md` returns 4 (all 4 new IDs land in correct sections).
- `grep "OTel tracing pipeline (traces backend)" .planning/REQUIREMENTS.md` returns at least 1 (Out of Scope row + OBSERV-12 cross-reference).
- `grep "logs-generic-default" .planning/REQUIREMENTS.md` returns at least 2 (OBSERV-13 + TEST-07 both reference).
- `grep "Phase11WebAppFactory" .planning/REQUIREMENTS.md` returns at least 1 (TEST-07).
- `grep "ExportIntervalMilliseconds = 1_000" .planning/REQUIREMENTS.md` returns 1 (TEST-07 Pitfall 7 mitigation cited).
- `grep "elasticsearch:8.15.5" .planning/REQUIREMENTS.md` AND `grep "prom/prometheus:v3.11.3" .planning/REQUIREMENTS.md` AND `grep "otel-collector:0.152.0" .planning/REQUIREMENTS.md` each return 1 (image pins enumerated).
- `git log -1 --format=%s` matches `docs(req): amend OBSERV-12 + INFRA-06 + add OBSERV-13/14 + INFRA-08 + TEST-07 for Phase 11 shape` verbatim.
- `git show --stat HEAD` shows exactly 1 file changed.
- `git status --porcelain` returns empty after the commit.
</verification>

<success_criteria>
1. REQUIREMENTS.md OBSERV-12 marked `[SUPERSEDED — Phase 11 D-03]` and body rewritten to point at Out of Scope row; checkbox flipped `[x]` → `[ ]`.
2. REQUIREMENTS.md Out of Scope table carries a new row "OTel tracing pipeline (traces backend)" with D-03 rationale.
3. REQUIREMENTS.md INFRA-06 text extended to enumerate ES + Prom + collector image bump + extended depends_on chain.
4. REQUIREMENTS.md has 4 new REQ-IDs: OBSERV-13 (logs land in ES), OBSERV-14 (metrics scraped by Prom with service_name label), INFRA-08 (ES + Prom compose declarations + collector bump), TEST-07 (E2E round-trip test class).
5. REQUIREMENTS.md Traceability table updated: OBSERV-12 row marked Superseded; 4 new rows Pending Phase 11.
6. REQUIREMENTS.md per-phase footnote block: Phase 5 line drops OBSERV-12; new Phase 11 line appears.
7. REQUIREMENTS.md document footer reads `*Last updated: 2026-05-28 — Phase 11 amendments: OBSERV-12 superseded (traces removed); INFRA-06 extended (ES + Prom + collector image bump); new REQ-IDs OBSERV-13/14 + INFRA-08 + TEST-07*`.
8. Single git commit `docs(req): amend OBSERV-12 + INFRA-06 + add OBSERV-13/14 + INFRA-08 + TEST-07 for Phase 11 shape` exists at HEAD; modifies exactly 1 file (`.planning/REQUIREMENTS.md`); working tree clean post-commit.
</success_criteria>

<output>
After completion, create `.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-01-SUMMARY.md`.
</output>
</content>
</invoke>