---
phase: 11-migrate-prometheus-and-elastic-containers-from-compose-stack
plan: 04
type: execute
wave: 3
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - prometheus.yml
autonomous: true
requirements:
  - INFRA-08
  - OBSERV-14
must_haves:
  truths:
    - "`prometheus.yml` exists at the sk_p repo root (NOT in `compose/` — sk2_1 verbatim layout per D-08)"
    - "`prometheus.yml` declares global scrape_interval: 15s, evaluation_interval: 15s, scrape_timeout: 10s (verbatim from sk2_1 per D-08)"
    - "`prometheus.yml` declares exactly one scrape_configs job: job_name 'otel-collector' with one static_target 'otel-collector:8889' on metrics_path '/metrics'"
    - "NO authentication blocks (basic_auth, bearer_token, tls_config) — dev posture per D-08"
    - "NO metric_relabel_configs — raw OTel → Prometheus name translation (resource_to_telemetry_conversion lives on the collector's prometheus exporter, NOT on Prom-side rewriting)"
    - "`docker compose -f compose.yaml restart prometheus` succeeds after this file lands; Prom logs show 'Server is ready to receive web requests' and no scrape errors against otel-collector:8889"
  artifacts:
    - path: "prometheus.yml"
      provides: "Phase 11 Prom scrape config — single static target pointed at the collector's Prom-exporter endpoint"
      contains: "otel-collector:8889"
  key_links:
    - from: "prometheus.yml scrape_configs[0].static_configs[0].targets"
      to: "compose.yaml prometheus.volumes (bind-mount)"
      via: "Prom container reads /etc/prometheus/prometheus.yml on startup (bind-mounted from sk_p repo root via Plan 11-02 Task 3 volumes entry)"
      pattern: "otel-collector:8889"
    - from: "prometheus.yml scrape_configs[0].static_configs[0].targets ('otel-collector:8889')"
      to: "compose/otel-collector-config.yaml exporters.prometheus.endpoint ('0.0.0.0:8889')"
      via: "Compose-internal DNS — `otel-collector` resolves to the sk-otel-collector container; its Prom exporter listens on container :8889 per Plan 11-03 D-07"
      pattern: "0.0.0.0:8889"
---

<objective>
Create a new file `prometheus.yml` at the sk_p repo root carrying the verbatim sk2_1 scrape configuration per CONTEXT D-08. This file is bind-mounted into the new `prometheus` service (created in Plan 11-02 Task 3) at `/etc/prometheus/prometheus.yml:ro`.

Single static scrape target: `otel-collector:8889` (compose-internal DNS resolves to the sk-otel-collector container; Plan 11-03 wired its `prometheus` exporter on `0.0.0.0:8889`). 15-second scrape interval. No relabel configs. No authentication.

Purpose: complete the Prom side of the metrics pipeline. After this commit + a Prom restart, the standalone Prometheus container scrapes the collector's `/metrics` endpoint every 15 s, ingests the OTLP-converted metric series, and exposes `/api/v1/query` on host port 9090 for test code to probe.

This plan now runs in **Wave 3** (depends on Plan 11-02 + 11-01) — revised from the original Wave 2 placement after the checker flagged a runtime dependency: Task 2's `docker compose restart prometheus` requires the prometheus service block + `./prometheus.yml` bind-mount declaration to already exist in `compose.yaml` (which Plan 11-02 lands). Plan 11-04 is parallel-safe with Plan 11-05 within Wave 3 (different files: 11-04 edits prometheus.yml + restarts container; 11-05 edits ObservabilityServiceCollectionExtensions.cs + .gitignore + deletes tests/.otel-out/). Plan 11-03 (collector config rewire) still runs in Wave 2 alongside Plan 11-02.

Output: A single atomic commit `feat(prometheus): add scrape config for otel-collector:8889 (verbatim from sk2_1)` creating exactly one new file (`prometheus.yml`). After commit lands + Prom restart, Prom logs show successful scrape of `otel-collector:8889`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-RESEARCH.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-PATTERNS.md
@C:/Users/UserL/source/repos/sk2_1/prometheus.yml

<interfaces>
<!-- Verbatim sk2_1 prometheus.yml content (per CONTEXT D-08) -->

```yaml
# Phase 12 INFR-07: Prometheus scrape config for the otel-collector's prometheus exporter.
# Single static target — the collector exposes /metrics on :8889 (see otel-collector-config.yaml).
# 15s scrape interval matches CONTEXT.md D-07.

global:
  scrape_interval: 15s        # D-07: matches default; explicit for clarity
  evaluation_interval: 15s    # Rule eval cadence (no rules defined — kept for default sanity)
  scrape_timeout: 10s         # Must be < scrape_interval per Prometheus validation

scrape_configs:
  - job_name: 'otel-collector'
    # NO authentication — dev mode (parallels docker-compose.yml D-05).
    # NO tls_config — dev mode.
    static_configs:
      - targets:
          # Compose-internal hostname; resolves via the default compose network.
          - 'otel-collector:8889'
    # Path defaults to /metrics — explicit for documentation clarity.
    metrics_path: '/metrics'
    # No metric_relabel_configs — we want raw OTel→Prometheus name translation
    # (resource_to_telemetry_conversion: true lives on the collector's prometheus exporter).
```

The sk2_1 comment header references "Phase 12 INFR-07" and "CONTEXT.md D-07" — those are sk2_1-internal labels. For sk_p Phase 11, rewrite the comment header to reference Phase 11 D-08 + Phase 11 D-07 (the collector exporter setting) so a reader of this file can trace back to the canonical sk_p decision document.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prometheus.yml at the sk_p repo root</name>
  <files>prometheus.yml</files>
  <read_first>
    - C:/Users/UserL/source/repos/sk2_1/prometheus.yml (full file — verbatim source per D-08)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-07 + D-08 verbatim)
    - compose.yaml (Plan 11-02 Task 3 added bind-mount `./prometheus.yml:/etc/prometheus/prometheus.yml:ro` to the prometheus service block — verify this is in place before this task runs by grepping `./prometheus.yml:/etc/prometheus/prometheus.yml:ro` in compose.yaml)
  </read_first>
  <action>
    Create a new file `prometheus.yml` at the sk_p repo root (`C:/Users/UserL/source/repos/SK_P/prometheus.yml`). NOT in `compose/` — D-08 + sk2_1 verbatim lock the layout at repo root. The bind-mount in `compose.yaml` is `./prometheus.yml:/etc/prometheus/prometheus.yml:ro` (Compose evaluates `./` relative to the compose file's directory = repo root).

    File content (verbatim from sk2_1 with sk_p-adapted comment header):
    ```yaml
    # Phase 11 D-08 — Prometheus scrape config for the otel-collector's prometheus exporter.
    # Single static target — the collector exposes /metrics on :8889 (see compose/otel-collector-config.yaml
    # exporters.prometheus.endpoint per Phase 11 D-07). 15s scrape interval matches sk2_1 lock-in.
    #
    # Layout: this file lives at the sk_p repo root (matches sk2_1's flat layout per D-08); the
    # `prometheus` service in compose.yaml bind-mounts it read-only at /etc/prometheus/prometheus.yml.

    global:
      scrape_interval: 15s        # D-07 / D-08: matches default; explicit for clarity
      evaluation_interval: 15s    # Rule eval cadence (no rules defined — kept for default sanity)
      scrape_timeout: 10s         # Must be < scrape_interval per Prometheus validation

    scrape_configs:
      - job_name: 'otel-collector'
        # NO authentication — dev mode (parallels compose.yaml D-12/D-13 no-auth posture).
        # NO tls_config — dev mode (production hardening lives in a downstream service repo).
        static_configs:
          - targets:
              # Compose-internal hostname; resolves to sk-otel-collector container via the
              # default compose network. RESEARCH Pattern 5 — Prom container is INSIDE compose
              # so it uses service-DNS, not host-DNS. Test code outside compose uses
              # http://localhost:9090 (Prom HTTP API) instead.
              - 'otel-collector:8889'
        # Path defaults to /metrics — explicit for documentation clarity.
        metrics_path: '/metrics'
        # No metric_relabel_configs — we want raw OTel → Prometheus name translation.
        # The resource_to_telemetry_conversion: true setting lives on the collector's
        # prometheus exporter (Phase 11 D-07; compose/otel-collector-config.yaml). That is
        # what makes `service.name` become the `service_name` Prom label — non-negotiable
        # for Phase 11 D-17 round-trip test assertions.
    ```

    Use the Write tool (file does not exist yet — NEVER use bash `cat << 'EOF'` or heredoc for file creation per <critical_rules>).

    Do NOT touch `compose.yaml` (already mutated by Plan 11-02 Task 3) or `compose/otel-collector-config.yaml` (rewired by Plan 11-03).
  </action>
  <verify>
    <automated>test -f prometheus.yml exits 0 (file exists at repo root); grep "Phase 11 D-08" prometheus.yml returns 1; grep "Phase 11 D-07" prometheus.yml returns at least 1; grep -E "^global:$" prometheus.yml returns 1; grep "scrape_interval: 15s" prometheus.yml returns 1; grep "scrape_timeout: 10s" prometheus.yml returns 1; grep -E "^scrape_configs:$" prometheus.yml returns 1; grep "job_name: 'otel-collector'" prometheus.yml returns 1; grep "'otel-collector:8889'" prometheus.yml returns 1; grep "metrics_path: '/metrics'" prometheus.yml returns 1; ! grep -E "basic_auth|bearer_token|tls_config|metric_relabel_configs" prometheus.yml (no auth blocks, no relabel configs)</automated>
  </verify>
  <done>prometheus.yml exists at sk_p repo root with verbatim sk2_1 content + Phase 11 D-08 comment header; declares the single static scrape target `otel-collector:8889` on metrics_path `/metrics` with 15s/15s/10s timing.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-restart prometheus + confirm scrape succeeds</name>
  <files>prometheus.yml</files>
  <read_first>
    - prometheus.yml (just-created; do not re-read — file is in current context)
    - compose.yaml (verify bind-mount line `./prometheus.yml:/etc/prometheus/prometheus.yml:ro` exists in the prometheus service block — sanity check that Plan 11-02 Task 3 landed correctly)
  </read_first>
  <action>
    Restart the Prom container to load the new bind-mounted config and confirm it works:

    1. `docker compose -f compose.yaml restart prometheus`
       - Restarts only the Prom container; ES + collector remain up.
       - The container reads `/etc/prometheus/prometheus.yml` on each start (bind-mounted from sk_p repo root).

    2. After the restart completes (~5-10s), inspect the container logs:
       `docker logs --tail 60 sk-prometheus 2>&1`
       Look for:
       - `"Server is ready to receive web requests."` (final startup line; means config parsed + scrapes are active)
       - NO `error` lines containing `parsing YAML file` or `invalid configuration` (means config syntactically valid)
       - NO `error` lines about the scrape target — Prom will log connect errors if `otel-collector:8889` is unreachable. The collector is on the same compose network so this should resolve.

    3. Wait one scrape cycle (15s + small buffer), then query Prom's targets endpoint to confirm the scrape succeeded:
       `curl -fs http://localhost:9090/api/v1/targets | grep -o '"health":"[^"]*"' | head -3`
       Expected output: `"health":"up"` (the otel-collector target is healthy from Prom's perspective). If `"health":"down"`, run `curl -fs http://localhost:9090/api/v1/targets` (full body) to see the `lastError` field — likely a transient compose-network resolution issue; wait another 15s and retry.

    4. Issue a sample metrics query to confirm end-to-end ingestion path. Since no application traffic has driven the SDK yet (Program.cs strip + tests come in Plans 11-05 + 11-06+), the SDK has NOT exported any application metrics. But the collector itself emits self-metrics via the Prom exporter (otelcol-internal series). Query for one:
       `curl -fs "http://localhost:9090/api/v1/query?query=up{job=\"otel-collector\"}" | grep -o '"value":\[[^]]*\]'`
       Expected: a single result vector with `"value":[<unix_ts>,"1"]` — confirms Prom successfully scraped the collector.

    If any step fails: examine the error, fix the prometheus.yml content if it is malformed, OR check that compose.yaml's bind-mount line is correct (`./prometheus.yml:/etc/prometheus/prometheus.yml:ro`), OR confirm `otel-collector` is reachable on `:8889` from inside the compose network (Plan 11-03 Task 4 verified this).
  </action>
  <verify>
    <automated>docker compose -f compose.yaml restart prometheus exits 0; docker logs --tail 60 sk-prometheus 2>&1 | grep "Server is ready to receive web requests" returns 1; ! docker logs --tail 60 sk-prometheus 2>&1 | grep -iE "error.*parsing YAML|invalid configuration" (negation — no parse errors); curl -fs http://localhost:9090/api/v1/targets exits 0; curl -fs "http://localhost:9090/api/v1/query?query=up{job=\"otel-collector\"}" exits 0 with a non-empty result vector</automated>
  </verify>
  <done>Prom container restarts cleanly; logs show "Server is ready"; targets endpoint shows `"health":"up"` for the otel-collector target; sample query against `up{job="otel-collector"}` returns a non-empty result vector confirming the scrape succeeded.</done>
</task>

<task type="auto">
  <name>Task 3: Commit prometheus.yml as commit #4 of the Phase 11 sequence</name>
  <files>prometheus.yml</files>
  <read_first>
    - prometheus.yml (just-created; use `git status` to confirm scope is only this single new file)
  </read_first>
  <action>
    Stage ONLY `prometheus.yml` (it's a NEW file; use `git add prometheus.yml` explicitly — do NOT use `git add .` or `git add -A` which could accidentally include unrelated untracked files). Create commit #4 with the exact message:

    ```
    feat(prometheus): add scrape config for otel-collector:8889 (verbatim from sk2_1)
    ```

    Use a HEREDOC for the commit message body. Verify `git status --porcelain` returns empty after the commit. Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --format=%s returns "feat(prometheus): add scrape config for otel-collector:8889 (verbatim from sk2_1)"; git show --stat HEAD lists exactly 1 file added (prometheus.yml); git status --porcelain returns empty</automated>
  </verify>
  <done>Single prometheus.yml commit landed; working tree clean. Phase 11 backend stack now fully wired end-to-end: SDK emits OTLP → collector ships logs to ES + metrics to Prom-exporter → Prom scrapes the collector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prom container → collector :8889 | Plain-HTTP scrape over compose-internal network. No auth, no TLS. |
| Host → Prom :9090 | Prom HTTP API exposed to host on `0.0.0.0:9090` by Compose default. No auth, no TLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-T1 (Prom config injection — malicious scrape target) | T (Tampering) | prometheus.yml scrape_configs | accept | File is committed under git history; same posture as compose.yaml. No external input is templated into the file. Single literal target `otel-collector:8889`. Future hardening: pin sha256 digests on image references (deferred). **Verify:** Task 1 verify gate confirms exactly one job_name and one static target. |
| T-11-04-T2 (Prom /-/reload abuse via web.enable-lifecycle) | T (Tampering) | prometheus service command (lives in compose.yaml, not this file) | accept | Dev posture per D-13 verbatim; out-of-scope row in REQUIREMENTS.md (added by Plan 11-01 INFRA-08). Reload trigger requires HTTP POST access to host port 9090 — same exposure as the existing scrape-data /api/v1/query endpoint. **Verify:** N/A this plan; addressed by Plan 11-01 INFRA-08 + Plan 11-02 prometheus service. |
| T-11-04-T3 (high-cardinality label explosion from raw OTel→Prom translation) | A (Availability — DoS against Prom container) | prometheus.yml (absence of metric_relabel_configs) | accept | RESEARCH Out of Scope row "Prometheus high-cardinality protection" — v1 traffic volume is manageable per sk2_1 lock-in; future relabel-configs hardening is deferred. The `http_route` label is bounded by the route table (~12 entry routes across the 5 entities + Orchestration + tests); `service_name` is bounded by the single sk-api service. No unbounded user-input → label paths. **Verify:** `grep metric_relabel_configs prometheus.yml` returns 0 (intentional absence documented in the file's inline comment). |
</threat_model>

<verification>
- `test -f prometheus.yml` exits 0 (file exists).
- `grep "Phase 11 D-08" prometheus.yml` returns 1.
- `grep -c "Phase 11 D-07" prometheus.yml` returns at least 1.
- `grep "'otel-collector:8889'" prometheus.yml` returns 1.
- `grep "metrics_path: '/metrics'" prometheus.yml` returns 1.
- `! grep -E "basic_auth|bearer_token|tls_config|metric_relabel_configs" prometheus.yml` (no auth, no relabels).
- `docker compose -f compose.yaml restart prometheus` exits 0.
- `docker logs --tail 60 sk-prometheus 2>&1 | grep "Server is ready to receive web requests"` returns 1.
- `curl -fs "http://localhost:9090/api/v1/query?query=up{job=\"otel-collector\"}"` returns 0 with a non-empty result vector (`"value":[<ts>,"1"]`).
- `git log -1 --format=%s` matches `feat(prometheus): add scrape config for otel-collector:8889 (verbatim from sk2_1)`.
- `git show --stat HEAD` shows exactly 1 file added (prometheus.yml).
- `git status --porcelain` returns empty after the commit.
</verification>

<success_criteria>
1. `prometheus.yml` exists at the sk_p repo root (NOT in `compose/`).
2. Body is verbatim from sk2_1 with the comment header rewritten to reference Phase 11 D-07 + D-08.
3. Single scrape_configs job `otel-collector` with one static target `otel-collector:8889` on metrics_path `/metrics`.
4. No authentication / TLS / metric_relabel_configs blocks (dev posture per D-08).
5. After Prom container restart, logs show "Server is ready to receive web requests" with no config parse errors.
6. After one scrape cycle (~15s), `curl http://localhost:9090/api/v1/targets` shows the otel-collector job with `"health":"up"`.
7. Sample query `up{job="otel-collector"}` returns a result vector confirming Prom successfully scraped the collector.
8. Single git commit `feat(prometheus): add scrape config for otel-collector:8889 (verbatim from sk2_1)` exists at HEAD; modifies exactly 1 new file (prometheus.yml); working tree clean post-commit.
</success_criteria>

<output>
After completion, create `.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-04-SUMMARY.md`.
</output>
</content>
</invoke>
