---
phase: 11-migrate-prometheus-and-elastic-containers-from-compose-stack
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - compose.yaml
autonomous: false
requirements:
  - INFRA-06
  - INFRA-08
must_haves:
  truths:
    - "`compose.yaml` declares 4 backend services: postgres (existing, byte-identical), elasticsearch (NEW), otel-collector (mutated — image bump + port 8889 + bind-mount/user override removed), prometheus (NEW). baseapi-service `depends_on` extends to all 4 healthchecked services."
    - "`docker compose -f compose.yaml config --quiet` exits 0 (YAML valid; resolved service graph parses)"
    - "`docker compose up -d --wait` brings the stack to all-services-healthy within 120 seconds wall-clock on a warm developer host"
    - "`curl -fs http://localhost:9200/_cluster/health?wait_for_status=yellow&timeout=5s` returns 200 after compose-up (ES reachable from host on port 9200)"
    - "`curl -fs http://localhost:9090/-/healthy` returns 200 after compose-up (Prom reachable from host on port 9090)"
    - "`curl -fs http://localhost:8889/metrics` returns 200 after compose-up + at least one OTLP push (collector Prom-exporter endpoint exposed to host)"
  artifacts:
    - path: "compose.yaml"
      provides: "Phase 11 compose stack — adds elasticsearch + prometheus; mutates otel-collector image + ports + bind-mounts; extends baseapi-service depends_on chain"
      contains: "docker.elastic.co/elasticsearch/elasticsearch:8.15.5"
      contains_2: "prom/prometheus:v3.11.3"
      contains_3: "otel/opentelemetry-collector-contrib:0.152.0"
  key_links:
    - from: "compose.yaml elasticsearch service"
      to: "compose.yaml baseapi-service.depends_on"
      via: "elasticsearch healthcheck (curl /_cluster/health) gating baseapi-service startup per D-15"
      pattern: "elasticsearch:\\s+condition: service_healthy"
    - from: "compose.yaml prometheus service"
      to: "compose.yaml otel-collector service"
      via: "prometheus.depends_on otel-collector: service_started (Prom waits for collector before first scrape) per D-13"
      pattern: "otel-collector:\\s+condition: service_started"
    - from: "compose.yaml prometheus service volumes"
      to: "prometheus.yml (NEW file at repo root — created by Plan 11-04)"
      via: "bind-mount ./prometheus.yml:/etc/prometheus/prometheus.yml:ro"
      pattern: "./prometheus.yml:/etc/prometheus/prometheus.yml:ro"
---

<objective>
Mutate `compose.yaml` at the repo root to land the Phase 11 stack shape per CONTEXT D-09..D-15:

1. **Add `elasticsearch` service** (after `postgres`, before `otel-collector`): image `docker.elastic.co/elasticsearch/elasticsearch:8.15.5`, dev posture (single-node, no auth, no TLS, no volume, 512m heap), port `9200:9200`, healthcheck `curl -fs '/_cluster/health?wait_for_status=yellow&timeout=5s'` with `start_period: 60s`.

2. **Mutate `otel-collector` service**: bump image from `otel/opentelemetry-collector-contrib:0.95.0` → `0.152.0` (D-09); add port `8889:8889` to the existing ports list (D-14); REMOVE the `./tests/.otel-out:/var/otel-out` volume bind-mount AND the `user: "0:0"` override (D-14 — file exporter is gone after Plan 11-03's collector config rewire, so the Windows-bind-mount-permission-tweak is no longer necessary). Preserve the existing `./compose/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro` bind-mount byte-identical. Preserve the existing "no compose-level healthcheck on distroless image" comment block (RESEARCH Pitfall 3 — image 0.152.0 is still distroless).

3. **Add `prometheus` service** (after `otel-collector`, before `baseapi-service`): image `prom/prometheus:v3.11.3`, command `["--config.file=/etc/prometheus/prometheus.yml", "--web.enable-lifecycle"]`, bind-mount `./prometheus.yml:/etc/prometheus/prometheus.yml:ro`, port `9090:9090`, healthcheck `wget --no-verbose --tries=1 --spider http://localhost:9090/-/healthy`, `depends_on: otel-collector: service_started` (Prom waits for the collector to be listening on :8889 before its first scrape — D-13).

4. **Extend `baseapi-service.depends_on`**: add `elasticsearch: service_healthy` + `prometheus: service_healthy` entries alongside the existing `postgres: service_healthy` + `otel-collector: service_started` entries (D-15). Preserve indentation style verbatim.

Purpose: stand up the two new backends so subsequent plans (collector config rewire, code strip, helper tests) have a real target. Sequencing matters — the collector config still uses the file exporter at this point, so the stack remains green even after this commit lands. Plan 11-03 (rewire) is the next commit that switches the pipelines.

Output: A single atomic commit `feat(compose): add elasticsearch + prometheus services; bump otel-collector to 0.152.0; extend baseapi-service depends_on chain` modifying only `compose.yaml`. After commit lands, `docker compose -f compose.yaml config --quiet` exits 0 and `docker compose up -d --wait` brings all 5 services to healthy/started within 120s.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-RESEARCH.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-PATTERNS.md
@compose.yaml
@C:/Users/UserL/source/repos/sk2_1/docker-compose.yml

<interfaces>
<!-- Current compose.yaml shape (verified by Read) -->

Existing postgres service (lines 6-22, KEEP byte-identical except for surrounding whitespace if needed):
```yaml
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
```

Existing otel-collector service (lines 24-57, MUTATE per D-09/D-14):
```yaml
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.95.0           # <-- BUMP to 0.152.0
    container_name: sk-otel-collector
    restart: unless-stopped
    command: ["--config=/etc/otel-collector-config.yaml"]
    user: "0:0"                                                  # <-- DELETE this line
    volumes:
      - ./compose/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro    # <-- KEEP
      - ./tests/.otel-out:/var/otel-out                          # <-- DELETE this line
    ports:
      - "4317:4317"
      - "4318:4318"
      - "13133:13133"
      # <-- ADD "8889:8889" here per D-14
    # NOTE block lines 47-57 KEEP — distroless caveat still applies to 0.152.0 image
```

Existing baseapi-service.depends_on (lines 76-80, EXTEND per D-15):
```yaml
    depends_on:
      postgres:
        condition: service_healthy
      otel-collector:
        condition: service_started
      # <-- ADD elasticsearch + prometheus blocks here per D-15
```

Existing volumes block (lines 82-83, KEEP byte-identical):
```yaml
volumes:
  pgdata:
```

sk2_1 verbatim elasticsearch + prometheus blocks (verbatim source per D-12/D-13):
```yaml
  # sk2_1 docker-compose.yml lines 41-72 (elasticsearch)
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.5
    container_name: sk-elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - xpack.security.enrollment.enabled=false
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    ports:
      - "9200:9200"
    healthcheck:
      test: ["CMD-SHELL", "curl -fs 'http://localhost:9200/_cluster/health?wait_for_status=yellow&timeout=5s' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s

  # sk2_1 docker-compose.yml lines 77-100 (prometheus)
  prometheus:
    image: prom/prometheus:v3.11.3
    container_name: sk-prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--web.enable-lifecycle"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      otel-collector:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
```

Ordering convention (CONTEXT specifics — Claude's Discretion): preserve depends-on order. Final ordering: `postgres → elasticsearch → otel-collector → prometheus → baseapi-service` (ES before collector because collector exports to ES; Prom after collector because Prom depends on collector :8889 being live to scrape).

Container-name collision caveat (RESEARCH Pitfall 4): sk_p reuses sk2_1's container_name literals (`sk-elasticsearch`, `sk-prometheus`, `sk-otel-collector`). sk_p + sk2_1 stacks are mutually exclusive on a single Docker daemon — developer must `docker compose -f /path/to/sk2_1/docker-compose.yml down` before bringing sk_p up.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add elasticsearch service block between postgres and otel-collector</name>
  <files>compose.yaml</files>
  <read_first>
    - compose.yaml (current shape; verify postgres block ends at line 22 and otel-collector block starts at line 24 with a blank line at line 23)
    - C:/Users/UserL/source/repos/sk2_1/docker-compose.yml (lines 41-72 verbatim elasticsearch source)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-10 + D-12 verbatim)
  </read_first>
  <action>
    Insert a new `elasticsearch:` service block in `compose.yaml` immediately AFTER the closing `start_period: 5s` line of the `postgres` service (line 22) and BEFORE the `otel-collector:` service header (line 24). Preserve the existing blank line between services as the separator.

    Insert exactly this block (2-space indented under `services:` — matches existing postgres/otel-collector indentation):
    ```yaml
      # Phase 11 D-10 + D-12 — verbatim sk2_1 dev posture: single-node, no auth, no TLS, no volume,
      # 512m heap, port 9200:9200 only (9300 cluster transport intentionally not exposed).
      # container_name reuses sk2_1's `sk-elasticsearch` literal — sk_p + sk2_1 stacks are
      # mutually exclusive on a single Docker daemon (RESEARCH Pitfall 4).
      elasticsearch:
        image: docker.elastic.co/elasticsearch/elasticsearch:8.15.5
        container_name: sk-elasticsearch
        environment:
          - discovery.type=single-node
          - xpack.security.enabled=false
          - xpack.security.enrollment.enabled=false
          - ES_JAVA_OPTS=-Xms512m -Xmx512m
        ports:
          - "9200:9200"
        healthcheck:
          # Single-quote the URL so the shell does not parse `&` as a background operator
          # (would silently background curl and skip `|| exit 1`, making the healthcheck always green).
          # wait_for_status=yellow — single-node ES never reaches green (replicas can never be assigned).
          # RESEARCH Pitfall 6 — start_period: 60s essential; ES 8.x cold-start ~30s on warm dev machine,
          # default start_period: 0s exhausts the retries budget before ES is ready.
          test: ["CMD-SHELL", "curl -fs 'http://localhost:9200/_cluster/health?wait_for_status=yellow&timeout=5s' || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 60s
    ```

    The body is verbatim from sk2_1 (lines 41-72) with the comment block adapted to reference Phase 11 D-10/D-12 + RESEARCH Pitfalls 4 + 6 verbatim.

    Do NOT touch any other line in `compose.yaml` for this task.
  </action>
  <verify>
    <automated>grep -E "^  elasticsearch:$" compose.yaml returns 1; grep "docker.elastic.co/elasticsearch/elasticsearch:8.15.5" compose.yaml returns 1; grep "container_name: sk-elasticsearch" compose.yaml returns 1; grep "discovery.type=single-node" compose.yaml returns 1; grep "xpack.security.enabled=false" compose.yaml returns 1; grep "xpack.security.enrollment.enabled=false" compose.yaml returns 1; grep "ES_JAVA_OPTS=-Xms512m -Xmx512m" compose.yaml returns 1; grep "9200:9200" compose.yaml returns 1; grep "wait_for_status=yellow" compose.yaml returns 1; grep "start_period: 60s" compose.yaml returns 1; docker compose -f compose.yaml config --quiet exits 0 (YAML still valid after insertion)</automated>
  </verify>
  <done>elasticsearch service block lands verbatim from sk2_1 with the Phase 11 D-10/D-12 + Pitfalls 4/6 comment block; `docker compose config --quiet` still exits 0; postgres and otel-collector blocks unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mutate otel-collector service per D-09 + D-14</name>
  <files>compose.yaml</files>
  <read_first>
    - compose.yaml (current otel-collector block lines 24-57 — verify exact current state before mutation)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-09 + D-14 verbatim)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-RESEARCH.md (Pitfall 3 — distroless image stance preserved for 0.152.0)
  </read_first>
  <action>
    Three surgical edits on the existing `otel-collector:` service block in `compose.yaml`.

    Edit 1 — image bump (current line 28). Replace `image: otel/opentelemetry-collector-contrib:0.95.0` with `image: otel/opentelemetry-collector-contrib:0.152.0`. Also update the inline comment immediately above (lines 25-27): rewrite the educational comment to read:
    ```yaml
        # Phase 11 D-09 — bumped 0.95.0 -> 0.152.0 (gives us the native elasticsearch + prometheus
        # exporters with current `mapping.mode` semantics; 0.95.0 predates `mapping.mode` field).
        # Receives OTLP from BaseApi.Service (gRPC :4317 / HTTP :4318), exposes Prom-scrape
        # endpoint on :8889 (Phase 11 D-14), exposes health_check extension on :13133 (carried
        # forward — host-side `curl localhost:13133/` smoke probe; distroless image still has
        # no in-container probe per RESEARCH Pitfall 3).
    ```

    Edit 2 — remove `user: "0:0"` override AND the educational comment block above it (lines 32-39 in the current file — the block from `# Override the default non-root user...` through `# ship a separate image with a dedicated otel user matching the host).` and the `user: "0:0"` line itself).

    Result: the volumes block (currently lines 40-42) should follow directly from the `command:` line (line 31) with no `user:` override and no preceding comment block about file-exporter UID workarounds. Both the comment block AND the `user:` line are removed because the file exporter is being dropped in Plan 11-03 and the entire UID-mismatch workaround is obsolete.

    Edit 3 — within the volumes block, remove the `- ./tests/.otel-out:/var/otel-out` line entirely. Preserve `- ./compose/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro` byte-identical.

    Edit 4 — within the ports list, ADD `- "8889:8889"   # Phase 11 D-14 — Prom scrape endpoint exposed to host` immediately after the existing `- "4318:4318"` line and BEFORE the `- "13133:13133"` line. Result final ports order: `4317:4317, 4318:4318, 8889:8889, 13133:13133`.

    Preserve the existing "NOTE (fix-forward of Plan 05-01 deviation, Rule 1 — distroless image has no wget)" comment block (currently lines 47-57). The 0.152.0 image is STILL distroless — the comment remains educational and load-bearing. Optionally update the opening line to read `NOTE (Phase 5 Plan 05-01 deviation + Phase 11 0.152.0 verification) — distroless image still has no wget` to make the carry-forward explicit, but the body should remain byte-identical.
  </action>
  <verify>
    <automated>grep "otel/opentelemetry-collector-contrib:0.152.0" compose.yaml returns 1; ! grep "otel/opentelemetry-collector-contrib:0.95.0" compose.yaml (negation — old image gone); ! grep -E "^    user: \"0:0\"$" compose.yaml (negation — UID override removed); ! grep "./tests/.otel-out:/var/otel-out" compose.yaml (negation — file exporter bind-mount removed); grep "./compose/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro" compose.yaml returns 1 (collector config bind-mount preserved); grep -c "8889:8889" compose.yaml returns 1; grep -c "4317:4317" compose.yaml returns 1; grep -c "4318:4318" compose.yaml returns 1; grep -c "13133:13133" compose.yaml returns 1; grep "Phase 11 D-09" compose.yaml returns 1; grep "distroless" compose.yaml returns at least 1 (RESEARCH Pitfall 3 stance preserved); docker compose -f compose.yaml config --quiet exits 0</automated>
  </verify>
  <done>otel-collector service: image bumped to 0.152.0; port 8889 added; tests/.otel-out bind-mount + user:0:0 override removed; collector-config bind-mount preserved; distroless comment block preserved; `docker compose config` still validates.</done>
</task>

<task type="auto">
  <name>Task 3: Add prometheus service block between otel-collector and baseapi-service</name>
  <files>compose.yaml</files>
  <read_first>
    - compose.yaml (otel-collector block now mutated by Task 2; baseapi-service block starts at the old line 59 — find current line number via Read after Tasks 1+2)
    - C:/Users/UserL/source/repos/sk2_1/docker-compose.yml (lines 77-100 verbatim prometheus source)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-11 + D-13 verbatim)
  </read_first>
  <action>
    Insert a new `prometheus:` service block in `compose.yaml` immediately AFTER the closing line of the (mutated) `otel-collector` service block (the closing line is now the last comment line of the distroless-image-NOTE block) and BEFORE the `baseapi-service:` service header. Preserve a blank line as the separator between services.

    Insert exactly this block (2-space indented under `services:`):
    ```yaml
      # Phase 11 D-11 + D-13 — verbatim sk2_1 dev posture. Scrapes otel-collector:8889 every 15s
      # (configured in prometheus.yml at repo root; bind-mounted read-only). No persistence volume
      # (ephemeral dev), no remote_write, no alerting/recording rules (deferred Out of Scope).
      # container_name reuses sk2_1's `sk-prometheus` literal — mutually exclusive with sibling
      # sk2_1 stack on a single Docker daemon (RESEARCH Pitfall 4).
      prometheus:
        image: prom/prometheus:v3.11.3
        container_name: sk-prometheus
        command:
          - "--config.file=/etc/prometheus/prometheus.yml"
          # --web.enable-lifecycle exposes POST /-/reload for runtime config reload (dev convenience).
          - "--web.enable-lifecycle"
        volumes:
          # prometheus.yml lives at the sk_p repo root (NEW file in Plan 11-04).
          - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
        ports:
          - "9090:9090"   # Prometheus HTTP API — host-side `/api/v1/query` reachable from tests.
        depends_on:
          otel-collector:
            # service_started is sufficient: otel-collector 0.152.0 starts listening on :8889
            # immediately; service_healthy would require a healthcheck on the distroless image
            # (currently absent per RESEARCH Pitfall 3).
            condition: service_started
        healthcheck:
          # /-/healthy is the Prometheus liveness endpoint (200 == process running + config loaded).
          test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
          interval: 10s
          timeout: 5s
          retries: 3
          start_period: 10s
    ```

    The body is verbatim from sk2_1 (lines 77-100) with the comment block adapted to reference Phase 11 D-11/D-13 + RESEARCH Pitfalls 3/4.

    Do NOT touch any other line in `compose.yaml` for this task.
  </action>
  <verify>
    <automated>grep -E "^  prometheus:$" compose.yaml returns 1; grep "prom/prometheus:v3.11.3" compose.yaml returns 1; grep "container_name: sk-prometheus" compose.yaml returns 1; grep "./prometheus.yml:/etc/prometheus/prometheus.yml:ro" compose.yaml returns 1; grep "9090:9090" compose.yaml returns 1; grep "/-/healthy" compose.yaml returns 1; grep -c "condition: service_started" compose.yaml returns at least 1 (the new prometheus.depends_on entry; baseapi-service.depends_on also has one — Task 4 doesn't touch that); grep "--web.enable-lifecycle" compose.yaml returns 1; docker compose -f compose.yaml config --quiet exits 0</automated>
  </verify>
  <done>prometheus service block lands verbatim from sk2_1 with the Phase 11 D-11/D-13 + Pitfalls 3/4 comment block; bind-mounts the (NEW) `./prometheus.yml`; healthcheck declared; depends_on otel-collector: service_started; `docker compose config` still validates.</done>
</task>

<task type="auto">
  <name>Task 4: Extend baseapi-service.depends_on with elasticsearch + prometheus entries</name>
  <files>compose.yaml</files>
  <read_first>
    - compose.yaml (current baseapi-service.depends_on block — line numbers shifted by Tasks 1-3; find via Read)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-15 verbatim)
  </read_first>
  <action>
    Find the existing `baseapi-service.depends_on:` block in `compose.yaml` (the 4-line block whose body is `postgres:`/`condition: service_healthy` + `otel-collector:`/`condition: service_started`). Append two more entries to the end of this block, immediately after the `condition: service_started` line for otel-collector:

    ```yaml
          elasticsearch:
            condition: service_healthy
          prometheus:
            condition: service_healthy
    ```

    Use 6-space indentation for the service-name lines (`elasticsearch:`/`prometheus:`) and 8-space indentation for the `condition:` lines — matching the existing 6/8-space pattern of `postgres:`/`otel-collector:` entries.

    Final `baseapi-service.depends_on` should have 4 entries in this order: `postgres → otel-collector → elasticsearch → prometheus`. The order is depends-on-order (the natural reading order of "what must be alive before me"); D-15 does not lock alphabetical ordering.

    Preserve the surrounding `baseapi-service` body byte-identical (image build, environment, ports, healthcheck — all unchanged this phase).
  </action>
  <verify>
    <automated>grep -A 10 -E "^    depends_on:$" compose.yaml | grep -c "condition: service_healthy" returns at least 3 (postgres + elasticsearch + prometheus); grep -A 10 -E "^    depends_on:$" compose.yaml | grep -c "condition: service_started" returns at least 1 (otel-collector); grep -A 10 -E "^    depends_on:$" compose.yaml | grep -E "^      (postgres|otel-collector|elasticsearch|prometheus):$" | wc -l returns 4; docker compose -f compose.yaml config --quiet exits 0; docker compose -f compose.yaml config | grep -A 12 "baseapi-service:" | grep -c "service_healthy" returns at least 3 (sanity check via fully-resolved compose graph — confirms the new depends_on entries parse correctly)</automated>
  </verify>
  <done>baseapi-service.depends_on has 4 entries in postgres → otel-collector → elasticsearch → prometheus order; ES + Prom both gated by service_healthy; indentation matches existing pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Wave-0 host-port + container-name collision check + compose-up smoke (manual)</name>
  <what-built>compose.yaml now declares 4 backend services with the Phase 11 stack shape. Before the developer runs `docker compose up -d --wait` against the new file, RESEARCH Pitfall 4 demands a one-time mutual-exclusivity check against the sibling sk2_1 stack which uses identical container_name literals and host ports.</what-built>
  <how-to-verify>
    Run these commands from the sk_p repo root in the order shown and confirm each step succeeds:

    1. **Pre-flight collision check** (host ports + container names):
       ```powershell
       docker ps --filter "name=sk-elasticsearch" --format "{{.Names}}"
       docker ps --filter "name=sk-prometheus" --format "{{.Names}}"
       docker ps --filter "name=sk-otel-collector" --format "{{.Names}}"
       docker ps --filter "publish=9200" --format "{{.Names}}:{{.Image}}"
       docker ps --filter "publish=9090" --format "{{.Names}}:{{.Image}}"
       docker ps --filter "publish=8889" --format "{{.Names}}:{{.Image}}"
       ```
       If ANY output is non-empty AND the listed container belongs to the sibling sk2_1 stack, run `docker compose -f C:/Users/UserL/source/repos/sk2_1/docker-compose.yml down` to free the names/ports before proceeding. The sk_p stack will fail compose-up with a port-collision or name-collision error otherwise.

    2. **Compose-config validation** (offline; no daemon work):
       ```powershell
       docker compose -f compose.yaml config --quiet
       ```
       Must exit 0. Any non-zero exit means the YAML is malformed and the previous tasks need to be re-run before proceeding.

    3. **Compose-up with --wait** (brings stack to healthy; ~60-90s wall-clock):
       ```powershell
       docker compose -f compose.yaml up -d --wait --timeout 120
       ```
       Must exit 0 within 120s. Expected behaviour: postgres reaches healthy in ~10s, otel-collector reports started immediately, prometheus reaches healthy in ~15s, elasticsearch reaches healthy in ~60-90s on a warm dev machine. baseapi-service is NOT started by this command if Phase 8's `phase-8` profile gating has been removed (Plan 08-01 dropped the profile) — verify behaviour matches current `baseapi-service` posture.

    4. **Backend smoke probes** (verify each new service is reachable from the host):
       ```powershell
       curl -fs "http://localhost:9200/_cluster/health?wait_for_status=yellow&timeout=5s"
       curl -fs "http://localhost:9090/-/healthy"
       curl -fs "http://localhost:13133/"
       # 8889 is the collector's Prom-scrape endpoint; it exposes /metrics:
       curl -fs "http://localhost:8889/metrics" | head -5
       ```
       ES probe should return `{"cluster_name":"docker-cluster","status":"yellow",...}` (yellow is correct for single-node — never reaches green). Prom probe should return `Prometheus Server is Healthy.`. Collector :13133 should return `{"status":"Server available",...}` (or 200 with text body). Collector :8889 should return a Prometheus text-format body (collector self-metrics — `# HELP otelcol_*` series — even before any application telemetry arrives).

    5. **Confirm 5-service graph after the up** (no name collisions, all in same default network):
       ```powershell
       docker compose -f compose.yaml ps --format "{{.Service}}:{{.Status}}"
       ```
       Expected services: postgres, elasticsearch, otel-collector, prometheus. baseapi-service shows up only if the Dockerfile build is invoked (Phase 8 baseline). The first 4 must all show "Up" + "(healthy)" or "(started)".

    Approve if every step exits cleanly. If any step fails, note WHICH step in the resume-signal so the planner can adjust the prior tasks. Do NOT mutate any file as part of this task — this is verification only.
  </how-to-verify>
  <resume-signal>Type "approved — stack healthy in N seconds" with N filled in, OR describe which step failed and what the error was (e.g., "step 3 timed out at 120s — elasticsearch never reached healthy; docker logs sk-elasticsearch shows ...").</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Commit compose.yaml mutation as commit #2 of the Phase 11 sequence</name>
  <files>compose.yaml</files>
  <read_first>
    - compose.yaml (just-mutated; do not re-read — use `git diff` to confirm scope is ONLY this single file)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-09 through D-15)
  </read_first>
  <action>
    Stage ONLY `compose.yaml`. Create commit #2 with the exact message:

    ```
    feat(compose): add elasticsearch + prometheus services; bump otel-collector to 0.152.0; extend baseapi-service depends_on chain
    ```

    Use a HEREDOC for the commit message body. Verify `git status --porcelain` returns empty after the commit. Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --format=%s returns "feat(compose): add elasticsearch + prometheus services; bump otel-collector to 0.152.0; extend baseapi-service depends_on chain"; git show --stat HEAD lists exactly 1 file changed (compose.yaml); git status --porcelain returns empty</automated>
  </verify>
  <done>Single compose-only commit landed; working tree clean. Stack remains green (file exporter pipeline still wired in collector config — Plan 11-03 rewires next).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → compose-default-network | Host ports 9200 (ES), 9090 (Prom), 8889 (collector Prom-exporter), 13133 (collector health-check), 4317 (OTLP gRPC), 4318 (OTLP HTTP), 5433 (Postgres) all bind to 0.0.0.0 on the dev host by Docker Compose's default `host-published` semantics. On a multi-tenant dev box, any process on the host (or any container on the host network) can reach all backends without auth (dev posture per D-12/D-13). |
| compose-default-network internal | Service-to-service traffic between elasticsearch, prometheus, otel-collector, baseapi-service, postgres is over compose-internal DNS (`elasticsearch:9200`, `otel-collector:8889`, etc.) on plain HTTP/gRPC. No mutual TLS, no auth. |
| supply chain | 3 new image pulls + 1 image bump: `docker.elastic.co/elasticsearch/elasticsearch:8.15.5`, `prom/prometheus:v3.11.3`, `otel/opentelemetry-collector-contrib:0.152.0` (bumped from 0.95.0). All exact-version pins; no `:latest`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-T1 (image supply chain — typosquat / registry compromise) | T (Tampering) | compose.yaml image: lines | mitigate | Exact-version pins (D-09/D-10/D-11): `docker.elastic.co/elasticsearch/elasticsearch:8.15.5`, `prom/prometheus:v3.11.3`, `otel/opentelemetry-collector-contrib:0.152.0`. No `:latest`. Future hardening (deferred): pin to sha256 digest. **Verify:** `grep "image:" compose.yaml` should show ONLY these three new images + the existing `postgres:17-alpine` image; ZERO image references should match `:latest` or floating tags. |
| T-11-02-T2 (port exposure on multi-tenant dev host) | I (Information Disclosure) | compose.yaml ports: lines | accept | Dev posture per CONTEXT Out of Scope: production deployment owns auth + TLS in a separate repo. Documented as Out of Scope. **Verify:** the ports list literally enumerates `9200:9200`, `9090:9090`, `8889:8889`, `13133:13133` — DO NOT change to `0.0.0.0:9200:9200` form (Compose default already binds host-side; making the bind address explicit-everywhere is a separate hardening pass deferred to v2). |
| T-11-02-T3 (cross-stack data mix — sk_p logs landing in sk2_1 ES due to name collision) | T (Tampering) | container_name: sk-elasticsearch (and siblings) | mitigate | Task 5 manual checkpoint runs `docker ps --filter "name=sk-elasticsearch"` BEFORE compose-up to confirm no sibling instance is running. RESEARCH Pitfall 4 + Out of Scope documented. **Verify:** Task 5 checkpoint must be approved before Task 6 commit can proceed. |
| T-11-02-T4 (web.enable-lifecycle exposes Prom POST /-/reload to any host process) | T (Tampering) | prometheus service command | accept | Dev posture per D-13 verbatim from sk2_1; same posture as ES `xpack.security.enabled=false`. Out of Scope row in REQUIREMENTS.md captures the production-tightening requirement. **Verify:** the `--web.enable-lifecycle` flag is documented inline in the Task 3 comment block. |
| T-11-02-T5 (insufficient start_period causes false-negative compose-up) | D (Denial of Service against developer workflow) | elasticsearch healthcheck | mitigate | `start_period: 60s` per D-12 — preserves 110s effective window before retries-budget exhaustion (Pitfall 6). **Verify:** Task 1 verify gate includes `grep "start_period: 60s"`. |
</threat_model>

<verification>
- `grep -E "^  (postgres|elasticsearch|otel-collector|prometheus|baseapi-service):$" compose.yaml | wc -l` returns 5 (all 5 services declared; alphabetical-or-depends-on ordering does not matter — the count is what matters).
- `grep "docker.elastic.co/elasticsearch/elasticsearch:8.15.5" compose.yaml` returns 1.
- `grep "prom/prometheus:v3.11.3" compose.yaml` returns 1.
- `grep "otel/opentelemetry-collector-contrib:0.152.0" compose.yaml` returns 1.
- `! grep "otel/opentelemetry-collector-contrib:0.95.0" compose.yaml` (negation — old image gone).
- `! grep "./tests/.otel-out:/var/otel-out" compose.yaml` (negation — file exporter bind-mount removed).
- `! grep -E '^    user: "0:0"$' compose.yaml` (negation — UID override removed).
- `grep "./prometheus.yml:/etc/prometheus/prometheus.yml:ro" compose.yaml` returns 1.
- `grep "./compose/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro" compose.yaml` returns 1 (existing collector-config bind-mount preserved).
- `grep -E "(9200|9090|8889|13133):" compose.yaml | wc -l` returns at least 4 (host port mappings for ES, Prom, collector Prom-exporter, collector health-check).
- `grep -A 10 -E "^    depends_on:$" compose.yaml | grep -E "^      (postgres|otel-collector|elasticsearch|prometheus):$" | wc -l` returns 4 (baseapi-service depends on all four backends).
- `docker compose -f compose.yaml config --quiet` exits 0 (resolved graph parses).
- `docker compose -f compose.yaml up -d --wait --timeout 120` exits 0 (Task 5 checkpoint).
- `git log -1 --format=%s` matches `feat(compose): add elasticsearch + prometheus services; bump otel-collector to 0.152.0; extend baseapi-service depends_on chain`.
- `git show --stat HEAD` shows exactly 1 file changed.
</verification>

<success_criteria>
1. `compose.yaml` declares 5 services in postgres → elasticsearch → otel-collector → prometheus → baseapi-service order (or equivalent — the count is what's load-bearing).
2. New elasticsearch service: image `docker.elastic.co/elasticsearch/elasticsearch:8.15.5`, container_name `sk-elasticsearch`, 4 env vars per D-12, port 9200:9200, healthcheck with start_period 60s, no volume.
3. Mutated otel-collector service: image 0.152.0 (was 0.95.0), port 8889 added to existing ports list, tests/.otel-out bind-mount + user:0:0 override removed, collector-config bind-mount preserved, distroless-comment block preserved.
4. New prometheus service: image `prom/prometheus:v3.11.3`, container_name `sk-prometheus`, command flags + prometheus.yml bind-mount + port 9090:9090 + wget healthcheck + depends_on otel-collector: service_started.
5. baseapi-service.depends_on extends to 4 entries (postgres + otel-collector + elasticsearch + prometheus); ES + Prom both gated by service_healthy.
6. `docker compose -f compose.yaml config --quiet` exits 0 (YAML valid; resolved graph parses).
7. `docker compose -f compose.yaml up -d --wait --timeout 120` exits 0 within 120s on a warm dev host (manual checkpoint Task 5 approved by developer).
8. Backend smoke probes succeed: ES /_cluster/health returns yellow; Prom /-/healthy returns 200; collector :13133 returns 200; collector :8889/metrics returns Prom-text-format body.
9. Single git commit `feat(compose): ...` exists at HEAD; modifies exactly 1 file (compose.yaml); working tree clean post-commit.
</success_criteria>

<output>
After completion, create `.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-02-SUMMARY.md`.
</output>
</content>
</invoke>