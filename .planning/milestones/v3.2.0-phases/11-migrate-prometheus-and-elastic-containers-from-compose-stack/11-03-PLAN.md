---
phase: 11-migrate-prometheus-and-elastic-containers-from-compose-stack
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - compose/otel-collector-config.yaml
autonomous: true
requirements:
  - OBSERV-03
  - OBSERV-04
  - OBSERV-08
  - OBSERV-13
  - OBSERV-14
must_haves:
  truths:
    - "`compose/otel-collector-config.yaml` declares the new pipelines: logs → [elasticsearch] exporter; metrics → [filter/health_metrics] processor → [prometheus] exporter; NO traces pipeline anywhere in the file"
    - "exporters block contains exactly two exporters: `elasticsearch` (verbatim from sk2_1 — endpoints [http://elasticsearch:9200] + mapping.mode: none) and `prometheus` (verbatim from sk2_1 — endpoint 0.0.0.0:8889 + resource_to_telemetry_conversion.enabled: true + send_timestamps: true)"
    - "NO `file` exporter, NO `logging` exporter, NO `debug` exporter anywhere in the file"
    - "filter/health_metrics processor body preserved byte-identical (OTTL expression unchanged); only the pipeline that consumes it changes from feeding [file, logging] to feeding [prometheus]"
    - "extensions block declares health_check on 0.0.0.0:13133 (RESEARCH Open Q4 recommendation — kept for host-side `curl localhost:13133/` smoke probe)"
    - "service.extensions: [health_check] preserved"
  artifacts:
    - path: "compose/otel-collector-config.yaml"
      provides: "Phase 11 collector pipeline shape — logs to ES, metrics to Prom, no traces, no file exporter"
      contains: "endpoints:\\n      - http://elasticsearch:9200"
      contains_2: "endpoint: 0.0.0.0:8889"
      contains_3: "mapping:\\n      mode: none"
  key_links:
    - from: "compose/otel-collector-config.yaml service.pipelines.logs.exporters"
      to: "compose/otel-collector-config.yaml exporters.elasticsearch"
      via: "logs pipeline routes OTLP receivers to the elasticsearch exporter ONLY (single-sink discipline per D-01)"
      pattern: "exporters: \\[elasticsearch\\]"
    - from: "compose/otel-collector-config.yaml service.pipelines.metrics.exporters"
      to: "compose/otel-collector-config.yaml exporters.prometheus"
      via: "metrics pipeline routes OTLP receivers through filter/health_metrics processor to the prometheus exporter ONLY (single-sink discipline per D-02; D-04 preserves the filter)"
      pattern: "exporters: \\[prometheus\\]"
    - from: "compose/otel-collector-config.yaml processors.filter/health_metrics"
      to: "compose/otel-collector-config.yaml service.pipelines.metrics.processors"
      via: "OBSERV-08 / HEALTH-05 — /health/* metric data points dropped before reaching Prom exporter (Phase 5 Plan 05-02 fix-forward preserved + re-pointed per D-04)"
      pattern: "processors: \\[filter/health_metrics\\]"
---

<objective>
Rewire `compose/otel-collector-config.yaml` per CONTEXT D-01..D-07 to ship logs to Elasticsearch and metrics to Prometheus (no file fan-out; no traces pipeline; no debug/logging exporters). Edit in place (Claude's Discretion option from CONTEXT) to preserve the Phase 5 fix-forward narrative of the `filter/health_metrics` processor.

Mutations:
1. **Receivers** — KEEP byte-identical (OTLP gRPC on 4317 + HTTP on 4318).
2. **Processors** — KEEP `filter/health_metrics` body byte-identical (OTTL expression preserves the OBSERV-08 invariant per D-04). Optionally compress its 24-line educational comment block by 2-3 lines IF the rewrite of the surrounding file context makes the comment redundant; do NOT remove the rationale entirely.
3. **Exporters** — REPLACE the current `file` + `logging` block with the new `elasticsearch` + `prometheus` block (per D-06 + D-07; verbatim from sk2_1).
4. **Extensions** — KEEP `health_check` on `0.0.0.0:13133` (RESEARCH Open Q4 — host-side smoke probe).
5. **Service.pipelines** — rewrite:
   - `logs:` receivers [otlp] → exporters [elasticsearch] (single-sink per D-01)
   - `metrics:` receivers [otlp] → processors [filter/health_metrics] → exporters [prometheus] (single-sink per D-02; D-04 filter preserved)
   - **DELETE the entire `traces:` pipeline block** (per D-03)
6. **Top-of-file comment block** — rewrite to describe the new shape (logs → ES, metrics → Prom, no traces) and reference Phase 11 D-01..D-07 + Phase 5 Plan 05-02 filter/health_metrics carry-forward.

Purpose: this is the load-bearing wiring change. Once committed, the live `otel-collector` container (already bumped to 0.152.0 by Plan 11-02) reads the new config on next compose-up and starts shipping logs to ES + metrics to Prom. No SDK changes required — Program.cs still emits OTLP unchanged (the SDK-side `.WithTracing()` strip lives in Plan 11-05).

Output: A single atomic commit `feat(otel-collector): rewire pipelines — logs to elasticsearch, metrics to prometheus, drop traces + file exporter` modifying only `compose/otel-collector-config.yaml`. After commit lands + `docker compose restart otel-collector`, the collector logs (`docker logs sk-otel-collector`) show "Started" with no parse errors and the prometheus + elasticsearch exporters initialize cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-RESEARCH.md
@.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-PATTERNS.md
@compose/otel-collector-config.yaml
@C:/Users/UserL/source/repos/sk2_1/otel-collector-config.yaml

<interfaces>
<!-- Current compose/otel-collector-config.yaml shape (verified by Read — full 69 lines) -->

Top-of-file 8-line comment block (current lines 1-8):
```yaml
# OTel Collector Contrib config — sourced verbatim from CONTEXT.md D-10, then
# extended in Plan 05-02 with a filterprocessor entry to close the SC#4 metrics-half
# gap that Plan 05-01 deferred (Rule 1 deviation — see 05-01-SUMMARY Deviation #2).
#
# Receivers accept OTLP gRPC (4317) + HTTP (4318); exporters write to a
# host-mounted JSON-lines file (read by tests/BaseApi.Tests/Observability/*Tests.cs)
# AND stream to container stdout (live tailing via `docker logs sk-otel-collector`).
# health_check extension on :13133 backs the compose-level service healthcheck.
```

Receivers block (current lines 10-16, KEEP byte-identical):
```yaml
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
```

Processors block (current lines 18-42, KEEP body + OTTL byte-identical; preserve the educational rationale block since it is load-bearing context — Plan 05-02 fix-forward):
```yaml
processors:
  filter/health_metrics:
    error_mode: ignore
    metrics:
      datapoint:
        - 'metric.name == "http.server.request.duration" and IsMatch(attributes["http.route"], "^/health/.*")'
```

Exporters block (current lines 44-51, REPLACE entirely):
```yaml
exporters:
  file:                                # <-- DELETE entire block
    path: /var/otel-out/telemetry.jsonl
    rotation:
      max_megabytes: 10
      max_days: 1
  logging:                             # <-- DELETE entire block
    verbosity: detailed
```

Extensions block (current lines 53-55, KEEP byte-identical):
```yaml
extensions:
  health_check:
    endpoint: 0.0.0.0:13133
```

Service block (current lines 57-69, REWRITE pipelines):
```yaml
service:
  extensions: [health_check]
  pipelines:
    logs:
      receivers: [otlp]
      exporters: [file, logging]                          # <-- D-01: change to [elasticsearch]
    metrics:
      receivers: [otlp]
      processors: [filter/health_metrics]
      exporters: [file, logging]                          # <-- D-02: change to [prometheus]
    traces:                                               # <-- D-03: DELETE entire 3-line block
      receivers: [otlp]
      exporters: [file, logging]
```

sk2_1 verbatim exporter block (source for the REPLACE — sk2_1's otel-collector-config.yaml lines 28-62):
```yaml
exporters:
  # Phase 12 CONTEXT.md D-02: exposes /metrics on :8889 for Prometheus to scrape.
  # OTel→Prometheus naming: dot-form → underscore-form + `_total` suffix on monotonic Sum;
  # histograms become `_count`/`_sum`/`_bucket` triplets with `_seconds` suffix when unit="s".
  prometheus:
    endpoint: 0.0.0.0:8889
    # CRITICAL: without this, resource attributes (service.name, service.version) do NOT
    # become Prometheus labels. Tests assert {service_name="sk-api"} — non-negotiable.
    resource_to_telemetry_conversion:
      enabled: true
    # Emit sample timestamps from the OTLP source rather than scrape time. Reduces
    # Prometheus's apparent metric latency by ~15s — improves E2E determinism.
    send_timestamps: true

  # Phase 12 CONTEXT.md D-09: native elasticsearch exporter (otel-collector-contrib).
  # mapping.mode: none → preserves OTLP field structure (Body, SeverityText,
  # Attributes.<key>, Resource.<key>) and produces index "logs-generic-default".
  elasticsearch:
    endpoints:
      # Compose-internal hostname — resolves to sk-elasticsearch container.
      - http://elasticsearch:9200
    mapping:
      mode: none
    # No auth/tls blocks — dev mode.
```

Note: sk2_1 keeps the `debug` exporter alongside; CONTEXT D-05 explicitly states "No file exporter anywhere in the new collector config" and the sk2_1 file has BOTH `debug` AND the new exporters in its pipelines. For sk_p Phase 11, drop `debug` too (CONTEXT D-05 single-sink discipline — `[elasticsearch]` and `[prometheus]` ONLY in the respective pipelines).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite top-of-file comment block + replace exporters block</name>
  <files>compose/otel-collector-config.yaml</files>
  <read_first>
    - compose/otel-collector-config.yaml (current top-of-file comment lines 1-8 + current exporters block lines 44-51)
    - C:/Users/UserL/source/repos/sk2_1/otel-collector-config.yaml (lines 28-62 — verbatim exporters source per D-06 + D-07)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-01 through D-07)
  </read_first>
  <action>
    Two surgical edits in `compose/otel-collector-config.yaml`.

    Edit 1 — replace the top-of-file 8-line comment block (current lines 1-8) with a new comment block that describes the Phase 11 shape:
    ```yaml
    # OTel Collector Contrib config — Phase 11 shape (was Phase 5 D-10 + Plan 05-02 fix-forward).
    #
    # Pipelines: logs → elasticsearch exporter (single-sink per Phase 11 D-01);
    #            metrics → filter/health_metrics processor → prometheus exporter
    #            (single-sink per Phase 11 D-02; D-04 preserves the Phase 5 fix-forward
    #            that drops /health/* data points before egress);
    #            traces → REMOVED (Phase 11 D-03 — no traces backend in v1).
    #
    # Receivers accept OTLP gRPC (4317) + HTTP (4318) — unchanged from Phase 5.
    # health_check extension on :13133 — host-side `curl localhost:13133/` smoke probe
    # (Phase 5 D-10 + RESEARCH Open Q4 carry-forward; distroless image has no in-container
    # probe per Plan 05-01 deviation #1 + RESEARCH Pitfall 3).
    #
    # Backends (compose-internal DNS):
    #   - http://elasticsearch:9200 (sk-elasticsearch container, D-12 dev posture)
    #   - http://prometheus:9090 (scrape source: this collector exposes /metrics on :8889)
    ```

    Edit 2 — replace the current exporters block (lines 44-51, `file:` + `logging:` blocks) with the new Phase 11 exporters block. The replacement starts at the line `exporters:` and ends just before the `extensions:` line. Verbatim content:
    ```yaml
    exporters:
      # Phase 11 D-07 — exposes /metrics on :8889 for the sk-prometheus container to scrape.
      # OTel → Prometheus naming (load-bearing for test assertions):
      #   dot-form `http.server.request.duration` → underscore-form `http_server_request_duration`;
      #   Histogram (unit="s") expands to `_count` + `_sum` + `_bucket` triplet with `_seconds` suffix
      #   → final Prom name `http_server_request_duration_seconds_count` (and _sum / _bucket).
      #   monotonic Sum gains `_total` suffix (e.g., `db.client.operations` → `db_client_operations_total`).
      # (RESEARCH Pitfall 1 — the only complete reference for this mapping is the OTel spec at
      #  https://opentelemetry.io/docs/specs/otel/compatibility/prometheus_and_openmetrics/.)
      prometheus:
        endpoint: 0.0.0.0:8889
        # CRITICAL per RESEARCH Pitfall 1: without resource_to_telemetry_conversion: true,
        # resource attributes (service.name, service.version) do NOT become Prometheus labels.
        # Phase 11 D-17 round-trip tests assert {service_name="sk-api"} — non-negotiable.
        resource_to_telemetry_conversion:
          enabled: true
        # Emit sample timestamps from the OTLP source rather than scrape time. Reduces
        # Prometheus's apparent metric latency by ~15s — improves E2E test determinism.
        send_timestamps: true

      # Phase 11 D-06 — native elasticsearch exporter (otel-collector-contrib).
      # mapping.mode: none preserves OTLP raw field structure (Body, SeverityText,
      # Attributes.<key>, Resource.<key> — capital first letter) and produces the
      # data stream `logs-generic-default` (backing index `.ds-logs-generic-default-NNNN`).
      # NOTE (RESEARCH Open Q1): sk2_1's live ES on this host shows the OTel-mode index
      # `logs-generic.otel-default` despite an identical `mapping.mode: none` YAML. Wave 0
      # verification in Plan 11-07 confirms the actual index name on first sk_p compose-up
      # by running `curl /_cat/indices`; the test helper constant is baked from that probe.
      elasticsearch:
        endpoints:
          # Compose-internal hostname — resolves to sk-elasticsearch container via default
          # compose network (Pattern 5: collector inside compose, tests outside via host-DNS).
          - http://elasticsearch:9200
        mapping:
          mode: none
        # No auth/tls blocks — dev posture per Phase 11 D-12 + sk2_1 lock-in.
    ```

    Do NOT touch the receivers / processors / extensions / service blocks in this task — Task 2 handles the service block rewrite.
  </action>
  <verify>
    <automated>grep -E "^# OTel Collector Contrib config — Phase 11 shape" compose/otel-collector-config.yaml returns 1; grep "Phase 11 D-01" compose/otel-collector-config.yaml returns 1; grep "Phase 11 D-02" compose/otel-collector-config.yaml returns 1; grep "Phase 11 D-03 — no traces backend in v1" compose/otel-collector-config.yaml returns 1; grep "endpoint: 0.0.0.0:8889" compose/otel-collector-config.yaml returns 1; grep "resource_to_telemetry_conversion:" compose/otel-collector-config.yaml returns 1; grep "send_timestamps: true" compose/otel-collector-config.yaml returns 1; grep -E "^      - http://elasticsearch:9200$" compose/otel-collector-config.yaml returns 1; grep -E "^      mode: none$" compose/otel-collector-config.yaml returns 1; ! grep -E "^  file:$" compose/otel-collector-config.yaml (negation — file exporter gone); ! grep -E "^  logging:$" compose/otel-collector-config.yaml (negation — logging exporter gone); ! grep -E "^  debug:$" compose/otel-collector-config.yaml (negation — sk2_1's debug exporter NOT carried over per D-05 single-sink discipline)</automated>
  </verify>
  <done>Top-of-file comment block describes the Phase 11 shape (logs → ES, metrics → Prom, no traces); exporters block carries the two new exporters with educational comments referencing Phase 11 decisions + RESEARCH pitfalls; file/logging/debug exporters absent.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite service.pipelines block — drop traces, switch logs to [elasticsearch], switch metrics to [prometheus]</name>
  <files>compose/otel-collector-config.yaml</files>
  <read_first>
    - compose/otel-collector-config.yaml (current service block at the bottom of the file; line numbers shifted by Task 1)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-01/D-02/D-03/D-04 verbatim)
  </read_first>
  <action>
    Replace the existing `service:` block (lines 57-69 in the original file; line numbers will have shifted after Task 1). The new block:

    ```yaml
    service:
      extensions: [health_check]
      pipelines:
        logs:
          # Phase 11 D-01 — single sink. No file fan-out. No debug exporter.
          receivers: [otlp]
          exporters: [elasticsearch]
        metrics:
          # Phase 11 D-02 — single sink to Prometheus exporter.
          # Phase 11 D-04 — filter/health_metrics processor re-pointed at this pipeline
          # (was metrics→file in Phase 5; now metrics→prometheus). OBSERV-08 invariant
          # preserved: /health/* data points dropped before reaching the Prom exporter.
          receivers: [otlp]
          processors: [filter/health_metrics]
          exporters: [prometheus]
        # Phase 11 D-03 — traces pipeline REMOVED. No traces backend in v1. OBSERV-12
        # superseded to Out of Scope (REQUIREMENTS.md Phase 11 amendment). The collector
        # will still ACCEPT trace OTLP records on the OTLP receiver but will silently
        # drop them since no pipeline consumes them — this is intentional and matches
        # sk2_1's lock-in posture.
    ```

    The service.pipelines block goes from 3 pipelines (logs + metrics + traces) to 2 pipelines (logs + metrics only). The traces block is fully deleted; the inline comment above explains why so future readers don't add it back accidentally.
  </action>
  <verify>
    <automated>grep -A 1 -E "^    logs:$" compose/otel-collector-config.yaml | grep -E "receivers: \[otlp\]|exporters: \[elasticsearch\]" | wc -l returns at least 1 (logs pipeline parses); grep -E "^      exporters: \[elasticsearch\]$" compose/otel-collector-config.yaml returns 1; grep -E "^      exporters: \[prometheus\]$" compose/otel-collector-config.yaml returns 1; grep -E "^      processors: \[filter/health_metrics\]$" compose/otel-collector-config.yaml returns 1; ! grep -E "^    traces:$" compose/otel-collector-config.yaml (negation — no traces pipeline); ! grep "exporters: \[file" compose/otel-collector-config.yaml (negation — file exporter not in any pipeline); ! grep "exporters: \[debug" compose/otel-collector-config.yaml (negation — debug exporter not in any pipeline); grep -E "^    metrics:$" compose/otel-collector-config.yaml returns 1; grep "Phase 11 D-04" compose/otel-collector-config.yaml returns 1</automated>
  </verify>
  <done>service.pipelines has exactly 2 pipelines (logs + metrics); logs ships to [elasticsearch]; metrics ships through [filter/health_metrics] processor to [prometheus]; traces pipeline absent with inline rationale; extensions: [health_check] preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Lightweight refresh of filter/health_metrics processor comment block</name>
  <files>compose/otel-collector-config.yaml</files>
  <read_first>
    - compose/otel-collector-config.yaml (current processors block — the 24-line educational comment + 6-line OTTL config; line numbers shifted by Tasks 1+2)
    - .planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-CONTEXT.md (D-04 verbatim)
  </read_first>
  <action>
    The existing `processors.filter/health_metrics` block has a 24-line educational comment from Phase 5 Plan 05-02 explaining WHY the filter exists (the SDK-side workaround was impossible because `MeterProviderBuilder.AddAspNetCoreInstrumentation` is parameterless in OTel 1.15.0). This rationale is STILL load-bearing — the OTel 1.15.0 pin has not changed. Do NOT delete the rationale.

    Two surgical adjustments:

    1. **Update the opening line** of the comment block to reference Phase 11 D-04 alongside Plan 05-02:
       - BEFORE: `  # Plan 05-02 fix-forward to Plan 05-01 (closes SC#4 metrics-half gap).`
       - AFTER:  `  # Plan 05-02 fix-forward to Plan 05-01 (closes SC#4 metrics-half gap); Phase 11 D-04 re-points this processor at the Prometheus exporter pipeline (was [file, logging]).`

    2. **Update the SOLUTION paragraph** (currently the second-half of the comment block) to reflect the new pipeline destination:
       - BEFORE: `# SOLUTION (Collector-side filtering): the otel-collector-contrib filterprocessor`
       - The line below it currently says `# drops before the file exporter — tests observe ZERO health-route data points.`
       - AFTER (just this one line): `# drops data points whose http.route starts with /health/ before the Prometheus exporter — tests observe ZERO health-route samples in Prom queries (Phase 11 D-04).`

    Do NOT touch the OTTL expression itself:
    ```yaml
      filter/health_metrics:
        error_mode: ignore
        metrics:
          datapoint:
            - 'metric.name == "http.server.request.duration" and IsMatch(attributes["http.route"], "^/health/.*")'
    ```

    This stays byte-identical (OBSERV-08 invariant per D-04). The comment edits ONLY refresh the pipeline-destination narrative so a future reader does not get confused by the old `[file, logging]` reference.
  </action>
  <verify>
    <automated>grep "Phase 11 D-04 re-points this processor at the Prometheus exporter pipeline" compose/otel-collector-config.yaml returns 1; grep "ZERO health-route samples in Prom queries (Phase 11 D-04)" compose/otel-collector-config.yaml returns 1; grep "filter/health_metrics:" compose/otel-collector-config.yaml returns 1; grep "metric.name == \"http.server.request.duration\" and IsMatch" compose/otel-collector-config.yaml returns 1 (OTTL expression byte-preserved); grep "^/health/.*" compose/otel-collector-config.yaml returns 1 (regex preserved)</automated>
  </verify>
  <done>filter/health_metrics processor body byte-identical; comment block updated to reference Phase 11 D-04 + Prom exporter destination; old "[file, logging]" reference in comment removed; OBSERV-08 invariant preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Smoke-restart otel-collector + verify config loads cleanly</name>
  <files>compose/otel-collector-config.yaml</files>
  <read_first>
    - compose/otel-collector-config.yaml (just-mutated; do not re-read — use git diff to confirm scope)
  </read_first>
  <action>
    Run a smoke restart of the collector to confirm the new config parses and the new exporters initialize cleanly:

    1. `docker compose -f compose.yaml restart otel-collector`
       - This restarts ONLY the collector; ES + Prom remain up. The collector reads the bind-mounted `compose/otel-collector-config.yaml` on each start.

    2. After the restart completes (usually <5s), inspect the container logs:
       `docker logs --tail 80 sk-otel-collector 2>&1`
       Look for THREE markers (all should appear):
       - `"Started"` (collector successfully read config and listened on receivers)
       - `"Exporter started"` (or similar — confirms `elasticsearch` and `prometheus` exporters both initialized; the contrib-image log format may render this differently — accept any "exporter ... start" success line for both exporters)
       - **NO `error`** lines containing the strings `parse`, `unmarshal`, or `config` (these would indicate a malformed YAML or unknown field). If a known-benign warning appears (e.g., `mapping.mode field is deprecated`), document it inline in the SUMMARY but do NOT block.

    3. Smoke-probe the new exporter ports from the host:
       - `curl -fs http://localhost:8889/metrics | head -10` — expected: Prom-text-format collector self-metrics (e.g., `# HELP otelcol_process_uptime ...`). Empty body or 503 means the prometheus exporter did NOT initialize.
       - `curl -fs http://localhost:13133/` — expected: `{"status":"Server available",...}` (health_check extension still wired).

    4. Confirm ES is reachable from the collector container (this is the load-bearing path for OBSERV-13):
       - `docker exec sk-otel-collector wget -qO- http://elasticsearch:9200/_cluster/health` will fail (distroless image has no wget) — instead, use `docker compose -f compose.yaml exec elasticsearch curl -fs http://localhost:9200/_cluster/health` to confirm ES is healthy from inside the compose network. The compose-internal DNS resolution from the collector → ES is implicit (both on the same default compose network). If ES is reachable from the host (via Plan 11-02 Task 5 verify), it is reachable from the collector.

    If any step in 2/3 fails: examine the error, restore the file to the pre-Task-1 state via `git checkout compose/otel-collector-config.yaml`, and re-attempt Tasks 1-3 with the corrected content. Do NOT commit a broken config.
  </action>
  <verify>
    <automated>docker compose -f compose.yaml restart otel-collector exits 0; docker logs --tail 80 sk-otel-collector 2>&1 | grep -i "Started" returns at least 1; ! docker logs --tail 80 sk-otel-collector 2>&1 | grep -iE "error.*(parse|unmarshal|config)" (negation — no config parse errors); curl -fs http://localhost:8889/metrics exits 0 with non-empty body; curl -fs http://localhost:13133/ exits 0</automated>
  </verify>
  <done>otel-collector restarts cleanly with new config; both new exporters initialize; host-side probes on :8889 (Prom-exporter /metrics) and :13133 (health_check) succeed.</done>
</task>

<task type="auto">
  <name>Task 5: Commit collector config rewire as commit #3 of the Phase 11 sequence</name>
  <files>compose/otel-collector-config.yaml</files>
  <read_first>
    - compose/otel-collector-config.yaml (just-mutated; do not re-read — use `git diff` to confirm scope is ONLY this single file)
  </read_first>
  <action>
    Stage ONLY `compose/otel-collector-config.yaml`. Create commit #3 with the exact message:

    ```
    feat(otel-collector): rewire pipelines — logs to elasticsearch, metrics to prometheus, drop traces + file exporter
    ```

    Use a HEREDOC for the commit message body. Verify `git status --porcelain` returns empty after the commit. Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --format=%s returns "feat(otel-collector): rewire pipelines — logs to elasticsearch, metrics to prometheus, drop traces + file exporter"; git show --stat HEAD lists exactly 1 file changed (compose/otel-collector-config.yaml); git status --porcelain returns empty</automated>
  </verify>
  <done>Single config-only commit landed; working tree clean. Stack is now actively shipping OTLP logs to ES and OTLP metrics to Prom for any traffic the existing Program.cs emits. `.WithTracing()` SDK strip + test migration are sequenced next plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OTel collector → Elasticsearch | OTLP-log records exit the collector via HTTP POST to `http://elasticsearch:9200/_bulk` over the plain-HTTP compose-internal network. No auth, no TLS. |
| OTel collector → Prometheus | The Prom exporter exposes `:8889/metrics` for the standalone Prom container (in the same compose network) to scrape via plain HTTP. No auth, no TLS. |
| Collector receivers (host port 4317/4318) | OTLP receivers bound to 0.0.0.0 in the compose-internal network; the host port mapping exposes them on `localhost:4317` and `localhost:4318` for any host process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-T1 (logs carrying PII or secrets leak to ES) | I (Information Disclosure) | compose/otel-collector-config.yaml service.pipelines.logs | mitigate | Phase 4 `CorrelationIdMiddleware` sanitization (Phase 4 D-02 IsValid guard) preserves the no-PII posture on the correlation-id field. Phase 5 T-05-PII (Npgsql default-secure parameter capture) is unaffected — the only path that captured parameter values was via `.WithTracing().AddNpgsql()` which Plan 11-05 removes entirely. Logs continue to carry only `correlation.id`, route, method, status, and any developer-emitted log message bodies (no auto-captured request bodies). **Verify:** Plan 11-08's migrated `LogExportTests` asserts on the sanitized 32-char-hex `Attributes.CorrelationId` field; raw injection chars (`\r`, `\n`, `injected`) are negation-asserted absent from any exported log doc. |
| T-11-03-T2 (over-sharing traces — traces pipeline lingering) | I (Information Disclosure) | compose/otel-collector-config.yaml service.pipelines | mitigate | Task 2 explicitly deletes the `traces:` pipeline block AND adds an inline comment explaining why (Phase 11 D-03; sk2_1 lock-in). The collector continues to receive trace OTLP records but silently drops them since no pipeline consumes them. Plan 11-05 strips the SDK-side `.WithTracing()` so the receive side is also nulled out. **Verify:** `grep -E "^    traces:$" compose/otel-collector-config.yaml` returns 0; `grep -E "traces" compose/otel-collector-config.yaml | grep -v "no traces\|TRACES"` returns 0 (only the comment block references the word). |
| T-11-03-T3 (filter/health_metrics processor accidentally re-wired into [file, logging] reference) | T (Tampering / Misconfiguration) | compose/otel-collector-config.yaml service.pipelines.metrics.exporters | mitigate | Task 3 explicitly updates the processor's educational comment to reference Prom (not file). The OTTL expression body is byte-preserved so the OBSERV-08 invariant (no `/health/*` data points in Prom queries) holds. **Verify:** Task 2 verify gate confirms `exporters: [prometheus]` and `processors: [filter/health_metrics]` co-occur in the metrics pipeline. |
| T-11-03-T4 (mapping.mode: none silently dropped if collector misreads YAML) | I (Information Disclosure) | exporters.elasticsearch.mapping | mitigate | Task 4 includes a smoke-restart that scans collector logs for `error.*(parse|unmarshal|config)` patterns. RESEARCH Open Q1 explicitly flags the `mapping.mode: none` vs `otel` index-name discrepancy on sk2_1's live ES; Wave 0 verification in Plan 11-07 empirically confirms the actual produced index name before the test code bakes in a constant. **Verify:** Task 4 logs-scan succeeds; Plan 11-07 Wave-0 task probes `_cat/indices` against the live stack. |
</threat_model>

<verification>
- `grep -E "^# OTel Collector Contrib config — Phase 11 shape" compose/otel-collector-config.yaml` returns 1.
- `grep "Phase 11 D-01" compose/otel-collector-config.yaml` returns 1.
- `grep "Phase 11 D-02" compose/otel-collector-config.yaml` returns 1.
- `grep "Phase 11 D-03" compose/otel-collector-config.yaml` returns at least 1 (top-of-file comment + traces-rationale block both reference D-03).
- `grep "Phase 11 D-04 re-points" compose/otel-collector-config.yaml` returns 1.
- `grep "Phase 11 D-06" compose/otel-collector-config.yaml` returns 1.
- `grep "Phase 11 D-07" compose/otel-collector-config.yaml` returns 1.
- `grep -E "^      mode: none$" compose/otel-collector-config.yaml` returns 1.
- `grep "endpoint: 0.0.0.0:8889" compose/otel-collector-config.yaml` returns 1.
- `grep "resource_to_telemetry_conversion:" compose/otel-collector-config.yaml` returns 1.
- `grep -E "^      exporters: \[elasticsearch\]$" compose/otel-collector-config.yaml` returns 1.
- `grep -E "^      exporters: \[prometheus\]$" compose/otel-collector-config.yaml` returns 1.
- `! grep -E "^    traces:$" compose/otel-collector-config.yaml` (no traces pipeline).
- `! grep -E "^  file:$" compose/otel-collector-config.yaml` (no file exporter).
- `! grep -E "^  logging:$" compose/otel-collector-config.yaml` (no logging exporter).
- `! grep -E "^  debug:$" compose/otel-collector-config.yaml` (no debug exporter — single-sink discipline per D-05).
- `docker compose -f compose.yaml restart otel-collector` exits 0 (Task 4).
- `docker logs --tail 80 sk-otel-collector 2>&1 | grep -iE "error.*(parse|unmarshal|config)"` returns 0 lines.
- `curl -fs http://localhost:8889/metrics | head -1` returns 0 and a Prom-text-format body.
- `git log -1 --format=%s` matches `feat(otel-collector): rewire pipelines — logs to elasticsearch, metrics to prometheus, drop traces + file exporter`.
- `git show --stat HEAD` shows exactly 1 file changed.
</verification>

<success_criteria>
1. `compose/otel-collector-config.yaml` top-of-file comment block describes the Phase 11 shape (logs → ES, metrics → Prom, no traces) and references D-01..D-07.
2. Receivers block (otlp grpc + http) preserved byte-identical from Phase 5.
3. processors.filter/health_metrics body preserved byte-identical (OTTL expression unchanged); educational comment refreshed to reference Phase 11 D-04 and the Prom destination.
4. exporters block REPLACED: file + logging blocks deleted; new prometheus block (endpoint 0.0.0.0:8889 + resource_to_telemetry_conversion: enabled: true + send_timestamps: true) + new elasticsearch block (endpoints [http://elasticsearch:9200] + mapping mode: none) added. NO `debug` exporter (single-sink per D-05).
5. extensions.health_check block preserved byte-identical (host-side smoke probe per Open Q4).
6. service.pipelines has exactly 2 pipelines: logs (receivers [otlp] → exporters [elasticsearch]) + metrics (receivers [otlp] → processors [filter/health_metrics] → exporters [prometheus]). NO traces pipeline.
7. `docker compose restart otel-collector` succeeds; container logs show "Started" with no config parse errors; both new exporters initialize.
8. Host-side probes succeed: `curl http://localhost:8889/metrics` returns Prom-text body; `curl http://localhost:13133/` returns 200.
9. Single git commit `feat(otel-collector): rewire pipelines — logs to elasticsearch, metrics to prometheus, drop traces + file exporter` exists at HEAD; modifies exactly 1 file; working tree clean post-commit.
</success_criteria>

<output>
After completion, create `.planning/phases/11-migrate-prometheus-and-elastic-containers-from-compose-stack/11-03-SUMMARY.md`.
</output>
</content>
</invoke>