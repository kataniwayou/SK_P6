---
phase: 65-fan-out-workflow-seeder-clean-state-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs
autonomous: true
requirements: [WF-01, WF-02]
must_haves:
  truths:
    - "Running the seeder fact once against a reset-clean DB creates exactly 1 workflow (cron '*/30 * * * * *', entry step = A), 9 steps all bound to the single processor-sample processor_id, 8 step_next_steps edges, 1 workflow_entry_steps row, 9 assignments, 9 workflow_assignments rows."
    - "The 8 edges are exactly A->B, B->C, C->D1, C->D2, D1->E1, D2->E2, E1->F1, E2->F2; sinks F1 and F2 each have 0 outgoing edges."
    - "Each of the 9 assignment payloads parses as JSON with an integer 'number' and a 'label' matching ^Step_(A|B|C|D1|E1|F1|D2|E2|F2)$; the 9 labels are distinct and cover the full node set; numbers are A=1,B=2,C=3,D1=4,E1=5,F1=6,D2=7,E2=8,F2=9."
    - "Calling the seed routine twice in-process without a reset leaves counts at 1/9/9/8 (no duplicates) and the workflow id is unchanged across the two calls (idempotent by sentinel name 'v8-fanout-proof')."
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs"
      provides: "Self-verifying RealStack fan-out seeder fixture + SeedAssignmentAsync helper + run-twice idempotency [Fact]"
      contains: "FanOutSeeder"
      min_lines: 120
  key_links:
    - from: "FanOutSeederE2ETests.cs"
      to: "SampleRoundTripE2ETests.SeedProcessorAsync"
      via: "internal static helper reuse (by-source-hash GET-or-create)"
      pattern: "SeedProcessorAsync"
    - from: "FanOutSeederE2ETests.cs"
      to: "/api/v1/assignments"
      via: "POST AssignmentCreateDto with StepId FK"
      pattern: "/api/v1/assignments"
    - from: "FanOutSeederE2ETests.cs"
      to: "step_next_steps"
      via: "Npgsql SELECT count(*) self-verification"
      pattern: "step_next_steps"
---

<objective>
Create the idempotent fan-out workflow seeder as a self-verifying C# RealStack E2E fixture (D-01..D-04). It builds the topology A->B->C->{D1->E1->F1, D2->E2->F2} (1 workflow, 9 steps, 8 edges, 9 assignments; entry A; one shared processor-sample; cron '*/30 * * * * *'; each step's assignment payload `{ number, label:"Step_*" }`), keyed for idempotency on the stable sentinel workflow name `v8-fanout-proof`. After seeding it queries the DB back via Npgsql and asserts the SPEC acceptance counts inline, then runs the seed routine a second time in-process and asserts the workflow id is unchanged.

Purpose: Deliver the runnable seeder artifact (invoked by `dotnet test --filter "FullyQualifiedName~FanOutSeeder"`) that Phases 67/68 call, AND prove WF-01/WF-02 in one fact. Closes WF-01, WF-02.

Output: tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs (new fixture, new SeedAssignmentAsync helper, reverse-topo edge wiring, Npgsql self-verify, run-twice idempotency).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-SPEC.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-CONTEXT.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-RESEARCH.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-PATTERNS.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-VALIDATION.md

<interfaces>
<!-- Reuse these verbatim; do NOT re-explore the codebase. All verified at file:line 2026-06-14. -->

Reusable internal static helpers in tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs:
  - SeedProcessorAsync(HttpClient client, string sourceHash, CancellationToken ct, Guid? configSchemaId = null) -> Task<Guid>   (:341-374, by-source-hash GET-or-create, idempotent)
  - SeedConfigSchemaAsync(HttpClient client, string sentinelName, string definition, CancellationToken ct) -> Task<Guid>        (:383-400, GET-list-match idempotency PATTERN to mirror for the workflow)
  - SeedStepAsync(HttpClient client, Guid processorId, CancellationToken ct) -> Task<Guid>                                       (:402-415, hardcodes NextStepIds:null + EntryCondition.Always — clone/extend to take Name + NextStepIds)
  - SeedWorkflowAsync(HttpClient client, List<Guid> entryStepIds, string cron, CancellationToken ct) -> Task<Guid>              (:417-431, passes AssignmentIds:null + random Name — clone/extend to take AssignmentIds + sentinel Name)

RealStackWebAppFactory nested class (SampleRoundTripE2ETests.cs:443-536):
  - new RealStackWebAppFactory(); await factory.InitializeAsync(); using var client = factory.CreateClient();
  - points in-proc WebApi at host stack (Postgres 5433 / Redis 6380 / RMQ 5673 / otel 4317)
  - HostPostgres const (:474-475) = "Host=localhost;Port=5433;Database=stepsdb;Username=postgres;Password=postgres;Maximum Pool Size=20;Timeout=15"

Genuine processor-sample SourceHash resolution (SampleRoundTripE2ETests.cs:111-113):
  var hash = typeof(global::Processor.Sample.SampleProcessor).Assembly
      .GetCustomAttributes<AssemblyMetadataAttribute>()
      .First(a => a.Key == "SourceHash").Value!;

DTO shapes (positional records):
  StepCreateDto(string Name, string Version, string? Description, Guid ProcessorId, List<Guid>? NextStepIds, StepEntryCondition EntryCondition)   (StepDtos.cs:13-19; StepEntryCondition.Always)
  AssignmentCreateDto(string Name, string Version, string? Description, Guid StepId, string Payload)   (AssignmentDtos.cs:11-16; Payload is a JSON STRING, jsonb server-side)
  WorkflowCreateDto(string Name, string Version, string? Description, List<Guid> EntryStepIds, List<Guid>? AssignmentIds, string CronExpression)   (WorkflowDtos.cs:20-26)
  Routes: POST /api/v1/steps, POST /api/v1/assignments, POST /api/v1/workflows, GET /api/v1/workflows

Self-verify Npgsql precedent: tests/BaseApi.Tests/Integration/StepsIntegrationTests.cs:71-79 (NpgsqlConnection + NpgsqlCommand "SELECT count(*)" against snake_case tables).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture shell + SeedAssignmentAsync helper + reverse-topo fan-out build routine</name>
  <files>tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs (the file you are creating)
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs:70-135 (fixture shell, traits, ctor, genuine-hash resolve :111-113, current 1-step linear seed :117-121)
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs:341-431 (the 4 reusable internal static seed helpers)
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs:443-536 (RealStackWebAppFactory nested class + HostPostgres const)
    - src/BaseApi.Service/Features/Assignment/AssignmentDtos.cs:11-16 (AssignmentCreateDto shape)
    - src/BaseApi.Service/Features/Step/StepDtos.cs:13-19 (StepCreateDto shape + NextStepIds)
    - src/BaseApi.Service/Features/Workflow/WorkflowDtos.cs:20-26 (WorkflowCreateDto shape)
  </read_first>
  <behavior>
    - Build routine returns the seeded workflow id; on a clean DB it produces exactly 1 workflow / 9 steps / 8 edges / 9 assignments.
    - Build routine is idempotent: when a workflow named "v8-fanout-proof" already exists, it GET-matches by name and returns the existing id WITHOUT creating duplicates.
    - SeedAssignmentAsync posts AssignmentCreateDto with the step's StepId FK and a `{number,label}` JSON payload string, returns the new assignment id.
  </behavior>
  <action>
    Create tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs in the same namespace as SampleRoundTripE2ETests so the `internal static` seed helpers are directly callable. Mirror the fixture shell at SampleRoundTripE2ETests.cs:70-107: `[Trait("Category","E2E")] [Trait("Category","RealStack")] [Collection("Observability")] public sealed class FanOutSeederE2ETests`.

    Add the new helper `SeedAssignmentAsync` exactly per 65-PATTERNS.md lines 77-88:
    ```csharp
    internal static async Task<Guid> SeedAssignmentAsync(HttpClient client, Guid stepId, int number, string label, CancellationToken ct)
    {
        var payload = JsonSerializer.Serialize(new { number, label }); // {"number":1,"label":"Step_A"}
        var dto = new AssignmentCreateDto(Name: $"asg-{label}-{Guid.NewGuid():N}", Version: "1.0.0", Description: null, StepId: stepId, Payload: payload);
        var resp = await client.PostAsJsonAsync("/api/v1/assignments", dto, ct);
        resp.EnsureSuccessStatusCode();
        var read = await resp.Content.ReadFromJsonAsync<AssignmentReadDto>(cancellationToken: ct);
        return read!.Id;
    }
    ```

    Add an extended step helper that accepts a Name + NextStepIds (the existing SeedStepAsync hardcodes NextStepIds:null) using StepCreateDto(Name, "1.0.0", null, processorId, nextStepIds, StepEntryCondition.Always), POST /api/v1/steps, return read.Id.

    Add the core `private static async Task<Guid> SeedFanOutAsync(HttpClient client, CancellationToken ct)` build routine:
    1. Resolve the genuine processor-sample SourceHash via reflection (SampleRoundTripE2ETests.cs:111-113) and `var procId = await SeedProcessorAsync(client, hash, ct);` — the ONE shared processor id for all 9 steps.
    2. IDEMPOTENCY GATE (mirror SeedConfigSchemaAsync :383-400): `var all = await client.GetFromJsonAsync<List<WorkflowReadDto>>("/api/v1/workflows", ct); var existing = all!.FirstOrDefault(w => w.Name == "v8-fanout-proof"); if (existing is not null) return existing.Id;`
    3. REVERSE-TOPOLOGICAL step create (sinks first — both step_next_steps FKs are OnDelete(Restrict) so each NextStepId must already exist). Create in this exact order with these exact NextStepIds:
       F1 (Next: null), F2 (Next: null), E1 (Next: [F1]), E2 (Next: [F2]), D1 (Next: [E1]), D2 (Next: [E2]), C (Next: [D1, D2]), B (Next: [C]), A (Next: [B]).
       Use a distinct step Name per node (e.g. $"step-{node}-{Guid.NewGuid():N}") and keep a `Dictionary<string,Guid>` node->stepId.
       This yields exactly 8 edges: A->B, B->C, C->D1, C->D2, D1->E1, D2->E2, E1->F1, E2->F2.
    4. Per node create one step-bound assignment via SeedAssignmentAsync with the FIXED number<->label mapping (D-08): Step_A=1, Step_B=2, Step_C=3, Step_D1=4, Step_E1=5, Step_F1=6, Step_D2=7, Step_E2=8, Step_F2=9. The `label` is the verbatim token (e.g. "Step_A") — NO extra prefix (Phase 64 D-10). Collect all 9 assignment ids.
    5. Create the single workflow: WorkflowCreateDto(Name:"v8-fanout-proof", "1.0.0", null, EntryStepIds:[A only], AssignmentIds:[all 9], CronExpression:"*/30 * * * * *"). POST /api/v1/workflows, return the new workflow id.
    Do NOT use 5-field "* * * * *" (that is the stale linear-seed cron at :121). Use the 6-field "*/30 * * * * *". Seed via REST only — NO raw SQL inserts.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` exits 0 with 0 warnings (project gate).
    - File tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs exists and contains the strings `SeedAssignmentAsync`, `v8-fanout-proof`, `*/30 * * * * *`, and `StepEntryCondition.Always`.
    - File contains all 9 node labels Step_A, Step_B, Step_C, Step_D1, Step_E1, Step_F1, Step_D2, Step_E2, Step_F2.
    - File contains `NextStepIds` populated for the fan-out node C referencing two children (D1 and D2).
    - File does NOT contain the 5-field literal `"* * * * *"` (no embedded 5-field cron).
  </acceptance_criteria>
  <done>The fixture compiles in Release with 0 warnings; SeedFanOutAsync builds the 9-step/8-edge/9-assignment graph in reverse-topo order, keyed idempotent on sentinel name v8-fanout-proof, all assignments two-sided bound (StepId FK + workflow AssignmentIds).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-verify [Fact] (inline Npgsql count assertions) + run-twice idempotency</name>
  <files>tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs (the file from Task 1)
    - tests/BaseApi.Tests/Integration/StepsIntegrationTests.cs:7,40-79 (NpgsqlConnection + NpgsqlCommand "SELECT count(*)" snake_case junction-verification precedent)
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs:474-475 (HostPostgres connection string const)
    - .planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-RESEARCH.md (lines 169-185 self-verification queries; lines 337-345 Open Questions 1 & 2)
  </read_first>
  <behavior>
    - Test 1 (WF-01/WF-02 acceptance): after one seed against a reset-clean DB, Npgsql counts are workflows-with-cron=1, steps=9, distinct processor_id=1, step_next_steps=8, workflow_entry_steps=1, assignments=9, workflow_assignments=9; F1 and F2 each have 0 outgoing edges; each assignment payload has int `number` + `label` matching ^Step_(A|B|C|D1|E1|F1|D2|E2|F2)$ with 9 distinct labels.
    - Test 2 (idempotency): calling SeedFanOutAsync twice in one process returns an equal workflow id both times and counts remain 1/9/9/8.
  </behavior>
  <action>
    Add the public `[Fact] public async Task FanOutSeeder_SeedsAndSelfVerifies(...)` to FanOutSeederE2ETests. Inside:
    `var ct = TestContext.Current.CancellationToken;` (xUnit v3), `await using var factory = new RealStackWebAppFactory(); await factory.InitializeAsync(); using var client = factory.CreateClient();`

    Call `var wfId1 = await SeedFanOutAsync(client, ct);` then immediately `var wfId2 = await SeedFanOutAsync(client, ct);` (run-twice idempotency in ONE Fact per RESEARCH Open-Q2). `Assert.Equal(wfId1, wfId2);`

    Open one `await using var conn = new NpgsqlConnection(HostPostgres); await conn.OpenAsync(ct);` (copy the HostPostgres const into this fixture or reference it). Run these exact count queries via NpgsqlCommand + ExecuteScalarAsync and assert (per RESEARCH lines 169-185; PATTERNS lines 108-117):
      - `SELECT count(*) FROM workflows WHERE cron_expression = '*/30 * * * * *'` == 1
      - `SELECT count(*) FROM steps` == 9
      - `SELECT count(DISTINCT processor_id) FROM steps` == 1
      - `SELECT count(*) FROM step_next_steps` == 8
      - `SELECT count(*) FROM workflow_entry_steps` == 1
      - `SELECT count(*) FROM assignments` == 9
      - `SELECT count(*) FROM workflow_assignments` == 9   (Pitfall 2 — proves the two-sided binding)
    Tables/columns are snake_case (verified vs migration + StepsIntegrationTests.cs:76). Presume a reset-clean DB (the 67/68 harness contract calls reset BEFORE the seeder — RESEARCH Open-Q1); add a clear assertion message if a count is off (e.g. "steps != 9 — DB not reset-clean before seeder").

    Edge-set + sink-zero + payload assertions:
      - `SELECT step_id, next_step_id FROM step_next_steps` -> read into a set; assert it has exactly 8 rows. Map each step_id back to a node via the assignment payload label (join steps->assignments on assignments.step_id, parse label) and assert the edge node-pairs == {A->B, B->C, C->D1, C->D2, D1->E1, D2->E2, E1->F1, E2->F2}.
      - For F1 and F2: `SELECT count(*) FROM step_next_steps WHERE step_id = @sink` == 0 each (bind the sink step Guid via NpgsqlParameter).
      - `SELECT payload FROM assignments` -> for each: JsonDocument.Parse, assert root has an integer `number` and a string `label` matching regex `^Step_(A|B|C|D1|E1|F1|D2|E2|F2)$`; collect labels, assert 9 distinct and the set equals the full node set.
    Use `[Trait("Category","RealStack")]` on the class so the hermetic filter `Category!=RealStack` excludes it; it runs live via `--filter "FullyQualifiedName~FanOutSeeder"`.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~FanOutSeeder" --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~FanOutSeeder" --configuration Release` exits 0 against a live stack that has been reset+brought-up (run scripts/phase-65-up.ps1 then scripts/phase-65-reset.ps1 first if the DB is dirty).
    - The fixture file contains all 7 count queries above (grep for `step_next_steps`, `workflow_entry_steps`, `workflow_assignments`, `count(DISTINCT processor_id)`).
    - The fixture file contains the regex literal `^Step_(A|B|C|D1|E1|F1|D2|E2|F2)$`.
    - The fixture file contains `Assert.Equal(wfId1, wfId2)` (or equivalent id-equality assertion across the two seed calls).
    - Hermetic run `dotnet test --configuration Release --filter "Category!=RealStack"` does NOT execute this fact (excluded by the RealStack trait) and stays green.
  </acceptance_criteria>
  <done>The single RealStack fact seeds twice, asserts the workflow id is stable, and self-verifies all SPEC acceptance counts (1/9/9/8/1, distinct processor=1, workflow_assignments=9), the exact 8-edge set, F1/F2 zero-outgoing, and the 9 distinct `{int number, Step_* label}` payloads — proving WF-01 + WF-02 in one runnable artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> WebApi REST (in-proc) | All writes go through validated DTOs (cron, payload JSON, FK Guid checks) at the route entry; no raw SQL writes |
| test fixture -> Postgres (Npgsql read-only) | Self-verification issues only SELECT count(*) statements with no interpolated external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | Npgsql self-verify SELECTs in fixture | accept | Read-only SELECT count(*) over static snake_case table names; the only parameterized value is a sink step Guid bound via NpgsqlParameter (no string interpolation of external input). Dev-stack only. |
| T-65-02 | Info Disclosure | HostPostgres connection string (postgres/postgres) embedded in fixture | accept | Dev-only default creds already used across the test suite (SampleRoundTripE2ETests.cs:474-475); not a new secret, not internet-facing. No HIGH-severity exposure. |
| T-65-03 | Spoofing/Validation | Assignment Payload + cron written via REST | mitigate | Seeder posts through existing validated endpoints — AssignmentDtoValidator (JSON + 1MB cap) and WorkflowDtoValidator (6-field cron) enforce shape server-side; no validation bypass introduced. |

This plan is test infrastructure with no production attack surface and no new auth path. No HIGH-severity threats.
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` exits 0, 0 warnings (Processor.BadConfig still compiles — GateACompositionE2ETests.cs:88 unaffected).
- `dotnet test --filter "FullyQualifiedName~FanOutSeeder" --configuration Release` exits 0 against a live stack.
- Hermetic suite `dotnet test --configuration Release` stays green; the RealStack fact is excluded by trait in hermetic runs.
- No product code (src/**) modified by this plan.
</verification>

<success_criteria>
- WF-01: seeder creates 1 workflow (cron '*/30 * * * * *', entry A), 9 steps all bound to one processor-sample id, exactly 8 step_next_steps edges (A->B,B->C,C->D1,C->D2,D1->E1,D2->E2,E1->F1,E2->F2), F1/F2 zero outgoing — asserted inline.
- WF-02: 9 assignments each `{int number, label:"Step_*"}` (9 distinct labels covering the node set, numbers 1..9 in label order); run-twice leaves 1/9/9/8 and an unchanged workflow id (idempotent by sentinel name v8-fanout-proof).
- Invokable via `dotnet test --filter "FullyQualifiedName~FanOutSeeder"` (the artifact 67/68 call).
</success_criteria>

<output>
After completion, create `.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-01-SUMMARY.md`
</output>
