---
phase: 65-fan-out-workflow-seeder-clean-state-stack
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/phase-65-reset.ps1
autonomous: false
requirements: [ENV-02]
must_haves:
  truths:
    - "Running scripts/phase-65-reset.ps1 against a running stack FLUSHALLs Redis, waits (bounded ~60s, fail-loud) for at least one fresh processor-sample per-instance liveness key (skp:proc:{procId}:{instanceId}) to reappear, then DELETEs only the 6 workflow-graph tables in FK-safe order, PRESERVING processors and config_schemas (schemas) rows, and asserts the running processor set is exactly {processor-sample}."
    - "After reset, the workflow-graph tables (workflows, steps, assignments, step_next_steps, workflow_entry_steps, workflow_assignments) are empty; processors and schemas row counts are unchanged; no sk-processor-badconfig container is running; the 2 processor-sample replicas are NOT removed."
    - "A reset->seed cycle leaves a clean baseline: re-running the Plan 01 seeder after this reset yields exactly 1/9/9/8 with no leftover prior-run skp:data:* / skp:msg:* keys at seed time. The stack stays UP (no docker compose down, no -v)."
  artifacts:
    - path: "scripts/phase-65-reset.ps1"
      provides: "FLUSHALL + heal-wait + FK-safe psql DELETE (preserving processors/schemas) + processor-set assertion"
      contains: "redis-cli FLUSHALL"
      min_lines: 50
  key_links:
    - from: "scripts/phase-65-reset.ps1"
      to: "skp:proc:*"
      via: "redis-cli --scan heal-wait poll for per-instance liveness key"
      pattern: "skp:proc"
    - from: "scripts/phase-65-reset.ps1"
      to: "stepsdb"
      via: "docker compose exec -T postgres psql -U postgres -d stepsdb -c DELETE"
      pattern: "psql -U postgres -d stepsdb"
    - from: "scripts/phase-65-reset.ps1"
      to: "step_next_steps"
      via: "FK-safe DELETE (migration Down() order)"
      pattern: "DELETE FROM step_next_steps"
---

<objective>
Create scripts/phase-65-reset.ps1 (D-05, D-06, D-07), a standalone PowerShell clean-state reset that, with the stack UP: (1) `docker exec sk-redis redis-cli FLUSHALL`; (2) heal-waits (bounded ~60s, fail-loud) for a fresh processor-sample per-instance liveness key to reappear in `skp:proc:*` (the exact L2 state the orchestration-start liveness gate reads); (3) `docker compose exec -T postgres psql -U postgres -d stepsdb` DELETEs only the 6 workflow-graph rows + junctions in FK-safe (migration Down()) order, PRESERVING processors + config_schemas; (4) asserts the running processor set is exactly {processor-sample}, removing only genuinely-stray containers (badconfig by exact name sk-processor-badconfig) and NEVER the 2 unnamed processor-sample replicas. Stack stays up — NO docker compose down, NO -v.

Purpose: Deliver the runnable per-run clean-state reset the 67/68 harness calls (reset, then the Plan 01 seeder) so each run's metrics/logs are attributable to that run only. Closes ENV-02.

Output: scripts/phase-65-reset.ps1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-SPEC.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-CONTEXT.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-RESEARCH.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-PATTERNS.md

<!-- depends_on 01: the reset is exercised by a reset->seed cycle. The Plan 01 seeder fixture
     (FanOutSeederE2ETests, --filter ~FanOutSeeder) must exist so the reset's heal-wait +
     processor-set assertion can be proven via reset->seed->assert. -->

<interfaces>
<!-- Reuse the proven docker/redis-cli/psql shapes; all verified at file:line 2026-06-14. -->

Container naming (compose.yaml):
  - NAMED (use `docker exec <name>`): sk-redis, sk-rabbitmq, sk-processor-badconfig (:304), sk-elasticsearch, sk-prometheus, sk-orchestrator, sk-otel-collector.
  - UNNAMED (use `docker compose exec`/`ps`): postgres, keeper, processor-sample, baseapi-service. processor-sample is replicas:2 -> compose-generated names (NEVER docker rm by a `processor*` glob — Pitfall 4).

Redis liveness key shapes (src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:31-65):
  - per-instance liveness key: `skp:proc:{procId:D}:{instanceId}`  (has a SECOND ':' segment after proc:)
  - index SET: `skp:proc:{procId:D}`  (no second ':' segment — EXCLUDE this from the heal-wait match)
  - run-data keys wiped by FLUSHALL: skp:data:{guid}, skp:msg:{guid}
Heartbeat interval = 10s default (src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs:21-22, no compose override) -> 60s heal-wait budget is 6x, generous + fail-loud.

Postgres connection (.env): POSTGRES_DB=stepsdb, POSTGRES_USER=postgres, POSTGRES_PASSWORD=postgres. postgres has NO container_name -> use `docker compose exec -T postgres psql -U postgres -d stepsdb`. (Pitfall 3: close scripts use `psql -U postgres -lqt` which is a DB-LIST and silently no-ops for row deletes — the `-d stepsdb` is LOAD-BEARING.)

FK-safe DELETE order (src/BaseApi.Service/Persistence/Migrations/20260528074618_InitialCreate.cs Down() :271-288):
  step_next_steps -> workflow_assignments -> workflow_entry_steps -> assignments -> workflows -> steps
  PRESERVE: processors, config_schemas (schemas) — NOT deleted.

Reused PowerShell shapes (scripts/phase-62-close.ps1): `docker exec sk-redis redis-cli ...` (:320), `docker compose exec -T postgres psql -U postgres ...` (:314, ADD -d stepsdb), `docker compose ps <svc> --format json` NDJSON parse (:297-303, :265-273).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/phase-65-reset.ps1 (FLUSHALL + heal-wait + FK-safe psql DELETE + processor-set assert)</name>
  <files>scripts/phase-65-reset.ps1</files>
  <read_first>
    - scripts/phase-65-reset.ps1 (the file you are creating)
    - scripts/phase-62-close.ps1:139-339 (docker/redis-cli/psql invocation shapes, NDJSON-per-replica health parse, embedded-SourceHash read, container enumeration)
    - src/BaseApi.Service/Persistence/Migrations/20260528074618_InitialCreate.cs:271-288 (Down() drop order = FK-safe DELETE order)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs:31-65 (liveness key shapes skp:proc:{procId:D}:{instanceId} + index SET skp:proc:{procId:D})
    - src/BaseProcessor.Core/Configuration/ProcessorLivenessOptions.cs:21-22 (heartbeat interval 10s default)
    - .planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-PATTERNS.md (lines 125-169 the reset script shape)
    - .planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-RESEARCH.md (lines 256-291 code examples; Pitfalls 1,3,4 lines 224-246)
  </read_first>
  <action>
    Create scripts/phase-65-reset.ps1. Use `$ErrorActionPreference = 'Stop'` and phase-tagged Write-Host logging (match the close-script style). Execute in this exact order:

    STEP 0 — pre-flight (optional but recommended): assert the stack is up (e.g. `docker compose ps postgres --format json` returns a running instance); fail loud with `exit 2` if the stack is not up (the reset assumes a running stack — D-06).

    STEP 1 — Redis FLUSHALL (named container):
      `docker exec sk-redis redis-cli FLUSHALL | Out-Null`

    STEP 2 — HEAL-WAIT (D-07; gate on the liveness KEY, not container readiness). Poll up to a 60s deadline at ~2s interval; succeed when >=1 per-instance liveness key reappears:
      ```powershell
      $deadline = (Get-Date).AddSeconds(60)
      $healed = $false
      while ((Get-Date) -lt $deadline) {
          $keys = @(docker exec sk-redis redis-cli --scan --pattern 'skp:proc:*' | Where-Object { $_ -notmatch '^skp:proc:[^:]+$' })
          if ($keys.Count -ge 1) { $healed = $true; break }
          Start-Sleep -Seconds 2
      }
      if (-not $healed) { Write-Host 'Liveness did not reconverge in 60s after FLUSHALL — aborting.' -ForegroundColor Red; exit 2 }
      ```
      The regex `^skp:proc:[^:]+$` matches the bare index SET (one segment after proc:) and EXCLUDES it; a key with a second ':' segment is the per-instance liveness key written by a live replica. Do NOT poll the deprecated flat `skp:{procId}` key (deleted Phase 61). Fail loud on non-convergence — this directly prevents a subsequent seed+Start 422 (Pitfall 1).

    STEP 3 — FK-safe psql DELETE (Pitfall 3: MUST use `docker compose exec`, MUST include `-d stepsdb`). Single transaction in migration Down() order:
      ```powershell
      docker compose exec -T postgres psql -U postgres -d stepsdb -c "BEGIN;
      DELETE FROM step_next_steps; DELETE FROM workflow_assignments; DELETE FROM workflow_entry_steps;
      DELETE FROM assignments; DELETE FROM workflows; DELETE FROM steps;
      COMMIT;"
      ```
      PRESERVE processors + config_schemas (schemas) — they are idempotent (source-hash / sentinel-name) and re-seeding them is wasteful (D-06). Do NOT add them to the DELETE list. Statements are static literals (no interpolated input). Check the psql exit code and fail loud if non-zero.

    STEP 4 — processor-set assertion + orphan removal (Pitfall 4 — NEVER rm the unnamed processor-sample replicas):
      ```powershell
      $sample = @(docker compose ps processor-sample --format json 2>$null | Where-Object { $_ -match '\S' } | ForEach-Object { $_ | ConvertFrom-Json })
      # assert processor-sample replicas are present (expect 2); fail loud if 0
      $bad = @(docker ps --filter 'name=sk-processor-badconfig' --format '{{.Names}}')
      if ($bad.Count -gt 0) { Write-Host "Removing stray badconfig container(s): $($bad -join ',')" -ForegroundColor Yellow; docker rm -f sk-processor-badconfig | Out-Null }
      ```
      Only remove processor containers whose compose service is a processor TYPE OTHER THAN processor-sample (badconfig is targetable by its exact name sk-processor-badconfig). Assert the running processor set == {processor-sample}; if processor-sample replica count is 0, fail loud (exit 2). Do NOT `docker compose down`, do NOT pass `-v`, do NOT drop the DB/volume — the stack stays up.

    STEP 5 — success: print a one-line summary (e.g. "[phase-65-reset] FLUSHALL ok; liveness reconverged; graph rows deleted (processors+schemas preserved); processor set == {processor-sample}") and `exit 0`.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "if (-not (Test-Path scripts/phase-65-reset.ps1)) { exit 1 }; $errs = $null; [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/phase-65-reset.ps1), [ref]$null, [ref]$errs); if ($errs.Count -gt 0) { $errs; exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase-65-reset.ps1 exists and parses with zero PowerShell parse errors (verify command exits 0).
    - File contains `redis-cli FLUSHALL`.
    - File contains the heal-wait poll on `skp:proc:*` with the exclude-index-SET regex `^skp:proc:[^:]+$` and a 60s bounded deadline with a fail-loud `exit 2`.
    - File contains the literal `docker compose exec -T postgres psql -U postgres -d stepsdb` (NOT `docker exec sk-postgres`, NOT `-lqt`).
    - File contains all 6 DELETE statements in order: `DELETE FROM step_next_steps`, `DELETE FROM workflow_assignments`, `DELETE FROM workflow_entry_steps`, `DELETE FROM assignments`, `DELETE FROM workflows`, `DELETE FROM steps`.
    - File does NOT contain `DELETE FROM processors` or `DELETE FROM config_schemas` or `DELETE FROM schemas` (preservation).
    - File does NOT contain `docker compose down` or the `-v` volume flag.
    - File references `sk-processor-badconfig` for orphan removal and does NOT `docker rm` by a bare `processor*`/`processor-sample` glob.
  </acceptance_criteria>
  <done>scripts/phase-65-reset.ps1 parses cleanly and performs FLUSHALL -> bounded fail-loud heal-wait on the per-instance liveness key -> FK-safe transactional DELETE of the 6 graph tables via `psql -d stepsdb` (preserving processors+schemas) -> processor-set assertion that removes only badconfig-by-name and never the processor-sample replicas, with the stack staying up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify reset->seed cycle on a live Docker host</name>
  <files>scripts/phase-65-reset.ps1</files>
  <action>This is a verification checkpoint (not implementation). The reset's FLUSHALL, heal-wait, and processor-set assertion cannot be exercised hermetically — they require a live Docker stack. The operator runs the reset->seed cycle below and confirms the clean-state baseline.</action>
  <what-built>scripts/phase-65-reset.ps1 (FLUSHALL + heal-wait + FK-safe DELETE preserving processors/schemas + processor-set assert) exercised end-to-end against a live stack in a reset->seed cycle with the Plan 01 seeder.</what-built>
  <read_first>
    - scripts/phase-65-reset.ps1
    - scripts/phase-65-up.ps1 (Plan 03 — bring the stack up first)
    - tests/BaseApi.Tests/Orchestrator/FanOutSeederE2ETests.cs (Plan 01 — the seeder run after reset)
    - .planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <how-to-verify>
    On a Docker host (the reset/heal-wait cannot run hermetically):
    1. Capture preserved-row baseline: `docker compose exec -T postgres psql -U postgres -d stepsdb -t -c "SELECT count(*) FROM processors; SELECT count(*) FROM config_schemas;"` and note both counts.
    2. Bring the stack up: `pwsh -File scripts/phase-65-up.ps1` -> expect exit 0 (10 service types healthy, 0 badconfig).
    3. Seed once: `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "FullyQualifiedName~FanOutSeeder" --configuration Release` -> expect exit 0.
    4. Run the reset: `pwsh -File scripts/phase-65-reset.ps1` -> expect exit 0 and the heal-wait to report reconvergence.
    5. Confirm graph rows gone: `docker compose exec -T postgres psql -U postgres -d stepsdb -t -c "SELECT count(*) FROM workflows; SELECT count(*) FROM steps; SELECT count(*) FROM assignments; SELECT count(*) FROM step_next_steps;"` -> all 0.
    6. Confirm preservation: re-run the count from step 1 -> processors + config_schemas counts UNCHANGED.
    7. Confirm Redis run-data wiped: `docker exec sk-redis redis-cli --scan --pattern 'skp:data:*'` and `'skp:msg:*'` -> both empty; `--scan --pattern 'skp:proc:*'` -> >=1 per-instance key present (reconverged).
    8. Confirm processor set: `docker compose ps processor-sample --format json` -> 2 healthy replicas; `docker ps --filter name=sk-processor-badconfig` -> empty. Stack still up (orchestrator/keeper/baseapi-service still running).
    9. Re-seed: re-run the seeder fact (step 3) -> exit 0 with the fixture's inline 1/9/9/8 self-verify passing (proves a clean reset->seed baseline).
  </how-to-verify>
  <verify>
    <automated>MISSING — live Docker host required; verified manually via the reset->seed cycle in how-to-verify (PowerShell AST parse of the script is automated in Task 1).</automated>
  </verify>
  <done>Operator confirms: reset exits 0, graph rows cleared, processors+schemas preserved, run-data keys empty + liveness reconverged, processor set == {processor-sample}, stack stayed up, and the re-seed self-verify passes 1/9/9/8.</done>
  <resume-signal>Type "approved" if all checks pass; otherwise describe which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Redis (FLUSHALL / scan) | Destructive keyspace flush scoped to the local dev sk-redis container only |
| operator -> Postgres (psql DELETE) | Destructive row delete scoped to `-d stepsdb`, a static table list preserving processors/schemas; no compose down, no volume drop |
| operator -> Docker daemon (ps / rm) | Container removal targets only sk-processor-badconfig by exact name; never the processor-sample replicas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-06 | Tampering | psql DELETE statements | mitigate | Static literal DELETE statements (no interpolated external input), scoped to `-d stepsdb` and a fixed 6-table list; processors + config_schemas explicitly preserved. Single transaction (BEGIN/COMMIT) avoids partial-delete-on-error. |
| T-65-07 | DoS (dev) | redis-cli FLUSHALL + container rm pointed at wrong target | mitigate | FLUSHALL targets the named sk-redis dev container only; orphan removal is restricted to the exact name sk-processor-badconfig and NEVER a processor-sample glob (Pitfall 4); NO `docker compose down` / `-v` so the dev stack survives. Reset is dev-only — must never be pointed at a shared/prod DB. |
| T-65-08 | Info Disclosure | Compose default creds (postgres/postgres) in psql invocation | accept | Dev-only default creds already used by the close scripts; script embeds no new secrets and is not internet-facing. No HIGH-severity exposure. |

This plan is destructive dev-stack tooling. The realistic risk (destructive op pointed at the wrong target) is mitigated by stepsdb-scoping, static table lists, exact-name container removal, and the no-down/no-volume-drop constraint. No HIGH-severity threats.
</threat_model>

<verification>
- scripts/phase-65-reset.ps1 parses with zero errors (PowerShell AST parse).
- Live reset->seed cycle (checkpoint): graph rows cleared, processors+schemas preserved, skp:data:*/skp:msg:* empty + liveness reconverged, processor set == {processor-sample}, stack stayed up, re-seed self-verify passes 1/9/9/8.
- No product code (src/**) modified; no compose.yaml edit; no docker compose down / -v.
</verification>

<success_criteria>
- ENV-02: reset FLUSHALLs + heal-waits for liveness reconvergence before allowing a seed; DELETEs the 6 workflow-graph tables in FK-safe order while PRESERVING processors + config_schemas; asserts the running processor set == {processor-sample} (badconfig never started / orphans removed by exact name); the stack stays up; two consecutive reset->seed cycles leave no leftover prior-run skp:data:*/skp:msg:* keys at seed time.
</success_criteria>

<output>
After completion, create `.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-02-SUMMARY.md`
</output>
