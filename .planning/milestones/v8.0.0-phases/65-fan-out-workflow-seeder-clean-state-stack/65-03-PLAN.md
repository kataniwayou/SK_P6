---
phase: 65-fan-out-workflow-seeder-clean-state-stack
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-65-up.ps1
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "Running scripts/phase-65-up.ps1 runs `docker compose up -d` against the default profile and waits until all 10 service types (postgres, redis, rabbitmq, otel-collector, elasticsearch, prometheus, orchestrator, keeper, baseapi-service, processor-sample) report healthy/ready, then exits 0."
    - "After bring-up the running container set contains processor-sample (2 replicas, deploy.replicas:2 retained) and ZERO processor-badconfig containers."
    - "The script makes NO edit to compose.yaml — processor-badconfig is excluded solely by its existing profiles:[\"badconfig\"] gate (compose.yaml:300)."
  artifacts:
    - path: "scripts/phase-65-up.ps1"
      provides: "Minimal-stack bring-up + 10-service health wait + zero-badconfig assertion"
      contains: "docker compose up -d"
      min_lines: 40
  key_links:
    - from: "scripts/phase-65-up.ps1"
      to: "docker compose ps <svc> --format json"
      via: "per-service .Health=='healthy' NDJSON poll"
      pattern: "docker compose ps"
    - from: "scripts/phase-65-up.ps1"
      to: "sk-processor-badconfig"
      via: "docker ps --filter name= assertion count==0"
      pattern: "sk-processor-badconfig"
---

<objective>
Create scripts/phase-65-up.ps1 (D-09), a standalone PowerShell minimal-stack bring-up. It runs `docker compose up -d` (default profile — processor-badconfig is already gated behind profiles:["badconfig"] at compose.yaml:300, so it is excluded with NO compose edit), waits for all 10 service types to report healthy/ready (NDJSON-per-replica parse reused from phase-62-close.ps1:291-309), and asserts ZERO sk-processor-badconfig containers. processor-sample keeps deploy.replicas:2 (one TYPE, two replicas — attribution comes from correlationId, not single-instance).

Purpose: Deliver the runnable bring-up artifact the 67/68 harness calls to start the minimal proof stack. Closes ENV-01.

Output: scripts/phase-65-up.ps1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-SPEC.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-CONTEXT.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-RESEARCH.md
@.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-PATTERNS.md

<interfaces>
<!-- Reuse the proven PowerShell shapes; verified at file:line 2026-06-14. -->

Compose facts (compose.yaml):
  - processor-badconfig is behind `profiles: ["badconfig"]` (:300) and named `container_name: sk-processor-badconfig` (:304) — default `docker compose up -d` EXCLUDES it, no compose edit needed.
  - processor-sample and keeper carry `deploy.replicas: 2` (NDJSON emits 2 lines each — require ALL instances healthy).
  - The 10 service types to wait on: postgres, redis, rabbitmq, otel-collector, elasticsearch, prometheus, orchestrator, keeper, baseapi-service, processor-sample.
  - otel-collector has NO in-container healthcheck (compose.yaml:69-79) — treat its 'running' state as ready (do NOT block on .Health for it).

Reused PowerShell patterns (scripts/phase-62-close.ps1:289-309):
  - `docker compose ps <svc> --format json` emits one NDJSON object per replica; parse with ForEach-Object { $_ | ConvertFrom-Json }; a service is healthy when every instance's `.Health -eq 'healthy'`.
  - `docker ps --filter 'name=sk-processor-badconfig' --format '{{.Names}}'` enumerates badconfig containers by exact name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/phase-65-up.ps1 (compose up + 10-service health wait + zero-badconfig assert)</name>
  <files>scripts/phase-65-up.ps1</files>
  <read_first>
    - scripts/phase-65-up.ps1 (the file you are creating)
    - scripts/phase-62-close.ps1:289-309 (the NDJSON-per-replica compose-health pre-flight loop to reuse; canonical service list + all-instances-healthy logic)
    - .planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-PATTERNS.md (lines 173-201 the bring-up script shape)
    - .planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-RESEARCH.md (lines 293-308 bring-up health-wait code example)
  </read_first>
  <action>
    Create scripts/phase-65-up.ps1. Use `$ErrorActionPreference = 'Stop'` and emit clear phase-tagged Write-Host lines (match the close-script logging style).

    1. Bring up the default profile: `docker compose up -d | Out-Null`. This excludes processor-badconfig automatically via its profiles:["badconfig"] gate — DO NOT pass `--profile badconfig`, DO NOT edit compose.yaml.

    2. Define the canonical service list (D-09):
       `$services = @('postgres','redis','rabbitmq','otel-collector','elasticsearch','prometheus','orchestrator','keeper','baseapi-service','processor-sample')`

    3. Health-wait loop (reuse phase-62-close.ps1:291-309 NDJSON parse). For each service, poll up to a bounded deadline (e.g. 180s overall, ~2s poll interval — generous for elasticsearch/start_period cold starts):
       ```powershell
       $instances = @(docker compose ps $svc --format json 2>$null | Where-Object { $_ -match '\S' } | ForEach-Object { $_ | ConvertFrom-Json })
       $health = if ($instances.Count -eq 0) { 'not-running' }
                 else { $u = @($instances | Where-Object { $_.Health -ne 'healthy' }); if ($u.Count -gt 0) { "$($u[0].Health)" } else { 'healthy' } }
       ```
       A service is READY when `$health -eq 'healthy'`. SPECIAL CASE: otel-collector has no in-container healthcheck — treat its 'running' instance state as ready (do not require .Health='healthy' for it). keeper and processor-sample are replicas:2 — require ALL instances healthy (the NDJSON parse above already handles this by failing if ANY instance is not healthy). FAIL LOUD (Write-Host red + `exit 2`) if any required service does not converge before the deadline, naming the offending service + its last $health value.

    4. Zero-badconfig assertion (ENV-01):
       ```powershell
       $bad = @(docker ps --filter 'name=sk-processor-badconfig' --format '{{.Names}}')
       if ($bad.Count -ne 0) { Write-Host "badconfig container running — ENV-01 violated." -ForegroundColor Red; exit 2 }
       ```

    5. On success, print a one-line summary (e.g. "[phase-65-up] 10 service types healthy; 0 badconfig; processor-sample replicas:2") and `exit 0`.

    Do NOT change deploy.replicas for processor-sample (keep 2). Do NOT force single-replica. Do NOT remove the Processor.BadConfig project (this script only governs the running container).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "if (-not (Test-Path scripts/phase-65-up.ps1)) { exit 1 }; $errs = $null; [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/phase-65-up.ps1), [ref]$null, [ref]$errs); if ($errs.Count -gt 0) { $errs; exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/phase-65-up.ps1 exists and parses with zero PowerShell parse errors (the verify command above exits 0).
    - File contains the literal `docker compose up -d`.
    - File contains all 10 service names: postgres, redis, rabbitmq, otel-collector, elasticsearch, prometheus, orchestrator, keeper, baseapi-service, processor-sample.
    - File contains `sk-processor-badconfig` and an `exit 2` fail-loud path when a badconfig container is found.
    - File does NOT contain `--profile badconfig` and makes NO modification to compose.yaml.
    - Live exercise (manual, on a Docker host): `pwsh -File scripts/phase-65-up.ps1` exits 0 with all 10 service types healthy and 0 badconfig containers.
  </acceptance_criteria>
  <done>scripts/phase-65-up.ps1 parses cleanly, brings up the default profile, waits for the 10 service types healthy (otel-collector 'running'=ready; replicas:2 require all instances), and exits non-zero if any service fails to converge or any sk-processor-badconfig container is running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Docker daemon (compose up / ps) | Script issues only compose lifecycle + read (ps) commands against the local dev stack; no destructive ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | Tampering/DoS (dev) | `docker compose up -d` scope | accept | Bring-up is non-destructive (no down, no -v, no row/keyspace mutation); targets the local compose project only. Dev-stack only. |
| T-65-05 | Info Disclosure | Compose default creds (postgres/postgres, guest/guest) | accept | Script embeds no secrets; relies on existing compose/.env defaults (dev-only posture documented across phases). No new secret surface. |

This plan is operator tooling with no production attack surface. No HIGH-severity threats.
</threat_model>

<verification>
- scripts/phase-65-up.ps1 parses with zero errors (PowerShell AST parse).
- On a Docker host, the script exits 0 with all 10 service types healthy and 0 sk-processor-badconfig containers.
- compose.yaml is unmodified; `dotnet build` still compiles Processor.BadConfig (project untouched).
</verification>

<success_criteria>
- ENV-01: bring-up starts the 10 service types healthy and ZERO processor-badconfig containers; processor-sample keeps replicas:2; no compose edit; Processor.BadConfig project still compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/65-fan-out-workflow-seeder-clean-state-stack/65-03-SUMMARY.md`
</output>
