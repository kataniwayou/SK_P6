---
phase: 67-fault-injection-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs
autonomous: false
requirements: [FAULT-01, FAULT-03]
user_setup: []

must_haves:
  truths:
    - "The analyzer fixture honors a caller-supplied scenario id (SCENARIO_ID) and writes analyzer-reports/{that-id}.json"
    - "The analyzer fixture honors caller-supplied window bounds (WINDOW_START_UTC, WINDOW_END_UTC) for its ES cohort range"
    - "Standalone Phase 66 (dotnet test ~Analyzer with no env vars) still passes on the const/UtcNow defaults"
  artifacts:
    - path: "tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs"
      provides: "Env-var seam (SCENARIO_ID / WINDOW_START_UTC / WINDOW_END_UTC) with const/UtcNow fallback"
      contains: "SCENARIO_ID"
  key_links:
    - from: "AnalyzerE2ETests.cs scenarioId"
      to: "Environment.GetEnvironmentVariable(\"SCENARIO_ID\")"
      via: "?? DefaultScenarioId fallback"
      pattern: "GetEnvironmentVariable\\(\"SCENARIO_ID\"\\)"
    - from: "AnalyzerE2ETests.cs windowStartUtc / snapshotUtc"
      to: "WINDOW_START_UTC / WINDOW_END_UTC env vars"
      via: "ParseUtcOr(..., UtcNow) fallback feeding BuildStepSearchBody range"
      pattern: "WINDOW_(START|END)_UTC"
---

<objective>
Add the D-16 env-var seam to the Phase 66 analyzer *test* fixture so the Phase 67 harness can pass a per-scenario id and the harness-recorded 5-minute window into the analyzer — making D-04 (`-ScenarioId` → `analyzer-reports/{scenarioId}.json`) and D-13 (score the cohort fired within the harness window) genuinely real. Each env var falls back to the current `const`/`UtcNow` default so the standalone Phase 66 `dotnet test ~Analyzer` stays green.

Purpose: This is the prerequisite seam Plan 03's reference runs depend on. Without it every report is misnamed `TEST-01.json` and scores only the fixture's self-computed ~60s window, not the full ~10-fire cohort (RESEARCH Open Question 1 / Pitfall 1).
Output: A ~10-line test-only edit to `AnalyzerE2ETests.cs`. NO product code, NO new product log/metric — stays inside v8.0.0 scope discipline (D-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-fault-injection-harness/67-CONTEXT.md
@.planning/phases/67-fault-injection-harness/67-PATTERNS.md

<interfaces>
<!-- The exact lines the seam overrides (verified 2026-06-14). Executor uses these directly. -->

From tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs (current, hardwired):
```csharp
:64   private const string DefaultScenarioId = "TEST-01";
:68   private static readonly Regex ScenarioIdPattern = new(@"^[A-Za-z0-9_-]+$", RegexOptions.Compiled);
:74   var scenarioId = DefaultScenarioId;                 // ← replace RHS with env read
:80   ScenarioIdPattern.IsMatch(scenarioId),              // whitelist guard — KEEP, still applies to env value
:93   var windowStartUtc = DateTimeOffset.UtcNow;         // ← replace RHS with env read (WINDOW_START_UTC)
:108  var snapshotUtc = DateTimeOffset.UtcNow;            // ← replace RHS with env read (WINDOW_END_UTC)
:154-168  BuildStepSearchBody(windowStart, snapshot)     // ALREADY parameterized: interpolates gte:{windowStart:o} lte:{snapshot:o} — NO edit needed
```

Read-with-fallback analog — tests/BaseApi.Tests/Observability/ResolveInstanceIdFacts.cs:34-38 (same Observability/ tree):
```csharp
private static string Resolve() =>
    Environment.GetEnvironmentVariable("POD_NAME")
    ?? Environment.GetEnvironmentVariable("HOSTNAME")
    ?? Environment.MachineName
    ?? Guid.NewGuid().ToString("N");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add SCENARIO_ID / WINDOW_START_UTC / WINDOW_END_UTC env-var seam with const/UtcNow fallback</name>
  <files>tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs (the file being modified — read the full method around lines 60-170 to see scenarioId usage, windowStartUtc, snapshotUtc, the ScenarioIdPattern guard at :80, the DrainMs delay at ~:100, and BuildStepSearchBody at :154-168)
    - tests/BaseApi.Tests/Observability/ResolveInstanceIdFacts.cs (the read-with-fallback `?? `-chain idiom to replicate, lines 28-39)
  </read_first>
  <action>
    Make exactly three RHS substitutions in `tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs`, each reading an env var with a fallback to the current default. NO product code, NO new logging/metric. Do NOT touch `BuildStepSearchBody` (:154-168) — it is already parameterized and honors whatever timestamps it is handed; feeding the env values into windowStartUtc + snapshotUtc is sufficient.

    1. Line ~74 — scenario id:
       Replace `var scenarioId = DefaultScenarioId;`
       with: `var scenarioId = Environment.GetEnvironmentVariable("SCENARIO_ID") ?? DefaultScenarioId;`
       The existing whitelist guard at :80 (`ScenarioIdPattern.IsMatch(scenarioId)` → `^[A-Za-z0-9_-]+$`) MUST stay and now validates the env-supplied id (path-traversal guard T-66-07 / see threat_model T-67-03). Do not remove or weaken it.

    2. Line ~93 — window start:
       Replace `var windowStartUtc = DateTimeOffset.UtcNow;`
       with: `var windowStartUtc = ParseUtcOr(Environment.GetEnvironmentVariable("WINDOW_START_UTC"), DateTimeOffset.UtcNow);`

    3. Line ~108 — window end (ES upper bound):
       Replace `var snapshotUtc = DateTimeOffset.UtcNow;`
       with: `var snapshotUtc = ParseUtcOr(Environment.GetEnvironmentVariable("WINDOW_END_UTC"), DateTimeOffset.UtcNow);`

    Add one small private static helper to the test class (place it near the other private statics, e.g. just after the ScenarioIdPattern field):
    ```csharp
    private static DateTimeOffset ParseUtcOr(string? value, DateTimeOffset fallback) =>
        DateTimeOffset.TryParse(
            value,
            System.Globalization.CultureInfo.InvariantCulture,
            System.Globalization.DateTimeStyles.AssumeUniversal | System.Globalization.DateTimeStyles.AdjustToUniversal,
            out var parsed)
            ? parsed
            : fallback;
    ```
    Rationale for AssumeUniversal|AdjustToUniversal: the harness will emit timestamps via PowerShell `Get-Date -Format o` / `.ToString("o")` (round-trip ISO-8601 with offset); this parse normalizes to UTC and falls back to `UtcNow` on null/empty/malformed so the standalone Phase 66 run (no env vars set) is byte-for-byte unchanged.

    Preserve the existing `await Task.Delay(DrainMs)` (~:100) and poll-to-stable cycle exactly — `WINDOW_END_UTC` only overrides the ES upper-bound `snapshotUtc`; the drain/settle still runs so late-arriving redelivered docs are indexed before scoring. Do NOT touch `PassFailEngine` scoring (~:133) — Phase 66 stays the single source of truth.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GetEnvironmentVariable("SCENARIO_ID")' tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` returns 1
    - `grep -c 'WINDOW_START_UTC' tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` returns ≥1 and `WINDOW_END_UTC` ≥1
    - `grep -c 'ParseUtcOr' tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs` returns ≥3 (helper def + 2 call sites)
    - `ScenarioIdPattern.IsMatch` guard at ~:80 still present (grep finds it) — env value still whitelist-validated
    - `BuildStepSearchBody` body is unchanged (no diff in lines 154-168 region beyond what's above) — `git diff` shows edits only near :74/:93/:108 + the helper, never inside BuildStepSearchBody or PassFailEngine
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release -warnaserror` exits 0 (0 warnings)
  </acceptance_criteria>
  <done>The three RHS substitutions + the ParseUtcOr helper are in place; the whitelist guard and BuildStepSearchBody/PassFailEngine are untouched; the test project builds 0-warning Release.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 66 standalone analyzer run stays green (defaults fallback)</name>
  <files>tests/BaseApi.Tests/Observability/AnalyzerE2ETests.cs</files>
  <action>Run the standalone Phase 66 analyzer fixture (no harness env vars set) and confirm it still exits 0 and writes analyzer-reports/TEST-01.json — proving the D-16 fallback defaults are preserved. This is verification of the Task 1 edit, not new code. See <how-to-verify> for the exact steps.</action>
  <what-built>The env-var seam in AnalyzerE2ETests.cs with const/UtcNow fallback. This checkpoint confirms the standalone Phase 66 regression — `dotnet test ~Analyzer` with NO env vars set — still passes exactly as before, proving the fallback defaults are preserved.</what-built>
  <read_first>
    - .planning/phases/67-fault-injection-harness/67-VALIDATION.md (Wave 0 note: "verify Phase 66 standalone green first")
  </read_first>
  <how-to-verify>
    Requires the live stack up (analyzer is a RealStack fixture against Prometheus + ES). Steps:
    1. Ensure no harness env vars are set: in the shell run (PowerShell) `Remove-Item Env:SCENARIO_ID,Env:WINDOW_START_UTC,Env:WINDOW_END_UTC -ErrorAction SilentlyContinue`.
    2. Bring the stack up if not already: `pwsh scripts/phase-65-up.ps1`.
    3. Run the standalone analyzer fixture exactly as Phase 66 did:
       `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter "Category=RealStack&FullyQualifiedName~Analyzer"`
    4. Expected: exit 0 (PASS), and a report written to `analyzer-reports/TEST-01.json` (the const default, since SCENARIO_ID is unset). Confirm the filename is `TEST-01.json` — proving the fallback chose the const, not an env value.
  </how-to-verify>
  <acceptance_criteria>
    - `dotnet test ~Analyzer` (no env vars set) exits 0
    - the report file produced is named `TEST-01.json` (const fallback engaged), NOT a missing/renamed file
  </acceptance_criteria>
  <resume-signal>Type "approved" if the standalone analyzer run exits 0 and writes TEST-01.json, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness env → test fixture | The harness sets SCENARIO_ID / WINDOW_*_UTC env vars; the fixture reads them and uses SCENARIO_ID to compose a filesystem report path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03 | Tampering/Elevation (path traversal) | `scenarioId` → `analyzer-reports/{scenarioId}.json` path composition | mitigate | KEEP the existing `ScenarioIdPattern` whitelist `^[A-Za-z0-9_-]+$` (:80) — it now validates the env-supplied SCENARIO_ID before the path is built, so `../../etc` style values are rejected exactly as the literal default path (matches Phase 66 T-66-07). |
| T-67-04 | Denial of Service / correctness | malformed WINDOW_*_UTC env value | accept | `ParseUtcOr` falls back to `UtcNow` on any unparseable value (TryParse → false), so a bad timestamp degrades to the self-window default rather than crashing; harness emits round-trip `o`-format so this path is not normally exercised. |
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release -warnaserror` exits 0
- `dotnet test ~Analyzer` with no env vars exits 0 and writes `analyzer-reports/TEST-01.json` (Phase 66 regression green via fallback)
- `git diff` confirms edits are confined to the scenarioId/windowStartUtc/snapshotUtc RHS + the ParseUtcOr helper; BuildStepSearchBody and PassFailEngine untouched
</verification>

<success_criteria>
- AnalyzerE2ETests.cs reads SCENARIO_ID / WINDOW_START_UTC / WINDOW_END_UTC, each with a const/UtcNow fallback (D-16)
- The scenario-id whitelist guard still validates the env value (T-67-03 mitigated)
- Standalone Phase 66 `dotnet test ~Analyzer` (no env) stays green on defaults (FAULT-03 prerequisite; no Phase 66 regression)
</success_criteria>

<output>
After completion, create `.planning/phases/67-fault-injection-harness/67-01-SUMMARY.md`
</output>
