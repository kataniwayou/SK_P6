---
phase: 67-fault-injection-harness
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [FAULT-01, FAULT-02, FAULT-03]
user_setup: []

must_haves:
  truths:
    - "Running `pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-01` completes clean→seed→activate→observe→analyze→teardown with no human step and writes analyzer-reports/TEST-01.json with a verdict"
    - "TEST-01 scores the full ~10-fire cohort (TriggerCount ≈ 10) — proving the D-16 window handoff bounds the 5-min window, not a ~60s tail (RESEARCH Open Question 2)"
    - "Running `pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-02` injects the whole-tier processor crash mid-window, recovers, and writes analyzer-reports/TEST-02.json with a verdict (correctly named, not TEST-01.json)"
    - "Both runs ran fully automated — no Read-Host, no manual intervention after launch (FAULT-03)"
  artifacts:
    - path: "analyzer-reports/TEST-01.json"
      provides: "Baseline run verdict + TriggerCount/CompleteRuns from the harness-driven analyzer"
    - path: "analyzer-reports/TEST-02.json"
      provides: "Processor-crash run verdict (recovery proven or a real finding per D-11)"
  key_links:
    - from: "harness run"
      to: "analyzer-reports/{scenarioId}.json"
      via: "D-16-seamed dotnet test ~Analyzer producing a correctly-named report"
      pattern: "analyzer-reports/TEST-0[12]\\.json"
---

<objective>
Prove the harness end-to-end on the two reference runs (D-10): TEST-01 no-fault baseline FIRST (isolates harness-wiring bugs), THEN the TEST-02-shaped processor crash (isolates fault-injection bugs). Each run must complete fully automated — no human step — and produce the analyzer's verdict + a correctly-named `analyzer-reports/{scenarioId}.json`. This is FAULT-01/02/03's live acceptance and the phase's validation cohort.

Purpose: Phase 67 is "done" when the harness runs end-to-end and PRODUCES the verdict with no human step (D-11). We EXPECT PASS for both, but per D-11 a non-PASS is a real finding to investigate, NOT a harness defect — the harness's correctness hinges on producing a verdict, not on its value.
Output: Two analyzer reports (TEST-01, TEST-02) + recorded verdicts. No files modified in the repo (the harness writes to the test bin dir).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-fault-injection-harness/67-CONTEXT.md
@.planning/phases/67-fault-injection-harness/67-VALIDATION.md
@.planning/phases/67-fault-injection-harness/67-RESEARCH.md

<interfaces>
<!-- Run budget: ~8-10 min per run (5-min window + ~2-min drain + bring-up/reset). Both ~20 min + teardown. -->
<!-- D-10: TEST-01 first (baseline-first isolates wiring bugs), then TEST-02 (crash). -->
<!-- D-14: between runs, the harness's STEP B (phase-65-reset) re-cleans; stack stays up; -->
<!--       each run also does its own STEP A bring-up + STEP Z teardown, so the two runs are independent invocations. -->
<!-- Verdict semantics: analyzer exit 0 = PASS; non-0 = FAIL = a finding to record, not a harness pass/fail (D-11). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: TEST-01 baseline reference run (no fault) — full harness end-to-end</name>
  <files>(none — runs the harness; analyzer writes analyzer-reports/TEST-01.json to the test bin dir)</files>
  <action>Operator-launched, then fully automated: run `pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-01` against an available Docker host and confirm the full clean->seed->activate->observe->analyze->teardown sequence completes with no human prompt, producing a correctly-named analyzer-reports/TEST-01.json with TriggerCount~10 and a recorded verdict. See <how-to-verify> for exact steps and checks.</action>
  <verify>
    <automated>pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-01  # operator-launched; then inspect analyzer-reports/TEST-01.json TriggerCount and $LASTEXITCODE</automated>
  </verify>
  <what-built>The complete harness (Plan 02) + the D-16 analyzer seam (Plan 01). This checkpoint runs the no-fault baseline through the whole harness to confirm the clean pipeline goes green BEFORE introducing a fault (D-10) — isolating any harness-wiring bug from a fault-injection bug.</what-built>
  <read_first>
    - .planning/phases/67-fault-injection-harness/67-VALIDATION.md (V1–V6, V9–V11 self-checks; the run is operator-launched then fully automated)
    - .planning/phases/67-fault-injection-harness/67-RESEARCH.md (Open Question 2: confirm TriggerCount ≈ 10; Pitfall 1: confirm report is correctly named)
  </read_first>
  <how-to-verify>
    1. Ensure Docker is running and no harness env vars linger:
       `Remove-Item Env:SCENARIO_ID,Env:WINDOW_START_UTC,Env:WINDOW_END_UTC -ErrorAction SilentlyContinue`
    2. Launch the baseline run (it is fully automated after launch — no prompts expected):
       `pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-01`
    3. Watch the `[phase-67-harness]` trace progress through STEP A (bring-up) → B (reset) → C (seed) → D (wf-id) → E (204 gate) → F (window, no inject) → H (analyze) → Z (teardown). It should complete in ~8–10 min with ZERO human prompts.
    4. Confirm the harness printed an `analyzer-reports/TEST-01.json` path and a verdict line. Open that JSON and check:
       - `Verdict` is `"Pass"` (expected) — if `"Fail"`, record it as a finding (D-11), it does not by itself mean the harness is broken.
       - `TriggerCount` ≈ 10 (the ~10-fire cohort over the 5-min window — proves the D-16 window handoff bounded the full window, NOT a ~60s tail; RESEARCH Open Question 2). A TriggerCount near 1–2 means the window bounds did not take effect — that IS a harness/seam bug to fix.
       - the report filename is `TEST-01.json` (not a default-misnamed file).
    5. Confirm the harness's final exit code mirrors the analyzer (0 on PASS): `$LASTEXITCODE` after the run.
  </how-to-verify>
  <acceptance_criteria>
    - the run completes with NO human prompt (fully automated; V11 — no Read-Host hit)
    - `analyzer-reports/TEST-01.json` exists and is correctly named (V9; Pitfall 1)
    - the report's `TriggerCount` ≈ 10 (RESEARCH Open Question 2 — window handoff bounds the 5-min cohort)
    - the harness final exit code == the analyzer exit (0 expected on PASS; D-04)
    - `Verdict` recorded (Pass expected; a Fail is logged as a D-11 finding, not a harness defect)
  </acceptance_criteria>
  <resume-signal>Type "approved" if TEST-01 ran fully automated, produced TEST-01.json with TriggerCount≈10 and a recorded verdict; otherwise describe the failure (which STEP / exit code / TriggerCount).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: TEST-02 processor-crash reference run — fault injected mid-window, recovery + verdict</name>
  <files>(none — runs the harness; analyzer writes analyzer-reports/TEST-02.json to the test bin dir)</files>
  <action>Operator-launched, then fully automated: run `pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-02` ONLY after TEST-01 is green (D-10). Confirm the harness stops the whole processor-sample tier mid-window (after ~4 fires), dwells 45s, restarts it, waits both replicas healthy, drains, and writes a correctly-named analyzer-reports/TEST-02.json with a recorded verdict — fully automated, no human prompt. See <how-to-verify> for exact steps; a non-PASS verdict or counter-reset reconciliation discrepancy is a D-11 finding to record, not a harness defect.</action>
  <verify>
    <automated>pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-02  # operator-launched; then inspect analyzer-reports/TEST-02.json verdict and confirm no exit-60 health-wait abort</automated>
  </verify>
  <what-built>The harness's fault-injection path (Plan 02 STEP F crash sequencer + health-wait). This checkpoint runs the canonical processor-crash scenario (D-09) to confirm the harness stops the whole processor-sample tier mid-window, dwells ≥ one 30s cron interval, restarts it, waits both replicas healthy, drains, and produces a verdict — proving FAULT-02 end-to-end. Run this ONLY after TEST-01 is green (D-10).</what-built>
  <read_first>
    - .planning/phases/67-fault-injection-harness/67-VALIDATION.md (V6–V9 — baseline-firing, fault-injected, recovery, verdict)
    - .planning/phases/67-fault-injection-harness/67-RESEARCH.md (§Counter-reset hazard — a mid-window Prom counter reset is EXPECTED on restart and is NOT a harness fault; the analyzer is ES-primary; Pitfall 5; A5 — a restart-induced short Prom delta could surface as a Reconciliation finding per D-11)
  </read_first>
  <how-to-verify>
    1. (Stack may already be down from TEST-01's STEP Z teardown — the harness's STEP A brings it back up.) Clear lingering env vars as in Task 1.
    2. Launch the crash run (fully automated after launch):
       `pwsh scripts/phase-67-harness.ps1 -ScenarioId TEST-02`
    3. Watch the trace. After ~4 observed fires (~120s into the window) it should print `crashing whole tier 'processor-sample'`, then `dwell 45s (tier down)`, then `restarting tier 'processor-sample'`, then a health-wait confirming BOTH replicas return `healthy`. While the tier is down, `docker compose ps processor-sample` (in another shell) should show 0 running during the dwell.
    4. Confirm the harness recovered the tier (both replicas healthy) BEFORE the window closed — i.e. it did NOT abort with exit 60 on the post-start health-wait (Pitfall 3).
    5. Confirm an `analyzer-reports/TEST-02.json` path was printed and a verdict recorded. Open the JSON:
       - report is correctly named `TEST-02.json` (NOT TEST-01.json — Pitfall 1).
       - `Verdict` recorded. Expected `"Pass"` (recovery should hold; message-level redelivery during the fault is reported, not failed — milestone carve-out). A `"Fail"`, or a `Reconciliation` discrepancy from the mid-window counter reset, is a REAL FINDING to investigate per D-11 — record it; it does not mean the harness is defective (the harness's job is to produce the verdict).
    6. Confirm no human prompt occurred at any point (V11).
  </how-to-verify>
  <acceptance_criteria>
    - the harness stopped the whole processor-sample tier (both replicas) mid-window and restarted it (FAULT-02; V7)
    - both replicas returned `Health=healthy` before window close — no exit-60 health-wait abort (V8; Pitfall 3)
    - `analyzer-reports/TEST-02.json` exists, correctly named (V9; Pitfall 1)
    - the run completed fully automated, NO human prompt (FAULT-03; V11)
    - a `Verdict` is recorded (Pass expected; any non-PASS or counter-reset reconciliation discrepancy logged as a D-11 finding, not a harness defect)
  </acceptance_criteria>
  <resume-signal>Type "approved" if TEST-02 injected the crash, recovered both replicas, ran fully automated, and produced TEST-02.json with a recorded verdict; otherwise describe the failure (which STEP / exit code) or the verdict finding to investigate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan runs the already-authored harness (Plan 02) — no new code, no new surface. The harness's trust boundaries (psql static literal, scoped docker control via `$scenario.targetContainers`, localhost-only HTTP) are modeled and mitigated in Plan 02's `<threat_model>` (T-67-01..T-67-04). This plan only invokes it with the two in-table scenario ids.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05 | Tampering (operational) | running the harness against a live stack | accept | Both invocations use ONLY the in-table ids (TEST-01/TEST-02), validated by Plan 02's `$Scenarios.Contains` guard. Crash is scoped to `processor-sample` from the table. `docker compose down` (no `-v`) preserves data volumes. Operator-launched, local dev stack — accepted. |
</threat_model>

<verification>
- TEST-01 produces `analyzer-reports/TEST-01.json` with TriggerCount ≈ 10 and a recorded verdict, fully automated (V9/V11, Open Question 2)
- TEST-02 injects the whole-tier crash, recovers both replicas, produces a correctly-named `analyzer-reports/TEST-02.json` with a recorded verdict, fully automated (V7/V8/V9/V11)
- Neither run requires a human step after launch (FAULT-03)
- Both verdicts recorded; non-PASS treated as a D-11 finding, not a harness defect
</verification>

<success_criteria>
- Harness proven end-to-end on TEST-01 (baseline) and TEST-02 (processor crash), in that order (D-10)
- Each run fully automated clean→seed→activate→inject→observe→analyze→teardown, no human step (FAULT-01/02/03)
- Each run produces the analyzer's verdict + a correctly-named report (D-04/D-11)
- TEST-01 confirms the D-16 window handoff bounds the full ~10-fire cohort (Open Question 2 closed)
</success_criteria>

<output>
After completion, create `.planning/phases/67-fault-injection-harness/67-03-SUMMARY.md`
</output>
