---
phase: 68-live-resilience-proof-7-scenarios-capstone
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - analyzer-reports/phase-68-summary.json
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07]

must_haves:
  truths:
    - "The 7-scenario live sweep runs end-to-end via scripts/phase-68-sweep.ps1 in numeric order (TEST-01 → TEST-07)"
    - "Each scenario produces an automated PASS verdict derived solely from Prometheus + Elasticsearch (no human correctness verification, no triple-SHA net-zero gate)"
    - "Each scenario PASSES iff zero-missing (every started correlationId reaches both sinks) AND effect-once (no duplicate (correlationId, StepLabel)) hold over its window"
    - "Message-level redelivery during a fault is reported, not failed (the analyzer scores it correctly; it is not a verdict FAIL)"
    - "The roll-up shows 7/7 PASS and the wrapper exits 0 — the capstone milestone proof"
    - "A verdict FAIL (exit 1) is investigated as a real finding (TTL-vs-dwell artifact vs genuine defect), NEVER blind-retried; re-run permitted ONLY on a distinct INFRA-ABORT exit (10-70), documented (D-01b/D-04)"
  artifacts:
    - path: "analyzer-reports/phase-68-summary.json"
      provides: "The 7-row capstone roll-up: scenarioId · verdict · zeroMissing · effectOnce · started/complete counts · harnessExit · class"
      contains: "TEST-07"
  key_links:
    - from: "scripts/phase-68-sweep.ps1"
      to: "analyzer-reports/{TEST-01..07}.json"
      via: "per-scenario harness run → analyzer report → roll-up aggregation"
      pattern: "analyzer-reports"
    - from: "the live sweep verdict"
      to: "TEST-01..TEST-07 requirements"
      via: "7/7 PASS proves the recovery machinery survives each fault class"
      pattern: "7/7 PASS"
---

<objective>
Run the live 7-scenario resilience sweep — the milestone capstone proof. Drive `scripts/phase-68-sweep.ps1` (authored in Plan 01) over all 7 fault classes against the full live docker stack, and prove each produces an automated PASS verdict from Prometheus + Elasticsearch. The proof IS this sweep; there is no new code to write here.

Purpose: Demonstrate that the existing recovery machinery (keeper, exactly-once-effect slot-array, per-replica liveness, broker nack-requeue redelivery) survives every fault class — happy path, processor, orchestrator, keeper whole-tier, redis, rabbitmq, and combined redis+rabbitmq crash.
Output: `analyzer-reports/phase-68-summary.json` + console table showing 7/7 PASS; the 7 per-scenario `analyzer-reports/{id}.json`; a documented verdict.

WALL-CLOCK: ~70-85 min for a clean all-PASS sweep (7 × ~10-12 min). Plan for a single uninterrupted ~1.5-hour window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/68-live-resilience-proof-7-scenarios-capstone/68-CONTEXT.md
@.planning/phases/68-live-resilience-proof-7-scenarios-capstone/68-RESEARCH.md

<recovery_risk_map>
<!-- From 68-RESEARCH.md §Recovery Semantics. The investigate-first hypothesis per stateful row. -->
- TEST-01 (baseline) / TEST-02 (processor): already PASS in Phase 67. The sweep re-proves them.
- TEST-03 (orchestrator): MEDIUM-LOW. Lost cron fires during the 45s outage surface as NEVER-STARTED runs (non-fatal Prom corroboration warning), not MISSING. Watch: a started-but-incomplete run mis-advanced on result-queue redelivery (real MISSING). Watch: bounded 90s health-wait on a cold cold-boot (infra-abort exit 60 → re-runnable).
- TEST-04 (keeper, BOTH replicas — total liveness blackout, Note 1): LOW for correctness (keeper is idle on the happy fan-out path), but this is the Note-1 design question. If the total blackout leaves the gate Closed / recovery endpoint Stopped after restart, that is a REAL FINDING for the spec owner — do NOT silently weaken to single-replica.
- TEST-05 (redis): HIGH — the most likely verdict FAIL. The 5s `Processor__ExecutionDataTtl` (compose:285) vs the 45s dwell is the #1 hazard: execution-data keys expire mid-outage → either reinject-DROP (MISSING) or forward-pass re-execution (DUPLICATE Step_* → FAIL). A FAIL may be a TEST-ENV ARTIFACT (data legitimately expired) OR a genuine effect-once defect.
- TEST-06 (rabbitmq): MEDIUM. nack-requeue redelivery is the designed path (TEST-02 proved it). Watch: results un-redelivered past the drain window (MISSING); same 5s-TTL hazard for keys minted just before the outage.
- TEST-07 (redis+rabbitmq): HIGH — the hardest; superset of 05+06. The reinject-with-missing-data silent-drop path is most likely to fire here.

THE BINDING RULE (D-01b/D-04): a verdict FAIL is a REAL FINDING to investigate first (artifact vs defect) — NEVER blind-bump the dwell, NEVER retry a verdict FAIL. Re-run is permitted ONLY on a distinct INFRA-ABORT exit (10-70), and must be documented (which code, why). Tuning a single row's dwell upward is a permitted deviation-WITH-RATIONALE only if a FAIL is traced to insufficient recovery time — that requires the spec-owner checkpoint below, not silent action.
</recovery_risk_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 7-scenario live sweep and capture the roll-up</name>
  <read_first>
    - scripts/phase-68-sweep.ps1 (the wrapper authored in Plan 01 — confirm it loops all 7 ids and the final-exit gate)
    - scripts/phase-67-harness.ps1 (the per-scenario flow A0→Z + the EXIT-CODE TABLE lines 31-42 — needed to read each exit code's meaning)
    - .planning/phases/68-live-resilience-proof-7-scenarios-capstone/68-RESEARCH.md (§Wall-Clock; §Per-Run Isolation — 7-in-a-row is isolation-safe; §Recovery Semantics for the per-row risk map)
    - .planning/phases/68-live-resilience-proof-7-scenarios-capstone/68-CONTEXT.md (D-02 run-all+collect, D-03 roll-up, D-04 flake policy)
  </read_first>
  <action>
    PRECONDITION: docker + docker compose v2 and the .NET 8 SDK are available, and no other heavy load is competing (cold-start under load can push a tier past the bounded 90s health-wait → infra-abort exit 60, which is re-runnable per D-04). The harness brings the stack up/down per run, so no manual stack prep is needed.

    Run the full sweep, capturing all console output to a log:
    ```
    pwsh -File scripts/phase-68-sweep.ps1 *>&1 | Tee-Object -FilePath phase-68-sweep.log
    ```
    This loops `phase-67-harness.ps1 -ScenarioId <id>` over TEST-01..TEST-07 in numeric order (run-all, NO fail-fast, D-02), captures + classifies each exit code, reads the 7 `analyzer-reports/{id}.json`, and writes `analyzer-reports/phase-68-summary.json` + a console table.

    Do NOT interrupt the sweep (~70-85 min). Do NOT auto-retry anything inside this task.

    After the sweep completes, read `analyzer-reports/phase-68-summary.json` and the console table. For EACH of the 7 rows record: `scenarioId`, `verdict`, `zeroMissing`, `effectOnce`, `startedRuns`, `completeRuns`, `harnessExit`, `class`.

    Then branch on the result:
    - **All 7 `class == PASS` (harnessExit 0, wrapper exit 0):** the capstone is proven. Proceed to Task 2 to record the verdict.
    - **Any row `class == INFRA_ABORT` (exit 10-70):** this is an operator-re-runnable infra hiccup (D-04), NOT a verdict FAIL. Re-invoke ONLY that one scenario via the bare harness — `pwsh -File scripts/phase-67-harness.ps1 -ScenarioId <id>` — and document which exit code and why (e.g. "TEST-03 exit 60: cold-start health-wait timeout under load"). After the re-run, if it now PASSES, treat that scenario as PASS for the roll-up (note the re-run in the summary). NEVER re-run inside the wrapper; NEVER re-run a verdict FAIL this way.
    - **Any row `class == VERDICT_FAIL` (exit 1):** STOP. This is a REAL FINDING (D-01b). Do NOT bump the dwell, do NOT change `Processor__ExecutionDataTtl`, do NOT retry. Read that scenario's `analyzer-reports/{id}.json` and classify the failure:
        - MISSING (`Missing > 0`): a started correlationId did not reach both sinks. Hypothesis order: (i) reinject-DROP from execution-data expiring during the 45s outage vs the 5s `ExecutionDataTtl` (test-env artifact — check `keeper_reinject_dropped_total` corroboration / CorroborationDetail), vs (ii) a genuine zero-missing recovery defect.
        - DUPLICATE (`Duplicates.Count > 0`): a redelivery re-executed `ProcessAsync` and emitted a second `Step_*` log. Hypothesis order: (i) the slot array's TTL expired mid-outage so the EXISTS-L2 idempotency check saw `false` and ran the forward pass again (TTL-vs-dwell artifact), vs (ii) a genuine effect-once defect.
        - For TEST-04 specifically: if the FAIL traces to the total-blackout leaving the gate Closed / recovery endpoint Stopped after restart (no replica re-opened), that is the Note-1 design finding.
      Capture the evidence (the report JSON, the relevant log lines, the hypothesis) and proceed to the CHECKPOINT in Task 2 — the spec owner decides artifact-acceptance vs deviation-with-rationale vs defect-fix. Do NOT self-resolve a verdict FAIL.
  </action>
  <acceptance_criteria>
    - `analyzer-reports/phase-68-summary.json` exists and contains 7 rows for scenarioIds TEST-01..TEST-07
    - For the clean-PASS path: every row has `harnessExit == 0` and `class == "PASS"`, and `zeroMissing == true` AND `effectOnce == true`; the wrapper's final exit was 0 (`$LASTEXITCODE -eq 0` after the sweep)
    - The 7 per-scenario reports exist: `analyzer-reports/{id}.json` discoverable under tests/BaseApi.Tests/bin for each id (the harness wrote them)
    - `phase-68-sweep.log` captured the full run
    - For any INFRA_ABORT: the re-run (bare harness, single id) and its exit code are documented; for any VERDICT_FAIL: the report classification (MISSING vs DUPLICATE) and the investigate-first hypothesis are captured, and NO dwell/TTL/retry change was made
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$s = Get-Content -Raw analyzer-reports/phase-68-summary.json | ConvertFrom-Json; $ids = $s.scenarioId; foreach($i in 1..7){ if(($ids -join ',') -notmatch ('TEST-0'+$i)){ throw ('summary missing TEST-0'+$i) } }; $pass = @($s | Where-Object { $_.harnessExit -eq 0 }).Count; Write-Host ('PASS '+$pass+'/7'); if($pass -ne 7){ throw ('capstone not 7/7 — '+$pass+'/7 PASS; investigate per D-01b/D-04 before declaring proven') }; 'OK'"</automated>
  </verify>
  <done>The sweep ran end-to-end; analyzer-reports/phase-68-summary.json holds 7 rows; either all 7 are PASS (capstone proven) or every non-PASS is correctly classified (INFRA_ABORT documented + re-run, or VERDICT_FAIL captured with an investigate-first hypothesis for the checkpoint) with no blind dwell/TTL/retry change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capstone verdict checkpoint — confirm 7/7 PASS or adjudicate a finding</name>
  <what-built>
    The 7-scenario live resilience sweep ran via `scripts/phase-68-sweep.ps1`, producing `analyzer-reports/phase-68-summary.json` (7-row roll-up) + the 7 per-scenario `analyzer-reports/{id}.json` reports + `phase-68-sweep.log`. Each scenario was scored PASS/FAIL by the Phase 66 analyzer purely from Prometheus + Elasticsearch (no human correctness verification, no triple-SHA net-zero gate).
  </what-built>
  <how-to-verify>
    1. Open `analyzer-reports/phase-68-summary.json` and review the 7-row table. Confirm each row: `verdict`, `zeroMissing`, `effectOnce`, `startedRuns`/`completeRuns`, `harnessExit`, `class`.
    2. Confirm the milestone outcome:
       - **If 7/7 PASS** (every `harnessExit == 0`, every row `zeroMissing && effectOnce`, wrapper exit 0): the capstone is proven. Reply "approved — 7/7 PASS" and the phase completes. Message-level redelivery during a fault that did NOT cause a duplicate Step_* is correctly reported-not-failed — that is expected and is a PASS.
       - **If any scenario is VERDICT_FAIL (exit 1):** review the captured investigate-first finding from Task 1 (the report's MISSING-vs-DUPLICATE classification + hypothesis: TTL-vs-dwell artifact vs genuine defect; for TEST-04, the Note-1 total-blackout question). As the spec owner, DECIDE the disposition:
         - **(a) Test-env artifact** (e.g. the 5s `ExecutionDataTtl` legitimately expired across the 45s outage): accept-with-rationale, OR approve a documented deviation (e.g. a longer TTL / dwell tuning for that one row) — your call; either path is recorded as a finding, not a silent pass.
         - **(b) Genuine recovery defect** (zero-missing or effect-once truly broke): this is the real finding the capstone exists to surface — it becomes follow-up work (a gap-closure or a separate product-fix phase), NOT something patched here.
         - **(c) TEST-04 Note-1 design boundary** (total keeper blackout outside the recovery envelope): decide whether to keep whole-tier per D-06 (finding stands) or accept single-replica failover as the proven envelope.
         Describe the disposition in your reply so it is recorded in the SUMMARY.
       - **If any scenario is INFRA_ABORT (exit 10-70)** and was re-run to PASS in Task 1: confirm the documented re-run is acceptable (D-04), then treat as PASS.
    3. Do NOT approve a blind dwell/TTL bump or a retry of a verdict FAIL — those violate D-01b/D-04.
  </how-to-verify>
  <resume-signal>Reply "approved — 7/7 PASS" to complete the capstone, or describe the finding disposition (artifact-accept / deviation-with-rationale / defect→follow-up / TEST-04 envelope decision) for the non-PASS scenario(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sweep CLI | The only "input" is the operator running `phase-68-sweep.ps1`. No network-exposed surface, no remote caller. |
| sweep → harness → docker/dotnet/localhost | The harness shells `docker compose`, `dotnet test`, and `Invoke-WebRequest` against `localhost:8080`/`localhost:9090` only, per run. |
| analyzer → Prometheus + Elasticsearch | The verdict is read from local Prom (`localhost:9090`) + ES on the dev stack; no external/untrusted source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | Tampering | `$ScenarioId` fed to the harness | accept | The sweep feeds only the 7 hardcoded literal ids; the harness whitelist-validates each against `^[A-Za-z0-9_-]+$` (line 95) before any docker/psql op. No untrusted input crosses the boundary. |
| T-68-05 | Information Disclosure | `analyzer-reports/phase-68-summary.json` + `phase-68-sweep.log` | accept | Contain only scenario ids, verdicts, run counts, and ops trace — no secrets, PII, credentials, or connection strings. Local dev/ops artifacts. |
| T-68-06 | Denial of Service | the ~1.5-hour live sweep against the local stack | accept | Runs against the local dev compose stack only; resource consumption is self-contained and operator-initiated. Not a multi-tenant or production surface. |

**Disposition rationale:** This plan only RUNS the local ops tooling authored in Plan 01 and reads its output. No network-exposed surface, no untrusted input, no auth/secrets/PII handling, no new product code. No high-severity threats — documented honestly per the security gate.
</threat_model>

<verification>
- `analyzer-reports/phase-68-summary.json` exists with 7 rows TEST-01..07.
- The clean-PASS gate (`@($rows | ? harnessExit -eq 0).Count -eq 7`) holds, OR every non-PASS is correctly adjudicated at the checkpoint (artifact-accept / deviation-with-rationale / defect→follow-up / TEST-04 envelope decision).
- The verdict was derived solely from Prometheus + Elasticsearch (no human correctness verification, no net-zero gate).
- No blind dwell/TTL bump and no retry of a verdict FAIL occurred (D-01b/D-04 respected).
</verification>

<success_criteria>
- All 7 scenarios produce an automated PASS verdict (zero-missing + effect-once over each window), OR any non-PASS is surfaced as a documented finding adjudicated by the spec owner — never silently accepted.
- The roll-up shows 7/7 PASS ⇔ wrapper exit 0 — the single-glance milestone proof that the existing recovery machinery survives each fault class.
- Message-level redelivery during faults is reported, not failed.
</success_criteria>

<output>
After completion, create `.planning/phases/68-live-resilience-proof-7-scenarios-capstone/68-02-SUMMARY.md` — include the 7-row roll-up, the wall-clock, any INFRA-ABORT re-runs (code + reason), and any VERDICT_FAIL finding + its checkpoint disposition.
</output>
