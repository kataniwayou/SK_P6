---
phase: 40-keeper-recovery-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Messaging.Contracts/IExecutionCorrelated.cs
  - src/Keeper/Recovery/KeeperRecoveryHandler.cs
  - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs
  - src/Keeper/Consumers/FaultExecutionResultConsumer.cs
  - src/Keeper/Program.cs
  - tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs
autonomous: true
requirements: [KHARD-03]
must_haves:
  truths:
    - "Exactly one class named KeeperRecoveryHandler exists; it owns the full recover/probe/reinject/park/pause/resume body once."
    - "Both fault consumers' Consume methods are one-line delegations to handler.HandleAsync(...)."
    - "The shared body reads inner.H through the generic constraint (H is declared on IExecutionCorrelated)."
    - "The asymmetric *ConsumerDefinition retry-owner seam is byte-unchanged."
    - "The existing hermetic Keeper suite stays GREEN after the extraction (observable bus behavior identical)."
  artifacts:
    - path: "src/Keeper/Recovery/KeeperRecoveryHandler.cs"
      provides: "Single shared generic recovery body both consumers delegate to"
      contains: "public sealed class KeeperRecoveryHandler"
      min_lines: 60
    - path: "src/Messaging.Contracts/IExecutionCorrelated.cs"
      provides: "H exposed on the shared inner interface so the generic body can read inner.H"
      contains: "string H"
  key_links:
    - from: "src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs"
      to: "KeeperRecoveryHandler.HandleAsync"
      via: "one-line expression-body delegation passing FaultTypeDispatch + queue:{ProcessorId:D}"
      pattern: "handler\\.HandleAsync"
    - from: "src/Keeper/Consumers/FaultExecutionResultConsumer.cs"
      to: "KeeperRecoveryHandler.HandleAsync"
      via: "one-line expression-body delegation passing FaultTypeResult + queue:{OrchestratorQueues.Result}"
      pattern: "handler\\.HandleAsync"
    - from: "src/Keeper/Program.cs"
      to: "KeeperRecoveryHandler"
      via: "AddSingleton beside L2ProbeRecovery"
      pattern: "AddSingleton<Keeper\\.Recovery\\.KeeperRecoveryHandler>"
---

<objective>
KHARD-03 keystone: collapse the two verbatim-duplicate Keeper fault-consumer bodies
(`FaultEntryStepDispatchConsumer.Consume`, `FaultExecutionResultConsumer.Consume`) into ONE
injected generic helper `KeeperRecoveryHandler` that both consumers delegate to in one line.
This is the keystone — KHARD-01's cap (Plan 02) lands inside this single body, so the cap
exists in exactly one place.

Purpose: eliminate the IN-01 duplication so future recovery changes (and the cap) land once,
not twice.
Output: a new `KeeperRecoveryHandler` class, two one-line delegating consumers, an `H` member
on `IExecutionCorrelated`, the DI registration, and the harness wired to the new handler — with
the existing hermetic Keeper suite still GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-keeper-recovery-hardening/40-RESEARCH.md
@.planning/phases/40-keeper-recovery-hardening/40-PATTERNS.md

<decisions>
- D-A6 (RESOLVED): `IExecutionCorrelated` (`src/Messaging.Contracts/IExecutionCorrelated.cs:10-17`)
  declares ExecutionId/WorkflowId/StepId/ProcessorId/EntryId + CorrelationId (via ICorrelated) but
  NOT `H`. The shared body reads `inner.H` at 4 sites (intake log, PauseWorkflow, ResumeWorkflow,
  re-inject context). **CHOICE (a): add `string H { get; }` to `IExecutionCorrelated`.** Rationale:
  both concrete records already expose `public string H { get; init; } = "";`
  (`EntryStepDispatch.cs:17`, `ExecutionResult.cs:18`), so `init` satisfies a `{ get; }` interface
  member with ZERO record changes; lowest-risk, one-line interface add; lets the generic bound be
  `where T : class, IExecutionCorrelated`. (Rejected: Func<T,string> hSelector — adds a param to
  every delegation for no benefit since both records already carry H.)
- Helper shape = injected sealed singleton mirroring `L2ProbeRecovery` (NOT an abstract base
  consumer). The two per-type deltas are DATA (fault_type tag literal, reinject endpoint URI), not
  behavior, so they pass as method params. (40-RESEARCH §KHARD-03; 40-PATTERNS §keystone.)
</decisions>

<interfaces>
<!-- Extracted from codebase — the executor uses these directly, no exploration needed. -->

From src/Messaging.Contracts/IExecutionCorrelated.cs (CURRENT — add H to this):
```csharp
public interface IExecutionCorrelated : ICorrelated
{
    Guid ExecutionId { get; }
    Guid WorkflowId  { get; }
    Guid StepId      { get; }
    Guid ProcessorId { get; }
    string EntryId   { get; }
}
```

From src/Messaging.Contracts/EntryStepDispatch.cs:17 and ExecutionResult.cs:18 (BOTH already have):
```csharp
public string H { get; init; } = "";   // init satisfies a { get; } interface member — no record edit needed
```

From src/Keeper/Recovery/L2ProbeRecovery.cs:19,24 (the ctor + entry-point shape to mirror):
```csharp
public sealed class L2ProbeRecovery(IConnectionMultiplexer redis, IOptions<ProbeOptions> opts, KeeperMetrics metrics)
public async Task<ProbeOutcome> RunAsync(string entryId, string h, string procId, CancellationToken ct)
public enum ProbeOutcome { Recovered, GaveUp }
```

From src/Keeper/Observability/KeeperMetrics.cs — instruments + tag helpers the body uses:
```csharp
metrics.FaultConsumed / Recovered / DlqPushed / WorkflowPaused / WorkflowResumed / RecoveryDuration  // Counter<long> / Histogram<double>
KeeperMetricTags.FaultTags(string faultType, string procId)  // => KeyValuePair<string,object?>[]
KeeperMetricTags.FaultTypeDispatch = "dispatch";  FaultTypeResult = "result";
KeeperMetricTags.ProcessorId / Reason / Outcome / FaultType;  ReasonProbeExhausted = "probe_exhausted";
KeeperMetricTags.OutcomeRecovered = "recovered";  OutcomeGaveUp = "gave_up";
```

Re-inject endpoints (the per-type delta): Dispatch => `new Uri($"queue:{inner.ProcessorId:D}")`;
Result => `new Uri($"queue:{OrchestratorQueues.Result}")`. Park endpoint (both): `new Uri($"queue:{KeeperQueues.DeadLetter}")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add H to IExecutionCorrelated</name>
  <files>src/Messaging.Contracts/IExecutionCorrelated.cs</files>
  <read_first>
    - src/Messaging.Contracts/IExecutionCorrelated.cs (the interface to extend)
    - src/Messaging.Contracts/EntryStepDispatch.cs:17 (already has `public string H { get; init; } = ""`)
    - src/Messaging.Contracts/ExecutionResult.cs:18 (already has `public string H { get; init; } = ""`)
  </read_first>
  <action>
    In `src/Messaging.Contracts/IExecutionCorrelated.cs`, add a `string H { get; }` member to the
    `IExecutionCorrelated` interface (per D-A6). Place it after `string EntryId { get; }` with a
    one-line doc comment: `/// <summary>The stable per-message content hash (64-hex), used as the
    cross-message recovery key. Declared on the concrete records; hoisted here so the generic Keeper
    recovery body can read it (KHARD-03).</summary>`. Do NOT change either concrete record — their
    existing `public string H { get; init; }` already satisfies the `{ get; }` interface member.
  </action>
  <verify>
    <automated>cd C:/Users/UserL/source/repos/SK_P; dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "string H { get; }" src/Messaging.Contracts/IExecutionCorrelated.cs` == 1
    - `dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Release` exits 0 with 0 warnings
    - No edits to EntryStepDispatch.cs or ExecutionResult.cs (`git diff --name-only` does NOT list either record)
  </acceptance_criteria>
  <done>IExecutionCorrelated declares `string H { get; }`; both records compile unchanged against it; Messaging.Contracts builds 0-warning Release.</done>
</task>

<task type="auto">
  <name>Task 2: Extract KeeperRecoveryHandler and shrink both consumers to delegators</name>
  <files>src/Keeper/Recovery/KeeperRecoveryHandler.cs, src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs, src/Keeper/Consumers/FaultExecutionResultConsumer.cs</files>
  <read_first>
    - src/Keeper/Recovery/L2ProbeRecovery.cs:19-54 (the injected-singleton-helper shape to mirror, same namespace Keeper.Recovery)
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs:32-104 (the verbatim body to extract; ctor at :32-34)
    - src/Keeper/Consumers/FaultExecutionResultConsumer.cs:33-105 (the byte-identical twin; only deltas = fault_type literal :46,83,98 + reinject endpoint :72)
    - src/Keeper/Observability/KeeperMetrics.cs:38-138 (instruments + KeeperMetricTags used by the body)
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumerDefinition.cs + FaultExecutionResultConsumerDefinition.cs (DO-NOT-TOUCH — read only to confirm you leave them alone)
  </read_first>
  <action>
    Create `src/Keeper/Recovery/KeeperRecoveryHandler.cs`, namespace `Keeper.Recovery`, a
    `public sealed class KeeperRecoveryHandler(ILogger<KeeperRecoveryHandler> logger, L2ProbeRecovery
    recovery, KeeperMetrics metrics)` — mirroring the `L2ProbeRecovery` primary-ctor singleton shape.
    (Do NOT inject `IConnectionMultiplexer`/`IOptions<ProbeOptions>` here — Plan 02 adds the cap deps;
    keep this plan a pure behavior-preserving extraction.)

    Add ONE generic method that absorbs the verbatim body from
    `FaultEntryStepDispatchConsumer.Consume` (lines 36-103), with the THREE per-type deltas as params:
    ```csharp
    public async Task HandleAsync<T>(
        ConsumeContext<Fault<T>> context,
        string faultTypeTag,            // KeeperMetricTags.FaultTypeDispatch | FaultTypeResult
        Func<T, Uri> reinjectEndpoint,  // inner => queue:{ProcessorId:D} | queue:{OrchestratorQueues.Result}
        CancellationToken ct)
        where T : class, IExecutionCorrelated
    ```
    Copy the body verbatim, substituting:
    - `nameof(EntryStepDispatch)` in the intake log → `typeof(T).Name`
    - `KeeperMetricTags.FaultTypeDispatch` (3 sites: :45 FaultConsumed, :82 Recovered, :97 DlqPushed) → `faultTypeTag`
    - re-inject endpoint `new Uri($"queue:{inner.ProcessorId:D}")` (:71) → `reinjectEndpoint(inner)`
    - thread `ct` (the passed CancellationToken) wherever the body used `context.CancellationToken`
    Keep EVERYTHING else byte-identical: the double `context.Message.Message` unwrap, `Stopwatch`,
    `FaultConsumed.Add`, the LOAD-BEARING manual CorrelationId scope + `ExecutionLogScope.BuildState(inner)`
    + the single Information log, `Publish(PauseWorkflow)` + `WorkflowPaused.Add`, the awaited
    `recovery.RunAsync(inner.EntryId, inner.H, procId, ct)`, the Recovered branch (Send inner +
    Publish ResumeWorkflow + WorkflowResumed/Recovered/RecoveryDuration{recovered}), and the GaveUp
    branch (Send context.Message to `queue:{KeeperQueues.DeadLetter}` + DlqPushed{probe_exhausted}/
    RecoveryDuration{gave_up}). Add the needed usings (System, System.Collections.Generic,
    System.Diagnostics, Keeper.Observability, MassTransit, Messaging.Contracts, Microsoft.Extensions.Logging).

    Then rewrite BOTH consumers to one-line delegators:
    - `FaultEntryStepDispatchConsumer`: ctor → `(ILogger<FaultEntryStepDispatchConsumer> logger,
      KeeperRecoveryHandler handler)`; body →
      `public Task Consume(ConsumeContext<Fault<EntryStepDispatch>> context) =>
          handler.HandleAsync(context, KeeperMetricTags.FaultTypeDispatch,
              inner => new Uri($"queue:{inner.ProcessorId:D}"), context.CancellationToken);`
    - `FaultExecutionResultConsumer`: ctor → `(ILogger<FaultExecutionResultConsumer> logger,
      KeeperRecoveryHandler handler)`; body →
      `public Task Consume(ConsumeContext<Fault<ExecutionResult>> context) =>
          handler.HandleAsync(context, KeeperMetricTags.FaultTypeResult,
              inner => new Uri($"queue:{OrchestratorQueues.Result}"), context.CancellationToken);`
      Keep the `using ExecutionResult = Messaging.Contracts.ExecutionResult;` alias (:9) so the
      `Fault<ExecutionResult>` generic and the lambda resolve to the contract type.
    Remove now-unused usings from the consumers (Stopwatch/Collections.Generic, etc.) to stay 0-warning.
    DO NOT touch either `*ConsumerDefinition.cs` (Pitfall 4 — the asymmetric retry-owner seam stays exact).
  </action>
  <verify>
    <automated>cd C:/Users/UserL/source/repos/SK_P; dotnet build src/Keeper/Keeper.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "class KeeperRecoveryHandler" src/Keeper/Recovery/KeeperRecoveryHandler.cs` == 1
    - `git grep -c "handler.HandleAsync" src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs` == 1
    - `git grep -c "handler.HandleAsync" src/Keeper/Consumers/FaultExecutionResultConsumer.cs` == 1
    - `git grep -c "recovery.RunAsync" src/Keeper/Consumers/` == 0 (the probe-await moved into the handler — no longer in either consumer)
    - `git grep -c "KeeperQueues.DeadLetter" src/Keeper/Recovery/KeeperRecoveryHandler.cs` == 1 (the single park site)
    - `git diff --name-only` does NOT list FaultEntryStepDispatchConsumerDefinition.cs or FaultExecutionResultConsumerDefinition.cs
    - `dotnet build src/Keeper/Keeper.csproj -c Release` exits 0 with 0 warnings (TreatWarningsAsErrors)
  </acceptance_criteria>
  <done>One KeeperRecoveryHandler owns the full body once; both Consume methods are single delegating expressions; the definition seam is untouched; Keeper builds 0-warning Release.</done>
</task>

<task type="auto">
  <name>Task 3: Register the handler in DI and wire the hermetic harness</name>
  <files>src/Keeper/Program.cs, tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs</files>
  <read_first>
    - src/Keeper/Program.cs:31-54 (the L2ProbeRecovery AddSingleton block + the consumer registration)
    - tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs:108-122 (BuildHarness — consumers ctor-inject the handler now)
  </read_first>
  <action>
    In `src/Keeper/Program.cs`, directly AFTER the existing
    `builder.Services.AddSingleton<Keeper.Recovery.L2ProbeRecovery>();` line (:33), add:
    `builder.Services.AddSingleton<Keeper.Recovery.KeeperRecoveryHandler>();` with a one-line comment
    `// KHARD-03 — the shared recovery body both fault consumers delegate to (ctor-injects L2ProbeRecovery + KeeperMetrics).`
    Leave the `Configure<ProbeOptions>` (:29) and the consumer-registration block (:50-54) unchanged.

    In `tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs` `BuildHarness` (:108-122), add
    `.AddSingleton<KeeperRecoveryHandler>()` to the ServiceCollection chain immediately after the
    existing `.AddSingleton<L2ProbeRecovery>()` line (:116). This is the Wave-0 harness-registration
    gap: both fault consumers now ctor-inject the handler, so the harness must resolve it. Do NOT
    change any test assertions — the observable bus behavior (Sent reinject / Sent park) is identical.
  </action>
  <verify>
    <automated>cd C:/Users/UserL/source/repos/SK_P; dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter "FullyQualifiedName~KeeperProbeLoopTests"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "AddSingleton<Keeper.Recovery.KeeperRecoveryHandler>" src/Keeper/Program.cs` == 1
    - `git grep -c "AddSingleton<KeeperRecoveryHandler>" tests/BaseApi.Tests/Keeper/KeeperProbeLoopTests.cs` == 1
    - `dotnet test ... --filter "FullyQualifiedName~KeeperProbeLoopTests"` exits 0 (all 6 existing facts GREEN — Probe_Success_Reinjects, Probe_GiveUp_ParksToDlq, Probe_AcksOnlyAfterLoop, + 3 helper facts)
  </acceptance_criteria>
  <done>The handler is a DI singleton beside L2ProbeRecovery; BuildHarness resolves it; all 6 existing KeeperProbeLoopTests facts stay GREEN through the extraction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal bus → Keeper consumer | Fault<T> envelopes arrive on the trusted internal RabbitMQ bus; no external attack surface is added by this refactor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering/Correctness | KeeperRecoveryHandler (extracted body) | mitigate | Body copied verbatim with only the 3 documented data deltas; the 6 existing hermetic facts (Task 3) prove observable bus behavior (reinject endpoint, park envelope, no-reinject-on-giveup) is unchanged — regression surfaces RED. |
| T-40-02 | Information disclosure | Intake Information log | accept | Unchanged from the source consumers: only ExceptionType + Message are surfaced as structured params (no stack frames, no interpolation) — the existing T-35-04/05 disposition carries over verbatim. |
| T-40-03 | Repudiation/Correctness | *ConsumerDefinition retry seam | mitigate | Pitfall 4 — definitions are byte-unchanged (acceptance criterion asserts they are not in `git diff`), so the single-owner endpoint retry filter is not double-registered. |
</threat_model>

<verification>
- `dotnet build src/Keeper/Keeper.csproj -c Release` and `src/Messaging.Contracts` Release: 0 warnings.
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter "Category!=RealStack"` GREEN (full hermetic suite — the extraction must not regress any sibling fact).
- Exactly one `KeeperRecoveryHandler` class; both consumers delegate in one line; neither definition file changed.
</verification>

<success_criteria>
- One shared recovery body (`KeeperRecoveryHandler`) both consumers delegate to (no duplication) — KHARD-03 satisfied; the cap (Plan 02) now has exactly one place to land.
- `IExecutionCorrelated` exposes `H`; the generic body reads `inner.H` through the bound.
- Full hermetic suite GREEN; Release 0-warning; the retry-owner definition seam untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/40-keeper-recovery-hardening/40-01-SUMMARY.md`
</output>
