---
phase: 33-fault-recovery-spike-de-risk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs
autonomous: true
requirements: [INTAKE-01, INTAKE-02, INTAKE-04, PROBE-06]

must_haves:
  truths:
    - "The spike test file exists, compiles clean (Release 0/0), and is excluded from the hermetic suite via [Trait(\"Category\",\"RealStack\")] so the full --filter-not-trait \"Category=RealStack\" run shows zero regression."
    - "The file binds IConsumer<Fault<EntryStepDispatch>> AND IConsumer<Fault<ExecutionResult>> on a short-lived in-test IBusControl (INTAKE-01 bind half, authored — proven live in Plan 02)."
    - "Each fault-probe body unwraps the inner message via context.Message.Message (double .Message) and captures all 6 IExecutionCorrelated ids + H (INTAKE-02)."
    - "The test computes the dispatch-path H and result-path resultH a-priori via MessageIdentity (ComputeH/HashBlob/HashManifest), arms WRONGTYPE poison, re-injects the extracted Fault<T>.Message verbatim by type via GetSendEndpoint + Send, and registers every poison/data/flag key in L2KeysToCleanup (INTAKE-04 + PROBE-06 + net-zero, authored)."
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs"
      provides: "RealStack spike: dual Fault<T> capture + WRONGTYPE dispatch/result trips + verbatim re-inject + duplicate-collapse + negative command-fault proof"
      contains: "IConsumer<Fault<EntryStepDispatch>>"
      min_lines: 300
  key_links:
    - from: "FaultRecoverySpikeE2ETests probe bodies"
      to: "context.Message.Message"
      via: "double .Message unwrap into the 6-id + H capture tuple"
      pattern: "context\\.Message\\.Message"
    - from: "FaultRecoverySpikeE2ETests re-inject"
      to: "queue:{processorId:D} / queue:orchestrator-result"
      via: "bus.GetSendEndpoint(...).Send(inner)"
      pattern: "GetSendEndpoint\\(new Uri\\(\\$\"queue:"
    - from: "FaultRecoverySpikeE2ETests poison + teardown"
      to: "factory.L2KeysToCleanup"
      via: "every armed poison/data/flag key registered for net-zero deletion"
      pattern: "L2KeysToCleanup\\.Add"
---

<objective>
Author the entire `FaultRecoverySpikeE2ETests` RealStack proof in ONE pass — the clone of `IdempotentExactlyOnceE2ETests` plus the 6 fault-specific grafts (dual `Fault<T>` capture, double-`.Message` unwrap, WRONGTYPE dispatch + result trips, a-priori `resultH`, verbatim re-inject ×2, negative command-fault proof) — and prove it compiles clean and causes zero hermetic regression.

Purpose: De-risk the v3.7.0 Keeper milestone's load-bearing assumption (bind → unwrap → re-inject-by-type → `flag[H]`-collapse) with a standing RealStack regression guard. This plan delivers the committed, compiling artifact; Plan 02 runs it live (operator-gated) and adds the close gate.

Output: `tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs` — compiles, RealStack-tagged (so excluded from the hermetic suite), authored against the proven clone rig + git-recovered Phase-32 recipes.

This is a PROOF spike (D-03): NO production `src/` changes, NO Keeper code, NO metric work (D-11). ~80% of the file is a verbatim clone; ~15% is git-recovered; ~5% is the genuinely new dual-`Fault<T>` capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-CONTEXT.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-RESEARCH.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-PATTERNS.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from RESEARCH/PATTERNS (line-verified). Do NOT re-explore. -->

Clone source (copy whole-file, then graft): tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs
  - Reuse VERBATIM: RealStackWebAppFactory nested class (:510-588), SeedProcessorAsync/SeedStepAsync/SeedWorkflowAsync (:442-499),
    embedded SourceHash reflection (:95-97), PollForHealthyLivenessAsync (:345-380), ScanKeys/PollForNewKeyAsync (:384-438),
    PrewriteFlagPendingAsync/PollForFlagAckAsync (:227-260), BuildEffectQuery/CountEsHitsAsync + ElasticsearchTestClient.PollEsForLog (:283-341),
    HostRedis const + DownstreamEffectMessage="step output written content-addressed" (:71,501),
    the short-lived IBusControl lifecycle bracket — IBusControl is NOT IAsyncDisposable, use StartAsync/try/finally StopAsync (:264-279).

Inner messages (both implement IExecutionCorrelated — 6 ids + H):
  - src/Messaging.Contracts/EntryStepDispatch.cs (: IExecutionCorrelated, ctor (wfId, stepId, procId, Payload), init CorrelationId/ExecutionId/EntryId/H)
  - src/Messaging.Contracts/ExecutionResult.cs (: IExecutionCorrelated)
  - src/Messaging.Contracts/IExecutionCorrelated.cs — CorrelationId, WorkflowId, StepId, ProcessorId, EntryId, ExecutionId (+ H)

Identity hashing (NEVER re-implement SHA-256): src/Messaging.Contracts/Hashing/MessageIdentity.cs
  - ComputeH(corr, wf, step, proc, entryId), HashBlob(payload), HashManifest(manifestJson), EntryEntryId(corr, stepId)

L2 keys (poison targets + net-zero scan): src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
  - ExecutionData(string hash) => skp:data:{hash}, Flag(string h) => skp:flag:{h}

Endpoints: src/Messaging.Contracts/OrchestratorQueues.cs — Result = "orchestrator-result"
  - dispatch re-inject URI: queue:{processorId:D}   |   result re-inject URI: queue:{OrchestratorQueues.Result}

Command messages (faults must NOT route to the spike — ICorrelated only, NOT IExecutionCorrelated):
  - src/Messaging.Contracts/StartOrchestration.cs, src/Messaging.Contracts/StopOrchestration.cs

Trip surfaces (poison the INFRA Redis op, no catch):
  - dispatch: src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs:178 output StringSetAsync (WRONGTYPE target)
  - result:   src/Orchestrator/Consumers/ResultConsumer.cs:65 flag[m.H] StringGetAsync (FIRST Redis op, WRONGTYPE target)

Receiver dedup gates (ride, do not add — both survived 32.1 revert byte-intact):
  - processor: EntryStepDispatchConsumer.cs:76-84 flag[dispatch.H]=="Ack"
  - orchestrator: ResultConsumer.cs:65-72 flag[m.H]=="Ack"
</interfaces>

<git_recovery>
<!-- Paste-ready recovered recipes. Run these git show commands to lift the exact code. -->
git show 3aca386:tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs   # double-.Message probe shape (lines 42-52)
git show a6c6825:tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs  # ArmWrongTypePoisonAsync (251-257) + trip-send (142-160)
</git_recovery>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Clone the rig + graft dual Fault<T> capture probes (GRAFT 1+2)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs (clone source — whole file)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-PATTERNS.md (GRAFT 1+2 sections, "Rig copied 1:1" list)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-RESEARCH.md (Pattern 1, Code Examples "Inner-message unwrap probe")
    - src/Messaging.Contracts/EntryStepDispatch.cs, src/Messaging.Contracts/ExecutionResult.cs, src/Messaging.Contracts/IExecutionCorrelated.cs
  </read_first>
  <behavior>
    - The new file exists with the clone's full rig copied verbatim (RealStackWebAppFactory, seed helpers, SourceHash reflection, liveness/scan/flag pollers, ES effect query, consts).
    - It carries `[Trait("Category","E2E")]` + `[Trait("Category","RealStack")]` + `[Collection("Observability")]` so the hermetic filter excludes it.
    - Two file-local/nested probe classes exist: `FaultDispatchProbe : IConsumer<Fault<EntryStepDispatch>>` and `FaultResultProbe : IConsumer<Fault<ExecutionResult>>`, each unwrapping `context.Message.Message` into a shared `ConcurrentBag` capture tuple of (H, corr, wf, step, proc, entry, exec, inner).
  </behavior>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs`. Copy the WHOLE clone source `IdempotentExactlyOnceE2ETests.cs` verbatim as the base, then:
    (a) Rename the class to `FaultRecoverySpikeE2ETests` (keep `sealed`, keep the three traits at :64-67 exactly — `[Trait("Category","E2E")]`, `[Trait("Category","RealStack")]`, `[Collection("Observability")]`). Add `using System.Collections.Concurrent;` for the capture bag.
    (b) Keep VERBATIM (do NOT re-author): the `RealStackWebAppFactory` nested class (clone :510-588 — env host overrides RMQ 5673 / Redis 6380 / PG 5433 / OTLP 4317, `L2KeysToCleanup`, `ParentIndexMembersToSrem`, net-zero `DisposeAsync`); `SeedProcessorAsync`/`SeedStepAsync`/`SeedWorkflowAsync` (:442-499); the embedded SourceHash reflection (:95-97 — `typeof(global::Processor.Sample.SampleProcessor).Assembly ... a.Key=="SourceHash"`); `PollForHealthyLivenessAsync` (:345-380); `ScanKeys`/`PollForNewKeyAsync` (:384-438); `PrewriteFlagPendingAsync`/`PollForFlagAckAsync` (:227-260); `BuildEffectQuery`/`CountEsHitsAsync` + `ElasticsearchTestClient.PollEsForLog` (:283-341); the `HostRedis` const + `DownstreamEffectMessage = "step output written content-addressed"` (:71,501); the poll-timeout consts.
    (c) GRAFT 2 — add two probe classes (nested inside the test class or file-local). Lift the shape from `git show 3aca386:tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs` (:42-52). Body for `FaultDispatchProbe`:
        `public Task Consume(ConsumeContext<Fault<EntryStepDispatch>> context) { var m = context.Message.Message; _captured.Add((m.H, m.CorrelationId, m.WorkflowId, m.StepId, m.ProcessorId, m.EntryId, m.ExecutionId, m)); return Task.CompletedTask; }`
        `FaultResultProbe` is identical with `IConsumer<Fault<ExecutionResult>>` / inner `ExecutionResult`. Each takes the shared `ConcurrentBag<(string h, Guid corr, Guid wf, Guid step, Guid proc, Guid entry, Guid exec, object inner)>` via ctor. Use `context.Message.Message` (double `.Message`) — proven verbatim instance, NO header parsing, NO re-deserialize, NO fallback.
    (d) Replace the clone's single `[Fact]` test body with an empty (or `Skip`-less) `[Fact] public async Task FaultRecover_ReinjectByType_CollapsesDuplicate()` stub that declares `var ct = TestContext.Current.CancellationToken;`, `await using var factory = new RealStackWebAppFactory();` `await factory.InitializeAsync();` `using var client = factory.CreateClient();` and the SourceHash + seed scaffolding — leave the trip/capture/re-inject/assert sections to be filled by Tasks 2-3 (mark with `// TODO Task 2/3` comments). The file MUST compile after this task (RealStack-tagged so it never runs hermetically).
    Do NOT touch any `src/` file. Do NOT add metrics (D-11). Do NOT reference `L2ProjectionKeys.Cancelled` / `CancelledMarkerValue` / `WorkflowCancelled` (removed in the 32.1 revert).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs` exists.
    - `dotnet build SK_P.sln -c Release` reports **0 Warning / 0 Error**.
    - File contains `class FaultRecoverySpikeE2ETests`, `[Trait("Category", "RealStack")]`, `IConsumer<Fault<EntryStepDispatch>>`, `IConsumer<Fault<ExecutionResult>>`, and at least two `context.Message.Message` occurrences (one per probe).
    - File contains `m.CorrelationId`, `m.WorkflowId`, `m.StepId`, `m.ProcessorId`, `m.EntryId`, `m.ExecutionId`, and `m.H` (the 6-id tuple + H, INTAKE-02).
    - `grep -c "Cancelled\|WorkflowCancelled\|CancelledMarkerValue" FaultRecoverySpikeE2ETests.cs` == 0 (no reverted symbols).
    - `git diff --name-only` shows ONLY `tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs` (no `src/` changes — D-03).
  </acceptance_criteria>
  <done>The clone rig + dual `Fault<T>` capture probes are authored; the solution builds clean Release 0/0; the test is RealStack-tagged and excluded from the hermetic suite.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Graft WRONGTYPE dispatch + result trips and a-priori H computation (GRAFT 3+4)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs (the file from Task 1)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-PATTERNS.md (GRAFT 3 + GRAFT 4)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-RESEARCH.md (Pattern 2, 3, 4 + Pitfall 1 + Pitfall 3)
    - src/Messaging.Contracts/Hashing/MessageIdentity.cs (ComputeH/HashBlob/HashManifest/EntryEntryId)
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (ExecutionData/Flag)
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs (:162,178,196-216 — output write + resultH derivation chain)
    - src/Orchestrator/Consumers/ResultConsumer.cs (:60,65 — flag[m.H] StringGetAsync first op)
  </read_first>
  <behavior>
    - DISPATCH trip (the primary, unambiguous novel-risk proof): poison `L2ProjectionKeys.ExecutionData(MessageIdentity.HashBlob("spike-dispatch-trip"))` as a Redis LIST so the processor's `:178` output `StringSetAsync` throws WRONGTYPE every attempt; `Immediate(N)` exhausts; MassTransit publishes `Fault<EntryStepDispatch>`; the spike's `FaultDispatchProbe` captures it; the captured `H` equals `ComputeH(corr, wfId, stepId, procId, EntryEntryId(corr, stepId))`.
    - RESULT trip (the second-type proof): `resultH` is computed a-priori via the full echo chain; poison `L2ProjectionKeys.Flag(resultH)` so the orchestrator `ResultConsumer:65` first `StringGetAsync(Flag(m.H))` throws WRONGTYPE; `Fault<ExecutionResult>` published + captured; captured `H == resultH`. Pitfall-1 timing addressed (see action).
    - Every poison key is registered in `factory.L2KeysToCleanup`.
  </behavior>
  <action>
    Fill the trip sections of `FaultRecoverySpikeE2ETests.cs`.
    (1) Add the `ArmWrongTypePoisonAsync` helper VERBATIM from `git show a6c6825:tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs` (:251-257):
        `private static async Task ArmWrongTypePoisonAsync(string key, CancellationToken ct) { await using var mux = await ConnectionMultiplexer.ConnectAsync(HostRedis); var db = mux.GetDatabase(); await db.KeyDeleteAsync(key); await db.ListRightPushAsync(key, "poison"); }` (the LIST type forces WRONGTYPE on any subsequent String op).
    (2) DISPATCH trip (GRAFT 3, Pattern 2). `const string TripPayload = "spike-dispatch-trip";` `var poisonedKey = L2ProjectionKeys.ExecutionData(MessageIdentity.HashBlob(TripPayload));` `await ArmWrongTypePoisonAsync(poisonedKey, ct);` `factory.L2KeysToCleanup.Add(poisonedKey);` Build the dispatch (trip-send shape from `git a6c6825:142-160`): `var corr = NewId.NextGuid(); var entryId = MessageIdentity.EntryEntryId(corr, stepId); var dispatch = new EntryStepDispatch(wfId, stepId, procId, Payload: JsonSerializer.Serialize(TripPayload)) { CorrelationId = corr, ExecutionId = Guid.Empty, EntryId = entryId, H = MessageIdentity.ComputeH(corr, wfId, stepId, procId, entryId) };` Send it to `queue:{procId:D}` via the short-lived bus (GetSendEndpoint + Send) so the processor's `:178` output write hits WRONGTYPE every attempt → `Immediate(N)` exhausts → `Fault<EntryStepDispatch>` published → `FaultDispatchProbe` captures.
    (3) RESULT trip (GRAFT 4, Pattern 3+4). Compute `resultH` a-priori per the echo chain (SampleProcessor echoes payload): `var rPayload = "spike-result-trip"; var blobHash = MessageIdentity.HashBlob(rPayload); var manifestJson = JsonSerializer.Serialize(new[]{ blobHash }); var manifestEntryId = MessageIdentity.HashManifest(manifestJson); var resultH = MessageIdentity.ComputeH(rCorr, wfId, stepId, procId, manifestEntryId);` `var poisonedFlag = L2ProjectionKeys.Flag(resultH); factory.L2KeysToCleanup.Add(poisonedFlag);`
        Pitfall-1 (MUST address): do NOT poison `flag[resultH]` before the processor's own `EntryStepDispatchConsumer:210-212` pre-write of `flag[resultH]="Pending"` runs (that would trip a processor-side `Fault<EntryStepDispatch>` instead). Implement the post-pre-write window arm: drive a normal round-trip for `rCorr`, `await PollForFlagAckAsync`-style wait OR poll until `skp:flag:{resultH}` exists (the processor pre-write landed), THEN `await ArmWrongTypePoisonAsync(poisonedFlag, ct)` so the orchestrator's first `StringGetAsync(Flag(resultH))` (`ResultConsumer:65`) hits the LIST → WRONGTYPE → `Immediate(N)` exhausts → `Fault<ExecutionResult>` published → `FaultResultProbe` captures. Wrap the arm-window logic with a clear `// D-06 fallback: if the live window proves fragile, publish a synthetic Fault<ExecutionResult> (real inner ExecutionResult, H=resultH) here instead` comment + a `PublishSyntheticResultFaultAsync` helper stub the operator can switch to (keep the live path primary). Mark the dispatch-trip assertions as the standalone novel-risk proof and the result-trip as the second-type proof in xmldoc/comments.
    Poison ONLY the INFRA ops named above (Pitfall 3). Register EVERY armed key in `L2KeysToCleanup` (net-zero). No `src/` edits.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` reports **0 Warning / 0 Error**.
    - File contains `ArmWrongTypePoisonAsync`, `ListRightPushAsync`, `L2ProjectionKeys.ExecutionData(MessageIdentity.HashBlob(`, and `L2ProjectionKeys.Flag(resultH`.
    - File contains the a-priori chain literals: `HashBlob(`, `HashManifest(`, `JsonSerializer.Serialize(new[]` and `ComputeH(` for both the dispatch path (with `EntryEntryId`) and the result path (with `manifestEntryId`).
    - File contains a `// D-06` comment AND a synthetic-fallback helper (e.g. `PublishSyntheticResultFaultAsync`) for the result type only.
    - `grep -c "L2KeysToCleanup.Add" FaultRecoverySpikeE2ETests.cs` >= 3 (dispatch poison + result poison + scanned run keys).
    - Dispatch-trip code does NOT poison `flag[resultH]` before a processor-pre-write wait (grep shows the result-poison arm follows a flag-exists/Ack poll, addressing Pitfall 1).
    - `git diff --name-only` shows ONLY the test file (no `src/`).
  </acceptance_criteria>
  <done>Both WRONGTYPE trips are authored (dispatch unambiguous + result with Pitfall-1 window + D-06 synthetic fallback); a-priori H/resultH computed via MessageIdentity; all poison keys net-zero registered; Release builds 0/0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Graft verbatim re-inject ×2, duplicate-collapse assertion, and negative command-fault proof (GRAFT 5+6); hermetic regression gate</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs (the file from Task 2)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-PATTERNS.md (GRAFT 5 + GRAFT 6 + "Shared Patterns")
    - .planning/phases/33-fault-recovery-spike-de-risk/33-RESEARCH.md (Pattern 5, 6 + Pitfall 2 + Anti-Patterns)
    - src/Messaging.Contracts/OrchestratorQueues.cs (Result = "orchestrator-result")
    - src/Messaging.Contracts/StartOrchestration.cs, src/Messaging.Contracts/StopOrchestration.cs
  </read_first>
  <behavior>
    - Re-inject (INTAKE-04): the EXTRACTED `Fault<T>.Message` instance is forwarded VERBATIM (same H) to its origin endpoint by type — `queue:{inner.ProcessorId:D}` (dispatch), `queue:{OrchestratorQueues.Result}` (result) — via `GetSendEndpoint` + `Send` (NOT Publish, no orchestrator round-trip).
    - Duplicate-collapse (PROBE-06 / D-08): the same extracted instance is Sent TWICE with ONE `flag[H]="Pending"` seed and a flag-Ack wait between sends; the receiver's surviving Phase-31 `flag[H]` gate drops delivery 2; `CountEsHitsAsync` == 1 (not 2) over the 8s+ settle window.
    - Negative proof (INTAKE-01 negative / D-09): after publishing `Fault<StartOrchestration>` + `Fault<StopOrchestration>`, the spike's two execution-fault consumers record ZERO captures over a settle window.
    - The hermetic suite still passes with zero regression and the new file is correctly excluded.
  </behavior>
  <action>
    Fill the re-inject + assertion + negative sections, and close the net-zero teardown.
    (1) GRAFT 5 re-inject (Pattern 5, clone :272-273 + OrchestratorQueues.cs:16). For the captured dispatch: `var inner = (EntryStepDispatch)cap.inner; var ep = await bus.GetSendEndpoint(new Uri($"queue:{inner.ProcessorId:D}"));`. For the captured result: `var ep2 = await bus.GetSendEndpoint(new Uri($"queue:{OrchestratorQueues.Result}"));`. Forward the extracted `.Message` instance VERBATIM — NEVER hand-reconstruct from the 6 ids (drift risk on H), NEVER `Publish` (fans out / loops through orchestrator).
    (2) GRAFT 5/6 duplicate-collapse (Pattern 6, Pitfall 2). `await PrewriteFlagPendingAsync(cap.h);` ONCE only (re-arming Ack→Pending leaks the dup — Anti-Pattern). `await ep.Send(inner, ct);` (delivery 1 → effect, flips flag[H] Pending→Ack). `await PollForFlagAckAsync(cap.h, ct);` `await ep.Send(inner, ct);` (delivery 2 → flag==Ack → dropped by the receiver gate). Then assert ONE downstream effect via `BuildEffectQuery` + `ElasticsearchTestClient.PollEsForLog` + `CountEsHitsAsync == 1` (clone :283-341, 8s+ ingest-settle window). Make the dispatch-side collapse its own assertion block (the standalone novel-risk proof) distinct from the result-side.
    (3) GRAFT 6 negative (Pattern, D-09). Via the same in-test bus, `Publish` a `Fault<StartOrchestration>` and a `Fault<StopOrchestration>` (construct minimal instances — they carry only `WorkflowIds` + `CorrelationId`, are `ICorrelated` NOT `IExecutionCorrelated`). After a settle delay (mirror the `CountEsHitsAsync` window idiom, ~8s), assert the spike's capture bag count for these did not increase — i.e. the two execution-fault consumers recorded ZERO command-fault captures (the bindings are type-scoped to `Fault--EntryStepDispatch` / `Fault--ExecutionResult` exchanges only).
    (4) Net-zero teardown (Shared Patterns, clone :203-218). In teardown: best-effort `POST /api/v1/orchestration/stop` for the seeded workflowId(s); `foreach` scan `ScanKeys("data:*")`/`ScanKeys("flag:*")` and register any key not present in the before-snapshots into `factory.L2KeysToCleanup`; ensure every armed poison key (dispatch data + result flag) is registered. The IBusControl is bracketed Start/try/finally Stop (NOT IAsyncDisposable). Re-confirm NO `src/` changes.
    (5) Run the hermetic regression gate (this is the autonomously-verifiable signal — the live run is Plan 02, operator-gated).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Release; dotnet test tests/BaseApi.Tests -c Debug -- --filter-not-trait "Category=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` reports **0 Warning / 0 Error**.
    - `dotnet test tests/BaseApi.Tests -c Debug -- --filter-not-trait "Category=RealStack"` reports **0 failed** AND the new `FaultRecoverySpikeE2ETests` is NOT in the run list (RealStack-excluded; zero regression vs the prior 447-pass baseline — the new file adds 0 hermetic tests since it is RealStack-only).
    - File contains `GetSendEndpoint(new Uri($"queue:{inner.ProcessorId:D}"` (dispatch re-inject by type) and `GetSendEndpoint(new Uri($"queue:{OrchestratorQueues.Result}"` (result re-inject by type).
    - File contains exactly-once-collapse markers: `PrewriteFlagPendingAsync` called exactly once before the two `ep.Send`, `PollForFlagAckAsync` between them, and a `CountEsHitsAsync` assertion expecting `1`.
    - File contains `Publish` of `Fault<StartOrchestration>` AND `Fault<StopOrchestration>` followed by a zero-capture assertion (D-09 negative).
    - `grep -c "Publish<EntryStepDispatch>\|\\.Publish(inner\|Publish(dispatch" FaultRecoverySpikeE2ETests.cs` shows re-inject uses `Send`, NOT `Publish`, for the captured execution messages (only the negative command-faults are Published).
    - File contains a teardown `orchestration/stop` POST and `ScanKeys(` registration into `L2KeysToCleanup`.
    - `git diff --name-only` shows ONLY `tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs`.
  </acceptance_criteria>
  <done>Re-inject-by-type ×2, duplicate-collapse (CountEsHits==1), and the negative command-fault proof are authored; net-zero teardown closed; Release 0/0 and the hermetic suite passes with zero regression and the RealStack test excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike in-test IBusControl → live sk-rabbitmq | A NEW external subscriber binds the Fault--T fanout exchanges + Sends to origin queues (the only new transport surface). |
| test → live Redis (skp:*) | The spike arms WRONGTYPE poison LISTs and pre-seeds/scans flag/data keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Denial-of-service (operational integrity) | armed WRONGTYPE poison + run-produced skp:data/flag keys | mitigate | Every armed poison key + every scanned run key registered in `factory.L2KeysToCleanup` (Task 2+3); drained in `DisposeAsync`. Keeps the close-gate `redis-cli --scan` BEFORE==AFTER triple-SHA (net-zero). All keys are TTL'd/content-addressed; no no-TTL markers exist in this spike. |
| T-33-02 | Tampering (state leak) | re-injected / poison messages leaking into other RealStack tests' state | mitigate | Trip payloads are unique literals (`spike-dispatch-trip`/`spike-result-trip`) → content-addressed keys are spike-private; the seeded workflow is `POST /orchestration/stop`-ed in teardown so its cron stops churning flag keys (NET-ZERO-31). |
| T-33-03 | Information disclosure | bus credentials in the test | accept | guest/guest against local dev `sk-rabbitmq` (localhost:5673) — identical to the existing clone source; dev-only, no production secret, no new surface (ASVS V2 n/a per RESEARCH security domain). |
| T-33-04 | Spoofing (type-scope bypass) | spike consumer capturing a command-fault it must not see | mitigate | D-09 negative proof asserts ZERO captures after publishing `Fault<Start/Stop>Orchestration`; structurally the spike binds only `Fault--EntryStepDispatch`/`--ExecutionResult` exchanges. |

No **high**-severity threats: the spike adds one test file, no production attack surface, no auth, no external input (RESEARCH Security Domain — all bounded/TTL'd/net-zeroed).
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` → 0 Warning / 0 Error.
- `dotnet test tests/BaseApi.Tests -c Debug -- --filter-not-trait "Category=RealStack"` → 0 failed, RealStack test excluded, zero regression.
- `git diff --name-only` across all three task commits → ONLY `tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs` (D-03: no `src/` changes).
- File greps: dual `IConsumer<Fault<T>>`, double `context.Message.Message`, 6-id+H capture, both WRONGTYPE poison targets, both re-inject-by-type URIs, `Send` (not `Publish`) for execution messages, D-09 negative, net-zero registration, zero reverted symbols.
</verification>

<success_criteria>
- The spike file is authored, compiles Release 0/0, RealStack-tagged, and causes zero hermetic regression.
- INTAKE-01 bind (dual `Fault<T>` consumers + D-09 negative), INTAKE-02 unwrap (double `.Message` + 6-id+H), INTAKE-04 re-inject-by-type, PROBE-06 collapse (one flag[H] seed + Ack-wait + CountEsHits==1) are ALL authored against proven precedents.
- Pitfall-1 result-trip timing addressed (post-pre-write window arm) with the D-06 synthetic fallback present; dispatch-trip proof stands independently as the novel-risk carrier.
- The live execution + the close gate are deferred to Plan 02 (autonomous:false operator runbook).
</success_criteria>

<output>
After completion, create `.planning/phases/33-fault-recovery-spike-de-risk/33-01-SUMMARY.md`.
</output>
