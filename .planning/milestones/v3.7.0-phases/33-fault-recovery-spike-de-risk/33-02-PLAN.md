---
phase: 33-fault-recovery-spike-de-risk
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - scripts/phase-33-close.ps1
autonomous: false
requirements: [INTAKE-01, INTAKE-02, INTAKE-04, PROBE-06]

must_haves:
  truths:
    - "scripts/phase-33-close.ps1 exists, ParseFile-clean (exit 0), BOM-free, carrying no destructive FLUSHDB and no skp:cancelled scan-clean (the spike writes only TTL'd/content-addressed keys)."
    - "Against the rebuilt v3.7.0 compose stack the LIVE FaultRecoverySpikeE2ETests proves: both Fault<T> types captured (command-faults NOT), 6-id+H unwrapped, re-inject-by-type produces the effect exactly once, the duplicate collapses (CountEsHits==1) — and the close gate holds the redis/psql/rabbitmq triple-SHA BEFORE==AFTER (net-zero)."
    - "The D-10 {procId}_error retention decision (TTL'd forensic, source-agnostic DLQ-1 in Phase 36) is recorded in the SUMMARY; no _error topology change in this phase (D-11: no metric work)."
  artifacts:
    - path: "scripts/phase-33-close.ps1"
      provides: "v3.7.0 triple-SHA net-zero close gate (clone of phase-32.1-close.ps1, relabeled 32.1->33)"
      contains: "Phase 33 close gate"
  key_links:
    - from: "phase-33-close.ps1"
      to: "live RealStack stack (redis/psql/rabbitmq)"
      via: "3x GREEN full-suite + unfiltered triple-SHA BEFORE==AFTER"
      pattern: "redis-cli --scan"
---

<objective>
Author the `phase-33-close.ps1` net-zero close gate (byte-faithful clone of `phase-32.1-close.ps1`, relabeled 32.1→33, version stays v3.7.0... — NOTE: per the 32.1 precedent the script version label tracks the milestone; see action for the exact label), record the D-10 `_error` retention decision, and define the operator runbook that runs the LIVE `FaultRecoverySpikeE2ETests` + close gate against the rebuilt v3.7.0 compose stack.

Purpose: This is the de-risk SPIKE's authoritative live proof. The live trip → recover → re-inject → collapse + the negative command-fault proof require the full compose stack with REBUILT processor-sample/orchestrator/baseapi containers (embedded SourceHash must match the host build) — not runnable in a non-interactive executor. Per the Phase-31/31.1/32/32.1 precedent, this plan AUTHORS + COMMITS the close-gate script, RECORDS the decision, and documents the live run as an operator runbook in the SUMMARY Pending-Verification section. Do-not-block (auto-approve-human-verify policy).

Output: `scripts/phase-33-close.ps1`. The live execution is operator-gated.

NO production `src/` changes. NO Keeper code (D-03). NO `_error` topology change, NO metric work (D-10/D-11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-CONTEXT.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-RESEARCH.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-PATTERNS.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-VALIDATION.md
@.planning/phases/33-fault-recovery-spike-de-risk/33-01-PLAN.md

<interfaces>
<!-- Clone source for the close gate. Extracted from PATTERNS "No Analog Found / Contingent". -->
Close-gate clone source: scripts/phase-32.1-close.ps1 (337 lines, proven v3.6.0 triple-SHA gate)
  - Retain: zero-warning Debug+Release build; idempotent GET-or-create Processor seed (unique source-hash);
    3x consecutive GREEN full RealStack run; UNFILTERED redis-cli --scan + rabbitmqctl list_queues + psql \l SHA-256 BEFORE==AFTER;
    settle-drain for TTL-bounded skp:flag:*/skp:data:*; NO destructive whole-db flush; NO skp:cancelled:* scan-clean
    (the breaker is reverted — no such key is ever written; the spike writes only TTL'd/content-addressed keys).
  - phase-32.1 header line 1: "# Phase 32.1 close gate — v3.6.0 (triple-SHA)" → relabel to Phase 33 (see action for the exact version label).

Operator-gated build prerequisite (RESEARCH Runtime State Inventory / MEMORY):
  docker compose up -d --build processor-sample orchestrator baseapi-service   (embedded SourceHash must match host build)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author scripts/phase-33-close.ps1 (clone phase-32.1-close, relabel 32.1->33) + record D-10 _error decision</name>
  <read_first>
    - scripts/phase-32.1-close.ps1 (clone source — whole file)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-PATTERNS.md ("No Analog Found / Contingent" table — the phase-33-close.ps1 disposition)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-RESEARCH.md (Validation Architecture → Sampling Rate phase-gate; Runtime State Inventory → Build artifacts)
    - .planning/STATE.md (Phase 32.1 Plan 02 entry — the proven relabel protocol + verification grep set)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-CONTEXT.md (D-10 _error retention decision)
  </read_first>
  <behavior>
    - `scripts/phase-33-close.ps1` is a byte-faithful clone of `scripts/phase-32.1-close.ps1` with ONLY the named relabels (32.1→33). It ParseFile-cleanly, is BOM-free, carries no `FLUSHDB`, no `skp:cancelled` scan-clean, and retains the full triple-SHA / 3×GREEN / zero-warning-build protocol.
    - The D-10 `_error` retention decision is captured for the SUMMARY: keep `{procId}_error` as the TTL'd forensic copy consolidating source-agnostically into DLQ-1 (Phase 36 per INTAKE-03/DLQ-02), NEVER Keeper's worklist; operator triage axis is MECHANISM not origin component; Phase 33 RECORDS this only — no `_error` topology change.
  </behavior>
  <action>
    Copy `scripts/phase-32.1-close.ps1` verbatim to `scripts/phase-33-close.ps1`, then apply ONLY these relabels (mirror the proven 32-07/32.1-02 protocol — named relabels, no logic divergence):
    (a) Header line 1: from `# Phase 32.1 close gate — v3.6.0 (triple-SHA)` to `# Phase 33 close gate — v3.7.0 (triple-SHA)`. **IMPORTANT version note:** the 32.1 gate kept `v3.6.0` because phases 31/31.1/32/32.1 shared that milestone. Phase 33 is the FIRST phase of the v3.7.0 milestone (STATE.md milestone: v3.7.0; ROADMAP Phase 33 under v3.7.0) — so the label IS `v3.7.0`. Update every internal `v3.6.0` version token to `v3.7.0` accordingly (the 32.1 gate carried 6 such tokens — replace all 6).
    (b) Header line 2: relabel to a Phase-33 description, e.g. `# Fault-recovery spike — pub/sub bind → unwrap → re-inject-by-type → flag[H] collapse closeout`.
    (c) Replace every `32.1` / `phase-32.1-close` self-reference label with `33` / `phase-33-close`. Leave EXACTLY ONE provenance/clone-source comment referencing the clone origin (matching how the 32.1 gate kept a single phase-31 provenance line).
    (d) Keep VERBATIM: the steady-state Processor pre-flight GET-or-create on the unique source-hash; the `$services` health gate (processor-sample REQUIRED healthy); the UNFILTERED `redis-cli --scan` / `rabbitmqctl list_queues` / `psql \l` BEFORE+AFTER triple-SHA; the settle-drain for TTL-bounded `skp:flag:*`/`skp:data:*`; the 3-consecutive-GREEN RealStack loop; the Release+Debug zero-warning build; NO destructive whole-db flush. Do NOT add a `skp:cancelled:*` scan-clean (the breaker is reverted — no such key is ever written; the spike's poison/data/flag keys are all TTL'd/content-addressed and captured by the unfiltered `--scan` SHA + drained by the test's `L2KeysToCleanup`).
    (e) Add a header comment noting the spike's poison keys are net-zeroed by the TEST's `L2KeysToCleanup` (not the gate), and that the operator must rebuild `processor-sample orchestrator baseapi-service` before the live run (embedded SourceHash must match the host build).
    Write with NO BOM (ASCII/UTF8-no-BOM). Record the D-10 `_error` retention decision verbatim into the SUMMARY (Task is doc + script only). NO `src/` changes.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path ./scripts/phase-33-close.ps1), [ref]$null, [ref]$null) | Out-Null; if ($Error.Count -eq 0) { Write-Output 'PARSE OK' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-33-close.ps1` exists; `git ls-files scripts/ | grep -c phase-32.1-close` keeps the 32.1 gate (this is an ADD, not a rename — the prior gate stays).
    - ParseFile exit 0 (`PARSE OK`).
    - First 3 bytes are NOT the UTF-8 BOM `EF BB BF` (BOM-free).
    - `grep -c FLUSHDB scripts/phase-33-close.ps1` == 0.
    - `grep -c "skp:cancelled" scripts/phase-33-close.ps1` == 0.
    - `grep -c "redis-cli --scan" scripts/phase-33-close.ps1` == 12 (matches the 32.1 gate's unfiltered BEFORE/AFTER count).
    - `grep -c "v3.7.0" scripts/phase-33-close.ps1` == 6; `grep -cE "v3.6.0|v3.6.1" scripts/phase-33-close.ps1` == 0.
    - Header line 1 contains `Phase 33 close gate`.
    - `grep -cE "Phase 32.1|phase-32.1-close" scripts/phase-33-close.ps1` == 1 (the single provenance/clone-source line).
    - `git diff --name-only` shows ONLY `scripts/phase-33-close.ps1` (no `src/`, no test changes).
  </acceptance_criteria>
  <done>`scripts/phase-33-close.ps1` is authored (clone of phase-32.1-close relabeled 32.1→33, version v3.7.0, no cancelled scan-clean), ParseFile-clean + BOM-free; the D-10 `_error` retention decision is recorded for the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OPERATOR RUNBOOK — live FaultRecoverySpikeE2ETests + phase-33-close gate (autonomous:false)</name>
  <read_first>
    - .planning/phases/33-fault-recovery-spike-de-risk/33-VALIDATION.md (Manual-Only Verifications table + Phase Requirements → Observable Signal Map)
    - .planning/phases/33-fault-recovery-spike-de-risk/33-RESEARCH.md (Environment Availability + Common Pitfalls 1/2/3/5)
    - tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs (the Plan-01 artifact under test)
    - scripts/phase-33-close.ps1 (the gate from Task 1)
    - .planning/STATE.md (Phase 31.1 operational note: rebuild containers before the live gate; read GATE_*_EXIT from the gate output, not the bg-task wrapper exit)
  </read_first>
  <what-built>
    Plan 01 authored the full `FaultRecoverySpikeE2ETests` (dual `Fault<T>` capture, WRONGTYPE dispatch + result trips, verbatim re-inject ×2, duplicate-collapse, D-09 negative). Task 1 authored `scripts/phase-33-close.ps1`. Both are committed. The LIVE trip/recover/re-inject/collapse + the close gate need the full v3.7.0 compose stack up healthy with REBUILT containers — NOT runnable in this non-interactive executor. This is the operator gate.
  </what-built>
  <how-to-verify>
    1. Rebuild the three containers so the embedded `Processor.Sample` SourceHash matches the host build (Pitfall 5):
       `docker compose up -d --build processor-sample orchestrator baseapi-service`
       Wait until all three report healthy (processor-sample REQUIRED healthy).
    2. Run the live spike test:
       `dotnet test tests/BaseApi.Tests -- --filter-class "*FaultRecoverySpikeE2ETests"`
       EXPECT GREEN. Observable signals (33-VALIDATION map):
         - Both `Fault<EntryStepDispatch>` + `Fault<ExecutionResult>` captured (capture count ≥ 1 each) — INTAKE-01 bind.
         - Published `Fault<StartOrchestration>`/`Fault<StopOrchestration>` produce ZERO captures — INTAKE-01 negative (D-09).
         - Captured tuple's 6 ids all non-empty + `H` matches the a-priori `ComputeH(...)` — INTAKE-02.
         - A NEW `skp:data:*` (dispatch) / advance effect (result) appears for the re-injected identity at the correct origin endpoint — INTAKE-04.
         - `CountEsHitsAsync == 1` (NOT 2) over the 8s+ settle window for the duplicated re-inject — PROBE-06 collapse.
       If the live RESULT-trip timing proves fragile (Pitfall 1 — captures a `Fault<EntryStepDispatch>` when expecting `Fault<ExecutionResult>`), switch the result path to the committed D-06 synthetic-fallback helper (the dispatch trip alone carries the full novel risk; the milestone is de-risked either way). Record which path was used.
    3. Run the close gate:
       `pwsh -NoProfile -File ./scripts/phase-33-close.ps1`
       EXPECT GATE_EXIT=0: 3× GREEN full RealStack run + redis/psql/rabbitmq triple-SHA BEFORE==AFTER (net-zero). Read `GATE_*_EXIT` from the GATE OUTPUT, NOT the bg-task wrapper exit (MEMORY note). If a prior-phase stale/flaky ES assertion or dirty-BEFORE redis SHA surfaces, triage per the close-gate cadence notes (rebuild containers; ensure the spike's `L2KeysToCleanup` drained — a leaked poison/data/flag key churns the SHA).
    4. Report the three results (container health, test GREEN/RED, GATE_EXIT). INTAKE-01/02/04 + PROBE-06 flip to complete + the plan/phase counter advances only on GATE_EXIT=0.
  </how-to-verify>
  <resume-signal>Type "approved" (with GATE_EXIT=0 + which result-trip path was used), or describe the failure for triage.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close gate → live infra (redis/psql/rabbitmq) | The gate snapshots + compares triple-SHA; the live test mutates bounded TTL'd state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Denial-of-service (operational integrity) | leaked spike keys breaking the net-zero close-gate SHA | mitigate | The gate retains the unfiltered `redis-cli --scan` BEFORE==AFTER triple-SHA + settle-drain; the spike test registers every poison/data/flag key in `L2KeysToCleanup` (Plan 01) so the AFTER snapshot equals BEFORE. NO destructive FLUSHDB, NO no-TTL markers. |
| T-33-06 | Tampering | re-injected/poison messages leaking into other RealStack tests | mitigate | Unique trip-payload literals → spike-private content-addressed keys; `POST /orchestration/stop` in teardown stops the cron churn (NET-ZERO-31). |
| T-33-07 | Elevation/Spoofing | stale container SourceHash → liveness gate false-pass/timeout | mitigate | Operator rebuilds `processor-sample/orchestrator/baseapi-service` before the run (Pitfall 5); the gate's processor pre-flight asserts the unique source-hash row resolves. |

No **high**-severity threats: a close-gate clone + an operator-gated live run; no new production surface, no auth, all live mutation bounded/TTL'd/net-zeroed.
</threat_model>

<verification>
- `scripts/phase-33-close.ps1` ParseFile exit 0, BOM-free, `FLUSHDB`==0, `skp:cancelled`==0, `redis-cli --scan`==12, `v3.7.0`==6, `v3.6.0|v3.6.1`==0, header "Phase 33 close gate", single phase-32.1 provenance line.
- `git diff --name-only` (Task 1) → ONLY `scripts/phase-33-close.ps1`.
- LIVE (operator gate, autonomous:false): rebuilt containers healthy → `*FaultRecoverySpikeE2ETests` GREEN → `phase-33-close.ps1` GATE_EXIT=0 (3×GREEN + triple-SHA BEFORE==AFTER).
- D-10 `_error` retention decision recorded in the SUMMARY; no `_error` topology change, no metric work (D-11).
</verification>

<success_criteria>
- `scripts/phase-33-close.ps1` authored + committed (clone of phase-32.1-close, relabeled 32.1→33, v3.7.0, no cancelled scan-clean), ParseFile-clean + BOM-free.
- The operator runbook for the live spike + close gate is documented in the SUMMARY Pending-Verification section (do-not-block per the Phase-31/32 precedent).
- The D-10 `_error` retention decision is recorded.
- On the operator gate (GATE_EXIT=0): INTAKE-01/02/04 + PROBE-06 are proven live; net-zero holds.
</success_criteria>

<output>
After completion, create `.planning/phases/33-fault-recovery-spike-de-risk/33-02-SUMMARY.md` (include a Pending-Verification section with the full operator runbook + failure-triage, and the recorded D-10 decision).
</output>
