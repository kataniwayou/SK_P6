---
phase: 35-fault-intake-correlation
plan: 03
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs
autonomous: false
requirements: [INTAKE-03, KMET-04]
must_haves:
  truths:
    - "A live WRONGTYPE trip publishes a real Fault<EntryStepDispatch>; the RUNNING Keeper container (not an in-test bus) consumes it and emits an ES log carrying the propagated correlationId + execution-scope ids (SC3)"
    - "The Keeper-emitted ES log is correlated to the original execution: attributes.CorrelationId == the tripped message's correlationId AND attributes.StepId == the tripped step (proves the manual CorrelationId scope works end-to-end)"
    - "The separation property holds (INTAKE-03 slice): Keeper recovers off the Fault<T> pub/sub stream (its keeper-fault-recovery queue binds the fault exchanges) and the intake fires off the Fault<T> event — Keeper never reads the _error queue"
    - "The test is net-zero (skp:* triple-SHA BEFORE==AFTER): all run-minted data:/flag:/poison keys registered for teardown, workflow stopped"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs"
      provides: "RealStack SC3 proof — Keeper container correlated-ES-log assertion"
      contains: "service.name"
  key_links:
    - from: "tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs"
      to: "the running Keeper container's ES logs"
      via: "PollEsForLog on resource.attributes.service.name=keeper + attributes.CorrelationId + attributes.StepId + body.text"
      pattern: "PollEsForLog"
user_setup:
  - service: docker-compose-stack
    why: "SC3 proves the RUNNING Keeper container emits the correlated log — requires the full v3.7.0 stack up healthy with the Keeper container REBUILT (embedded SourceHash must match this phase's code, else the old placeholder runs and no intake log appears)."
    dashboard_config:
      - task: "Rebuild + bring up the stack before the live run: docker compose up -d --build keeper processor-sample orchestrator baseapi-service"
        location: "host shell at repo root"
---

<objective>
Prove SC3 against the RUNNING Keeper container (D-09): a live WRONGTYPE trip publishes a real `Fault<EntryStepDispatch>`, the Keeper container consumes it, and `PollEsForLog` asserts the Keeper-emitted ES log is correlated to the original execution by `correlationId` + `StepId`. This also confirms the INTAKE-03 Phase-35 separation slice: Keeper recovers off the `Fault<T>` pub/sub stream (its intake fires off the published fault event), never the `_error` queue.

Per the established Phase-31..34 precedent, the LIVE half is operator-gated (the authored test + a runbook complete the plan; the live `Assert.NotNull(hit)` GREEN against the rebuilt stack flips SC3 to proven). The test is a SIBLING of the standing Phase-33 spike (clone the harness, do NOT mutate the spike — keep its invariant isolated).

Purpose: SC3 end-to-end correlation proof + the INTAKE-03 Phase-35 separation confirmation (the consolidated TTL'd DLQ-1 BUILD is Phase 36 — do NOT build it here).

Output: one RealStack test `KeeperFaultIntakeE2ETests.cs` + an operator runbook in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-fault-intake-correlation/35-CONTEXT.md
@.planning/phases/35-fault-intake-correlation/35-RESEARCH.md
@.planning/phases/35-fault-intake-correlation/35-PATTERNS.md
@.planning/phases/35-fault-intake-correlation/35-02-SUMMARY.md

<interfaces>
<!-- Reuse-verbatim harness pieces. From tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs. -->

- Traits/collection: `[Trait("Category","E2E")]` + `[Trait("Category","RealStack")]` + `[Collection("Observability")]`.
- `RealStackWebAppFactory` + env-var host overrides incl. `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317` (factory ~:743-820).
- Embedded SourceHash reflection off `Processor.Sample.SampleProcessor`; `SeedProcessorAsync`/`SeedStepAsync`/`SeedWorkflowAsync` with `cron:"* * * * *"`.
- `PollForHealthyLivenessAsync(procId, ct)` liveness gate.
- The proven WRONGTYPE live-trip: build the dispatch via `MessageIdentity.EntryEntryId`/`ComputeH`, then `ArmWrongTypePoisonAsync(L2ProjectionKeys.Flag(dispatchH))` as a LIST → `EntryStepDispatchConsumer` dedup-gate GET throws WRONGTYPE every delivery → `Immediate(N)` exhausts → `Fault<EntryStepDispatch>` fans out (recipe at `ArmWrongTypePoisonAsync` ~:381-387).
- Net-zero teardown: `factory.ParentIndexMembersToSrem` / `factory.L2KeysToCleanup` register workflow/step/poison keys + the run-minted data:/flag: keys; `POST /api/v1/orchestration/stop`.
- ES query builder `BuildEffectQuery` (~:518-534) + `ElasticsearchTestClient.PollEsForLog(query, EsPollTimeoutMs, ct)`; `EsPollTimeoutMs = 120_000`.
- ES field paths: `resource.attributes.service.name`, `attributes.CorrelationId`, `attributes.StepId`; log body matched on `body.text`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author KeeperFaultIntakeE2ETests — live-trip + Keeper-container correlated-ES-log assertion</name>
  <files>tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs (the FULL harness to clone — traits, factory, seed helpers, SourceHash reflection, liveness poll, ArmWrongTypePoisonAsync, net-zero teardown, BuildEffectQuery, PollEsForLog)
    - tests/BaseApi.Tests/Orchestrator/CorrelationPropagationE2ETests.cs (PollEsForLog + correlationId-keyed ES readback precedent)
    - tests/BaseApi.Tests/Observability/Helpers/EsIndexNames.cs (the exact field-path constants: ResourceAttributesFieldPath, CorrelationIdFieldPath, the body.text path)
    - src/Keeper/appsettings.json (confirm Service.Name = "keeper" — the ES service.name to filter on; compose sets no override)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs` as a SIBLING clone of `FaultRecoverySpikeE2ETests` (do NOT modify the spike — RESEARCH Open Q1). COPY verbatim the reusable rig: the three traits + `[Collection("Observability")]`, `RealStackWebAppFactory` + `InitializeAsync` env-var overrides (incl. `OTEL_EXPORTER_OTLP_ENDPOINT`), the embedded SourceHash reflection off `Processor.Sample.SampleProcessor`, the seed helpers (`cron:"* * * * *"`), `PollForHealthyLivenessAsync`, `ArmWrongTypePoisonAsync`, and the net-zero `skp:*` teardown (`ParentIndexMembersToSrem`/`L2KeysToCleanup`).

    CHANGE vs the spike (the SC3 delta):
    1. NO in-test Fault probe and NO re-inject — let the RUNNING Keeper container consume the published `Fault<EntryStepDispatch>` (D-09). Do NOT clear the poison after arming (you WANT the fault published and consumed by the container).
    2. Live-trip via the proven WRONGTYPE recipe: seed processor/step/workflow, poll `processor-sample` Healthy, build the entry-step dispatch with `MessageIdentity.EntryEntryId`/`ComputeH` to get `dispatchH` + capture the dispatch's `CorrelationId` (`dCorr`) and `StepId` (`stepId`), then `ArmWrongTypePoisonAsync(L2ProjectionKeys.Flag(dispatchH))` → the consumer's dedup-gate GET throws WRONGTYPE every delivery → `Immediate(N)` exhausts → `Fault<EntryStepDispatch>` fans out to `keeper-fault-recovery` → the Keeper container's `FaultEntryStepDispatchConsumer` emits the intake log.
    3. Assert the KEEPER container's correlated ES log: adapt `BuildEffectQuery` to filter `resource.attributes.service.name = "keeper"` + `attributes.CorrelationId = dCorr:D` + `attributes.StepId = stepId:D` + `wildcard body.text = "*keeper fault intake*"` (the D-08 phrasing chosen in Plan 02). `Assert.NotNull(hit)` via `PollEsForLog(query, EsPollTimeoutMs, ct)` (`EsPollTimeoutMs = 120_000`). The hit's presence with the matching `attributes.CorrelationId` is the SC3 + INTAKE-03-separation proof: the log fired off the `Fault<T>` event with the PROPAGATED correlationId (not a fresh Guid — proves the manual CorrelationId scope from Plan 02 works end-to-end through the container).
    4. Net-zero teardown: `POST /api/v1/orchestration/stop` + register all run-minted `skp:data:*`/`skp:flag:*` + the poison key for deletion.

    Resolve the `MassTransit.ExecutionResult` vs `Messaging.Contracts.ExecutionResult` ambiguity with the alias if the file references the result type (mirror the spike). Do NOT build any DLQ-1/TTL/`keeper-dlq` topology and do NOT touch any `_error` topology (Pitfall 4 — that is Phase 36).

    BUILD verification (autonomously runnable): `dotnet build SK_P.sln -c Release` (0/0) and confirm the RealStack file adds ZERO hermetic tests (it is RealStack-trait excluded from the fast suite): `dotnet test tests/BaseApi.Tests` stays GREEN at the prior count. Then author the operator runbook (see Task 2). The LIVE run itself is operator-gated.
  </action>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs` exists with `[Trait("Category","RealStack")]` (grep == 1)
    - `grep -c "service.name" tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs` >= 1 AND the query filters `"keeper"` (grep `keeper` in the service.name term >= 1)
    - `grep -c "PollEsForLog" tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs` >= 1
    - `grep -c "ArmWrongTypePoisonAsync" tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs` >= 1 (the proven live-trip)
    - `grep -c "attributes.CorrelationId\|CorrelationIdFieldPath" tests/...KeeperFaultIntakeE2ETests.cs` >= 1 AND `attributes.StepId` referenced (the correlation assertion)
    - `grep -c "keeper fault intake" tests/...KeeperFaultIntakeE2ETests.cs` >= 1 (the body.text wildcard matches the D-08 log phrasing from Plan 02)
    - NO scope creep: `grep -c "x-message-ttl\|x-dead-letter-exchange\|keeper-dlq" tests/...KeeperFaultIntakeE2ETests.cs` == 0 (Pitfall 4 — no DLQ-1/TTL/DLQ-2 build)
    - net-zero: the run-minted keys + poison registered via `L2KeysToCleanup`/`ParentIndexMembersToSrem`; `POST` `/orchestration/stop` in teardown (grep each >= 1)
    - `dotnet build SK_P.sln -c Release` exits 0 (0 Warning / 0 Error)
    - `dotnet test tests/BaseApi.Tests` GREEN at the prior hermetic count (the RealStack file adds 0 hermetic tests — `--list-tests` shows the new class only under RealStack)
  </acceptance_criteria>
  <verify>
    <automated>dotnet build SK_P.sln -c Release</automated>
  </verify>
  <done>KeeperFaultIntakeE2ETests authored (sibling clone, spike untouched): WRONGTYPE live-trip → Keeper-container correlated-ES-log assertion on service.name=keeper + CorrelationId + StepId + body.text; net-zero teardown; no DLQ-1/TTL scope creep; solution builds 0/0; hermetic suite GREEN (RealStack excluded).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): Operator-gated live SC3 run against the rebuilt Keeper container</name>
  <what-built>
    The SC3 RealStack proof `KeeperFaultIntakeE2ETests` is authored and the solution builds 0/0. The LIVE run requires the rebuilt v3.7.0 compose stack (Keeper container's code changed this phase — a stale SourceHash runs the old placeholder consumer and emits NO intake log, Pitfall 5). Per the Phase-31..34 operator-gate precedent, the live half is verified by an operator and is do-not-block.
  </what-built>
  <how-to-verify>
    1. Rebuild + bring up the stack (Keeper MUST be rebuilt): `docker compose up -d --build keeper processor-sample orchestrator baseapi-service`
    2. Wait for all four services Healthy (compose health gate).
    3. Run the SC3 test: `dotnet test tests/BaseApi.Tests -- --filter-class "*KeeperFaultIntakeE2ETests"`
    4. Expected: GREEN — `PollEsForLog` returns a hit with `resource.attributes.service.name = "keeper"`, `attributes.CorrelationId` == the tripped dispatch's correlationId, `attributes.StepId` == the tripped step, and `body.text` matching "keeper fault intake". This proves the running Keeper container emitted the correlated log end-to-end (SC3) off the `Fault<T>` event (INTAKE-03 separation).
    5. Net-zero check: no leftover `skp:*` keys after the run (teardown registers all run-minted + poison keys).
    Failure triage: if `PollEsForLog` times out with no `service.name=keeper` hit, the most likely cause is a stale Keeper container (rebuild step skipped) or `OTEL_EXPORTER_OTLP_ENDPOINT` not wired — see the runbook in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" once the live run is GREEN (or describe the failure for triage).</resume-signal>
  <action>Operator action (do-not-block per Phase-31..34 precedent): rebuild the Keeper container + bring up the stack, run the SC3 class-filtered RealStack test, confirm the correlated Keeper ES log hit, and verify net-zero skp:* — see <how-to-verify>. The executor authored the test in Task 1; this gate records the live result.</action>
  <verify><automated>docker compose up -d --build keeper processor-sample orchestrator baseapi-service; dotnet test tests/BaseApi.Tests -- --filter-class "*KeeperFaultIntakeE2ETests"</automated></verify>
  <done>The SC3 RealStack test is GREEN against the rebuilt stack: a Keeper-container ES log with service.name=keeper + attributes.CorrelationId == the tripped correlationId + attributes.StepId == the tripped step + body.text ~ "keeper fault intake"; net-zero skp:* after teardown.</done>
  <files>tests/BaseApi.Tests/Orchestrator/KeeperFaultIntakeE2ETests.cs</files>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ broker → running Keeper container | The live `Fault<T>` envelope (induced by the test's WRONGTYPE trip) crosses into the deployed Keeper container's consumer. Internal compose network, no external input; the test is the only producer of the trip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-08 | Repudiation | the SC3 correlation assertion | mitigate | The test asserts the Keeper log's `attributes.CorrelationId` == the ORIGINAL tripped correlationId (not a fresh Guid) — this is the end-to-end guard that the manual CorrelationId scope (Plan 02) actually propagates through the container, so a real fault is operator-traceable. |
| T-35-09 | DoS (test residue / non-net-zero state) | run-minted skp:* + poison keys | mitigate | Net-zero teardown registers every run-minted `data:`/`flag:` + the poison key for deletion and stops the workflow (`POST /orchestration/stop`); the close-gate triple-SHA stays BEFORE==AFTER. No `_error` topology is touched. |
| T-35-10 | Tampering (scope creep into Phase 36 topology) | the test body | accept→mitigate | The test only CONFIRMS the separation (binds fault exchanges, intake fires off the event); it builds NO DLQ-1/TTL/`keeper-dlq`. Acceptance grep `x-message-ttl|x-dead-letter-exchange|keeper-dlq` == 0 enforces this (Pitfall 4). |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` — 0/0.
- Full hermetic suite GREEN at the prior count (RealStack file adds 0 hermetic tests).
- LIVE (operator-gated): `*KeeperFaultIntakeE2ETests` GREEN against the rebuilt stack — the Keeper container emits the correlated ES log; net-zero `skp:*`.
- No DLQ-1/TTL/`keeper-dlq` topology introduced (Pitfall 4 grep == 0).
</verification>

<success_criteria>
- SC3: a faulted message processed by the running Keeper container produces an ES log correlated by correlationId + ids (operator-gated GREEN).
- INTAKE-03 Phase-35 slice: Keeper recovers off the `Fault<T>` pub/sub stream (intake fires off the event), never the `_error` queue; no DLQ-1/TTL build.
- Net-zero teardown; solution + hermetic suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/35-fault-intake-correlation/35-03-SUMMARY.md` (with a Pending-Verification / operator-runbook section for the live gate, per the Phase-31..34 precedent).
</output>
