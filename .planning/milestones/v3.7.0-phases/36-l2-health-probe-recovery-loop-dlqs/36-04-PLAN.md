---
phase: 36-l2-health-probe-recovery-loop-dlqs
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs
autonomous: false
requirements: [PROBE-03, PROBE-04, PROBE-05]
must_haves:
  truths:
    - "A RealStack E2E induces a live L2 outage that dead-letters both an EntryStepDispatch and an ExecutionResult to Keeper"
    - "On L2 return, Keeper re-injects each verbatim to its origin endpoint with exactly-once downstream effect (no duplicate)"
    - "The give-up path (L2 stays down past MaxAttempts) parks the original Fault<T> in keeper-dlq"
    - "The probe scratch-key family skp:keeper:probe:* is net-zero (TTL self-cleans)"
  artifacts:
    - path: "tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs"
      provides: "RealStack recover-both-paths + give-up proof (sibling of FaultRecoverySpikeE2ETests)"
      contains: "RealStack"
      min_lines: 80
  key_links:
    - from: "tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs"
      to: "live keeper container probe loop → re-inject | keeper-dlq"
      via: "ArmWrongTypePoisonAsync trip + PollEsForLog effect proof + keeper-dlq depth check"
      pattern: "ArmWrongTypePoisonAsync|PollEsForLog"
---

<objective>
Author the RealStack E2E that proves Phase 36's recovery engine LIVE: induce an L2 outage that dead-letters both an `EntryStepDispatch` and an `ExecutionResult` to Keeper, then prove (a) recover-both-paths — on L2 return Keeper re-injects each verbatim to origin with exactly-once downstream effect — and (b) the give-up path — L2 stays down past MaxAttempts → the original `Fault<T>` lands in `keeper-dlq`. Net-zero `skp:*` teardown including the new `skp:keeper:probe:*` scratch family.

Purpose: PROBE-03/04/05 LIVE half. Phase 36's bar is hermetic-green (Plans 01-03) + ONE RealStack recover/give-up pass (this plan). The 3xGREEN triple-SHA close gate is Phase 39's job, NOT this plan's.
Output: 1 RealStack test file (sibling of the standing FaultRecoverySpikeE2ETests rig). autonomous:false — the live run needs the rebuilt compose stack (operator gate, auto-approve-human-verify precedent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-CONTEXT.md
@.planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-RESEARCH.md
@.planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-PATTERNS.md
@.planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-VALIDATION.md
@.planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-02-SUMMARY.md
@.planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-03-SUMMARY.md

<interfaces>
<!-- The standing RealStack rig to clone. Copy verbatim — these are the proven primitives. -->

From tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs (PROVEN LIVE, GATE_EXIT=0):
- Traits (lines 54-56): `[Trait("Category","E2E")]` + `[Trait("Category","RealStack")]` + `[Collection("Observability")]`.
- Embedded SourceHash reflection (lines 101-103) — REBUILD the keeper container before the run (SourceHash must match — project gotcha).
- RealStackWebAppFactory host env overrides (lines 743-771): RMQ `localhost:5673`, Redis `localhost:6380`, `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317` (env var is the live knob — appsettings key is dead, project memory).
- `ArmWrongTypePoisonAsync` (lines 381-387) — LIST poison to trip a live Fault<T>: poison a GET key (the dedup-gate flag[H], a StringGetAsync → WRONGTYPE), NOT a SET/output key (project memory "RealStack fault-trip mechanics").
- Re-inject collapse proof (lines 213-231): re-inject delivery x2, receiver flag[H] gate collapses dup, `CountEsHitsAsync == 1`.
- `PollEsForLog` / `CountEsHitsAsync` (lines 541-576) — downstream-effect proof; ES message body is `body.text` (project memory).
- Net-zero teardown (lines 283-292, 802-819): `L2KeysToCleanup` + `ScanKeys("data:*"|"flag:*")` BEFORE/AFTER + `POST /api/v1/orchestration/stop`.

From Plan 02 (the live behavior under test): the keeper container now runs the probe loop → re-inject-by-type | keeper-dlq park. The RUNNING keeper container consumes the published Fault<EntryStepDispatch>/Fault<ExecutionResult> (D-09, like the Phase-35 KeeperFaultIntakeE2ETests) — this test does NOT run an in-test probe; it observes the deployed container's recovery.

From Plan 01: `L2ProjectionKeys.KeeperProbe(h)` = `skp:keeper:probe:{h}` (the new scratch family to scan in net-zero); `KeeperQueues.DeadLetter` = `keeper-dlq`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author KeeperRecoveryE2ETests (recover-both-paths + give-up)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/FaultRecoverySpikeE2ETests.cs (the rig to clone — traits, SourceHash reflection, RealStackWebAppFactory env overrides, ArmWrongTypePoisonAsync, PollEsForLog/CountEsHitsAsync, net-zero teardown)
    - tests/BaseApi.Tests/Keeper/KeeperFaultIntakeE2ETests.cs (the Phase-35 RealStack Keeper sibling — the running-keeper-container observe pattern, D-09; the closest precedent for observing the deployed container rather than an in-test probe)
    - .planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-PATTERNS.md (the "RealStack E2E" pattern-assignment — COPY-verbatim list + WHAT-TO-ADD: scan keeper:probe:* + recover-both-paths + give-up)
    - .planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-VALIDATION.md (the PROBE-03/04 live row + the Manual-Only kill-mid-loop runbook)
    - Project memory: "RealStack fault-trip mechanics" + "OTLP & real-stack E2E stack gotchas"
  </read_first>
  <files>tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs</files>
  <action>
Create `tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs` — a sibling clone of `FaultRecoverySpikeE2ETests` adapted to observe the deployed keeper container's recovery (D-09 — like KeeperFaultIntakeE2ETests, the RUNNING keeper consumes the Fault<T>; do NOT run an in-test probe).

COPY VERBATIM from the spike rig:
- The 3 traits (E2E + RealStack + Observability).
- The embedded SourceHash reflection read (note in a comment: rebuild keeper + processor/orchestrator/baseapi before the run — SourceHash must match, and the BaseConsole.Core error-transport change from Plan 03 means all three must be rebuilt).
- `RealStackWebAppFactory` host env overrides (RMQ 5673 / Redis 6380 / OTEL_EXPORTER_OTLP_ENDPOINT).
- `ArmWrongTypePoisonAsync` (LIST poison on a GET key — the dedup-gate flag[H], trips a live Fault<T>).
- `PollEsForLog` / `CountEsHitsAsync` (ES body = body.text; EsPollTimeoutMs window).
- Net-zero teardown: `L2KeysToCleanup` + `POST /api/v1/orchestration/stop`.

ADD (Phase-36 deltas):
1. RECOVER-BOTH-PATHS fact (`KeeperRecovery_RecoversBothPaths`, PROBE-03 live): start a workflow; trip a live L2 fault for an EntryStepDispatch (WRONGTYPE on flag[dispatchH]) so MT exhausts Immediate(N) and publishes Fault<EntryStepDispatch> → the running keeper probe loop starts; CLEAR the poison (simulate L2 return) within the probe window (DelaySeconds×MaxAttempts = 60s default — bring L2 back well before max); assert the keeper re-injected the verbatim dispatch to `queue:{ProcessorId:D}` and the downstream effect fired EXACTLY ONCE (`CountEsHitsAsync == 1`, the receiver flag[H] collapses any dup — PROBE-06). Repeat the trip+clear for an ExecutionResult (re-inject to `queue:orchestrator-result`) — use the spike's a-priori resultH derivation or the D-06 synthetic Fault<ExecutionResult> fallback if the result-hop Pitfall-1 window is fragile live (the spike documents both).
2. GIVE-UP fact (`KeeperRecovery_GivesUp_ParksToDlq`, PROBE-04 live): trip a live L2 fault and LEAVE the poison in place past MaxAttempts (use a Keeper appsettings/env override to a small MaxAttempts for the test, or wait the full 60s) so the probe loop exhausts → assert the original Fault<T> lands in `keeper-dlq` (check the queue depth via the management API / a bound probe consumer in-test, OR assert via an ES "give up" keeper log if the consumer logs the park). Then CLEAR the poison in teardown.
3. NET-ZERO delta: extend the BEFORE/AFTER scan to include the new scratch family `ScanKeys("keeper:probe:*")` (assert net-zero — the 30s TTL self-cleans); register any minted keys + the poison key in `L2KeysToCleanup`. Drain the keeper-dlq parked message in teardown (it is a terminal queue — purge it so the test is net-zero for the Phase-39 gate).

Name the facts so VALIDATION.md's filter matches: `--filter "Category=RealStack&FullyQualifiedName~KeeperRecovery"`.

Document in the file's doc-comment + the SUMMARY that the LIVE run is an OPERATOR GATE (auto-approve-human-verify precedent, Phases 33-35): the authored test is the deliverable; the live GREEN run + the kill-mid-loop runbook (VALIDATION.md Manual-Only) is the operator's responsibility, and Phase-39's close gate is the authoritative live signal.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs` exists with all 3 RealStack traits (`grep -c 'RealStack' <file>` >= 1; `grep -c '"E2E"' <file>` >= 1)
    - `grep -c 'ArmWrongTypePoisonAsync' <file>` >= 1 AND `grep -c 'PollEsForLog' <file>` >= 1 (the proven primitives cloned)
    - `grep -c 'keeper:probe:\*\|KeeperProbe\|keeper:probe' <file>` >= 1 (the new scratch family scanned in net-zero)
    - `grep -c 'keeper-dlq\|KeeperQueues.DeadLetter' <file>` >= 1 (give-up park asserted)
    - Facts named to match `FullyQualifiedName~KeeperRecovery`: `grep -c 'KeeperRecovery_RecoversBothPaths\|KeeperRecovery_GivesUp' <file>` >= 1
    - The new RealStack file adds 0 hermetic tests: full hermetic suite `dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"` reports 0 failed (the RealStack-trait file is excluded; zero regression)
    - `dotnet build SK_P.sln -c Release` reports 0 Warning / 0 Error
  </acceptance_criteria>
  <done>The RealStack E2E compiles, is correctly RealStack-trait-excluded from the hermetic suite (zero regression), and asserts recover-both-paths + give-up-park + net-zero scratch scan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator gate — live recover/give-up run</name>
  <action>
This is a human-verify checkpoint (auto-approve-human-verify precedent, Phases 33-35 — the executor does NOT block: it finalizes the authored deliverable and records the runbook below; the operator runs the live proof, and Phase-39's close gate is the authoritative live signal). The executor's job here is to confirm the authored test is hermetic-excluded-green and to write the runbook into the SUMMARY's Pending-Verification section.
  </action>
  <what-built>
The KeeperRecoveryE2ETests RealStack E2E (Task 1) — recover-both-paths (dispatch + result re-inject, exactly-once effect) + give-up (keeper-dlq park) + net-zero skp:keeper:probe:* scan. Authored + hermetic-excluded-green.
  </what-built>
  <how-to-verify>
Operator runbook (Phase-39 close gate is the authoritative live signal):
1. Rebuild the stack — ALL of keeper + processor-sample + orchestrator + baseapi-service must be rebuilt (the Plan-03 BaseConsole.Core error-transport change is embedded in all three console images, and the keeper SourceHash must match):
   `docker compose up -d --build keeper processor-sample orchestrator baseapi-service`
2. One-time broker hygiene (Pitfall 1/2): on the dev RabbitMQ, delete any pre-TTL `skp-dlq-1` and any orphan `{queue}_error` queues so the corrected x-message-ttl declaration takes effect and the snapshot is clean.
3. Wait for all containers healthy.
4. Run: `dotnet test tests/BaseApi.Tests --filter "Category=RealStack&FullyQualifiedName~KeeperRecovery"` → expect GREEN (recover-both-paths exactly-once + give-up keeper-dlq park).
5. Verify net-zero: `redis-cli --scan` shows NO lingering `skp:keeper:probe:*`; the keeper-dlq parked message was purged in teardown.
6. (Optional, VALIDATION.md Manual-Only) kill-mid-loop: trip a fault, `docker kill` keeper while the loop is mid-await, observe redelivery + loop restart in logs/ES, confirm no message loss (PROBE-05 at-least-once).
  </how-to-verify>
  <acceptance_criteria>
    - The authored test is hermetic-excluded-green (full hermetic suite 0 failed; the RealStack file adds 0 hermetic tests).
    - The operator runbook (rebuild all-3-consoles + broker hygiene + the RealStack filter command + net-zero check) is recorded in the SUMMARY Pending-Verification section.
  </acceptance_criteria>
  <resume-signal>Type "approved" to finalize (the authored test is the deliverable; live GREEN is operator-gated per the 33-35 precedent), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → live RabbitMQ/Redis | the E2E poisons a live L2 key and publishes/observes real Fault<T> traffic on the dev stack |
| live keeper container → origin endpoints / keeper-dlq | the deployed container re-injects / parks real messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14 | DoS | poison key / parked dlq message left on the dev stack drifting the Phase-39 close-gate triple-SHA | mitigate | Net-zero teardown: poison + minted keys registered in L2KeysToCleanup; `skp:keeper:probe:*` self-cleans via 30s TTL; the keeper-dlq parked message is purged in teardown (RealStack rig precedent). Asserted by the BEFORE/AFTER scan. |
| T-36-15 | Tampering | duplicate downstream effect from the re-inject | mitigate | `CountEsHitsAsync == 1` asserts exactly-once — the receiver's surviving Phase-31 flag[H] gate collapses the dup (PROBE-06). |
| T-36-16 | Information disclosure | E2E reads ES logs / broker depth | accept | Test-only, dev stack; operators already have access. No new exposure. |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` — 0 Warning / 0 Error.
- `dotnet test tests/BaseApi.Tests --filter "Category!=RealStack"` — 0 failed (the new RealStack file adds 0 hermetic tests; zero regression).
- LIVE (operator gate): `dotnet test --filter "Category=RealStack&FullyQualifiedName~KeeperRecovery"` GREEN after the rebuild + broker hygiene — recorded as the runbook, Phase-39 is authoritative.
</verification>

<success_criteria>
- The RealStack E2E asserts recover-both-paths (exactly-once effect on each re-inject) + give-up (keeper-dlq park) + net-zero scratch scan.
- Authored + hermetic-excluded-green; the live GREEN run is the operator gate (auto-approve-human-verify).
</success_criteria>

<output>
After completion, create `.planning/phases/36-l2-health-probe-recovery-loop-dlqs/36-04-SUMMARY.md`
</output>
