---
phase: 37-orchestrator-pause-resume-coordination
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Messaging/PauseResumeContractTests.cs
  - tests/BaseApi.Tests/Keeper/KeeperPausePublishTests.cs
  - tests/BaseApi.Tests/Orchestrator/Scheduling/PauseResumeSchedulingTests.cs
  - tests/BaseApi.Tests/Orchestrator/PauseResumeConsumerTests.cs
autonomous: true
requirements: [PAUSE-01, PAUSE-02, PAUSE-03, PAUSE-04, PAUSE-05]

must_haves:
  truths:
    - "Four RED test files exist that name every contract, scheduler method, consumer, and Keeper publish behavior this phase ships"
    - "Tests compile-fail or assert-fail ONLY because the production symbols don't exist yet (RED), not because of harness errors"
    - "Each test passes TestContext.Current.CancellationToken to every Quartz call (xUnit1051)"
    - "Each test uses a unique quartz.scheduler.instanceName = test-{Guid:N} RAM scheduler"
  artifacts:
    - path: "tests/BaseApi.Tests/Messaging/PauseResumeContractTests.cs"
      provides: "PAUSE-01 contract-shape assertions (ICorrelated, WorkflowId, H, CorrelationId)"
      contains: "PauseResumeContract"
    - path: "tests/BaseApi.Tests/Keeper/KeeperPausePublishTests.cs"
      provides: "PAUSE-01 Keeper publish-site assertions (PauseWorkflow at intake, ResumeWorkflow on Recovered, nothing on GaveUp)"
      contains: "KeeperPausePublish"
    - path: "tests/BaseApi.Tests/Orchestrator/Scheduling/PauseResumeSchedulingTests.cs"
      provides: "PAUSE-02/03/05 RAM-scheduler assertions (Paused / fresh Normal / ignore None+Normal)"
      contains: "PauseSuppressesFire"
    - path: "tests/BaseApi.Tests/Orchestrator/PauseResumeConsumerTests.cs"
      provides: "PAUSE-04 idempotency under serial replay"
      contains: "PauseResumeIdempotent"
  key_links:
    - from: "PauseResumeSchedulingTests.cs"
      to: "Orchestrator.Scheduling.WorkflowScheduler"
      via: "GetTriggerState(new TriggerKey(jobId.ToString(\"D\")))"
      pattern: "new TriggerKey\\(.*ToString\\(\"D\"\\)\\)"
---

<objective>
Create the four Wave 0 RED test files defined by 37-VALIDATION.md. These tests encode the
acceptance contract for every requirement (PAUSE-01..05) BEFORE any production code exists, so the
implementation plans (02, 03, 04) have an executable target. The tests will fail to compile / fail
assertions until the contracts, scheduler methods, consumers, and Keeper publish sites land — that
RED state is the intended Wave 0 output.

Purpose: Nyquist compliance — every downstream implementation task has an `<automated>` verify that
points at a test file created here. No implementation is written against an unspecified target.
Output: Four xUnit test files (RED) using real Quartz RAMJobStore + NSubstitute, mirroring the
existing test analogs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-orchestrator-pause-resume-coordination/37-CONTEXT.md
@.planning/phases/37-orchestrator-pause-resume-coordination/37-RESEARCH.md
@.planning/phases/37-orchestrator-pause-resume-coordination/37-PATTERNS.md
@.planning/phases/37-orchestrator-pause-resume-coordination/37-VALIDATION.md

<interfaces>
<!-- Symbols these tests reference. They DO NOT EXIST YET — that is why the tests are RED. -->
<!-- Plans 02/03/04 create them. Names/signatures are fixed here so impl matches the tests. -->

Contracts (Plan 02 creates — src/Messaging.Contracts/):
```csharp
public sealed record PauseWorkflow(Guid WorkflowId, string H) : ICorrelated { public Guid CorrelationId { get; init; } }
public sealed record ResumeWorkflow(Guid WorkflowId, string H) : ICorrelated { public Guid CorrelationId { get; init; } }
```

Scheduler (Plan 02 adds to src/Orchestrator/Scheduling/WorkflowScheduler.cs):
```csharp
public Task PauseAsync(Guid jobId, CancellationToken ct);                 // wraps IScheduler.PauseJob(JobKey)
public Task<TriggerState> GetTriggerStateAsync(Guid jobId, CancellationToken ct); // wraps GetTriggerState(TriggerKey)
// plus: every trigger now stamped .WithIdentity(new TriggerKey(jobId.ToString("D")))
```

Existing harness to reuse (already in repo — READ these, do not recreate):
- tests/BaseApi.Tests/Orchestrator/SchedulingTests.cs — NewRamScheduler idiom (lines 16-29), GetTriggersOfJob + state assertion (61-66)
- tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs — Build(...) wires WorkflowL1Store + WorkflowScheduler + WorkflowLifecycle + consumer with NullLogger (43-53); dispatch DidNotReceive idiom (187-189)
- tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs — Context<T>(message, ct) builds Substitute.For<ConsumeContext<T>>() (129-136)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract-shape + Keeper-publish RED tests (PAUSE-01)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs (Context<T> stub — lines 129-136)
    - src/Messaging.Contracts/StartOrchestration.cs (record shape to assert against)
    - src/Messaging.Contracts/ICorrelated.cs (the interface)
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs (publish sites: intake line ~49, Recovered Send ~55-56, GaveUp else ~58-64)
    - src/Keeper/Consumers/FaultExecutionResultConsumer.cs (sibling: intake ~50, Recovered ~56-57)
    - 37-VALIDATION.md (rows PAUSE-01) + 37-PATTERNS.md ("Keeper publish-site test" section, lines 431-437)
  </read_first>
  <action>
    Create tests/BaseApi.Tests/Messaging/PauseResumeContractTests.cs with class name containing
    `PauseResumeContract`. Assert (RED — these reference not-yet-existing records):
    - `typeof(PauseWorkflow).GetInterfaces()` contains `ICorrelated`; same for `ResumeWorkflow`.
    - Constructing `new PauseWorkflow(someGuid, "h-val") { CorrelationId = corr }` exposes
      `.WorkflowId == someGuid`, `.H == "h-val"`, `.CorrelationId == corr`. Same for `ResumeWorkflow`.
    Use the same namespace pattern as the existing Messaging tests (tests/BaseApi.Tests/Messaging/).

    Create tests/BaseApi.Tests/Keeper/KeeperPausePublishTests.cs with class name containing
    `KeeperPausePublish`. Using a `Substitute.For<ConsumeContext<Fault<EntryStepDispatch>>>()`
    (mirror OrchestratorTestStubs.Context<T>), drive `FaultEntryStepDispatchConsumer.Consume`:
    - Stub `recovery.RunAsync(...)` to return `ProbeOutcome.Recovered`; assert
      `context.Received(1).Publish(Arg.Any<PauseWorkflow>(), Arg.Any<CancellationToken>())` AND
      `context.Received(1).Publish(Arg.Any<ResumeWorkflow>(), Arg.Any<CancellationToken>())`, and that
      the published `PauseWorkflow.WorkflowId == inner.WorkflowId` and `.H == inner.H`.
    - Stub `recovery.RunAsync(...)` to return `ProbeOutcome.GaveUp`; assert
      `context.Received(1).Publish(Arg.Any<PauseWorkflow>(), ...)` but
      `context.DidNotReceive().Publish(Arg.Any<ResumeWorkflow>(), ...)` (D-09: GaveUp publishes no Resume).
    Mock the `recovery` (`L2ProbeRecovery`) dependency with NSubstitute the same way the existing Keeper
    consumer tests construct the consumer. If `L2ProbeRecovery` is not substitutable (concrete/sealed),
    leave a `// TODO Plan 04: assert via real recovery stub` and assert only the publish calls reachable
    via the mocked ConsumeContext — the RED target is the publish behavior, not the probe internals.
    Pass `TestContext.Current.CancellationToken` to every Consume call.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj 2>&1 | Select-String "PauseWorkflow|ResumeWorkflow|PauseResumeContract|KeeperPausePublish"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/BaseApi.Tests/Messaging/PauseResumeContractTests.cs exists; grep finds `PauseResumeContract` and `ICorrelated`.
    - File tests/BaseApi.Tests/Keeper/KeeperPausePublishTests.cs exists; grep finds `KeeperPausePublish`, `Received(1).Publish`, and `DidNotReceive().Publish`.
    - Build FAILS with errors naming `PauseWorkflow`/`ResumeWorkflow` (symbols not yet defined) — this RED state is correct and expected for Wave 0.
    - No `TestContext.Current.CancellationToken` omission on any Quartz/Consume call.
  </acceptance_criteria>
  <done>Two PAUSE-01 test files exist, reference the not-yet-created contracts and publish behavior, and fail to compile only because those production symbols are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Scheduler + consumer RED tests (PAUSE-02/03/04/05)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SchedulingTests.cs (NewRamScheduler idiom lines 16-29; GetTriggersOfJob + GetTriggerState assertion 61-66)
    - tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs (Build(...) wiring 43-53; dispatch DidNotReceive 187-189)
    - src/Orchestrator/Scheduling/WorkflowScheduler.cs (ScheduleAsync :26-48, RescheduleAsync :55-71, UnscheduleAsync :76-77 — the methods under test)
    - 37-VALIDATION.md (rows PAUSE-02/03/04/05) + 37-PATTERNS.md ("RAM-scheduler harness" + "State-assertion idiom" 396-429)
  </read_first>
  <action>
    Create tests/BaseApi.Tests/Orchestrator/Scheduling/PauseResumeSchedulingTests.cs. Build a real RAM
    scheduler per SchedulingTests.cs:21-28 (unique `quartz.scheduler.instanceName = test-{Guid:N}`),
    construct `WorkflowScheduler` with that scheduler + a `FakeTimeProvider`/`TimeProvider`. Tests (RED —
    `PauseAsync`/`GetTriggerStateAsync` don't exist yet):
    - `PauseSuppressesFire` (PAUSE-02): `ScheduleAsync(wf, jobId, cron, ct)`, then
      `PauseAsync(jobId, ct)`, assert
      `await scheduler.GetTriggerState(new TriggerKey(jobId.ToString("D")), ct) == TriggerState.Paused`.
      Also assert that AFTER a fresh `ScheduleAsync` (before pause) the same key is `TriggerState.Normal`
      — this proves the `.WithIdentity` stamping (the load-bearing Plan-02 change) is present.
    - `ResumeReschedulesFresh` (PAUSE-03): from a Paused state, run the resume sequence
      (GetTriggerStateAsync == Paused → UnscheduleAsync(jobId) → ScheduleAsync(wf, jobId, cron)); assert
      exactly one trigger via `GetTriggersOfJob(JobKey(jobId))`, `GetTriggerState(TriggerKey) == Normal`,
      and the trigger's `GetNextFireTimeUtc()` is in the future (no misfire).
    - `ResumeIgnoresStoppedAndRunning` (PAUSE-05/D-09): after `UnscheduleAsync(jobId)` assert
      `GetTriggerStateAsync(jobId, ct) == TriggerState.None`; after a plain `ScheduleAsync` assert
      `== TriggerState.Normal`. (These prove the resume guard's two ignore branches resolve to non-Paused.)
    Pass `TestContext.Current.CancellationToken` to EVERY Quartz call.

    Create tests/BaseApi.Tests/Orchestrator/PauseResumeConsumerTests.cs with class containing
    `PauseResumeIdempotent` (PAUSE-04). Wire `WorkflowL1Store` + `WorkflowScheduler` + the new
    `PauseWorkflowConsumer`/`ResumeWorkflowConsumer` mirroring StopConsumerLifecycleTests.Build(...).
    Seed one workflow into L1 (jobId + cron). Invoke `PauseWorkflowConsumer.Consume` TWICE serially, then
    `ResumeWorkflowConsumer.Consume` TWICE serially, over the SAME scheduler. Assert the end state is
    exactly one Normal trigger and `(await scheduler.GetJobKeys(GroupMatcher<JobKey>.AnyGroup(), ct)).Count == 1`
    (no orphan jobs). This proves idempotency on replay (D-07).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj 2>&1 | Select-String "PauseAsync|GetTriggerStateAsync|PauseWorkflowConsumer|ResumeWorkflowConsumer"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/BaseApi.Tests/Orchestrator/Scheduling/PauseResumeSchedulingTests.cs exists; grep finds `PauseSuppressesFire`, `ResumeReschedulesFresh`, `ResumeIgnoresStoppedAndRunning`, and `new TriggerKey(` with `ToString("D")`.
    - File tests/BaseApi.Tests/Orchestrator/PauseResumeConsumerTests.cs exists; grep finds `PauseResumeIdempotent` and `GetJobKeys`.
    - Every Quartz call in both files passes `TestContext.Current.CancellationToken` (no xUnit1051 violation in the eventual compile).
    - Build FAILS naming `PauseAsync`/`GetTriggerStateAsync`/`PauseWorkflowConsumer`/`ResumeWorkflowConsumer` — RED is correct for Wave 0.
  </acceptance_criteria>
  <done>Two test files exist covering PAUSE-02/03/04/05, asserting the deterministic-TriggerKey state model and serial idempotency, failing to compile only because the production scheduler methods and consumers are absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 adds test files only. No runtime surface, no bus, no input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-00 | Tampering | Test fixtures sharing a Quartz repository | mitigate | Each test uses a unique `quartz.scheduler.instanceName = test-{Guid:N}` (existing repo idiom), preventing cross-test state bleed. No security-relevant surface introduced by tests. |
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj` fails ONLY with errors naming the four
  not-yet-created production symbols (PauseWorkflow, ResumeWorkflow, PauseAsync/GetTriggerStateAsync,
  Pause/ResumeWorkflowConsumer). No harness/syntax errors.
- All four test files present at their VALIDATION.md paths.
</verification>

<success_criteria>
- Four RED test files exist, one per VALIDATION.md Wave 0 row.
- Tests encode PAUSE-01..05 acceptance via real RAMJobStore + NSubstitute, reusing the named analogs.
- RED state is purely "symbol does not exist yet" — confirming the implementation target is well-defined.
</success_criteria>

<output>
After completion, create `.planning/phases/37-orchestrator-pause-resume-coordination/37-01-SUMMARY.md`
</output>
