---
phase: 42-v3.7.0-docs-traceability-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every satisfied v3.7.0 REQ-ID checkbox in REQUIREMENTS.md reads [x] (INTAKE-01..04, PROBE-01..06, DLQ-01..04, PAUSE-01..05, KMET-01..04, plus already-checked KEEP/TEST/KHARD)"
    - "The traceability rows for INTAKE/PROBE/DLQ/PAUSE/KMET-04 read a Complete-style status (Phase-39 live gate) instead of 'Not started'"
    - "MLBL-01..05 rows exist in the traceability table mapped to Phase 38"
    - "The coverage footer states the COUNTED total (34 across phases 33-39, + KHARD-01..03 Phase 40) with an internally-consistent per-phase breakdown — the stale '29 requirements across 6 phases, 33-38' is gone"
    - "The stale Phase-42-scope NOTE block (the '> **NOTE (stale ...' paragraph) is removed"
    - "REQUIREMENTS.md has no UTF-8 BOM and no mojibake (no occurrences of the corrupt em-dash/×/→ byte sequences)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled v3.7.0 requirement checkboxes + traceability table + corrected coverage footer"
      contains: "MLBL-01"
  key_links:
    - from: "REQUIREMENTS.md checkboxes (lines ~24-58)"
      to: "REQUIREMENTS.md traceability table (lines ~94-127)"
      via: "every checked REQ-ID has a matching Complete-style traceability row"
      pattern: "MLBL-0[1-5].*Phase 38"
---

<objective>
Reconcile `.planning/REQUIREMENTS.md` to the COUNTED TRUTH of the delivered v3.7.0 milestone
(SC1 + SC2). Flip every satisfied requirement checkbox to `[x]`, rewrite the stale "Not started"
traceability rows to a Complete status, add the five missing MLBL-01..05 rows (Phase 38), and
correct the coverage footer + delete the stale NOTE.

Purpose: REQUIREMENTS.md currently lies about a fully-delivered, live-proven milestone — the
checkboxes read `[ ]`, the traceability says "Not started", MLBL is entirely absent, and the
footer count (29 / 6 phases / 33-38) predates the Phase-38 insertion and Phase-39 close gate.
`/gsd-complete-milestone` (run later) consumes this file; it must tell the truth first.

Output: A truthful, internally-consistent REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-v3.7.0-docs-traceability-reconciliation/42-CONTEXT.md

<counted_truth>
<!-- Pre-counted by the planner from REQUIREMENTS.md itself. The executor MUST re-count to confirm
     before writing; if the recount differs, the FILE wins and a one-line note is added (D-03). -->

v3.7.0 REQ-IDs defined as checkboxes in REQUIREMENTS.md (phases 33-39):
  KEEP-01,02,03 (3) · INTAKE-01,02,03,04 (4) · PROBE-01..06 (6) · DLQ-01,02,03,04 (4) ·
  PAUSE-01..05 (5) · KMET-01,02,03,04 (4) · TEST-01,02,03 (3)  =  29 in-file

  + MLBL-01..05 (5, Phase 38 — currently ABSENT from this file's traceability table)  =  34 across phases 33-39
  + KHARD-01,02,03 (3, Phase 40 gap-closure) — already present + already [x]

Per-phase breakdown (counted truth):
  33 = 4  (INTAKE-01, INTAKE-02, INTAKE-04, PROBE-06)
  34 = 3  (KEEP-01, KEEP-02, KEEP-03)
  35 = 2  (INTAKE-03, KMET-04)
  36 = 9  (PROBE-01..05, DLQ-01..04)
  37 = 5  (PAUSE-01..05)
  38 = 5  (MLBL-01..05)
  39 = 6  (KMET-01,02,03, TEST-01,02,03)
  -------------------------------------
  Total = 34 across phases 33-39   (the OLD footer's "38=6" was wrong: Phase 38 carries MLBL's 5, not 6)
  + KHARD-01..03 (Phase 40)

Checkboxes currently `[ ]` that must flip to `[x]` (satisfied + live-proven by the Phase-39 close gate):
  INTAKE-01, INTAKE-02, INTAKE-03, INTAKE-04,
  PROBE-01, PROBE-02, PROBE-03, PROBE-04, PROBE-05, PROBE-06,
  DLQ-01, DLQ-02, DLQ-03, DLQ-04,
  PAUSE-01, PAUSE-02, PAUSE-03, PAUSE-04, PAUSE-05,
  KMET-04
Already `[x]` (leave): KEEP-01/02/03, KMET-01/02/03, TEST-01/02/03, KHARD-01/02/03.

Traceability rows currently reading "Not started" that must become a Complete status:
  INTAKE-03, KMET-04 (Phase 35); PROBE-01..05, DLQ-01..04 (Phase 36); PAUSE-01..05 (Phase 37).
  (INTAKE-01/02/04 + PROBE-06 already read "Spike-proven LIVE"; KMET-01/02/03 + TEST-01/02/03 already "Complete".)
</counted_truth>

<encoding_hazard>
This repo's STATE/ROADMAP/REQUIREMENTS files mojibake under wholesale Set-Content/Write (BOM +
corrupted em-dashes —, ×, →). MUST use the Edit tool for targeted in-place edits only. NEVER
rewrite the whole file. The acceptance criteria include a BOM + mojibake grep.
</encoding_hazard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip satisfied v3.7.0 checkboxes [ ] → [x] and reconcile traceability rows</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being edited — read every REQ-ID checkbox + every traceability row)
    - .planning/phases/42-v3.7.0-docs-traceability-reconciliation/42-CONTEXT.md (D-01/D-02/D-03 — reconcile to counted truth, not the SC's example)
    - .planning/ROADMAP.md Phase 35/36/37/39 details (confirms these REQ-IDs are satisfied + live-proven by the Phase-39 close gate)
  </read_first>
  <action>
    Using the Edit tool for EACH targeted change (never a full-file rewrite):

    (a) RE-COUNT first: walk the `## v3.7.0 Requirements` section and confirm the checkbox set
        matches `<counted_truth>` above. If a discrepancy is found, the file's actual content wins
        (D-01) — flip exactly the REQ-IDs that are genuinely satisfied and add a one-line note for
        any genuine discrepancy (D-03); do NOT invent.

    (b) Flip these 20 checkboxes from `- [ ] **{ID}**` to `- [x] **{ID}**` (per D-01 — all
        satisfied + live-proven by the Phase-39 close gate, 3×500 GREEN triple-SHA):
        INTAKE-01, INTAKE-02, INTAKE-03, INTAKE-04,
        PROBE-01, PROBE-02, PROBE-03, PROBE-04, PROBE-05, PROBE-06,
        DLQ-01, DLQ-02, DLQ-03, DLQ-04,
        PAUSE-01, PAUSE-02, PAUSE-03, PAUSE-04, PAUSE-05,
        KMET-04.
        Leave KEEP-01/02/03, KMET-01/02/03, TEST-01/02/03, KHARD-01/02/03 as-is (already [x]).
        Change ONLY the checkbox glyph; do NOT touch the requirement prose.

    (c) Rewrite the traceability-table `Status` cell for each row currently reading exactly
        `Not started`, to a Complete status that names the Phase-39 live gate. Use this exact text
        for each (preserve the existing `| {ID} | {Phase column} |` prefix — only replace the
        trailing `Not started` status cell):
          - INTAKE-03, KMET-04 (Phase 35 rows): `Complete (Phase-39 live gate — 3×500 GREEN triple-SHA)`
          - PROBE-01..05, DLQ-01..04 (Phase 36 rows): `Complete (Phase-39 live gate — 3×500 GREEN triple-SHA)`
          - PAUSE-01..05 (Phase 37 rows): `Complete (Phase-39 live gate — 3×500 GREEN triple-SHA)`
        Do NOT alter rows that already read "Spike-proven LIVE" (INTAKE-01/02/04, PROBE-06) or
        "Complete" (KMET-01/02/03, TEST-01/02/03, KHARD rows).

    Keep all em-dashes (—) and the `·` separators byte-intact — do not let the editor normalize them.
  </action>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*\(INTAKE\|PROBE\|DLQ\|PAUSE\)" .planning/REQUIREMENTS.md` returns `0` (no unchecked INTAKE/PROBE/DLQ/PAUSE remain)
    - `grep -c "^- \[ \] \*\*KMET-04" .planning/REQUIREMENTS.md` returns `0`
    - `grep -c "^- \[x\] \*\*INTAKE-01" .planning/REQUIREMENTS.md` returns `1` (spot-check a flip)
    - `grep -c "^- \[x\] \*\*PAUSE-05" .planning/REQUIREMENTS.md` returns `1`
    - The traceability table contains no row whose status cell is exactly `Not started`: `grep -c "| Not started |" .planning/REQUIREMENTS.md` returns `0`
    - `grep -c "Phase-39 live gate" .planning/REQUIREMENTS.md` returns `>= 14` (the rewritten Phase-35/36/37 rows)
    - The 3 KEEP, 3 KMET-01/02/03, 3 TEST, 3 KHARD checkboxes are still `[x]` (unchanged)
    - Encoding: file has no UTF-8 BOM (first 3 bytes are NOT EF BB BF) AND `grep -c "â€"" .planning/REQUIREMENTS.md` returns `0` AND `grep -c "Ã—" .planning/REQUIREMENTS.md` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning/REQUIREMENTS.md'; $bytes=[IO.File]::ReadAllBytes($f); $bom = ($bytes[0]-eq0xEF -and $bytes[1]-eq0xBB -and $bytes[2]-eq0xBF); $c=Get-Content $f -Raw; $unchecked = ([regex]::Matches($c,'(?m)^- \[ \] \*\*(INTAKE|PROBE|DLQ|PAUSE|KMET-04)')).Count; $notstarted = ([regex]::Matches($c,'\| Not started \|')).Count; $moji = ([regex]::Matches($c,'â€”|Ã—')).Count; if($bom){throw 'BOM present'}; if($unchecked -ne 0){throw \"unchecked left: $unchecked\"}; if($notstarted -ne 0){throw \"Not started rows: $notstarted\"}; if($moji -ne 0){throw 'mojibake'}; Write-Host 'T1 OK'"</automated>
  </verify>
  <done>All 20 target checkboxes are [x]; no INTAKE/PROBE/DLQ/PAUSE/KMET-04 unchecked; no "Not started" status cells remain; ≥14 rows name the Phase-39 live gate; no BOM, no mojibake.</done>
</task>

<task type="auto">
  <name>Task 2: Add MLBL-01..05 traceability rows + correct the coverage footer + delete the stale NOTE</name>
  <read_first>
    - .planning/REQUIREMENTS.md (traceability table tail + the `**Coverage:**` footer line + the `> **NOTE (stale ...` block)
    - .planning/ROADMAP.md Phase 38 details (MLBL-01..05 mapped to Phase 38, "Uniform service_name + Instance Labels", completed 2026-06-06, verified 5/5)
    - .planning/phases/42-v3.7.0-docs-traceability-reconciliation/42-CONTEXT.md (D-02: headline shape; D-03: flag genuine discrepancies, never invent)
  </read_first>
  <action>
    Using the Edit tool for EACH targeted change (never a full-file rewrite):

    (a) Insert five MLBL rows into the traceability table. Place them in phase order — AFTER the
        PAUSE-05 row (Phase 37) and BEFORE the KMET-01 row (Phase 39) — so the table stays sorted
        by phase. Use exactly this block (em-dash glyph `—`, single `|`-delimited cells):
          | MLBL-01 | 38 — Uniform service_name + Instance Labels | Complete (38-02/38-04; live scrape gate, verified 5/5) |
          | MLBL-02 | 38 — Uniform service_name + Instance Labels | Complete (38-04; non-empty service_instance_id across runtime/HTTP/business) |
          | MLBL-03 | 38 — Uniform service_name + Instance Labels | Complete (38-01/38-03/38-04; DB-sourced processor name+version via MeterProviderHolder swap) |
          | MLBL-04 | 38 — Uniform service_name + Instance Labels | Complete (38-02; logs service.name kept bare, ES contract preserved) |
          | MLBL-05 | 38 — Uniform service_name + Instance Labels | Complete (38-02/38-04; PromQL consumers reconciled, 0 bare literals) |

    (b) Replace the opening traceability sentence on line ~92. OLD (verbatim):
          `Every REQ-ID maps to exactly one phase (29 requirements across 6 phases, 33–38).`
        NEW:
          `Every REQ-ID maps to exactly one phase (34 requirements across phases 33–39, + KHARD-01..03 gap-closure in Phase 40).`

    (c) Replace the `**Coverage:**` footer line (~129). OLD (verbatim):
          `**Coverage:** 29/29 requirements mapped (PROBE-06 → Phase 33 with the spike; DLQ-04 added → Phase 36). Per-phase counts: 33=4 · 34=3 · 35=2 · 36=9 · 37=5 · 38=6.`
        NEW:
          `**Coverage:** 34/34 requirements mapped across phases 33–39, + KHARD-01..03 gap-closure (Phase 40). Per-phase counts: 33=4 · 34=3 · 35=2 · 36=9 · 37=5 · 38=5 · 39=6 (= 34). All 34 satisfied + live-proven by the Phase-39 close gate (3×500 GREEN, triple-SHA net-zero, GATE substance passed); KHARD-01..03 closed the recovery-hardening tech-debt in Phase 40.`
        (RE-COUNT before writing: if your count differs from 34 / this breakdown, the file wins —
        write the counted total and add a one-line note explaining the genuine discrepancy, per D-03.
        Use the `·` separator glyph and `—`/`–` dash glyphs byte-intact.)

    (d) DELETE the entire stale NOTE blockquote — the paragraph beginning
          `> **NOTE (stale — full reconciliation is Phase 42's scope):**`
        through the end of that blockquote (it is the last content block of the file). Its premise
        ("predates the Phase-38 insertion ... Phase 42 fixes") is now satisfied by this very plan,
        so it must not survive.
  </action>
  <acceptance_criteria>
    - `grep -c "MLBL-0[1-5] | 38" .planning/REQUIREMENTS.md` returns `5` (all five MLBL rows present, mapped to Phase 38)
    - The MLBL rows sit between PAUSE-05 and KMET-01 in the table (phase-sorted) — confirm by reading the table region
    - `grep -c "29 requirements across 6 phases" .planning/REQUIREMENTS.md` returns `0` (stale opener gone)
    - `grep -c "29/29 requirements mapped" .planning/REQUIREMENTS.md` returns `0` (stale footer gone)
    - `grep -c "34 requirements across phases 33" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "34/34 requirements mapped" .planning/REQUIREMENTS.md` returns `1`
    - Per-phase breakdown present: `grep -c "38=5 · 39=6" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "NOTE (stale" .planning/REQUIREMENTS.md` returns `0` (stale NOTE deleted)
    - Encoding: no UTF-8 BOM AND `grep -c "â€"" .planning/REQUIREMENTS.md` returns `0` AND `grep -c "Ã—" .planning/REQUIREMENTS.md` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning/REQUIREMENTS.md'; $bytes=[IO.File]::ReadAllBytes($f); $bom=($bytes[0]-eq0xEF -and $bytes[1]-eq0xBB -and $bytes[2]-eq0xBF); $c=Get-Content $f -Raw; $mlbl=([regex]::Matches($c,'MLBL-0[1-5] \| 38')).Count; $staleOpen=([regex]::Matches($c,'29 requirements across 6 phases')).Count; $staleFoot=([regex]::Matches($c,'29/29 requirements mapped')).Count; $newFoot=([regex]::Matches($c,'34/34 requirements mapped')).Count; $note=([regex]::Matches($c,'NOTE \(stale')).Count; $moji=([regex]::Matches($c,'â€”|Ã—')).Count; if($bom){throw 'BOM'}; if($mlbl -ne 5){throw \"MLBL rows: $mlbl\"}; if($staleOpen -ne 0){throw 'stale opener'}; if($staleFoot -ne 0){throw 'stale footer'}; if($newFoot -ne 1){throw 'new footer missing'}; if($note -ne 0){throw 'stale NOTE remains'}; if($moji -ne 0){throw 'mojibake'}; Write-Host 'T2 OK'"</automated>
  </verify>
  <done>Five MLBL-01..05 rows mapped to Phase 38 exist phase-sorted; opener + footer state the counted 34/34 with the 33=4..39=6 (=34) breakdown; stale "29 ..." strings and the stale NOTE are gone; no BOM, no mojibake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No attack surface — this plan edits a single local documentation file
(`.planning/REQUIREMENTS.md`). No runtime, no network, no inputs, no auth, no data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | (n/a) | local markdown edit | accept | No runtime/network/input/data-flow changes; documentation-only. The only integrity risk is editor-induced encoding corruption (BOM/mojibake), mitigated by the Edit-tool-only rule + BOM/mojibake grep in every task's acceptance criteria. |
</threat_model>

<verification>
- REQUIREMENTS.md: zero unchecked INTAKE/PROBE/DLQ/PAUSE/KMET-04 checkboxes; zero "Not started" status cells.
- Five MLBL-01..05 rows present, mapped to Phase 38, phase-sorted between PAUSE-05 and KMET-01.
- Opener + footer state the counted 34/34 total with per-phase breakdown 33=4 · 34=3 · 35=2 · 36=9 · 37=5 · 38=5 · 39=6 (=34); KHARD-01..03 noted as Phase-40 gap-closure.
- Stale strings ("29 requirements across 6 phases", "29/29 requirements mapped", "NOTE (stale") fully removed.
- No UTF-8 BOM; no mojibake byte sequences.
</verification>

<success_criteria>
SC1 + SC2 of Phase 42 are met: all satisfied v3.7.0 checkboxes read [x] with Complete-style
traceability rows naming the Phase-39 live gate; MLBL-01..05 rows exist mapped to Phase 38; the
coverage footer states the correct counted totals; the stale 29/6-phases/33-38 framing and stale
NOTE are gone — all without introducing a BOM or mojibake.
</success_criteria>

<output>
After completion, create
`.planning/phases/42-v3.7.0-docs-traceability-reconciliation/42-01-SUMMARY.md`.
</output>
