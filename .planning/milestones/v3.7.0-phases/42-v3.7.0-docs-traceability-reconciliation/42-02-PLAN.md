---
phase: 42-v3.7.0-docs-traceability-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The ROADMAP.md Progress-table Phase-38 row reads its true plan count (4/4) and status Complete with the 2026-06-06 completion date"
    - "No Progress-table row in ROADMAP.md still reads '0/?' or 'Not started' for Phase 38"
    - "ROADMAP.md has no UTF-8 BOM and no mojibake byte sequences"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Progress table — Phase-38 row truthful"
      contains: "38. Uniform"
  key_links:
    - from: "ROADMAP.md Phase-38 Progress row"
      to: "Phase-38 directory plan files (38-01..38-04-PLAN.md)"
      via: "plan count 4/4 matches the four 38-*-PLAN.md files on disk"
      pattern: "4/4 .* Complete"
---

<objective>
Correct the ROADMAP.md Progress table so the Phase-38 row tells the truth (SC3): change the stale
`0/? | Not started | —` to `4/4 | Complete | 2026-06-06`.

Purpose: Phase 38 ("Uniform service_name + Instance Labels Across All Metrics") shipped 4 plans and
is verified Complete (see its phase-detail block + 38-VERIFICATION.md), but the Progress table at
the bottom of ROADMAP.md still shows it as un-started — the lone false row in an otherwise-Complete
v3.7.0 ladder. `/gsd-complete-milestone` reads this table.

Output: A truthful ROADMAP Progress table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<ground_truth>
Phase-38 directory `.planning/phases/38-metrics-service-instance-labels/` contains exactly four
plan files: 38-01-PLAN.md, 38-02-PLAN.md, 38-03-PLAN.md, 38-04-PLAN.md → plan count = 4/4.
The phase is Complete (completed 2026-06-06; verified 5/5 must-haves per 38-VERIFICATION.md and the
ROADMAP Phase-38 detail block at line ~290). The current Progress-table row reads:
  `| 38. Uniform `service_name` + Instance Labels Across All Metrics | v3.7.0 | 0/? | Not started | — |`
</ground_truth>

<encoding_hazard>
This repo's ROADMAP file mojibakes under wholesale Set-Content/Write (BOM + corrupted em-dashes —,
×, →, and the table-cell `—` placeholders). MUST use the Edit tool for a single targeted in-place
edit of the one Phase-38 Progress row. NEVER rewrite the whole file. The acceptance criteria
include a BOM + mojibake grep.
</encoding_hazard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Phase-38 plan count, then fix the Progress-table Phase-38 row</name>
  <read_first>
    - .planning/ROADMAP.md (the `## Progress` table near the end — locate the Phase-38 row reading `0/? | Not started`)
    - .planning/phases/42-v3.7.0-docs-traceability-reconciliation/42-CONTEXT.md (D-06: touch only ROADMAP.md / REQUIREMENTS.md / 39-VERIFICATION.md; SC3)
  </read_first>
  <action>
    (a) CONFIRM the plan count by listing the Phase-38 plan files:
        `Get-ChildItem .planning/phases/38-metrics-service-instance-labels/38-*-PLAN.md` — expect
        exactly 4 (38-01..38-04). Use the COUNTED number, not a hardcoded one; if it is not 4, write
        the counted value `{n}/{n}` and note the discrepancy in the SUMMARY (D-03).

    (b) Using the Edit tool, replace ONLY the cells of the single Progress-table Phase-38 row.
        Find the row (it is the one matching `| 38. Uniform` ... `| 0/? | Not started | — |`) and
        change its last three cells from `0/?` / `Not started` / `—` to `4/4` / `Complete` /
        `2026-06-06`. Preserve the leading `| 38. Uniform `service_name` + Instance Labels Across
        All Metrics | v3.7.0 |` prefix exactly (including the backtick-wrapped `service_name` and the
        column alignment). Do NOT touch any other row, the execution-order line, or any phase-detail
        block. Keep the em-dash glyph in OTHER rows byte-intact (only the Phase-38 row's trailing `—`
        date placeholder becomes the real date).
  </action>
  <acceptance_criteria>
    - The Phase-38 directory has exactly 4 plan files: `(Get-ChildItem .planning/phases/38-metrics-service-instance-labels/38-*-PLAN.md).Count` == 4
    - The Phase-38 Progress row now reads 4/4 + Complete: `grep -c "38. Uniform .*4/4 .* Complete .* 2026-06-06" .planning/ROADMAP.md` returns `1` (the row matches plan-count 4/4, status Complete, date 2026-06-06)
    - The stale cells are gone from the Phase-38 row: the Progress table no longer has a `38. Uniform` row containing `0/?` — `grep -c "38. Uniform.*0/?" .planning/ROADMAP.md` returns `0`
    - No OTHER Progress row was changed (Phase-39 still `4/4 .* Complete`; Phase-42 still `0/? .* Not started`): `grep -c "39. Keeper Observability.*4/4 .* Complete" .planning/ROADMAP.md` returns `1`
    - Encoding: ROADMAP.md has no UTF-8 BOM (first 3 bytes NOT EF BB BF) AND `grep -c "â€"" .planning/ROADMAP.md` returns `0` AND `grep -c "Ã—" .planning/ROADMAP.md` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$plans=(Get-ChildItem .planning/phases/38-metrics-service-instance-labels/38-*-PLAN.md).Count; if($plans -ne 4){throw \"plan count $plans\"}; $f='.planning/ROADMAP.md'; $bytes=[IO.File]::ReadAllBytes($f); $bom=($bytes[0]-eq0xEF -and $bytes[1]-eq0xBB -and $bytes[2]-eq0xBF); $c=Get-Content $f -Raw; $fixed=([regex]::Matches($c,'38\. Uniform.*4/4.*Complete.*2026-06-06')).Count; $stale=([regex]::Matches($c,'38\. Uniform.*0/\?')).Count; $p39=([regex]::Matches($c,'39\. Keeper Observability.*4/4.*Complete')).Count; $moji=([regex]::Matches($c,'â€”|Ã—')).Count; if($bom){throw 'BOM'}; if($fixed -ne 1){throw 'phase-38 row not fixed'}; if($stale -ne 0){throw 'stale 0/? remains'}; if($p39 -ne 1){throw 'phase-39 row disturbed'}; if($moji -ne 0){throw 'mojibake'}; Write-Host 'T1 OK'"</automated>
  </verify>
  <done>The Phase-38 Progress row reads `4/4 | Complete | 2026-06-06`; no `0/?` remains on that row; no other Progress row changed; no BOM, no mojibake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No attack surface — this plan edits a single local documentation file (`.planning/ROADMAP.md`).
No runtime, no network, no inputs, no auth, no data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | (n/a) | local markdown edit | accept | No runtime/network/input/data-flow changes; documentation-only. Only integrity risk is editor-induced encoding corruption (BOM/mojibake), mitigated by the Edit-tool-only single-row rule + BOM/mojibake grep in the acceptance criteria. |
</threat_model>

<verification>
- Phase-38 directory confirmed at 4 plan files.
- ROADMAP Progress-table Phase-38 row reads `4/4 | Complete | 2026-06-06`; stale `0/? | Not started` gone from that row.
- No other Progress row mutated.
- No UTF-8 BOM; no mojibake.
</verification>

<success_criteria>
SC3 of Phase 42 is met: the ROADMAP.md Progress-table Phase-38 row reads its true plan count (4/4)
and Complete status — without introducing a BOM or mojibake and without disturbing any other row.
</success_criteria>

<output>
After completion, create
`.planning/phases/42-v3.7.0-docs-traceability-reconciliation/42-02-SUMMARY.md`.
</output>
