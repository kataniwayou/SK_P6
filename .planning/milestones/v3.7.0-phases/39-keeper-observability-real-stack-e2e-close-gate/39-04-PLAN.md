---
phase: 39-keeper-observability-real-stack-e2e-close-gate
plan: 04
type: execute
wave: 4
depends_on: [39-03]
files_modified:
  - scripts/phase-39-close.ps1
autonomous: false
requirements: [TEST-03]
must_haves:
  truths:
    - "scripts/phase-39-close.ps1 exists, cloned from phase-33-close.ps1, preserving the triple-SHA protocol (psql \\l / redis --scan / rabbitmqctl list_queues name BEFORE==AFTER) verbatim."
    - "The rebuild set INCLUDES keeper (DELTA 1) so the embedded SourceHash + the new Keeper meter are in the live images."
    - "An additive message-depth==0 assertion checks BOTH keeper-dlq AND skp-dlq-1 via `rabbitmqctl list_queues name messages` (DELTA 2), kept OUT of the name-SHA so depth churn cannot break the SHA."
    - "The unfiltered redis --scan SHA still covers any leaked skp:keeper:probe:* scratch key; net-zero DLQ drain stays in the E2E teardown (no gate-side purge_queue)."
    - "The gate runs 3x consecutive GREEN unfiltered full suite (RealStack live) + Release+Debug 0-warning."
  artifacts:
    - path: "scripts/phase-39-close.ps1"
      provides: "Phase 39 triple-SHA net-zero close gate (clone + 2 deltas)"
      contains: "keeper-dlq"
  key_links:
    - from: "scripts/phase-39-close.ps1"
      to: "rabbitmqctl list_queues name messages"
      via: "additive both-DLQ depth==0 assertion on keeper-dlq + skp-dlq-1"
      pattern: "list_queues name messages"
---

<objective>
Clone `scripts/phase-33-close.ps1` to `scripts/phase-39-close.ps1`, applying exactly TWO deltas:
(DELTA 1) add `keeper` to the rebuild/health service list, and (DELTA 2) add an additive
message-depth==0 assertion on BOTH `keeper-dlq` (DLQ-2) and `skp-dlq-1` (DLQ-1) via
`rabbitmqctl list_queues name messages` — kept SEPARATE from the queue-name SHA so depth churn cannot
break it. Everything else (pre-flight stable-processor seed, the psql/redis/rmq triple-SHA, 3×GREEN
cadence, settle-drain, invariant assertions) is cloned verbatim. Then run the gate live and confirm
`GATE_EXIT=0`.

This is the milestone-closing gate. The final checkpoint is the live operator run.

Purpose: TEST-03 — 3×GREEN + triple-SHA net-zero close gate incl. both DLQs + scratch scan-clean.
Output: `scripts/phase-39-close.ps1` (NEW) + a live GREEN gate run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-CONTEXT.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-RESEARCH.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-PATTERNS.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-03-SUMMARY.md

<interfaces>
<!-- Clone source: scripts/phase-33-close.ps1. Verbatim blocks (line refs into phase-33-close.ps1): -->
<!-- pre-flight stable-Processor-row seed (74-157); BEFORE triple-SHA (174-193); 0-warning build both configs (195-208); 3xGREEN cadence (210-243); settle-drain (245-266); AFTER triple-SHA (268-286); invariant assertions (288-328). -->

<!-- The $services list to extend (phase-33-close.ps1:161): -->
$services = @('postgres', 'redis', 'rabbitmq', 'otel-collector', 'elasticsearch', 'prometheus', 'orchestrator', 'processor-sample', 'baseapi-service')
<!-- DELTA 1: append 'keeper'. -->

<!-- The CONTRACT-CHANGE rebuild note (phase-33-close.ps1:49-55) — update to mention keeper. -->

<!-- DELTA 2 (additive both-DLQ depth==0 block, AFTER the RMQ name-SHA invariant, before final exit). Source: 39-PATTERNS.md lines 248-257 / 39-RESEARCH.md lines 333-348. -->
$depthRaw = docker exec sk-rabbitmq rabbitmqctl -q list_queues name messages | Out-String
foreach ($q in @('keeper-dlq','skp-dlq-1')) {
    $line  = ($depthRaw -split "`n" | Where-Object { $_ -match "^\s*$([regex]::Escape($q))\s+\d+\s*$" })
    $depth = if ($line -match "\s+(\d+)\s*$") { [int]$Matches[1] } else { -1 }
    if ($depth -ne 0) {
        Write-Host "DLQ depth invariant VIOLATED: $q depth=$depth (expected 0)" -ForegroundColor Red
        $allGood = $false
    }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone phase-33-close.ps1 to phase-39-close.ps1 with DELTA 1 (rebuild set) + DELTA 2 (both-DLQ depth==0)</name>
  <files>scripts/phase-39-close.ps1</files>
  <read_first>
    - 39-PATTERNS.md `### scripts/phase-39-close.ps1 (NEW)` (clone-verbatim block list lines 228-236, DELTA 1 lines 238-244, DELTA 2 lines 246-263, Discrepancy 1 confirmation lines 259-263)
    - scripts/phase-33-close.ps1 (the full clone source — read in full: pre-flight seed, triple-SHA capture, 3xGREEN cadence, settle-drain, AFTER SHA, invariants; note the $services list at line 161 and the CONTRACT-CHANGE note at 49-55)
    - 39-RESEARCH.md `### phase-39-close.ps1 delta` (lines 332-349) + `### Pitfall 4` (keep list_queues name SHA UNCHANGED; depth check is additive) + `### Discrepancy 1` (DLQ-1 is skp-dlq-1, NOT a *_error queue)
    - src/Messaging.Contracts/KeeperQueues.cs (keeper-dlq = KeeperQueues.DeadLetter) + src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs (Dlq1 = "skp-dlq-1")
  </read_first>
  <action>
Create `scripts/phase-39-close.ps1` as a clone of `scripts/phase-33-close.ps1`. Copy the entire script
verbatim, then apply exactly TWO deltas and update the header banner to say "Phase 39".

CLONE VERBATIM (do NOT alter these — Pitfall 4 / D-09 / D-10):
- The pre-flight stable-Processor-row seed (genuine embedded SourceHash reflection → GET-or-create via
  WebApi at :8080 → wait-for-healthy). Keep the version string `'3.7.0'` unless the milestone version moves.
- The BEFORE triple-SHA capture: `psql -lqt` / `redis-cli --scan | Sort-Object -CaseSensitive` /
  `rabbitmqctl -q list_queues name | Sort-Object -CaseSensitive`, each SHA-256'd. **Keep
  `list_queues name` UNCHANGED in the SHA input** (adding `messages` would churn the SHA every run).
- The zero-warning build gate for BOTH configs (Release + Debug).
- The 3×consecutive-GREEN cadence (full suite, NO `Category` filter — RealStack runs live), distinct-Passed==1 guard.
- The settle-drain + AFTER triple-SHA + the existing invariant assertions.
- The unfiltered redis `--scan` SHA (already covers any leaked `skp:keeper:probe:*` scratch key — no scan delta).

**DELTA 1 — rebuild set:** append `'keeper'` to the `$services` list (phase-33-close.ps1:161):
```powershell
$services = @('postgres','redis','rabbitmq','otel-collector','elasticsearch','prometheus',
              'orchestrator','processor-sample','baseapi-service','keeper')   # + keeper (DELTA 1)
```
Also update the CONTRACT-CHANGE rebuild note (lines 49-55) to state the rebuild set is
`baseapi-service orchestrator processor-sample keeper` — so the embedded SourceHash AND the new `Keeper`
meter are in the live images (D-10; RESEARCH Pitfall 3 — a stale Phase-34 keeper has no meter → empty
`keeper_*` series).

**DELTA 2 — additive both-DLQ depth==0 assertion** (NEW block, placed AFTER the RMQ name-SHA invariant
and BEFORE the final `if (-not $allGood)` exit; SEPARATE from the name-SHA per Pitfall 4):
```powershell
$depthRaw = docker exec sk-rabbitmq rabbitmqctl -q list_queues name messages | Out-String
foreach ($q in @('keeper-dlq','skp-dlq-1')) {   # DLQ-2 (KeeperQueues.DeadLetter) + DLQ-1 (ConsolidatedErrorTransportFilter.Dlq1)
    $line  = ($depthRaw -split "`n" | Where-Object { $_ -match "^\s*$([regex]::Escape($q))\s+\d+\s*$" })
    $depth = if ($line -match "\s+(\d+)\s*$") { [int]$Matches[1] } else { -1 }
    if ($depth -ne 0) {
        Write-Host "DLQ depth invariant VIOLATED: $q depth=$depth (expected 0)" -ForegroundColor Red
        $allGood = $false
    }
}
```
Target `keeper-dlq` AND `skp-dlq-1` (NOT a `*_error` queue — Phase 36's `ConsolidatedErrorTransportFilter`
replaced the per-`{queue}_error` default with the single const `skp-dlq-1`; RESEARCH Discrepancy 1).
Do NOT add a gate-side `purge_queue` (D-10 — net-zero drain stays in the E2E teardown; the gate
snapshots-and-compares so a teardown regression surfaces as depth>0, not a silent pass).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; $null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'scripts/phase-39-close.ps1'), [ref]$null, [ref]$null); 'parse-ok'"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-39-close.ps1` exists.
    - The `$services` list contains `'keeper'`.
    - The script contains `list_queues name messages` AND both queue literals `keeper-dlq` and `skp-dlq-1`.
    - The script contains NO `*_error` queue literal for the depth check and NO gate-side `purge_queue`.
    - The BEFORE/AFTER SHA capture still uses `list_queues name` (NOT `name messages`) — the depth column is NOT folded into the SHA.
    - The script still contains the 3×GREEN cadence, the psql/redis/rmq triple-SHA, and the pre-flight processor seed (cloned verbatim).
    - The script parses without error (the pwsh ParseFile verify exits 0).
    - Wires to VALIDATION.md TEST-03 falsifiable signal (exit 0 = 3×GREEN + SHA BEFORE==AFTER + both DLQs depth==0).
  </acceptance_criteria>
  <done>scripts/phase-39-close.ps1 is a faithful clone with the keeper rebuild + both-DLQ depth==0 deltas, parses clean, and keeps the name-SHA depth-free.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live close-gate run (operator) — GATE_EXIT=0</name>
  <what-built>
`scripts/phase-39-close.ps1` — the Phase 39 triple-SHA net-zero close gate (clone of phase-33-close.ps1
with the keeper rebuild + both-DLQ depth==0 deltas). It rebuilds `baseapi-service orchestrator
processor-sample keeper`, seeds the stable processor row, captures the BEFORE triple-SHA, runs the full
suite (RealStack live, including the Plan-03-extended KeeperRecovery facts) 3× consecutive GREEN, settles,
captures the AFTER triple-SHA, and asserts psql/redis/rmq SHA BEFORE==AFTER + `keeper-dlq`==0 +
`skp-dlq-1`==0 + Release+Debug 0-warning.
  </what-built>
  <how-to-verify>
1. Ensure the v3.7.0 compose stack is up. Rebuild the contract-changed + metered images:
   `docker compose up -d --build baseapi-service orchestrator processor-sample keeper`
   (RESEARCH Pitfall 3 — a stale keeper image has no meter → empty `keeper_*` series → gate RED).
2. Run the gate:  `pwsh -File scripts/phase-39-close.ps1`
3. Expected: the script prints 3 consecutive GREEN runs with an identical Passed count, the keeper_*
   scrape assertions inside both KeeperRecovery facts pass live, the triple-SHA reports BEFORE==AFTER for
   all three (psql / redis / rmq), and `keeper-dlq`==0 + `skp-dlq-1`==0. Final line / exit code is
   `GATE_EXIT=0` (read the GATE_*_EXIT value from the script's own output, NOT a background-wrapper exit —
   per MEMORY note on bg-task exit codes).
4. If RED: check `docker compose logs keeper` (meter present? rebuilt?), confirm the live histogram suffix
   matches the Plan-03 query strings (`_seconds_count` vs `_count`), and that the E2E teardown drained
   `keeper-dlq` (depth>0 = teardown regression, not a gate bug).
  </how-to-verify>
  <resume-signal>Type "approved" with the observed GATE_EXIT=0 (and the 3×Passed count + triple-SHA + both-DLQ==0 evidence), or describe the RED failure.</resume-signal>
  <files>(operator-run checkpoint — runs scripts/phase-39-close.ps1; writes no source file)</files>
  <action>OPERATOR CHECKPOINT (see how-to-verify): rebuild `baseapi-service orchestrator processor-sample keeper`, then run `pwsh -File scripts/phase-39-close.ps1` and confirm GATE_EXIT=0.</action>
  <verify>
    <automated>pwsh -File scripts/phase-39-close.ps1</automated>
  </verify>
  <done>GATE_EXIT=0: 3× consecutive GREEN identical Passed count + psql/redis/rmq SHA BEFORE==AFTER + keeper-dlq==0 + skp-dlq-1==0 + Release+Debug 0-warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate script → docker exec / rabbitmqctl / psql / redis-cli | The gate runs local shell commands against the dev compose stack. No external/untrusted input crosses; all queue names are fixed literals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | Tampering | rabbitmqctl depth parse | mitigate | Queue names are fixed literals (`keeper-dlq`, `skp-dlq-1`) escaped via `[regex]::Escape`; the depth is parsed with an anchored numeric regex. No external input. The depth check is additive and excluded from the name-SHA (Pitfall 4) so it cannot be used to mask a SHA drift. |
| T-39-08 | Repudiation | gate pass/fail evidence | accept | The gate emits explicit GREEN/RED + SHA + depth output for the operator record; net-zero drain stays in the E2E teardown so a regression surfaces as depth>0 rather than a silent pass (D-10). Residual risk: NONE. |

Honest conclusion: LOW/NONE residual risk. A local verification script operating on fixed-literal queue
names against the dev stack; no new attack surface.
</threat_model>

<verification>
- `scripts/phase-39-close.ps1` parses clean (pwsh ParseFile exits 0) and satisfies every grep condition.
- Live run: `pwsh -File scripts/phase-39-close.ps1` → GATE_EXIT=0 (3×GREEN identical Passed count +
  psql/redis/rmq SHA BEFORE==AFTER + `keeper-dlq`==0 + `skp-dlq-1`==0 + Release+Debug 0-warning).
</verification>

<success_criteria>
TEST-03: the close gate exists, parses clean, preserves the triple-SHA protocol, adds the keeper rebuild
+ both-DLQ depth==0 deltas, and runs 3× consecutive GREEN with triple-SHA net-zero (incl. both DLQs +
scratch scan-clean) at Release+Debug 0-warning — GATE_EXIT=0.
</success_criteria>

<output>
After completion, create `.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-04-SUMMARY.md`.
MUST record: the observed GATE_EXIT value, the 3× Passed count, the triple-SHA BEFORE/AFTER match, and the
`keeper-dlq` / `skp-dlq-1` depths.
</output>
