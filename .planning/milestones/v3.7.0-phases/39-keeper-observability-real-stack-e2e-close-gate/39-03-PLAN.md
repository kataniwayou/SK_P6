---
phase: 39-keeper-observability-real-stack-e2e-close-gate
plan: 03
type: execute
wave: 3
depends_on: [39-02]
files_modified:
  - tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "KeeperRecovery_RecoversBothPaths asserts the recover keeper_* series appear in Prometheus after the live recover flow, with non-empty service_instance_id and no workflowId."
    - "KeeperRecovery_GivesUp_ParksToDlq asserts keeper_dlq_pushed + keeper_recovery_duration{gave_up} + keeper_l2_probe_failed series appear after the live give-up flow."
    - "The scrape queries use the correct Prometheus suffixes confirmed by the second Wave-0 gate (one live scrape): Counter -> _total, Histogram -> _seconds_count (unit s), UpDownCounter -> bare gauge."
    - "The scrape block reuses PrometheusTestClient + the FirstMetricObject/TryGetNonEmpty label-shape helpers; no raw query URLs."
  artifacts:
    - path: "tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs"
      provides: "Two extended RealStack facts with keeper_* Prometheus scrape-assertion blocks"
      contains: "keeper_fault_consumed_total"
  key_links:
    - from: "tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs"
      to: "prometheus :9090"
      via: "PrometheusTestClient.PollPromForQuery(keeper_* query, VectorNonEmpty)"
      pattern: "PollPromForQuery"
---

<objective>
Extend the two EXISTING Phase-36 RealStack facts in `KeeperRecoveryE2ETests.cs`
(`KeeperRecovery_RecoversBothPaths`, `KeeperRecovery_GivesUp_ParksToDlq`) with a `keeper_*` Prometheus
scrape-assertion block — mirroring how `MetricsRoundTripE2ETests` asserts series after a live round-trip.
Do NOT author a separate test class (D-06): the two facts already induce the exact TEST-01/02 outages.

This plan ALSO performs the second Wave-0 gate — confirm the histogram's Prometheus suffix (`_seconds`
vs bare) with one live scrape (RESEARCH Pitfall 1, the #1 flake risk) — and locks the query strings to
match before finalizing.

Purpose: TEST-01 (recover-both-paths + recover series) + TEST-02 (give-up + give-up series).
Output: `KeeperRecoveryE2ETests.cs` (MODIFY — both facts extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-CONTEXT.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-RESEARCH.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-PATTERNS.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-01-SUMMARY.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-02-SUMMARY.md

<interfaces>
<!-- The scrape-assertion pattern to mirror. Source: tests/.../Orchestrator/MetricsRoundTripE2ETests.cs:120-152, 231-277 -->
using var prom = new PrometheusTestClient();
var consumed = await prom.PollPromForQuery(
    $"keeper_fault_consumed_total{{fault_type=\"dispatch\",ProcessorId=\"{procId:D}\"}}",
    PrometheusTestClient.VectorNonEmpty, PromPollTimeoutMs, ct);
Assert.NotNull(consumed);
// label-shape: FirstMetricObject + TryGetNonEmpty -> non-empty service_instance_id;
// foreach metric prop -> Assert.False(... workflowId ...)  (the cardinality ban, copy lines 243-248)

<!-- PrometheusTestClient (REUSE — already has Uri.EscapeDataString built in). -->
PrometheusTestClient.PollPromForQuery(string promQL, Predicate<...> VectorNonEmpty, int timeoutMs, CancellationToken ct)
PrometheusTestClient.VectorNonEmpty

<!-- Prom-suffix rules (RESEARCH Pitfall 1). Pin the histogram unit decision from Plan 01 SUMMARY first. -->
Counter        -> keeper_<name>_total
Histogram(s)   -> keeper_recovery_duration_seconds_count / _sum / _bucket
UpDownCounter  -> keeper_in_flight   (bare gauge, no suffix; TRANSIENT)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 gate — confirm the live histogram Prometheus suffix, then extend both E2E facts with scrape blocks</name>
  <files>tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs</files>
  <read_first>
    - 39-PATTERNS.md `### tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs (MODIFY)` (scrape block lines 193-200, label-shape helper lines 202-208, per-fact targets table lines 210-214, Pitfall 1 note lines 216-220)
    - tests/BaseApi.Tests/Keeper/KeeperRecoveryE2ETests.cs (current — the two facts KeeperRecovery_RecoversBothPaths line 112 + KeeperRecovery_GivesUp_ParksToDlq line 266; the existing procId capture FACT 1 line 129, FACT 2 line 277)
    - tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs:120-152, 231-277 (scrape block + AssertBusinessLabels/FirstMetricObject/TryGetNonEmpty)
    - tests/BaseApi.Tests/Observability/Helpers/PrometheusTestClient.cs (PollPromForQuery, VectorNonEmpty — REUSE)
    - 39-01-SUMMARY.md (the chosen histogram unit "s" -> expected _seconds suffix) + 39-02-SUMMARY.md (whether keeper_in_flight is labelled)
    - 39-RESEARCH.md `### Pitfall 1` + `## Open Questions` Q1 (in_flight transient — assert PRESENCE best-effort, not a value) + Q3 (unit "s" -> _seconds)
  </read_first>
  <action>
**Wave-0 gate (suffix confirmation) — do this BEFORE locking query strings.** Plan 01 chose unit `"s"`,
which makes the histogram Prom name `keeper_recovery_duration_seconds_*`. Confirm against the LIVE stack:
after the first live recover flow runs (or via a quick manual scrape against `http://localhost:9090`),
verify whether the series is `keeper_recovery_duration_seconds_count` (unit set) or
`keeper_recovery_duration_count` (unit omitted). Lock the query string to whichever the live scrape shows.
Record the confirmed suffix in the SUMMARY. (If the live stack is not yet up, write the queries to the
unit-`"s"` `_seconds` form per Plan 01 and confirm on the first GREEN gate run in Plan 04 — note this in
the SUMMARY.)

Add `private const int PromPollTimeoutMs = 120_000;` to the test class (mirror MetricsRoundTripE2ETests.cs:61).

Add a `keeper_*` scrape-assertion block at the END of each fact (after the existing recover/give-up
assertions, reusing the `procId` each fact already captures). Construct `using var prom = new
PrometheusTestClient();` and `PollPromForQuery(...)` each target series. For each, `Assert.NotNull(...)`
and run the label-shape check (copy `FirstMetricObject` + `TryGetNonEmpty` for non-empty
`service_instance_id`, and the `foreach (var prop in metric.EnumerateObject()) Assert.False(...workflowId...)`
loop from MetricsRoundTripETests.cs:243-248).

Per-fact targets (PATTERNS lines 210-214; Prom-suffixed names, unit "s" histogram):
- **`KeeperRecovery_RecoversBothPaths`** (recover flow): assert
  `keeper_fault_consumed_total`, `keeper_recovered_total`, `keeper_workflow_paused_total`,
  `keeper_workflow_resumed_total`, and `keeper_recovery_duration_seconds_count{outcome="recovered"}` —
  all filtered by `ProcessorId="{procId:D}"` where the series carries ProcessorId; `fault_type="dispatch"`
  on the dispatch-path series.
- **`KeeperRecovery_GivesUp_ParksToDlq`** (give-up flow): assert
  `keeper_dlq_pushed_total{reason="probe_exhausted",fault_type="result"}`,
  `keeper_recovery_duration_seconds_count{outcome="gave_up"}`, and `keeper_l2_probe_failed_total` —
  filtered by `ProcessorId="{procId:D}"`.

`keeper_in_flight` is a TRANSIENT gauge (returns to 0 after the loop) — do NOT assert a specific value;
if asserted at all, assert series PRESENCE best-effort only (its correctness is covered by the hermetic
KeeperMetricsFacts, not this live race). RESEARCH OQ-1.

Never build raw query URLs — `PrometheusTestClient` applies `Uri.EscapeDataString` internally (V5 /
T-30-01). Interpolate only the validated `{procId:D}` GUID. Do NOT change the existing outage recipe
(`ArmWrongTypePoisonAsync`), the `L2KeysToCleanup` teardown, or the `keeper-dlq` ACK-drain — D-07/D-10
keep those exactly as Phase 36 built them.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `KeeperRecoveryE2ETests.cs` contains `PrometheusTestClient`, `PollPromForQuery`, and `PromPollTimeoutMs`.
    - The recover fact references `keeper_fault_consumed_total`, `keeper_recovered_total`, `keeper_workflow_paused_total`, `keeper_workflow_resumed_total`, and `keeper_recovery_duration_seconds_count` (or the bare `_count` form if the live scrape confirmed unit was omitted — SUMMARY states which).
    - The give-up fact references `keeper_dlq_pushed_total`, `keeper_recovery_duration_seconds_count`, and `keeper_l2_probe_failed_total`.
    - Both facts assert non-empty `service_instance_id` and assert NO `workflowId` label (the cardinality ban loop is present).
    - No raw query-URL string concatenation (all queries go through PollPromForQuery).
    - `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0 with 0 warnings.
    - Wires to VALIDATION.md TEST-01 + TEST-02 + the Wave-0 histogram-suffix requirement (#2).
  </acceptance_criteria>
  <done>Both E2E facts compile with keeper_* scrape-assertion blocks using the live-confirmed Prom suffixes, reusing PrometheusTestClient and the no-workflowId label-shape helper; the live GREEN run is gated by Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → Prometheus :9090 HTTP query | The only interpolated input is the PromQL query string; it crosses into the Prometheus HTTP API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Tampering | PromQL query interpolation in the scrape block | mitigate | Queries are built via `PrometheusTestClient.PollPromForQuery`, which applies `Uri.EscapeDataString` (T-30-01). Only a validated `{procId:D}` GUID is interpolated — never raw user input. The acceptance criteria forbid raw query-URL concatenation. |
| T-39-06 | Information Disclosure | scrape assertions | accept | The test reads telemetry series + label shapes only (no message bodies). Asserting absence of `workflowId` actively guards the cardinality/info-shape contract. Residual risk: NONE. |

Honest conclusion: LOW residual risk. Test-only code reading telemetry; the one interpolation point is
escaped by the reused client and limited to a validated GUID.
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release` exits 0, 0 warnings.
- The two facts contain the keeper_* scrape assertions with the confirmed suffixes and the no-workflowId loop.
- Live GREEN execution of both facts is asserted by the Plan 04 close gate (3×GREEN, RealStack live).
</verification>

<success_criteria>
TEST-01 + TEST-02: the two extended RealStack facts assert the recover and give-up keeper_* series appear
in Prometheus with the expected fault_type/outcome/reason/ProcessorId + non-empty service_instance_id
labels and no workflowId, using live-confirmed suffixes.
</success_criteria>

<output>
After completion, create `.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-03-SUMMARY.md`.
MUST record: the live-confirmed histogram Prometheus suffix (`_seconds_count` vs `_count`) and the exact
keeper_* query strings each fact uses, so the Plan 04 gate run matches.
</output>
