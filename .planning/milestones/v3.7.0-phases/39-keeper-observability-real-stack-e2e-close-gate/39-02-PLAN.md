---
phase: 39-keeper-observability-real-stack-e2e-close-gate
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs
  - src/Keeper/Consumers/FaultExecutionResultConsumer.cs
  - src/Keeper/Recovery/L2ProbeRecovery.cs
  - tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs
autonomous: true
requirements: [KMET-01, KMET-02, KMET-03]
must_haves:
  truths:
    - "Each fault consumer increments keeper_fault_consumed at intake, keeper_workflow_paused after Publish(Pause), keeper_recovered + keeper_workflow_resumed in the Recovered branch, keeper_dlq_pushed in the give-up branch — with the correct tags and the right fault_type."
    - "Each consumer records keeper_recovery_duration with sw.Elapsed.TotalSeconds (NOT milliseconds), tagged outcome=recovered or outcome=gave_up, spanning intake to terminal."
    - "L2ProbeRecovery.RunAsync increments keeper_in_flight +1 on entry / -1 in finally, and keeper_l2_probe_failed per RedisException catch."
    - "No control-flow change to the existing consumer/probe branches — only metric increments are threaded in."
    - "A hermetic KeeperMetricsFacts test subscribes to the Keeper meter, drives a faked consume/probe, and asserts each instrument fired with the exact tag set, in-flight went +1 then -1, and NO workflowId tag exists."
  artifacts:
    - path: "src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs"
      provides: "fault_type=dispatch increment sites + recovery_duration Stopwatch"
      contains: "metrics.FaultConsumed.Add"
    - path: "src/Keeper/Consumers/FaultExecutionResultConsumer.cs"
      provides: "fault_type=result increment sites + recovery_duration Stopwatch"
      contains: "metrics.FaultConsumed.Add"
    - path: "src/Keeper/Recovery/L2ProbeRecovery.cs"
      provides: "in_flight ++/finally-- + l2_probe_failed per catch"
      contains: "InFlight.Add"
    - path: "tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs"
      provides: "Hermetic MeterListener assertion of all instruments + tags + no-workflowId"
      contains: "MeterListener"
  key_links:
    - from: "src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs"
      to: "src/Keeper/Observability/KeeperMetrics.cs"
      via: "KeeperMetrics metrics primary-ctor param + metrics.X.Add(...) increment sites"
      pattern: "metrics\\.(FaultConsumed|Recovered|DlqPushed|WorkflowPaused|WorkflowResumed)\\.Add"
    - from: "src/Keeper/Recovery/L2ProbeRecovery.cs"
      to: "src/Keeper/Observability/KeeperMetrics.cs"
      via: "KeeperMetrics metrics primary-ctor param + InFlight/L2ProbeFailed increments"
      pattern: "metrics\\.(InFlight|L2ProbeFailed)\\.Add"
---

<objective>
Thread the `KeeperMetrics` instruments (from Plan 01) into the three existing instrumentation sites —
`FaultEntryStepDispatchConsumer`, `FaultExecutionResultConsumer`, and `L2ProbeRecovery` — at the exact
already-existing branches, with NO control-flow change. Add a hermetic `KeeperMetricsFacts` test that
subscribes to the `"Keeper"` meter via a `MeterListener`, drives the consume/probe paths, and asserts
each instrument fires with the correct tag set (and that no `workflowId` label leaks).

Purpose: KMET-01/02/03 — wire the meter to its emission sites + prove it hermetically.
Output: 3 instrumented source files (MODIFY) + `KeeperMetricsFacts.cs` (NEW hermetic test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-CONTEXT.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-RESEARCH.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-PATTERNS.md
@.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-01-SUMMARY.md

<interfaces>
<!-- KeeperMetrics instrument surface (from Plan 01). Increment via metrics.X.Add(1, ...tags). -->
KeeperMetrics (src/Keeper/Observability/KeeperMetrics.cs):
  Counter<long> FaultConsumed, Recovered, DlqPushed, WorkflowPaused, WorkflowResumed, L2ProbeFailed
  UpDownCounter<long> InFlight
  Histogram<double> RecoveryDuration   // unit "s" — Record(sw.Elapsed.TotalSeconds, ...)
  + interned-label helpers / tag-builder (Plan 01's KeeperMetricTags shape)

<!-- The tag-add idiom to copy. Source: src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs:248-250 -->
metrics.ResultSent.Add(1,
    new KeyValuePair<string, object?>("ProcessorId", context.Id!.Value.ToString("D")),
    new KeyValuePair<string, object?>("outcome", OutcomeLabel(result.Outcome)));

<!-- Consumer current primary ctor (VERIFIED). Add `KeeperMetrics metrics` as a 3rd param. -->
FaultEntryStepDispatchConsumer(ILogger<FaultEntryStepDispatchConsumer> logger, L2ProbeRecovery recovery)
FaultExecutionResultConsumer(ILogger<FaultExecutionResultConsumer> logger, L2ProbeRecovery recovery)

<!-- L2ProbeRecovery current primary ctor + RunAsync (VERIFIED). Add `KeeperMetrics metrics` param. -->
L2ProbeRecovery(IConnectionMultiplexer redis, IOptions<ProbeOptions> opts)
  public async Task<ProbeOutcome> RunAsync(string entryId, string h, CancellationToken ct)
  // for (var attempt = 0; attempt < max; attempt++) { try {...} catch (RedisException) {...} }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instrument both fault consumers (fault_type-tagged increments + recovery_duration Stopwatch)</name>
  <files>src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs, src/Keeper/Consumers/FaultExecutionResultConsumer.cs</files>
  <read_first>
    - 39-PATTERNS.md `### src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs (MODIFY)` (increment-site map table lines 109-118) + `### src/Keeper/Consumers/FaultExecutionResultConsumer.cs (MODIFY)` (lines 122-124, identical map, fault_type=result)
    - src/Keeper/Consumers/FaultEntryStepDispatchConsumer.cs (current branch structure — VERIFIED, lines 30-77; DO NOT change control flow)
    - src/Keeper/Consumers/FaultExecutionResultConsumer.cs (current — VERIFIED, recovered endpoint is OrchestratorQueues.Result not queue:{procId})
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs:248-269 (tag-add idiom + interned-label switch)
    - 39-RESEARCH.md `### Pitfall 2` (record TotalSeconds, not ms)
  </read_first>
  <behavior>
    - In each consumer, add `KeeperMetrics metrics` as the third primary-ctor param.
    - At top of Consume (after `var inner = context.Message.Message;`): start a Stopwatch AND increment FaultConsumed with tags {fault_type, ProcessorId}.
    - After `context.Publish(new PauseWorkflow(...))`: increment WorkflowPaused with tag {ProcessorId} (NO fault_type — workflow-scoped).
    - In the Recovered branch, after `context.Publish(new ResumeWorkflow(...))`: increment WorkflowResumed {ProcessorId}, increment Recovered {fault_type, ProcessorId}, and Record RecoveryDuration(sw.Elapsed.TotalSeconds) with tag {outcome="recovered", ProcessorId}.
    - In the else (give-up) branch, around `dlq.Send(...)`: increment DlqPushed {reason="probe_exhausted", fault_type, ProcessorId} and Record RecoveryDuration(sw.Elapsed.TotalSeconds) with {outcome="gave_up", ProcessorId}.
    - fault_type is "dispatch" in the dispatch consumer, "result" in the result consumer.
  </behavior>
  <action>
For BOTH consumers, add `KeeperMetrics metrics` as the third primary-ctor parameter (mirrors the
`ProcessorMetrics metrics` injection precedent), and add `using Keeper.Observability;` + `using System.Diagnostics;`.

Thread these increments into the EXISTING branches (no control-flow change). `ProcessorId` is
`inner.ProcessorId.ToString("D")`. Use the Plan-01 interned-label helpers / tag-builder.

Increment-site map (from PATTERNS lines 109-118). For `FaultEntryStepDispatchConsumer` use
`fault_type = "dispatch"`; for `FaultExecutionResultConsumer` use `fault_type = "result"`:

1. Top of `Consume`, immediately after `var inner = context.Message.Message;`:
   - `var sw = Stopwatch.StartNew();`
   - `metrics.FaultConsumed.Add(1, fault_type=<dispatch|result>, ProcessorId);`
2. Immediately after the existing `await context.Publish(new PauseWorkflow(...));`:
   - `metrics.WorkflowPaused.Add(1, ProcessorId);`   // no fault_type
3. In the `if (outcome == ProbeOutcome.Recovered)` branch, AFTER the existing `await context.Publish(new ResumeWorkflow(...));`:
   - `metrics.WorkflowResumed.Add(1, ProcessorId);`
   - `metrics.Recovered.Add(1, fault_type, ProcessorId);`
   - `metrics.RecoveryDuration.Record(sw.Elapsed.TotalSeconds, outcome="recovered", ProcessorId);`
4. In the `else` (give-up) branch, around the existing `await dlq.Send(...);`:
   - `metrics.DlqPushed.Add(1, reason="probe_exhausted", fault_type, ProcessorId);`
   - `metrics.RecoveryDuration.Record(sw.Elapsed.TotalSeconds, outcome="gave_up", ProcessorId);`

CRITICAL: `RecoveryDuration.Record(sw.Elapsed.TotalSeconds, ...)` — TotalSeconds, never
`ElapsedMilliseconds` (RESEARCH Pitfall 2; the buckets are seconds). Do NOT add a `workflowId` tag
anywhere. Do NOT alter any existing log scope, Publish, Send, or branch — increments only.
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - Both consumer files contain `KeeperMetrics metrics` in the primary ctor parameter list.
    - FaultEntryStepDispatchConsumer.cs contains `"dispatch"` and FaultExecutionResultConsumer.cs contains `"result"` as the fault_type literal.
    - Each consumer contains `metrics.FaultConsumed.Add`, `metrics.WorkflowPaused.Add`, `metrics.WorkflowResumed.Add`, `metrics.Recovered.Add`, `metrics.DlqPushed.Add`, and `metrics.RecoveryDuration.Record`.
    - Each consumer contains `sw.Elapsed.TotalSeconds` and contains NO `ElapsedMilliseconds`.
    - Neither consumer contains a `workflowId` metric tag literal.
    - The existing Publish/Send/branch control flow is unchanged (grep confirms PauseWorkflow, ResumeWorkflow, dlq.Send, GetSendEndpoint still present in the same branches).
    - `dotnet build src/Keeper/Keeper.csproj -c Release` exits 0 with 0 warnings.
    - Wires to VALIDATION.md KMET-02 (counters with fault_type/ProcessorId/reason, no workflowId) + KMET-03 (recovery_duration histogram).
  </acceptance_criteria>
  <done>Both consumers emit all six per-consumer increments at the correct branches with the right tags and second-scale duration recording, with no control-flow change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument L2ProbeRecovery (in_flight ++/finally-- + l2_probe_failed) and author hermetic KeeperMetricsFacts</name>
  <files>src/Keeper/Recovery/L2ProbeRecovery.cs, tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs</files>
  <read_first>
    - 39-PATTERNS.md `### src/Keeper/Recovery/L2ProbeRecovery.cs (MODIFY)` (RunAsync wrap lines 132-159 + the OPEN QUESTION on ProcessorId threading lines 161-166)
    - src/Keeper/Recovery/L2ProbeRecovery.cs (current — VERIFIED, lines 17-41; RunAsync(entryId, h, ct), for-loop, catch (RedisException))
    - 39-RESEARCH.md `### Pattern 3` (UpDownCounter API) + `## Open Questions` Q1 (in_flight label strategy) + `## Validation Architecture` Falsifiable Signals (hermetic MeterListener)
    - 39-PATTERNS.md `## No Analog Found` (KeeperMetricsFacts is a partial-analog — MeterListener mechanism, no exact in-repo example)
  </read_first>
  <behavior>
    - L2ProbeRecovery takes `KeeperMetrics metrics` as a ctor param.
    - RunAsync increments InFlight +1 on entry and -1 in a finally that wraps the whole loop body.
    - Each `catch (RedisException)` increments L2ProbeFailed.
    - ProcessorId threading (resolve the OPEN QUESTION): thread a `string procId` param into RunAsync so l2_probe_failed carries {ProcessorId}; in_flight may carry {ProcessorId} or be unlabelled — pick ONE explicitly. (Both consumers already hold inner.ProcessorId.ToString("D") to pass.)
    - KeeperMetricsFacts subscribes to the "Keeper" meter via MeterListener, drives a faked consume/probe (or directly exercises the instruments through a constructed KeeperMetrics + the consumers/probe), and asserts every instrument fired with the exact tag set, in_flight measured +1 then -1, and NO workflowId tag is present on any measurement.
  </behavior>
  <action>
**Resolve the OPEN QUESTION explicitly (do NOT leave ambiguous):** thread a `string procId` parameter
into `L2ProbeRecovery.RunAsync` — new signature `RunAsync(string entryId, string h, string procId,
CancellationToken ct)`. Update BOTH consumer call sites (Task 1's files) to pass
`inner.ProcessorId.ToString("D")`. Tag `keeper_l2_probe_failed` with `{ProcessorId = procId}` (D-03).
For `keeper_in_flight`: tag it with `{ProcessorId = procId}` as well (consistent with the rest;
`long` numeric type per Plan 01). [If threading proves to require touching call sites beyond the two
consumers, fall back to emitting `keeper_in_flight` UNLABELLED and tag only `l2_probe_failed` — state
which you chose in the SUMMARY. KMET-03 does not mandate a label on in_flight.]

Wrap `RunAsync`'s body:
```csharp
public async Task<ProbeOutcome> RunAsync(string entryId, string h, string procId, CancellationToken ct)
{
    metrics.InFlight.Add(1, new KeyValuePair<string, object?>("ProcessorId", procId));   // D-05 entry
    try
    {
        var db = redis.GetDatabase();
        var max = opts.Value.MaxAttempts;
        for (var attempt = 0; attempt < max; attempt++)
        {
            try { /* existing READ + WRITE/DEL scratch */ return ProbeOutcome.Recovered; }
            catch (RedisException)
            {
                metrics.L2ProbeFailed.Add(1, new KeyValuePair<string, object?>("ProcessorId", procId));  // D-03
                if (attempt + 1 < max)
                    await Task.Delay(TimeSpan.FromSeconds(opts.Value.DelaySeconds), ct);
            }
        }
        return ProbeOutcome.GaveUp;
    }
    finally { metrics.InFlight.Add(-1, new KeyValuePair<string, object?>("ProcessorId", procId)); }   // D-05 finally
}
```
Add `KeeperMetrics metrics` to the primary ctor `(IConnectionMultiplexer redis, IOptions<ProbeOptions> opts)`
and `using Keeper.Observability;`. Do NOT change the READ + WRITE/DEL probe body or the RedisException-only
catch semantics — increments only.

Author `tests/BaseApi.Tests/Keeper/KeeperMetricsFacts.cs` (hermetic, NO RealStack trait — it must run in
the `--filter-not-trait "Category=RealStack"` suite). Use a `System.Diagnostics.Metrics.MeterListener`
that enables instruments where `instrument.Meter.Name == "Keeper"`, records into an in-memory capture
list of (name, value, tags), then exercises the instruments. Assert:
- All eight instrument names are present and snake_case with NO `_total`/`_seconds` embedded in the instrument NAME (the collector adds those — assert the raw instrument names).
- A faked recover flow fires keeper_fault_consumed{fault_type,ProcessorId}, keeper_workflow_paused{ProcessorId}, keeper_recovered{fault_type,ProcessorId}, keeper_workflow_resumed{ProcessorId}, keeper_recovery_duration{outcome="recovered",ProcessorId}.
- A faked give-up flow fires keeper_dlq_pushed{reason="probe_exhausted",fault_type,ProcessorId} and keeper_recovery_duration{outcome="gave_up",ProcessorId}.
- keeper_in_flight measures +1 then -1 across a RunAsync invocation, and keeper_l2_probe_failed fires per simulated RedisException.
- NO measurement carries a `workflowId` tag key (assert absence across all captured tags — the cardinality ban).
Drive the faked flows the lightest way that exercises the real increment code: prefer constructing the
real consumers/probe with a fake IConnectionMultiplexer / mocked KeeperMetrics-backed MeterListener, OR
(if wiring the full consumer is heavy) directly invoke the KeeperMetrics instruments with the exact tag
sets the consumers use, asserting the listener captured them. The load-bearing assertion is the
instrument-name + tag-shape + no-workflowId contract, hermetically.
  </action>
  <verify>
    <automated>dotnet build src/Keeper/Keeper.csproj -c Release; dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -c Release --filter-not-trait "Category=RealStack" --filter-class "*KeeperMetricsFacts*"</automated>
  </verify>
  <acceptance_criteria>
    - `L2ProbeRecovery.cs` contains `KeeperMetrics metrics` in the primary ctor and `metrics.InFlight.Add(1` + `metrics.InFlight.Add(-1` + `metrics.L2ProbeFailed.Add(1`.
    - The `InFlight.Add(-1` call is inside a `finally` block.
    - `RunAsync` signature includes a `procId` param (or, if the unlabelled fallback was chosen, the SUMMARY states so and `keeper_in_flight` is emitted without a ProcessorId tag).
    - Both consumer call sites pass `inner.ProcessorId.ToString("D")` to RunAsync (if the param was added).
    - `KeeperMetricsFacts.cs` exists, contains `MeterListener` and `"Keeper"`, and asserts no `workflowId` tag.
    - `dotnet build src/Keeper/Keeper.csproj -c Release` exits 0, 0 warnings.
    - `KeeperMetricsFacts` passes (the class-filtered MTP run is GREEN).
    - Wires to VALIDATION.md KMET-01/02/03 hermetic falsifiable signal (MeterListener-captured instruments + tags + no workflowId).
  </acceptance_criteria>
  <done>L2ProbeRecovery emits in_flight ++/finally-- and per-catch l2_probe_failed; the hermetic KeeperMetricsFacts proves every instrument fires with the correct tags and no workflowId leaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fault<T> message → Keeper consumer | Already-existing boundary from Phase 35/36; this plan adds metric increments only, no new parsing or input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Denial of Service | metric labels at the increment sites | mitigate | ProcessorId is the only id label and is a bounded `{guid:D}` from a registered processor; fault_type/outcome/reason are closed enums. The hermetic KeeperMetricsFacts asserts NO workflowId tag — preventing high-cardinality Prometheus memory growth. |
| T-39-04 | Information Disclosure | metric labels | accept | No message body / PII / exception text is placed in any metric label (only ids + closed enums). Residual risk: NONE. |

Honest conclusion: LOW/NONE residual risk. This plan threads increments into existing branches; the only
data-shape concern (label cardinality) is mitigated and hermetically asserted.
</threat_model>

<verification>
- `dotnet build src/Keeper/Keeper.csproj -c Release` exits 0, 0 warnings.
- `dotnet test ... --filter-not-trait "Category=RealStack" --filter-class "*KeeperMetricsFacts*"` GREEN.
- All grep conditions in both tasks' acceptance criteria hold.
</verification>

<success_criteria>
KMET-01/02/03 instrumentation is live in code and hermetically proven: every increment site fires at the
correct existing branch with the right tags, recovery_duration records seconds, in_flight tracks probe-loop
occupancy, and no workflowId label exists anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/39-keeper-observability-real-stack-e2e-close-gate/39-02-SUMMARY.md`.
MUST record: whether `keeper_in_flight` is labelled with ProcessorId or unlabelled (the OPEN QUESTION
resolution) and the final RunAsync signature, so Plan 03's scrape assertions match.
</output>
