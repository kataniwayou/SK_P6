---
phase: 41-orchestrator-pause-resume-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Orchestrator/Hydration/WorkflowLifecycle.cs
autonomous: true
requirements: [WR-01]
must_haves:
  truths:
    - "When a Resume arrives for a workflow whose trigger is not exactly Paused, an INFORMATIONAL log is emitted carrying the WorkflowId and the observed TriggerState before the method returns."
    - "The behavior of ResumeAsync is otherwise unchanged: the ignore branch still returns without re-arming (no behavioral fix — D-01/D-02)."
    - "dotnet build -c Release of the Orchestrator project emits 0 warnings."
  artifacts:
    - path: "src/Orchestrator/Hydration/WorkflowLifecycle.cs"
      provides: "ResumeAsync ignore-branch informational diagnostic (WR-01 / D-01)"
      contains: "logger.LogInformation"
  key_links:
    - from: "src/Orchestrator/Hydration/WorkflowLifecycle.cs ResumeAsync ignore branch"
      to: "ILogger<WorkflowLifecycle> logger (already injected, ctor line 30)"
      via: "logger.LogInformation with {WorkflowId} and {TriggerState} placeholders"
      pattern: "logger\\.LogInformation\\(.*\\{WorkflowId\\}.*\\{TriggerState\\}"
---

<objective>
Close 37-REVIEW **WR-01 (SC1)**: make a Resume dropped during the narrow fire window observable.

On the `state != TriggerState.Paused` ignore branch in `WorkflowLifecycle.ResumeAsync`, emit a single
`logger.LogInformation(...)` carrying the `WorkflowId` and the observed `TriggerState` BEFORE the existing
`return`. This is **log-only** — no behavior change. Per CONTEXT.md **D-01/D-02/D-03**, do NOT re-arm on
`None` (it is overloaded with operator-Stopped); the dropped-Resume edge stays, it just becomes diagnosable.

Purpose: turn a silent no-op into a diagnosable event at the cost of one log line.
Output: one modified file (`WorkflowLifecycle.cs`) plus a 41-01-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-orchestrator-pause-resume-diagnostics/41-CONTEXT.md
@.planning/phases/41-orchestrator-pause-resume-diagnostics/41-PATTERNS.md

<interfaces>
<!-- Logger is already injected — no new dependency. Constructor (WorkflowLifecycle.cs lines 25-31): -->
public sealed class WorkflowLifecycle(
    IConnectionMultiplexer redis,
    IWorkflowL1Store store,
    WorkflowScheduler scheduler,
    TimeProvider timeProvider,
    ILogger<WorkflowLifecycle> logger)   // <-- use this

<!-- Existing in-file structured-logging convention (lines 45, 64, 99-100): -->
logger.LogWarning("Workflow {WorkflowId} absent from L2 root — skipping hydration (business)", workflowId);
logger.LogWarning("Step {StepId} of workflow {WorkflowId} absent from L2 — skipping step (business)", stepId, workflowId);

<!-- Convention rules: PascalCase named placeholders (never positional {0}); trailing parenthetical
     classifier on the message; positional args follow the template in placeholder order.
     WR-01 deviates on LEVEL ONLY: LogInformation (D-03), not LogWarning. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add informational log on the ResumeAsync non-Paused ignore branch (WR-01 / D-01)</name>
  <read_first>
    - src/Orchestrator/Hydration/WorkflowLifecycle.cs (the WHOLE file — confirm the injected `logger` at ctor line 30, the existing LogWarning convention at lines 45/64/99-100, and the exact ignore branch at lines 185-190)
    - .planning/phases/41-orchestrator-pause-resume-diagnostics/41-PATTERNS.md (§ WorkflowLifecycle.cs — D-01/D-03 locked constraints + target shape)
    - .planning/phases/41-orchestrator-pause-resume-diagnostics/41-CONTEXT.md (D-01, D-02, D-03 — why log-only, why NOT re-arm on None)
  </read_first>
  <files>src/Orchestrator/Hydration/WorkflowLifecycle.cs</files>
  <behavior>
    - Given a workflow present in L1 whose trigger state is None / Normal / Blocked / Error (anything != Paused),
      ResumeAsync logs ONE informational record containing the WorkflowId and the observed TriggerState, then returns.
    - Given a workflow whose trigger state IS Paused, the log is NOT emitted (control never reaches the ignore branch).
    - No re-arm, no UnscheduleAsync, no ScheduleAsync on the ignore branch — behavior is identical to before plus the log.
  </behavior>
  <action>
Edit the `ResumeAsync` ignore branch in `src/Orchestrator/Hydration/WorkflowLifecycle.cs`.

Current branch (lines 185-190):
```csharp
        var state = await scheduler.GetTriggerStateAsync(wf.JobId, ct);
        if (state != TriggerState.Paused)
        {
            // None(Stopped)/Normal(Running)/Blocked/Error -> ignore (D-09).
            return;
        }
```

Add a `logger.LogInformation(...)` call immediately BEFORE the `return;`, carrying `workflowId` and the observed
`state`. Replace the branch body with EXACTLY:
```csharp
        var state = await scheduler.GetTriggerStateAsync(wf.JobId, ct);
        if (state != TriggerState.Paused)
        {
            // None(Stopped)/Normal(Running)/Blocked/Error -> ignore (D-09). Log-only diagnostic so a Resume
            // dropped in the narrow fire window is observable (WR-01 / D-01). NOT a re-arm: None is overloaded
            // with operator-Stopped, so re-arming would resurrect a Stopped workflow (D-02).
            logger.LogInformation(
                "Resume ignored for workflow {WorkflowId} — trigger state is {TriggerState}, not Paused (D-09)",
                workflowId, state);
            return;
        }
```

Constraints (locked — DO NOT deviate):
- Level MUST be `LogInformation` (D-03), NOT LogWarning/LogDebug.
- Placeholders MUST be `{WorkflowId}` and `{TriggerState}` (PascalCase, named), positional args `workflowId, state` in that order.
- Do NOT add any behavioral change (no re-arm, no extra scheduler call). The `return;` stays exactly where it was.
- Exact prose is discretionary; the two fields and the level are not. If you adjust wording, keep both placeholders and the trailing `(...)` classifier.
- `workflowId` is the `ResumeAsync` parameter (already in scope); `state` is the local just read above.
  </action>
  <verify>
    <automated>cd "C:/Users/UserL/source/repos/SK_P" && dotnet build src/Orchestrator/Orchestrator.csproj -c Release -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WorkflowLifecycle.cs` contains a `logger.LogInformation(` call on the `state != TriggerState.Paused` ignore branch (grep: `logger\.LogInformation\(.*WorkflowId.*TriggerState` matches within ResumeAsync).
    - The call uses named placeholders `{WorkflowId}` AND `{TriggerState}` (grep both literal placeholder strings present in the new line).
    - The branch still ends with `return;` and contains NO `Unschedule`, `Schedule`, or `RescheduleAsync` call (grep the ignore-branch block — only the new log + the existing return).
    - No `LogWarning` / `LogDebug` was added on this branch (level is Information per D-03).
    - `dotnet build src/Orchestrator/Orchestrator.csproj -c Release -warnaserror` exits 0 (0 warnings).
  </acceptance_criteria>
  <done>The ResumeAsync ignore branch emits an informational log with WorkflowId + observed TriggerState before returning; behavior is otherwise unchanged; Release build is 0-warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change adds a structured log line on an internal, already-reached code path. No new external input, no new trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Information Disclosure | `WorkflowLifecycle.ResumeAsync` log line | accept | Logged fields are `WorkflowId` (a Guid) and a Quartz `TriggerState` enum — neither is PII or a secret. They use structured named placeholders (no string interpolation of message-controlled data), consistent with the codebase logging discipline. No sensitive data exposed. |

**No-new-surface reasoning (honest):** The change is a single `LogInformation` on an internal code path that is
already reached today (it currently `return`s silently). No new input is parsed, no new field crosses a trust
boundary, no behavior changes. Both logged values are non-sensitive (Guid + enum) and are passed structurally.
threats_open: 0.
</threat_model>

<verification>
- `dotnet build src/Orchestrator/Orchestrator.csproj -c Release -warnaserror` → 0 warnings, exit 0.
- Hermetic suite stays GREEN (no test touches this branch's behavior; the log addition is non-breaking):
  `cd "C:/Users/UserL/source/repos/SK_P" && dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj --filter "Category!=RealStack"` exits 0.
- grep confirms `logger.LogInformation` with `{WorkflowId}` and `{TriggerState}` on the ignore branch.
</verification>

<success_criteria>
- SC1 / WR-01 satisfied: ResumeAsync emits an INFORMATIONAL log on the non-Paused ignore branch carrying WorkflowId + observed TriggerState.
- No behavioral change (no re-arm; D-01/D-02 honored).
- Release build 0-warning; hermetic suite GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/41-orchestrator-pause-resume-diagnostics/41-01-SUMMARY.md`.
</output>
