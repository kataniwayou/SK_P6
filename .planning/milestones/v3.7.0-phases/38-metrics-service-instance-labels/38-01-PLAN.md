---
phase: 38-metrics-service-instance-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Messaging.Contracts/ProcessorQueries.cs
  - src/BaseApi.Service/Features/Processor/Responders/GetProcessorBySourceHashConsumer.cs
  - src/BaseProcessor.Core/Identity/IProcessorContext.cs
  - src/BaseProcessor.Core/Identity/ProcessorContext.cs
  - tests/BaseApi.Tests/Observability/ProcessorIdEnricherTests.cs
  - tests/BaseApi.Tests/Processor/FakeProcessorContext.cs
  - tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs
  - tests/BaseApi.Tests/Messaging/ProcessorResponderTests.cs
autonomous: true
requirements: [MLBL-03]
must_haves:
  truths:
    - "ProcessorIdentityFound carries Name + Version"
    - "GetProcessorBySourceHashConsumer populates Name + Version from the DB row"
    - "IProcessorContext / ProcessorContext store and expose Name + Version"
    - "Solution builds 0-error in Debug + Release (no CS0535 from the new interface members)"
  artifacts:
    - path: "src/Messaging.Contracts/ProcessorQueries.cs"
      provides: "ProcessorIdentityFound positional record extended with string Name, string Version"
      contains: "string Name, string Version"
    - path: "src/BaseProcessor.Core/Identity/ProcessorContext.cs"
      provides: "Name/Version auto-props + SetIdentity storage"
      contains: "Name = identity.Name"
    - path: "tests/BaseApi.Tests/Messaging/ProcessorResponderTests.cs"
      provides: "Name/Version assertions on the Found response"
      contains: "found.Message.Name"
  key_links:
    - from: "GetProcessorBySourceHashConsumer.cs"
      to: "ProcessorIdentityFound"
      via: "new ProcessorIdentityFound(p.Id, ..., p.Name, p.Version)"
      pattern: "p\\.Name, p\\.Version"
    - from: "ProcessorContext.SetIdentity"
      to: "ProcessorContext.Name/Version props"
      via: "Name = identity.Name; Version = identity.Version;"
      pattern: "Name = identity\\.Name"
---

<objective>
Extend the processor identity round-trip so the DB Name/Version reach the processor process (MLBL-03 (i)). This is the contract/context plumbing half of MLBL-03 — it gates the MeterProvider swap in Plan 03, which reads `found.Message.Name`/`.Version` at the swap call-site.

Purpose: the DB is the single source of truth for the processor's `{Name}_{Version}` metric identity; today `ProcessorIdentityFound` drops Name/Version and the responder never returns them. This plan threads them through the contract → responder → context, and updates every `IProcessorContext` test fake so the build stays 0-error (CS0535 firewall, Pitfall 6).

Output: extended contract record, populated responder, Name/Version-bearing context, 3 updated test fakes, and a 2-line extension to the responder round-trip test asserting the new fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-metrics-service-instance-labels/38-SPEC.md
@.planning/phases/38-metrics-service-instance-labels/38-CONTEXT.md
@.planning/phases/38-metrics-service-instance-labels/38-RESEARCH.md
@.planning/phases/38-metrics-service-instance-labels/38-PATTERNS.md

<interfaces>
<!-- Current contract (src/Messaging.Contracts/ProcessorQueries.cs:7-8) -->
public sealed record ProcessorIdentityFound(
    Guid Id, Guid? InputSchemaId, Guid? OutputSchemaId, Guid? ConfigSchemaId);

<!-- ProcessorReadDto already exposes Name (line 44) + Version (line 45) — verified; the responder edit is valid. -->
<!-- ProcessorContext.SetIdentity storage block (ProcessorContext.cs:57-63) stores Id + 3 schema Ids today. -->
<!-- WR-03 invariant (IProcessorContext.cs:22-31): identity props are plain auto-props, no barrier; safe to read cross-thread only after IsHealthy. Name/Version inherit this exactly. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the contract + responder with Name/Version, and prove it via the responder round-trip test</name>
  <read_first>
    - src/Messaging.Contracts/ProcessorQueries.cs (the record to extend — lines 7-8)
    - src/BaseApi.Service/Features/Processor/Responders/GetProcessorBySourceHashConsumer.cs (the projection at lines 25-26)
    - tests/BaseApi.Tests/Messaging/ProcessorResponderTests.cs (the harness; seeds Name="seed", Version="1.0.0" at lines 52-61; asserts Found fields at lines 107-111)
    - .planning/phases/38-metrics-service-instance-labels/38-RESEARCH.md (## MLBL-03 contract/context plumbing — the edit table, lines 227-239)
    - .planning/phases/38-metrics-service-instance-labels/38-PATTERNS.md (ProcessorQueries + responder + responder-test analog sections, lines 116-164)
  </read_first>
  <behavior>
    - Test 1 (extend existing SeededHash_Responds_ProcessorIdentityFound_With_Seeded_Fields): the Found message carries Name == "seed" and Version == "1.0.0" (the seeded row values).
    - Test 2 (unchanged): UnseededHash still responds ProcessorIdentityNotFound echoing the hash (no regression from the positional extension).
  </behavior>
  <action>
1. In `src/Messaging.Contracts/ProcessorQueries.cs`, extend the positional record (currently lines 7-8) by APPENDING two positional params — keep the existing 4 params in order:
   ```csharp
   public sealed record ProcessorIdentityFound(
       Guid Id, Guid? InputSchemaId, Guid? OutputSchemaId, Guid? ConfigSchemaId,
       string Name, string Version);
   ```
   Update the leading comment (lines 3-5) to note the Found fields now also carry the DB Name/Version (MLBL-03 single-source-of-truth).

2. In `GetProcessorBySourceHashConsumer.cs`, change the projection at lines 25-26 from:
   ```csharp
   new ProcessorIdentityFound(p.Id, p.InputSchemaId, p.OutputSchemaId, p.ConfigSchemaId)
   ```
   to:
   ```csharp
   new ProcessorIdentityFound(p.Id, p.InputSchemaId, p.OutputSchemaId, p.ConfigSchemaId, p.Name, p.Version)
   ```
   (`p` is a `ProcessorReadDto`; `p.Name` and `p.Version` are already exposed — verified RESEARCH A1, DTO lines 44-45.)

3. In `tests/BaseApi.Tests/Messaging/ProcessorResponderTests.cs`, inside `SeededHash_Responds_ProcessorIdentityFound_With_Seeded_Fields` after the existing `Assert.Equal(ConfigSchemaId, found.Message.ConfigSchemaId);` (line 111), ADD:
   ```csharp
   Assert.Equal("seed",  found.Message.Name);
   Assert.Equal("1.0.0", found.Message.Version);
   ```
   (The seed at lines 52-61 already sets `Name = "seed", Version = "1.0.0"`.)
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests --filter-not-trait "Category=RealStack" --filter "FullyQualifiedName~ProcessorResponderTests"</automated>
  </verify>
  <acceptance_criteria>
    - `ProcessorQueries.cs` contains `string Name, string Version` inside the `ProcessorIdentityFound` record declaration.
    - `GetProcessorBySourceHashConsumer.cs` contains `p.Name, p.Version` in the `new ProcessorIdentityFound(...)` call.
    - `ProcessorResponderTests.cs` contains `Assert.Equal("seed",  found.Message.Name)` and `Assert.Equal("1.0.0", found.Message.Version)`.
    - `ProcessorResponderTests` both facts pass (exit 0).
  </acceptance_criteria>
  <done>The contract carries Name+Version, the responder populates them from the DB row, and the round-trip test asserts the seeded values.</done>
</task>

<task type="auto">
  <name>Task 2: Add Name/Version to IProcessorContext + ProcessorContext, and update all three IProcessorContext test fakes (CS0535 firewall)</name>
  <read_first>
    - src/BaseProcessor.Core/Identity/IProcessorContext.cs (the interface; schema-Id props at lines 36-45; WR-03 invariant doc at lines 22-31)
    - src/BaseProcessor.Core/Identity/ProcessorContext.cs (auto-props at lines 33-42; SetIdentity storage at lines 57-63)
    - tests/BaseApi.Tests/Observability/ProcessorIdEnricherTests.cs (StubContext at line 41 — settable-props style)
    - tests/BaseApi.Tests/Processor/FakeProcessorContext.cs (FakeProcessorContext at line 18 — settable-props style)
    - tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs (RecordingContext at line 72 — delegate-to-_inner style)
    - .planning/phases/38-metrics-service-instance-labels/38-PATTERNS.md (## IProcessorContext fakes must add Name/Version — Pitfall 6 / CS0535, lines 266-283)
  </read_first>
  <action>
1. In `IProcessorContext.cs`, add two interface members immediately after the `ConfigSchemaId` member (after line 45), mirroring the schema-Id XML-doc style:
   ```csharp
   /// <summary>The resolved processor Name (DB single source of truth; null until Loop A completes). WR-03: read after IsHealthy.</summary>
   string? Name { get; }
   /// <summary>The resolved processor Version (DB single source of truth; null until Loop A completes). WR-03: read after IsHealthy.</summary>
   string? Version { get; }
   ```
   In the WR-03 memory-visibility invariant paragraph (lines 22-31), add `<see cref="Name"/>` and `<see cref="Version"/>` to the list of plain-auto-prop identity properties that are only safe to read after observing Healthy.

2. In `ProcessorContext.cs`, add two plain private-set auto-props after the `ConfigSchemaId` prop (after line 42) — NO volatile, mirror the existing Id/InputSchemaId shape:
   ```csharp
   /// <inheritdoc/>
   public string? Name { get; private set; }

   /// <inheritdoc/>
   public string? Version { get; private set; }
   ```
   Inside `SetIdentity` (lines 57-63), after `ConfigSchemaId = identity.ConfigSchemaId;`, append:
   ```csharp
   Name = identity.Name;
   Version = identity.Version;
   ```

3. Update ALL THREE test fakes that implement `IProcessorContext` (grep `: IProcessorContext` over tests/ returns exactly these — no other implementers):
   - `tests/BaseApi.Tests/Observability/ProcessorIdEnricherTests.cs` `StubContext` (line 41): add settable props mirroring its existing `Id`:
     ```csharp
     public string? Name { get; set; }
     public string? Version { get; set; }
     ```
   - `tests/BaseApi.Tests/Processor/FakeProcessorContext.cs` (line 18): add settable props mirroring its `Id`/`InputSchemaId` (lines 18-24):
     ```csharp
     public string? Name { get; set; }
     public string? Version { get; set; }
     ```
   - `tests/BaseApi.Tests/Processor/DispatchBindSequenceFacts.cs` `RecordingContext` (line 72): add delegate-to-`_inner` props mirroring its `Id => _inner.Id` style (lines 76-83):
     ```csharp
     public string? Name => _inner.Name;
     public string? Version => _inner.Version;
     ```
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug --nologo; dotnet build SK_P.sln -c Release --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `IProcessorContext.cs` contains `string? Name { get; }` and `string? Version { get; }`.
    - `ProcessorContext.cs` contains `Name = identity.Name;` and `Version = identity.Version;` inside SetIdentity, plus the two `public string? Name { get; private set; }` / `Version` auto-props.
    - `ProcessorIdEnricherTests.cs`, `FakeProcessorContext.cs`, and `DispatchBindSequenceFacts.cs` each contain a `Name` and `Version` member implementing `IProcessorContext`.
    - `dotnet build SK_P.sln` exits 0 in BOTH Debug and Release with 0 errors and 0 warnings (no CS0535).
  </acceptance_criteria>
  <done>IProcessorContext/ProcessorContext expose Name+Version, all three fakes compile against the extended interface, and the full solution builds 0-error/0-warning in both configurations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor DB row (`ProcessorEntity.Name`/`.Version`) → metric label value | The DB Name/Version now flow into a Prometheus label (`service_name`) via the contract; a label value crosses from stored data into telemetry cardinality space. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | (T)ampering / cardinality | `ProcessorIdentityFound.Name`/`.Version` → `service_name` Prom label | accept (existing mitigation) | A maliciously-crafted DB `Name`/`Version` becoming a high-cardinality or injected Prom label is bounded by the existing write-path FluentValidation `BaseDtoValidator`: `Name` NotEmpty/MaxLength(200) (VALID-05) and `Version` strict-SemVer (VALID-06) on BOTH create and update. The value set stays small + low-cardinality. No new endpoint or input surface is added in this plan; the contract only carries already-validated stored values. No additional mitigation needed. |
| T-38-02 | (I)nformation disclosure | the Name/Version in the bus response message | accept | Internal bus (RabbitMQ) inside the compose network; Name/Version are non-secret identity fields already present in the DB read DTO and logs. No disclosure delta. |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` and `-c Release` both exit 0, 0 warnings (CS0535 firewall holds).
- `dotnet test tests/BaseApi.Tests --filter-not-trait "Category=RealStack" --filter "FullyQualifiedName~ProcessorResponderTests"` passes (Name/Version asserted).
</verification>

<success_criteria>
- ProcessorIdentityFound has Name+Version; responder populates from p.Name/p.Version.
- IProcessorContext/ProcessorContext store + expose Name+Version (plain auto-props, WR-03 honored).
- All three IProcessorContext test fakes updated; solution builds 0-error/0-warning Debug+Release.
- ProcessorResponderTests asserts the seeded Name/Version on the Found response.
</success_criteria>

<output>
After completion, create `.planning/phases/38-metrics-service-instance-labels/38-01-SUMMARY.md`
</output>
