---
phase: 32-cancelled-circuit-breaker
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseApi.Service/Features/Step/StepEntryCondition.cs
  - src/Messaging.Contracts/StepOutcome.cs
  - tests/BaseApi.Tests/Orchestrator/StepEntryConditionEnumFacts.cs
autonomous: true
requirements: [req-6]
must_haves:
  truths:
    - "StepEntryCondition has no PreviousCancelled member and no member numbered 3"
    - "A StepDto with EntryCondition == 3 fails IsInEnum validation"
    - "StepOutcome.Cancelled = 3 is kept (live for the token-cancellation EXEC-08 outcome)"
    - "Existing members 0/1/2/4/5 are NOT renumbered"
  artifacts:
    - path: "src/BaseApi.Service/Features/Step/StepEntryCondition.cs"
      provides: "StepEntryCondition with 3 left as a numeric gap"
      contains: "Always = 4"
    - path: "src/Messaging.Contracts/StepOutcome.cs"
      provides: "StepOutcome.Cancelled = 3 retained with corrected mirror comment"
      contains: "Cancelled = 3"
  key_links:
    - from: "StepDtoValidator.IsInEnum()"
      to: "StepEntryCondition (no member 3)"
      via: "FluentValidation IsInEnum auto-reject"
      pattern: "EntryCondition.*IsInEnum"
---

<objective>
Remove the dead `StepEntryCondition.PreviousCancelled (3)` entry-condition member — leaving `3` as a numeric gap (NOT renumbering 0/1/2/4/5) — so `StepDtoValidator.IsInEnum()` auto-rejects `EntryCondition == 3` going forward. Keep `StepOutcome.Cancelled = 3` (still live for the token-cancellation EXEC-08 outcome) and fix its now-stale mirror comment. Fully standalone — disjoint from every other Plan 32 file.

Purpose: Close out req-6 (D-12). The member is never set by live data (grep-confirmed 2 source touch-points only); its removal hardens validation without touching any behavior (`StepAdvancement.SelectNext` never names `PreviousCancelled`).
Output: One deleted enum line, one corrected comment, and a hermetic test proving `EntryCondition == 3` now fails validation while `StepOutcome.Cancelled = 3` is intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cancelled-circuit-breaker/32-SPEC.md
@.planning/phases/32-cancelled-circuit-breaker/32-CONTEXT.md
@.planning/phases/32-cancelled-circuit-breaker/32-PATTERNS.md

<interfaces>
<!-- Live source 2026-06-04. -->

From src/BaseApi.Service/Features/Step/StepEntryCondition.cs (the member to delete is :19):
```csharp
public enum StepEntryCondition
{
    PreviousProcessing = 0,
    PreviousCompleted = 1,
    PreviousFailed = 2,
    PreviousCancelled = 3,   // <-- DELETE this line only; leave 3 as a numeric gap, do NOT renumber 4/5
    Always = 4,
    Never = 5,
}
```

From src/Messaging.Contracts/StepOutcome.cs (KEEP the member; update the comment at :20 + the remark at :5-6):
```csharp
public enum StepOutcome
{
    Processing = 0, // == StepEntryCondition.PreviousProcessing
    Completed = 1,  // == StepEntryCondition.PreviousCompleted
    Failed = 2,     // == StepEntryCondition.PreviousFailed
    Cancelled = 3,  // == StepEntryCondition.PreviousCancelled   <-- comment now stale (Previous*(3) removed)
}
```

From src/BaseApi.Service/Features/Step/StepDtoValidator.cs (NO change — behavior falls out of IsInEnum):
```csharp
RuleFor(x => x.EntryCondition).IsInEnum();   // appears at both Create (:37-38) and Update (:68-69)
```
FluentValidation `IsInEnum()` rejects any integer value not defined on the enum — once `3` has no member, `EntryCondition == 3` fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete PreviousCancelled (3); fix the StepOutcome mirror comment; pin behavior with a hermetic test (req-6, D-12)</name>
  <read_first>
    - src/BaseApi.Service/Features/Step/StepEntryCondition.cs (full file — delete only :19)
    - src/Messaging.Contracts/StepOutcome.cs (full file — KEEP :20 member, fix its comment + the :5-6 enum remark)
    - src/BaseApi.Service/Features/Step/StepDtoValidator.cs (confirm NO change needed — IsInEnum at :37-38 and :68-69)
    - src/Orchestrator/Dispatch/StepAdvancement.cs (confirm SelectNext never names PreviousCancelled — RESEARCH Unknown-5)
    - 32-RESEARCH.md §"Unknown 5" (grep-confirmed 2 source touch-points; the IsInEnum + Cancelled-kept confirmations)
    - 32-PATTERNS.md §"StepEntryCondition.cs + StepOutcome.cs" (exact action: delete :19, comment-only update on the outcome)
  </read_first>
  <action>
**StepEntryCondition.cs:** delete ONLY the line `PreviousCancelled = 3,` (current :19). Leave `Always = 4,` and `Never = 5,` UNCHANGED (do NOT renumber to 3/4 — `3` is now a deliberate numeric gap so existing DB integer step data is NOT reinterpreted, per D-12 and the file's own ENTITY-06 "assignments preserved across migrations" doc). Result enum: `PreviousProcessing=0, PreviousCompleted=1, PreviousFailed=2, Always=4, Never=5`. Do NOT touch `StepDtoValidator.cs` — the rejection of `3` falls out of the existing `IsInEnum()`.

**StepOutcome.cs:** KEEP `Cancelled = 3` (it is LIVE — `EntryStepDispatchConsumer.cs:257` maps it in `OutcomeLabel`, `BuildCancelled` `:289` emits it for the EXEC-08 token-cancellation outcome, unchanged by this phase). Update ONLY the comment on that line from `// == StepEntryCondition.PreviousCancelled` to e.g. `// special-cased by the consumer (token-cancellation, EXEC-08); not matched by SelectNext`. Also update the enum-level `<summary>`/`<remarks>` remark at `:5-6` that says "Int values mirror StepEntryCondition.Previous* (0-3)" — `Previous*(3)` no longer exists; reword to note `Cancelled (3)` is special-cased and no longer mirrors a `Previous*` member.

Create `tests/BaseApi.Tests/Orchestrator/StepEntryConditionEnumFacts.cs` (hermetic). Assert:
1. `!Enum.IsDefined(typeof(StepEntryCondition), 3)` AND `Enum.GetNames(typeof(StepEntryCondition))` does NOT contain `"PreviousCancelled"`.
2. The surviving members keep their values: `(int)StepEntryCondition.Always == 4`, `(int)StepEntryCondition.Never == 5`, `(int)StepEntryCondition.PreviousFailed == 2` (proves no renumber).
3. A `StepDto` (or the validator's DTO) with `EntryCondition == (StepEntryCondition)3` fails `StepDtoValidator` validation on the `EntryCondition` `IsInEnum` rule (construct the validator, run `Validate`, assert `IsValid == false` with an error on `EntryCondition`). Mirror the validator-construction idiom used by existing `StepDtoValidator` tests if one exists; otherwise `new StepDtoValidator().Validate(dto)`.
4. `(int)StepOutcome.Cancelled == 3` (the outcome member is RETAINED).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-class "*StepEntryConditionEnumFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PreviousCancelled" src/` returns 0 (both source touch-points gone: the member deleted, the StepOutcome comment reworded)
    - `StepEntryCondition.cs` contains `Always = 4` and `Never = 5` with those exact values (no renumber)
    - `StepOutcome.cs` still contains `Cancelled = 3`
    - `dotnet build SK_P.sln -c Debug` exits 0, 0 Warning / 0 Error
    - `StepEntryConditionEnumFacts` asserts: no member 3 on StepEntryCondition; EntryCondition==3 fails IsInEnum; StepOutcome.Cancelled==3 kept
    - `dotnet test tests/BaseApi.Tests -- --filter-class "*StepEntryConditionEnumFacts"` exits 0
  </acceptance_criteria>
  <done>`PreviousCancelled (3)` is removed as a numeric gap; `EntryCondition == 3` is auto-rejected by `IsInEnum`; `StepOutcome.Cancelled = 3` is retained; a hermetic test pins all four facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → WebApi StepDto | An attacker could submit `EntryCondition == 3` (a now-undefined enum value) to corrupt step routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Tampering | StepDto.EntryCondition input | mitigate | Removing the `3` member makes `StepDtoValidator`'s existing `IsInEnum()` auto-reject `EntryCondition == 3` at the WebApi boundary (Create + Update). Hardens, not weakens, the validation surface. |
| T-32-06b | Tampering | existing DB step rows | accept | `3` left as a numeric gap (no renumber) so no existing integer step value is silently reinterpreted; D-12 expects no live row uses 3 (dual-pipeline steps use Always/4 — the live-data assertion is owned by Plan 05's E2E/data check). |
</threat_model>

<verification>
- `grep -rn "PreviousCancelled" src/` returns nothing.
- `dotnet build SK_P.sln -c Release` = 0 Warning / 0 Error.
- `StepEntryConditionEnumFacts` GREEN; full hermetic suite GREEN (zero regression).
</verification>

<success_criteria>
- No `PreviousCancelled` member; no member numbered `3` on `StepEntryCondition`; `IsInEnum` rejects 3.
- `StepOutcome.Cancelled = 3` retained with a corrected comment.
- 0/1/2/4/5 not renumbered.
</success_criteria>

<output>
After completion, create `.planning/phases/32-cancelled-circuit-breaker/32-03-SUMMARY.md`. Note the "no live step has EntryCondition == 3" DATA assertion is owned by Plan 05 (E2E/DB check), not this code change.
</output>
