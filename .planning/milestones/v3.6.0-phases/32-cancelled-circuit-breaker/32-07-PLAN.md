---
phase: 32-cancelled-circuit-breaker
plan: 07
type: execute
wave: 3
depends_on: [32-02, 32-03, 32-04, 32-05, 32-06]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs
  - scripts/phase-32-close.ps1
autonomous: false
requirements: [req-5, req-6, req-8]
user_setup:
  - service: docker-compose-stack
    why: "The live breaker trip → halt → manual resume round-trip needs the full v3.6.0 compose stack up healthy with REBUILT processor-sample/orchestrator/baseapi containers (embedded SourceHash must match the host build) — not observable in the dev harness"
    dashboard_config:
      - task: "docker compose up -d --build processor-sample orchestrator baseapi-service before the E2E + close gate"
        location: "local docker compose"
must_haves:
  truths:
    - "On a live breaker trip, NO Cancelled ExecutionResult is sent to orchestrator-result; the dead-lettered message still reaches _error"
    - "After the breaker trips, the Quartz job is unscheduled (future cron fires stop) and the cancelled marker is set with no TTL (TTL == -1)"
    - "Resume = clear the marker + re-POST /orchestration/start re-fires the workflow"
    - "No live step row has EntryCondition == 3"
    - "phase-32-close.ps1 holds the triple-SHA (BEFORE == AFTER) including an explicit scan-clean of the no-TTL skp:cancelled:* namespace"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs"
      provides: "live breaker trip → halt → resume E2E + no-Cancelled-on-result-queue + _error-retained + TTL==-1 + no-live-EntryCondition-3 assertions"
      min_lines: 80
    - path: "scripts/phase-32-close.ps1"
      provides: "3×GREEN + triple-SHA close gate with explicit skp:cancelled:* delete before the AFTER snapshot"
      contains: "skp:cancelled:*"
  key_links:
    - from: "CancelledCircuitBreakerE2ETests teardown"
      to: "skp:cancelled:* (no-TTL marker)"
      via: "L2KeysToCleanup explicit registration"
      pattern: "skp:cancelled"
    - from: "phase-32-close.ps1 settle/teardown"
      to: "skp:cancelled:* namespace"
      via: "explicit redis-cli del before AFTER snapshot"
      pattern: "skp:cancelled:\\*"
---

<objective>
The capstone: the live real-stack proof that the two-level stop works end-to-end, plus the `phase-32-close.ps1` 3×GREEN + triple-SHA close gate. The E2E drives a real workflow to infra-exhaustion (breaker trip), asserts the halt (marker set no-TTL, Quartz unscheduled, NO `Cancelled` ExecutionResult on `orchestrator-result`, `_error` still receives the dead-letter), then resumes (clear marker + re-`POST /orchestration/start`) and asserts the workflow re-fires. The close gate extends the Phase-31 gate with the one substantive divergence: an EXPLICIT scan-clean of the no-TTL `skp:cancelled:*` namespace before the AFTER snapshot (else the triple-SHA drifts every run).

Purpose: Close req-5 (no Cancelled on the result queue; `_error` retained), req-8's live half (manual resume), and req-6's data assertion (no live step has `EntryCondition == 3`). Prove the whole phase live and lock the net-zero gate.
Output: One real-stack E2E test + the `phase-32-close.ps1` gate script, both verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cancelled-circuit-breaker/32-SPEC.md
@.planning/phases/32-cancelled-circuit-breaker/32-CONTEXT.md
@.planning/phases/32-cancelled-circuit-breaker/32-RESEARCH.md
@.planning/phases/32-cancelled-circuit-breaker/32-PATTERNS.md
@.planning/phases/32-cancelled-circuit-breaker/32-VALIDATION.md
@.planning/phases/32-cancelled-circuit-breaker/32-04-SUMMARY.md
@.planning/phases/32-cancelled-circuit-breaker/32-06-SUMMARY.md

<interfaces>
<!-- Analogs (live 2026-06-04). -->

E2E analog: tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs (cloned from SampleRoundTripE2ETests). Reuse: genuine embedded-SourceHash reflection off Processor.Sample.dll, GET-or-create Processor row (idempotent on the unique source-hash), PollForHealthyLivenessAsync (poll the real processor-sample skp:{procId:D} heartbeat), PollEsForLog (otel→ES assertion), RealStackWebAppFactory, and the net-zero teardown that registers run keys into L2KeysToCleanup. Traits: [Trait("Category","E2E")] + [Trait("Category","RealStack")] + [Collection("Observability")].

Close-gate analog: scripts/phase-31-close.ps1 (byte-faithful clone; relabel 31→32). Its redis triple-SHA uses an UNFILTERED `docker exec sk-redis redis-cli --scan` (so skp:cancelled:* IS captured automatically) and a SETTLE-DRAIN loop (lines ~236-256) that waits ≤330s for the TTL-bounded skp:flag:*/skp:data:* keys to drain back to the BEFORE baseline. NO FLUSHDB; rebuilds are operator-run before the gate.

Marker key (Plan 02): `L2ProjectionKeys.Cancelled(workflowId) => skp:cancelled:{workflowId:D}`, no TTL, value "true".
Resume endpoint (D-08): existing `POST /orchestration/start` (no new API).
</interfaces>

<critical_divergence>
The no-TTL `skp:cancelled:*` marker (D-07) is the ONE thing that breaks the Phase-31 settle-drain: it will NEVER self-expire, so the gate's `--scan` AFTER set will differ from BEFORE by every `skp:cancelled:{wf:D}` key the E2E created → the redis triple-SHA drifts every run (the exact MEMORY failure mode `reference_close_gate_container_rebuild_and_flag_churn` / NET-ZERO-31). The gate MUST explicitly DELETE `skp:cancelled:*` before the AFTER snapshot, AND the E2E teardown MUST register the marker keys it creates into `L2KeysToCleanup`. Both belt-and-braces.
</critical_divergence>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CancelledCircuitBreakerE2ETests — live breaker trip → halt → resume (req-5, req-8-live) + no-live-EntryCondition-3 (req-6 data)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs (the full clone source — SourceHash reflection, GET-or-create Processor, liveness poll, Start, PollEsForLog, net-zero teardown / L2KeysToCleanup)
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the base round-trip + RealStackWebAppFactory)
    - 32-PATTERNS.md §"Real-stack E2E" (the divergence: teardown MUST scan-clean skp:cancelled:* — NO TTL, won't self-expire)
    - 32-SPEC.md req-5 + req-8 Acceptance + 32-VALIDATION.md rows 32-T-req5 / 32-T-req8e / 32-T-req6b
    - src/Messaging.Contracts/OrchestratorQueues.cs (orchestrator-result is the shared queue req-5 says must NOT carry a Cancelled on the breaker path) + src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (Cancelled builder)
  </read_first>
  <action>
Create `tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs` cloning `IdempotentExactlyOnceE2ETests` (genuine embedded SourceHash reflection, GET-or-create Processor row, `PollForHealthyLivenessAsync`, `RealStackWebAppFactory`, net-zero teardown). Traits `[Trait("Category","E2E")]`, `[Trait("Category","RealStack")]`, `[Collection("Observability")]`.

**Drive a breaker trip:** seed a workflow whose entry step dispatches to a step/processor that will INFRA-fail through `Limit` attempts. The cleanest reproducible infra-exhaustion: point the entry step at a processor/input that forces a Redis/Send infra fault on every attempt (e.g. an input the processor can't resolve in a way that throws infra, OR — if the Sample processor cannot be made to infra-throw — induce it by seeding the dispatch so the output-write/Send path faults; consult the breaker seam in 32-04-SUMMARY for the exact infra trigger the unit tests used and reproduce it live). The trip must exhaust `UseMessageRetry(Immediate(Limit))` so MassTransit auto-publishes `Fault<EntryStepDispatch>` AND dead-letters to `_error`.

**Assert the halt (req-5 + req-2 live):**
1. After the trip, `GET skp:cancelled:{workflowId:D}` returns `"true"` AND `TTL skp:cancelled:{workflowId:D}` returns `-1` (no TTL — D-07). Use the redis handle the E2E already has.
2. NO `Cancelled` `ExecutionResult` reaches `orchestrator-result` on the breaker path (req-5) — assert via ES/log absence of a Cancelled-outcome advancement for this workflow on the breaker path (distinguish from the pre-existing token-cancellation Cancelled which is a different trigger), OR by confirming the orchestrator emitted the `FaultUnscheduleConsumer` WARN "Fault halt — unscheduling workflow {WorkflowId}" (the halt came via the Fault fanout, not a Cancelled result). Prefer the positive Fault-halt-log assertion via `PollEsForLog` on `attributes.WorkflowId == {wfId}` + the "Fault halt" message.
3. `_error` still received the dead-lettered message (req-5) — assert the `_error` queue / dead-letter is non-empty for this message, or (if `_error` is not directly pollable in the harness) assert the Fault was published (the same exhaustion produces both — D-03; the Fault-halt log is the observable proxy).
4. The Quartz job is unscheduled — assert no further round-trip output key appears for this workflow after the trip across a settle window (future cron fires stopped).

**Assert resume (req-8-live):** clear the marker (`DEL skp:cancelled:{workflowId:D}`) and re-`POST /orchestration/start` for the workflow; assert the workflow fires again (a fresh round-trip effect — e.g. a new `skp:data:*` output key or an advance log appears, in-flight messages no longer dropped).

**Assert no live step has EntryCondition == 3 (req-6 data):** run a data assertion against the target DB — `SELECT COUNT(*) FROM "Steps" WHERE "EntryCondition" = 3` expects 0 (via the EF context / the test's DB access; mirror how other E2E/data tests query). If the prod DB isn't reachable in the harness, run it as a seed-and-query against the E2E's DB. This satisfies VALIDATION row 32-T-req6b.

**Net-zero teardown (CRITICAL — D-07 no-TTL):** register EVERY `skp:cancelled:{workflowId:D}` key the test created into `L2KeysToCleanup` (the existing net-zero mechanism) so it is deleted in teardown — these keys have NO TTL and will NOT self-expire (unlike skp:flag:*/skp:data:* which the 330s settle-drain handles). Also drain any skp:data:*/skp:flag:* the run created, per the existing teardown. If resume re-fires, ensure the re-fire's keys are also cleaned and the workflow is stopped in teardown (POST /orchestration/stop) so it doesn't keep cron-firing and churning keys (the NET-ZERO-31 lesson).
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs` exists with `[Trait("Category","RealStack")]`
    - Contains a `TTL` == -1 assertion on `skp:cancelled:` (no-TTL marker)
    - Contains a "Fault halt" / FaultUnscheduleConsumer WARN log assertion (the halt came via Fault fanout, not a Cancelled result) — req-5
    - Contains a clear-marker (DEL skp:cancelled) + re-POST /orchestration/start resume sequence — req-8
    - Contains a `WHERE "EntryCondition" = 3` count == 0 data assertion — req-6
    - Teardown registers `skp:cancelled:*` keys into `L2KeysToCleanup` (grep `skp:cancelled` in the teardown region)
    - `dotnet build tests/BaseApi.Tests -c Debug` exits 0, 0 Warning / 0 Error
    - Full hermetic suite `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` GREEN (the new E2E is correctly excluded — zero hermetic regression)
  </acceptance_criteria>
  <done>The real-stack E2E authored: it trips the breaker live, asserts the no-TTL marker + Fault-halt (no Cancelled on the result queue, _error retained) + unschedule, resumes via clear-marker + re-Start, asserts no live EntryCondition==3, and net-zero-cleans the no-TTL skp:cancelled:* keys. (Live RUN is the Task-3 human-verify gate.)</done>
</task>

<task type="auto">
  <name>Task 2: phase-32-close.ps1 — 3×GREEN + triple-SHA with explicit skp:cancelled:* scan-clean (req-5/req-8 gate)</name>
  <read_first>
    - scripts/phase-31-close.ps1 (FULL file — the byte-faithful clone source: the steady-state Processor pre-flight, the UNFILTERED redis --scan BEFORE/AFTER, the ~236-256 settle-drain loop, the 3-GREEN loop, the Release+Debug zero-warning build, the triple-SHA, NO FLUSHDB)
    - 32-PATTERNS.md §"Close script" (the ONE substantive divergence: explicitly DELETE skp:cancelled:* before the AFTER snapshot — the no-TTL marker won't drain in the settle loop)
    - STATE.md Phase 31.1 operational notes + MEMORY reference_close_gate_container_rebuild_and_flag_churn (rebuild containers first; net-zero teardown must scan-clean no-TTL namespaces or the triple-SHA drifts)
    - 32-VALIDATION.md §"Sampling Rate" Phase gate + §"Wave 0 Requirements" close-gate teardown extension bullet
  </read_first>
  <action>
Create `scripts/phase-32-close.ps1` as a byte-faithful clone of `scripts/phase-31-close.ps1`, relabeling phase identifiers `31 → 32` throughout (header comment, `Write-Host` banners, the steady-state Processor row `description` string, the provenance/clone-source comment). Keep the version label `v3.6.0` (this phase is still in the v3.6.0 milestone — Phase 31/31.1 are too; do NOT bump to a new milestone version). Keep the steady-state Processor.Sample pre-flight (GET-or-create on the unique source-hash → stable procId/liveness key/queue), the `$services` full v3.6.0 stack list (processor-sample REQUIRED healthy), the UNFILTERED `docker exec sk-redis redis-cli --scan` BEFORE/AFTER capture (the unfiltered scan already captures `skp:cancelled:*` in the triple-SHA — no widening needed), the 3-consecutive-GREEN loop, the Release+Debug zero-warning build, the psql `\l` + rabbitmqctl `list_queues` SHAs, and NO FLUSHDB.

**The ONE substantive divergence** — extend the settle/teardown step (the ~236-256 settle-drain region) so that BEFORE the AFTER `--scan` snapshot, the gate EXPLICITLY deletes the no-TTL cancelled markers:
```powershell
# Phase 32 (D-07): the skp:cancelled:* marker has NO TTL — it will NOT drain in the settle loop above
# (unlike the TTL-bounded skp:flag:*/skp:data:*). Without an explicit delete the AFTER --scan would carry
# every skp:cancelled:{wf} the E2E created and the redis triple-SHA would drift every run (NET-ZERO-31).
# The E2E teardown also registers these into L2KeysToCleanup (belt-and-braces); this is the gate-side guard.
$cancelledKeys = docker exec sk-redis redis-cli --scan --pattern 'skp:cancelled:*'
if ($cancelledKeys) {
    $cancelledKeys | ForEach-Object { docker exec sk-redis redis-cli del $_ | Out-Null }
    Write-Host "  Deleted $($cancelledKeys.Count) no-TTL skp:cancelled:* marker(s) before AFTER snapshot." -ForegroundColor Gray
}
```
Place this AFTER the existing skp:flag:*/skp:data:* settle-drain wait and BEFORE the AFTER `$afterRedis = ... --scan ...` capture. Adjust the settle-mismatch diagnostic message (the "Investigate a leaked skp: key" block) to also mention `skp:cancelled:*` as a possible residual. Do NOT FLUSHDB; do NOT filter the BEFORE/AFTER scans (the cancelled delete is targeted, the snapshot stays full-keyspace). Ensure the script ParseFile-validates clean and is BOM-free (the STATE/MEMORY encoding caution).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path ./scripts/phase-32-close.ps1), [ref]$null, [ref]$null); if ($?) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-32-close.ps1` exists and ParseFile-validates (exit 0), BOM-free
    - Contains the explicit `redis-cli --scan --pattern 'skp:cancelled:*'` + `del` block placed before the AFTER `--scan` snapshot
    - Retains the UNFILTERED BEFORE/AFTER `redis-cli --scan` (grep finds `--scan` without a `skp:` prefix filter on the snapshot lines), the 3-GREEN loop, the triple-SHA (psql \l + redis --scan + rabbitmqctl list_queues), and NO `FLUSHDB`
    - Phase labels updated 31→32; version stays v3.6.0
    - `grep -c "FLUSHDB" scripts/phase-32-close.ps1` == 0
  </acceptance_criteria>
  <done>The close gate is a faithful Phase-31 clone plus the explicit no-TTL skp:cancelled:* scan-clean so the redis triple-SHA holds BEFORE==AFTER across runs; it parses clean and is BOM-free. (Live RUN is the Task-3 human-verify gate.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live E2E + close-gate run (operator, blocking)</name>
  <action>Operator-run only (not autonomous): rebuild the v3.6.0 containers, run the live CancelledCircuitBreakerE2ETests, then run phase-32-close.ps1 and confirm GATE_EXIT=0 with the triple-SHA HELD. See <how-to-verify> for exact commands.</action>
  <what-built>The CancelledCircuitBreakerE2ETests (live breaker trip → halt → resume) and phase-32-close.ps1 (3×GREEN + triple-SHA with the no-TTL skp:cancelled:* scan-clean). Both are authored + build/parse-verified, but the LIVE run needs the full v3.6.0 compose stack up healthy with REBUILT containers (embedded SourceHash must match the host build — the wire contract / breaker code is new this phase) — not observable in this environment.</what-built>
  <how-to-verify>
    1. Rebuild the live containers so they run the Phase-32 code (the embedded SourceHash must match the host build, else liveness fails on a procId mismatch):
       `docker compose up -d --build processor-sample orchestrator baseapi-service`
    2. Run the live E2E (expect GREEN):
       `dotnet test tests/BaseApi.Tests -- --filter-class "*CancelledCircuitBreakerE2ETests"`
       Expect: breaker trips, skp:cancelled marker set with TTL==-1, Fault-halt WARN log in ES, no Cancelled ExecutionResult on orchestrator-result, _error received the dead-letter, resume re-fires, no live EntryCondition==3.
    3. Run the close gate (expect GATE_EXIT=0):
       `pwsh -NoProfile -File ./scripts/phase-32-close.ps1`
       Expect: 3×GREEN full suite, Release+Debug 0/0, triple-SHA BEFORE==AFTER all HELD (the explicit skp:cancelled:* delete keeps the redis SHA stable). Read GATE_*_EXIT from the gate's own output, NOT the background-task wrapper exit.
  </how-to-verify>
  <resume-signal>Type "approved" with the GATE_EXIT=0 + triple-SHA evidence, or describe the failure (e.g. a drifted redis SHA → check the skp:cancelled:* delete ran; a never-tripping breaker → re-check the Wave-0 R1 pinned boundary in 32-01-SUMMARY).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live compose stack | The E2E drives the real broker/Redis/Postgres; the close gate snapshots the live keyspace. No untrusted external input — the test mints its own workflow. |
| operator → resume (clear marker + re-Start) | Resume is operator-driven (manual clear + re-POST) — the human-in-the-loop re-arm the breaker design wants (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Tampering | unauthorized resume (clearing the marker) | accept | Resume requires Redis write access to `DEL skp:cancelled:*` + a `POST /orchestration/start` — the same broker-internal Redis + existing WebApi trust boundary already governing every projection; no new auth surface. The manual-clear design is deliberate (D-08: a human confirms the fault is fixed before re-arming). Documented-accept. |
| T-32-09 | Denial of Service | marker-namespace exhaustion (no-TTL keys accumulating) | mitigate | The no-TTL `skp:cancelled:*` keys are bounded by the number of distinct cancelled workflows (operator-cleared on resume); the close gate's explicit scan-clean + the E2E's `L2KeysToCleanup` registration prevent unbounded test-run accumulation. In production the marker is one key per halted workflow, cleared on resume — bounded by the workflow count, not per-fire churn. |
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests -c Debug` = 0/0; full hermetic suite GREEN (E2E excluded).
- `phase-32-close.ps1` ParseFile exit 0, BOM-free, NO FLUSHDB, contains the skp:cancelled:* delete.
- LIVE: GATE_EXIT=0 with triple-SHA BEFORE==AFTER (operator-run, Task 3).
</verification>

<success_criteria>
- Live: breaker trips, no-TTL marker set, Quartz unscheduled, NO Cancelled on orchestrator-result, _error retained, resume re-fires.
- No live step has EntryCondition == 3.
- phase-32-close.ps1 holds the triple-SHA with the explicit skp:cancelled:* scan-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/32-cancelled-circuit-breaker/32-07-SUMMARY.md`. Document the close-gate evidence (GATE_EXIT, triple-SHA values) once the operator reports them. req-5/req-8-live/req-6-data marked complete only after GATE_EXIT=0.
</output>
