---
phase: 32-cancelled-circuit-breaker
plan: 05
type: execute
wave: 2
depends_on: [32-02]
files_modified:
  - src/Orchestrator/Consumers/ResultConsumer.cs
  - tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs
autonomous: true
requirements: [req-3, req-7]
must_haves:
  truths:
    - "ResultConsumer ack-and-discards an in-flight ExecutionResult when the cancelled marker is set"
    - "Results for other (un-cancelled) workflows are unaffected"
    - "The flag[H]==Ack drop gate increments orchestrator_result_deduped once"
    - "The cancelled check reads the cancelled marker only, never flag[H] (D-13)"
  artifacts:
    - path: "src/Orchestrator/Consumers/ResultConsumer.cs"
      provides: "check-and-drop gate + orchestrator_result_deduped increment at the flag[H] gate"
      contains: "L2ProjectionKeys.Cancelled"
  key_links:
    - from: "ResultConsumer check-and-drop gate"
      to: "L2ProjectionKeys.Cancelled(m.WorkflowId)"
      via: "StringGetAsync after flag[H] gate, before store.TryGet"
      pattern: "StringGetAsync.*Cancelled"
    - from: "flag[H]==Ack gate"
      to: "metrics.ResultDeduped"
      via: "Add(1, ProcessorId)"
      pattern: "ResultDeduped.Add"
---

<objective>
The orchestrator half of the in-flight stop. Two edits to `ResultConsumer`: (1) the check-and-drop gate that ack-and-discards an in-flight `ExecutionResult` for a cancelled workflow (req-3, orchestrator side — so a result already on the shared `orchestrator-result` queue when the breaker trips does not advance the DAG); (2) the dedup counter at the existing `flag[H]=="Ack"` drop gate (req-7, orchestrator dedup). The orchestrator does NOT set the marker or trip the breaker — it only checks-and-drops (the trip is processor-side per D-01).

Purpose: Complete req-3's "every receiver checks the marker before processing" — both the processor (Plan 04) and the orchestrator (this plan) drain to a halt. Surface the orchestrator-side redelivery-collapse rate via `orchestrator_result_deduped`.
Output: The extended consumer + a hermetic Facts class pinning the orchestrator check-and-drop + dedup counter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cancelled-circuit-breaker/32-SPEC.md
@.planning/phases/32-cancelled-circuit-breaker/32-CONTEXT.md
@.planning/phases/32-cancelled-circuit-breaker/32-RESEARCH.md
@.planning/phases/32-cancelled-circuit-breaker/32-PATTERNS.md

<interfaces>
<!-- Live source 2026-06-04. Plan-02 additions (Cancelled builder, CancelledMarkerValue, ResultDeduped) are dependencies. -->

From src/Messaging.Contracts/Projections/L2ProjectionKeys.cs (added by Plan 02):
```csharp
public static string Cancelled(Guid workflowId) => $"{Prefix}cancelled:{workflowId:D}";
public const string CancelledMarkerValue = "true";
```

From src/Orchestrator/Observability/OrchestratorMetrics.cs (ResultDeduped added by Plan 02):
```csharp
public Counter<long> ResultConsumed { get; }   // existing
public Counter<long> ResultDeduped { get; }    // NEW (Plan 02) — D-10
```

From src/Orchestrator/Consumers/ResultConsumer.cs — current structure (line anchors):
- :40-46 — ctor `(IWorkflowL1Store store, StepAdvancement advancement, IStepDispatcher dispatcher, IConnectionMultiplexer redis, OrchestratorMetrics metrics, ILogger<ResultConsumer> logger)`
- :55 — existing `metrics.ResultConsumed.Add(1, new KeyValuePair<string,object?>("ProcessorId", m.ProcessorId.ToString("D")))` — the ProcessorId-tag idiom (m.ProcessorId is a non-nullable Guid → NO bang, unlike the processor)
- :60 — `var db = redis.GetDatabase();`
- :65-66 — existing flag[H] gate: `if ((string?)await db.StringGetAsync(L2ProjectionKeys.Flag(m.H)) == "Ack") return;`
- :69 — the L1 `store.TryGet(m.WorkflowId, out var wf)` (the cancelled check goes BEFORE this)
- `m.WorkflowId` available on ExecutionResult (via IExecutionCorrelated); `m.ProcessorId` non-nullable Guid

Analog test: tests/BaseApi.Tests/Orchestrator/ResultAckTests.cs + ResultConsumeTests.cs + OrchestratorTestStubs.cs (the OrchestratorTestStubs.Context<ExecutionResult> + NoopRedis/IDatabase substitute idioms; OrchestratorTestStubs.Metrics() real-IMeterFactory helper).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResultConsumer check-and-drop gate + orchestrator_result_deduped counter (req-3 orchestrator, req-7 dedup; D-05/D-10)</name>
  <behavior>
    - flag[m.H]=="Ack": consumer increments orchestrator_result_deduped (tagged ProcessorId) once, returns (existing drop), no dispatch/advancement
    - cancelled marker set (skp:cancelled:{m.WorkflowId:D} == "true"): consumer returns (ack-and-discard) producing NO dispatch/advancement, NO dedup counter, NO marker write, NO L1 read
    - neither: consumer proceeds to the existing L1 read + manifest unbundle + fan-out (unchanged)
    - a result for a DIFFERENT (un-cancelled) workflow proceeds normally even if some other workflow's marker is set
    - the cancelled check reads L2ProjectionKeys.Cancelled(m.WorkflowId), NEVER L2ProjectionKeys.Flag(...) (D-13)
  </behavior>
  <read_first>
    - src/Orchestrator/Consumers/ResultConsumer.cs (full file — :55 ProcessorId-tag idiom (non-nullable, no bang); :65-66 flag gate; :69 the store.TryGet the cancelled check precedes)
    - tests/BaseApi.Tests/Orchestrator/ResultAckTests.cs + ResultConsumeTests.cs (the consume-drive + IDatabase substitute idioms)
    - tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs (Context<T>, NoopRedis, Metrics() helper)
    - 32-RESEARCH.md §"Unknown 3" Orchestrator paragraph (place AFTER the flag[H] gate at :65 and BEFORE the L1 store.TryGet at :69; identical shape using m.WorkflowId)
    - 32-PATTERNS.md §"ResultConsumer.cs" Edit 1 + Edit 2 + the Divergence note (ResultConsumer does NOT set the marker or trip — check-and-drop only)
  </read_first>
  <action>
**Edit 1 — dedup counter at the existing flag[m.H]=="Ack" gate (:65-66).** Before the `return`, add the increment using the :55 ProcessorId-tag idiom (m.ProcessorId is non-nullable Guid — NO bang) with the NEW counter:
```csharp
if ((string?)await db.StringGetAsync(L2ProjectionKeys.Flag(m.H)) == "Ack")
{
    metrics.ResultDeduped.Add(1, new KeyValuePair<string, object?>("ProcessorId", m.ProcessorId.ToString("D")));
    return;
}
```

**Edit 2 — check-and-drop gate, placed AFTER the flag[H] gate (:66) and BEFORE the L1 store.TryGet (:69)** (req-3 / D-05):
```csharp
// NEW (req-3 / D-05): drop in-flight result for a cancelled workflow. INFRA read (no catch) → propagates.
// Reads the cancelled marker ONLY — never flag[H] (D-13). ack-and-discard, NO dedup counter.
if ((string?)await db.StringGetAsync(L2ProjectionKeys.Cancelled(m.WorkflowId))
        == L2ProjectionKeys.CancelledMarkerValue)
    return;
```
`m.WorkflowId` is on `ExecutionResult` (via `IExecutionCorrelated`). Use the `CancelledMarkerValue` const. The orchestrator does NOT write the marker and does NOT trip the breaker (Divergence — processor-side only, D-01).

Create `tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs` (analog: `ResultAckTests.cs`). Tests:
1. flag[m.H]=="Ack" → ONE `orchestrator_result_deduped` increment, return, NO `dispatcher.DispatchAsync` call (assert via a recording/Substitute dispatcher), NO `store.TryGet`-driven advancement.
2. cancelled marker set (stub `StringGetAsync(Cancelled(wfId))` → "true", flag → null) → return, NO dispatch, NO `orchestrator_result_deduped` increment, NO `StringSetAsync` to any key.
3. another workflow's marker set but THIS result's `WorkflowId` not cancelled → proceeds to the normal L1 path (assert it reaches `store.TryGet` / dispatches as the existing ResultConsumeTests expect) — proves workflowId-keyed.
4. the cancelled path issues NO read/write against any `L2ProjectionKeys.Flag(...)` key beyond the existing flag-gate read (D-13 guard).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-class "*ResultCheckAndDropFacts"</automated>
  </verify>
  <acceptance_criteria>
    - `ResultConsumer.cs` contains `metrics.ResultDeduped.Add(1,` inside the flag[m.H]=="Ack" branch
    - `ResultConsumer.cs` contains `StringGetAsync(L2ProjectionKeys.Cancelled(m.WorkflowId))` compared against `L2ProjectionKeys.CancelledMarkerValue`
    - The cancelled-check appears AFTER the flag[H] gate and BEFORE `store.TryGet` (grep line order)
    - `ResultConsumer.cs` contains NO `StringSetAsync(...Cancelled...` (orchestrator never WRITES the marker)
    - `ResultCheckAndDropFacts` asserts: dedup +1 on flag-Ack; cancelled-set → drop with no dispatch/no counter; other-workflow proceeds
    - `dotnet build src/Orchestrator -c Debug` exits 0, 0 Warning / 0 Error
    - `dotnet test tests/BaseApi.Tests -- --filter-class "*ResultCheckAndDropFacts"` exits 0
  </acceptance_criteria>
  <done>The orchestrator ack-and-discards in-flight results for a cancelled workflow (workflowId-keyed, marker read-only, no flag[H] touch) and increments `orchestrator_result_deduped` at the flag-Ack gate; a hermetic test pins both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker → ResultConsumer (shared orchestrator-result queue) | Untrusted ExecutionResult crosses here; the consumer reads the broker-internal Redis L2 marker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Tampering | in-flight ExecutionResult advancing a cancelled workflow | mitigate | The check-and-drop gate reads `skp:cancelled:{m.WorkflowId:D}` before the L1 advance and ack-and-discards when set — a result already queued when the breaker tripped cannot advance the halted DAG. `ResultCheckAndDropFacts` asserts no dispatch on a cancelled-set result. |
| T-32-02 | Tampering | `skp:cancelled:*` marker read | accept | Read-only here against the broker-internal Redis (same trust boundary as the existing flag/data dedup reads); marker integrity is the pre-existing infra trust boundary. The orchestrator never writes the marker (asserted: no `StringSetAsync(...Cancelled...)`). |
| T-32-08 | Denial of Service | per-message extra Redis GET | accept | One extra `GET` per received result — accepted (same Redis already hit for the flag[H] dedup; SPEC Constraints "per-message cost"). |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` = 0 Warning / 0 Error.
- `ResultCheckAndDropFacts` GREEN.
- Full hermetic suite GREEN — ResultConsumer has pre-existing facts (ResultAckTests/ResultConsumeTests/ManifestFanoutFacts/MergeCollapseFacts); the new gate is read-only and must not regress them. If a pre-existing test now hits the cancelled read, stub `StringGetAsync(Cancelled(...))` → null in its harness (Rule-3 mechanical realignment via OrchestratorTestStubs.NoopRedis, the 31-04 precedent).
</verification>

<success_criteria>
- ResultConsumer ack-and-discards in-flight results for a cancelled workflow; other workflows unaffected.
- `orchestrator_result_deduped` increments once at the flag-Ack gate.
- The orchestrator reads the marker only — never writes it, never touches flag[H] on the cancelled path.
</success_criteria>

<output>
After completion, create `.planning/phases/32-cancelled-circuit-breaker/32-05-SUMMARY.md`.
</output>
