---
phase: 32-cancelled-circuit-breaker
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/BaseApi.Tests/Processor/RetryAttemptNumberingFacts.cs
  - tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs
autonomous: true
requirements: [req-1, req-4]
must_haves:
  truths:
    - "The exact GetRetryAttempt() value on the exhausting delivery of a single endpoint-level Immediate(Limit) policy is pinned by a hermetic test"
    - "Fault<EntryStepDispatch>.Message.WorkflowId round-trips through a real in-memory MassTransit harness"
  artifacts:
    - path: "tests/BaseApi.Tests/Processor/RetryAttemptNumberingFacts.cs"
      provides: "Pins the GetRetryAttempt() boundary value for Immediate(Limit) — unblocks the breaker seam (Risk R1)"
      min_lines: 40
    - path: "tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs"
      provides: "Proves Fault<EntryStepDispatch>.Message.WorkflowId extraction through real MT fault publication (Risk R2 / D-06 assumption)"
      min_lines: 40
  key_links:
    - from: "RetryAttemptNumberingFacts"
      to: "a real Immediate(Limit) receive endpoint + deterministically-throwing infra op"
      via: "InMemoryTestHarness"
      pattern: "GetRetryAttempt"
    - from: "FaultConsumerBindingFacts"
      to: "IConsumer<Fault<EntryStepDispatch>>"
      via: "InMemoryTestHarness publish + consume"
      pattern: "Fault<EntryStepDispatch>"
---

<objective>
Wave-0 MassTransit-reality probes. Two hermetic tests that MUST land before the breaker seam (Plan 04) and the fault consumer (Plan 05) are trusted. They resolve the two load-bearing assumptions in 32-RESEARCH.md (A1/A2 and A4) against real MassTransit behavior — not against NSubstitute stubs.

Purpose: De-risk the SPEC-locked `GetRetryAttempt() == Limit` boundary (Risk R1) and the `Fault<EntryStepDispatch>.Message.WorkflowId` extraction (Risk R2) before any production code depends on them. If R1 disagrees with `== Limit`, this plan's task ESCALATES rather than silently changing the SPEC.
Output: Two new test classes, both GREEN against an in-memory MassTransit harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cancelled-circuit-breaker/32-SPEC.md
@.planning/phases/32-cancelled-circuit-breaker/32-RESEARCH.md
@.planning/phases/32-cancelled-circuit-breaker/32-PATTERNS.md
@.planning/phases/32-cancelled-circuit-breaker/32-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source 2026-06-04. -->

From src/Messaging.Contracts/EntryStepDispatch.cs:
```csharp
public sealed record EntryStepDispatch(
    Guid WorkflowId, Guid StepId, Guid ProcessorId, string Payload) : IExecutionCorrelated
{
    public Guid CorrelationId { get; init; }
    public Guid ExecutionId  { get; init; } = Guid.Empty;
    public string EntryId    { get; init; } = "";
    public string H          { get; init; } = "";
}
```

MassTransit Fault<T> interface (8.5.5): `{ Guid FaultId; Guid? FaultedMessageId; DateTime Timestamp; ExceptionInfo[] Exceptions; HostInfo Host; T Message; }` — `Fault<EntryStepDispatch>.Message` IS the original EntryStepDispatch instance.

RetryOptions: `Messaging.Contracts.Configuration.RetryOptions` — `int Limit { get; init; } = 3;`. The 4 production sites configure `UseMessageRetry(r => r.Immediate(retryOptions.Value.Limit))` at the RECEIVE-ENDPOINT level (the documented reliable case for GetRetryAttempt).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RetryAttemptNumberingFacts — pin the GetRetryAttempt() exhaustion boundary (Risk R1)</name>
  <read_first>
    - tests/BaseApi.Tests/Processor/EffectFirstDedupFacts.cs (the NSubstitute Context kit — and why it CANNOT be reused here: a Substitute ConsumeContext does not implement the GetRetryAttempt header extension)
    - 32-RESEARCH.md §"Unknown 1" + §"Pitfall 1" + §"Risk R1" (the SPEC locks `== Limit`; 0-based: first delivery=0, the Limit-th retry=Limit)
    - 32-PATTERNS.md §"Hermetic processor Facts" Divergence note (this is the one test that needs a REAL Immediate(Limit) endpoint, not the substitute-context kit)
    - src/Orchestrator/Consumers/ResultConsumerDefinition.cs:41 (the production Immediate(Limit) idiom this probe mirrors)
  </read_first>
  <action>
Create `tests/BaseApi.Tests/Processor/RetryAttemptNumberingFacts.cs`. This is a hermetic test driving a REAL MassTransit in-memory bus (use `MassTransit.Testing.InMemoryTestHarness` / `ServiceCollection().AddMassTransitTestHarness(...)` — the same MassTransit 8.5.5 already referenced by the test project), NOT the NSubstitute `OrchestratorTestStubs.Context` (a Substitute ConsumeContext does NOT implement the `GetRetryAttempt()` header extension — confirmed in PATTERNS Divergence note).

Configure ONE receive endpoint with a SINGLE endpoint-level `UseMessageRetry(r => r.Immediate(LIMIT))` policy where `const int LIMIT = 3` (mirror `ResultConsumerDefinition.cs:41` — endpoint-level, single policy, the documented reliable case). Bind a tiny probe consumer over a trivial message type whose `Consume` does two things: (a) records `context.GetRetryAttempt()` into a thread-safe list keyed by delivery, then (b) UNCONDITIONALLY throws an infra-style exception (e.g. `new RedisTimeoutException("forced", CommandStatus.Unknown)` or any `Exception` — the type does not matter, only that it forces the retry to exhaust). Publish/Send ONE message; await the harness until the message reaches `_error` (exhausted).

Assert the EXACT recorded attempt values: the first delivery records `0`, and the FINAL (exhausting) delivery records `LIMIT` (i.e. attempts observed are `0,1,2,3` for LIMIT=3, and the value on the delivery that should trip the breaker is `== LIMIT`). Assert the count of deliveries is `LIMIT + 1`.

**ESCALATION CLAUSE (do NOT silently adapt):** If the pinned boundary value on the exhausting delivery is NOT `== LIMIT` (e.g. it's `LIMIT - 1`, or `GetRetryAttempt()` returns `0` every delivery — the consumer-level/stacked-policy bug from MassTransit#1217/#3216), DO NOT change the SPEC's `== Limit` semantics and DO NOT proceed to make the test pass against a different value. Instead: write the test to assert the ACTUAL observed boundary, mark it with an `[Trait("Escalate","Risk-R1")]` trait, and record a BLOCKING note in the plan SUMMARY ("Wave-0 R1 pinned value = X, SPEC locks == Limit; escalate to /gsd-discuss-phase before Plan 04 builds the breaker catch"). The downstream breaker seam (Plan 04) reads the pinned value from THIS test, not from a guess.

Add `[Trait("Category","Hermetic")]` (NOT RealStack — this uses the in-memory harness, no compose stack). No `_total`/metric assertions here. No production code change in this task.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-class "*RetryAttemptNumberingFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Processor/RetryAttemptNumberingFacts.cs` exists and contains `GetRetryAttempt`
    - The test configures a single endpoint-level `Immediate(` policy (grep the file for `Immediate(`)
    - The test uses an in-memory MassTransit harness (grep for `AddMassTransitTestHarness` OR `InMemoryTestHarness`), NOT `OrchestratorTestStubs.Context`
    - `dotnet test tests/BaseApi.Tests -- --filter-class "*RetryAttemptNumberingFacts"` exits 0
    - If the boundary is NOT `== Limit`: the test carries `[Trait("Escalate","Risk-R1")]` AND the SUMMARY records a BLOCKING escalation note (this is a PASS condition for the task — pinning the truth — not a failure)
  </acceptance_criteria>
  <done>The exhaustion-attempt boundary for this project's single endpoint-level Immediate(Limit) policy is pinned by a GREEN hermetic test; the SPEC's `== Limit` is either confirmed or escalated (never silently changed).</done>
</task>

<task type="auto">
  <name>Task 2: FaultConsumerBindingFacts — prove Fault&lt;EntryStepDispatch&gt;.Message.WorkflowId round-trips (Risk R2)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/ResultAckTests.cs + tests/BaseApi.Tests/Orchestrator/OrchestratorTestStubs.cs (the orchestrator hermetic-test idioms; OrchestratorTestStubs.Context&lt;T&gt; is generic and supports ConsumeContext&lt;Fault&lt;EntryStepDispatch&gt;&gt;)
    - 32-RESEARCH.md §"Unknown 2" (the confirmed Fault<T> interface shape + "Fault.Message IS the original message instance") + §"Risk R2"
    - 32-PATTERNS.md §"Hermetic orchestrator Facts" Divergence note (FaultConsumerBindingFacts should use a MassTransit in-memory harness to prove the MT binding, not just the consumer code)
    - src/Messaging.Contracts/EntryStepDispatch.cs (WorkflowId is the first ctor positional)
  </read_first>
  <action>
Create `tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs`. Use a REAL MassTransit in-memory harness (`AddMassTransitTestHarness`) — the goal is to prove the MASSTRANSIT BINDING, not the consumer logic (a pure NSubstitute stub would only prove your own code reads `.Message.Message`, not that MT actually populates it).

Set up: a consumer over some message type `X` whose `Consume` throws (forcing MT to publish `Fault<X>`), plus a probe consumer `IConsumer<Fault<EntryStepDispatch>>` — where `X` IS `EntryStepDispatch`. Configure `Immediate(0)` (or a tiny limit) on the throwing endpoint so it exhausts fast and MT auto-publishes `Fault<EntryStepDispatch>`. The probe consumer captures `context.Message.Message.WorkflowId` (double `.Message`: outer is `Fault<EntryStepDispatch>`, inner is the original `EntryStepDispatch`).

Publish ONE `EntryStepDispatch` with a KNOWN `WorkflowId` (a fixed `Guid`), e.g. `new EntryStepDispatch(knownWfId, NewId.NextGuid(), NewId.NextGuid(), "{}")`. Await the harness until the `Fault<EntryStepDispatch>` is consumed by the probe. Assert: the probe was invoked exactly once AND the captured `WorkflowId == knownWfId`. This proves D-06's assumption "Fault.Message carries WorkflowId" holds with NO fallback needed.

Optionally (belt-and-braces for Risk R2) assert no OTHER consumer in the harness received the fault (the grep already confirms no other `IConsumer<Fault<EntryStepDispatch>>` exists in src today — A4).

Add `[Trait("Category","Hermetic")]`. No production code change in this task.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-class "*FaultConsumerBindingFacts"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs` exists and contains `Fault<EntryStepDispatch>`
    - The test contains the double-Message extraction pattern `.Message.Message.WorkflowId` (grep)
    - The test uses a MassTransit in-memory harness (grep for `AddMassTransitTestHarness` OR `InMemoryTestHarness`)
    - `dotnet test tests/BaseApi.Tests -- --filter-class "*FaultConsumerBindingFacts"` exits 0
    - The assertion compares the captured WorkflowId against a known fixed Guid (not Guid.Empty)
  </acceptance_criteria>
  <done>A GREEN hermetic test proves Fault&lt;EntryStepDispatch&gt;.Message.WorkflowId round-trips through real MassTransit fault publication; Plan 05's fault consumer can rely on the double-Message extraction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → in-memory bus | Fully in-process; no external broker/Redis. No untrusted input crosses any boundary in these probes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-W0-01 | Tampering | in-memory test harness | accept | Hermetic, in-process, no network/broker/Redis surface; no production code or data touched. No mitigation needed. |
</threat_model>

<verification>
- Both files exist under `tests/BaseApi.Tests/`.
- `dotnet build tests/BaseApi.Tests -c Debug` = 0 Warning / 0 Error.
- Both `--filter-class` runs exit 0.
- Full hermetic suite after this wave: `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` GREEN (zero regression; +2 new hermetic facts).
</verification>

<success_criteria>
- `RetryAttemptNumberingFacts` pins the exact `GetRetryAttempt()` exhaustion boundary; `== Limit` confirmed OR escalated.
- `FaultConsumerBindingFacts` proves `Fault<EntryStepDispatch>.Message.WorkflowId` extraction works against real MT.
- Plans 04 and 05 can proceed on a verified foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/32-cancelled-circuit-breaker/32-01-SUMMARY.md`. If Task 1 escalated Risk R1, record the BLOCKING note prominently (Plan 04 must read the pinned value before building the breaker catch).
</output>
