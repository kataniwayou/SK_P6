---
phase: 32.1
slug: dead-letter-on-exhaustion
status: validated-partial
nyquist_compliant: false
hermetic_complete: true
wave_0_complete: true
created: 2026-06-05
---

# Phase 32.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

**Net-deletion phase.** This phase REVERTS the Phase-32 cancelled circuit-breaker to plain
dead-lettering on exhaustion. Most requirements are validated by *deletion*: the breaker-specific
tests were removed, and the absence of the removed symbols is proven by zero-match dangling-reference
greps plus a green hermetic suite — generating a new test to "prove code is gone" would be
tautological. The KEEP-intact behavior (Phase-31 `flag[H]` dedup gates + `DispatchDeduped` /
`ResultDeduped` counters) retains its hermetic coverage, re-homed into the trimmed facts classes.
The only non-hermetic items are the live real-stack close gate and the framework-default
dead-letter-to-`_error` landing, which require the live compose stack (operator gate).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit (.NET 8, Microsoft.Testing.Platform) |
| **Config file** | `tests/BaseApi.Tests/BaseApi.Tests.csproj` |
| **Quick run command** | `dotnet test tests/BaseApi.Tests -- --filter-class "*CheckAndDropFacts"` |
| **Full suite command** | `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` |
| **Estimated runtime** | ~185 seconds (full hermetic suite, 447 facts) |

---

## Sampling Rate

- **After every task commit:** Run the relevant `--filter-class` quick command
- **After every plan wave:** Run the full hermetic suite
- **Before `/gsd-verify-work`:** Full suite must be green (447/0 confirmed at HEAD)
- **Max feedback latency:** ~185 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 32.1-01-01 | 01 | 1 | req-1, req-2, req-4 | T-32.1-01 / T-32.1-02 | Breaker catch + both cancelled check-and-drop gates + marker key + trip metric removed; Phase-31 `flag[H]` dedup gates + `DispatchDeduped`/`ResultDeduped` retained | structural + unit | `git grep -nE "L2ProjectionKeys\.Cancelled\|CancelledMarkerValue\|WorkflowCancelled\|GetRetryAttempt" -- 'src/*.cs'` == 0 AND `--filter-class "*CheckAndDropFacts"` / `"*ResultCheckAndDropFacts"` / `"*BreakerMetricsFacts"` | ✅ | ✅ green |
| 32.1-01-01 | 01 | 1 | req-3 | T-32.1-03 | `FaultUnscheduleConsumer` + definition + `Program.cs` registration deleted; orchestrator builds with Start/Stop/Result only | structural + build | `git grep -c FaultUnscheduleConsumer -- 'src/*.cs'` == 0 AND `dotnet build SK_P.sln -c Release` 0/0 | ✅ | ✅ green |
| 32.1-01-02 | 01 | 1 | req-5 (hermetic half) | — | 8 breaker test files deleted; kept dedup-counter coverage re-homed; zero dangling references | unit (full suite) | `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` == 447/0 AND `git grep -nE "...removed symbols..." -- 'tests/*.cs'` == 0 | ✅ | ✅ green |
| 32.1-02-01 | 02 | 2 | req-5 (script half) | T-32.1-06 / T-32.1-08 | `phase-32.1-close.ps1` authored (parse-clean, BOM-free, FLUSHDB=0, skp:cancelled=0); `phase-32-close.ps1` deleted | static / parse | `ParseFile` exit 0 AND first 3 bytes `23 20 50` AND `git grep -c FLUSHDB` == 0 AND `git grep -c skp:cancelled` == 0 | ✅ | ✅ green |
| 32.1-02-02 | 02 | 2 | req-5 (live half) | T-32.1-07 | Live close gate `GATE_EXIT=0` — 3×GREEN + triple-SHA BEFORE==AFTER on the real v3.6.0 compose stack | manual (real-stack) | `pwsh -NoProfile -File ./scripts/phase-32.1-close.ps1` | ✅ (script) | ⬜ pending (manual-only) |
| — | — | — | req-6 | — | `StepEntryCondition.cs` / `StepOutcome.cs` untouched; `BuildCancelled` (EXEC-08 token-cancellation) retained | structural | `git diff --name-only 50c52f6 HEAD` excludes both enum files AND `git grep -c BuildCancelled src/.../EntryStepDispatchConsumer.cs` ≥ 1 | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new framework, fixtures, or test stubs
required — this is a net-deletion phase; the retained behavior (Phase-31 dedup gates + counters)
keeps its existing hermetic coverage in the trimmed `CheckAndDropFacts` / `ResultCheckAndDropFacts`
/ `BreakerMetricsFacts` classes.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live close gate `GATE_EXIT=0` (3×GREEN + triple-SHA BEFORE==AFTER; ZERO `skp:cancelled:*` in AFTER) | req-5 (live half) | Requires the full v3.6.0 compose stack with rebuilt containers (embedded SourceHash must match the host build); not executable in a non-interactive environment | `docker compose up -d --build processor-sample orchestrator baseapi-service` → `pwsh -NoProfile -File ./scripts/phase-32.1-close.ps1` (read `GATE_*_EXIT` from the gate output, not the bg-wrapper). Tracked in `32.1-HUMAN-UAT.md`. |
| Infra-fault exhaustion dead-letters to `{processorId}_error` (no marker, no trip metric, no unschedule) | req-1 | Dead-lettering is MassTransit `UseMessageRetry(Immediate(Limit))` framework behavior — requires a real RabbitMQ broker to observe the `_error` queue landing | On the live stack, induce an infra-faulting dispatch through `Limit` attempts; confirm the message lands in `{processorId}_error` and NO `skp:cancelled:*` key is written. Covered by the live close gate run. |
| Orchestrator starts with Start/Stop/Result endpoints only (no Fault endpoint) | req-3 | Endpoint binding is observable only against a live RabbitMQ bus at orchestrator startup | On the live stack, confirm the orchestrator boots healthy and the management UI shows no `Fault<EntryStepDispatch>` consumer endpoint. Covered by the live close gate stack-health pre-flight. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are documented manual-only (real-stack operator gate)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — existing infra covers all)
- [x] No watch-mode flags
- [x] Feedback latency < 185s
- [ ] `nyquist_compliant: true` — NOT set: 3 real-stack behaviors are legitimately manual-only (operator gate). Hermetic coverage is complete (`hermetic_complete: true`); 0 fillable automated gaps.

**Approval:** validated-partial 2026-06-05 (hermetic complete; live close gate is the operator gate)
