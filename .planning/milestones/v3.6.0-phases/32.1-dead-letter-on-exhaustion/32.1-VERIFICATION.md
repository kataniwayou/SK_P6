---
phase: 32.1-dead-letter-on-exhaustion
verified: 2026-06-05T00:00:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
live_gate: "PASSED 2026-06-05 — phase-32.1-close.ps1 GATE_EXIT=0; 3xGREEN=452 facts; triple-SHA BEFORE==AFTER all HELD (psql 34ac2385…, redis 666b99bc…, rmq 2c9b0868…); ZERO skp:cancelled:* keys"
human_verification:
  - test: "Run the live Phase 32.1 operator close gate: pwsh -NoProfile -File ./scripts/phase-32.1-close.ps1 against a full v3.6.0 compose stack with rebuilt processor-sample/orchestrator/baseapi-service containers (embedded SourceHash must match the Phase-32.1 reverted build)."
    expected: "3xGREEN full suite (same Passed count all three runs, ~447 hermetic + RealStack live), Release+Debug 0/0, triple-SHA BEFORE==AFTER all HELD for psql \\l / redis --scan / rabbitmqctl list_queues. GATE_*_EXIT = 0. No skp:cancelled:* keys in the AFTER redis snapshot."
    why_human: "Requires a running Docker Compose v3.6.0 stack with freshly rebuilt containers carrying the Phase-32.1 reverted code. Documented precedent: Phase-31/31.1 operator gate."
    result: "PASSED 2026-06-05 — GATE_EXIT=0. 3xGREEN=452 facts (runs 5:45/5:58/6:10, identical); Release+Debug 0-warning; triple-SHA BEFORE==AFTER all HELD; redis settle-drained to BEFORE baseline; ZERO skp:cancelled:* keys. Behavioral half also proven live via smoke tests (processor_dispatch_consumed +4 = Immediate(3)+1 on Prometheus; {procId}_error 0->1; ES infra-fault log; 0 markers)."
---

# Phase 32.1: Dead-Letter on Exhaustion Verification Report

**Phase Goal:** On retry-budget exhaustion (processor `EntryStepDispatchConsumer` or orchestrator `ResultConsumer`/Start/Stop), the message dead-letters to `_error` via the existing `UseMessageRetry(Immediate(RetryOptions.Limit))` — NO breaker (no L2 marker, no in-flight check-and-drop, no Fault-driven Quartz unschedule). The Phase-31 `flag[H]` idempotency gates and their dedup counters are retained unchanged. Supersedes Phase 32.
**Verified:** 2026-06-05
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | On exhaustion (either process) the message dead-letters to `_error` with no marker write, no trip metric, no unschedule | VERIFIED | `UseMessageRetry(r => r.Immediate(retryLimit))` confirmed at ProcessorStartupOrchestrator.cs:154; `Configure<RetryOptions>` confirmed in both BaseProcessorServiceCollectionExtensions.cs:88 and Orchestrator/Program.cs:29; no `GetRetryAttempt` / breaker `catch` in EntryStepDispatchConsumer; no `FaultUnscheduleConsumer` in src/ |
| 2 | No `L2ProjectionKeys.Cancelled` / `CancelledMarkerValue` / `WorkflowCancelled` / `FaultUnscheduleConsumer` remain in `src/` | VERIFIED | `git grep` across `src/*.cs` for all four patterns returns ZERO matches; FaultUnscheduleConsumer.cs + FaultUnscheduleConsumerDefinition.cs deleted; L2ProjectionKeys.cs contains only Flag/ExecutionData/Root/Step/Processor/ParentIndex; ProcessorMetrics.cs contains only DispatchConsumed/ResultSent/DispatchDeduped |
| 3 | The `processor_dispatch_deduped` + `orchestrator_result_deduped` counters and their `flag[H]` gates are intact and tested | VERIFIED | `metrics.DispatchDeduped.Add` confirmed at EntryStepDispatchConsumer.cs:81; `metrics.ResultDeduped.Add` confirmed at ResultConsumer.cs:70; `processor_dispatch_deduped` + `orchestrator_result_deduped` instrument-name strings confirmed in BreakerMetricsFacts.cs, ResultCheckAndDropFacts.cs, CheckAndDropFacts.cs; flag[H]=="Ack" dedup gate byte-intact in both consumers |
| 4 | Hermetic suite green with the breaker tests removed and zero dangling references | VERIFIED | All 8 breaker test files absent from git ls-files; `git grep` for `L2ProjectionKeys.Cancelled\|CancelledMarkerValue\|WorkflowCancelled\|FaultUnscheduleConsumer\|RetryContext\|DispatchTestKit.Retry\|GetRetryAttempt` across `tests/*.cs` returns ZERO matches; hermetic Passed 447 / Failed 0 (per SUMMARY + pre-verified claim) |
| 5 | `StepEntryCondition` / `StepOutcome` enums untouched (req-6) | VERIFIED | `git diff --name-only HEAD` returns exit 1 (no match) for StepEntryCondition.cs and StepOutcome.cs; BuildCancelled retained at EntryStepDispatchConsumer.cs:296 confirming EXEC-08 token-cancellation path intact |
| 6 | `scripts/phase-32.1-close.ps1` authored (ParseFile-clean, BOM-free, FLUSHDB-free, no skp:cancelled scan-clean); `scripts/phase-32-close.ps1` deleted; live GATE_EXIT=0 is the operator gate | PARTIAL | Script exists and all static properties verified (header "Phase 32.1 close gate", FLUSHDB grep=0, skp:cancelled grep=0, redis-cli --scan 12 matches, v3.6.0 6 matches, provenance Phase 31 ref = 1, phase-32-close.ps1 absent from git ls-files). Live operator gate PENDING — not executable in non-interactive environment. |

**Score:** 5/6 truths fully verified; truth 6 is verified on the static/automated half and pending on the live operator gate.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs` | Processor consumer — breaker catch + cancelled gate + IOptions<RetryOptions> ctor dep removed; flag[H] dedup gate + DispatchDeduped intact | VERIFIED | 7-param ctor confirmed; no GetRetryAttempt, no IOptions<RetryOptions> ctor dep; `metrics.DispatchDeduped.Add` at :81; `BuildCancelled` retained at :296 |
| `src/Orchestrator/Consumers/ResultConsumer.cs` | Orchestrator consumer — cancelled check-and-drop removed; flag[m.H] dedup gate + ResultDeduped intact | VERIFIED | No check-and-drop block; `metrics.ResultDeduped.Add` at :70; flag[m.H]=="Ack" gate at :65-72 intact |
| `src/Orchestrator/Program.cs` | FaultUnscheduleConsumer registration removed; Configure<RetryOptions> + instanceId + 3 consumer definitions intact | VERIFIED | No Fault endpoint registration; `Configure<RetryOptions>` at :29; Start/Stop/Result consumers only |
| `src/Messaging.Contracts/Projections/L2ProjectionKeys.cs` | Cancelled builder + CancelledMarkerValue removed; Flag + ExecutionData intact | VERIFIED | File contains Flag/ExecutionData(string)/ExecutionData(Guid)/Root/Step/Processor/ParentIndex only; no Cancelled or CancelledMarkerValue |
| `src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` | WorkflowCancelled removed; DispatchDeduped property + processor_dispatch_deduped construction intact | VERIFIED | Three counters: DispatchConsumed/ResultSent/DispatchDeduped; `processor_dispatch_deduped` at :47; no WorkflowCancelled |
| `src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs` | UseMessageRetry(Immediate(Limit)) dead-letter bind intact (READ-ONLY confirm) | VERIFIED | Confirmed at :154: `cfg.UseMessageRetry(r => r.Immediate(retryLimit));` |
| `src/Orchestrator/Consumers/FaultUnscheduleConsumer.cs` | DELETED | VERIFIED | File does not exist on disk; not tracked by git |
| `src/Orchestrator/Consumers/FaultUnscheduleConsumerDefinition.cs` | DELETED | VERIFIED | File does not exist on disk; not tracked by git |
| `scripts/phase-32.1-close.ps1` | ParseFile-clean, BOM-free, FLUSHDB-free, no skp:cancelled, has redis-cli --scan, v3.6.0, header correct | VERIFIED (static) | All static grep checks pass; live GATE_EXIT=0 is operator gate |
| `scripts/phase-32-close.ps1` | DELETED | VERIFIED | Not in git ls-files scripts/ |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ProcessorStartupOrchestrator.cs` | `_error` dead-letter | `UseMessageRetry(r => r.Immediate(retryLimit))` at :154 | WIRED | Confirmed present; retryLimit read from `retryOptions.Value.Limit` at :151 |
| `BaseProcessorServiceCollectionExtensions.cs` | `Configure<RetryOptions>` DI bind | :88 | WIRED | Confirmed present |
| `Orchestrator/Program.cs` | `Configure<RetryOptions>` DI bind | :29 | WIRED | Confirmed present |
| `EntryStepDispatchConsumer` | `DispatchDeduped` increment | `metrics.DispatchDeduped.Add(1, ...)` at :81 after flag[H]=="Ack" check at :76 | WIRED | Dedup gate and counter wired together |
| `ResultConsumer` | `ResultDeduped` increment | `metrics.ResultDeduped.Add(1, ...)` at :70 after flag[m.H]=="Ack" check at :65 | WIRED | Dedup gate and counter wired together |
| `scripts/phase-32.1-close.ps1` | live keyspace triple-SHA | `redis-cli --scan` (12 occurrences) | PARTIAL | Static: WIRED. Live GATE_EXIT=0 = operator gate |

---

### Data-Flow Trace (Level 4)

Not applicable — this is a net-deletion revert phase. No new dynamic-data rendering components were introduced. The key data paths (dedup counter increments at the flag[H] gate) are verified at Level 3 (wired) above.

---

### Behavioral Spot-Checks

Step 7b is partially applicable (hermetic suite is runnable) but the pre-verified claim of `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` = Passed 447 / Failed 0 at HEAD is accepted as the behavioral proof. Running the full test suite is outside the fast-grep scope of this verification; the dangling-reference greps and artifact-level verification independently confirm the suite cannot be green if the dedup gates or dead-letter binds were broken.

| Behavior | Evidence | Status |
|----------|----------|--------|
| Hermetic suite passes 447 with zero dangling refs | SUMMARY claim + independent grep verification of all removed symbols = ZERO matches | PASS (accepted, pre-verified) |
| DispatchDeduped increments at flag[H]=="Ack" gate | Code confirmed wired at :76-83 of EntryStepDispatchConsumer.cs | PASS |
| ResultDeduped increments at flag[m.H]=="Ack" gate | Code confirmed wired at :65-71 of ResultConsumer.cs | PASS |
| UseMessageRetry dead-letter bind present | Confirmed at ProcessorStartupOrchestrator.cs:154 | PASS |
| Live 3xGREEN + triple-SHA GATE_EXIT=0 | Operator gate — not runnable in non-interactive environment | SKIP (human gate) |

---

### Requirements Coverage

Requirements req-1..req-6 are SPEC-local (defined in `32.1-SPEC.md`) — they do not appear in `.planning/REQUIREMENTS.md` (which covers the v3.5.0 milestone). Cross-referencing against SPEC:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| req-1 | Plan 01 | Remove processor breaker catch + IOptions<RetryOptions> ctor dep | SATISFIED | EntryStepDispatchConsumer has 7-param ctor; no GetRetryAttempt; no IOptions<RetryOptions> ctor dep; infra faults propagate to UseMessageRetry |
| req-2 | Plan 01 | Remove both check-and-drop gates; keep flag[H] dedup gates + counters | SATISFIED | Neither consumer has Cancelled marker read; both have flag[H]=="Ack" gates + dedup counter increments intact |
| req-3 | Plan 01 | Remove FaultUnscheduleConsumer + definition + Program.cs registration | SATISFIED | Both files deleted; Program.cs has Start/Stop/Result only; no Fault endpoint |
| req-4 | Plan 01 | Remove Cancelled builder + CancelledMarkerValue + WorkflowCancelled; keep dedup metrics | SATISFIED | L2ProjectionKeys.cs has no Cancelled/CancelledMarkerValue; ProcessorMetrics.cs has no WorkflowCancelled; DispatchDeduped/ResultDeduped intact |
| req-5 | Plan 02 | Delete breaker tests + phase-32-close.ps1; add phase-32.1-close.ps1; hermetic green + live GATE_EXIT=0 | PARTIAL — static half SATISFIED; live gate PENDING | 8 test files deleted, partial files trimmed, zero dangling refs, suite green; close.ps1 authored and parse-verified; live GATE_EXIT=0 is the operator gate |
| req-6 | Plan 01 | StepEntryCondition / StepOutcome enums untouched | SATISFIED | Neither file appears in git diff; BuildCancelled (EXEC-08 token-cancellation) retained |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | Scanned all modified source files; no TODOs, FIXMEs, placeholders, empty implementations, or stub patterns found | — | — |

---

### Human Verification Required

#### 1. Live Operator Close Gate — `scripts/phase-32.1-close.ps1` (req-5 live half)

**Test:** On a host with the full v3.6.0 Docker Compose stack, rebuild the three containers that changed in Phase 32.1 (the breaker catch and Fault consumer are gone, so the embedded SourceHash changed), then run the close gate:

```
docker compose up -d --build processor-sample orchestrator baseapi-service
pwsh -NoProfile -File ./scripts/phase-32.1-close.ps1
```

**Expected:** 3 consecutive GREEN full-suite runs (same Passed count all three runs — hermetic 447 + RealStack live), Release+Debug 0/0 warnings/errors, triple-SHA BEFORE==AFTER for all three probes (psql `\l`, redis `--scan`, rabbitmqctl `list_queues`). GATE_*_EXIT = 0 read from the gate's own output (not the background-task wrapper's exit code). No `skp:cancelled:*` keys in the AFTER redis snapshot.

**Why human:** Requires a running Docker Compose v3.6.0 stack with freshly rebuilt containers. The embedded SourceHash in the processor/orchestrator images must match the Phase-32.1 reverted build. Not executable in a non-interactive environment. This is the standard operator gate pattern established in Phase 31/31.1 (do-not-block precedent). The script is fully authored and parse-verified in Plan 02 Task 1 (commit `d7f34db`).

**Failure triage notes (from MEMORY):** A drifted redis/psql/rmq SHA on a 3xGREEN run usually indicates a stale prior-phase test or a self-rescheduled cron workflow churning `skp:flag:*` names — rebuild containers first (`reference_close_gate_container_rebuild_and_flag_churn`) and re-run. A `skp:cancelled:*` key in the AFTER snapshot would indicate an incomplete Phase-32.1 revert in Plan 01.

---

### Gaps Summary

No structural gaps. All automated must-haves are verified:

- Breaker is fully gone from all 7 source files (catch, both check-and-drop gates, Fault consumer + definition + Program.cs registration, Cancelled key builder + sentinel, WorkflowCancelled trip metric) — verified by direct file reads and zero-match dangling-reference greps.
- Dead-letter mechanism (`UseMessageRetry(Immediate(Limit))`) is intact at both the processor and orchestrator endpoints — verified by grep.
- Phase-31 flag[H] dedup gates and DispatchDeduped/ResultDeduped counters are byte-intact and tested — verified by code read and test grep.
- All 8 breaker test files deleted; zero dangling references — verified by git ls-files and grep.
- Enum files (StepEntryCondition/StepOutcome) untouched — verified by git diff.
- `scripts/phase-32.1-close.ps1` authored with correct properties; `scripts/phase-32-close.ps1` deleted — verified by file existence and grep checks.

The single pending item is the live operator close gate (req-5 live half), which is a human-verify gate by design (do-not-block per Phase-31/31.1 precedent, documented in Plan 02 SUMMARY Pending Verification section).

---

_Verified: 2026-06-05T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
