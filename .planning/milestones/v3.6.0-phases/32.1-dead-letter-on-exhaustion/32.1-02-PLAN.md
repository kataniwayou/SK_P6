---
phase: 32.1-dead-letter-on-exhaustion
plan: 02
type: execute
wave: 2
depends_on: [32.1-01]
files_modified:
  - scripts/phase-32.1-close.ps1
  - scripts/phase-32-close.ps1
autonomous: false
requirements: [req-5]

must_haves:
  truths:
    - "scripts/phase-32-close.ps1 (the superseded breaker close gate, including its skp:cancelled:* scan-clean) is deleted"
    - "scripts/phase-32.1-close.ps1 exists, is a byte-faithful clone of phase-31-close.ps1 relabeled 31 -> 32.1 (version stays v3.6.0), with NO skp:cancelled:* scan-clean block (no such keys are written anymore)"
    - "phase-32.1-close.ps1 ParseFile-validates (exit 0), is BOM-free, and contains no FLUSHDB token"
    - "The hermetic suite is green with zero dangling breaker references (the proof bar for net-zero)"
  artifacts:
    - path: "scripts/phase-32.1-close.ps1"
      provides: "Phase 32.1 close gate — 3xGREEN full suite + triple-SHA (psql \\l / redis --scan / rabbitmqctl list_queues), v3.6.0, no destructive flush, no skp:cancelled:* scan-clean"
      contains: "redis-cli --scan"
  key_links:
    - from: "scripts/phase-32.1-close.ps1"
      to: "the live keyspace triple-SHA (BEFORE == AFTER)"
      via: "unfiltered redis-cli --scan + psql \\l + rabbitmqctl list_queues snapshots around 3 GREEN runs"
      pattern: "redis-cli --scan"
---

<objective>
Author `scripts/phase-32.1-close.ps1` as a byte-faithful clone of `scripts/phase-31-close.ps1` (relabel `31 -> 32.1`, version stays **v3.6.0** — Phase 31/31.1/32/32.1 share the milestone), and delete the superseded `scripts/phase-32-close.ps1`. The Phase-32.1 gate is SIMPLER than the Phase-32 gate it replaces: because the breaker is reverted, NO `skp:cancelled:*` marker keys are ever written, so the explicit `skp:cancelled:*` scan-clean block that Phase-32's gate carried is NOT included.

Purpose: Provide the standard 3-consecutive-GREEN + triple-SHA close gate for the revert phase. The live `GATE_EXIT=0` run is the operator gate (this environment cannot run the live compose stack — handle per the Phase-31/31.1 precedent: author + parse-verify the script, document the live run as an operator runbook, do-not-block).

Output: `scripts/phase-32.1-close.ps1` (parse-clean, BOM-free, FLUSHDB-free); `scripts/phase-32-close.ps1` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32.1-dead-letter-on-exhaustion/32.1-SPEC.md
@scripts/phase-31-close.ps1
@scripts/phase-32-close.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone phase-31-close.ps1 -> phase-32.1-close.ps1 (relabel, NO cancelled scan-clean), delete phase-32-close.ps1</name>
  <read_first>
    - scripts/phase-31-close.ps1 (the clone SOURCE — byte-faithful template)
    - scripts/phase-32-close.ps1 (the SUPERSEDED gate — to confirm what NOT to carry over, i.e. the skp:cancelled:* scan-clean divergence)
    - .planning/phases/32.1-dead-letter-on-exhaustion/32.1-SPEC.md (req-5 acceptance)
  </read_first>
  <action>
1. **Create `scripts/phase-32.1-close.ps1` as a byte-faithful clone of `scripts/phase-31-close.ps1`**, applying ONLY these relabels:
   - Header line 1: `# Phase 31 close gate — v3.6.0 (triple-SHA)` -> `# Phase 32.1 close gate — v3.6.0 (triple-SHA)`.
   - Header line 2: replace the "Idempotent execution — exactly-once-effect round-trip closeout" subtitle with `# Dead-letter on exhaustion (breaker reverted) closeout`.
   - Every other `Phase 31` / `phase 31` / `phase-31` literal in comments and console-output strings -> the `32.1` equivalent (`Phase 32.1` / `phase-32.1`). Keep the provenance comment style (phase-31-close.ps1's own clone-source note becomes "Identical protocol to scripts/phase-31-close.ps1").
   - **Version stays `v3.6.0` everywhere** — do NOT bump it (Phase 31/31.1/32/32.1 share the milestone, confirmed in ROADMAP/SPEC).
2. **Do NOT add any `skp:cancelled:*` scan-clean block.** The Phase-32 gate (`phase-32-close.ps1`) inserted an explicit `docker exec sk-redis redis-cli --scan --pattern 'skp:cancelled:*'` + `del` between the settle-drain and the AFTER snapshot, because the no-TTL marker never self-expired. The breaker is reverted — no `skp:cancelled:*` key is ever written — so this block is NOT carried over. The Phase-31 source has no such block; cloning it faithfully already gives the correct (simpler) gate. Also do NOT carry over phase-32's `skp:cancelled:*` mentions in the settle-timeout / redis-mismatch diagnostics.
3. **KEEP byte-faithful from the phase-31 source:** the steady-state Processor pre-flight (GET-or-create on the unique source-hash for a stable procId/liveness key/queue), the full v3.6.0 `$services` health gate (processor-sample REQUIRED healthy), the UNFILTERED `redis-cli --scan` BEFORE/AFTER capture (captures `skp:flag:*`/`skp:data:*` automatically in the SHA — no widening, no filter), the settle-drain for the TTL-bounded `skp:flag:*`/`skp:data:*`, the 3-consecutive-GREEN loop (full suite, RealStack run live), the Release+Debug zero-warning build, the psql `\l` + rabbitmqctl `list_queues` SHAs, and NO destructive whole-db flush.
4. **Delete `scripts/phase-32-close.ps1`** (the superseded breaker close gate).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile('scripts/phase-32.1-close.ps1', [ref]$null, [ref]$null); if ($?) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-32.1-close.ps1` exists; `scripts/phase-32-close.ps1` does NOT exist (`git ls-files scripts/ | grep "phase-32-close"` returns ZERO matches).
    - ParseFile validates exit 0 (the automated verify command above).
    - BOM-free: the first 3 bytes of `scripts/phase-32.1-close.ps1` are `23 20 50` (`# P`), NOT `ef bb bf`. Verify with `(Get-Content scripts/phase-32.1-close.ps1 -Encoding Byte -TotalCount 3) -join ' '` == `35 32 80`.
    - `git grep -c "FLUSHDB" scripts/phase-32.1-close.ps1` == 0 (no destructive whole-db flush).
    - `git grep -c "skp:cancelled" scripts/phase-32.1-close.ps1` == 0 (no cancelled-marker scan-clean — the breaker is reverted).
    - `git grep -n "redis-cli --scan" scripts/phase-32.1-close.ps1` returns matches (the unfiltered BEFORE/AFTER snapshot is retained).
    - `git grep -n "v3.6.0" scripts/phase-32.1-close.ps1` returns matches (version unchanged); `git grep -nE "v3\.6\.1|v3\.7" scripts/phase-32.1-close.ps1` returns ZERO matches.
    - Header line 1 of `scripts/phase-32.1-close.ps1` contains `Phase 32.1 close gate`.
    - `git grep -cE "Phase 31|phase-31-close" scripts/phase-32.1-close.ps1` — only the single provenance/clone-source comment reference is acceptable (matches the phase-31 gate's own reference to phase-29); no stray `Phase 31` label in the gate's own console output.
  </acceptance_criteria>
  <done>`scripts/phase-32.1-close.ps1` is a parse-clean, BOM-free, FLUSHDB-free, cancelled-scan-clean-free v3.6.0 clone of the phase-31 gate; `scripts/phase-32-close.ps1` is deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator LIVE close gate run (phase-32.1-close.ps1 — GATE_EXIT=0)</name>
  <action>Run the live close gate per the how-to-verify runbook below. This is the operator gate — do-not-block per the Phase-31/31.1 precedent: the script is authored + parse-verified in Task 1; the live run requires a full v3.6.0 compose stack with rebuilt containers (embedded SourceHash must match the host build), which is not executable in a non-interactive environment. Narrate the GATE_EXIT evidence if the operator ran it; otherwise treat as approved and record the runbook in the SUMMARY Pending-Verification section.</action>
  <what-built>
`scripts/phase-32.1-close.ps1` — the Phase 32.1 close gate (3xGREEN full suite + triple-SHA: psql `\l` / redis `--scan` / rabbitmqctl `list_queues`, BEFORE == AFTER). It is a byte-faithful clone of the proven phase-31 gate, v3.6.0, with NO `skp:cancelled:*` scan-clean (the breaker is reverted, so no such keys are written). Plan 01 removed the breaker from all 7 source files + the breaker tests; the hermetic suite is GREEN with zero dangling references.

The LIVE gate run cannot execute in this non-interactive environment (no live compose stack; rebuilt containers' embedded SourceHash must match the host build). This is the operator gate, per the Phase-31/31.1 precedent.
  </what-built>
  <how-to-verify>
Operator runbook (run on a host with the full v3.6.0 compose stack):

1. **Rebuild + bring up the v3.6.0 stack** (the containers must run the Phase-32.1 reverted code — the breaker catch + Fault consumer are GONE this phase; the embedded SourceHash must match the host build):
   ```
   docker compose up -d --build processor-sample orchestrator baseapi-service
   ```
   Confirm `docker compose ps` shows postgres, redis, rabbitmq, otel-collector, elasticsearch, prometheus, orchestrator, processor-sample, baseapi-service all healthy.

2. **Run the close gate** (expect `GATE_EXIT = 0`):
   ```
   pwsh -NoProfile -File ./scripts/phase-32.1-close.ps1
   ```
   Expect: 3xGREEN full suite (same fact count all 3 runs — now WITHOUT the deleted breaker tests/E2E), Release+Debug 0/0, triple-SHA `BEFORE == AFTER` all HELD. Because no `skp:cancelled:*` key is ever written, the redis `--scan` SHA holds with no special scan-clean. **Read `GATE_*_EXIT` from the gate's own `exit 0` / "PASSED" output, NOT the background-task wrapper exit** (MEMORY: bg-task "exit 0" is the wrapper's). Append the three SHA values + the Passed count to the STATE.md Phase 32.1 close entry.

Failure triage (MEMORY references):
- Drifted redis/psql/rmq SHA on a 3xGREEN that previously held → usually a stale/flaky prior-phase test or a self-rescheduled cron workflow churning `skp:flag:*` names; rebuild containers first (`reference_close_gate_container_rebuild_and_flag_churn`) and re-run.
- A `skp:cancelled:*` key appearing in the AFTER snapshot would indicate the breaker revert was incomplete (Plan 01) — there should be ZERO such keys.
  </how-to-verify>
  <resume-signal>Type "approved" (treat as approved per the auto-approve-human-verify policy; narrate the GATE_EXIT evidence if the operator ran it) or describe the gate failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live stack | the close gate snapshots the live keyspace/db/queues; it has read-only SHA intent (NO FLUSHDB, NO destructive writes beyond the idempotent GET-or-create Processor pre-flight row) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1-06 | Tampering | a destructive `FLUSHDB` accidentally introduced into the close gate would wipe live state | mitigate | acceptance_criteria assert `grep -c FLUSHDB == 0`; the gate is SNAPSHOT-and-COMPARE only (cloned from the proven phase-31 gate which has no flush) |
| T-32.1-07 | Denial of Service | a leftover `skp:cancelled:*` scan-clean (carried over from phase-32) deleting unrelated keys, or masking an incomplete revert | mitigate | acceptance_criteria assert `grep -c skp:cancelled == 0`; the block is deliberately NOT cloned — the breaker is reverted so no such key exists |
| T-32.1-08 | Repudiation | a parse-broken or BOM-prefixed gate silently failing to run, leaving the phase "closed" without a real triple-SHA | mitigate | acceptance_criteria assert ParseFile exit 0 + BOM-free first-3-bytes check |
</threat_model>

<verification>
- `pwsh` ParseFile of `scripts/phase-32.1-close.ps1` exits 0.
- First 3 bytes == `35 32 80` (`# P`), BOM-free.
- `git grep -c "FLUSHDB" scripts/phase-32.1-close.ps1` == 0; `git grep -c "skp:cancelled" scripts/phase-32.1-close.ps1` == 0.
- `git grep -n "redis-cli --scan" scripts/phase-32.1-close.ps1` returns matches; version `v3.6.0` retained.
- `scripts/phase-32-close.ps1` deleted (not tracked).
- LIVE `GATE_EXIT = 0` is the operator gate (documented runbook above; do-not-block per Phase-31/31.1 precedent).
</verification>

<success_criteria>
- `scripts/phase-32.1-close.ps1` authored — 3xGREEN + triple-SHA, v3.6.0, no destructive flush, no `skp:cancelled:*` scan-clean.
- `scripts/phase-32-close.ps1` removed.
- ParseFile-clean, BOM-free, FLUSHDB-grep == 0.
- Live `GATE_EXIT = 0` is the operator gate.
</success_criteria>

<output>
After completion, create `.planning/phases/32.1-dead-letter-on-exhaustion/32.1-02-SUMMARY.md`
</output>
