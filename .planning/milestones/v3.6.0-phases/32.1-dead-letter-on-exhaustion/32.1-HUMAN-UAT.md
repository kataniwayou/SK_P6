---
status: resolved
phase: 32.1-dead-letter-on-exhaustion
source: [32.1-VERIFICATION.md, 32.1-02-SUMMARY.md]
started: 2026-06-05
updated: 2026-06-05
---

## Current Test

[none — live close gate PASSED 2026-06-05]

## Tests

### 1. phase-32.1-close.ps1 triple-SHA close gate (GATE_EXIT=0)
expected: With the full v3.6.0 compose stack up (rebuilt containers — embedded SourceHash must match the host build), `pwsh -NoProfile -File ./scripts/phase-32.1-close.ps1` runs 3×GREEN full suite (same fact count all 3 runs, now WITHOUT the deleted breaker tests/E2E), Release+Debug 0-warning builds, and triple-SHA `BEFORE == AFTER` all HELD (psql `\l` / redis `--scan` / rabbitmqctl `list_queues`). Because the breaker is reverted, NO `skp:cancelled:*` key is ever written — the redis `--scan` SHA holds with no special scan-clean; ZERO `skp:cancelled:*` keys must appear in the AFTER snapshot (any such key would mean the Plan-01 revert was incomplete). Read `GATE_*_EXIT` from the gate's own `exit 0` / "PASSED" output, NOT the background-task wrapper exit.
result: [PASSED 2026-06-05 — GATE_EXIT=0. 3×GREEN = 452 facts (runs 5:45 / 5:58 / 6:10, identical count); Release+Debug 0-warning. Triple-SHA BEFORE==AFTER all HELD: psql \l `34ac23852ac61a2fa704e7a1a30b7f85821490b0c4d0924c28b3c0dd530ca911`, redis --scan `666b99bc0183e005f289f787d04c88b4a038fb18221947cd8f9bee42c6e1f140`, rabbitmqctl list_queues `2c9b086863bafbd34f8361f2060aa0dc5fb6d0be877e51dadfbf9b62712eceee`. Settle-drain returned redis to the BEFORE baseline (net-zero); ZERO skp:cancelled:* keys.]

## Live smoke verification (2026-06-05) — behavioral half PROVEN

The dead-letter-on-exhaustion runtime behavior was verified live against the rebuilt v3.6.0 compose
stack (orchestrator + processor-sample + baseapi-service rebuilt to the reverted SourceHash
`7aab7dc0…`). A scratch RealStack test induced a deterministic infra fault by poisoning the
consumer's first Redis GET (`StringGetAsync(Flag(dispatch.H))` against a LIST key → WRONGTYPE on
every delivery), then self-verified via Prometheus + Elasticsearch + the RabbitMQ management API
(no human inspection):

- **[Prometheus]** `processor_dispatch_consumed_total{ProcessorId}` climbed by **4** for the
  exhausted dispatch = `Immediate(Limit=3)` + 1 — the retry attempts are observable as a metric.
- **[RabbitMQ]** the `{procId:D}_error` queue went **0 → 1** — the exhausted fault dead-letters to
  `_error` (plain dead-lettering, the breaker's replacement).
- **[Elasticsearch]** a processor-sample infra-fault log (WRONGTYPE / R-FAULT) was ingested.
- **[Redis]** **zero** `skp:cancelled:*` keys — the breaker is reverted, no marker is ever written.

This proves req-1 (processor dead-letters on exhaustion via `UseMessageRetry`, no marker/trip/
unschedule) and the no-`skp:cancelled` revert invariant LIVE. The broader `phase-32.1-close.ps1`
**3×GREEN + triple-SHA** keyspace-net-zero close gate (test #1) was subsequently run and **PASSED
(GATE_EXIT=0)** — both halves of the live verification are now complete.

## Operator runbook

1. `docker compose up -d --build processor-sample orchestrator baseapi-service`  (rebuild required — the containers must run the Phase-32.1 reverted code; the breaker catch + Fault consumer are GONE this phase)
2. Confirm `docker compose ps` shows postgres, redis, rabbitmq, otel-collector, elasticsearch, prometheus, orchestrator, processor-sample, baseapi-service all healthy
3. `pwsh -NoProfile -File ./scripts/phase-32.1-close.ps1`  → expect GATE_EXIT=0 (3×GREEN + triple-SHA BEFORE==AFTER incl. skp:flag:* / skp:data:* settled; ZERO skp:cancelled:*)

On GATE_EXIT=0: append the three SHA values + the Passed count to the STATE.md Phase 32.1 close entry, then mark this item passed via `/gsd-verify-work 32.1`.

Failure triage (MEMORY references):
- Drifted redis/psql/rmq SHA on a 3×GREEN that previously held → usually a stale/flaky prior-phase test or a self-rescheduled cron workflow churning `skp:flag:*` names; rebuild containers first and re-run.
- A `skp:cancelled:*` key appearing in the AFTER snapshot would indicate the breaker revert (Plan 01) was incomplete — there must be ZERO such keys.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
