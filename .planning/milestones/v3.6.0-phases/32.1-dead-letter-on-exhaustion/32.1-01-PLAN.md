---
phase: 32.1-dead-letter-on-exhaustion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
  - src/Orchestrator/Consumers/ResultConsumer.cs
  - src/Orchestrator/Program.cs
  - src/Orchestrator/Consumers/FaultUnscheduleConsumer.cs
  - src/Orchestrator/Consumers/FaultUnscheduleConsumerDefinition.cs
  - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
  - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - tests/BaseApi.Tests/Processor/EntryStepDispatchScopeTests.cs
  - tests/BaseApi.Tests/Processor/EntryStepDispatchRuntimeScopeTests.cs
  - tests/BaseApi.Tests/Processor/CheckAndDropFacts.cs
  - tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs
  - tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs
  - tests/BaseApi.Tests/Processor/BreakerTriggerFacts.cs
  - tests/BaseApi.Tests/Processor/CancelledMarkerFacts.cs
  - tests/BaseApi.Tests/Processor/RetryAttemptNumberingFacts.cs
  - tests/BaseApi.Tests/Contracts/CancelledMarkerKeyFacts.cs
  - tests/BaseApi.Tests/Orchestrator/FaultUnscheduleFacts.cs
  - tests/BaseApi.Tests/Orchestrator/FaultIdempotencyFacts.cs
  - tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs
  - tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs
autonomous: true
requirements: [req-1, req-2, req-3, req-4, req-6]

must_haves:
  truths:
    - "On processor dispatch-budget exhaustion, the infra fault propagates unmodified to UseMessageRetry(Immediate(Limit)) and dead-letters to {processorId}_error — no L2 marker is written, no trip metric increments"
    - "On orchestrator result/Start/Stop exhaustion, the message dead-letters to _error — no in-flight check-and-drop, no Fault-driven unschedule"
    - "In-flight dispatches and results process normally regardless of any cancellation state (both check-and-drop gates removed)"
    - "MassTransit still auto-publishes Fault<EntryStepDispatch> on exhaustion but no consumer binds it (discarded at the exchange, harmless)"
    - "The Phase-31 flag[H]==Ack dedup gates and the DispatchDeduped / ResultDeduped counters remain byte-intact and tested"
    - "The hermetic suite is green with every breaker test removed and zero dangling references to the removed symbols"
  artifacts:
    - path: "src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs"
      provides: "Processor consumer with the breaker catch + cancelled check-and-drop + IOptions<RetryOptions> ctor dep removed; flag[H] dedup gate + DispatchDeduped intact"
      contains: "metrics.DispatchDeduped.Add"
    - path: "src/Orchestrator/Consumers/ResultConsumer.cs"
      provides: "Orchestrator consumer with the cancelled check-and-drop removed; flag[H] dedup gate + ResultDeduped intact"
      contains: "metrics.ResultDeduped.Add"
    - path: "src/Messaging.Contracts/Projections/L2ProjectionKeys.cs"
      provides: "L2 keys with Cancelled builder + CancelledMarkerValue removed; Flag + ExecutionData intact"
      contains: "public static string Flag(string h)"
    - path: "src/BaseProcessor.Core/Observability/ProcessorMetrics.cs"
      provides: "ProcessorMetrics with WorkflowCancelled removed; DispatchDeduped intact"
      contains: "DispatchDeduped"
  key_links:
    - from: "src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs"
      to: "UseMessageRetry(Immediate(Limit))"
      via: "dispatch-endpoint bind at :154 — the dead-letter mechanism that REPLACES the breaker"
      pattern: "UseMessageRetry\\(r => r.Immediate\\(retryLimit\\)\\)"
    - from: "src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs"
      to: "Configure<RetryOptions>"
      via: "the RetryOptions bind feeding ProcessorStartupOrchestrator — KEPT (only the consumer ctor dep is removed)"
      pattern: "Configure<RetryOptions>"
---

<objective>
Surgically revert the Phase-32 runtime "cancelled circuit-breaker" across the 7 source files and the breaker-specific test surface, leaving plain dead-lettering on retry-budget exhaustion (the pre-Phase-32 behavior). This is a NET DELETION: remove the L2 cancellation marker, both in-flight check-and-drop gates, the final-attempt breaker catch, the `IOptions<RetryOptions>` ctor dep on `EntryStepDispatchConsumer`, the `Fault<EntryStepDispatch>` fan-out consumer + its `Program.cs` registration, the `Cancelled` key builder + `CancelledMarkerValue` sentinel, and the `WorkflowCancelled` trip metric.

Purpose: After review the breaker was judged not worth its surface area (32-REVIEW.md WR-01 — best-effort future-fire stop; no-TTL marker + manual resume; degrades to plain `_error` under the very infra-down conditions that trip it). The user took "stop the job" off the table.

Output: A buildable, hermetic-green codebase with the breaker gone and the Phase-31 `flag[H]` idempotency gates + their `DispatchDeduped`/`ResultDeduped` counters + the configurable retry `Limit` binding all byte-intact.

CRITICAL KEEP-INTACT (do NOT touch — these are the dead-letter mechanism + the retained idempotency layer):
- `ProcessorStartupOrchestrator.cs:151,154` — `var retryLimit = retryOptions.Value.Limit;` + `cfg.UseMessageRetry(r => r.Immediate(retryLimit));` (the processor dispatch-endpoint dead-letter bind; the breaker's REPLACEMENT).
- `BaseProcessorServiceCollectionExtensions.cs:88` — `services.Configure<RetryOptions>(cfg.GetSection("Retry"));` (feeds ProcessorStartupOrchestrator; KEEP).
- `Orchestrator/Program.cs:29` — `builder.Services.Configure<RetryOptions>(...)` and the 3 ConsumerDefinitions (`Result`/`Start`/`Stop`) `UseMessageRetry(Immediate(Limit))` (orchestrator dead-letter binds; KEEP).
- The `flag[H]=="Ack"` dedup gate + `metrics.DispatchDeduped.Add(...)` at the TOP of `EntryStepDispatchConsumer.Consume`; the `flag[m.H]=="Ack"` gate + `metrics.ResultDeduped.Add(...)` at the TOP of `ResultConsumer.Consume`.
- `StepEntryCondition.PreviousCancelled (3)` and `StepOutcome.Cancelled (3)` — DO NOT touch either enum file (req-6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32.1-dead-letter-on-exhaustion/32.1-SPEC.md
@.planning/STATE.md

<interfaces>
<!-- Exact KEEP-intact code (Phase-31 dedup gates + retry binds). Do NOT alter these blocks. -->

KEEP — EntryStepDispatchConsumer.Consume, flag[H] dedup gate (current :78-86):
```csharp
if ((string?)await db.StringGetAsync(L2ProjectionKeys.Flag(dispatch.H)) == "Ack")
{
    metrics.DispatchDeduped.Add(1,
        new KeyValuePair<string, object?>("ProcessorId", context.Id!.Value.ToString("D")));
    return;
}
```

KEEP — ResultConsumer.Consume, flag[m.H] dedup gate (current :65-72):
```csharp
if ((string?)await db.StringGetAsync(L2ProjectionKeys.Flag(m.H)) == "Ack")
{
    metrics.ResultDeduped.Add(1, new KeyValuePair<string, object?>("ProcessorId", m.ProcessorId.ToString("D")));
    return;
}
```

KEEP — ProcessorStartupOrchestrator.cs:151,154 (the dead-letter REPLACEMENT for the breaker):
```csharp
var retryLimit = retryOptions.Value.Limit;
...
cfg.UseMessageRetry(r => r.Immediate(retryLimit));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the breaker from the 7 source files (catch, both check-and-drop gates, ctor dep, Fault consumer + Program.cs, Cancelled key+sentinel, WorkflowCancelled metric)</name>
  <read_first>
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
    - src/Orchestrator/Consumers/ResultConsumer.cs
    - src/Orchestrator/Program.cs
    - src/Orchestrator/Consumers/FaultUnscheduleConsumer.cs
    - src/Orchestrator/Consumers/FaultUnscheduleConsumerDefinition.cs
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
    - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs
    - src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs (READ-ONLY — confirm the KEPT UseMessageRetry bind at :151,154)
    - .planning/phases/32.1-dead-letter-on-exhaustion/32.1-SPEC.md
  </read_first>
  <action>
Perform exactly these edits. Each names the exact symbol to remove and the exact KEEP-intact neighbors.

**Edit A — `src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs` (req-1, req-2):**
  1. Remove the ctor parameter `IOptions<RetryOptions> retryOptions,` at line 50 (the breaker-only DI dep). The ctor becomes `(IConnectionMultiplexer redis, IProcessorContext context, BaseProcessor processor, IOptions<ProcessorLivenessOptions> options, ISendEndpointProvider sendProvider, ProcessorMetrics metrics, ILogger<EntryStepDispatchConsumer> logger)`.
  2. Remove the cancelled check-and-drop gate at lines 88-96 — the entire `// ---- 0b. Check-and-drop gate for a cancelled workflow (req-3 / D-05) ----` comment block AND its `if ((string?)await db.StringGetAsync(L2ProjectionKeys.Cancelled(dispatch.WorkflowId)) == L2ProjectionKeys.CancelledMarkerValue) return;` statement.
  3. Remove the breaker `try`/`catch` wrapper (lines 98-105 the `// ---- BREAKER SEAM ... ----` comment + `try {`, and lines 245-268 the closing `}` + the entire `catch (Exception) when (ctx.GetRetryAttempt() == retryOptions.Value.Limit) { ... throw; }` block). The infra body (input resolution through the CAS flip, current lines 106-244) must be DE-INDENTED back to its pre-breaker indentation and left otherwise byte-identical — it is no longer wrapped. The two INNER business catches around `processor.ExecuteAsync` (the `OperationCanceledException` and generic `catch (Exception ex)` at lines 150-161) STAY exactly as-is.
  4. Remove the now-unused `using Microsoft.Extensions.Options;` (line 11) and `using Messaging.Contracts.Configuration;` (line 7) ONLY IF no other symbol in the file still needs them — `IOptions<ProcessorLivenessOptions>` still needs `Microsoft.Extensions.Options`, so KEEP line 11; `RetryOptions` lived in `Messaging.Contracts.Configuration` — if nothing else references that namespace, remove line 7. Verify by grep before removing.
  KEEP BYTE-INTACT: the `// ---- 0. Effect-first dedup gate (D-06) ----` block (lines 74-86) including `metrics.DispatchDeduped.Add(...)`; `metrics.DispatchConsumed.Add(...)` at :64-65; the whole `SendResult`/`OutcomeLabel`/`BuildCompleted`/`BuildFailed`/`BuildCancelled` tail (lines 271-349). `BuildCancelled` STAYS (EXEC-08 token-cancellation business outcome, req-6).

**Edit B — `src/Orchestrator/Consumers/ResultConsumer.cs` (req-2):**
  Remove the cancelled check-and-drop gate at lines 74-82 — the entire `// ---- 0a. Check-and-drop for a cancelled workflow (req-3 / D-05) ----` comment block AND its `if ((string?)await db.StringGetAsync(L2ProjectionKeys.Cancelled(m.WorkflowId)) == L2ProjectionKeys.CancelledMarkerValue) return;` statement.
  KEEP BYTE-INTACT: the `metrics.ResultConsumed.Add(...)` at :55; the `// ---- 0. Effect-first dedup gate (D-06, orchestrator hop) ----` flag[m.H] block (lines 62-72) including `metrics.ResultDeduped.Add(...)`; the L1 read + manifest unbundle + N×M fan-out + CAS flip (lines 84-126).

**Edit C — `src/Orchestrator/Program.cs` (req-3):**
  Remove the Fault registration at lines 44-49 — the `// Phase 32 (req-4 / D-06): the future-fire stop. ...` comment block AND the `x.AddConsumer<FaultUnscheduleConsumer, FaultUnscheduleConsumerDefinition>().Endpoint(e => { e.InstanceId = instanceId; e.Temporary = true; });` statement. The `instanceId` closure (line 35) STAYS (Start/Stop still use it). The `Configure<RetryOptions>` at line 29 STAYS.

**Edit D — DELETE both files (req-3):**
  - `src/Orchestrator/Consumers/FaultUnscheduleConsumer.cs`
  - `src/Orchestrator/Consumers/FaultUnscheduleConsumerDefinition.cs`

**Edit E — `src/Messaging.Contracts/Projections/L2ProjectionKeys.cs` (req-4):**
  1. Remove the `Cancelled(Guid workflowId)` builder (the `/// <summary> Phase 32 (D-02/D-07)...` doc + `public static string Cancelled(Guid workflowId) => $"{Prefix}cancelled:{workflowId:D}";` at lines 54-64).
  2. Remove the `CancelledMarkerValue` const (the `/// <summary> Phase 32 (D-02)...` doc + `public const string CancelledMarkerValue = "true";` at lines 66-71).
  3. Remove the `<item><description>Cancelled: {Prefix}cancelled:{workflowId:D} — the no-TTL in-flight cancellation marker (D-02/D-07)</description></item>` line in the class doc `<list>` (line 26).
  KEEP BYTE-INTACT: `Prefix`, `ParentIndex`, `Root`, `Step`, `Processor`, `ExecutionData(string)`, `ExecutionData(Guid)`, `Flag(string h)`.

**Edit F — `src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` (req-4):**
  1. Remove the `WorkflowCancelled` property (the `/// <summary> Phase 32 (D-11)...` doc + `public Counter<long> WorkflowCancelled { get; }` at lines 42-46).
  2. Remove its construction line `WorkflowCancelled = meter.CreateCounter<long>("workflow_cancelled");` at line 54.
  KEEP BYTE-INTACT: `MeterName`, `DispatchConsumed`, `ResultSent`, and `DispatchDeduped` (the property at :36-40 AND its construction `DispatchDeduped = meter.CreateCounter<long>("processor_dispatch_deduped");` at :53).
  </action>
  <verify>
    <automated>dotnet build src/Messaging.Contracts -c Debug; dotnet build src/BaseProcessor.Core -c Debug; dotnet build src/Orchestrator -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build SK_P.sln -c Release` reports 0 Warning / 0 Error.
    - `git grep -nE "L2ProjectionKeys\.Cancelled|CancelledMarkerValue" -- 'src/*.cs'` returns ZERO matches.
    - `git grep -nE "WorkflowCancelled|workflow_cancelled" -- 'src/*.cs'` returns ZERO matches.
    - `git grep -nE "FaultUnscheduleConsumer" -- 'src/*.cs'` returns ZERO matches; `src/Orchestrator/Consumers/FaultUnscheduleConsumer.cs` and `FaultUnscheduleConsumerDefinition.cs` do NOT exist on disk.
    - `git grep -n "GetRetryAttempt" -- 'src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs'` returns ZERO matches.
    - `git grep -n "IOptions<RetryOptions>" -- 'src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs'` returns ZERO matches.
    - KEEP confirmed: `git grep -n "metrics.DispatchDeduped.Add" src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs` returns exactly 1 match; `git grep -n "metrics.ResultDeduped.Add" src/Orchestrator/Consumers/ResultConsumer.cs` returns exactly 1 match.
    - KEEP confirmed: `git grep -n "DispatchDeduped" src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` returns matches (property + construction); `git grep -n "processor_dispatch_deduped" src/BaseProcessor.Core/Observability/ProcessorMetrics.cs` returns 1 match.
    - KEEP confirmed (the dead-letter mechanism, UNCHANGED): `git grep -n "UseMessageRetry(r => r.Immediate(retryLimit))" src/BaseProcessor.Core/Startup/ProcessorStartupOrchestrator.cs` returns 1 match; `git grep -n "Configure<RetryOptions>" src/BaseProcessor.Core/DependencyInjection/BaseProcessorServiceCollectionExtensions.cs` returns 1 match; `git grep -n "Configure<RetryOptions>" src/Orchestrator/Program.cs` returns 1 match.
    - KEEP confirmed (req-6, enums untouched): `git diff --name-only` does NOT list `src/BaseApi.Service/Features/Step/StepEntryCondition.cs` nor `src/Messaging.Contracts/StepOutcome.cs`.
    - `git grep -n "BuildCancelled" src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs` still returns matches (EXEC-08 token-cancellation path retained).
  </acceptance_criteria>
  <done>The breaker is gone from all 7 source files; the solution builds 0/0 in Release; the flag[H] dedup gates + DispatchDeduped/ResultDeduped + the UseMessageRetry dead-letter binds are byte-intact; the two enum files are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the breaker-specific tests, re-home the kept dedup-counter coverage, and thread the removed RetryOptions ctor arg out of the test kit</name>
  <read_first>
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
    - tests/BaseApi.Tests/Processor/EntryStepDispatchScopeTests.cs
    - tests/BaseApi.Tests/Processor/EntryStepDispatchRuntimeScopeTests.cs
    - tests/BaseApi.Tests/Processor/CheckAndDropFacts.cs
    - tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs
    - tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs
    - tests/BaseApi.Tests/Processor/BreakerTriggerFacts.cs
    - tests/BaseApi.Tests/Processor/CancelledMarkerFacts.cs
  </read_first>
  <action>
**Edit A — DELETE these wholly-breaker test files (req-5):**
  - `tests/BaseApi.Tests/Processor/BreakerTriggerFacts.cs` (breaker catch: RetryContext + marker + WorkflowCancelled)
  - `tests/BaseApi.Tests/Processor/CancelledMarkerFacts.cs` (no-TTL marker write)
  - `tests/BaseApi.Tests/Processor/RetryAttemptNumberingFacts.cs` (GetRetryAttempt boundary probe — only the breaker needed it)
  - `tests/BaseApi.Tests/Contracts/CancelledMarkerKeyFacts.cs` (Cancelled key builder + sentinel)
  - `tests/BaseApi.Tests/Orchestrator/FaultUnscheduleFacts.cs`
  - `tests/BaseApi.Tests/Orchestrator/FaultIdempotencyFacts.cs`
  - `tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs`
  - `tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs`

**Edit B — `tests/BaseApi.Tests/Processor/DispatchTestKit.cs` (test-infra revert):**
  1. Remove the `Retry(int limit = 3)` helper (the `/// <summary>The retry budget the breaker check reads...` doc + the method, lines 133-139).
  2. Remove the `RetryContext<T>(...)` helper (the `/// <summary>A ConsumeContext<T> substitute whose GetRetryAttempt()...` doc + the whole method, lines 163-191).
  3. In `Build(...)` (lines 142-149), remove the `Retry(),` argument from the `new(...)` call so it matches the new 7-param ctor: `new(redis, context, processor, Options(executionDataTtlSeconds), sendProvider, Metrics(), NullLogger<EntryStepDispatchConsumer>.Instance);`.
  4. Remove the now-unused `using Microsoft.Extensions.Options;` (line 9) ONLY IF no remaining symbol needs it — `Options(...)` returns `IOptions<ProcessorLivenessOptions>` and uses `Microsoft.Extensions.Options.Options.Create`, so KEEP line 9. Verify by grep.

**Edit C — `tests/BaseApi.Tests/Processor/EntryStepDispatchScopeTests.cs`:**
  Remove `DispatchTestKit.Retry(),` from BOTH direct `new EntryStepDispatchConsumer(...)` calls (at :73-75 and :120-122) so each becomes `new EntryStepDispatchConsumer(WritableRedis(), context, processor, DispatchTestKit.Options(300), send, DispatchTestKit.Metrics(), logger);`.

**Edit D — `tests/BaseApi.Tests/Processor/EntryStepDispatchRuntimeScopeTests.cs`:**
  Remove the `.AddSingleton(DispatchTestKit.Retry())` registration (and its 2-line `// Phase 32: the breaker check reads IOptions<RetryOptions>...` comment at :124-126). The `EntryStepDispatchConsumer` no longer takes `IOptions<RetryOptions>`, so the runtime-scope DI harness no longer needs to register it.

**Edit E — `tests/BaseApi.Tests/Processor/CheckAndDropFacts.cs` (re-home kept dedup coverage, req-2):**
  KEEP the `FlagAck_Increments_DispatchDeduped_Once_And_Discards` fact (lines 53-102) — it is the kept `DispatchDeduped` increment coverage at the flag[H] gate. Remove the 3 cancelled-marker facts: `CancelledMarkerSet_AckAndDiscards_NoSend_NoCounter_NoWrite` (:104-161), `OtherWorkflowMarker_DoesNotAffectThisDispatch` (:163-192), `CancelledPath_TouchesNoFlagKey` (:194-233). Fix the `BuildWith(...)` helper (:47-51) to drop the `DispatchTestKit.Retry(),` arg (now `new(redis, context, processor, DispatchTestKit.Options(300), send, metrics, NullLogger<EntryStepDispatchConsumer>.Instance)`). Update the class XML doc `<list>` to describe ONLY the retained dedup fact (remove the 3 cancelled bullets). Rename the class to `DispatchDedupFacts` if "CheckAndDrop" no longer fits — OPTIONAL; if renamed, also rename the file to `DispatchDedupFacts.cs`. If you keep the filename/classname, that is acceptable as long as it compiles and only the dedup fact remains.

**Edit F — `tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs` (re-home kept dedup coverage, req-2):**
  KEEP the `FlagAlreadyAck_IncrementsDedupOnce_Drops_NoDispatch` fact (:110-159) — the kept `ResultDeduped` increment coverage — AND the `DedupScope` helper (:66-106) + `SeedWorkflow`/`Step`/`Build` helpers it depends on. Remove the 2 cancelled facts: `CancelledMarkerSet_AckAndDiscards_NoDispatch_NoDedup_NoWrite` (:163-220) and `OtherWorkflowCancelled_ThisResultProceeds_Dispatches` (:224-280) AND the `RecordingDispatcher` nested class (:282-296) IF nothing else in the trimmed file references it (the kept fact uses `Substitute.For<IStepDispatcher>()`, so `RecordingDispatcher` becomes unused — remove it). Update the class XML doc `<list>` to describe ONLY the retained dedup fact. Optionally rename to `ResultDedupFacts`.

**Edit G — `tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs` (re-home kept dedup-metric coverage, req-4):**
  Remove every `WorkflowCancelled` reference: in `Processor_New_Counters_Construct_NonNull` (:43-54) drop the `Assert.NotNull(metrics.WorkflowCancelled);` line (KEEP the `Assert.NotNull(metrics.DispatchDeduped);`); in `Recorded_Measurement_Carries_ProcessorId_But_No_WorkflowId_Label` (:68-113) drop the `procMetrics.WorkflowCancelled.Add(1, tag);` line (:102) and change the expected measurement count from `Assert.Equal(3, capturedTagKeySets.Count);` to `Assert.Equal(2, capturedTagKeySets.Count);` (now only `DispatchDeduped` + `ResultDeduped` fire). KEEP `Meter_Name_Consts_Are_Unchanged`, `Orchestrator_New_Counter_Constructs_NonNull` (ResultDeduped), and all `DispatchDeduped`/`ResultDeduped` assertions. Update the class XML doc to drop the `workflow_cancelled` mention. Optionally rename to `DedupMetricsFacts`.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests -c Debug; dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build tests/BaseApi.Tests -c Debug` reports 0 Warning / 0 Error.
    - Full hermetic suite `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` = Failed 0 (all GREEN).
    - The 8 deleted test files do NOT exist on disk: `git ls-files tests/ | grep -iE "BreakerTriggerFacts|CancelledMarkerFacts|RetryAttemptNumberingFacts|CancelledMarkerKeyFacts|FaultUnscheduleFacts|FaultIdempotencyFacts|FaultConsumerBindingFacts|CancelledCircuitBreakerE2ETests"` returns ZERO matches.
    - `git grep -nE "L2ProjectionKeys\.Cancelled|CancelledMarkerValue|WorkflowCancelled|FaultUnscheduleConsumer|RetryContext|DispatchTestKit\.Retry|GetRetryAttempt" -- 'tests/*.cs'` returns ZERO matches (zero dangling references).
    - KEEP confirmed: `git grep -n "DispatchDeduped" -- 'tests/*.cs'` returns matches (CheckAndDrop/DispatchDedup fact + BreakerMetrics/DedupMetrics fact); `git grep -n "ResultDeduped" -- 'tests/*.cs'` returns matches.
    - `git grep -nE "processor_dispatch_deduped|orchestrator_result_deduped" -- 'tests/*.cs'` returns at least one match each (the kept cardinality/increment coverage survives).
  </acceptance_criteria>
  <done>All 8 breaker test files deleted; the 3 partial files trimmed to their kept dedup-counter coverage only; the test kit's `Retry()`/`RetryContext()` helpers + ctor arg removed; the hermetic suite is GREEN with zero dangling references to any removed symbol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker → processor consume | `EntryStepDispatch` crosses into `EntryStepDispatchConsumer`; on infra exhaustion it now dead-letters to `_error` (was: breaker trip + marker write) |
| broker → orchestrator consume | `ExecutionResult` / `Fault<EntryStepDispatch>` cross into the orchestrator; the Fault binding is removed (Fault discarded at the exchange) |
| process → Redis L2 | the cancellation-marker WRITE path (no-TTL `skp:cancelled:*`) is REMOVED — net reduction of write surface |

## STRIDE Threat Register

This phase REMOVES attack surface. Each entry is a threat ELIMINATED or a regression we must NOT introduce.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32.1-01 | Tampering | regression: accidentally deleting the Phase-31 `flag[H]` idempotency gate while removing the cancelled check-and-drop | mitigate | acceptance_criteria assert `metrics.DispatchDeduped.Add` (1 match) + `metrics.ResultDeduped.Add` (1 match) remain; the flag[H]=="Ack" gate is the byte-intact KEEP block in `<interfaces>` |
| T-32.1-02 | Denial of Service | regression: removing the `UseMessageRetry(Immediate(Limit))` dead-letter bind while removing the breaker `IOptions<RetryOptions>` ctor dep — an infra fault would loop forever with no `_error` backstop | mitigate | acceptance_criteria assert `ProcessorStartupOrchestrator.cs` `UseMessageRetry(r => r.Immediate(retryLimit))` (1 match) + both `Configure<RetryOptions>` binds remain; only the consumer ctor dep is removed, NOT the endpoint bind |
| T-32.1-03 | Spoofing/Repudiation | forged `Fault<EntryStepDispatch>` after the consumer is removed | accept (now eliminated) | with no binding the Fault is discarded at the exchange — the unschedule attack surface (forged-fault DoS, old T-32-04) is GONE; no mitigation needed |
| T-32.1-04 | Information Disclosure | no-TTL marker-namespace exhaustion (old T-32-09) via `skp:cancelled:*` accumulation | accept (now eliminated) | the marker WRITE site is removed; no `skp:cancelled:*` key is ever written again — the close-gate scan-clean (Plan 02) is consequently NOT needed |
| T-32.1-05 | Elevation/Tampering | unauthorized manual resume via `DEL skp:cancelled` (old T-32-05) | accept (now eliminated) | no marker exists to resume from; the manual-resume operator path is gone with the breaker |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Release` — 0 Warning / 0 Error.
- `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` — Failed 0.
- `git grep -nE "L2ProjectionKeys\.Cancelled|CancelledMarkerValue|WorkflowCancelled|workflow_cancelled|FaultUnscheduleConsumer|RetryContext|DispatchTestKit\.Retry|GetRetryAttempt" -- 'src/*.cs' 'tests/*.cs'` — ZERO matches.
- KEEP greps (each returns ≥1): `metrics.DispatchDeduped.Add` in EntryStepDispatchConsumer.cs; `metrics.ResultDeduped.Add` in ResultConsumer.cs; `UseMessageRetry(r => r.Immediate(retryLimit))` in ProcessorStartupOrchestrator.cs; `Configure<RetryOptions>` in both BaseProcessorServiceCollectionExtensions.cs and Orchestrator/Program.cs.
- `git diff --name-only` does NOT include `StepEntryCondition.cs` or `StepOutcome.cs` (req-6).
</verification>

<success_criteria>
- On exhaustion the processor/orchestrator messages dead-letter to `_error` with no marker write, no trip metric, no unschedule (verified structurally: the catch + gates + Fault consumer are gone; the `UseMessageRetry` binds remain).
- No `L2ProjectionKeys.Cancelled` / `CancelledMarkerValue` / `WorkflowCancelled` / `FaultUnscheduleConsumer` remain in `src/`.
- `processor_dispatch_deduped` + `orchestrator_result_deduped` counters and their `flag[H]` gates are intact and tested.
- Hermetic suite green with the breaker tests removed and zero dangling references.
- `StepEntryCondition` / `StepOutcome` untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/32.1-dead-letter-on-exhaustion/32.1-01-SUMMARY.md`
</output>
