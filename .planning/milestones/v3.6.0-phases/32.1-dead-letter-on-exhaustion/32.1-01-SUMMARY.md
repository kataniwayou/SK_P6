---
phase: 32.1-dead-letter-on-exhaustion
plan: 01
subsystem: processor + orchestrator messaging (dead-letter on exhaustion)
tags: [revert, net-deletion, circuit-breaker, dead-letter, idempotency, observability]
requires: [32-02, 32-04, 32-05, 32-06]
provides:
  - "plain dead-lettering on retry-budget exhaustion (pre-Phase-32 behavior restored)"
  - "Phase-31 flag[H] idempotency gates + DispatchDeduped/ResultDeduped counters byte-intact"
affects:
  - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
  - src/Orchestrator/Consumers/ResultConsumer.cs
  - src/Orchestrator/Program.cs
  - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
  - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs
tech-stack:
  added: []
  patterns:
    - "UseMessageRetry(Immediate(Limit)) -> _error is the sole exhaustion mechanism (breaker REPLACEMENT, unchanged)"
key-files:
  created: []
  modified:
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
    - src/Orchestrator/Consumers/ResultConsumer.cs
    - src/Orchestrator/Program.cs
    - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
    - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
    - tests/BaseApi.Tests/Processor/EntryStepDispatchScopeTests.cs
    - tests/BaseApi.Tests/Processor/EntryStepDispatchRuntimeScopeTests.cs
    - tests/BaseApi.Tests/Processor/CheckAndDropFacts.cs
    - tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs
    - tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs
  deleted:
    - src/Orchestrator/Consumers/FaultUnscheduleConsumer.cs
    - src/Orchestrator/Consumers/FaultUnscheduleConsumerDefinition.cs
    - tests/BaseApi.Tests/Processor/BreakerTriggerFacts.cs
    - tests/BaseApi.Tests/Processor/CancelledMarkerFacts.cs
    - tests/BaseApi.Tests/Processor/RetryAttemptNumberingFacts.cs
    - tests/BaseApi.Tests/Contracts/CancelledMarkerKeyFacts.cs
    - tests/BaseApi.Tests/Orchestrator/FaultUnscheduleFacts.cs
    - tests/BaseApi.Tests/Orchestrator/FaultIdempotencyFacts.cs
    - tests/BaseApi.Tests/Orchestrator/FaultConsumerBindingFacts.cs
    - tests/BaseApi.Tests/Orchestrator/CancelledCircuitBreakerE2ETests.cs
decisions:
  - "Breaker removed in full (catch + both check-and-drop gates + Fault consumer + L2 marker + trip metric); plain _error dead-lettering retained as the only exhaustion path"
  - "Phase-31 flag[H] dedup gates + DispatchDeduped/ResultDeduped counters + configurable retry Limit all kept byte-intact"
  - "StepEntryCondition / StepOutcome enums untouched (req-6)"
metrics:
  duration: ~25m
  completed: 2026-06-05
  tasks: 2
  files_changed: 17
requirements: [req-1, req-2, req-3, req-4, req-6]
---

# Phase 32.1 Plan 01: Dead-Letter on Exhaustion (Breaker Reverted) Summary

Surgically reverted the Phase-32 runtime "cancelled circuit-breaker" across 7 source files and the breaker-specific test surface — a NET DELETION (161 source deletions, 0 insertions) leaving plain dead-lettering to `_error` on retry-budget exhaustion, with the Phase-31 `flag[H]` idempotency gates + their `DispatchDeduped`/`ResultDeduped` counters + the configurable retry `Limit` binding all byte-intact.

## What Was Done

### Task 1 — Remove the breaker from 7 source files (commit `c046cc8`)
- **EntryStepDispatchConsumer.cs:** removed the final-attempt breaker `catch (Exception) when (ctx.GetRetryAttempt() == retryOptions.Value.Limit)` (no-TTL marker write + `WorkflowCancelled` trip + WARN log + re-throw), the cancelled check-and-drop gate (0b), the `IOptions<RetryOptions> retryOptions` ctor parameter, and the now-unused `using Messaging.Contracts.Configuration;`. The infra body de-indented back (it was wrapped at the same level, so no re-indent needed). KEPT byte-intact: the `flag[H]=="Ack"` dedup gate + `metrics.DispatchDeduped.Add`, `DispatchConsumed`, both inner business catches, and the whole `SendResult`/`OutcomeLabel`/`Build*` tail (incl. `BuildCancelled`, EXEC-08).
- **ResultConsumer.cs:** removed the cancelled check-and-drop gate (0a). KEPT: `ResultConsumed`, the `flag[m.H]=="Ack"` gate + `metrics.ResultDeduped.Add`, the L1 read + manifest unbundle + N×M fan-out + CAS flip.
- **Program.cs:** removed the `FaultUnscheduleConsumer` endpoint registration + its comment. KEPT: the `instanceId` closure (Start/Stop still use it) and `Configure<RetryOptions>`.
- **Deleted** `FaultUnscheduleConsumer.cs` + `FaultUnscheduleConsumerDefinition.cs`.
- **L2ProjectionKeys.cs:** removed the `Cancelled(Guid)` builder, the `CancelledMarkerValue` const, and the class-doc `<list>` item. KEPT: `Prefix`/`ParentIndex`/`Root`/`Step`/`Processor`/`ExecutionData(string)`/`ExecutionData(Guid)`/`Flag(string)`.
- **ProcessorMetrics.cs:** removed the `WorkflowCancelled` property + its `CreateCounter` line. KEPT `DispatchDeduped` (property + `processor_dispatch_deduped` construction).
- **READ-ONLY confirmed kept (the dead-letter REPLACEMENT):** `ProcessorStartupOrchestrator.cs` `UseMessageRetry(r => r.Immediate(retryLimit))`; both `Configure<RetryOptions>` binds.

### Task 2 — Delete breaker tests, re-home kept dedup coverage (commit `f325a5f`)
- **Deleted 8 wholly-breaker test files:** `BreakerTriggerFacts`, `CancelledMarkerFacts`, `RetryAttemptNumberingFacts`, `CancelledMarkerKeyFacts`, `FaultUnscheduleFacts`, `FaultIdempotencyFacts`, `FaultConsumerBindingFacts`, `CancelledCircuitBreakerE2ETests`.
- **DispatchTestKit.cs:** removed the `Retry(int)` and `RetryContext<T>(...)` helpers + the `Retry()` arg from `Build(...)` (now the 7-param ctor). KEPT `using Microsoft.Extensions.Options;` (`Options(...)` needs it).
- **EntryStepDispatchScopeTests.cs:** dropped `DispatchTestKit.Retry(),` from both direct `new EntryStepDispatchConsumer(...)` calls.
- **EntryStepDispatchRuntimeScopeTests.cs:** dropped the `.AddSingleton(DispatchTestKit.Retry())` registration + its comment.
- **CheckAndDropFacts.cs:** kept `FlagAck_Increments_DispatchDeduped_Once_And_Discards`; removed the 3 cancelled-marker facts; fixed `BuildWith(...)` to drop the `Retry()` arg; trimmed the class doc.
- **ResultCheckAndDropFacts.cs:** kept `FlagAlreadyAck_IncrementsDedupOnce_Drops_NoDispatch` + `DedupScope`/`SeedWorkflow`/`Step`/`Build` helpers; removed the 2 cancelled facts + the now-unused `RecordingDispatcher`; trimmed the class doc.
- **BreakerMetricsFacts.cs:** removed every `WorkflowCancelled` reference (the non-null assert + the `.Add(1, tag)` line); changed the expected measurement count 3 → 2; KEPT all `DispatchDeduped`/`ResultDeduped` assertions + meter-name consts.

## Verification

- `dotnet build SK_P.sln -c Release` — **0 Warning / 0 Error**.
- `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` — **Passed 447, Failed 0** (was 467 before the 20 net breaker-test removals; zero regression).
- ZERO-match dangling-reference greps all empty (`L2ProjectionKeys.Cancelled` / `CancelledMarkerValue` / `WorkflowCancelled` / `workflow_cancelled` / `FaultUnscheduleConsumer` / `RetryContext` / `DispatchTestKit.Retry` / `GetRetryAttempt` across `src/*.cs` + `tests/*.cs`); both Fault source files + all 8 breaker test files absent on disk.
- KEEP greps all confirmed ≥1: `metrics.DispatchDeduped.Add` (1) in EntryStepDispatchConsumer.cs; `metrics.ResultDeduped.Add` (1) in ResultConsumer.cs; `DispatchDeduped` property + `processor_dispatch_deduped` (1 construction) in ProcessorMetrics.cs; `UseMessageRetry(r => r.Immediate(retryLimit))` (1) in ProcessorStartupOrchestrator.cs; `Configure<RetryOptions>` (1 each) in BaseProcessorServiceCollectionExtensions.cs and Orchestrator/Program.cs; `BuildCancelled` (3) retained; dedup coverage (`DispatchDeduped`/`ResultDeduped` + the `processor_dispatch_deduped`/`orchestrator_result_deduped` instrument-name strings) intact in tests.
- `git diff --name-only` does NOT list `StepEntryCondition.cs` nor `StepOutcome.cs` (req-6 — enums untouched).

## Deviations from Plan

None — the plan executed exactly as written. Both tasks are pure deletions/trims matching the named symbols; the plan's snapshot line numbers were verified against the live files before each edit (the only structural note: the breaker `try` wrapped the infra body at the same indentation level, so no re-indent of the body was required after removing the `try`/`catch`). No bugs, no missing critical functionality, no blocking issues, no architectural decisions, and no authentication gates were encountered.

## Requirements Landed

- **req-1** — processor breaker catch + `IOptions<RetryOptions>` ctor dep removed; infra fault now propagates to `Immediate(Limit)` → `_error`.
- **req-2** — both check-and-drop gates removed; the `flag[H]=="Ack"` dedup gates + their counters remain byte-intact.
- **req-3** — `FaultUnscheduleConsumer` + definition + `Program.cs` registration deleted (Fault now discarded at the exchange, harmless).
- **req-4** — `Cancelled` builder + `CancelledMarkerValue` + `WorkflowCancelled` trip metric removed; `DispatchDeduped`/`ResultDeduped` kept and tested.
- **req-6** — `StepEntryCondition` / `StepOutcome` untouched.

(req-5 — the `phase-32.1-close.ps1` close-gate + live net-zero proof — is Plan 02's scope, per the phase plan.)

## Self-Check: PASSED

- SUMMARY.md present at `.planning/phases/32.1-dead-letter-on-exhaustion/32.1-01-SUMMARY.md`.
- Task commits `c046cc8` (source) + `f325a5f` (tests) present in git log.
- `FaultUnscheduleConsumer.cs` confirmed absent on disk.
