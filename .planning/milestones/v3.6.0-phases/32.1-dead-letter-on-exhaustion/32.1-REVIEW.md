---
phase: 32.1-dead-letter-on-exhaustion
reviewed: 2026-06-05T00:00:00Z
depth: standard
files_reviewed: 13
files_reviewed_list:
  - scripts/phase-32.1-close.ps1
  - src/BaseProcessor.Core/Observability/ProcessorMetrics.cs
  - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
  - src/Messaging.Contracts/Configuration/RetryOptions.cs
  - src/Messaging.Contracts/Projections/L2ProjectionKeys.cs
  - src/Orchestrator/Consumers/ResultConsumer.cs
  - src/Orchestrator/Program.cs
  - tests/BaseApi.Tests/Orchestrator/BreakerMetricsFacts.cs
  - tests/BaseApi.Tests/Orchestrator/ResultCheckAndDropFacts.cs
  - tests/BaseApi.Tests/Processor/CheckAndDropFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - tests/BaseApi.Tests/Processor/EntryStepDispatchRuntimeScopeTests.cs
  - tests/BaseApi.Tests/Processor/EntryStepDispatchScopeTests.cs
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 32.1: Code Review Report

**Reviewed:** 2026-06-05
**Depth:** standard
**Files Reviewed:** 13
**Status:** clean

## Summary

Phase 32.1 is a net-deletion revert of the Phase-32 "cancelled circuit-breaker",
returning the system to plain dead-lettering on retry-budget exhaustion. The diff
across the seven source files is 341 insertions / 90 deletions, where the 341
insertions are almost entirely the renamed close script (`phase-32-close.ps1` ->
`phase-32.1-close.ps1`, R069) and the deletions are the breaker machinery.

I verified each of the four risk areas called out for this revert, plus a full
compile of both the source and test projects. All reviewed files meet quality
standards. No issues found.

### Verification performed

**1. KEEP-intact logic preserved byte-for-byte.** The Phase-31 idempotency surface
the revert was required to leave untouched is all present and correct in
`EntryStepDispatchConsumer.Consume`:
- the `flag[H]=="Ack"` effect-first dedup gate (lines 76-84),
- the `DispatchDeduped` counter increment inside that gate (lines 81-82),
- the outbound `flag[resultH]="Pending"` seed (lines 208-210),
- the final effect-first CAS flip `StringSetAsync(..., keepTtl: true, when: When.Exists)`
  (line 223).
On the orchestrator side, `ResultConsumer`'s `flag[m.H]=="Ack"` gate +
`ResultDeduped` counter are intact (only the cancelled check-and-drop block was
removed). `RetryOptions.Limit = 3` default is preserved, and the four
`UseMessageRetry(Immediate(Limit))` binds and their `IOptions<RetryOptions>`
injections remain (`Program.cs`, the three orchestrator ConsumerDefinitions,
`BaseProcessorServiceCollectionExtensions`, `ProcessorStartupOrchestrator`).

**2. No dangling references to removed symbols.** Grepped the whole `*.cs` tree for
`WorkflowCancelled`, `CancelledMarkerValue`, `L2ProjectionKeys.Cancelled`,
`FaultUnscheduleConsumer`, `workflow_cancelled`, `Fault<EntryStepDispatch>`,
`GetRetryAttempt`, `RetryContext`, `ConsumeRetryContext`, and
`DispatchTestKit.Retry` — zero matches outside the deleted code. The two deleted
consumer files (`FaultUnscheduleConsumer.cs`, `FaultUnscheduleConsumerDefinition.cs`)
and seven deleted test files leave no orphans; the surviving `RetryOptions`
references are all legitimate KEEP-intact consumers.

**3. De-indentation of the formerly-wrapped infra body is clean.** The breaker
`try { ... } catch (...) when (GetRetryAttempt()==Limit) { ... }` wrapper was
removed and the entire infra body (input resolution, ProcessAsync invoke, per-result
output writes, manifest assembly, outbound Pending seed, the Send, and the inbound
CAS flip — lines 86-224) is now correctly at method-body indentation. No orphaned
braces, no double-nested blocks, no unreachable code, and the two business catches
around `ProcessAsync` (lines 130-141) survive intact at the correct level.

**4. No real bugs introduced by the deletions.** The consumer's 7-parameter primary
constructor (dropping `IOptions<RetryOptions> retryOptions`) is matched at every
call site: `DispatchTestKit.Build`, `CheckAndDropFacts.BuildWith`, both
`EntryStepDispatchScopeTests` direct `new(...)` sites, and the
`EntryStepDispatchRuntimeScopeTests` DI registration all drop the `Retry()` argument.
The unused `Messaging.Contracts.Configuration` import was removed from the consumer.
Test docstrings were rewritten to describe the retained Phase-31 dedup coverage
rather than the removed breaker, and the now-unused `DispatchTestKit.Retry()` and
`RetryContext<T>` helpers were deleted. `BreakerMetricsFacts` correctly drops the
`WorkflowCancelled` assertion and adjusts its tag-set count from 3 to 2.

**Build confirmation:** `dotnet build` of the `Orchestrator` project (transitively
`BaseProcessor.Core` + `Messaging.Contracts`) and of the `BaseApi.Tests` project both
returned **Build succeeded, 0 Error(s), 0 warnings** — the strongest available
guarantee that this deletion-revert dangles nothing at compile time.

All reviewed files meet quality standards. No issues found.

---

_Reviewed: 2026-06-05_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
