---
phase: 31-idempotent-execution-exactly-once-effect
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/Messaging.Contracts/IExecutionCorrelated.cs
  - src/Messaging.Contracts/EntryStepDispatch.cs
  - src/Messaging.Contracts/ExecutionResult.cs
  - src/Orchestrator/Dispatch/IStepDispatcher.cs
  - src/Orchestrator/Dispatch/StepDispatcher.cs
  - src/Orchestrator/Consumers/ResultConsumer.cs
  - src/Orchestrator/Scheduling/WorkflowFireJob.cs
  - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs
  - src/BaseConsole.Core/Messaging/InboundExecutionScopeConsumeFilter.cs
  - tests/BaseApi.Tests/Contracts/EntryStepDispatchTests.cs
  - tests/BaseApi.Tests/Contracts/ExecutionResultContractTests.cs
  - tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs
  - tests/BaseApi.Tests/Processor/DispatchOutputWriteFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchInputFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - tests/BaseApi.Tests/Processor/EntryStepDispatchScopeTests.cs
  - tests/BaseApi.Tests/Processor/EntryStepDispatchRuntimeScopeTests.cs
  - tests/BaseApi.Tests/Console/ConsoleExecutionScopeFilterTests.cs
  - tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs
  - tests/BaseApi.Tests/Orchestrator/ResultConsumeTests.cs
autonomous: true
requirements: [req-1, req-2]
must_haves:
  truths:
    - "EntryId is a string (lowercase 64-hex content address) on both wire contracts and IExecutionCorrelated"
    - "EntryStepDispatch and ExecutionResult carry a string H field (default empty)"
    - "The whole solution compiles green after the type change (production + ~12 test files)"
    - "The dispatcher signature and the execution-scope filter guard handle string EntryId"
  artifacts:
    - path: "src/Messaging.Contracts/IExecutionCorrelated.cs"
      provides: "string EntryId on the correlated interface"
      contains: "string EntryId"
    - path: "src/Messaging.Contracts/EntryStepDispatch.cs"
      provides: "string EntryId + string H init props"
      contains: "string H"
    - path: "src/Messaging.Contracts/ExecutionResult.cs"
      provides: "string EntryId + string H init props"
      contains: "string H"
  key_links:
    - from: "src/Orchestrator/Dispatch/StepDispatcher.cs"
      to: "EntryStepDispatch.EntryId"
      via: "string entryId param assignment"
      pattern: "EntryId = entryId"
    - from: "src/BaseConsole.Core/Messaging/InboundExecutionScopeConsumeFilter.cs"
      to: "IExecutionCorrelated.EntryId"
      via: "string null/empty guard"
      pattern: "IsNullOrEmpty"
---

<objective>
Land the `EntryId` Guid->string type change and the new `H` field across the 3 wire-contract files AND every production + test site that names `EntryId` or the `EntryId == Guid.Empty` sentinel — as ONE compile-green wave. This is mechanical-but-wide (RESEARCH Pitfall 1, HIGH risk): if a contract changes but a ripple site (`StepDispatcher`, the scope-filter guard, ~12 test files) is left for "later", the tree will not build. No protocol logic changes here — every site keeps its CURRENT behavior, just re-typed; `H`/`EntryId` are threaded as empty/placeholder values that Plans 03/04 then populate with real hashes.

Purpose: a green tree is the precondition for the receiver rework. Doing the type ripple in isolation makes Plans 03/04/05 pure logic changes against a stable contract.

Output: string `EntryId` + `H` everywhere; solution builds 0/0; full hermetic suite green (assertions updated to string).

Note (scope/context cost): this plan's 21 files_modified intentionally exceeds the 15-file soft threshold. The consolidation is deliberate and non-splittable per RESEARCH Pitfall 1 — the `Guid->string` EntryId ripple MUST land compile-green in ONE wave (any contract change with a ripple site left for "later" breaks the build). The elevated context cost is the price of an atomic, mechanical-but-wide type change; no task restructuring is warranted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-SPEC.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-CONTEXT.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-RESEARCH.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-PATTERNS.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-01-SUMMARY.md

<interfaces>
<!-- Current shapes (verified) — change Guid EntryId -> string; add string H. -->

src/Messaging.Contracts/IExecutionCorrelated.cs:15 — `Guid EntryId { get; }` -> `string EntryId { get; }`
src/Messaging.Contracts/EntryStepDispatch.cs:12-14:
```csharp
public Guid CorrelationId { get; init; }
public Guid ExecutionId  { get; init; } = Guid.Empty;
public Guid EntryId      { get; init; } = Guid.Empty;   // -> string EntryId { get; init; } = "";  + add  public string H { get; init; } = "";
```
src/Messaging.Contracts/ExecutionResult.cs:13-15:
```csharp
public Guid CorrelationId { get; init; }
public Guid ExecutionId { get; init; }
public Guid EntryId { get; init; }   // -> string EntryId { get; init; } = "";  + add  public string H { get; init; } = "";
```
src/Orchestrator/Dispatch/IStepDispatcher.cs:21-22 + StepDispatcher.cs:15,22 — `Guid entryId` param -> `string entryId`; `EntryId = entryId` assignment unchanged (now string).
src/Orchestrator/Scheduling/WorkflowFireJob.cs:82-84 — currently passes `Guid.Empty` for entryId. THIS PLAN: pass `""` (placeholder; Plan 04 replaces with `MessageIdentity.EntryEntryId(correlationId, entryStepId)`). Keep the change minimal here.
src/Orchestrator/Consumers/ResultConsumer.cs:68 — passes `m.EntryId` through (type flows automatically once contract is string).
src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs:74 — `if (dispatch.EntryId == Guid.Empty)`. THIS PLAN: change the guard to `string.IsNullOrEmpty(dispatch.EntryId)` to preserve EXACTLY today's behavior (Plan 03 then removes the branch entirely and moves to InputDefinition==null). :91 `ExecutionData(dispatch.EntryId)` now takes string. :153/:171 `newEntryId = NewId.NextGuid()` stays a Guid LOCALLY this plan but must feed `ExecutionData(string)` — convert with `.ToString("D")` as a TEMPORARY shim (Plan 03 replaces with `MessageIdentity.HashBlob`). :264/:273 BuildFailed/BuildCancelled `EntryId = Guid.Empty` -> `EntryId = ""`.
src/BaseConsole.Core/Messaging/InboundExecutionScopeConsumeFilter.cs:33 — `if (ec.EntryId != Guid.Empty) state[...] = ec.EntryId.ToString();` -> `if (!string.IsNullOrEmpty(ec.EntryId)) state[...] = ec.EntryId;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-type the 3 contracts + add H; ripple through production sites (compile-green)</name>
  <read_first>
    - src/Messaging.Contracts/IExecutionCorrelated.cs, EntryStepDispatch.cs, ExecutionResult.cs (the three contract files in full)
    - src/Orchestrator/Dispatch/IStepDispatcher.cs, StepDispatcher.cs (signature + assignment)
    - src/Orchestrator/Scheduling/WorkflowFireJob.cs (lines 82-84 — the Guid.Empty dispatch)
    - src/Orchestrator/Consumers/ResultConsumer.cs (line 68 — EntryId pass-through)
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs (lines 74-111, 142-201, 251-275 — the guard + write + builders)
    - src/BaseConsole.Core/Messaging/InboundExecutionScopeConsumeFilter.cs (line 33 — the != Guid.Empty guard)
    - 31-RESEARCH.md § Pitfall 1 (the FULL production-side inventory with line numbers) + § Open Questions Q1 (Failed/Cancelled sentinel)
    - 31-CONTEXT.md D-01, D-02 (the type change + H field; the Guid.Empty sentinel removal is staged here as IsNullOrEmpty, fully removed in Plan 03)
  </read_first>
  <files>src/Messaging.Contracts/IExecutionCorrelated.cs, src/Messaging.Contracts/EntryStepDispatch.cs, src/Messaging.Contracts/ExecutionResult.cs, src/Orchestrator/Dispatch/IStepDispatcher.cs, src/Orchestrator/Dispatch/StepDispatcher.cs, src/Orchestrator/Consumers/ResultConsumer.cs, src/Orchestrator/Scheduling/WorkflowFireJob.cs, src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs, src/BaseConsole.Core/Messaging/InboundExecutionScopeConsumeFilter.cs</files>
  <action>
Apply the MINIMAL re-typing — behavior is UNCHANGED, only types. Pin the Failed/Cancelled "no output" sentinel as `""` (empty string) per Open Q1 decision (a Failed/Cancelled result short-circuits before any manifest read; `""` mirrors the old `Guid.Empty` and is never used as a content key).

1. `IExecutionCorrelated.cs`: `Guid EntryId { get; }` -> `string EntryId { get; }`. (Do NOT add `H` to the interface — both concrete contracts carry it directly; keeps the interface minimal, RESEARCH/PATTERNS note.)

2. `EntryStepDispatch.cs`: `public Guid EntryId { get; init; } = Guid.Empty;` -> `public string EntryId { get; init; } = "";` and ADD `public string H { get; init; } = "";`. Update the class XML doc to drop the "entryId Guid.Empty" wording.

3. `ExecutionResult.cs`: `public Guid EntryId { get; init; }` -> `public string EntryId { get; init; } = "";` and ADD `public string H { get; init; } = "";`.

4. `IStepDispatcher.cs` + `StepDispatcher.cs`: change the `Guid entryId` parameter to `string entryId` in both the interface and impl signatures. The `EntryId = entryId` assignment is now string-to-string (unchanged). (The `flag[H]=Pending` pre-write is NOT added here — Plan 04 owns it.)

5. `WorkflowFireJob.cs:82-84`: change the dispatch call's entryId argument from `Guid.Empty` to `""` (placeholder; Plan 04 replaces with the real `EntryEntryId` hash + threads H). Leave everything else identical.

6. `ResultConsumer.cs:68`: `m.EntryId` flows through unchanged (now string). No logic change.

7. `EntryStepDispatchConsumer.cs`:
   - `:74` change `if (dispatch.EntryId == Guid.Empty)` to `if (string.IsNullOrEmpty(dispatch.EntryId))` — PRESERVES today's exact branch behavior (Plan 03 removes this branch and switches to InputDefinition==null).
   - `:153` keep `var newEntryId = NewId.NextGuid();` but where it feeds `L2ProjectionKeys.ExecutionData(...)` (`:171`) pass `newEntryId.ToString("D")` as a TEMPORARY string shim so it compiles against `ExecutionData(string)`. Where `BuildCompleted(dispatch, executionId, newEntryId)` is called, change `BuildCompleted` to take `string newEntryId` and pass `newEntryId.ToString("D")`. (Plan 03 replaces the mint with `MessageIdentity.HashBlob`.)
   - `BuildCompleted` signature: `Guid newEntryId` -> `string newEntryId`; `EntryId = newEntryId` (now string).
   - `BuildFailed`/`BuildCancelled` `:264/:273`: `EntryId = Guid.Empty` -> `EntryId = ""`.
   - The nested `BeginScope` `[ExecutionLogScope.EntryId] = newEntryId.ToString()` becomes `= newEntryId.ToString("D")` if newEntryId is still a Guid local, or `= newEntryId` once it is the string param — keep it consistent with whatever `newEntryId` is at that point (it is the Guid local this plan).

8. `InboundExecutionScopeConsumeFilter.cs:33`: `if (ec.EntryId != Guid.Empty) state[ExecutionLogScope.EntryId] = ec.EntryId.ToString();` -> `if (!string.IsNullOrEmpty(ec.EntryId)) state[ExecutionLogScope.EntryId] = ec.EntryId;`. The other four guards (WorkflowId/StepId/ProcessorId/ExecutionId — still Guids) are UNCHANGED.

Do NOT touch retry sites, do NOT add flag I/O, do NOT add manifest logic — those are Plans 03/04/05.
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `IExecutionCorrelated.cs` contains `string EntryId { get; }` and NOT `Guid EntryId`
    - `EntryStepDispatch.cs` and `ExecutionResult.cs` each contain `public string EntryId` and `public string H`
    - `IStepDispatcher.cs` and `StepDispatcher.cs` signatures contain `string entryId` (grep: no `Guid entryId` param remains in these two files)
    - `EntryStepDispatchConsumer.cs` contains `string.IsNullOrEmpty(dispatch.EntryId)` and contains neither `EntryId == Guid.Empty` nor `EntryId = Guid.Empty`
    - `InboundExecutionScopeConsumeFilter.cs` contains `!string.IsNullOrEmpty(ec.EntryId)`
    - `dotnet build SK_P.sln -c Debug` exits 0 (0 errors)
  </acceptance_criteria>
  <done>Production tree compiles green with string EntryId + H everywhere; behavior is byte-equivalent to before (placeholder/shim values), ready for Plans 03/04 to inject real hashes.</done>
</task>

<task type="auto">
  <name>Task 2: Update the ~12 test files that assert EntryId as a Guid (compile + green)</name>
  <read_first>
    - 31-RESEARCH.md § Pitfall 1 (the full TEST-side inventory of ~12 files) + § Phase Requirements (req-2 acceptance: FireDispatch Guid.Empty -> non-empty assertion)
    - tests/BaseApi.Tests/Processor/DispatchTestKit.cs (line ~148 — `EntryId = entryId`, the shared kit other tests use)
    - tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs (the `Guid.Empty` EntryId-on-fire assertions)
    - tests/BaseApi.Tests/Console/ConsoleExecutionScopeFilterTests.cs (the `record ExecProbeMessage(... Guid EntryId)` — must re-type to string)
    - 31-VALIDATION.md § Wave 0 Requirements (final bullet — these updates are part of the Guid->string wave)
  </read_first>
  <files>tests/BaseApi.Tests/Contracts/EntryStepDispatchTests.cs, tests/BaseApi.Tests/Contracts/ExecutionResultContractTests.cs, tests/BaseApi.Tests/Orchestrator/FireDispatchTests.cs, tests/BaseApi.Tests/Processor/DispatchOutputWriteFacts.cs, tests/BaseApi.Tests/Processor/DispatchInputFacts.cs, tests/BaseApi.Tests/Processor/DispatchTestKit.cs, tests/BaseApi.Tests/Processor/EntryStepDispatchScopeTests.cs, tests/BaseApi.Tests/Processor/EntryStepDispatchRuntimeScopeTests.cs, tests/BaseApi.Tests/Console/ConsoleExecutionScopeFilterTests.cs, tests/BaseApi.Tests/Orchestrator/StopConsumerLifecycleTests.cs, tests/BaseApi.Tests/Orchestrator/ResultConsumeTests.cs</files>
  <action>
For each test file, replace `Guid` EntryId usage with the string equivalent, preserving the test's INTENT:
- Where a test built/asserted `EntryId = Guid.Empty` for the "no input / source step" case, use `EntryId = ""` (the new empty sentinel) UNLESS it is a FireDispatch entry-step assertion (see below).
- Where a test constructed a real `EntryId = someGuid`, use `EntryId = someGuid.ToString("D")` (or a fixed 64-hex literal) so the value is a valid string content address.
- `DispatchTestKit.cs:~148` (`EntryId = entryId`): change the kit's `entryId` parameter type to `string` (default `""`) so every consumer of the kit gets the string shape.
- `ConsoleExecutionScopeFilterTests.cs`: the local `record ExecProbeMessage(... Guid EntryId ...)` implements `IExecutionCorrelated` — change its `EntryId` to `string` so it still satisfies the (now string) interface; update the assertion that the scope skips empty EntryId to use `""` instead of `Guid.Empty` and assert a non-empty string value flows when set.
- `FireDispatchTests.cs`: the entry-step dispatch assertions that today assert `EntryId == Guid.Empty` become — for THIS plan — `EntryId == ""` (WorkflowFireJob passes `""` this plan; Plan 04 flips them to the non-empty `EntryEntryId` hash assertion per req-2). Add an explicit `// Plan 04 changes this to the non-empty hash (req-2)` comment so the next executor finds it.

Run the hermetic suite; fix any residual `Guid`/string mismatch until green. Do NOT add new behavioral tests here (the new behavior tests are Wave-0 files in Plans 03/04/05).
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build tests/BaseApi.Tests -c Debug` exits 0 (all ~12 files compile against the string contracts)
    - Grep across the listed test files: no remaining `EntryId = Guid.Empty` and no `Guid EntryId` member in `ExecProbeMessage`
    - `DispatchTestKit.cs` exposes a `string` entryId parameter (default `""`)
    - `FireDispatchTests.cs` contains the `Plan 04 changes this` marker comment
    - `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` exits 0 (full hermetic + integration suite green, zero regression)
  </acceptance_criteria>
  <done>Every EntryId-as-Guid test assertion is re-typed to string; the full hermetic suite is green, confirming the type ripple introduced no behavioral regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/process wire (existing) | The `EntryStepDispatch`/`ExecutionResult` envelopes cross the MassTransit/RabbitMQ boundary between orchestrator and processor. No NEW boundary — the field types change, the transport/lifecycle do not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | EntryId as a Redis key segment (now free-ish string) | mitigate | EntryId is ALWAYS a server-derived 64-hex (or the reserved `""` sentinel) — never user free text; the `^[a-f0-9]{64}$` shape is enforced by the Plan-01 helper and pinned by golden tests |
| T-31-05 | Information disclosure | EntryId in the execution-scope filter | mitigate | The filter places EntryId only as a scope VALUE under the fixed `ExecutionLogScope.EntryId` key, never interpolated into a template (T-18-04 precedent preserved through the Guid->string change) |
| T-31-06 | Tampering | mixed-version deployment (old Guid processor vs new string orchestrator) | accept | Both processes reference the single `Messaging.Contracts` leaf and are rebuilt/redeployed together (RESEARCH Runtime State Inventory — no rolling upgrade across the contract change) |
</threat_model>

<verification>
- `dotnet build SK_P.sln -c Debug` exits 0.
- `dotnet test tests/BaseApi.Tests -- --filter-not-trait "Category=RealStack"` green.
- Grep confirms no `Guid EntryId` / `EntryId == Guid.Empty` / `EntryId = Guid.Empty` survive in production or the listed tests.
</verification>

<success_criteria>
- `EntryId` is `string` and `H` exists on both contracts; the interface carries string `EntryId`.
- The whole solution compiles 0/0 and the full hermetic suite is green.
- All behavior is unchanged from before the type ripple (placeholder/shim values); Plans 03/04 inject the real hashes.
</success_criteria>

<output>
After completion, create `.planning/phases/31-idempotent-execution-exactly-once-effect/31-02-SUMMARY.md`.
</output>
</content>
