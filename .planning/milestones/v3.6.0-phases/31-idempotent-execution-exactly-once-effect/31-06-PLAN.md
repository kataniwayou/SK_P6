---
phase: 31-idempotent-execution-exactly-once-effect
plan: 06
type: execute
wave: 4
depends_on: [31-03, 31-04, 31-05]
files_modified:
  - tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs
  - scripts/phase-31-close.ps1
autonomous: false
requirements: [req-8]
user_setup:
  - service: docker-compose-stack
    why: "The req-8 real-stack proof requires the full v3.6.0 compose stack (Redis :6380, RabbitMQ :5673, Postgres :5433, OTLP/ES :4317, and a REBUILT processor-sample container running the new wire contract) up and healthy. A mixed-version deployment (old Guid-EntryId processor) mis-deserializes the new string-EntryId+H contract."
    dashboard_config:
      - task: "docker compose up -d --build processor-sample orchestrator baseapi-service (rebuild with the Plan 02 wire contract before the E2E)"
        location: "repo compose stack"
must_haves:
  truths:
    - "A real-stack E2E builds the merge topology (two entry steps feeding one successor) and runs across cron fires plus an induced retry/redelivery"
    - "Per CorrelationId, the ES downstream-effect set equals the expected per-fire set with ZERO duplicates, even with the induced retry"
    - "The close gate phase-31-close.ps1 holds 3-consecutive-GREEN + triple-SHA BEFORE==AFTER with skp:data:* (64-hex) and skp:flag:* covered by scan-clean teardown"
  artifacts:
    - path: "tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs"
      provides: "live exactly-once-effect proof under the merge topology + induced duplicate"
      contains: "Category"
    - path: "scripts/phase-31-close.ps1"
      provides: "close gate extending scan-clean to skp:flag:* + 64-hex skp:data:*"
      contains: "skp:flag"
  key_links:
    - from: "tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs"
      to: "Elasticsearch via PollEsForLog"
      via: "per-CorrelationId downstream-effect set assertion"
      pattern: "PollEsForLog"
    - from: "scripts/phase-31-close.ps1"
      to: "redis skp:flag:* + skp:data:*"
      via: "scan-clean teardown + triple-SHA"
      pattern: "skp:flag"
---

<objective>
Prove exactly-once-effect on the real stack (req-8) and ship the phase close gate. Clone `SampleRoundTripE2ETests` into `IdempotentExactlyOnceE2ETests`, diverge to build the MERGE topology test-side (two entry steps feeding ONE successor via `NextStepIds`), induce a duplicate (re-publish the same `EntryStepDispatch` and/or a throw-once test processor forcing an `Immediate(N)` re-run), and assert that per `CorrelationId` the ES downstream-effect set equals the expected per-fire set with ZERO duplicates — the inverse of the live `StepB4` x2. Then clone the prior close gate into `phase-31-close.ps1`, extending scan-clean/triple-SHA to the new `skp:flag:*` and 64-hex `skp:data:*` namespaces.

Purpose: this is the milestone's only end-to-end guarantee that the deterministic-identity + effect-first-dedup protocol actually collapses duplicates against real containers; the close gate makes the new key namespaces net-zero so the BEFORE==AFTER discipline holds.

Output: `IdempotentExactlyOnceE2ETests` (req-8) + `phase-31-close.ps1` (req-8 + D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-SPEC.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-CONTEXT.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-RESEARCH.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-PATTERNS.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-03-SUMMARY.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-04-SUMMARY.md
@.planning/phases/31-idempotent-execution-exactly-once-effect/31-05-SUMMARY.md

<interfaces>
<!-- The clone source + close-gate analog. -->

tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs — the clone target. Load-bearing pieces to copy:
- Genuine embedded SourceHash (GetCustomAttributes<AssemblyMetadataAttribute> "SourceHash" off Processor.Sample.dll; GET-or-create the Processor row on it).
- Truthful liveness gate (PollForHealthyLivenessAsync polls host Redis for the real container's skp:{procId:D} Healthy heartbeat before POST /start).
- PollEsForLog (term on attributes.WorkflowId / attributes.CorrelationId scoped by resource.attributes.service.name).
- ScanExecutionDataKeys (scans skp:data:* by prefix) + net-zero teardown (RealStackWebAppFactory.DisposeAsync + L2KeysToCleanup/ParentIndexMembersToSrem).
- [Trait("Category","RealStack")] gating; the * * * * * cron so the fire actually dispatches.

scripts/phase-29-close.ps1 (and phase-28-close.ps1) — the close-gate analog: $services full-stack health pre-flight (processor-sample REQUIRED healthy), the triple-SHA (psql \l + redis-cli --scan + rabbitmqctl list_queues) BEFORE==AFTER, the 3-consecutive-GREEN loop, the Release+Debug zero-warning build. Clone byte-faithful, update label strings 29->31, and extend the redis scan/teardown to skp:flag:*.

Decisions locked for this plan:
- Merge topology fixture (Open Q3): seed TWO entry steps feeding ONE successor (both predecessors' NextStepIds -> the merge step) — built test-side; there is NO named StepB4 fixture.
- Induced duplicate (D-11): re-publish the same EntryStepDispatch (simulated redelivery) and/or a throw-once processor. Broker-level fault injection rejected (heavier/flakier).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IdempotentExactlyOnceE2ETests — merge topology + induced duplicate, zero downstream dup (req-8)</name>
  <read_first>
    - tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs (the WHOLE file — the clone source: SourceHash reflection, liveness poll, PollEsForLog, ScanExecutionDataKeys, net-zero teardown, RealStackWebAppFactory)
    - tests/BaseApi.Tests/Orchestrator/CorrelationPropagationE2ETests.cs (the RealStackWebAppFactory override + teardown discipline it reuses)
    - src/BaseProcessor.Core/Processing/EntryStepDispatchConsumer.cs (Plan 03 — the new content-addressed/dedup behavior the E2E proves live)
    - src/Orchestrator/Consumers/ResultConsumer.cs (Plan 04 — the manifest fan-out + dedup the E2E proves)
    - 31-CONTEXT.md D-11 (induced-duplicate mechanism), D-12 (scan-clean both namespaces)
    - 31-RESEARCH.md § Open Q3 (merge topology construction), § Environment Availability, § Validation Tier 3
    - 31-PATTERNS.md Cluster 8 (the exact clone-then-diverge instructions)
  </read_first>
  <files>tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs</files>
  <behavior>
    - Seeds the merge topology: two entry steps (StepA1, StepA2) each with NextStepIds -> a single merge successor step (StepB), all in one workflow with a * * * * * cron
    - Registers the Processor row with the GENUINE embedded SourceHash; polls the real processor-sample skp:{procId:D} Healthy key before POST /start
    - Runs across cron fires AND induces a duplicate (re-publishes the same EntryStepDispatch, and/or uses a throw-once path forcing an Immediate(N) re-run)
    - Asserts per CorrelationId: the ES downstream-effect set for the merge step StepB equals the expected per-fire count with ZERO duplicates (the StepB4-x2 inverse) — even with the induced retry
    - Net-zero teardown: registers the run's skp:data:{64hex} AND skp:flag:{64hex} keys for deletion (D-12)
  </behavior>
  <action>
Create `tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs` (namespace `BaseApi.Tests.Orchestrator`, `[Trait("Category","RealStack")]`). Clone `SampleRoundTripE2ETests` wholesale, then diverge:

1. **Merge topology seed:** instead of one entry step, POST/seed TWO entry steps (`StepA1`, `StepA2`) whose `NextStepIds` BOTH point to a single successor step `StepB` (the merge step, `EntryCondition = Always`/4). The workflow lists both A-steps as entry steps. Use the existing Step/Workflow seeding helpers from the clone source.

2. **Genuine SourceHash + truthful liveness:** keep the clone's reflection of the embedded SourceHash off `Processor.Sample.dll`, the GET-or-create Processor row, and `PollForHealthyLivenessAsync` against the real `skp:{procId:D}` key. POST `/orchestration/start` only after the real heartbeat is fresh.

3. **Induce a duplicate (D-11):** after the first fire round-trips, re-publish (re-Send) the SAME `EntryStepDispatch` for the merge step (capture the dispatched message or reconstruct it with the SAME correlationId/workflowId/stepId/processorId/EntryId so its `H` is identical) to simulate broker redelivery. (Alternatively/additionally, if a throw-once test processor variant is available, use it to force an `Immediate(N)` re-run; if not readily available, the re-publish path alone satisfies D-11's "simulated broker redelivery".) Prefer the re-publish path — it is the lighter, less-flaky mechanism the CONTEXT chose over broker fault injection.

4. **Assert zero downstream duplication:** via `PollEsForLog`, term on `attributes.CorrelationId == {correlationId}` AND the merge step's downstream-effect marker (the `StepB` Completed/advance log, scoped by `resource.attributes.service.name`). Assert the StepB downstream effect appears EXACTLY the expected per-fire count (one execution per distinct merge-input EntryId; identical inputs collapse) and NOT twice-from-the-duplicate. This is the StepB4-x2 inverse: the duplicate `EntryStepDispatch` (same `H`) is dropped by the processor's effect-first `flag[H]` gate, so no extra StepB downstream effect is emitted.

5. **Net-zero teardown (D-12):** extend `ScanExecutionDataKeys` / the `L2KeysToCleanup` registration to ALSO scan + register `skp:flag:*` keys created by the run, so the triple-SHA BEFORE==AFTER holds. Add a parallel `skp:flag:*` scan mirroring the existing `skp:data:*` scan.

Generous poll budgets (cover container boot + up-to-60s next-minute fire + otel->ES ingest), mirroring the clone. Mark `[Trait("Category","RealStack")]` so the hermetic tier excludes it.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs` exists with `[Trait("Category","RealStack")]`
    - Seeds TWO entry steps whose NextStepIds both reference ONE merge successor step (grep: two entry-step seeds + a shared successor id)
    - Reflects the genuine embedded SourceHash (`GetCustomAttributes` / `AssemblyMetadataAttribute "SourceHash"`) and polls the real `skp:{procId:D}` liveness key
    - Re-publishes/re-sends the same `EntryStepDispatch` (same identity -> same H) to induce a duplicate
    - Uses `PollEsForLog` asserting the merge-step downstream effect appears the expected per-fire count with zero duplicates
    - Registers `skp:flag:*` (and 64-hex `skp:data:*`) keys for net-zero teardown
    - `dotnet build tests/BaseApi.Tests -c Debug` exits 0 (the test compiles; the live run is the human-verify gate in Task 3)
  </acceptance_criteria>
  <done>The real-stack exactly-once-effect proof is authored: merge topology + induced redelivery, asserting zero extra downstream execution via ES, with both new L2 namespaces in net-zero teardown.</done>
</task>

<task type="auto">
  <name>Task 2: phase-31-close.ps1 — clone the close gate; extend scan-clean to skp:flag:* + 64-hex skp:data:*</name>
  <read_first>
    - scripts/phase-29-close.ps1 (the most recent close gate — clone byte-faithful, update labels 29->31)
    - scripts/phase-28-close.ps1 (the processor-sample-in-$services precedent, in case 29 differs)
    - 31-CONTEXT.md D-12 (scan-clean both new namespaces; triple-SHA extends to them)
    - 31-RESEARCH.md § Pitfall 6 (close-gate triple-SHA must include the new namespaces)
    - 31-PATTERNS.md Cluster 8 close-gate note
  </read_first>
  <files>scripts/phase-31-close.ps1</files>
  <action>
Copy `scripts/phase-29-close.ps1` to `scripts/phase-31-close.ps1` byte-faithfully. Update ONLY:
1. Phase-identifying label strings `29` -> `31` (echo/title/comment lines that name the phase).
2. Confirm `$services` still includes `processor-sample` as REQUIRED healthy (it must — the contract change means the rebuilt processor-sample must be up and current; keep it in the pre-flight, NOT a health exception).
3. The redis scan/teardown: the triple-SHA already snapshots `redis-cli --scan` over ALL keys, so the new `skp:flag:*` + 64-hex `skp:data:*` are captured automatically in the BEFORE/AFTER SHA. Confirm there is NO `FLUSHDB` and NO prefix filter that would EXCLUDE `skp:flag:*` from the scan; if the gate scopes the scan to `skp:data:*` only, WIDEN it (or rely on the existing full `--scan`) so the flag namespace is in the SHA. The net-zero discipline is enforced by the E2E teardown (Task 1) registering both namespaces; the gate's job is to SNAPSHOT-and-compare.
4. Keep the 3-consecutive-GREEN loop, the Release+Debug zero-warning build, and the triple-SHA (psql \l + redis-cli --scan + rabbitmqctl list_queues) unchanged.
Write as UTF-8 WITHOUT BOM; ensure it parses (`pwsh -NoProfile -Command "[void][ScriptBlock]::Create((Get-Content -Raw scripts/phase-31-close.ps1))"` / ParseFile).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$t=$null;$e=$null;[System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'scripts/phase-31-close.ps1'),[ref]$t,[ref]$e); if($e.Count){$e|ForEach-Object{$_.Message};exit 1} else {'PARSE OK'}"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-31-close.ps1` exists, parses clean (ParseFile exit 0, no errors), and is BOM-free
    - Contains `phase-31` / `31` phase labels (grep: no stray `phase-29`/`Phase 29` label remains)
    - `$services` includes `processor-sample` (REQUIRED healthy)
    - Contains the triple-SHA (`psql`, `redis-cli --scan`, `rabbitmqctl list_queues`) and the 3-GREEN loop
    - Contains NO `FLUSHDB`; the redis `--scan` is NOT prefix-filtered to exclude `skp:flag:*` (grep: a `skp:flag` reference OR an unfiltered `redis-cli --scan`)
    - PARSE OK
  </acceptance_criteria>
  <done>The phase-31 close gate is authored: full-stack 3-GREEN + triple-SHA BEFORE==AFTER, covering the new skp:flag:* and 64-hex skp:data:* namespaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live exactly-once E2E + close-gate verification (operator-run)</name>
  <action>Operator-run gate (no code change): execute the live E2E and the close gate against the rebuilt full compose stack exactly as listed in <how-to-verify>, then report GATE_EXIT and the triple-SHA evidence per <resume-signal>.</action>
  <what-built>
    - `IdempotentExactlyOnceE2ETests` (merge topology + induced redelivery; asserts zero downstream duplication via ES).
    - `phase-31-close.ps1` (3-consecutive-GREEN + triple-SHA BEFORE==AFTER, processor-sample required healthy, skp:flag:*/skp:data:* in scan).
    Both compile/parse clean; the LIVE run requires the full v3.6.0 compose stack up with a REBUILT processor-sample (the wire contract is not backward-compatible).
  </what-built>
  <how-to-verify>
    1. Rebuild the contract-affected containers so the live stack runs the new string-EntryId+H contract:
       `docker compose up -d --build processor-sample orchestrator baseapi-service`
    2. Run the live exactly-once E2E:
       `dotnet test tests/BaseApi.Tests -- --filter-class "*IdempotentExactlyOnceE2ETests"`
       Expect: GREEN — per CorrelationId the merge-step downstream effect appears the expected per-fire count with NO extra execution despite the induced redelivery.
    3. Run the close gate against the live stack:
       `pwsh -NoProfile -File ./scripts/phase-31-close.ps1`
       Expect: GATE_EXIT=0 — 3-consecutive full-suite GREEN, triple-SHA BEFORE==AFTER all HELD (incl. the new skp:flag:* / 64-hex skp:data:* in the redis SHA), Release+Debug 0 Warning/0 Error.
    Note (project memory): the bg-task "exit 0" is the wrapper's, not the gate's — read GATE_*_EXIT from the gate output. The close gate's first full live run may surface a stale/flaky prior-phase ES assertion or a dirty-BEFORE redis SHA; treat a RED that is a prior-phase artifact per the close-gate-surfaces-stale-tests memory, not a current-phase bug.
  </how-to-verify>
  <resume-signal>Type "approved" with the GATE_EXIT=0 evidence (3xGREEN counts + the three BEFORE==AFTER SHAs), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live compose stack (existing) | The E2E drives the real orchestrator<->processor round-trip over RabbitMQ/Redis/Postgres/OTLP. Test-only re-publish induces a duplicate inside the existing boundary; no new external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-17 | Tampering | induced-duplicate test re-publish | accept | Test-only mechanism inside the test harness (D-11); re-publishes a message with the SAME server-derived identity — it exercises the dedup, it does not weaken it |
| T-31-18 | Information disclosure | ES log assertions on CorrelationId/H | mitigate | The E2E queries ES on ids that are already emitted only as scope VALUES under fixed keys (T-18-04); the test reads them, it does not introduce new template interpolation |
| T-31-19 | Denial of service | per-fire key accumulation | mitigate | L2 TTL outlives the slowest fire+redelivery (constraint); the close-gate net-zero teardown scan-cleans skp:data:*/skp:flag:* so keys do not accumulate across gate runs (D-12) |
</threat_model>

<verification>
- `dotnet build tests/BaseApi.Tests -c Debug` exits 0 (E2E compiles).
- `scripts/phase-31-close.ps1` ParseFile exit 0, BOM-free.
- LIVE (human-verify gate): `IdempotentExactlyOnceE2ETests` GREEN + `phase-31-close.ps1` GATE_EXIT=0 with triple-SHA BEFORE==AFTER.
</verification>

<success_criteria>
- The real-stack merge-topology + induced-redelivery E2E asserts zero downstream duplication per CorrelationId (req-8).
- The phase-31 close gate holds 3-GREEN + triple-SHA with the new namespaces covered (req-8 + D-12).
</success_criteria>

<output>
After completion, create `.planning/phases/31-idempotent-execution-exactly-once-effect/31-06-SUMMARY.md`.
</output>
