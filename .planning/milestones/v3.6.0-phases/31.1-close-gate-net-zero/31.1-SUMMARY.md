---
phase: "31.1"
name: close-gate-net-zero
parent: "31"
gap_closure: true
tracks: NET-ZERO-31
status: complete
completed: 2026-06-04
requirements-completed: [NET-ZERO-31, req-8b]
---

# Phase 31.1 — Close-Gate Redis Net-Zero (NET-ZERO-31) — SUMMARY

## Outcome

`phase-31-close.ps1` now **PASSES clean**:

- 3-GREEN cadence: Run 1/2/3 = **Passed 446 / Exit 0** each.
- psql `\l` SHA-256: **HELD** `b48ce78302d9dd8ca93e6a7e694c153dc46705ec9ab4458b31c6933ea2e33fef`
- redis `--scan` SHA-256: **HELD** `9f09db5bdca0fe49ffe44f70e0987d05181e3b81611776f3c5e2066d4981ac2c` (BEFORE == AFTER)
- rabbitmq `list_queues` SHA-256: **HELD** `2eca7621f0d9fb809b952895b6547b65d15a9589174af8a114f0b0b9a0c1f16a`
- Settle-drain: "redis keyspace returned to the BEFORE set."

req-8b (the close-gate triple-SHA clause deferred from Phase 31) is now satisfied.

## What changed

### Task 1 — Stop-workflow-in-teardown (5 RealStack E2E tests)
Each RealStack E2E test now `POST /api/v1/orchestration/stop` for its workflow after the round-trip
(best-effort; in the `finally` for OrchestrationLogs so it runs even on throw). This unschedules the
self-rescheduling Quartz job (`StopOrchestration` → `UnscheduleOnlyAsync`), so the workflow stops
firing and no longer mints a fresh per-fire `skp:flag:{H}` name every minute.
- `tests/BaseApi.Tests/Orchestrator/SampleRoundTripE2ETests.cs`
- `tests/BaseApi.Tests/Orchestrator/CorrelationPropagationE2ETests.cs`
- `tests/BaseApi.Tests/Orchestrator/MetricsRoundTripE2ETests.cs`
- `tests/BaseApi.Tests/Orchestrator/IdempotentExactlyOnceE2ETests.cs`
- `tests/BaseApi.Tests/Observability/OrchestrationLogsE2ETests.cs`

### Task 2 — Close-gate settle-drain
`scripts/phase-31-close.ps1` waits (≤330 s) for `skp:flag:*`/`skp:data:*` to drain back to the BEFORE
keyset before the AFTER snapshot. Leak detection preserved: a key that never drains still fails the SHA.

### Task 3 — One-time purge + live gate
Restarted `sk-orchestrator` (RAMJobStore in-memory; parent index `skp:` empty → hydration reschedules
nothing) to clear the leaked jobs from prior runs; verified churn stopped (flag count 0 after 85 s).

## Deviations / additional fixes (committed)

- `test(31): ResultAckTests keepTtl-overload-robust` — the Phase 31 `keepTtl` flip (`2a4837f`) binds the
  `StringSetAsync(...,bool,When,...)` overload where `When.Exists` is a `When` enum, not a
  `ValueCondition("XX")`. `ResultAckTests` asserted only the `ValueCondition` form and regressed; relaxed
  to accept either (mirrors `EffectFirstDedupFacts`). Caught by the gate's full-suite Run 1.
- **Container rebuild required:** the gate's `dotnet build` recomputes `Processor.Sample`'s embedded
  SourceHash from current source; the running processor-sample/orchestrator/baseapi-service containers
  must be rebuilt (`docker compose up -d --build`) to match, or the live liveness gate fails on a
  procId mismatch. (Operational note, not a code change.)

## Verification

`SK_P.sln -c Release` 0/0 · hermetic dedup facts 11/11 · ResultAckTests 6/6 · **phase-31-close.ps1 PASSED
(3×446 GREEN + triple-SHA all HELD).**
